---
title: Pi launch fnm-multishell race — resolve pi to the stable node-installation bin, not the racy per-shell multishell symlink
status: validation
source: "Captain (2026-06-20): `./spacedock pi --plugin-dir . -- --model z-ai/glm-5.2 --thinking xhigh` failed with `Error: Cannot find module '/Users/clkao/.local/state/fnm_multishells/65968_1781888446149/bin/pi'` (Node MODULE_NOT_FOUND). Root cause: spacedock's `exec.LookPath(\"pi\")` resolves to an fnm per-shell multishell symlink; between LookPath (Go, at launch) and Node's Module._resolveFilename (milliseconds later), fnm tore down the 65968_… multishell dir (that shell exited), so the absolute path Node was handed is dead. Reproduced: `node …/65968_…/bin/pi --version` works after the fact (fnm recreated/cleaned it) — the failure window is tiny but real. Not a Spacedock bug per se, but Spacedock is the victim and can be resilient."
score:
started: 2026-06-20T05:07:34Z
completed:
verdict:
worktree: .worktrees/spacedock-ensign-pi-launch-fnm-multishell-race
issue:
sprint:
sprint-readiness:
id: j7nhrmghyy0kmwtphd0fmq32
mod-block:
pr: "#416"
---

# Pi launch fnm-multishell race

## End value

`spacedock pi` launches reliably under fnm (and other per-shell Node-version shims) regardless of whether sibling shells are exiting. The `pi` binary is resolved to its **stable node-installation bin** (e.g. `~/.local/share/fnm/node-versions/<ver>/installation/bin/pi`), bypassing the racy per-shell multishell symlink layer, OR launched by bare name with a retry on `MODULE_NOT_FOUND`. No more launch failures from a stale/tearing-down multishell path.

## Problem — root cause already determined

`spacedock pi` (internal/cli/pi.go) launches via `execPiRuntimeOps.LookPath("pi")` → `exec.LookPath("pi")`, then `ops.Launch(argv)` where `argv[0]` is the resolved absolute path. Under fnm:

- `PATH` includes `~/.local/state/fnm_multishells/<pid>_<timestamp>/bin` — a per-shell symlink farm fnm creates on shell startup and tears down on shell exit. The `pi` entry there is a symlink to `../lib/node_modules/@earendil-works/pi-coding-agent/dist/cli.js` (relative), resolving through the node-installation.
- `exec.LookPath("pi")` returns that multishell absolute path (first `PATH` hit).
- Spacedock execs `node <multishell-path>`. Between `LookPath` (Go) and Node's `Module._resolveFilename` (child, milliseconds later), fnm can tear down the multishell dir (a sibling shell exited). The absolute path Node was handed is now dead → `Cannot find module '…/65968_…/bin/pi'` (`MODULE_NOT_FOUND`, `requireStack: []`).
- By the time anyone re-checks (`ls`, `node …/65968_…/bin/pi --version`), fnm has recreated or left the path, so it works — the failure window is tiny but real, and it strikes at launch.

Verified: `node /Users/clkao/.local/state/fnm_multishells/65968_1781888446149/bin/pi --version` returns `0.79.8` after the fact (the path resolved again). The stable install bin `~/.local/share/fnm/node-versions/v24.13.1/installation/bin/pi` exists and is NOT subject to teardown.

## Approach — ideation decision (finalized 2026-06-19)

**Mechanism chosen: (a) — resolve the fnm-multishell-symlinked `pi` through to the stable node-installation bin, bypassing the racy per-shell layer.** (b) bare-name exec is rejected: `execHost.Launch` already execs by resolving `argv[0]` via `exec.LookPath` at exec time, and the child inherits the same stale `PATH` that holds the tearing-down multishell dir — so bare-name still hits the dead path; (b) does not narrow the race. (c) catch `MODULE_NOT_FOUND` + retry is recorded as a DEFENSIVE BACKSTOP only (the child's Node error is already a process exit by the time Spacedock sees it; retry would need a fresh resolve-and-relaunch loop), DEFERRED past the (a) primary fix — not in this task's scope.

### Code-validated seam (correction to the dispatch's framing)

The dispatch's framing locates the bug at `execPiRuntimeOps.LookPath` (returns `exec.LookPath("pi")`). Reading the actual launch path corrects this: `runPi` builds `argv := []string{"pi", "--extension", …}` (argv[0] is the literal bare name `"pi"`) and calls `ops.Launch(argv)`; `execPiRuntimeOps.Launch` delegates to `execHost{}.Launch(argv, os.Environ())`; `execHost.Launch` does `bin, err := exec.LookPath(argv[0])` (the **stdlib**, not `piRuntimeOps.LookPath`) then `syscall.Exec(bin, argv, env)`. So the actual exec'd binary is chosen by **`execHost.Launch`'s stdlib `exec.LookPath(argv[0]="pi")`**, which BYPASSES `execPiRuntimeOps.LookPath` entirely. Therefore routing the resolution through `execPiRuntimeOps.LookPath` alone would NOT fix the launch (the banner/check would show the stable path, but `execHost.Launch` would re-resolve `"pi"` to the racy multishell path). The resolution must change **what `execHost.Launch` resolves as the exec'd `bin`**, which means setting `argv[0]` to the stable **absolute** path (so stdlib `exec.LookPath(<absolute path>)` returns it unchanged) OR supplying the stable bin to a pi-specific launch. The launch path in `runPi` is the correct seam; `execPiRuntimeOps.LookPath` is left as the raw `exec.LookPath` seam (unchanged) — it is the input the resolution consumes, not the fix site.

### shebang-argv spike (settles the argv[0] question — recorded 2026-06-19)

`syscall.Exec(bin, argv, env)` where `bin` is a `#!/usr/bin/env node` shebang script: the kernel transforms to `execve(node, [node, bin, argv[0], argv[1], …], env)`, so our `argv[0]` becomes the child Node's `process.argv[2]` (the first user arg), and `bin` becomes `process.argv[1]` (the script Node resolves via `Module._resolveFilename`). So setting `argv[0]` to the stable absolute path changes pi's `process.argv[2]` from `"pi"` to the stable path string — a potential pi parser regression. Spiked against the real stable bin `~/.local/share/fnm/node-versions/v24.13.1/installation/bin/pi`:
- `node <stable> pi --version` (today's shape, argv[0]="pi") → `0.79.8`, exit 0.
- `node <stable> <stable> --version` (fixed shape, argv[0]=stable path) → `0.79.8`, exit 0.
- `node <stable> <stable> --extension /no/such --skill /no/such -- --model z-ai/glm-5.2 --thinking xhigh` → arg-parse proceeds identically to the `"pi"` shape; the only errors are `Unknown option: --` and `Failed to load extension`, which are **shape-independent** (identical in the argv[0]="pi" shape) and are the SEPARATE `--` passthrough concern, out of scope here.

**Conclusion:** pi strips/ignores the leading `process.argv[2]` positional whatever it is ("pi" or a path string); setting `argv[0]` to the stable absolute path is SAFE for pi's parser. So the chosen design sets `argv[0]` to the stable path when the fnm-multishell pattern is detected (matches the dispatch's AC-1 framing literally), and leaves `argv[0]="pi"` for all non-multishell cases (no-regression guarantee).

### Finalized design

- New **pure helper** `resolveFnmMultishellPi(lookedUp string) (stable string, ok bool)` in `internal/cli/pi.go`:
  - if `!strings.Contains(lookedUp, "/fnm_multishells/")` → return `"", false` (non-fnm / direct installs unaffected).
  - `binDir := filepath.Dir(lookedUp)`; `stableDir, err := filepath.EvalSymlinks(binDir)`; on err → return `"", false` (fall back, never block the launch).
  - `stable := filepath.Join(stableDir, "pi")`; `os.Lstat(stable)` must succeed; on err → return `"", false`.
  - if `stable == lookedUp` → return `"", false` (already stable, no change).
  - return `stable, true`.
- `execPiRuntimeOps.LookPath`: **UNCHANGED** (`exec.LookPath`) — the raw seam the resolution consumes; routing it through the helper would let `execHost.Launch`'s stdlib re-resolve "pi" to the racy path, so it stays raw.
- `execHost.Launch`: **UNCHANGED** (shared with claude/codex via `hostOps`).
- `runPi`: immediately before `ops.Launch(argv)`, insert:
  ```go
  if lp, err := ops.LookPath("pi"); err == nil {
      if stable, ok := resolveFnmMultishellPi(lp); ok {
          argv[0] = stable
      }
  }
  ```
  On LookPath error or not-ok, `argv[0]` stays `"pi"` (current behavior, no regression). On ok, `argv[0]` becomes the stable absolute path; `execHost.Launch`'s `exec.LookPath(<absolute>)` returns it unchanged; `syscall.Exec(stable, argv, env)` → the kernel's shebang hands Node `process.argv[1]=stable` → `Module._resolveFilename` stats a path under the real, never-torn-down installation dir → the race is closed. `process.argv[2]` becomes the stable path string (spike-proven safe; see above).
- **Banner note (accepted, cosmetic):** `launchBanner` calls `ops.LookPath("pi")` and prints the resolved path; under fnm it will still show the multishell path while the launch uses the stable path. This is an informational mismatch only and is accepted to keep the change single-seamed (one resolution, in `runPi`). The banner does not affect launch correctness. (If a future pass wants banner/launch parity, route `launchBanner`'s `lookPath` through the helper — but that is a cosmetic nicety, NOT in this task's scope.)

### Generalization — nvm/volta (recorded; deferred — no race observed)

- **nvm:** PATH carries `~/.nvm/versions/node/v<ver>/bin` — a REAL directory (nvm only rewrites PATH on `nvm use`; the versions dir is not torn down per shell). No per-shell teardown race. **No action.**
- **volta:** bins under `~/.volta/bin` (real, stable) + shims; node version pinned via project `package.json`/volta config, not per-shell symlinks. No per-shell teardown race. **No action.**
- **fnm is the unique host** with a per-shell symlink-farm teardown (`fnm_multishells/<pid>_<ts>` is unlinked on shell exit). **First cut: fnm-only.** If a future shim with equivalent per-shell teardown appears, extend the pattern set in the helper (rename `resolveFnmMultishellPi` → `resolvePerShellShimPi`, add a pattern table) — recorded as the extension seam, not implemented now. The `"/fnm_multishells/"` substring check is the v1 detection; the helper's pure-function shape keeps the extension localized.

## Scope (finalized)

In scope:
- The `resolveFnmMultishellPi` pure helper (`internal/cli/pi.go`) + the `argv[0]` resolution block in `runPi` (the launch path in `runPi` — NOT `execPiRuntimeOps.LookPath`, per the code-validated seam correction above).
- Detect the fnm multishell pattern (`"/fnm_multishells/"` substring); resolve through to the stable `…/node-versions/<ver>/installation/bin/pi` via `filepath.EvalSymlinks` of the bin dir.
- Fallback to current behavior (argv[0]="pi") for non-matching paths or resolution failure (no regression on non-fnm setups or direct installs).
- A new `internal/cli/pi_launch_test.go` with a synthetic fnm symlink tree asserting the stable path is chosen (AC-1), the direct path is unchanged (AC-2), and the torn-down-parent fallback holds.
- nvm/volta: recorded as no-action (no per-shell teardown race observed; see Generalization) — deferred, not implemented.
- (c) catch `MODULE_NOT_FOUND` + retry: recorded as a deferred defensive backstop, NOT implemented in this task.

Out of scope:
- Changing fnm's teardown behavior (upstream, out of repo control).
- Routing `execPiRuntimeOps.LookPath` through the helper (would not fix the launch; see the seam correction) and `launchBanner` parity (cosmetic; banner note accepted).
- The (c) retry backstop.
- The safehouse-flag-parity work (`pi-safehouse-flag-parity`, sibling) — disjoint `runPi` region.
- Cross-host (claude/codex) — they don't go through this `pi` launch path.
- The `--` passthrough "Unknown option" pi-side error (spike-noted; separate concern).

## Acceptance criteria (finalized — entity-level; proof = behavior, never prose-grep)

**AC-1 — `spacedock pi` launches a fnm-multishell-symlinked `pi` via its stable node-installation bin.**
Property: when `ops.LookPath("pi")` returns a path under `*/fnm_multishells/*/bin/pi`, `runPi` passes the resolved stable install bin (`…/node-versions/<ver>/installation/bin/pi`, a path NOT under `fnm_multishells/`) as the launched `argv[0]` to `ops.Launch`, not the multishell path.
Verified by: a Go unit test (`TestResolveFnmMultishellPi_StableChosen` + a `runPi`-level `TestRunPi_LaunchArgv0_StableForMultishell`) that builds a synthetic fnm tree in `t.TempDir()` — `fnm_multishells/<pid>_<ts>` as a symlink to a synthetic `node-versions/<ver>/installation`, `…/bin/pi` as a relative symlink to the synthetic `lib/node_modules/.../dist/cli.js`; the fake `LookPath` returns the multishell path; the test asserts (a) `resolveFnmMultishellPi` returns the synthetic stable `…/installation/bin/pi` with `ok=true`, and (b) the fake `Launch`'s recorded `argv[0]` equals that stable path and does NOT contain `/fnm_multishells/`.

**AC-2 — Non-multishell `pi` resolution is unchanged (no regression).**
Property: when `ops.LookPath("pi")` returns a non-multishell path (direct install / `/usr/local/bin/pi`-style), the launched `argv[0]` is the literal `"pi"` exactly as today.
Verified by: a Go unit test (`TestResolveFnmMultishellPi_NonMultishell_Unchanged` + `TestRunPi_LaunchArgv0_UnchangedForDirect`) where the fake `LookPath` returns a path with no `/fnm_multishells/` substring; the test asserts `resolveFnmMultishellPi` returns `"", false` and the fake `Launch`'s `argv[0]` equals `"pi"`. The existing `internal/cli/pi_frontdoor_test.go` suite stays green (`go test ./internal/cli/`), confirming no front-door / doctor / check-only regression.

**AC-3 — The race window is closed end-to-end (determination: structural — CI race simulation impractical).**
Property: the path Node `Module._resolveFilename`s at child startup lives under a directory fnm never tears down, so a sibling shell exiting between Go's `LookPath` and the child's resolve cannot ENOENT the launched script.
Determination recorded: a TRUE teardown-during-launch race simulation is impractical in CI — it requires a sibling fnm shell to exit and fnm to `unlink` the `fnm_multishells/<pid>_<ts>` symlink inside the millisecond-wide window between Go's `execHost.Launch` `LookPath` and the child Node's `Module._resolveFilename`, non-deterministically, against a live fnm. AC-3 is therefore satisfied by **AC-1 + AC-2 + the structural no-teardown reasoning**: (i) the stable install bin's parent `~/.local/share/fnm/node-versions/<ver>/installation/bin` is a REAL directory (verified: not a symlink, not per-shell), and fnm's teardown only ever unlinks the per-shell `fnm_multishells/<pid>_<ts>` symlink (a sibling-shell artifact), never the shared `node-versions/` installation tree; (ii) with `argv[0]=stable`, `execHost.Launch`'s `exec.LookPath(<absolute stable>)` returns the stable path unchanged and `syscall.Exec(stable, …)` makes the kernel hand Node `process.argv[1]=stable` as the script; `Module._resolveFilename(stable)` stats a path in the never-torn-down installation dir → cannot ENOENT from a sibling-shell teardown. The dispatched fallback ("if a true race simulation is impractical in CI, AC-1+AC-2 + a reasoning argument that the stable path is never torn down suffices — record the determination") is invoked here. A `pi-live` **manual** smoke is recommended as a best-effort human verification (tear down the `fnm_multishells/<pid>_<ts>` symlink after Go's resolve but before re-invoking, confirm `spacedock pi --version` still launches via the stable path) but is NOT a CI gate.

## Test plan (finalized)

- **File:** new `internal/cli/pi_launch_test.go` (keeps the launch-resolution tests cohesive and separate from the front-door/doctor/`checkPiRuntime` tests in `pi_frontdoor_test.go`; reuses the existing `fakePiRuntimeOps` seam — no new fake).
- **AC-1 unit tests (fixture, on-disk synthetic tree — Go unit, ~30 LOC each, low cost):**
  - `TestResolveFnmMultishellPi_StableChosen`: in `t.TempDir()`, build `tmp/fnm_multishells/123_456` as a symlink to `tmp/stable-versions/v1.2.3/installation`; `…/installation/bin/pi` as a symlink to `../lib/node_modules/pkg/dist/cli.js` and create that target file; call `resolveFnmMultishellPi(tmp/fnm_multishells/123_456/bin/pi)` → assert returns `tmp/stable-versions/v1.2.3/installation/bin/pi`, `ok=true`. (Pure-function test; the helper takes only the looked-up string and uses `filepath.EvalSymlinks`/`os.Lstat` on the real FS against the synthetic tree.)
  - `TestRunPi_LaunchArgv0_StableForMultishell`: construct the synthetic tree as above; `fakePiRuntimeOps.LookPath` returns the multishell path; drive `runPi` with a minimal passthrough (`--version`) and a `fakeHost`/env that satisfies `piRuntimeLaunchReady`; assert the recorded `launchArgv[0]` equals the stable synthetic `…/installation/bin/pi` and does NOT contain `/fnm_multishells/`.
- **AC-2 unit tests (~15 LOC each, low cost):**
  - `TestResolveFnmMultishellPi_NonMultishell_Unchanged`: `resolveFnmMultishellPi("/usr/local/bin/pi")` → `"", false`; also `resolveFnmMultishellPi("/opt/bin/pi")` → `"", false`.
  - `TestRunPi_LaunchArgv0_UnchangedForDirect`: `fakePiRuntimeOps.LookPath` returns `/usr/local/bin/pi`; drive `runPi`; assert `launchArgv[0] == "pi"` (unchanged).
- **AC-2 regression gate:** `go test ./internal/cli/` (the existing `pi_frontdoor_test.go` suite) stays green — no `LookPath`/`Stat`/`checkPiRuntime` semantic change for non-multishell, so no rework of existing fixtures.
- **Edge test (~10 LOC):** `TestResolveFnmMultishellPi_TornDownParent_Fallback` — the multishell parent symlink is removed (simulating post-teardown) before the helper runs → `filepath.EvalSymlinks` errors → returns `"", false` (fall back, never block). This pins the "on resolution failure, leave argv[0]='pi'" guarantee.
- **AC-3:** structural — no CI test beyond AC-1/AC-2 (the determination above records why a live race sim is impractical). The `pi-live` **lane** is required for the manual smoke (this task touches the `internal/cli/pi.go` high-stakes front door), but the lane produces a human-verified smoke note, not an automated CI assertion. Validator gate accepts AC-1 + AC-2 green plus the structural reasoning recorded above (per the dispatch's fallback).
- **Lanes required:** `go test ./...`, `go test ./... -race`, `gofmt -w ./cmd ./internal` (the AGENTS.md expected commands); `pi-live` for the manual smoke (best-effort, non-gating).
- **Cost/complexity:** low. The change is one pure helper (~20 LOC) + one 5-line block in `runPi` + ~120 LOC of unit tests. No new dependency, no interface change, no fake change. The synthetic-tree tests use only stdlib `os`/`path/filepath`/`testing`.

## Region ownership & merge coordination

- **This task owns:** the `resolveFnmMultishellPi` pure helper + the `argv[0]` resolution block in `runPi` (`internal/cli/pi.go`). `execPiRuntimeOps.LookPath` is **not** changed (left as the raw seam). `execHost.Launch` is **not** changed (shared with claude/codex).
- **Sibling `pi-safehouse-flag-parity` (`qn`)** owns `parsePiFrontDoorArgs` / the `runPi` safehouse-wrap arm / `setPiHelp` — disjoint region. Both touch `runPi`; the safehouse-wrap arm is earlier in `runPi` (banner → argv build → `safehouse.Wrap`), the resolution block here is immediately before `ops.Launch(argv)` at the end. The two edits are non-overlapping line ranges; coordinate if both land in the same window (the merge is a trivial append of the two blocks, but the FO should sequence the two implementations to avoid a `runPi` rebase churn).

## Spike notes — durable evidence (recorded 2026-06-19, against the live fnm install)

- **Stable install bin exists and is not torn down:** `~/.local/share/fnm/node-versions/v24.13.1/installation/bin/pi` → symlink (`lrwxr-xr-x`, 63 chars) to `../lib/node_modules/@earendil-works/pi-coding-agent/dist/cli.js`. Parent `…/installation/bin` is a REAL directory (not a symlink). Verified `os.Lstat` → mode `Lrwxr-xr-x`.
- **Multishell chain:** `~/.local/state/fnm_multishells/<pid>_<ts>/bin/pi` → the same 63-char relative target. The per-shell `<pid>_<ts>` dir is a **symlink** to `~/.local/share/fnm/aliases/default` → `~/.local/share/fnm/node-versions/v24.13.1/installation`. fnm's teardown unlinks the `<pid>_<ts>` symlink (sibling-shell exit), killing `…/bin/pi`.
- **EvalSymlinks proof (Go, the mechanism's basis):** `filepath.EvalSymlinks(multishellBinDir)` → `~/.local/share/fnm/node-versions/v24.13.1/installation/bin` (the real stable bin dir); `filepath.Join(that, "pi")` → the stable install bin symlink. `filepath.EvalSymlinks` of both the multishell and the stable `pi` resolves to the SAME underlying `…/dist/cli.js`, confirming they are the same script via two routes — only the multishell route's parent is torn down.
- **shebang-argv proof (spike above):** `argv[0]` becomes Node's `process.argv[2]`; pi tolerates both `"pi"` and the stable path string there (both shapes → `0.79.8`, exit 0). The `--` "Unknown option" error is shape-independent (separate passthrough concern, out of scope).
- **No re-spike needed:** the race was reproduced by the captain (the `65968_…` MODULE_NOT_FOUND invocation) and post-hoc (`node …/65968_…/bin/pi --version` works after the fact). The stable-path mechanism is proven by the EvalSymlinks + Lstat evidence above and the shebang-argv spike. This matches the dispatch's "No spike needed" determination, with the argv[0] question added and settled.

## Q15 — cold-boot package workaround

Until this task lands, the 0223 Commander cold-boot package carries Q15: `export PATH="<stable node bin>:$PATH"` (e.g. `export PATH="$HOME/.local/share/fnm/node-versions/v24.13.1/installation/bin:$PATH"`) **ahead** of `spacedock pi`, so `exec.LookPath("pi")` resolves to the stable bin first and bypasses the multishell layer entirely. This workaround expires once `resolveFnmMultishellPi` ships in `spacedock`.

## Related

- `internal/cli/pi.go` (`execPiRuntimeOps.LookPath`, `runPi` launch, `execHost.Launch` via `internal/cli/host_exec.go`) — the source of truth.
- `internal/cli/host_exec.go` — `execHost.Launch`'s `exec.LookPath(argv[0])` + `syscall.Exec` (the actual exec'd-bin selection site; unchanged by this task).
- `internal/cli/pi_frontdoor_test.go` — `fakePiRuntimeOps` seam (reused by the new `pi_launch_test.go`); unchanged.
- `pi-safehouse-flag-parity` (`qn`, sibling) — disjoint region in `runPi`; coordinate the merge window.
- The captain's failing invocation: `./spacedock pi --plugin-dir . -- --model z-ai/glm-5.2 --thinking xhigh` → `MODULE_NOT_FOUND` on `65968_…/bin/pi`.
- The 0223 Commander cold-boot package — Q15 workaround above.

## Stage Report: ideation (2026-06-19)

- DONE: Picked mechanism **(a)** — resolve the fnm-multishell-symlinked `pi` to the stable node-installation bin. Rejected (b) bare-name exec (`execHost.Launch` already LookPaths `argv[0]` at exec time and the child inherits the stale PATH → same dead path; (b) does not narrow the race). Recorded (c) catch-MODULE_NOT_FOUND-retry as a deferred defensive backstop, NOT in scope.
- DONE: Code-validated the seam and **corrected the dispatch's framing**. The dispatch locates the bug at `execPiRuntimeOps.LookPath`; reading `runPi` → `execPiRuntimeOps.Launch` → `execHost{}.Launch` shows the actual exec'd binary is chosen by **`execHost.Launch`'s stdlib `exec.LookPath(argv[0]="pi")`**, which bypasses `execPiRuntimeOps.LookPath`. So routing the resolution through `execPiRuntimeOps.LookPath` alone would NOT fix the launch. The correct seam is the launch path in `runPi`: set `argv[0]` to the stable absolute path so stdlib `exec.LookPath(<absolute>)` returns it unchanged. `execPiRuntimeOps.LookPath` stays the raw seam (unchanged); `execHost.Launch` stays shared (unchanged).
- DONE: Spiked and settled the argv[0] question. `syscall.Exec` of a `#!/usr/bin/env node` shebang script makes our `argv[0]` the child Node's `process.argv[2]` (first user arg) and `bin` the `process.argv[1]` script. Live spike against the real stable bin: `node <stable> pi --version` → 0.79.8 (today's shape); `node <stable> <stable> --version` → 0.79.8 (fixed shape). pi tolerates a path string as the leading `process.argv[2]`; the `--` "Unknown option" error is shape-independent (separate passthrough concern, out of scope). Conclusion: setting `argv[0]` to the stable absolute path is SAFE for pi's parser; no-regression guarantee holds by leaving `argv[0]="pi"` for all non-multishell cases.
- DONE: Finalized the design — a pure helper `resolveFnmMultishellPi(lookedUp string) (stable string, ok bool)` (detect `"/fnm_multishells/"`; `filepath.EvalSymlinks` of the bin dir → `Join(…, "pi")`; `os.Lstat` guard; fall back to `"", false` on any miss or resolution failure) + a 5-line `runPi` block before `ops.Launch(argv)` that sets `argv[0]=stable` only when `ok`. No interface change, no fake change, no new dependency.
- DONE: Finalized 3 behavior-bound ACs. AC-1 (multishell → argv[0]=stable install bin, NOT under fnm_multishells), AC-2 (non-multishell → argv[0]="pi" unchanged + existing pi_frontdoor suite green), AC-3 (race closed: determination = structural; CI race-sim impractical, so AC-1+AC-2 + the no-teardown reasoning; the stable install bin's parent is a REAL dir fnm never tears down). Each AC states its test method.
- DONE: Finalized the test plan — new `internal/cli/pi_launch_test.go` reusing `fakePiRuntimeOps`; synthetic fnm symlink tree in `t.TempDir()` for AC-1; direct-path for AC-2; torn-down-parent fallback edge test; `go test ./... -race` + `gofmt` lanes; `pi-live` manual smoke (best-effort, non-gating). Cost/complexity: low (~20-LOC helper + 5-line runPi block + ~120 LOC tests).
- DONE: Generalization recorded — nvm (`~/.nvm/versions/node/v<ver>/bin`, real dir, not per-shell) and volta (`~/.volta/bin`, stable) exhibit NO per-shell teardown race; **no action**, deferred. fnm is the unique host with per-shell symlink-farm teardown; first cut is fnm-only, with the rename-to-`resolvePerShellShimPi` extension seam recorded.
- DONE: Region ownership recorded — owns the helper + the `runPi` argv[0] block; does NOT touch `execPiRuntimeOps.LookPath` or `execHost.Launch`. Sibling `pi-safehouse-flag-parity` (qn) is disjoint (owns the earlier safehouse-wrap arm in `runPi`); merge coordination note recorded.
- DONE: Spike notes committed as durable evidence — stable install bin `Lrwxr-xr-x` (real parent dir, not torn down); multishell `<pid>_<ts>` is a symlink to `aliases/default` → `node-versions/…/installation`; Go `filepath.EvalSymlinks(multishellBinDir)` → the real stable bin dir; both routes resolve to the same `…/dist/cli.js`. Matches the dispatch's "no spike needed" determination with the argv[0] question added and settled.
- DONE: Q15 cold-boot workaround recorded — `export PATH="<stable node bin>:$PATH"` ahead of `spacedock pi` until this lands.
- SKIPPED: Editing any product file (no `pi.go` edits) — ideation = design only; implementation is a separate stage, per the dispatch.
- SKIPPED: Routing `execPiRuntimeOps.LookPath`/`launchBanner` through the helper — would not fix the launch (banner/check only) and is cosmetic; accepted as a single-seam tradeoff.

### Summary

Ideation finalized. The entity body now carries the code-validated mechanism decision (a, with the dispatch's seam framing corrected to the `runPi` argv[0] block — `execHost.Launch`'s stdlib LookPath is the real exec'd-bin selector, not `execPiRuntimeOps.LookPath`), the shebang-argv spike proving setting `argv[0]` to the stable path is pi-parser-safe, 3 behavior-bound ACs with test methods, a finalized test plan (new `pi_launch_test.go`, synthetic fnm tree, low cost), the nvm/volta no-action determination, region ownership + merge coordination with the sibling `qn` task, spike evidence, and the Q15 workaround. No product files were edited (ideation = design only). Ready for the ideation gate.

### Open risks / questions for the gate

1. **argv[0] becomes pi's `process.argv[2]` as the stable path string.** Spiked safe (`pi --version` exits 0 in both shapes), but the spike used `--version`; the FO/validator should confirm the manual `pi-live` smoke (a real model invocation) launches cleanly with `argv[0]=stable` before declaring AC-1 end-to-end. If a real invocation reveals pi DOES choke on a path as `process.argv[2]`, the fallback design (keep `argv[0]="pi"`, put the resolution in `execPiRuntimeOps.Launch` with a separate exec'd-bin override) is recorded in the seam-correction section and is a small pivot — flag for implementation, not a gate blocker.
2. **Banner/launch path mismatch under fnm** is accepted (cosmetic); recorded for a future cosmetic pass.
3. **AC-3 is structural** (CI race-sim impractical) per the dispatch's explicit fallback; the validator gate accepts AC-1+AC-2 green + the no-teardown reasoning. A `pi-live` manual smoke is recommended but non-gating.

## Stage Report: implementation

- DONE: AC-1 — `spacedock pi` launches a fnm-multishell-symlinked `pi` via its stable node-installation bin. The new pure helper `resolveFnmMultishellPi` (internal/cli/pi.go) detects the `/fnm_multishells/` substring, `filepath.EvalSymlinks`s the bin dir to the stable node-installation bin dir (`…/node-versions/<ver>/installation/bin`, a REAL dir fnm never tears down), `os.Lstat`-guards `…/pi`, and returns the stable absolute path. In `runPi`, immediately before `ops.Launch(argv)`, a 5-line block sets `argv[0] = stable` only when the helper reports `ok`; `execHost.Launch`'s stdlib `exec.LookPath(<absolute>)` returns it unchanged and `syscall.Exec` hands Node `process.argv[1]=stable` as the script. Verified by `TestResolveFnmMultishellPi_StableChosen` (pure-function: synthetic fnm symlink tree in `t.TempDir()` → returns the synthetic `…/installation/bin/pi`, `ok=true`, NOT under `/fnm_multishells/`) and `TestRunPi_LaunchArgv0_StableForMultishell` (runPi-level: fake `LookPath` returns the multishell path → fake `Launch`'s recorded `argv[0]` equals the stable path and does NOT contain `/fnm_multishells/`).
- DONE: AC-2 — Non-multishell `pi` resolution is unchanged (no regression). When `ops.LookPath("pi")` returns a non-multishell path (e.g. `/usr/local/bin/pi`), `resolveFnmMultishellPi` returns `"", false` and `runPi` leaves `argv[0]="pi"` exactly as today. Verified by `TestResolveFnmMultishellPi_NonMultishell_Unchanged` (`/usr/local/bin/pi` + `/opt/bin/pi` → `"", false`) and `TestRunPi_LaunchArgv0_UnchangedForDirect` (fake `LookPath` → `/usr/local/bin/pi` → launched `argv[0] == "pi"`). The existing `internal/cli/pi_frontdoor_test.go` suite stays green (`go test ./internal/cli/` ok) — no `LookPath`/`Stat`/`checkPiRuntime` semantic change for non-multishell. `execPiRuntimeOps.LookPath` and `execHost.Launch` are UNCHANGED (no interface/fake/dependency change).
- DONE: AC-3 — The race window is closed structurally. Determination: a TRUE teardown-during-launch race simulation is impractical in CI (requires a sibling fnm shell to exit and fnm to `unlink` the `fnm_multishells/<pid>_<ts>` symlink inside the millisecond-wide window between Go's `execHost.Launch` `LookPath` and the child Node's `Module._resolveFilename`, non-deterministically, against a live fnm). AC-3 is satisfied by AC-1 + AC-2 + the structural no-teardown reasoning: with `argv[0]=stable`, `execHost.Launch`'s `exec.LookPath(<absolute stable>)` returns the stable path unchanged and `syscall.Exec(stable, …)` makes the kernel hand Node `process.argv[1]=stable` as the script; `Module._resolveFilename(stable)` stats a path under the real, never-torn-down `node-versions/<ver>/installation/` dir (fnm's teardown only ever unlinks the per-shell `fnm_multishells/<pid>_<ts>` symlink, a sibling-shell artifact, never the shared installation tree) → cannot ENOENT from a sibling-shell teardown. A `pi-live` manual smoke is recommended (tear down the `fnm_multishells/<pid>_<ts>` symlink after Go's resolve but before re-invoking, confirm `spacedock pi --version` still launches via the stable path) but is NOT a CI gate. Also pinned by `TestResolveFnmMultishellPi_TornDownParent_Fallback`: when the multishell parent symlink is removed before the helper runs, `EvalSymlinks` errors → `"", false` → `runPi` leaves `argv[0]="pi"` (fall back, never block the launch).
- DONE: Region ownership respected — owns the `resolveFnmMultishellPi` helper + the `runPi` argv[0] block ONLY; did NOT touch `execPiRuntimeOps.LookPath` (left as the raw seam) or `execHost.Launch` (shared with claude/codex). Sibling `pi-safehouse-flag-parity` (qn) edited `runPi`'s safehouse-wrap arm — disjoint line range; merge clean.

### Summary

Implementation landed. Worktree commit `15fa6366` on branch `spacedock-ensign/pi-launch-fnm-multishell-race` adds the `resolveFnmMultishellPi` pure helper + the 5-line `runPi` argv[0] resolution block in `internal/cli/pi.go` (no interface/fake/dependency change; `execPiRuntimeOps.LookPath` and `execHost.Launch` unchanged), and a new `internal/cli/pi_launch_test.go` reusing `fakePiRuntimeOps` with a synthetic fnm symlink tree for AC-1/AC-2 + the torn-down-parent fallback edge. Gates: `go test ./...` ok; `go test ./... -race` ok; `gofmt -w ./cmd ./internal` clean (pre-existing unrelated gofmt churn in three other files reverted, not part of this commit). AC-3 is structural (CI race-sim impractical) per the dispatch's fallback; AC-1 + AC-2 green + the no-teardown reasoning close the race, with a recommended-but-non-gating `pi-live` manual smoke.

### Open risks / questions

1. **argv[0] becomes pi's `process.argv[2]` as the stable path string.** Spiked safe for `--version` (ideation); the recommended `pi-live` manual smoke (a real model invocation) should confirm a real launch is clean with `argv[0]=stable` before declaring AC-1 fully end-to-end. Fallback design recorded in the ideation's seam-correction section (keep `argv[0]="pi"`, put the resolution in `execPiRuntimeOps.Launch` with a separate exec'd-bin override) — a small pivot if needed.
2. **Banner/launch path mismatch under fnm** is accepted (cosmetic); `launchBanner` still prints the multishell path while the launch uses the stable path. Recorded for a future cosmetic pass (route `launchBanner`'s `lookPath` through the helper) — NOT in this task's scope.
3. **AC-3 is structural** (CI race-sim impractical) per the dispatch's explicit fallback; the validator gate accepts AC-1+AC-2 green + the no-teardown reasoning. `pi-live` manual smoke is recommended but non-gating.

## Stage Report: implementation (feedback cycle 1 rework — REVISED)

- DONE: Reworked the fnm-multishell resolution to GATE it to the non-wrap path only (`if !wrap`), reverting the cycle-1 reorder's under-wrap resolution. Root cause (confirmed by a manual pi-live smoke under safehouse): the cycle-1 reorder stopped clobbering the safehouse wrap token (good — no more "Unknown options: --trust-workdir-config, --"), but the launch STILL failed — now with `Cannot find module '/Users/clkao/.local/share/fnm/node-versions/v24.13.1/installation/bin/pi'` (MODULE_NOT_FOUND). The fnm fix resolves the stable install bin as an ABSOLUTE path and sets the inner pi token to it; when safehouse wraps and execs the inner command, it runs INSIDE the sandbox, whose filesystem view does NOT include `~/.local/share/fnm/node-versions/...` (not the workdir, not added via `--safehouse-add-dirs`). Node's `Module._resolveFilename` on the absolute path fails → MODULE_NOT_FOUND. Verified: `node /Users/clkao/.local/share/fnm/node-versions/v24.13.1/installation/bin/pi --version` works OUTSIDE the sandbox (returns 0.79.8); the path is live unsandboxed, the sandbox can't see it.
- DONE: Chose option (i) — keep the resolution for the non-wrap path only. In `runPi` (`internal/cli/pi.go`), the `resolveFnmMultishellPi` block is now `if !wrap { … argv[0] = stable }`, placed before the `if wrap { … safehouse.Wrap(argv, extra) }` block. The unsandboxed launch gets the race fix (the stable path is visible outside the sandbox, no teardown); the sandboxed launch uses the BARE `"pi"` (parity with claude/codex, which pass the bare name `"claude"`/`"codex"` as `inner[0]` even when wrapped — `frontdoor.go:315,498`; safehouse execs the bare name via the sandbox's PATH, which safehouse preserves/constructs, so no absolute path → no sandbox-visibility failure). The `resolveFnmMultishellPi` helper is retained (still consumed by the non-wrap path); `execPiRuntimeOps.LookPath` and `execHost.Launch` stay UNCHANGED.
- DONE: AC-1 (revised) — UNSANDBOXED multishell pi → launched argv[0] = stable install bin (the race fix, valid where the path is visible). SANDBOXED (wrap=true) multishell pi → inner token is bare "pi" (parity with claude/codex; the race is a known shared limitation under safehouse). Pinned by `TestRunPi_LaunchArgv0_StableForMultishell` (no wrap, multishell → argv[0]=stable) and the NEW `TestRunPi_LaunchArgv0_BarePiUnderWrap` (wrap=true, multishell LookPath → launched argv[0]=="safehouse" AND inner token after "--" is bare "pi", NOT the stable path). The cycle-1 regression test `TestRunPi_LaunchArgv0_SafehouseWrapPreservesFfnResolution` (which asserted inner=stable under wrap — the bug) is REPLACED by `TestRunPi_LaunchArgv0_BarePiUnderWrap`.
- DONE: AC-2 (unchanged) — non-multishell → argv[0]="pi"; `TestRunPi_LaunchArgv0_UnchangedForDirect` stays green; the existing `pi_frontdoor_test.go` suite stays green.
- DONE: AC-3 (revised) — the race is closed for the UNSANDBOXED case (structural: stable path visible, no teardown). For the SANDBOXED case, the race is a known shared limitation recorded here (not closed; deferred to a future env-forwarding task that lets the sandbox see the stable path — the `qn`-deferred Decision 2: SPACEDOCK_BIN/PATH-through-sandbox forwarding, requires a `Launch(argv, env)` signature change).
- DONE: Adversarial re-check (both confirmed). (a) Removed the `!wrap` gate (resolution runs under wrap too → inner becomes the stable path, not "pi") → `TestRunPi_LaunchArgv0_BarePiUnderWrap` REDs with `inner argv[0]=<stable>, want bare "pi"`. (b) Inverted the gate to `if wrap` (unsandboxed resolution suppressed → argv[0] stays "pi") → `TestRunPi_LaunchArgv0_StableForMultishell` REDs with `launched argv[0]="pi", want stable`. Restored the correct `!wrap` gate → both GREEN.
- DONE: Gates — `go test ./...` ok; `go test ./... -race` ok; `gofmt` clean on the task files (`internal/cli/pi.go`, `internal/cli/pi_launch_test.go`). Pre-existing unrelated gofmt churn in three other files (`prose_function_routing_test.go`, `haiku_loop_spike_live_test.go`, `section_read.go` — this toolchain's gofmt normalizes some comment backticks/struct alignment on HEAD) reverted; not part of this commit.

### Summary

Feedback cycle 1 (REVISED) rework landed. Worktree commit `4ed26726` on branch `spacedock-ensign/pi-launch-fnm-multishell-race` gates the `resolveFnmMultishellPi` block in `runPi` to the non-wrap path only (`if !wrap`), placed before the safehouse wrap. The cycle-1 reorder had moved the resolution before the wrap (fixing the wrap-token clobber) but still set the inner pi token to the stable ABSOLUTE path under wrap, which the safehouse sandbox cannot see → MODULE_NOT_FOUND (manual pi-live smoke). Under wrap the inner token is now the bare `"pi"` (parity with claude/codex, `frontdoor.go:315,498`); the unsandboxed launch keeps the race fix. The cycle-1 regression test is replaced by `TestRunPi_LaunchArgv0_BarePiUnderWrap`; adversarial re-checks (a) remove-gate and (b) invert-gate both confirmed RED. The sandboxed-case race is recorded as a known shared limitation, deferred to a future env/PATH-forwarding task (`qn` Decision 2). Gates green.

### Open risks / questions

1. **The fnm-multishell race re-exists under safehouse.** This is a known, accepted, shared limitation (claude/codex have the same surface today; the race is rare). The real fix is env/PATH forwarding into the sandbox so the stable path is visible OR so safehouse's PATH resolution avoids the tearing-down multishell — the `qn`-deferred Decision 2 (`SPACEDOCK_BIN`-through-sandbox forwarding, requires a `Launch(argv, env)` signature change). Filed/note as a follow-up; NOT pulled into this cycle.
2. **Banner/launch path mismatch under fnm** is accepted (cosmetic); `launchBanner` still prints the multishell path while the unsandboxed launch uses the stable path. Recorded for a future cosmetic pass — NOT in this task's scope.
3. **AC-3 is split**: closed (structural) for the unsandboxed case; known-limitation (deferred) for the sandboxed case. A `pi-live` manual smoke under safehouse (the case that surfaced this rework) should confirm the bare-"pi" inner launches cleanly inside the sandbox; the unsandboxed smoke confirms the stable-path fix.

## Stage Report: implementation (feedback cycle 2 — env-forwarding pulled in; REAL fix)

- DONE: Reframe (captain-approved, smoke-verified). The cycle-1 `!wrap` gate (bare "pi" under wrap) STILL failed under safehouse+fnm: the sandbox preserves the inherited PATH, which carried a stale/tearing-down `fnm_multishells/<pid>_<ts>` dir → MODULE_NOT_FOUND on the multishell path. The captain pulled qn's deferred env-forwarding (Decision 2) into this task. The live smoke under the cycle-1-revised code with the approved B-i grant (`--add-dirs=installation/bin`) ALSO failed: MODULE_NOT_FOUND on the stable path `/Users/clkao/.local/share/fnm/node-versions/v24.13.1/installation/bin/pi`. Root cause reframed: under safehouse the real issue is filesystem VISIBILITY of pi's actual code, not only the multishell teardown. The stable `pi` is a symlink chain (`installation/bin/pi` → `installation/lib/node_modules/@earendil-works/pi-coding-agent/dist/cli.js`, and `@earendil-works/pi-coding-agent` is ITSELF an `npm link` symlink out to `~/git/pi-mono/packages/coding-agent`). The repo-root `.safehouse` profile grants only `~/git/spacedock`, so the sandbox sees NEITHER the multishell dir, the installation, NOR the dev-link target. Granting `installation/bin` (B-i) is insufficient — Node's `Module._resolveFilename` follows the symlink into `installation/lib` / the dev-link monorepo, which is invisible. Proven by an empirical grant ladder: (1) `installation/bin` → MODULE_NOT_FOUND on stable path; (2) `pi-mono/packages/coding-agent` (resolved package dir) → Node loads the script, then ERR_MODULE_NOT_FOUND on `cross-spawn` (hoisted to `pi-mono/node_modules`); (3) `~/git/pi-mono` (whole monorepo) → SMOKE PASSES (`0.79.8`, exit 0).
- DONE: A — resolve the stable path ALWAYS (dropped the `!wrap` gate). `resolveFnmMultishellPi` is unchanged; `runPi` now applies `argv[0] = stable` before the wrap, wrap or not. Under `!wrap` the stable path is visible (no sandbox) → deterministic; under `wrap` the stable path is the right input for the sandboxed grant (B). The `resolveFnmMultishellPi` helper, `execPiRuntimeOps.LookPath`, and `execHost.Launch` semantics are unchanged.
- DONE: B — make the sandbox see pi's actual code+deps. New pure helper `fnmStableSandboxDir(stable)`: `filepath.EvalSymlinks(stable)` (the REAL cli.js, resolving the symlink chain out of the installation), then walk up; grant the first ancestor that (a) contains a `node_modules` dir AND (b) is a workspace root (`package.json` with a `workspaces` key) — that is the dep-hoist root for the script. If no workspace root is found, grant the HIGHEST `node_modules`-bearing ancestor (the normal `npm i -g` install lands on `installation/lib`, which covers the pi package + all its deps). Returns `""` on failure → runPi omits the grant (never blocks). The grant is appended to the safehouse `extra` as `--add-dirs=<grant>` under wrap. (Captain's correction applied: the walk stops at the WORKSPACE root, NOT the first `node_modules` ancestor — the package's own `node_modules` holds only dev/test deps; the hoisted runtime deps live at the workspace root. Adversarial (c) confirms the first-ancestor bug REDs.)
- DONE: C — `Launch(argv, env)` signature change. `piRuntimeOps.Launch(argv []string)` → `Launch(argv []string, env []string)`; `execPiRuntimeOps.Launch(argv, env)` delegates `execHost{}.Launch(argv, env)`; `fakePiRuntimeOps.Launch(argv, env)` captures `launchedEnv`. `runPi` calls `ops.Launch(argv, launchEnv(os.Environ()))` and the wrap prefixes the safehouse extra with `launcherBinEnvPassFlags()` (`--env-pass SPACEDOCK_BIN`), mirroring claude/codex's `frontdoor.go:345` plumbing. `launchEnv`/`launcherBinEnvPassFlags`/`resolvedLauncherBin` are reused from `frontdoor.go` (same package); no new env machinery.
- DONE: AC-1 (revised) — multishell pi → launched argv[0]=stable install bin ALWAYS (wrap or not); the sandbox sees the stable path's resolved code+deps via `--safehouse-add-dirs <fnmStableSandboxDir>` (workspace root / node_modules root, NOT `installation/bin`). Pinned by `TestRunPi_LaunchArgv0_StableForMultishell` (no wrap, argv[0]=stable) and `TestRunPi_LaunchArgv0_StableUnderWrapWithAddDirs` (wrap, inner token=stable AND `--add-dirs=<grant>` present).
- DONE: AC-2 (unchanged) — non-multishell → argv[0]="pi"; `TestRunPi_LaunchArgv0_UnchangedForDirect` + `TestResolveFnmMultishellPi_NonMultishell_Unchanged` green; the existing `pi_frontdoor_test.go` suite green (two wrap-extra assertions updated to expect the `launcherBinEnvPassFlags()` prefix, reflecting AC-4's env-forwarding mirror).
- DONE: AC-3 (revised) — the race/visibility gap is closed for BOTH unsandboxed AND sandboxed. Unsandboxed: stable path visible (structural). Sandboxed: stable path's resolved code+deps granted via `--safehouse-add-dirs <fnmStableSandboxDir>`. The grant target is computed from the FULLY RESOLVED real script (not the symlink path), so it covers the symlink chain escaping the installation. `TestRunPi_LaunchArgv0_StableUnderWrapWithAddDirs` asserts the `--add-dirs=<grant>` token is present under wrap+multishell; `TestFnmStableSandboxDir_{NormalInstall,DevLinkWorkspaces}` pin the walk for both install shapes.
- DONE: AC-4 (new) — `Launch(argv, env)` signature change lands; `launchEnv`/`launcherBinEnvPassFlags()` env-pass plumbing mirrors `frontdoor.go`. Pinned by `TestRunPi_LaunchEnvForwarding` (env threaded, SPACEDOCK_BIN set on the launched env).
- DONE: Test updates — `TestRunPi_LaunchArgv0_BarePiUnderWrap` (cycle-1-revised) REPLACED by `TestRunPi_LaunchArgv0_StableUnderWrapWithAddDirs`; `TestRunPi_LaunchEnvForwarding` ADDED; `TestFnmStableSandboxDir_NormalInstall` + `TestFnmStableSandboxDir_DevLinkWorkspaces` ADDED; `TestRunPi_LaunchArgv0_StableForMultishell` (no wrap) STAYS; the two `pi_frontdoor_test.go` wrap-extra assertions updated for the env-pass prefix.
- DONE: Adversarial re-checks (all confirmed RED, then restored GREEN). (a) Drop the `--add-dirs` grant under wrap → `TestRunPi_LaunchArgv0_StableUnderWrapWithAddDirs` REDs (extra missing `--add-dirs=<grant>`). (b) Re-gate the resolution to `!wrap` → `TestRunPi_LaunchArgv0_StableUnderWrapWithAddDirs` REDs (inner argv[0]="pi", stable suppressed under wrap). (c) Break the walk to stop at the FIRST `node_modules` ancestor (the captain's rejected bug) → `TestFnmStableSandboxDir_DevLinkWorkspaces` REDs (returns `coding-agent`, want `pi-mono` workspace root).
- DONE: Gates — `go test ./...` ok; `go test ./... -race` ok; `gofmt -w internal/cli/pi.go internal/cli/pi_launch_test.go internal/cli/pi_frontdoor_test.go` clean. Pre-existing unrelated gofmt churn in three other files (`prose_function_routing_test.go`, `haiku_loop_spike_live_test.go`, `section_read.go` — this toolchain's gofmt normalizes comment backticks/struct alignment on HEAD) reverted; not part of this commit.
- DONE: Manual smoke (LOAD-BEARING this cycle). Worktree binary + real safehouse + fnm + dev-link (npm-link) pi install. `spacedock pi --plugin-dir . --safehouse -- --version` (forced wrap, AUTO-computed grant — no manual `--safehouse-add-dirs`):
  ```
  spacedock dev · launching pi as your first officer
  Workflow: docs/dev
  Sandbox: enabled (safehouse)
  pi is your first officer — ask it for the queue and next steps.
  0.79.8
  EXIT=0
  ```
  Before this cycle (cycle-1-revised `!wrap` gate + the approved `installation/bin` grant), the same smoke failed: `Error: Cannot find module '/Users/clkao/.local/share/fnm/node-versions/v24.13.1/installation/bin/pi' (MODULE_NOT_FOUND)`. The captain's original failing invocation (`spacedock pi --plugin-dir . -- --model z-ai/glm-5.2 --thinking xhigh`) is the same launch path; a full `--model` run is the captain's final gate (it consumes an agent session), but the `--version` smoke already proves Node resolved + loaded the pi script + hoisted deps INSIDE the sandbox — the MODULE_NOT_FOUND failure mode is closed.

### Summary

Feedback cycle 2 (REAL fix) landed. Worktree commit `a1690b5c` on branch `spacedock-ensign/pi-launch-fnm-multishell-race` resolves the stable fnm path ALWAYS (drops the cycle-1 `!wrap` gate), adds `fnmStableSandboxDir` (workspace-rooted `--safehouse-add-dirs` grant computed from `EvalSymlinks(stable)`, covering the symlink chain that escapes the fnm installation into a dev-link monorepo), and lands the `Launch(argv, env)` signature change mirroring claude/codex's `launchEnv`/`launcherBinEnvPassFlags()` env-forwarding plumbing. The captain-approved reframe: under safehouse the real issue is filesystem VISIBILITY of pi's actual code (the `.safehouse` profile grants only the workdir; the stable `pi` symlink chain escapes the installation), not only the multishell teardown. The grant target was corrected from the approved `installation/bin` to the workspace root (package.json `workspaces`) whose `node_modules` holds the hoisted runtime deps — proven by an empirical grant ladder and the captain's live-env verification (`cross-spawn` is hoisted to `pi-mono/node_modules`, not `coding-agent/node_modules`). The live smoke under real safehouse+fnm+dev-link now passes (`0.79.8`, exit 0) where cycle-1-revised failed with MODULE_NOT_FOUND. Tests: `StableUnderWrapWithAddDirs` (replaces `BarePiUnderWrap`), `LaunchEnvForwarding`, `FnmStableSandboxDir_{NormalInstall,DevLinkWorkspaces}` added; three adversarial re-checks confirmed RED. Gates green.

### Open risks / questions

1. **The grant is broader than `installation/bin`.** `fnmStableSandboxDir` grants the workspace root (dev-link: the whole monorepo, e.g. `~/git/pi-mono`; normal install: `installation/lib`). This is targeted to pi's actual code+dep tree (not the whole FS, not read-write across `/`), but it IS broader than the dispatch's original B-i. The captain approved this corrected target. If a future install shape scatters deps beyond a single workspace root (e.g. pnpm with isolated node_modules per package), the walk may need extension — recorded as the extension seam.
2. **`--model` full-session smoke is the captain's final gate.** The `--version` smoke proves the launch (Node resolves + loads pi + deps inside the sandbox); a real `--model z-ai/glm-5.2 --thinking xhigh` run consumes an agent session and is the captain's call to confirm end-to-end. The launch-path failure mode (MODULE_NOT_FOUND) is closed.
3. **Banner/launch path mismatch under fnm** remains accepted (cosmetic); `launchBanner` still prints the multishell path while the launch uses the stable path. Out of scope.
4. **Normal (non-dev-link) installs are covered by the walk's fallback** (highest `node_modules`-bearing ancestor = `installation/lib`), pinned by `TestFnmStableSandboxDir_NormalInstall`, but NOT live-smoke-tested here (the captain's env is dev-link). The unit test pins the shape.

## Stage Report: implementation (feedback cycle 2)

- DONE: A — resolve the stable path ALWAYS (dropped the `!wrap` gate); `argv[0]=stable` wrap or not. The cycle-1 `!wrap` gate (bare "pi" under wrap) still failed under safehouse+fnm: the sandbox preserves the inherited PATH, which carried a stale/tearing-down fnm_multishells dir → MODULE_NOT_FOUND on the multishell path.
- DONE: B — under wrap, grant the sandbox pi's actual code+dep tree via `--safehouse-add-dirs <fnmStableSandboxDir>`. `fnmStableSandboxDir` walks up from `filepath.EvalSymlinks(stable)` (the real cli.js) to the workspace root (`package.json` with `workspaces` key) whose `node_modules` holds the hoisted runtime deps; if no workspace root, the highest node_modules-bearing ancestor (normal `npm i -g` install lands on `installation/lib`). Dev-link lands on the monorepo root (e.g. `~/git/pi-mono`) — smoke-proven. The captain corrected the walk stop-signal: "first ancestor containing node_modules" stops one level too early for a dev-link env (`coding-agent/node_modules` has only dev/test deps, not `cross-spawn` which is hoisted to `pi-mono/node_modules`); the workspaces-root signal lands on `pi-mono`.
- DONE: C — `Launch(argv, env)` signature change (`piRuntimeOps` + `execPiRuntimeOps` + `fakePiRuntimeOps`), mirroring claude/codex's `launchEnv` + `launcherBinEnvPassFlags()` plumbing (frontdoor.go:345). `runPi` calls `ops.Launch(argv, launchEnv(os.Environ()))`; the wrap prefixes the safehouse extra with `launcherBinEnvPassFlags()` (`--env-pass SPACEDOCK_BIN`). This un-defers `qn`'s Decision 2 (SPACEDOCK_BIN-through-sandbox forwarding).
- DONE: reframed the problem (captain-approved, smoke-verified) — under safehouse the real issue is filesystem VISIBILITY of pi's actual code, not only the multishell teardown. The stable `pi` is a symlink chain that escapes the fnm installation (in a dev-link/npm-link install, out to a monorepo); the `.safehouse` profile grants only the workdir, so the sandbox sees neither the multishell dir, the installation, nor the dev-link target. The "race" framing was partly a misdiagnosis.
- DONE: smoke (load-bearing) — `spacedock pi --plugin-dir . --safehouse -- --version` under real safehouse+fnm+dev-link now prints `0.79.8`, exit 0 (was MODULE_NOT_FOUND on the stable path with the cycle-1 `!wrap` gate + `installation/bin` grant). The captain confirmed a full `spacedock pi --plugin-dir . -- --model z-ai/glm-5.2 --thinking xhigh` launch through the worktree binary succeeds.
- DONE: tests — `TestRunPi_LaunchArgv0_StableUnderWrapWithAddDirs` (replaces `BarePiUnderWrap`) asserts inner token=stable AND `--add-dirs=<grant>` present; `TestRunPi_LaunchEnvForwarding` asserts `Launch(argv, env)` + SPACEDOCK_BIN threaded; `TestFnmStableSandboxDir_{NormalInstall,DevLinkWorkspaces}` pin the walk for both install shapes.
- DONE: adversarial — (a) drop grant → reds; (b) re-gate `!wrap` → reds; (c) first-node_modules-ancestor walk (the ensign's original proposed stop-signal) → reds (lands on `coding-agent`, not `pi-mono`, cross-spawn invisible). All confirmed RED.
- DONE: gates — `go test ./...` ok; `go test ./... -race` ok; gofmt clean on task files.

### Summary

Cycle-2 fix landed (worktree commit `a1690b5c` on branch `spacedock-ensign/pi-launch-fnm-multishell-race`). The coupled fix: resolve stable ALWAYS (A) + workspaces-rooted sandbox grant (B, corrected by captain) + `Launch(argv, env)` env-forwarding (C, un-defers qn Decision 2). Manual smoke under safehouse+fnm+dev-link passes (was MODULE_NOT_FOUND). All gates green. The reframe — visibility of pi's actual code+deps under safehouse, not just the multishell race — is the correct mental model; the workspaces-rooted walk is the grant that matches Node's dep-resolution boundary. Open risk: the grant is broader than `installation/bin` (grants a monorepo / `node_modules` tree) but targeted to pi's actual code+deps, not the whole FS; a future hardening could narrow it further if safehouse gains per-dep grants.

## Stage Report: validation

Fresh-validator verification of cycle-2 (worktree commit `a1690b5c` on branch `spacedock-ensign/pi-launch-fnm-multishell-race`). Verified against the actual diff + live behavior; the implementation report was not trusted on faith.

- AC-1 (revised) — PASSED. `TestRunPi_LaunchArgv0_StableUnderWrapWithAddDirs` asserts ALL THREE required properties: (a) `launched[0]=="safehouse"` (wrap fired), (b) the inner token after `--` equals the stable install bin path (NOT bare `"pi"`, NOT a `/fnm_multishells/` path — the test also asserts `!strings.Contains(inner[0], "/fnm_multishells/")`), and (c) the safehouse extra contains `--add-dirs=<grant>` (a loop over the pre-`--` tokens matches `--add-dirs=`+wantGrant). `TestRunPi_LaunchArgv0_StableForMultishell` (no wrap) pins `argv[0]=stable`. Confirmed green: `go test ./internal/cli/ -run 'TestRunPi_LaunchArgv0_StableUnderWrapWithAddDirs|TestRunPi_LaunchArgv0_StableForMultishell' -count=1` → PASS. The resolution block in `runPi` applies ALWAYS (no `!wrap` gate); `argv[0]=stable` is set before the wrap, and `fnmSandboxDir` is computed and attached under wrap.
- AC-2 (unchanged) — PASSED. Non-multishell → `argv[0]="pi"`: `TestRunPi_LaunchArgv0_UnchangedForDirect` + `TestResolveFnmMultishellPi_NonMultishell_Unchanged` green. The existing `internal/cli/pi_frontdoor_test.go` suite green (`go test ./internal/cli/ -run TestPiFrontDoor -count=1` → ok). Two wrap-extra assertions in `pi_frontdoor_test.go` were updated to expect the `launcherBinEnvPassFlags()` prefix (`--env-pass SPACEDOCK_BIN`), correctly reflecting AC-4's env-forwarding mirror — verified the assertions match the new wrap composition.
- AC-3 (revised) — PASSED. Race/visibility gap closed for BOTH unsandboxed and sandboxed. Unsandboxed: stable path visible (structural — `resolveFnmMultishellPi` returns the `installation/bin/pi` path whose parent is a REAL dir fnm never tears down). Sandboxed: stable path's resolved code+deps granted via `--safehouse-add-dirs <fnmStableSandboxDir>`. The walk stop-signal IS the WORKSPACE ROOT (`package.json` with `workspaces` key), NOT the first `node_modules` ancestor — pinned by `TestFnmStableSandboxDir_DevLinkWorkspaces` (lands on `pi-mono`, not `coding-agent`) and `TestFnmStableSandboxDir_NormalInstall` (lands on `installation/lib`). Adversarial (c) below confirms the first-ancestor bug REDs. The grant is computed from `filepath.EvalSymlinks(stable)` (the FULLY RESOLVED real cli.js), so it covers the symlink chain escaping the installation into a dev-link monorepo.
- AC-4 (new) — PASSED. `Launch(argv []string, env []string)` signature change lands across `piRuntimeOps` (interface), `execPiRuntimeOps` (delegates `execHost{}.Launch(argv, env)` — verified it passes the threaded `env`, NOT `os.Environ()`), and `fakePiRuntimeOps` (captures `launchedEnv`). `runPi` calls `ops.Launch(argv, launchEnv(os.Environ()))`; the wrap prefixes the safehouse extra with `launcherBinEnvPassFlags()` (`--env-pass SPACEDOCK_BIN`), mirroring `frontdoor.go:345`'s `append(launcherBinEnvPassFlags(), extra...)`. `TestRunPi_LaunchEnvForwarding` asserts `launchedEnv` is non-empty AND `SPACEDOCK_BIN` is set (non-empty) on the launched env. Confirmed green.

### Adversarial audit (detached — all confirmed RED, then restored GREEN)

- (a) Drop the `--add-dirs` grant under wrap (replaced the `if fnmSandboxDir != "" { append }` block with a no-op) → `TestRunPi_LaunchArgv0_StableUnderWrapWithAddDirs` RED with `safehouse extra missing "--add-dirs=…/installation/lib"`. CAUGHT by the test. Restored → GREEN.
- (b) Re-gate the resolution to `!wrap` (wrapped the resolution block in `if !wrap { … }`, reproducing the cycle-1-revised gate) → `TestRunPi_LaunchArgv0_StableUnderWrapWithAddDirs` RED with `inner argv[0]="pi", want stable "…/installation/bin/pi"`. CAUGHT by the test (the non-wrap `StableForMultishell` test stayed green, correctly isolating the under-wrap regression). Restored → GREEN.
- (c) Revert the walk stop-signal to "first node_modules ancestor" (the ensign's original proposed signal the captain corrected) — mutated `fnmStableSandboxDir` to `return dir` on the first `node_modules`-bearing ancestor, skipping the `isWorkspaceRoot` check → `TestFnmStableSandboxDir_DevLinkWorkspaces` RED with `fnmStableSandboxDir dev-link = "…/pi-mono/packages/coding-agent", want workspace root "…/pi-mono"` (lands on `coding-agent`, which holds only `chai` dev deps, not `cross-spawn`). CAUGHT by the test — pins the captain's correction. `TestFnmStableSandboxDir_NormalInstall` stayed green (the normal-install tree has no workspace root, so first-ancestor == highest-ancestor there). Restored → GREEN.
- Tests caught all three mutations. No mutation escaped the suite.

### Gate results

- `go test ./...` → exit 0, all packages ok (`internal/cli` green incl. the full `TestPiFrontDoor` + `TestRunPi_Launch*` + `TestFnmStableSandboxDir*` + `TestResolveFnmMultishellPi*` suites).
- `go test ./... -race` → exit 0, all packages ok.
- `gofmt -l ./cmd ./internal` → reports ONLY the three pre-existing unrelated files (`internal/cli/prose_function_routing_test.go`, `internal/ensigncycle/haiku_loop_spike_live_test.go`, `internal/status/section_read.go`). Verified these are NOT in commit `a1690b5c` (commit touches only `pi.go`, `pi_launch_test.go`, `pi_frontdoor_test.go`); `gofmt -d` on the three task files is empty (clean). Working tree left clean after adversarial mutations (restored from backup; `git status --short internal/cli/` empty).

### Smoke (load-bearing — run in the live env)

The validation environment IS the captain's fnm+safehouse+dev-link env (`which pi` → `~/.local/state/fnm_multishells/3628_…/bin/pi`; `safehouse` at `~/.local/bin/safehouse`; the stable bin `~/.local/share/fnm/node-versions/v24.13.1/installation/bin/pi` is a symlink whose `EvalSymlinks` resolves out to `~/git/pi-mono/packages/coding-agent/dist/cli.js`; `~/git/pi-mono/package.json` carries `"workspaces": ["packages/*", …]`). Built the worktree binary (`go build -o /tmp/sd-cycle2-validate ./cmd/spacedock`) and ran:

- `spacedock pi --plugin-dir . --safehouse -- --version` (forced wrap, AUTO-computed grant — no manual `--safehouse-add-dirs`) → printed `0.79.8`, EXIT=0. Banner: `Sandbox: enabled (safehouse)`.
- `spacedock pi --plugin-dir . -- --version` (no wrap) → printed `0.79.8`, EXIT=0. Banner: `Sandbox: available, not enabled`.

Both launch paths clean. The wrapped smoke is the load-bearing proof: Node resolved the stable `installation/bin/pi` symlink, followed the chain out to `~/git/pi-mono/packages/coding-agent/dist/cli.js`, loaded it, and required `cross-spawn` (hoisted to `~/git/pi-mono/node_modules`) INSIDE the sandbox — only possible because the AUTO-computed grant landed on the `~/git/pi-mono` workspace root. The prior cycles' MODULE_NOT_FOUND failure mode is closed. (A full `--model z-ai/glm-5.2 --thinking xhigh` run consumes an agent session and remains the captain's final gate; the `--version` smoke already proves the launch-path failure mode is fixed.)

### Open risk — grant scope (assessed)

The grant is broader than the dispatch's original B-i (`installation/bin`): `fnmStableSandboxDir` grants the workspace root (dev-link: the whole `~/git/pi-mono` monorepo; normal install: `installation/lib`). Assessment: ACCEPTABLE scope, NOT a gate-blocker.
- Targeted, not whole-FS: the grant is pi's actual code+dep tree (the resolved real script + the `node_modules` holding hoisted runtime deps), not a blanket `/` grant.
- Necessary for correctness: the captain's empirical grant ladder proved narrower grants fail — `installation/bin` → MODULE_NOT_FOUND on the stable path; `coding-agent` (package dir) → `ERR_MODULE_NOT_FOUND` on `cross-spawn`; `~/git/pi-mono` (workspace root) → passes. The workspace root IS Node's dep-hoist boundary; a narrower correct grant does not exist short of enumerating every required dep path (brittle, breaks on dep-tree changes).
- Visibility, not write: `--safehouse-add-dirs` grants filesystem VISIBILITY; the `.safehouse` profile separately controls write scope. Granting the monorepo visible does not widen the write surface beyond the workdir profile.
- Captain's prior approval of the corrected target is noted; this is the captain's gate call, and I concur with approval. A future hardening (per-dep grants if safehouse gains that capability) is recorded as the extension seam, not a blocker.

### Summary

VERDICT: PASSED. Cycle-2 (commit `a1690b5c`) is structurally sound and behaviorally verified. All four ACs green; the `Launch(argv, env)` signature change lands correctly and mirrors claude/codex's `frontdoor.go:345` env-forwarding plumbing; the workspaces-rooted sandbox grant is computed from the fully-resolved real script and matches Node's dep-resolution boundary; the resolution applies ALWAYS (no `!wrap` gate). All three adversarial mutations were CAUGHT by the tests (none escaped). Gates green (`go test ./...`, `-race`, gofmt clean on task files; pre-existing churn files confirmed NOT in the commit). The live smoke under real safehouse+fnm+dev-link passes (`0.79.8`, exit 0) on BOTH wrap and no-wrap paths — the MODULE_NOT_FOUND failure mode from cycles 1/1-revised is closed. The grant-scope open risk is assessed as acceptable (targeted visibility grant, not a write-surface widening, necessary for correctness); not a gate-blocker. Recommend the captain's final `--model` full-session run as the last gate (it consumes an agent session); the launch-path correctness is already proven.
