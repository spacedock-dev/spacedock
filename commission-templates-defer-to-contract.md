---
id: 48wktz0b51941chr9c8kfask
title: Restructure the shipped commission templates — lead-with-the-end + defer universal rules to the FO/ensign contract + a workflow-specific-rules slot
status: implementation
source: captain (2026-06-14) — apply the dev-README slim ethos (lead-with-the-end + defer-to-contract; the README "move-3" work) to the SHIPPED commission templates. A templates assessment this session found development.md / experiment.md / refinement.md restate universal rules inline (proof discipline in development.md's "Recommended practices" ~L111-137; the Inputs/Outputs/Good/Bad stage-semantics block repeated per stage across all three). This is the template half descoped from rzp. Adjacent to but distinct from ey (which ports the proof-policy RULE into the contract + templates); this task makes the templates DEFER universal rules to the contract and keep only workflow-specific rules. 0.20.4 read-cost theme (every commissioned workflow inherits the scaffolding).
started: 2026-06-15T05:35:19Z
completed:
verdict:
score: 0.33
worktree:
issue:
sprint: 0204-structured-reads
sprint-readiness: ready
---

The three shipped commission templates restate universal rules that already live in the FO/ensign operating contract. Restructure them the way the dev README was just slimmed (commit `48edae4c`): lead with the workflow's outcome, defer universal rules to the contract, and keep only each shape's workflow-specific rules in a dedicated slot. Every commission session then reads leaner, non-stale scaffolding, and the template's rule prose can no longer drift from the contract it paraphrases.

## Problem

`skills/commission/references/templates/development.md`, `experiment.md`, and `refinement.md` each restate rules the FO/ensign contract already owns:
- proof discipline (development.md's "Recommended practices" ~L111-137: external-proof ACs, live-scenario, detached audit) restates the contract's "prefer a code gate over a prose-only rule" and the ensign's "prove by exercising, not re-reading";
- generic stage semantics (the Inputs / Outputs / Good / Bad bullets) restate the universal stage shape per stage across all three;
- AC-must-be-testable and lead-with-the-end, both already universal.

**Where the read cost actually lands.** Commission consumes a template in two distinct steps (verified against `skills/commission/SKILL.md`):
- **Phase 1 / Trait Detection** (`SKILL.md` L116) `Read`s the *whole* selected template file into the commission session's context, then follows its `## Adoption` section. This is where the bloat is paid — every commission of that shape reads the restated doctrine, whether or not it ever uses it.
- **Phase 2 / Generate** (`SKILL.md` L256, L316-430) writes the commissioned README from the **inline** structure in SKILL.md, auto-generating per-stage `Inputs/Outputs/Good/Bad` *specific to the mission* — it does NOT copy the template file's per-stage prose. The refinement template states this explicitly (L210: per-stage bullets "are auto-generated by commission Phase 2b … not pre-baked here").

So the template's per-stage prose is pure **reference/illustration** read at Phase 1, never copied forward; slimming it cuts commission-session read cost without changing what the commissioned workflow looks like. The one load-bearing exception is development.md's "Recommended practices" block, which is an *opt-in adoption instruction* (L113: "Adopt them by copying the guidance into the `validation` stage's Outputs and Bad lists above") — its dev-shape-specific disciplines must survive the restructure, only their restated universal backbone is deferred.

The duplication also means the template copies drift independently of the contract they paraphrase: a fix to the contract's proof rule (e.g. ey's independent-source rule) does not reach a paraphrase frozen in a template.

## Proposed approach

Mirror the three dev-README slim moves (`48edae4c`) into each template, in the template's own voice. The moves:

**Move A — lead with the end.** Each template's opening paragraph states the workflow's outcome/value before its mechanics. development.md and experiment.md mostly do this already (development: "Tasks move … and land via PR review"; experiment: "Each experiment is a hypothesis tested through tiers of evidence"); refinement.md's opening leads with "Iterate on an artifact through stages of improvement" — keep the value-first lead and ensure the *first sentence* names the outcome, not "what this is."

**Move B — defer the universal rules to the contract.** Add to each template a short `## Workflow-specific rules` section whose opening sentence defers generic stage semantics and proof discipline to the FO/ensign contract (the precedent wording is the README's new Proof-policy lede: "The FO contract's Operating Principles and Working Principles already govern generic proof discipline … Tasks in this workflow inherit that. The rules below add the {shape} specifics."). Then **remove the restated universal doctrine** from the template body:
  - development.md: replace the long restated paragraphs of "Recommended practices" (L111-137) with compact dev-shape pointers (see Move C / the per-template shape below). The universal backbone — "prove by exercising not re-reading," "code gate over prose-only," AC-must-be-testable, stage semantics — is deferred, not restated.
  - experiment.md / refinement.md: they carry no separate doctrine block, so the defer is a one-line note at the head of (or replacing) their generic per-stage prose.

**Move C — compact the per-stage `Good/Bad`.** The per-stage `Inputs/Outputs/Good/Bad` bullets are reference-only (Phase 2b auto-generates the commissioned README's stage bullets; the template's are illustration). Compress each stage to a one-line shape description plus any genuinely *shape-specific* note (development's `validation` fresh-agent independence, experiment's `holdout` out-of-sample independence, refinement's `review` human-in-the-loop bar). Drop the bullets that merely restate the universal "be specific / don't hand-wave" stage shape.

**The `## Workflow-specific rules` slot — what each shape keeps (the 3-5 unique rules):**
- **development.md:** the repo-mutation worktree layer (impl + validation run in a worktree; validation is `fresh`), the pr-merge mod offer (PR state on the `pr` field, no `pr_open` stage), and the dev-shape *opt-in* proof disciplines as **pointers** — test-first authoring, external-proof ACs, detached adversarial audit, live-scenario-for-runtime-claims — each kept as a one-or-two-line opt-in note (these are dev-specific and get copied into the commissioned validation stage per L113), with their generic justification deferred to the contract.
- **experiment.md:** falsifiable-hypothesis + success-criteria-fixed-before-evidence, the smoke/run/analysis/holdout tier-gating discipline, holdout out-of-sample independence, the parked-stages layer + silence-watcher offer.
- **refinement.md:** the human-in-the-loop quality bar (review is an approval gate, polish is cosmetic-only), the variant menu (outreach / integration / content-production / prd-authoring), and the "universal base shape; no layers by default" note.

**The `## Adoption` section stays intact in all three.** It is load-bearing commission machinery (pre-fill stages, apply layers, offer mods, inject snippet, surface variants, confirmation prose) consumed at Phase 1 — the restructure does not touch it beyond the development "Recommended practices" cross-reference (L113), which is reworded to point at the new `## Workflow-specific rules` slot.

### Composition with ey (`proof-policy-shipped-scaffolding`)

ey and this task share one file: `development.md`. The clean split (recorded in debrief 2026-06-14-03 as the session design): **ey lands the universal proof rule IN the contract** (`first-officer-shared-core.md` "Prefer a code gate over a prose-only rule" + `ensign-shared-core.md` "Prove by exercising, not by re-reading" gain the independent-source test); **this task makes the templates DEFER to it.**

Resolution of the overlap: once the proof rule's single home is the contract, **`development.md` does not carry it at all** — it defers. ey's captain-named `development.md` target (the live-scenario paragraph ~L135) is therefore *subsumed* by this task's defer: there is nothing for ey to edit in the template once the template points at the contract. Sequencing that avoids a clobber:
- **Preferred order: ey's contract edits first, then this restructure.** ey establishes the rule's home in the contract; this restructure then deletes the template's restated copy and points at it. If this task ships first, ey must skip its `development.md` line item (the template already defers) and land only the contract edits.
- **Either order is safe as long as the rule ends up in the contract and `development.md` ends up deferring** — the failure mode to avoid is *both* editing the live-scenario prose in `development.md` concurrently. The implementation must read ey's then-current state before touching `development.md` and reconcile (the dispatch checklist names this).

This task does NOT port any rule into the contract — that is ey's scope. If ey has not yet landed its contract edits when this restructure runs, the defer still points at the *existing* contract sections ("Prefer a code gate over a prose-only rule", "Prove by exercising, not by re-reading"), which already carry the generic discipline; ey later sharpens those sections in place without changing the template's pointer.

## Out of scope

- **The contract-side proof-policy port** — porting the independent-source rule into `first-officer-shared-core.md` / `ensign-shared-core.md` is ey, not this task.
- **The dev README itself** (`docs/dev/README.md`) — already slimmed FO-direct (`48edae4c`); this task is the shipped-template companion.
- **Codebase-specific rules that legitimately stay per-workflow** — the detached-audit high-stakes surface list and the contractlint instruction-read quarantine are dev-codebase specifics; they belong in this repo's README/contract, not abstracted into the shipped templates.
- **Any change to commission machinery** — the `## Adoption` sections, the inline GENERATE structure in `SKILL.md`, trait detection, and the Phase 2b auto-generation stay as they are. This is template prose/structure only.
- **Behavioral changes to the workflow stages themselves** — stage lists, gates, layers, and mods are unchanged; only the prose that *describes* them is restructured.
- **Adding a fourth template or new variants** — the restructure adapts the three existing files.

## Acceptance criteria

Each AC names a property of the finished scaffolding, not a stage action, and how it is verified — proof is behavioral or binds an independent source that can diverge, never a prose-grep over the template (a substring match over a file the model reads asserts only that we wrote what we wrote).

**AC-1 (behavioral, the load-bearing proof) — A workflow commissioned from the restructured `development.md` still governs by the universal rules even though the template no longer restates them.** The rules reach the commissioned workflow through the contract its FO loads at boot, not through restated template prose; removing the restatement does not remove the governance.
Verified by: a **live commission spike** — dispatch a real agent that runs `Skill(skill="spacedock:commission")` against the restructured `development.md` for a code-shipping mission, producing a commissioned workflow. Then **boot that commissioned workflow's first officer** and drive the universal-rule check: seed an entity whose only AC is a presence-oracle over an instruction file (a tautological proof) and confirm the FO/validator REJECTS it and demands a behavioral proof.

**What the grader rewards (and explicitly does NOT).** Grade ONLY on the durable rejection END-STATE — the rejection verdict / routed feedback recorded in the seeded entity's frontmatter + stage report after the drive (entity state before→after, observed durable outcome). The grader MUST NOT look for any specific rule-CITATION, doctrine phrasing, or rule-text in the commissioned workflow's output, README, or the FO's transcript — the rule's *text* is ey's to add to the contract, not this task's deliverable, so requiring a citation would (a) couple this AC to ey's wording and (b) reduce to the banned prose-grep. The behavior this AC proves is purely *the defer composes*: governance (the rejection) survives removal of the template's restatement. A run where the FO rejects the tautological AC and routes feedback PASSES regardless of how it phrases the rejection; a run where the FO accepts the tautological entity FAILS regardless of what rule text appears anywhere. Offline negative: a hand-built wrongly-accepted end-state (the seeded entity terminalized PASSED) must red the grader.

Additionally, confirm the commissioned README does NOT itself restate the rejection rule (`grep` for *absence* in the generated README — absence is a legitimate structural fact, unlike a presence assertion), so the surviving governance is attributable to the contract the FO loads, not to template prose. **Dependency: this behavior presupposes the contract carries the discipline** — ey lands (or the existing contract sections already carry) the rule the FO applies; this AC proves the *defer composes*, which is this task's distinct contribution, not ey's rule-port. Development is the single live representative (see the cost-scope note below); experiment/refinement are covered structurally (AC-2/AC-3) plus the no-spend commission dry-check that confirms Phase-1 completion for all three shapes (test plan).

**AC-2 (single-source structural invariant) — the universal-rule doctrine lives in exactly ONE place (the contract), not duplicated across the templates.** Each template carries a `## Workflow-specific rules` slot with only that shape's unique rules; the generic doctrine the templates used to restate is absent from all three template bodies.
Verified by: a contractlint-style **single-source check** modeled on `internal/contractlint` `TestStartupGateGuidanceHasSingleSource` (walks `skills/`+`agents/` markdown, asserts each universal-doctrine marker appears in exactly one file — its owning contract — and FAILS if a second copy reappears in a template). This is NOT a presence tautology: it binds two independent, divergeable things (contract content vs. template content) and reds precisely when someone re-restates the universal rule in a template (drift regression).

**The pinned marker set (the strings the check binds).** Four stable, contract-owned canonical phrasings — chosen because each is the contract's *own* central wording for a doctrine the templates currently paraphrase, and each must end up in exactly one owning file:
1. `"Prefer a code gate over a prose-only rule"` — owner: `skills/first-officer/references/first-officer-shared-core.md` (Working Principles, L204).
2. `"wording-present is not behavior"` — owner: `first-officer-shared-core.md` (L204; the anti-prose-grep core).
3. `"Prove by exercising, not by re-reading"` — owner: `skills/ensign/references/ensign-shared-core.md` (L27).
4. `"A substring search is not proof of behavior"` — owner: `ensign-shared-core.md` (L27; the anti-tautology core).

The check asserts: for each marker, exactly one file under `skills/`+`agents/` contains it, and that file is the named owner — so a template (or any other file) re-introducing the phrasing reds the test. Pair it with a `## Workflow-specific rules` heading-presence assertion per template (structural section presence, a machine-checkable property, not a doctrine-substring match).

**Known limitation the detached audit must attack (stated so the audit has a defined target).** A marker set is only as strong as its coverage: a paraphrase that re-restates the *same doctrine* in a template using different words would dodge these exact strings and pass. That is the precise hole the high-stakes detached audit targets (test plan) — it constructs such a paraphrase; if the paraphrase passes the check, the marker set is too narrow and must widen (add the dodged phrasing as a marker, or move to a structural absence-of-section check). The marker set is the *starting* binding, deliberately concrete so the audit can refute it, not a claim of exhaustive coverage. Lives in `internal/contractlint` alongside the existing structural checks; no model spend.

**AC-3 (structural) — each template's opening section names the workflow's outcome before its stage mechanics.** Lead-with-the-end shape is structurally present.
Verified by: a structural opening-section check (the template's first prose paragraph, between the frontmatter and the first `##` heading, exists and precedes the `## File Naming` / `## Stages` mechanics) — a positional/ordering assertion over the three template files, not a value-judgement substring match. The qualitative "does the lede actually lead with value" judgement is a **gate-review item** (the FO/captain read the before/after at the ideation and validation gates), not a machine assertion, since lede quality is not mechanically decidable. Recorded honestly: AC-3's machine half proves *a lede section exists in the right position*; the *quality* half is human-gated.

**Proof-shape honesty note.** This is prose scaffolding. The only fully behavioral proof is AC-1's live commission + rejection drive (development). AC-2 and AC-3 are structural invariants that bind independent sources or assert positional/structural facts — legitimate per the proof policy because they can diverge and fail, unlike a doctrine-substring match. No AC is satisfied by grepping the template for the words it was supposed to contain.

## Test plan

**Deliberate cost-scope: development is the single live representative.** Only `development.md` gets the full live governance drive (AC-1) — it is the captain-named primary shape, it is the file ey overlaps, and it carries the only load-bearing prose coupling (the L113 opt-in block). A live drive per template would triple the model spend to re-prove the same composition claim (defer survives removal) that is structurally identical across shapes. **Condition that makes this scope safe:** the no-spend commission dry-check below runs Phase-1 completion for `experiment.md` AND `refinement.md` too — so all three shapes are confirmed not to break commission, even though only development gets the live governance drive. Experiment and refinement are otherwise covered by the structural AC-2 (single-source) and AC-3 (lede) invariants, which apply to all three template files equally.

**The load-bearing proof — AC-1 live commission spike (development).** One live drive, two halves:
1. *Commission half:* dispatch a real agent (`spacedock claude` / the team harness) running `Skill(skill="spacedock:commission")` against the restructured `development.md` for a representative code-shipping mission; capture the generated workflow README + scaffolding. Confirm structurally that commission still completes (Adoption machinery intact: stages pre-filled, pr-merge offered, snippet injected) — proves the prose removal did not break Phase 1.
2. *Governance half:* boot the commissioned FO; seed an entity whose sole AC is a presence-oracle over an instruction file; assert durable rejection/routed-feedback end-state (the grader rewards the END-STATE, never a rule-citation — see AC-1); assert (via `grep` for *absence*) the commissioned README does not itself carry the rejection rule. Reuse the rejection-drive shape from ey's AC-1 scenario / `internal/ensigncycle` shared scenarios if available; otherwise a `claude -p` single-entity drive against the commissioned workflow. Cost: live-gated, ~one scenario; size at implementation. Offline negative: a hand-built wrongly-accepted end-state must red the grader.
   - **Riskiest-mechanism spike (do first, cheap) — covers ALL THREE shapes:** before the full live drive, run a **no-spend commission dry-check against `development.md`, `experiment.md`, AND `refinement.md`** — drive commission (or hand-trace Phase 1) against each restructured template and confirm Phase-1 completes: nothing in the `## Adoption` section or Phase-1 confirmation prose references removed prose in a way that breaks generation. This is the path that would invalidate the rest of the work if it broke (the restructure silently breaking commission for any shape), and it costs minutes per shape. The development `L113` cross-reference ("copying the guidance into the validation stage") is the known coupling — confirm it is rewired to the new slot; for experiment/refinement confirm no analogous coupling was severed. Record the per-shape result in the implementation body; this is the integration-level "smallest failing exercise first," and it is what lets only development carry the expensive live drive.

**AC-2 single-source structural check (no spend).** Add a Go test in `internal/contractlint` modeled on `TestStartupGateGuidanceHasSingleSource` using the four pinned markers named in AC-2 (each asserted to resolve to exactly its named owning contract file, failing if any template re-introduces it). Plus a per-template `## Workflow-specific rules` heading-presence assertion. Cost: trivial, runs in `go test ./internal/contractlint`. The detached audit (below) is the check on whether the marker set is wide enough.

**AC-3 opening-section structural check (no spend).** A Go test (or extend the contractlint structural suite) asserting each template has a non-empty lede paragraph positioned before the first `## ` mechanics heading. Cost: trivial.

**Gate-review items (human, not machine).** The qualitative lede-quality judgement (AC-3 quality half) and the before/after wording fidelity per the workflow's "template/skill text changes: specific before/after wording" rule are reviewed at the ideation gate (this body's before/after shape, below) and the validation gate, not asserted by a test.

**High-stakes surface → detached adversarial audit before merge.** The templates are shipped scaffolding (a named high-stakes surface). Before merge, run the read-only audit on a throwaway checkout: try to refute AC-1's grader (a transcript narrating rejection while the durable state accepted) and AC-2's single-source check (a paraphrase that re-restates the doctrine in a template while dodging the chosen markers — if a paraphrase passes, the marker set is too narrow). "Refuted nothing material" is a valid recorded outcome.

**Scaffolding guardrail (dispatch shape).** Per rzp and the FO write-scope rules, `skills/commission/references/templates/*.md` are shipped scaffolding — implementation runs in a **worktree under test by a dispatched worker**, never an FO-direct edit. This ideation runs without a worktree (the before/after lives in this body).

## Before/after restructure shape (the recorded diff target)

Per the workflow's "template/skill text changes: specific before/after wording" rule, implementation produces a concrete before/after per file. The structural shape ideation pins (exact wording is an implementation deliverable reviewed at the validation gate):

**development.md**
- *Opening:* keep the value-first lede ("Tasks move from a captain-curated backlog … land via PR review") as the first paragraph; ensure no "what this is" sentence precedes it.
- *Remove:* the restated universal doctrine inside "## Recommended practices (opt-in)" (~L111-137) — the long justification paragraphs that paraphrase the contract's proof discipline.
- *Add:* `## Workflow-specific rules` deferring generic proof discipline + stage semantics to the FO/ensign contract (one lede sentence), then the dev-shape opt-in pointers (test-first, external-proof, detached-audit, live-scenario) as compact one-to-two-line notes that point at the contract for their generic rationale.
- *Rewire:* the `L113` "copying the guidance into the validation stage" instruction points at the new slot.
- *Compact:* per-stage `Good/Bad` to one line + shape-specific note.
- Expected: ~27 restated-doctrine lines → a ~8-12 line slot + pointers; net read-cost cut at Phase 1.

**experiment.md**
- *Opening:* keep "Each experiment is a hypothesis tested through tiers of evidence …" value-first lede.
- *Add:* `## Workflow-specific rules` deferring stage semantics + proof discipline to the contract, keeping falsifiable-hypothesis / success-criteria-before-evidence, tier-gating, holdout independence, parked-stages + silence-watcher.
- *Compact:* per-stage `Good/Bad` to one line + tier-specific note.

**refinement.md**
- *Opening:* ensure the first sentence leads with the outcome ("track an artifact through rounds of improvement to a locked, shipped result") before "This is the universal base shape."
- *Add:* `## Workflow-specific rules` deferring universal semantics to the contract, keeping the human-in-the-loop quality bar, the variant menu, and "no layers by default."
- *Compact:* per-stage `Good/Bad` to one line + shape-specific note (the variant per-stage-detail note at L210 already defers correctly — keep it).

## Spike determination

The riskiest unverified mechanism is **whether removing the restated prose from a template breaks commission's Phase-1 flow** (does any Adoption/confirmation path depend on the removed prose?). Ideation has *partially* de-risked this by reading `SKILL.md`: Phase 2b auto-generates per-stage bullets (template per-stage prose is not copied forward — refinement.md L210 confirms), and the only known coupling is development.md L113's "copying the guidance into the validation stage." That coupling is identified and the rewire is specified above. The remaining unverified step — that a *full commission run* against the restructured template completes cleanly — is the implementation's first exercise (test plan, "no-spend commission dry-check"), run before the expensive live governance drive. No mechanism is assumed proven that has not been read or scheduled to be exercised first.

## Stage Report: ideation

- DONE: Coordinate the development.md split with ey — read ey's body and ensure the two edits compose without clobbering
  Read `proof-policy-shipped-scaffolding/index.md` (ey, status backlog, not in-flight in this team); resolved the shared `development.md` file as "ey lands the rule in the contract, this task defers to it"; the `## Composition with ey` section records the subsumption + sequencing (ey-first preferred, either order safe if rule ends in contract).
- DONE: Design the universal-defer + workflow-specific-rules-slot restructure across development.md / experiment.md / refinement.md
  Three slim moves (lead-with-end / defer-to-contract / compact per-stage prose) mirrored from the dev-README slim `48edae4c`; per-template `## Workflow-specific rules` slot contents pinned; `## Before/after restructure shape` records the concrete diff target per file.
- DONE: Behavior-first ACs whose load-bearing proof is a live commission + boot/behavior check that universal rules survive the defer (never a substring match), plus an independent-source structural check for the workflow-specific slot
  AC-1 = live commission spike against restructured development.md + boot the commissioned FO + rejection drive on a tautological-AC entity, graded on durable end-state (with absence-grep on the generated README); AC-2 = single-source structural check modeled on `TestStartupGateGuidanceHasSingleSource` (binds contract vs template, can diverge); AC-3 = positional lede check + human gate for quality.
- DONE: Record the before/after restructure shape
  `## Before/after restructure shape` section: per-file Opening / Remove / Add / Rewire / Compact, with the dev expected read-cost cut (~27 lines → ~8-12 line slot).

### Summary

Designed the template restructure as three slim moves ported from the dev-README slim (`48edae4c`): lead-with-the-end opening, a `## Workflow-specific rules` slot that defers universal stage-semantics + proof discipline to the FO/ensign contract, and compacted per-stage prose. The key finding (verified against `commission/SKILL.md`): commission reads the whole template at Phase 1 (where the read-cost lands) but auto-generates the commissioned README from an inline structure at Phase 2 — so the template's per-stage prose is reference-only, safe to slim; the one load-bearing exception is development.md's opt-in "Recommended practices" block (L113 copies it into the validation stage), whose dev-specifics are kept as pointers. ey-composition resolved: the proof rule's single home is the contract (ey), the template defers (this task), and `development.md`'s ey-target is subsumed by the defer. Scope decision flagged for the gate: AC-1's live commission uses development as the single live representative (captain-named primary), with experiment/refinement covered by the structural AC-2/AC-3 + a no-spend commission dry-check, rather than three live drives.

## Stage Report: ideation (cycle 2)

Revised per the independent preflight staff review (three items folded into the body + ACs + test plan together).

- DONE: Pin the AC-2 single-source marker set explicitly in the body
  AC-2 now names four stable contract-owned markers with their owning files: "Prefer a code gate over a prose-only rule" + "wording-present is not behavior" (first-officer-shared-core.md L204), "Prove by exercising, not by re-reading" + "A substring search is not proof of behavior" (ensign-shared-core.md L27); added the known-limitation paragraph giving the detached audit a defined target (a doctrine-paraphrase that dodges the markers means the set is too narrow → widen).
- DONE: State development-only live-test as deliberate cost-scope, with the no-spend dry-check covering Phase-1 completion for experiment AND refinement too
  Test plan opens with the cost-scope rationale (development = captain-named primary + ey-overlap + only load-bearing L113 coupling); the riskiest-mechanism spike is rewritten to run the no-spend commission dry-check against all three templates (development + experiment + refinement), recording per-shape Phase-1 completion — the condition that makes the single live drive safe.
- DONE: Make AC-1's grader explicit — rewards the durable rejection END-STATE, never a rule-CITATION in the commissioned output
  AC-1 gained a "What the grader rewards (and explicitly does NOT)" paragraph: grade only on the rejection verdict/routed-feedback durable end-state; the grader MUST NOT look for any rule-citation/doctrine phrasing (that text is ey's to add to the contract; requiring it would couple to ey's wording and reduce to prose-grep). The absence-grep on the commissioned README stays (attributes governance to the contract, not the template).

### Summary

Folded the three staff-review items without disturbing the rest (the read-cost finding, the genuinely-behavioral AC-1, and the clobber-safe ey composition all stand). AC-2's marker set is now concrete and attackable (four canonical contract phrasings + an explicit too-narrow-means-widen rule for the audit); the development-only live drive is justified as cost-scope and made safe by a three-shape no-spend Phase-1 dry-check; AC-1's grader is pinned to the durable rejection end-state and barred from rewarding any rule-citation, keeping the proof on "the defer composes" (this task) and off ey's rule-text. Re-filed; committing path-scoped and pushing.
