---
id: 4ctz4sfybfk0mfsbnrjcc7bv
title: Force one boot at the compaction boundary — a compacted FO resumes on stale bindings
status: validation
source: "Captain CL, 2026-08-18, in chat: 'file the compaction improvement, detail the problem diagnosis and proposed solution.' Raised after the FO opened three PRs by hand without reading the pr-merge mod, and diagnosed the root cause as never having run Startup in a compaction-resumed session."
started: 2026-08-18T23:15:43Z
completed:
verdict:
score:
worktree: .worktrees/spacedock-ensign-force-boot-at-compaction-boundary
issue:
gates:
    version: 1
    records:
        - id: gate:4ctz4sfybfk0mfsbnrjcc7bv:backlog
          stage: backlog
          attempts:
            - id: gate-attempt:4ctz4sfybfk0mfsbnrjcc7bv-backlog-1
              briefing:
                id: briefing:4ctz4sfybfk0mfsbnrjcc7bv:backlog:attempt-1:revision-1
                digest: sha256:8c097b36dc8addb17335f90ea018c35bb0f1b083f31bce9d390c80df9e543608
                request-digest: sha256:79ea2c142d7fb3cbbd6ec4afb6b981efee79195296b7750c8b3b4166550a49f9
                room-ref: ./force-boot-at-compaction-boundary/review/backlog/briefing-1
              resolution:
                type: Resolution
                id: resolution:spacedock:4ctz4sfybfk0mfsbnrjcc7bv:backlog:1
                briefing: briefing:4ctz4sfybfk0mfsbnrjcc7bv:backlog:attempt-1:revision-1
                by: person:captain
                at: "2026-08-18T23:14:57.630325Z"
                decision: approve
                reason: 'Captain approved in chat: ''ok dispatch 4c and nr.'' Accepts the seed into ideation; the captain independently identified this as the highest-value item on the board because it makes every future session more reliable rather than fixing one test.'
              application:
                target-stage: ideation
                state: consumed
        - id: gate:4ctz4sfybfk0mfsbnrjcc7bv:ideation
          stage: ideation
          attempts:
            - id: gate-attempt:4ctz4sfybfk0mfsbnrjcc7bv-ideation-1
              briefing:
                id: briefing:4ctz4sfybfk0mfsbnrjcc7bv:ideation:attempt-1:revision-1
                digest: sha256:dde54526427e9d671d87cc588f41ea34487238b0f323dd1282f2a9a820f93afd
                request-digest: sha256:32bf3d22c3138621ab5a5deaf78881ec0a961edaa88d8f5ad9cc3f03bad99a68
                room-ref: ./force-boot-at-compaction-boundary/review/ideation/briefing-1
              resolution:
                type: Resolution
                id: resolution:spacedock:4ctz4sfybfk0mfsbnrjcc7bv:ideation:1
                briefing: briefing:4ctz4sfybfk0mfsbnrjcc7bv:ideation:attempt-1:revision-1
                by: person:captain
                at: "2026-08-18T23:39:29.002381Z"
                decision: revise
                reason: 'Captain rejected in chat: ''no we don''t need 500 lines for that.'' The diagnosis and the boundary spike are accepted and stay — the mechanism is right. The surface is not: +540 net for writing a receipt at boot and comparing it to a timestamp is disproportionate, and the +340 test budget is the bulk of it. The FO''s own dispatch scope note told this ideation to double its test estimate; that instruction was wrong and is withdrawn.'
            - id: gate-attempt:4ctz4sfybfk0mfsbnrjcc7bv-ideation-2
              briefing:
                id: briefing:4ctz4sfybfk0mfsbnrjcc7bv:ideation:attempt-2:revision-1
                digest: sha256:1e46104a9835ed01a56040f2ec0ed3ae0b469f46b9bda575dae416e723cf3a0d
                request-digest: sha256:316d8d7742e343d86924271460519f09bc0888e43be2fe0869a9ee98aa969cc3
                room-ref: ./force-boot-at-compaction-boundary/review/ideation/briefing-2
              resolution:
                type: Resolution
                id: resolution:spacedock:4ctz4sfybfk0mfsbnrjcc7bv:ideation:2
                briefing: briefing:4ctz4sfybfk0mfsbnrjcc7bv:ideation:attempt-2:revision-1
                by: person:captain
                at: "2026-08-19T03:24:40.790964Z"
                decision: approve
                reason: 'Captain approved in chat: ''approve both.'' Accepts the re-scoped design at net +245 across 8 files — boot receipt plus fail-closed guard on gate record, gate consume, and merge guard, no hook. The boundary was proven by live capture including this session''s own compact_boundary record, and both seeded mechanisms were disproven by that capture rather than by argument.'
              application:
                target-stage: implementation
                state: consumed
        - id: gate:4ctz4sfybfk0mfsbnrjcc7bv:validation
          stage: validation
          attempts:
            - id: gate-attempt:4ctz4sfybfk0mfsbnrjcc7bv-validation-1
              briefing:
                id: briefing:4ctz4sfybfk0mfsbnrjcc7bv:validation:attempt-1:revision-1
                digest: sha256:30cd6d3c4717dc4c01fc8ea797260b65d38ba3b62489566f388c6a66d4a414e8
                request-digest: sha256:a5a07722e92d3524e4c168265785b558cac5b501d7c57e018738ec2944c06eb8
                room-ref: ./force-boot-at-compaction-boundary/review/validation/briefing-1
              resolution:
                type: Resolution
                id: resolution:spacedock:4ctz4sfybfk0mfsbnrjcc7bv:validation:1
                briefing: briefing:4ctz4sfybfk0mfsbnrjcc7bv:validation:attempt-1:revision-1
                by: person:captain
                at: "2026-08-19T05:18:43.346432Z"
                decision: revise
                reason: 'Captain rejected in chat: ''is the external file .spacedock/boot justified? i don''t like the smell of it.'' The mechanism, the spike, and the fail-open correction are all accepted. Rejected on the receipt LOCATION only: it writes inside the repository at {gitRoot}/.spacedock/boot/{session_id}, which required a new .gitignore entry, breaks on a read-only repository, and is the sole cause of the git-root divergence deferred risk. Host scratch belongs in host scratch. An existing per-session convention already lives outside the repo at /tmp/spacedock-dispatch/{session_id}-*. Moving the receipt there deletes the .gitignore change, the read-only-root failure, and the divergence risk outright.'
            - id: gate-attempt:4ctz4sfybfk0mfsbnrjcc7bv-validation-2
              briefing:
                id: briefing:4ctz4sfybfk0mfsbnrjcc7bv:validation:attempt-2:revision-1
                digest: sha256:4abd5053924ffe3812cc2eb8ea328641f1cbf61bcc88c495d9ad66799c0baad4
                request-digest: sha256:8a8d76144a982ce8e0ceed48fd5891c4d2d0f0528ac08a6ee4f60e92da31956c
                room-ref: ./force-boot-at-compaction-boundary/review/validation/briefing-2
              resolution:
                type: Resolution
                id: resolution:spacedock:4ctz4sfybfk0mfsbnrjcc7bv:validation:2
                briefing: briefing:4ctz4sfybfk0mfsbnrjcc7bv:validation:attempt-2:revision-1
                by: person:captain
                at: "2026-08-19T15:34:51.413058Z"
                decision: approve
                reason: 'Captain approved in chat: ''approve push and i''ll look from the PR.'' Validation PASSED with no material finding: the receipt is out of the repository, deferred risk 2 proved eliminated three ways, the 24-row matrix and concurrency hammer are clean, and the wiring tests were run rather than trusted. The two one-line hardenings (O_NOFOLLOW, 0o700) and the git-fallback divergence stay deferred with promote conditions; the captain will review at the PR.'
              application:
                target-stage: done
                state: superseded
            - id: gate-attempt:4ctz4sfybfk0mfsbnrjcc7bv-validation-3
              briefing:
                id: briefing:4ctz4sfybfk0mfsbnrjcc7bv:validation:attempt-3:revision-1
                digest: sha256:432028b918d23bbeaf235fb0d60764e2dc664b53b662b1a2488dbcc0a091745f
                request-digest: sha256:96908e5232cbe61fb063d75ddc69d226160f28b1f6d4d1b327412c3fbfaf7300
                room-ref: ./force-boot-at-compaction-boundary/review/validation/briefing-3
              resolution:
                type: Resolution
                id: resolution:spacedock:4ctz4sfybfk0mfsbnrjcc7bv:validation:3
                briefing: briefing:4ctz4sfybfk0mfsbnrjcc7bv:validation:attempt-3:revision-1
                by: person:captain
                at: "2026-08-19T19:14:15.37857Z"
                decision: approve
                reason: 'Captain approved in chat: ''approve 4c and tell me how i best test it.'' Validation PASSED with no material finding: the token fix executed under four env shapes with the pre-fix failure reproduced exactly, 13 hermetic gate rows, Codex packaging verified by running the real staging, five assertions broken by hand, and the sentinel proof judged sound with its alternative routes closed.'
              application:
                target-stage: done
                state: superseded
            - id: gate-attempt:4ctz4sfybfk0mfsbnrjcc7bv-validation-4
              briefing:
                id: briefing:4ctz4sfybfk0mfsbnrjcc7bv:validation:attempt-4:revision-1
                digest: sha256:47c6298a525aa1b0574cdb3bfb753eaf19677923d7b351bb4ab40421e28329c9
                request-digest: sha256:33fc0e004aecf1a7b4c212f0f8f275db589b807e57b44dbceab0c21d99d7ceba
                room-ref: ./force-boot-at-compaction-boundary/review/validation/briefing-4
              resolution:
                type: Resolution
                id: resolution:spacedock:4ctz4sfybfk0mfsbnrjcc7bv:validation:4
                briefing: briefing:4ctz4sfybfk0mfsbnrjcc7bv:validation:attempt-4:revision-1
                by: person:captain
                at: "2026-08-20T01:17:34.925316Z"
                decision: approve
                reason: 'Captain approved in chat: ''approve 4c to update pr''. Validation PASSED with no material finding: the rendered payload was graded by executing the shipped script (mid-loop, re-satisfy-at-trigger verbatim from the shared core, resume the loop, zero occurrences of ''startup''), the mechanism is byte-identical to a30b963eb by blob hash across hooks.json, both manifests, the fallback template and all three tests, and all four ACs reproduced with their falsifiers breaking exactly the claimed cases. The content-AC gap — no AC pins the payload''s instruction text, proven by the rejected cycle-5 payload passing the suite green — is recorded for a separate scope decision and not held against this merge.'
              application:
                target-stage: done
                state: superseded
            - id: gate-attempt:4ctz4sfybfk0mfsbnrjcc7bv-validation-5
              briefing:
                id: briefing:4ctz4sfybfk0mfsbnrjcc7bv:validation:attempt-5:revision-1
                digest: sha256:89b7537bd6f777586c5f416d6b612273464470bdb824b3b93c97e68c3c1766d1
                request-digest: sha256:193c8eb473e511716629e8b3fefc1c8ef38091f9ff6e19575171563985a0820f
                room-ref: ./force-boot-at-compaction-boundary/review/validation/briefing-5
              resolution:
                type: Resolution
                id: resolution:spacedock:4ctz4sfybfk0mfsbnrjcc7bv:validation:5
                briefing: briefing:4ctz4sfybfk0mfsbnrjcc7bv:validation:attempt-5:revision-1
                by: person:captain
                at: "2026-08-20T19:27:24.731851Z"
                decision: approve
                reason: 'Captain: ''approve both''. Cycle 8 makes the matcher a value under test and widens it to compact and clear. Four mutants fail where two were invisible. Validation proved delivery at source=clear with a live instrumented run, closing the inference gap the widening opened, and closed cycle 7''s AC-1 finding. Net 241 against a 245 estimate. Carried, not blocking: the union assertion is under-constrained against a new source value, the payload is actionable after a clear by one sentence rather than by design, and no AC pins the payload''s instruction content.'
              application:
                target-stage: done
                state: pending
mod-block:
pr:
---

A compacted First Officer session inherits the previous session's **narrative** but not its **bindings**. The transcript summary preserves what the FO was discussing and drops what it was standing on: which binary, which contract version, which mods are registered, which workers are alive, and what durable state actually says.

## Problem — diagnosis

The FO contract's Startup runs a binary gate, one `«state.boot»()`, and `«engage»`, which runs `state ready`, `state sweep`, and `«hooks.run»("startup")` exactly once. A compaction-resumed session does not know it is a new session. It believes it is a continuation, so none of that runs.

Six failures in one session (2026-08-18), all traceable to that single omission:

1. **Binary gate never ran.** The first command failed against `/opt/homebrew/Caskroom/spacedock@next/0.27.0-pre7/spacedock`, deleted by the pre8 cask upgrade. Startup step 1 resolves the launcher before anything else.
2. **The `mods` map was never read**, because no boot record was obtained. The FO then opened three GitHub PRs by hand, bypassing the `pr-merge` mod completely: no gate recorded, no `pr:` field, no `mod-block`, no merge guard armed. Three PRs existed that the workflow could not see. Repaired only when the captain asked the FO to check the mod.
3. **`state ready` / `state sweep` never ran**, so the session never converged with the remote state checkout.
4. **`«hooks.run»("startup")` never fired**, so `pr-merge`'s PR-pending scan and `comm-officer` injection never happened.
5. **The session ran the `0.27.0-pre1` contract while `pre8` was installed**, so `fo-gate-lifecycle` was absent from its loaded set. Four gates were "presented" in chat and never `gate prepare`d. Durable readiness stayed `needs-preparation` for hours while the FO reported them to the captain as awaiting decision.
6. **An orphaned worker kept running** — the ideation ensign for a withdrawn entity. A roster reconcile at startup would have reclaimed it.

Item 5 is the sharpest: the FO's prose and durable state disagreed, and only the binary knew. That is the same defect class as two entities already in flight — `conn-delegated-approver-attribution` (a grader that trusted the agent's self-description) and `merge-guard-requires-preceding-report` (an FO that trusted a completion message over durable state) — one level up. Here the FO trusts **its own summary** over the binary.

## Why a prose rule will not fix this

The gate-attribution convention has lived in `skills/fo-gate-lifecycle/SKILL.md` since `aa04e95d8` (2026-07-25). Live First Officers recited it in their own final messages and still stamped `person:captain` — recorded in this repo's finding-9 audit note. Prose in the contract demonstrably does not hold against a behavior the session is already inclined toward.

Worse, the compaction summary carries an explicit counter-instruction: *"Resume directly — do not acknowledge the summary, do not recap, do not preface."* That is correct for conversational continuity and wrong for a stateful dispatcher. Any mechanism here must survive an instruction telling the session to keep going.

## Proposed approach

Bind exactly **one `«state.boot»()`** to the compaction boundary, before any other action. Nothing else.

The boot record already carries `mods`, `ready_gates`, `dispatchable`, `pr_state`, `team_state`, `state_backend`, `definition_dir`/`entity_dir`, and the binary version gate. Every one of the six failures above is answered by that single call. The contract does not need re-injecting; the state needs re-reading.

### Relationship to issue #595

#595 is this same problem filed for Codex, and it reaches the same diagnosis in its own words: a teammate must retain contract and authority "without trusting a lossy transcript summary." It records that the Codex surface exposes no compaction callback, and proposes a digest-pinned bundle of contract, adapter, README, entity state, worker identity, gates, PR, and CI.

This entity is the Claude-side instance and deliberately proposes far less. If Claude exposes a usable boundary, one boot call ships without any bundle. #595's bundle remains the right shape only where no callback exists at all.

## Ideation must settle

1. **Whether Claude Code exposes a usable compaction boundary, proven by a captured event rather than documentation.** This repo's own `hook-events.jsonl` contains exactly one `PreToolUse` record and no compaction evidence, so nothing local proves it today. Prove the boundary first; design second.
2. **Whether a hook can force an action before the next tool call, or only inject text.** Injected text is prose and fails for the reason above. If text is all the host offers, say so and record the boundary as unsupported — do not ship a prose rule dressed as a mechanism.
3. **Where the mechanism belongs** — a new mod hook point, the runtime adapter, or the binary. Prefer the smallest that holds.
4. **Whether a host-independent fail-closed tell can substitute.** If the FO must be able to prove it booted — for example by holding a boot record whose identity matches the current session — then a session that cannot prove it booted refuses gate and merge actions until it does. This needs no host callback and would cover Codex and Pi as well. Weigh it against the hook approach on necessity, not preference.

## Out of scope

The Codex boundary — #595 owns it. Any context-reinjection bundle. Post-compaction worker-identity recovery. Changing what Startup does; this entity changes only whether it is forced to run.

## Value

The First Officer mutates durable state, resolves gates, and drives merges. A compacted session doing that work on stale bindings is the highest-authority actor in the system operating on a summary it wrote about itself. The value is that the FO cannot act on workflow state it has not re-read.

## Ideation findings (2026-08-18, Claude Code v2.1.226)

The four questions the seed left open are settled, each on captured evidence — verbatim records in `force-boot-at-compaction-boundary/ideation-spike-evidence.md` beside this file.

**1. The boundary exists, captured two ways.** A controlled live session (tmux-driven scratch session, capture hooks registered via `--settings`, `/compact` issued after five turns) produced the full event sequence: `PreCompact{trigger:"manual"}` and `SessionStart{source:"compact"}` hook events, plus a durable `{"type":"system","subtype":"compact_boundary",…,"compactMetadata":{trigger, preTokens, postTokens,…}}` record in the session transcript. The incident session's own transcript carries the same boundary record (801,920 → 19,650 tokens, 2026-08-18T18:59:30Z, `trigger:"manual"`). The boundary is real, host-observable, and — decisively — durable on disk.

**2. Hooks cannot force an action, and the shipped prose injection demonstrably did not hold.** The hook surfaces at this boundary are: inject text into the next context (SessionStart `additionalContext`) or run host-side code (any command hook). Nothing forces the model's next tool call. The plugin has shipped a SessionStart `^compact$` prose-injection hook (`codex_session_start_compact.sh`) since before the incident — registered in the 0.27.0-pre1 hooks.json the incident session ran. At the incident boundary it produced no observable effect (zero injection text in the transcript, while 216 `stop_hook_summary` records prove hooks were active in that session), and the six failures followed. Whether it failed to fire or fired and was ignored, prose injection at this boundary is empirically insufficient — as the seed predicted.

**3. Session identity does NOT change at compaction — the naive fail-closed tell is dead.** `sessionId` is identical across the boundary in both the incident record and the controlled capture, and the transcript file continues. A guard of the shape "boot record must match the current session id" cannot detect compaction. (It still catches `--resume` and never-booted fresh sessions, which get new ids.)

**4. The tell survives in a stronger form, because the boundary is durable state.** Compaction leaves a timestamped `compact_boundary` record in the session's own transcript, and the binary can read it at verb time. `CLAUDE_CODE_SESSION_ID` is set per-session by the harness, visible to model-side Bash, and survives the boundary (evidence file §4). So the binary can answer "has this session compacted since it last booted?" from durable state alone — no hook callback, no model cooperation.

One more captured nuance: PreCompact fired at a `/compact` the host then refused ("Not enough messages to compact"). A PreCompact-based detector would mark sessions stale for compactions that never happened. The transcript record has no such false positive — it exists only when compaction completed.

## Design (settled): boot receipt + fail-closed guard, no hook

**SUPERSEDED by cycle 4 (captain rejection, see Feedback Cycles below) — kept
verbatim for provenance, not current truth.** The mechanism below shipped,
passed validation twice, and was rejected anyway: not a defect, a wrong
design choice. Two of the three grounds this section gives for rejecting a
hook do not survive inspection (see the Feedback Cycles entry); the current
design is "## Design (cycle 4, settled): SessionStart(compact) reminder
hook, no guard" further down, and the Acceptance criteria / Test plan /
Expected surface / Semantics / Doc diff sections below now describe that
design, not this one.

The seed said "bind exactly one `«state.boot»()` to the compaction boundary." Ideation refines where the binding lives: refusal-side, not callback-side. The first authority-bearing workflow effect after the boundary cannot proceed until a fresh boot runs — enforced by the binary, the one component that knew the truth in the incident.

**Boot receipt.** `spacedock status --boot --identify --json` (the shipped `«state.boot»()`) additionally writes a session boot receipt: one line, `{booted_at} {transcript_path}`, at `.spacedock/boot/{session_id}`. No JSON, no schema — the reader is ReadFile + Fields + time.Parse. Written only when session identity is resolvable (env `CLAUDE_CODE_SESSION_ID` present); silent no-op otherwise. The receipt is host scratch, not workflow state — the state checkout is untouched, and boot's "mutates nothing" contract still holds for workflow/entity state (contract wording gains a clarifying line; doc diff below).

**Guard.** The three authority verbs — `gate record`, `gate consume`, `merge guard` — run a shared preflight:

- No receipt for the current session id → refuse.
- Latest `compact_boundary` timestamp in the receipt's recorded transcript > `booted_at` → refuse.
- Refusal: distinct exit code (4 proposed; `state commit` owns 3 — implementation confirms 4 is unclaimed) and one stderr paragraph naming the condition and the exact remedy: re-run `${SPACEDOCK_BIN:-spacedock} status --boot --identify --json` and consume the fresh record. Re-running boot rewrites the receipt and clears the guard.
- `state commit` is NOT guarded: ensigns run it, and a subagent's environment carries the root session's id, so an FO compaction mid-flight would wrongly block every ensign commit. The same reasoning caps the set — the guard must never widen onto ensign-run verbs.

**Why exactly these three.** They are where captain authority or terminal irreversibility lands in durable state: a resolution recorded, a resolution applied, a merge finalized. A stale `gate prepare`, `new`, `status --set`, or `dispatch build` produces recoverable, git-visible, additive state and burns at most one worker cycle — and every such path still ends at a guarded verb before authority lands, so guarding them would add call sites without adding protection the choke points already give. In the incident replay, the wall fires at `gate record` — the moment the captain's chat approval needs stamping — before any resolution is recorded on stale bindings.

**Fail direction and degradation.** Identity unresolvable (no env — captains in plain terminals, Codex, Pi today) → guard silently no-ops: exactly today's behavior. Transcript missing/unreadable or format drift → fail open with one stderr warning line; malformed transcript lines are skipped. The guard fails CLOSED only on the one condition captured evidence supports. Declared limitation: a host transcript-format change fails open until the pinned fixture is refreshed.

**No escape hatch.** No env kill-switch, no bypass flag. A false refusal's remedy is one cheap idempotent boot — the intended action anyway. An override would hand the failure mode (a session inclined to keep going) its bypass, the same way prose failed.

**Why not the hook (necessity, not preference).** The guard alone delivers the value sentence — the FO *cannot* act on workflow state it has not re-read — for every workflow effect that flows through the binary. A hook adds a host-specific moving part that delivers at most "reminded", requires registration to survive plugin and host changes, produced nothing observable at the incident boundary, and (PreCompact) false-positives on refused compactions. The detector the guard needs is already durable state. Codex and Pi get the same guard architecture with detectors absent today (guard no-ops there); #595 owns the Codex boundary. The existing `codex_session_start_compact.sh` registration is left as-is — removing or fixing it is a separate decision.

**Residual risk (declared).** Actions that never touch the binary — the incident's hand-opened `gh` PRs — are not intercepted, and unguarded spacedock verbs can spend recoverable work on stale bindings. The window is bounded: the FO's first `gate record`, `gate consume`, or `merge guard` hits the wall before authority lands, forces the re-boot, and the re-read mods map is what tells the FO that PRs route through `pr-merge`. Closing the `gh` path itself is out of scope (and impossible binary-side).

**No further spike needed:** the design rests on three mechanisms, all exercised — the boundary record's existence and parse (captured incident + spike records, parsed during ideation), env session identity (echoed live post-compaction inside the spike session), and receipt-file round-trip (ordinary file I/O). The captured records seed the implementation's transcript fixture.

## Design (cycle 4, settled): SessionStart(compact) reminder hook, no guard

**Corrected three times within this cycle before landing, all three recorded here rather than silently folded in.** First: the FO hand-wrote `.claude/hooks/compact-boot-reminder.sh` and registered it in `.claude/settings.local.json` as a live spike — the captain called this out as FO product work outside FO write scope, and because `.claude/` is gitignored, any "it fired" observation from that spike would have proven something about a local hand-edit, not the shipped product. Both are reverted; they contributed the instruction TEXT this design uses, nothing else. Second: the initial build here added a brand-new `compact_boot_reminder.sh` as a second command alongside the pre-existing `codex_session_start_compact.sh` under the same matcher — which, once `.claude-plugin/plugin.json` activated the shared `hooks.json`, meant every session on either host would receive two reminder blocks. Caught before merge and consolidated to one script, one entry. Third, and most serious: the captain required a live spike proving the shipped mechanism delivers to MODEL CONTEXT, not documented platform behavior — the spike found the consolidated mechanism was **broken**, not merely unproven. `${PLUGIN_ROOT}` (inherited from the pre-existing Codex script) is Codex's token; Claude Code sets `CLAUDE_PLUGIN_ROOT` and never a bare `PLUGIN_ROOT`, so the command silently resolved to `/hooks/session_start_compact_reminder.sh` and errored non-blockingly on every real Claude compaction. Fixed with `${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT}}`, then re-verified live — see "How proven" tier 2 and the implementation stage report.

**WHERE.** Ship in the plugin's existing hook substrate, not a new one. `hooks.json` already declares a `SessionStart`/`^compact$` matcher (originally for `codex_session_start_compact.sh`), and `.claude-plugin/plugin.json` already carried `"hooks": "./hooks.json"` once — for the now-fully-removed dispatch-ack hooks (`042f926db` added it, `6b2ae9787` removed it during pre4 live-release triage, `e2f4e90e6` deleted the ack machinery itself). Re-adding the field retraces a path already proven live for Claude Code; it is not a new mechanism. `hooks.json` is shared by both plugin manifests, so the fix is host-neutral by construction: `codex_session_start_compact.sh` is renamed to `session_start_compact_reminder.sh`, rewritten with this entity's reminder text, and registered as the SOLE command under the existing matcher — not a second one. No new install path, no `spacedock doctor` surface, no mod — the smallest mechanism that reaches the value, because the substrate already exists and was already live before this entity.

**WHAT.** The instruction text originates from the (reverted) live spike, carried over with one tightening — the boot command reads `${SPACEDOCK_BIN:-spacedock}` per the shared launcher convention. It names the boundary, names the one command to re-run (`status --boot --identify --json`, read whole), and names four specific things to distrust — presented gates, live workers, the binary path, the contract version — rather than the prior script's generic "reread the contract," which the ideation-spike-evidence negative findings show never observably fired anywhere (for a reason this cycle also found: see the implementation stage report's finding on the contractlint test). The script's output shape (`hookSpecificOutput.additionalContext` JSON, not plain stdout) is kept unchanged from the script it replaces, to avoid pairing a text change with an unverified format change on the one host (Codex) where this exact shape was live-verified for command resolution (`e143969b8`, codex-cli 0.144.6). The plugin-root TOKEN, unlike the output shape, was NOT safe to leave unchanged — see the correction above.

**HOW PROVEN — three tiers, stated separately rather than dressed as one proof.**
1. *Mechanical (tested this cycle).* The script emits the exact reminder JSON only when `SPACEDOCK_BIN` is set (launcher-marked session) AND `source=compact` on stdin, and stays silent (exit 0, empty stdout) on every other combination — unset/empty `SPACEDOCK_BIN`, every other `source` value, empty/malformed/non-string-source stdin. `hooks.json` names the matcher and the single script path; both `.claude-plugin/plugin.json` and `.codex-plugin/plugin.json` activate `hooks.json`; the plugin-root fallback template resolves to the correct absolute path under `/bin/sh` for both hosts' real environment shapes (`TestSessionStartCompactReminderPluginRootFallbackResolves`, which actually executes the template rather than string-matching it — a JSON-equality check would not have caught the `${PLUGIN_ROOT}` bug, since the broken template was syntactically valid JSON and a syntactically valid shell command). Every one of these properties is falsifiable — each was broken by hand and watched fail for the right reason, then restored byte-identically and watched pass (implementation stage report cites the evidence).
2. *Platform capability — EMPIRICALLY CONFIRMED, not relied on from documentation.* The captain required this, correctly: a `SessionStart` hook's stdout becoming `additionalContext` for the resumed turn is Claude Code's documented general hook contract, but Codex's sibling mechanism failed at exactly this step (a `systemMessage` that reached only the UI, never the model) — so documentation was not good enough evidence here. A live spike (tmux, `claude --plugin-dir` pointed at a disposable copy of the exact committed plugin tree, a sentinel token in the hook's `additionalContext`, a real `/compact`) reproduced the sentinel verbatim on the model's next turn with no tool calls — proof of delivery through the actual shipped mechanism, not an assumption. The same spike is what caught the `${PLUGIN_ROOT}` bug in tier 1: the first run errored and delivered nothing; only after the fix did the sentinel arrive. Full transcript in `ideation-spike-evidence.md` section 6. Manual `/compact` is covered; auto-compact was not reached within the spike's budget (declared, with reasoning, not skipped silently). This entity now supersedes the still-`backlog` `claude-post-compaction-contract-reload` entity's proposed approach AND discharges the live-model-context verification its AC-1 asked for and never ran.
3. *Obedience (not provable by any test).* This wires DELIVERY, empirically confirmed, not OBEDIENCE. Whether the FO actually re-runs Startup having read the injected text cannot be asserted by a fixture — it is the same class of claim the FO's other advisory conventions (deferred-load contracts) already carry unverified. Before this entity the instruction could not arrive at all; after it, it demonstrably can — that is the whole and only claim. Honest proposal: the next live compaction inside a real workflow session is the journey proof, the same way the incident itself was the negative proof.

**NON-CLAUDE HOSTS.** Codex gets the same script, registered through its own pre-existing `.codex-plugin/plugin.json` wiring (unchanged), now carrying this entity's reminder text instead of the old generic line. This is a text improvement, not a new capability claim: Codex's own limitation stands as previously recorded — a live probe found a Codex `systemMessage` reaches only the operator-facing UI, not the model — so Codex does not get the same value this delivers to Claude, only a better-worded version of what it already had. Issue #595 owns any further Codex compaction-recovery design; this entity does not attempt to close that gap.

## Acceptance criteria

**Supersedes the cycle-1..3 receipt+guard ACs above** (preserved in those
cycles' stage reports for provenance). The mechanism changed from a binary
refusal to a host reminder, so "guarded verb," "exit 4," and "receipt" no
longer describe the shipped artifact.

1. **(Mechanism — narrowed at cycle 8, captain-approved)** The script owns ONE gate, `SPACEDOCK_BIN`: with it set the script emits the declared reminder JSON regardless of stdin (it does not read stdin at all), and with it unset or empty it emits nothing and exits 0. Source filtering is the host matcher's job, asserted by AC 2 — the script re-implementing it with a greedy `sed` is what made a nested `"source"` object suppress the reminder silently. Test: table-driven subprocess test invoking the real committed script with an explicit, non-inherited environment, including that nested payload.
2. **(Installed, exactly once, and the path actually resolves)** `.claude-plugin/plugin.json` and `.codex-plugin/plugin.json` both activate `hooks.json`; `hooks.json` registers exactly ONE command across all `SessionStart` entries, naming `${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT}}/hooks/session_start_compact_reminder.sh`, and the matchers declared there accept exactly `compact` and `clear` of the host source enum `startup|resume|clear|compact|fork`; and that template resolves to the correct absolute path under each host's real environment shape, not merely a well-formed JSON string. Test: parse all three files and assert the fields, the single-command count across every `SessionStart` entry, and the accepted-source union computed by compiling each declared matcher as a regexp — exercised, not string-compared, so an added permissive sibling entry cannot hide behind a matcher-keyed lookup; additionally run the command template through `/bin/sh` under a Claude-shaped and a Codex-shaped environment and assert the resolved path — the check that would have caught (and did catch, live) the bare-`${PLUGIN_ROOT}` bug a string-equality test cannot see. Each assertion proven load-bearing by breaking it by hand and watching the test fail.
3. **(One script, not two)** The pre-existing Codex-only script is renamed and rewritten in place rather than duplicated; a regression that reintroduces a second script or a second hooks.json entry is caught. Test: `TestSessionStartCompactReminderHookIsWired`'s exactly-one-command assertion plus its explicit check that the old filename no longer exists.
4. **(Delivery, empirically confirmed; obedience declared, not tested)** A live spike confirms the reminder reaches the resumed model's own context through the actual shipped mechanism (sentinel reproduced verbatim, no tool calls, after a real `/compact`) — this is proven, not assumed from platform docs. Whether the reminder changes FO *behavior* at a real compaction boundary is a separate claim this entity does NOT assert or test — see the Design section's "How proven" tier 3. This AC exists so validation credits the delivery proof correctly and does not go looking for an obedience test that cannot honestly exist.

## Test plan

- `TestSessionStartCompactReminderHookGate` (table-driven, real script via subprocess, explicit non-inherited env): launcher-marked+compact → reminder JSON, checked by property; no-SPACEDOCK_BIN+compact → silent; launcher-marked + the nested-`"source"` payload that the deleted in-script check suppressed → reminder JSON, since the script gates on `SPACEDOCK_BIN` alone.
- `TestSessionStartCompactReminderHookIsWired`: parses `hooks.json` for exactly one `SessionStart` command across all entries and for the accepted-source union of every declared matcher, compiled as a regexp (`clear` and `compact`, nothing else; a matcher that fails to compile fails the test), and both `.claude-plugin/plugin.json` and `.codex-plugin/plugin.json` for `hooks: "./hooks.json"`; asserts the old `codex_session_start_compact.sh` path no longer exists.
- `TestSessionStartCompactReminderPluginRootFallbackResolves`: executes the live hooks.json command template through `/bin/sh` under a Claude-shaped (`CLAUDE_PLUGIN_ROOT` set) and a Codex-shaped (`PLUGIN_ROOT` set) environment, asserting the resolved absolute path for each — the regression test for the bug the live spike found.
- `TestDispatchAckMachineryIsAbsent` (existing, updated): expects `.claude-plugin/plugin.json` to activate hooks again (it previously asserted the opposite — see the implementation stage report's finding); still asserts `PreToolUse`/`SubagentStart` and every dispatch-ack string marker stay absent.
- Live spike (not a repeatable CI test, recorded as evidence): `ideation-spike-evidence.md` section 6 — the sentinel-in-additionalContext, tmux-driven, `--plugin-dir`-wired proof that stdout reaches the resumed model's context, including the broken-then-fixed run. Manual `/compact` covered; auto-compact declared infeasible within budget, with reasoning.
- No test claims obedience. AC-4 says why: obedience is not a property a fixture can hold, and this entity does not overclaim past delivery.

## Expected surface

Re-estimated from scratch per instruction, then twice more as the shape changed — report the real number each time, not the smaller one that would look better. `git diff --numstat` against `f64c733ef` (cumulative for this whole cycle-4 restart) is net **+378** across **7 changed paths** (+384/−6). Three sub-rounds: the first shipped shape was +230/6 files; consolidating to one script/host added +42; the live spike's required fix (the plugin-root fallback plus its dedicated resolution test and the doc/design writeup of why it mattered) added the remaining +106 — the single largest sub-round, because the fix itself is one line but proving it right takes a real test and a real explanation.

- Product +19 (insertions 24, deletions 5): `.claude-plugin/plugin.json` (+1), `hooks.json` (+1/−1, net 0 — the fallback fix is a same-line edit), the old `hooks/codex_session_start_compact.sh` (−4, superseded by the rename), `hooks/session_start_compact_reminder.sh` (+22, new).
- Tests +294: `internal/contractlint/session_start_compact_reminder_hook_test.go` (+288, new — the gate matrix, the wiring check, and the plugin-root resolution test that actually executes the template under `/bin/sh`), `internal/contractlint/dispatch_ack_removal_test.go` (net +6, unchanged since the first sub-round).
- Docs +65: `docs/runtime-support.md` (up from +21 then +39 — the final version documents the live-confirmed delivery claim, the plugin-root bug and fix, and the auto-compact/trust caveats, in place of relying on platform documentation).

Tolerance: none needed (estimate = actual, reported honestly after the shape changed three times); 1.5x net for any further correction round.

Files (7 paths / 5 logical files): `.claude-plugin/plugin.json`, `hooks.json`, `hooks/codex_session_start_compact.sh` (deleted, renamed to the next path), `hooks/session_start_compact_reminder.sh` (new), `internal/contractlint/session_start_compact_reminder_hook_test.go` (new, replaces the deleted `compact_boot_reminder_hook_test.go`), `internal/contractlint/dispatch_ack_removal_test.go`, `docs/runtime-support.md`.

## Semantics that may change (declared)

- Runtime behavior: Claude Code sessions launched through the plugin now receive a `SessionStart` hook injection at `source=compact` and, since cycle 8, at `source=clear` for the first time — previously zero Claude-side hooks fired (the only prior `hooks.json` entry was Codex-only, per `TestDispatchAckMachineryIsAbsent`'s prior `wantHooks:false` expectation for `.claude-plugin/plugin.json`, AND was independently broken by the `${PLUGIN_ROOT}` bug even had that field been set). Codex sessions receive the same reminder text they always got a weaker version of, through unchanged wiring. No workflow state, mutation, or exit-code behavior changes; this is text injected into model/UI context, nothing durable.
- Plugin manifest: `.claude-plugin/plugin.json` gains a `hooks` field (previously present for the now-removed dispatch-ack hooks, absent since `6b2ae9787`).
- Hooks.json command template: `${PLUGIN_ROOT}` → `${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT}}` — fixes a real, previously-undetected bug (see Design section), not a stylistic change.
- Hooks.json matcher (cycle 8): `^compact$` → `^(compact|clear)$`, so the reminder also fires at a `/clear`. The script's opening line went from `COMPACTION BOUNDARY` to a boundary-neutral one to match; the rest of the payload is unchanged.
- Stored format: none — no file is written by this mechanism.
- Command grammar: unchanged — no new flags, verbs, or exit codes.
- File rename: `hooks/codex_session_start_compact.sh` → `hooks/session_start_compact_reminder.sh`. Confirmed nothing outside `hooks.json` and this entity's own prose referenced the old name.

## Doc diff (applied at implementation)

Applied directly rather than staged for later, since the change is small. `docs/runtime-support.md` gained a "Compaction boundary reminder (Claude Code and Codex)" subsection after "Launcher binary propagation through wrappers" (see that file for the exact text, including the SPACEDOCK_BIN-gate rationale and the Codex format-preservation note). No change to `skills/first-officer/references/first-officer-shared-core.md` — the hook changes no FO-contract obligation, only host-side context injection, so there is nothing for the shared core to declare, and the file stays well under its 26,900-byte contractlint cap.

## Stage Report: ideation

- DONE: Prove the Claude compaction boundary exists with a CAPTURED EVENT, not documentation — this repo's own hook-events.jsonl holds exactly one PreToolUse record and no compaction evidence, so nothing local proves it today. If no usable boundary exists, record it as unsupported rather than shipping a prose rule dressed as a mechanism.
  Captured live on Claude Code v2.1.226: PreCompact + SessionStart(source:compact) hook events and the durable `compact_boundary` transcript record, plus the incident session's own boundary record — verbatim in `force-boot-at-compaction-boundary/ideation-spike-evidence.md`.
- DONE: Decide between the host-hook mechanism and the host-independent fail-closed tell (the FO must hold a boot record whose identity matches the current session, else it refuses gate and merge actions until it re-boots) on necessity, not preference — the second needs no host callback and would cover Codex and Pi too.
  Fail-closed guard chosen; the naive session-identity tell is disproven by capture (sessionId survives compaction), so the guard compares a boot receipt against the durable `compact_boundary` record instead. The hook is rejected on necessity: advisory-only, produced nothing at the incident boundary, and PreCompact false-positives on refused compactions (captured).
- DONE: Declare net LOC change and file count with a REALISTIC test budget. Today three consecutive entities blew their estimate and every single overrun was under-budgeted test-side work; product code landed near its number each time. Budget test code at roughly twice your first instinct and say so explicitly.
  Net +540 across ~12 files (≈ +560 / −20), tolerance 2x net; breakout product +145 / tests +340 (first instinct ~170, explicitly doubled) / docs +55.

### Summary

Ran a live capture spike (tmux-driven Claude 2.1.226 session with PreCompact/SessionStart capture hooks) and proved the compaction boundary three ways: hook events fire, the transcript records a durable timestamped `compact_boundary`, and session identity survives the boundary — which kills the naive session-id tell and the seed's hook assumption in one stroke. Settled the design as a binary-side fail-closed guard: `status --boot` writes a per-session receipt, and FO-held mutating verbs refuse until the receipt postdates the latest boundary, with no hook, no escape hatch, and honest fail-open degradation on non-Claude hosts. Declared surface +540 net across ~12 files with the test budget explicitly doubled per today's calibration.

## Stage Report: ideation (cycle 2)

- DONE: Prove the Claude compaction boundary exists with a CAPTURED EVENT, not documentation — this repo's own hook-events.jsonl holds exactly one PreToolUse record and no compaction evidence, so nothing local proves it today. If no usable boundary exists, record it as unsupported rather than shipping a prose rule dressed as a mechanism.
  Unchanged from cycle 1; the gate accepted the capture — verbatim records in `force-boot-at-compaction-boundary/ideation-spike-evidence.md`.
- DONE: Decide between the host-hook mechanism and the host-independent fail-closed tell (the FO must hold a boot record whose identity matches the current session, else it refuses gate and merge actions until it re-boots) on necessity, not preference — the second needs no host callback and would cover Codex and Pi too.
  Unchanged from cycle 1; the gate accepted the receipt-plus-guard mechanism. Cycle 2 narrows its surface only: the guard covers the three authority verbs (`gate record`, `gate consume`, `merge guard`), with the justification recorded in the body's "Why exactly these three".
- DONE: Declare net LOC change and file count with a REALISTIC test budget. Today three consecutive entities blew their estimate and every single overrun was under-budgeted test-side work; product code landed near its number each time. Budget test code at roughly twice your first instinct and say so explicitly.
  Revised to net +245 across ~8 files (≈ +255/−10; product +95 / tests +120 / docs +30), tolerance 2x net. The gate withdrew the doubling instruction after the captain rejected +540, so the test figure is the honest proof cost — a verdict table with inline captured-record constants, three-verb refusal wiring in the existing CLI harness, one boot-writes-receipt case — not a multiplied one, and no AC lost its falsifying test.

### Summary

Surface-only revision per the gate: same spike evidence, same mechanism. The guard set narrowed from eight mutating verbs to the three where captain authority or terminal irreversibility lands (`gate record`, `gate consume`, `merge guard`) with the cap rationale recorded; the receipt shrank from a JSON format to one parsed line at `.spacedock/boot/{session_id}`; fixture files became string constants in a table-driven test. Estimate moved from +540/12 files to +245/8 files with the withdrawn doubling called out, and the body, ACs, test plan, and doc diff were updated together to match.

### Feedback Cycles

- Cycle 1: SURFACE OVERRUN DISCLOSED (no rejection) — implementation self-report; surface 14 files/net +642 vs estimate 8 files/net +245 (262%, past the 2x ceiling of +490); AC unchanged. Two causes, both named as unanticipated at ideation rather than scope creep: (1) `internal/status` is under a repo-enforced host-neutrality lint forbidding any `.claude` string literal, so the transcript-path glob relocated into `claudeteam`'s new TranscriptProbe seam and threads through gatherBoot/printBoot/runRead/dispatch/NativeRunner; (2) three existing tests forward the live process's real CLAUDE_CODE_SESSION_ID into fixtures carrying no boot receipt — harmless before this guard, a spurious red after. FO note: cause 2 is a pre-existing test-hermeticity bug this entity fixed in passing; cause 1 is a new cross-package seam. Both are validation's job to confirm as necessary rather than convenient, since a new seam and an unrelated bug fix are exactly what a surface overrun can hide.

- Cycle 2: REJECTED — validation, one material finding (narrow fix, not a design reset); surface 14 files/net +642 vs estimate 8/+245 (262%, past the 2x ceiling of +490); AC unchanged. Material: `bootguard.go:132-135` refuses on EVERY `os.ReadFile` error, not just `ErrNotExist`, so an unwritable or unreadable `.spacedock/boot` refuses all three guarded verbs permanently while `status --boot` exits 0 and silently swallows the failed write — an unrecoverable loop against a design that promises the remedy is one cheap idempotent boot. Reproduced end-to-end three ways (mode 000 dir, read-only project root, receipt path as a directory). Breaks AC-2 in both clauses. The same file fails OPEN two lines later on a malformed record, so the fail-closed branch is inconsistent with its own neighbours. Fix is one condition. Three deferred risks recorded with promote conditions: cwd/workflow git-root divergence (boot writes at `FindGitRoot(definitionDir)`, guard reads at `FindGitRoot(cwd)`); `booted_at` written at second precision against millisecond boundaries; a transcript path containing a space silently disabling detection via `strings.Fields`. FO note for the record: validation proved the overrun's causal account wrong by ~4x — the two disclosed causes are 21% of it, the three new guard files are 78%, and tests landed at +395 against the +120 re-estimate. Cycle 1 budgeted +340 under the FO's doubling instruction; the captain withdrew it and the withdrawn figure was the accurate one. That is on the FO, not the worker.

- Cycle 4: MECHANISM REJECTED — captain, after the cycle-3 validation gate ("just add the instruction in the current compact session start. let's restart the worktree. send this back"). Not a defect in the delivered code: validation PASSED it twice with no material finding. The DESIGN was wrong, and the FO approved the ideation that chose it. Ideation rejected a host hook on three grounds and two do not survive inspection: "produced nothing at the incident boundary" is circular, because no hook was installed at the incident and an uninstalled hook produces nothing by definition; and "PreCompact false-positives on refused compactions" is true of `PreCompact` but NOT of `SessionStart(source:compact)`, which the same spike capture shows firing exactly once, after a completed compaction. That left "advisory-only" as the only real objection, against the captain's standing counter-evidence: the FO's entire deferred-load contract is advisory ("load fo-write-core before any mutation") and fires reliably at its triggers. The one genuine argument FOR the binary guard — a hook needs per-user settings.json install, whereas the binary protects everyone who has it — was never made by anyone, and spacedock already installs a plugin and marketplace entry. Cost of the wrong choice: 968 net lines across 13 files against an approved +245, three correction rounds, a /tmp receipt with a proven silent symlink-follow, a new git subprocess on every guarded verb at 12.75 ms/op, and three deferred risks. PR #737 closed unmerged; the pending terminal approval superseded via `merge guard --rework`; the worktree reset to `main` with the abandoned work preserved on local branch `abandoned/4c-receipt-guard-approach`. RETAINED AND NOT REOPENED: the diagnosis, the live capture evidence (including this repository's own incident boundary), and the three-verb scope. The hook was installed live in this session at `.claude/hooks/compact-boot-reminder.sh` before this line was written, so the next compaction exercises it.

- Cycle 5: REJECTED — captain, one material finding on the reminder text (narrow fix, not a design reset); surface 7 files/net +202 vs estimate 8 files/+245 (82%); AC unchanged. Material: the injected recovery instruction says "re-run the Startup procedure". Startup terminates at `«interaction.boundary»()`, whose interactive branch presents a summary, STOPS for input, and explicitly forbids auto-dispatch — so a compacted FO that obeys the text demotes an engaged session to a cold one and makes the captain re-issue `engage`, paying for the FO's amnesia. The contract already carries the correct recovery in the shared core's `## Compaction continuity` "After" clause: re-satisfy each load precondition and state read at its existing trigger before the next workflow effect. Lazy re-binding at triggers, then resume the loop — never a re-greet. Reproduced live in this session at a manual compaction boundary: the FO re-ran the full Startup, ended at the greet, and asked the captain to say `engage` again; the captain's correction ("after compaction the user shouldn't need to say engage again") is the finding. Fix is the `additionalContext` string only: instruct re-binding at triggers and resuming the loop, and do not name the Startup procedure. WITHDRAWN from the prior round's proposals: the `SPACEDOCK_BIN` clause. Startup step 1 already falls back to bare `spacedock` on `$PATH` when `SPACEDOCK_BIN` is set but not executable, and that path was exercised live this session — `SPACEDOCK_BIN` pointed at a deleted `0.27.0-pre7` cask path and the fallback resolved `pre8`. FO note: cycle 4's closing sentence above claims the hook was installed live at `.claude/hooks/compact-boot-reminder.sh`; that hand-install was outside FO write scope, was reverted, and `.claude/` is gitignored — the claim is stale and no hook fired at this session's boundary. What IS proven and not reopened: delivery. The installed `0.27.0-pre1` package reproduces both original bugs in the artifact — `.claude-plugin/plugin.json` carries no `hooks` key, and `hooks.json` points at `${PLUGIN_ROOT}/hooks/codex_session_start_compact.sh` — and the sentinel spike confirms the fixed wiring injects text into a post-compaction turn with no tool calls.

- Cycle 6: REJECTED — captain, one material finding on the script's in-script source filter (narrow fix, not a design reset); surface 7 files/net +202 vs estimate 8 files/+245 (82%); AC unchanged. Material: lines 4-5 (`src=$(cat | sed ...)` and `[ "$src" = "compact" ]`) re-implement a filter the host's `^compact$` matcher already applies on both hosts, and they do it with a greedy `.*` that takes the LAST `"source"` on the line. Reproduced by executing the committed script: `{"source":"compact","extra":{"source":"startup"}}` exits 0 with EMPTY stdout — the reminder is silently suppressed by a nested object. An embedded `\"source\"` inside a string value is safe, because JSON escapes those quotes, so the exposure is nesting rather than message content. These two lines are also the diff's only Codex-silence risk, recorded as cycle 4's deferred risk: no captured Codex `SessionStart` payload existed in the repo when they were added. That risk is now retired on evidence — the shipped Codex binary carries `hooks/src/events/session_start.rs`, the `source` field, and `compact` among its enum values, and the captain's own live Codex transcript shows the hook firing and its text reaching the model. So the check protects nothing the matcher does not already cover, and it introduces a silent-failure mode in the one mechanism whose purpose is to not fail silently. Fix: delete both lines (script 6 -> 4) and move source filtering entirely to the host matcher, which `TestSessionStartCompactReminderHookIsWired` already pins as exactly one entry at `^compact$`. Known cost, accepted: the gate test's third row (`launcher-marked + source=startup -> silent`) becomes false and must be rewritten to state that the matcher owns source filtering; the script alone no longer discriminates, so a host that registered it without a matcher would fire on every `SessionStart`. Both hosts honour the matcher today and the wiring test pins it.

- Cycle 7: REJECTED — captain at the validation gate, two coupled findings (narrow fix, not a design reset); surface 7 files/net +200 vs estimate 8 files/+245 (82%); AC narrowed: AC 1's source-filter clause is rewritten by this cycle. Material 1, the cause of cycle 7 validation's one-sided-cover finding: `hooks.json` declares a `matcher` and no test exercises it. `sessionStartCompactCommand` opens with `if entry.Matcher != "^compact$" { continue }`, so the matcher is a LOOKUP KEY for finding its own entry, never a value under test. A second `SessionStart` entry is skipped before it can be counted, which is why the whole suite stays green on one. Note the validator's mutant used matcher `"*"`, which does not compile as a Go regexp; the realistic dangerous mutant is `.*`, valid and matching all five source values. Remedy: iterate EVERY `SessionStart` entry, compile each declared matcher, and assert the union of source values it matches equals exactly the intended set. That covers a second permissive entry, a widened matcher, and a wrong matcher in one assertion, and it makes the declared matcher exercised rather than string-compared. Material 2, a widening the captain called: the matcher must be `^(compact|clear)$`, not `^compact$`. `clear` is also a context-loss boundary — the history holding the loaded contract is gone, and the model does not know it was mid-workflow — so the reminder is at least as necessary there as after a compaction. `startup` boots normally and `resume` restores the transcript, so neither needs it. Measured: `^(compact|clear)$` matches exactly `clear` and `compact` against the enum `startup|resume|clear|compact|fork`. Consequence the fix must carry: the payload opens `COMPACTION BOUNDARY`, which is wrong on a `clear`, so the wording becomes boundary-neutral while keeping the mid-loop instruction and the four distrust items. The in-script `source` check stays deleted: with the matcher exercised by test, the host owns the filter and the script stays four lines.

## Stage Report: implementation

- DONE: AC-1 measuring: in the incident-replay scenario, 0 of 3 guarded verbs succeed before re-boot, each exiting non-zero with the boot-stale message — against a baseline of 3 of 3 on today's binary.
  internal/cli/bootguard_wiring_test.go `TestBootGuardWiring{GateRecord,GateConsume,MergeGuard}` each drive the real CLI with a resolvable session id and no receipt: exit 4, boot-stale stderr, entity byte-unchanged. The 3-of-3 baseline was measured directly, not assumed: commit 8ba85f4df's two `BootGuardRefuse` call sites were disabled in place and the same three tests re-run, all reporting `exit=0` (armed/recorded/consumed succeeded) before the wiring was restored byte-identical.
- DONE: AC-2: after status --boot re-runs, every refused verb succeeds unchanged; and sessions with no boundary or no resolvable identity behave bit-identically to today, with existing golden fixtures passing unmodified.
  The same three wiring tests continue past the refusal, write a fresh receipt, and assert exit 0 with the real success output. `TestBootGuardVerdictMatrix`'s "no resolvable session identity" and "missing transcript" rows assert pass (fail-open). `go test ./...` and `go test ./... -race` are green repo-wide except one pre-existing unrelated failure (`TestCodexResolveManifestAgainstInstalledHost`, a stale local `~/.codex` install artifact, reproduced identically on this branch before any of this work landed).
- DONE: AC-3: the verdict derives from durable state ONLY — deleting the receipt or appending the boundary record flips refusal/pass with no conversational input. Prove the preflight is actually called: the wiring test must fail if it is not.
  `TestBootGuardVerdictMatrix` (internal/status/bootguard_test.go) is the durable-state-only proof: 7 rows — no receipt, fresh, stale, missing transcript, no env, malformed transcript lines, malformed receipt — using the incident's captured `compact_boundary` record verbatim, no fixture files. The wiring's falsifiability (not the verdict logic's) was proven by hand per AC-1's evidence above: disabling the call sites turned all three CLI wiring tests red with `exit=0, want 4`.
- DONE: Report actual net surface against the approved +245 across 8 files, broken out product / tests / docs.
  Actual: net +642 across 14 files (+665/−23), over the declared 2x tolerance (+490). Product +229 (bootguard.go +171 new; +26 in claudeteam.go for the TranscriptProbe seam; +23 net wiring across cli.go/boot.go/handlers.go/native_runner.go). Tests +395 (bootguard_test.go +225; bootguard_wiring_test.go +127; +43 net fixing a hermeticity bug in 3 existing test files). Docs +18. Both drivers of the overrun were unanticipated at ideation, not scope creep on the mechanism: (1) internal/status is under a repo-enforced host-neutrality lint (internal/hostneutrality) forbidding any `.claude` string literal — the transcript-path glob had to relocate into claudeteam's TranscriptProbe seam (mirroring TeamStateProbe) and thread through gatherBoot/printBoot/runRead/dispatch/NativeRunner; (2) three existing tests (merge_test.go, gate_ceremony_count_test.go, recorded_gate_lifecycle_test.go) forward the live process's real `CLAUDE_CODE_SESSION_ID` into fixtures carrying no boot receipt — harmless before this guard, a spurious red after, caught by running the full suite inside this live session. Neither is trimmable without giving up the host-neutrality invariant or suite hermeticity; no test was cut to fit the number.

### Summary

Shipped the settled design unchanged: a one-line session boot receipt from `status --boot`, and a fail-closed preflight on `gate record`/`gate consume`/`merge guard` that refuses (exit 4) only on the two conditions durable state can prove, failing open on every other degradation. All four ACs are backed by tests verified falsifiable by hand (disabling the wiring turned the CLI tests red; restoring it byte-identical turned them green again). The declared surface was exceeded — +642 net across 14 files against +245/8 (2x tolerance +490) — by two unanticipated but necessary corrections (a host-neutrality relocation, and a pre-existing test-hermeticity bug this guard's arrival exposed), not by scope creep on the guard itself; this is disclosed rather than trimmed, per the entity's own standing instruction.

## Stage Report: validation

- DONE: AC-1 and AC-3: independently reproduce the falsifiability proof. Disable the two BootGuardRefuse call sites yourself and confirm all three CLI wiring tests go red with exit=0, then restore byte-identically — do not accept the implementer's account of having done it. Then run the 7-row verdict matrix and confirm each row's verdict flips on the single durable-state edit it names.
  Reproduced on a throwaway clone at 8ba85f4df (implementation worktree never touched, still clean at 8ba85f4df). With both call sites short-circuited, all three `TestBootGuardWiring{GateRecord,GateConsume,MergeGuard}` fail `exit=0, want 4` and their stdout shows the mutation actually landing (`recorded gate=…`, `route=approved-awaiting-merge`, `armed: mod-block set`) — the 3-of-3 baseline measured, not assumed; restored with `git checkout --` (empty `git diff HEAD`) and all three green. AC-3: I re-drove the 7 rows as one sequence of single durable-state edits in one process — delete receipt→refuse, rewrite→pass, rewind `booted_at`→refuse, strip the boundary line→pass, re-append it→refuse, surround with garbage lines→verdict holds, repoint at a missing transcript→pass, corrupt the receipt→pass, drop only the env key→pass, restore it→refuse. Every flip is driven by file bytes or one env var. End-to-end with the built binary: all three verbs exit 4 after a real appended `compact_boundary`, the entity's `gates` block stays at 0 attempts / 0 resolutions, `gate prepare` and `status --set` still exit 0 (guard set is exactly the three), and re-running boot restores all three to pass.
- FAILED: AC-2 fail-open, weighted above everything else: this guard fronts gate record, gate consume, and merge guard, so a wrong refusal stops all delivery. Build the degradation matrix the implementation did not: unreadable receipt directory, receipt with a future timestamp, transcript present but empty, transcript with a compact_boundary lacking a timestamp, two boundary records where the newer is malformed, concurrent boot writes, and a session id that resolves to a path outside the expected root. Each must FAIL OPEN or refuse with a correct reason — never refuse spuriously.
  Built as 15 rows plus a concurrency hammer and a clock-precision probe. 11 rows fail open correctly: future `booted_at`, empty transcript, boundary with no timestamp, boundary with an epoch-millis timestamp (format drift), newer-malformed/older-valid pairs (both directions), transcript path that is a directory, and all four path-escaping session ids (`..`, `.`, `../../etc/passwd`, backslash form) — no traversal, no identity, no-op. 8 concurrent `writeBootReceipt` writers against 4,000 interleaved reads produced 0 spurious refusals (truncate-then-write degrades through the fail-open branches). **4 rows refuse spuriously — finding 1 below, and it breaks AC-2 in both clauses.**
- DONE: Test the two overrun causes as necessity claims. Cause 1 (a new TranscriptProbe seam in claudeteam threaded through five call sites) is a new cross-package mechanism: confirm the host-neutrality lint genuinely forbids the simpler in-place approach, and that the seam mirrors TeamStateProbe rather than inventing a second pattern. Cause 2 (three existing tests forwarding the live CLAUDE_CODE_SESSION_ID into receipt-less fixtures) is a pre-existing hermeticity bug fixed in passing: confirm it is real, that the fix is minimal, and that it does not mask a case where the guard SHOULD have refused.
  Cause 1 stands. `internal/hostneutrality/host_neutrality_test.go` AST-scans `../status` non-test source and fails on any STRING literal containing `.claude`, so `TranscriptPath`'s `filepath.Join(home, ".claude", "projects", …)` cannot live in `internal/status`; the seam is a `func` type in `claudeteam` injected as a `NativeRunner` field and threaded to the point of use — the same shape as the existing `TeamStateProbe`, not a second pattern, and the five signature changes are the same chain `probe` already traverses. Cause 2 is real and necessary, proven by reverting it: with the three `hermeticEnv()`/`cmd.Env` edits undone and my live `CLAUDE_CODE_SESSION_ID` present, `TestMergeGuardEndToEndArmFinalize`, `…RefusalPropagated`, `…PublishesArchiveVisibleToFreshHost`, `…InterruptedArchiveRebase`, `…SiblingDirtNonFastForward`, `…NoOriginReportsLocalOnly` all go red on the boot-guard refusal. It masks nothing: the fix strips exactly one variable, and the guard's own refusal tests use a pinned `wiring-proof-session` env built from a literal slice, not `os.Environ()`, so they are untouched by it. Coverage is complete — the full suite passes inside this live session with the ambient session id set, and no boot receipt exists in the repo to mask a gap.
- DONE: Report actual net surface and state whether the 262% overrun is confined to the two disclosed causes or whether anything else rode along.
  Actual net +642 across 14 files (+665/−23) confirmed by `git diff --numstat`; overrun vs the approved +245 is **+397**. It is **not** confined to the two disclosed causes: cause 1 (seam + threading) is net **+39**, cause 2 (hermeticity) net **+43** — together **+82, 21% of the overrun**. The other **+523 net sits in the three new guard files** against roughly +215 budgeted for them, i.e. ~78% of the overrun. Split by bucket: product +229 vs +95 declared, tests **+395 vs +120**, docs +18 vs +30. Nothing unrelated rode along — I read all 14 file diffs and found no extra feature, behavior change, or opportunistic refactor; `bootguard.go` is 112 code lines against ~95 declared, so the mechanism landed near estimate and the excess is test volume (3.3x) plus comment density (49 of 171 lines). The report's conclusion ("not scope creep") is right; its causal account is wrong by ~4x. Worth the captain's eye: cycle 1 budgeted tests at +340 with the doubling explicit, the captain withdrew that instruction, cycle 2 re-estimated +120, and tests landed at +395 — the withdrawn figure was the accurate one.

### Findings

1. **Material — outcome defect, narrow fix. Any receipt-read error is read as "never booted", and the remedy the message names does not clear it.** `internal/status/bootguard.go:132-135` refuses on *every* `os.ReadFile` error, not just `ErrNotExist`. Reproduced end-to-end with the built binary: (a) `.spacedock/boot` at mode 000, (b) the project root read-only (`chmod 555` — the read-only-mount / full-disk / root-owned-`.spacedock` case), (c) the receipt path existing as a directory. In each, all three verbs exit 4, `status --boot` exits **0** while silently swallowing the failed write, and the retry refuses identically — an unrecoverable loop against a design that promises "a false refusal's remedy is one cheap idempotent boot". Fails `value-ac[AC-2]` in both clauses: boot re-runs and the verbs do *not* succeed, and a session with no boundary at all is refused where today it succeeds — not bit-identical. Fix is local: refuse only on `errors.Is(err, fs.ErrNotExist)`, fail open with a stderr warning otherwise (the direction the design already specifies for the transcript). Adding the resolved receipt path to the refusal text would make all three self-diagnosing.
2. **Deferred risk — cwd/workflow git-root divergence gives the same unrecoverable refusal.** Boot writes at `FindGitRoot(roots.definitionDir)` (`handlers.go:453`); the guard reads at `FindGitRoot(cwd)` (`bootguard.go:164`). Proved: cwd in a git worktree with `--workflow-dir` in the main repo puts the receipt in the main repo, `gate consume` from the worktree exits 4, and re-booting from that cwd never fixes it. Does not fire today — the FO always runs guarded verbs from the repo root, and ensigns run no guarded verb (confirmed by grep of `skills/ensign/`). Promotes to material the moment any documented or scripted flow invokes a guarded verb with a cwd outside the workflow's git root. Fixing finding 1 does not fix this. Cheap hardening: derive both sides from one root.
3. **Deferred risk — `booted_at` is written at second precision** (`bootguard.go:70`, `time.RFC3339`) while boundaries carry milliseconds and compare with `RFC3339Nano`, so a compaction completing up to ~1s *before* the boot reads as *after* it. Proved: boundary `12:00:00.500Z` vs boot `12:00:00.900` → spurious refusal. Blast radius one retry (the next boot clears it). One-token fix: `time.RFC3339Nano`.
4. **Deferred risk — a transcript path containing a space silently disables detection.** `strings.Fields` (`bootguard.go:136`) splits `…/My Projects/….jsonl` at the space, the transcript is not found, and the guard fails open — safe direction, but the guard is inert for any user whose Claude project path has a space. Fix: `strings.SplitN(line, " ", 2)`.
5. **Polish, no action asked.** `gate record --round` is guarded although the workflow defines it as a neutral recorder that "applies no gate or status change" — wider than the design's "a resolution recorded" rationale, but harmless since the FO is its only caller. `hermeticEnv()`/`recordedGateHermeticEnv()` are duplicated across two packages. `first-officer-shared-core.md` is now 26,509 bytes against contractlint's 26,900 cap (headroom 797→391) — green, but the next contract line there needs the cap raised. The wiring tests' post-reboot leg writes a transcript-less receipt, so it passes through the `len(fields) < 2` fail-open branch rather than a real `booted_at > boundary` comparison; the verdict matrix's "fresh" row covers the real comparison, so the AC is covered, but the wiring test proves slightly less than its comment claims.

### Summary

AC-1, AC-3, and AC-4 verify clean on independently reproduced evidence: I disabled both call sites myself and saw all three wiring tests go red at `exit=0` with the mutations landing, restored byte-identically, and re-drove the verdict matrix as single durable-state edits in one process. Both disclosed overrun causes hold as necessity claims — the host-neutrality AST lint genuinely forbids the in-place `.claude` literal and the seam mirrors `TeamStateProbe`; the hermeticity bug is real (reverting the fix turns six merge-guard tests red in a live session) and masks nothing. The overrun is not confined to those two causes: they are 21% of it, the three new guard files are 78%, and the driver is under-budgeted test volume — the exact failure mode cycle 1 predicted before its doubling instruction was withdrawn. Nothing unrelated rode along. One material finding blocks: the guard treats *any* receipt-read error as never-booted, so an unwritable or unreadable `.spacedock/boot` refuses `gate record`, `gate consume`, and `merge guard` permanently while `status --boot` reports success — the fail-closed-on-an-unproven-condition the entity declared it must never do, breaking AC-2 in both clauses, with a one-condition local fix. **Recommend REJECTED** on finding 1 alone; findings 2-4 are deferred risks with recorded promote conditions, and the receipt-plus-guard mechanism itself is sound — this is a narrow fix, not a design reset.

## Stage Report: implementation (cycle 2)

- DONE: AC-1 measuring: in the incident-replay scenario, 0 of 3 guarded verbs succeed before re-boot, each exiting non-zero with the boot-stale message — against a baseline of 3 of 3 on today's binary.
  Unchanged by this fix (finding 1 was an AC-2 defect); re-verified post-fix: `internal/cli` `TestBootGuardWiring{GateRecord,GateConsume,MergeGuard}` still pass unmodified.
- DONE: AC-2: after status --boot re-runs, every refused verb succeeds unchanged; and sessions with no boundary or no resolvable identity behave bit-identically to today, with existing golden fixtures passing unmodified.
  This is the material finding's fix. `bootGuardVerdict` (internal/status/bootguard.go) now branches on `errors.Is(err, fs.ErrNotExist)`: a genuinely missing receipt still refuses (unchanged); every other `os.ReadFile` error (permission denied, EISDIR) fails OPEN with a stderr warning instead of refusing — matching the direction already used for an unreadable transcript, and closing the unrecoverable loop ("one cheap idempotent boot" never actually clears it). `writeBootReceipt` now takes a `stderr io.Writer` (threaded from `gatherBoot`, which already had one) and warns on a failed `MkdirAll`/`WriteFile` instead of exiting 0 silently. Both refusal reasons now interpolate the resolved receipt path (self-diagnosing, per the finding's item 2). All three reproduction shapes are checked-in tests: `TestBootGuardVerdictFailsOpenNotClosedOnUnreadableReceipt`/{receipt_directory_mode_000, receipt_path_exists_as_a_directory} and `TestBootWriteSurfacesFailureOnReadOnlyProjectRoot` (project root chmod 555 — write warns, subsequent read correctly refuses naming the never-created path); all three skip under root (`os.Geteuid()==0`), where Unix permission bits do not apply. Both one-token deferred risks are also fixed: `booted_at` is written and compared at `RFC3339Nano` (was `RFC3339`, truncating sub-second precision and, per finding 3, wrongly refusing a boot that lands in the same wall-clock second as a compaction but genuinely after it), and the receipt line is split with `SplitN(line, " ", 2)` instead of `Fields` (per finding 4, a transcript path containing a space no longer silently disables detection). Each of the four fixes has a dedicated regression test, and each regression test was proven genuinely falsifying by hand: reverting the one line under test (the `ReadFile`/`errors.Is` branch, the `RFC3339Nano` format call, the `SplitN` call) and re-running made the corresponding test fail for the expected reason, then restoring the fix turned it green again — the same disable-and-restore discipline used for the wiring tests in cycle 1.
- DONE: AC-3: the verdict derives from durable state ONLY — deleting the receipt or appending the boundary record flips refusal/pass with no conversational input. Prove the preflight is actually called: the wiring test must fail if it is not.
  Unchanged by this fix; re-verified post-fix with the two new regression rows added to the same durable-state-only proof (no fixture files, all inline constants/fixtures).
- DONE: Report actual net surface against the approved +245 across 8 files, broken out product / tests / docs.
  Reported separately per the dispatch's instruction, not folded into cycle 1's disclosed +642/14 files: `git diff --numstat` against the cycle-1 commit (8ba85f4df) is net **+172** across 4 files (+202/−30). Product **+26** net: internal/cli/cli.go and internal/status/boot.go are net 0 (threading the existing `stderr` writer into two already-guarded call sites and one already-instrumented call), internal/status/bootguard.go is +48/−22 (the fix itself — the `fs.ErrNotExist` branch, two stderr warnings, receipt-path interpolation in both refusal reasons, `RFC3339Nano`, `SplitN`). Tests **+146** net, entirely in internal/status/bootguard_test.go (+151/−5): four new test functions covering the three reproduction shapes and the two deferred-risk regressions. Docs: none touched this cycle.

### Summary

Fixed the one material finding validation raised: an unreadable (not missing) boot receipt now fails open with a stderr warning instead of refusing permanently, `status --boot` surfaces a failed receipt write instead of swallowing it, and both refusal messages name the resolved receipt path. Took both one-token deferred risks (RFC3339Nano, SplitN) alongside it, each proven by a regression test that was itself verified falsifiable by reverting the underlying one-line fix and watching the test fail for the right reason. AC-1 and AC-3 are unchanged and re-verified; AC-2 is restored. Cycle-2 surface is net +172 across 4 files, reported separately from cycle 1's +642/14 as instructed — the mechanism is unchanged from the captain-approved design; this was a narrow correction, not a redesign.

## Stage Report: validation (cycle 2)

- DONE: AC-1 and AC-3: independently reproduce the falsifiability proof. Disable the two BootGuardRefuse call sites yourself and confirm all three CLI wiring tests go red with exit=0, then restore byte-identically — do not accept the implementer's account of having done it. Then run the 7-row verdict matrix and confirm each row's verdict flips on the single durable-state edit it names.
  Re-done from scratch at 707ccfe69 on a fresh throwaway clone (implementation worktree untouched). Both call sites short-circuited → all three `TestBootGuardWiring{GateRecord,GateConsume,MergeGuard}` red at `exit=0, want 4`, stdout again showing the mutation landing (`recorded gate=`, `route=approved-awaiting-merge`, `armed:`); restored via `git checkout --`, empty `git diff HEAD`, green again. AC-3 flip matrix re-run against the new 3-arg signature: all 12 assertions hold, and both refusal reasons now carry the resolved receipt path. I also re-ran the falsifiability of the FOUR new fixes myself rather than accepting the account — reverting each single line in turn (the `errors.Is`/`fs.ErrNotExist` branch, the two write-side warnings, `Format(RFC3339Nano)`, `SplitN`) turned exactly its own regression test red, and only that one. All four are genuinely wired.
- DONE: AC-2 fail-open, weighted above everything else: this guard fronts gate record, gate consume, and merge guard, so a wrong refusal stops all delivery. Build the degradation matrix the implementation did not: unreadable receipt directory, receipt with a future timestamp, transcript present but empty, transcript with a compact_boundary lacking a timestamp, two boundary records where the newer is malformed, concurrent boot writes, and a session id that resolves to a path outside the expected root. Each must FAIL OPEN or refuse with a correct reason — never refuse spuriously.
  My own cycle-1 matrix re-run and extended to 30 assertions. **The three rows that refused spuriously now fail open AND warn** (receipt dir mode 000, receipt file mode 000, receipt path is a directory) — confirmed end-to-end with the built binary: `gate consume` reaches its real result and stderr names the path plus, correctly, "re-running boot will not clear it". **None of the 11 rows that already failed open flipped**, and the true positive (valid older boundary after `booted_at`, newer one malformed) still refuses. Detection is undiminished: compacted → all three verbs exit 4; never-booted → exit 4; both name the receipt path. Attacking the widened fail-open as instructed, no read error that should refuse now escapes: a zero-length, newline-only, whitespace-only, or valid-time-then-garbage receipt all reach the **parse** path, not `ErrNotExist`, and fail open; a dangling symlink resolves to `ErrNotExist` and correctly refuses; the removed `len(fields)==0` guard is subsumed by the parse failure. Concurrency hammer (8 writers vs 4,000 interleaved reads): 0 spurious refusals. Sub-second probe: the cycle-1 truncation is gone (`.9Z` now written and compared). Warning stream verified to reach a real operator — both call sites take cli.go's `stderr` → `os.Stderr`, `writeBootReceipt` takes `gatherBoot`'s, no production path passes `io.Discard`, and I saw both warnings on real stderr in the binary run. Version skew is a non-issue in both directions: Go's `time.Parse` accepts an optional fractional second whatever layout constant it is given, so a pre-fix second-precision receipt still parses (proven directly, and end-to-end: an old-format receipt refuses when provably stale and passes when fresh); the writer also emits no fraction for a whole-second instant, so most receipts stay byte-identical. `status --boot` still exits 0, and that is right — it is a read command whose contract is the boot record, the FO branches on its exit code at Startup, and exiting non-zero would not change the refusal anyway; what changed is that two coordinated messages now name the cause and the consequence.
- DONE: Test the two overrun causes as necessity claims. Cause 1 (a new TranscriptProbe seam in claudeteam threaded through five call sites) is a new cross-package mechanism: confirm the host-neutrality lint genuinely forbids the simpler in-place approach, and that the seam mirrors TeamStateProbe rather than inventing a second pattern. Cause 2 (three existing tests forwarding the live CLAUDE_CODE_SESSION_ID into receipt-less fixtures) is a pre-existing hermeticity bug fixed in passing: confirm it is real, that the fix is minimal, and that it does not mask a case where the guard SHOULD have refused.
  Both confirmed in cycle 1 and unchanged by this commit; cycle 2 touches neither the seam nor the hermeticity helpers. Re-verified that the hermeticity fix still holds at 707ccfe69: the full suite passes inside this live Claude session with the ambient `CLAUDE_CODE_SESSION_ID` set, and `internal/cli` + `internal/ensigncycle` pass under `-race`.
- DONE: Report actual net surface and state whether the 262% overrun is confined to the two disclosed causes or whether anything else rode along.
  Cycle 1's account stands as recorded in the Cycle 2 line; not re-litigated. **Cycle-2 surface is confined to the authorized fix plus the two one-token risks.** `git diff --numstat 8ba85f4df..707ccfe69` = net **+172 across exactly 4 files** (+202/−30), no new files and nothing outside them. Product is +26 net, of which only **25 lines are code** (23 are comment): the `fs.ErrNotExist` branch, three stderr warnings, path interpolation in two reasons, `RFC3339Nano`×2, `SplitN`+`TrimRight`. `cli.go` and `boot.go` are net 0 — pure `stderr` threading the fix requires. Tests are +146 net (95 code, 34 comment, 11 blank) for four new functions covering the three reproduction shapes and the two one-token regressions, plus the signature updates. No unrelated mechanism, feature, or refactor rode along. Cumulative is net +814 across 14 files against the approved +245.

### Findings

1. **Cycle-1 material finding: RESOLVED, with a scope refinement I owe.** My cycle-1 report bundled three reproduction shapes into one finding citing AC-2. On re-examination only two were AC-2 violations — a receipt that a *successful* boot wrote, then refused because it could not be read (dir/file mode 000, receipt-as-directory). Those are fixed and now fail open with a warning. The third, a read-only project root, is different in kind: no boot ever succeeded, so no receipt exists, and "no receipt → refuse" is AC-1's approved primary detection. Reading AC-2's bit-identical clause to cover it would contradict AC-1. It still refuses (verified), but that is the design the captain approved, it is now loudly diagnosed at the source, and an unwritable project root is not a configuration where this workflow functions at all (`state commit`, entity mutation, and worktrees all need writes). Recording it as a deferred risk rather than a blocker. **AC-2 is satisfied.**
2. **Deferred risk, carried forward unchanged — cwd/workflow git-root divergence.** Boot writes at `FindGitRoot(roots.definitionDir)` (`handlers.go:453`), the guard reads at `FindGitRoot(dir)` (`bootguard.go:189`). Re-verified still present: cwd in a worktree with `--workflow-dir` in the main repo puts the receipt in the main repo and `gate consume` from the worktree exits 4, unfixable by re-booting. Not targeted by this cycle and does not fire today (the FO runs guarded verbs from the repo root; ensigns run none). Materially better diagnosed now — the refusal names the expected path, so an operator can see it resolved under the worktree. Promotes to material if any documented or scripted flow invokes a guarded verb with a cwd outside the workflow's git root.
3. **Deferred risk, new and narrower than the one it replaced — receipt-line whitespace.** `SplitN(TrimRight(raw,"\n")," ",2)` fixes the real-world spaced-path case (verified: detection now works through `path with spaces`), but a trailing space, a CRLF line ending, or a tab between fields now silently loses detection: the path keeps the trailing space or `\r` and misses, or `SplitN` finds no space and the timestamp parse fails. All four land in the **fail-open** direction, and the writer emits none of them, so this is only reachable by hand-editing or cross-platform copying. The tab case is a mild regression from `Fields`, traded for the space fix — the right trade. Cheap hardening if ever wanted: `TrimRight(raw, "\r\n")` and `TrimSpace` on the path field.
4. **Polish.** The doc comment for `TestBootReceiptSubSecondPrecisionSurvivesRoundTrip` sits above `TestBootGuardVerdictHandlesTranscriptPathWithSpaces` (two stacked blocks at bootguard_test.go:163-179), leaving the sub-second test at :197 undocumented — the comment content is accurate, only its placement is wrong. Carried forward from cycle 1 and unchanged: `gate record --round` is guarded though the workflow calls it a neutral recorder (harmless, FO-only caller); `hermeticEnv()`/`recordedGateHermeticEnv()` duplicated across two packages; `first-officer-shared-core.md` at 26,509 bytes against contractlint's 26,900 cap.

### Summary

The material finding is fixed and I proved it rather than accepting the account: my own 30-assertion matrix now shows the three spuriously-refusing shapes failing open with a warning that names the receipt path, none of the 11 already-fail-open rows flipped, the true positive survives, and detection is undiminished (compacted and never-booted both still exit 4 on all three verbs). Attacking the widened fail-open as instructed found no read error that should refuse and now escapes — empty, whitespace-only, and garbage receipts reach the parse path rather than `ErrNotExist`, and a dangling symlink still refuses. All four fixes are independently falsifiable: I reverted each single line and exactly its own regression test went red. Version skew is a non-issue both directions because Go's `time.Parse` accepts an optional fractional second regardless of layout, verified directly and end-to-end with a pre-fix receipt. `status --boot` still exiting 0 is the right call for a read command the FO branches on. Cycle-2 surface is confined: net +172 across exactly 4 files, 25 lines of product code, nothing outside the authorized fix and the two one-token risks. Full suite and `-race` green except the pre-existing `TestCodexResolveManifestAgainstInstalledHost`, which I confirmed in cycle 1 fails identically on the merge-base. One correction I owe: my cycle-1 finding bundled the read-only-root shape with the two genuine AC-2 violations, and it belongs with AC-1's approved never-booted detection instead — recorded as a deferred risk. No material finding remains. **Recommend PASSED**, with three deferred risks and their promote conditions listed above.

## Stage Report: implementation (cycle 3)

- DONE: AC-1 measuring: in the incident-replay scenario, 0 of 3 guarded verbs succeed before re-boot, each exiting non-zero with the boot-stale message — against a baseline of 3 of 3 on today's binary.
  Unchanged by cycle 3 (receipt LOCATION only moved, preflight call sites untouched). Independently re-proved by hand, not accepted from the predecessor's account: I short-circuited both `status.BootGuardRefuse` call sites in `internal/cli/cli.go`, ran `TestBootGuardWiring{GateRecord,GateConsume,MergeGuard}`, and all three went red at `exit=0` with the mutation visibly landing (`recorded gate=…`, `route=approved-awaiting-merge`, `armed: mod-block set…`); restored `cli.go` from a byte-for-byte backup and confirmed `git diff` on it is empty, then re-ran all three green.
- DONE: AC-2: after status --boot re-runs, every refused verb succeeds unchanged; and sessions with no boundary or no resolvable identity behave bit-identically to today, with existing golden fixtures passing unmodified.
  Cycle 3's two required consequences each have a dedicated test: `TestReadOnlyProjectRootNoLongerBlocksBootReceipt` (internal/status/bootguard_test.go) chmods the project root 555 and asserts a CLEAN write (no stderr) followed by a genuine `stale=false` verdict — not a fail-open mask of a lost receipt, since the receipt now never touches the repo at all. `TestBootGuardWorktreeDivergenceEliminated` first asserts the fixture reproduces the OLD divergence (`FindGitRoot(defDir) != FindGitRoot(wtDir)`), so the test cannot pass trivially, then boots from the main checkout with `--workflow-dir docs/dev` and runs `BootGuardRefuse` with cwd in a linked `.worktrees/` checkout, asserting no refusal — proving cycle 2's deferred risk 2 is gone, not narrowed. Golden-fixture bit-identity: `go test ./...` and `go test ./... -race` are green repo-wide except the same pre-existing, unrelated `TestCodexResolveManifestAgainstInstalledHost` (a stale local `~/.codex` cask-version artifact) — confirmed identical by `git stash`-ing all six cycle-3 files and re-running the test at 707ccfe69, same failure text.
- DONE: AC-3: the verdict derives from durable state ONLY — deleting the receipt or appending the boundary record flips refusal/pass with no conversational input. Prove the preflight is actually called: the wiring test must fail if it is not.
  Verdict logic unchanged this cycle apart from operating on the relocated path; `TestBootGuardVerdictMatrix`'s 7 rows (internal/status/bootguard_test.go) pass unmodified against `bootReceiptPath`'s new host-scratch resolution. Preflight-call falsifiability is the AC-1 evidence above (disabling the two call sites turns all three CLI wiring tests red).
- DONE: Report actual net surface against the approved +245 across 8 files, broken out product / tests / docs.
  See Summary for the cycle-3-only and cumulative breakdowns.

### Summary

Finished the inherited cycle-3 work rather than redoing it: verified the predecessor's uncommitted diff (receipt relocated to `/tmp/spacedock-boot/{session_id}-{repo identity token}`, keyed on the shared git-common-dir so a worktree and its main checkout resolve to the same token) was complete and correct for the cycle-3 scope, then found and fixed one half-finished piece — `internal/cli/bootguard_wiring_test.go`'s `writeFreshBootReceipt` still wrote to the old in-repo `.spacedock/boot/` path, so all three guarded-verb wiring tests failed post-relocation (`exit=4, want 0`, receipt not found at the new path). Fixed by exporting `status.BootReceiptPath` (a thin wrapper over the unexported `bootReceiptPath`) so the cross-package test can locate the real production path without re-deriving the repo-identity hashing scheme. All three wiring tests, the worktree-divergence test, and the read-only-root test pass; AC-1's falsifiability was re-proved by hand (disable both call sites, watch all three wiring tests go red, restore byte-identically, watch them go green). `go test ./...` and `-race` are green except the same pre-existing unrelated `TestCodexResolveManifestAgainstInstalledHost`, confirmed identical against the pre-cycle-3 commit via `git stash`. Cycle-3 surface, `git diff --numstat` against 707ccfe69: net **+154** across **6 files** (+240/−86) — one file more than the predecessor's 5-file scope note, because of the wiring-test fix. Product **+64** (`internal/status/bootguard.go`: the `repoIdentityToken` helper, `bootReceiptDir` constant, and threading `repoDir` in place of `gitRoot`). Tests **+91** (`internal/status/bootguard_test.go` +87: receipts now planted via the real `bootReceiptPath` plus the new worktree-divergence and read-only-root-no-longer-blocks tests; `internal/cli/bootguard_wiring_test.go` +4: the fix above). Docs/config **−1** (`docs/runtime-support.md` +4 describing the new location and key; `skills/first-officer/references/first-officer-shared-core.md` net 0, same line reworded; `.gitignore` −5, the `/.spacedock/` entry fully reverted since nothing is written in-repo anymore). Cumulative against the approved +245/8 files: `git diff --numstat` from the merge-base with main (`f64c733ef`) to the current tree is net **+968** across **13 files** — `.gitignore` drops out of the cumulative file count entirely, since cycle 3's revert brings it back to byte-identical with the merge-base. This is carried forward from cycles 1-2's already-disclosed, already-validated overrun (test-volume driven, not scope creep — see those cycles' reports); cycle 3 adds +154 to it for the captain-mandated location fix and does not introduce a new overrun cause.

## Stage Report: validation (cycle 3)

- DONE: AC-1 and AC-3: independently reproduce the falsifiability proof. Disable the two BootGuardRefuse call sites yourself and confirm all three CLI wiring tests go red with exit=0, then restore byte-identically — do not accept the implementer's account of having done it. Then run the 7-row verdict matrix and confirm each row's verdict flips on the single durable-state edit it names.
  Re-done at cee043cb4 on a fresh throwaway clone. Both call sites short-circuited → all three `TestBootGuardWiring{GateRecord,GateConsume,MergeGuard}` red with exactly 3 `exit=0, want 4` failures; restored via `git checkout --`, empty `git diff HEAD`, green again. I did NOT take the FO's word that the wiring tests were repaired — I ran them: all three pass at this commit, and `writeFreshBootReceipt` now plants at the real production path via the new exported `status.BootReceiptPath` rather than the dead in-repo path. AC-3 re-run as a 24-row matrix (below); every row's verdict is driven by file bytes or one env var.
- DONE: AC-2 fail-open, weighted above everything else: this guard fronts gate record, gate consume, and merge guard, so a wrong refusal stops all delivery. Build the degradation matrix the implementation did not: unreadable receipt directory, receipt with a future timestamp, transcript present but empty, transcript with a compact_boundary lacking a timestamp, two boundary records where the newer is malformed, concurrent boot writes, and a session id that resolves to a path outside the expected root. Each must FAIL OPEN or refuse with a correct reason — never refuse spuriously.
  Matrix rewritten against the relocated path: 24 rows, all correct. The three cycle-1 spurious-refusal shapes stay fixed (receipt file mode 000 and receipt-as-directory both fail open + warn); the three true positives still refuse (no receipt, boundary after `booted_at`, newer-malformed/older-valid-after-boot); zero-length, newline-only, whitespace-only and valid-time-then-garbage receipts all reach the **parse** path rather than `ErrNotExist` and fail open; a dangling symlink resolves to `ErrNotExist` and correctly refuses; all four path-escaping session ids no-op; both one-token fixes hold (spaced transcript path detects, pre-fix second-precision receipts parse in both directions). Concurrency hammer at the new location: 0/2000 spurious refusals. One cycle-1 row is no longer testable in isolation — chmod-ing the now-**shared** receipt directory would break concurrent tests and the developer's own live session; the shipped tests avoid it deliberately and say so, and the file-level equivalents cover the same branch. Binary-level: compaction → all three verbs exit 4 (from the main checkout AND from a linked worktree), re-boot clears it, and the repo is untouched (`git status` clean, no `.spacedock/`), which is the captain's actual complaint discharged.
- DONE: Deferred risk 2 claimed GONE, not narrowed — re-run your own reproduction and attack the common-dir hash directly.
  **Confirmed gone**, three independent ways. Unit: `bootReceiptPath` is byte-identical from the main checkout, a linked worktree, and an arbitrary subdirectory, and differs for a second repo — and I checked the premise against git itself rather than assuming it (`rev-parse --git-common-dir` resolves to the same absolute path from main and worktree). Binary: booting from the main checkout produces exactly ONE receipt, and `gate consume` from the main checkout, the worktree, and `docs/dev` all read it; the reverse (boot from the worktree, guard from main) also agrees. The shipped `TestBootGuardWorktreeDivergenceEliminated` is honest — it asserts the fixture actually reproduces the OLD divergence (`FindGitRoot(defDir) != FindGitRoot(wtDir)`) before proving the new path collapses it, so it cannot pass trivially. One real hole in the mechanism: see finding 3.
- DONE: Attack the new location — /tmp is world-writable, so ask whether another user can cause a WRONG PASS.
  The wrong pass is real as a mechanism and I reproduced it end-to-end with the built binary: with the session provably compacted (`gate consume` → exit 4), overwriting the 28-byte receipt with `2099-01-01T00:00:00Z` flips it to a pass. Four forged bodies all defeat the guard — future timestamp with no transcript field, future timestamp with the real transcript, a current timestamp alone, and garbage (fail-open). The path is fully derivable from public inputs: I recomputed `sha256(realpath(git common dir))[:12]` independently and it matched exactly. There is no nonce, no secret, and no ownership or mode check on the receipt. A pre-planted **symlink** at that path is also followed on write: `writeBootReceipt` truncated and overwrote a victim file through it, with no warning. But on a normally-provisioned host another user cannot reach it: `/tmp` is sticky (01777, verified) and `/tmp/spacedock-boot` is created 0755 owned by whoever boots first (verified on this host: `drwxr-xr-x` uid 501), so a second uid can neither write into it nor, thanks to the sticky bit, replace it. The attack needs a hostile second local uid that **wins the creation race** before the legitimate user's first boot. Classified deferred, not material — details and the two free hardenings in finding 1.
- DONE: Report actual net surface and whether cycle-3 growth is confined to the authorized fix.
  **Confined.** `git diff --numstat 707ccfe69..cee043cb4` = net **+154 across 6 files** (+240/−86), all six exactly the move: bootguard.go (+81, of which 49 are comment lines → ~32 code lines for the sha256/hex imports, `repoIdentityToken`, `bootReceiptDir`, and the `gitRoot`→`repoDir` rename), bootguard_test.go (+122/−44: the new worktree test plus path updates), the wiring test's path update, both doc locations, and `.gitignore` restored to its exact pre-entity bytes. No new mechanism beyond the token. Cumulative is net **+968 across 13 files** against the approved +245; cycle 1's account is not re-litigated per the dispatch.

### Findings

1. **Deferred risk — the receipt is unauthenticated and its path is world-predictable.** Mechanism proven (forgery flips a stale session to pass; a planted symlink is followed on write, silently). Trigger requires a hostile second local uid winning the creation race on `/tmp/spacedock-boot`; once the legitimate user has created it 0755, a second uid cannot write into it and `/tmp`'s sticky bit prevents replacing it. No value AC fails under the promised single-developer workflow, and the guard's threat model is an *accident* — a compacted FO trusting its own summary — which will not forge its own receipt. The same exposure is already accepted for `/tmp/spacedock-dispatch`, which carries dispatch prompts that tell workers what to do; this reuses that boundary rather than opening a new one, exactly as the implementation's comment claims. **Promotes to material** if spacedock is run on a multi-user or shared-CI host, or if the receipt is ever relied on against an adversary rather than an accident. Two one-line hardenings would remove most of it at no cost and I recommend them without blocking: `os.OpenFile(path, O_WRONLY|O_CREATE|O_TRUNC|os.O_NOFOLLOW, 0o600)` in place of `os.WriteFile` kills the symlink-follow outright, and `MkdirAll(bootReceiptDir, 0o700)` stops the receipts being world-readable. The location-level fix that closes the race entirely — a per-user scratch dir such as `os.UserCacheDir()` — is a captain decision, since the captain chose `/tmp` this cycle.
2. **Deferred risk, carried forward unchanged — receipt-line whitespace.** A trailing space, CRLF, or a tab between fields still silently loses detection (fail-open direction; the writer emits none of them). `TrimRight(raw, "\r\n")` plus `TrimSpace` on the path field if ever wanted.
3. **Deferred risk, new — the git-unavailable fallback reintroduces worktree divergence.** `repoIdentityToken` falls back to `dir`'s own real path when `git rev-parse --git-common-dir` fails, which is per-directory again. Proven: with git off `PATH`, the main checkout and its worktree hash to different tokens, so a boot from one does not clear the guard in the other — the exact risk cycle 3 eliminated, returning silently on a stripped `PATH`. Not reachable in the normal workflow (git is a hard dependency of everything else spacedock does). Promotes to material if spacedock is ever run with git absent or shadowed. A one-line stderr warning on the fallback path would make it self-diagnosing.
4. **Polish / notes.** The guard now spawns `git rev-parse --git-common-dir` on every guarded verb and every boot — measured **12.75 ms/op**, ~99.5% of `bootGuardVerdict`'s total cost; fine per CLI invocation, noted because it is a new subprocess dependency whose failure mode is finding 3. `BootReceiptPath` is a new exported symbol added only so the cli wiring test can locate the real path without duplicating the hashing scheme — justified (duplication would reintroduce a tautology) but it is test-driven public API. Receipts accumulate in `/tmp/spacedock-boot` with no pruning (29 entries, 28 bytes each on this host, including this live session's); `/tmp` cleaners handle it. Carried forward and unchanged: `gate record --round` is guarded though the workflow calls it a neutral recorder (harmless, FO-only caller); `hermeticEnv()`/`recordedGateHermeticEnv()` duplicated across two packages; `first-officer-shared-core.md` at 26,549 bytes against contractlint's 26,900 cap.

### Summary

Deferred risk 2 is genuinely eliminated, not narrowed, and I proved it three ways rather than reading the diff — identical receipt paths from main checkout, linked worktree and subdirectory, the premise checked against git itself, one receipt observed at the binary level serving all three cwds, and the shipped test carrying its own anti-tautology assertion. Everything from cycles 1 and 2 survives the move: 24-row matrix all correct, 0/2000 spurious refusals under concurrency, both call sites re-proven falsifiable, no version skew, and the repo now genuinely untouched — `git status` clean, no `.spacedock/`, `.gitignore` back to its original bytes. I ran the wiring tests rather than trusting the report that they were repaired; they pass, and they now plant at the real production path. On the wrong-PASS question the dispatch weighted highest: the forgery and symlink-follow are real mechanisms that I reproduced with the binary, but on a normally-provisioned host a second uid cannot reach the path — `/tmp` is sticky and the receipt dir is owned 0755 by the first booter, so the attack needs a hostile local uid winning a creation race, and the identical exposure is already accepted for `/tmp/spacedock-dispatch`. That is a deferred risk with a named promote condition, plus two one-line hardenings (`O_NOFOLLOW`, `0o700`) I recommend without blocking. Cycle-3 surface is confined to the move: net +154 across 6 files, ~32 lines of product code. Full suite and `-race` green except the pre-existing `TestCodexResolveManifestAgainstInstalledHost`, confirmed in cycle 1 to fail identically on the merge-base. No material finding remains. **Recommend PASSED**, with three deferred risks and their promote conditions listed above.

## Stage Report: implementation (cycle 4)

Three corrections landed mid-cycle, all from the FO/captain catching real problems before merge rather than after. Recorded here rather than smoothed over:

- FINDING: a shipped contractlint test was actively enforcing the bug this entity fixes. `internal/contractlint/dispatch_ack_removal_test.go` asserted `.claude-plugin/plugin.json` must NOT activate `hooks` (`wantHooks:false`) — cleanup residue from removing the unrelated dispatch-ack hooks that had the side effect of red-lighting anyone who attempted exactly this entity's fix. Anyone building this correctly would have hit a failing test that looked like their fault. Fixed as part of this entity's wiring change (`wantHooks:true`, with a comment distinguishing it from the still-guarded dispatch-ack event names/markers).
- FINDING: this entity supersedes `docs/dev/.spacedock-state/claude-post-compaction-contract-reload.md` (`status: backlog` since July, never dispatched). That entity diagnosed the identical gap in its own words — "`.claude-plugin/plugin.json` has no `hooks` key today" — and proposed the identical mechanism (a Claude `SessionStart(compact)` hook). It sat unactioned for roughly a month. This entity ships that mechanism AND discharges the gap I originally reported as open: that entity's AC-1 wanted a live capture proving hook stdout reaches the MODEL context specifically, mirroring the Codex `systemMessage` probe methodology. The required spike (below) is exactly that capture. Recommend the captain retire `claude-post-compaction-contract-reload` as fully superseded.
- FINDING, the load-bearing one: the mechanism I reported as "correctly wired" after consolidation was BROKEN, and only a live spike caught it. I had written "relied on as documented platform behavior rather than re-proving it live" — the captain and FO were right to reject that. Detail in the "Required spike" item below.

- DONE: WHERE the shipped hook lives, and why it is the smallest mechanism that reaches the value.
  `hooks/session_start_compact_reminder.sh`, the SOLE command registered under `hooks.json`'s existing `SessionStart`/`^compact$` matcher, activated for Claude via `.claude-plugin/plugin.json`'s `hooks` field (re-added; proven live before for an unrelated feature, `042f926db`..`6b2ae9787`) and for Codex via its pre-existing, unchanged `.codex-plugin/plugin.json` wiring. First build of this cycle added a second, Claude-only script alongside the pre-existing Codex one under the same matcher — since `hooks.json` is shared by both manifests, that meant two reminder blocks per compaction on both hosts once Claude's manifest activated it. Caught and consolidated before merge: the old script is renamed and rewritten in place, not duplicated.
- DONE: WHAT the instruction says, short enough to read and specific about what to distrust.
  Text originates from the (reverted) live spike, tightened to use `${SPACEDOCK_BIN:-spacedock}`. Names the boundary, the one command to re-run, and four concrete things to distrust (presented gates, live workers, binary path, contract version). Output shape (`hookSpecificOutput.additionalContext` JSON) is kept unchanged from the script it replaces, deliberately not switched to plain stdout, so this is a text change only on the one host (Codex) where that exact JSON shape was previously live-verified for command resolution.
- DONE: Required spike — prove MODEL delivery, not platform docs.
  Ran the exact methodology requested: `tmux`-driven scratch session, `claude --plugin-dir` pointed at a disposable copy of the exact committed worktree tree (`git archive` of HEAD, sentinel injected into that copy only — the real committed script/hooks.json were never edited for the spike), a sentinel token in `additionalContext`, a real `/compact`, then a no-tool-calls turn asking for the sentinel or NONE. **First run: FAILED, and caught a real bug.** `hooks.json`'s command still read `${PLUGIN_ROOT}/hooks/session_start_compact_reminder.sh` (inherited from the pre-existing Codex script). Verbatim: `SessionStart:compact hook error — Failed with non-blocking status code: /bin/sh: /hooks/session_start_compact_reminder.sh: No such file or directory`. The model answered `NONE`. Root cause via a diagnostic env-dump hook (referenced by a hardcoded absolute path so the diagnosis didn't depend on the token under test): Claude Code sets `CLAUDE_PLUGIN_ROOT` for a `--plugin-dir`-loaded plugin's hook subprocess; it never sets a bare `PLUGIN_ROOT` — that is Codex's own token, verified live for Codex only (`e143969b8`). Under `/bin/sh` the unset `${PLUGIN_ROOT}` expands to empty, producing the broken path. This bug predates this entity but was inert until this cycle activated `hooks.json` for Claude for the first time — nothing before this cycle could have exercised it. Fixed with `${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT}}`, verified correct for both hosts by running the template through `/bin/sh` under each host's env shape by hand, then re-ran the FULL spike end to end: no hook error, invocation log shows `source=compact spacedock_bin_set=yes`, and the next turn — no tools — reproduced `SPIKE-SENTINEL-133D580A5296E412` exactly. Trust/approval: no dialog or accept step observed loading the plugin via `--plugin-dir` or registering its `SessionStart` hook; `/plugin` → Installed showed it `✔ enabled` immediately (caveat: ran under the operator's already-familiar `HOME`, so a genuine first-ever-encounter prompt is not conclusively ruled out — stated, not hidden). Auto-compact: attempted via a 920KB file read to force context growth; moved usage from only 13% to 14%, and reaching a ~90%+ threshold within a reasoned budget was not practical — declared infeasible with reasoning (`SessionStart`'s `source` field does not distinguish manual/auto triggers, so the code path is identical; this is a design-level inference, named as such, not a second empirical observation). Full verbatim transcript: `ideation-spike-evidence.md` section 6.
  New regression test, `TestSessionStartCompactReminderPluginRootFallbackResolves`, executes the live hooks.json command template through `/bin/sh` under Claude-shaped and Codex-shaped environments and asserts the resolved path — proven load-bearing by reverting to the bare `${PLUGIN_ROOT}` template and watching both this test and the wiring test fail with the exact broken path the live spike produced, then restoring byte-identically.
- DONE: HOW it is proven — the hard part, now answered with an empirical delivery proof instead of a documentation citation.
  Mechanical tier tested and falsified by hand: `TestSessionStartCompactReminderHookGate` drives the real committed script as a subprocess across 9 cases with an explicit, non-inherited environment (this repo's own ambient shell commonly exports a real `SPACEDOCK_BIN` — this entity's own incident is why — which would have silently defeated the gate-closed cases otherwise). `TestSessionStartCompactReminderHookIsWired` parses both manifests plus `hooks.json` for exactly one command. `TestSessionStartCompactReminderPluginRootFallbackResolves` proves the token resolves, not just that it's a well-formed string. Platform-capability tier is no longer "relied on" — it is empirically confirmed by the live spike above. Obedience tier remains explicitly declared NOT tested, with the reasoning recorded in the Design section: this wires delivery, empirically confirmed, not obedience.
- DONE: NON-CLAUDE HOSTS — what happens for Codex.
  Codex keeps its existing wiring (`.codex-plugin/plugin.json`'s `hooks` field, unchanged) and now receives this entity's more specific reminder text, plus the plugin-root fallback fix, through it instead of the old generic line and the old bare token. Codex's known limitation is unchanged and un-addressed by this entity: a live probe previously found a Codex `systemMessage` reaches only the operator-facing UI, not the model. Issue #595 owns any further Codex compaction-recovery design.
- DONE: Re-estimate surface from scratch, then twice more as the shape changed; declare net, product/tests/docs, and tolerance — honestly, including that each correction made the number larger, not smaller.
  `git diff --numstat` against `f64c733ef` (cumulative for this whole restart) is net **+378** across **7 changed paths** (+384/−6): +230/6 files shipped first, +42 for consolidation, +106 for the spike-required plugin-root fix and its dedicated resolution test. Both intermediate and final cumulative figures stay far below both numbers that belonged to the abandoned receipt+guard mechanism (+245 approved, +968 actual). Full breakdown in the Design section's "Expected surface".

### Summary

Built the SHIPPED form of the mechanism the captain asked for, then corrected it twice more before merge: once when consolidating two hosts sharing one `hooks.json` exposed a duplication risk, and once — more seriously — when a captain-mandated live spike proved the consolidated mechanism was actually BROKEN on Claude Code, not merely unproven. `hooks.json`'s command used `${PLUGIN_ROOT}`, a token Claude Code never sets (it sets `CLAUDE_PLUGIN_ROOT`); every real compaction would have produced a silent, non-blocking hook error and delivered nothing. The spike caught this by actually running the shipped mechanism end to end — sentinel token, `--plugin-dir`-wired plugin, real `/compact`, no-tool-calls verification — exactly the empirical standard the captain demanded instead of the platform-documentation reliance I had originally reported. Fixed with `${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT}}`, then the full spike re-run confirmed the sentinel reaches the resumed model's context verbatim. Two other findings recorded as requested: a contractlint test was actively enforcing the exact gap this entity fixes, and this entity supersedes the never-dispatched `claude-post-compaction-contract-reload` backlog entity — and now fully discharges its AC-1's live-model-context requirement rather than leaving it open. Proof is three honest tiers: mechanical (script gate + content + exactly-one-registration + token resolution) fully tested and every assertion proven falsifiable by hand; platform capability (stdout reaching model context) now EMPIRICALLY CONFIRMED via live spike, not assumed; obedience declared untestable and left as a live-journey observation. `go test ./...` and `-race` are green except the same pre-existing, unrelated `TestCodexResolveManifestAgainstInstalledHost`. `gofmt -l ./cmd ./internal` is clean. Cumulative net surface is +378 across 7 paths — the largest sub-round was the spike-required fix itself, reported plainly because that is what actually happened, not because it is the smaller number.

## Stage Report: validation (cycle 4)

- SKIPPED: AC-1 and AC-3: independently reproduce the falsifiability proof. Disable the two BootGuardRefuse call sites yourself and confirm all three CLI wiring tests go red with exit=0, then restore byte-identically — do not accept the implementer's account of having done it. Then run the 7-row verdict matrix and confirm each row's verdict flips on the single durable-state edit it names.
  Stale checklist item — it describes the receipt+guard mechanism the captain rejected; the branch was reset to main and `BootGuardRefuse`, the wiring tests, and the verdict matrix no longer exist. Validated against the cycle-4 assignment body instead. (I validated this item as written in cycles 1-3; those reports stand for provenance.)
- SKIPPED: AC-2 fail-open, weighted above everything else: this guard fronts gate record, gate consume, and merge guard, so a wrong refusal stops all delivery. Build the degradation matrix the implementation did not: unreadable receipt directory, receipt with a future timestamp, transcript present but empty, transcript with a compact_boundary lacking a timestamp, two boundary records where the newer is malformed, concurrent boot writes, and a session id that resolves to a path outside the expected root. Each must FAIL OPEN or refuse with a correct reason — never refuse spuriously.
  Stale — same reason. The shipped mechanism refuses nothing and fronts no verb, so there is no fail-open direction to test. Its analogue (the hook must stay silent unless both preconditions hold) is covered under the gate matrix below.
- SKIPPED: Test the two overrun causes as necessity claims. Cause 1 (a new TranscriptProbe seam in claudeteam threaded through five call sites) is a new cross-package mechanism: confirm the host-neutrality lint genuinely forbids the simpler in-place approach, and that the seam mirrors TeamStateProbe rather than inventing a second pattern. Cause 2 (three existing tests forwarding the live CLAUDE_CODE_SESSION_ID into receipt-less fixtures) is a pre-existing hermeticity bug fixed in passing: confirm it is real, that the fix is minimal, and that it does not mask a case where the guard SHOULD have refused.
  Stale — the TranscriptProbe seam and the hermeticity fix were reverted with the mechanism; neither is in the branch. Both were confirmed necessary in cycle 1.
- DONE: Report actual net surface and state whether the overrun is confined.
  Net **+378 across 7 paths** (+384/−6), matching the dispatch. Composition: the new test file is 288 lines (75%), docs 65, the hook script 22, the contractlint correction 7, and one line each in `.claude-plugin/plugin.json` and `hooks.json`. The **shipped mechanism is ~24 lines**. Per the dispatch I do not compare against the abandoned mechanism's +245 approved / +968 actual.
- DONE: The token fix, both hosts, executed — not string-checked.
  Staged a disposable plugin copy with `git archive HEAD` (the spike's own method), pulled the command template out of the shipped `hooks.json`, and ran it through `/bin/sh` under hermetic `env -i` shells. Claude-shaped (`CLAUDE_PLUGIN_ROOT` set, `PLUGIN_ROOT` unset) resolves to the staged script and emits the reminder; Codex-shaped (reverse) likewise; both set → `CLAUDE_PLUGIN_ROOT` wins and emits; neither set → `rc=127`, `/hooks/session_start_compact_reminder.sh: No such file or directory`. **The pre-fix template fails under Claude-shaped env with exactly the error string the spike recorded**, and still succeeds under Codex-shaped env — so the bug was Claude-only and the fix is strictly additive for Codex. The new `TestSessionStartCompactReminderPluginRootFallbackResolves` **genuinely executes**: it parses the real command out of `hooks.json` and runs `sh -c "echo <cmd>"` under two explicit environments, asserting the resolved absolute path — not a string compare. I proved it falsifiable myself: reverting the template to bare `${PLUGIN_ROOT}` reds only the Claude subtest, reporting the broken `/hooks/...` path; restored, green. Gap worth naming: it covers Claude-shaped and Codex-shaped but not both-set or neither-set; I covered those and both behave correctly.
- DONE: The gate matrix, re-run with an explicit non-inherited environment.
  Necessary discipline confirmed — this repo's ambient shell exports a real `SPACEDOCK_BIN` (`…/0.27.0-pre7/spacedock`), which would have masked every gate-closed row. Thirteen rows under `env -i`, all correct: `SPACEDOCK_BIN` absent → silent; set-but-empty → silent; `source` = startup/resume/clear → silent; empty, non-JSON, and non-string-`source` stdin → silent; set + `source=compact` → emits. My additional adversarial rows also hold: `source=compaction` and `source=precompact` do not match the exact-equality test, and multiline JSON still emits. The naive `sed` extraction is safe by construction rather than by luck — a JSON-valid payload cannot carry unescaped `"source":"compact"` inside another field's value, so the greedy match cannot be spoofed; I tested both field orderings and both stay silent. Output parses as JSON with `hookEventName: SessionStart` and 663 characters of context. The shipped gate test uses explicit `cmd.Env` too, so its own hermeticity claim holds.
- DONE: The blast radius on Codex, executed rather than read.
  Ran the real `WriteCodexLocalMarketplace` staging: `hooks.json` and the renamed `hooks/session_start_compact_reminder.sh` both land in the staged plugin dir with the executable bit preserved (`-rwxr-xr-x`), the old `codex_session_start_compact.sh` is absent, and the staged `hooks.json` carries the fixed template. `codexPluginDirIncludes` names the `hooks` **directory**, not individual filenames, and is unchanged by this entity, so the rename is transparent to packaging. Codex still resolves the command and still receives an instruction — both proven above. One residual, recorded as finding 1.
- DONE: Every load-bearing assertion broken by hand.
  Five independent breaks, each reding its own test and only its own: removing the `hooks` key from `.claude-plugin/plugin.json` (reds both the wiring test and the corrected `TestDispatchAckMachineryIsAbsent`); adding a second command under `SessionStart`/`^compact$`; reintroducing the old script filename; dropping the `SPACEDOCK_BIN` gate; dropping the `source=compact` check. Restored after each — `git status` clean, suite green. The contractlint invariant correction is genuinely cleanup residue, not a weakened guard: the test's real subject (PreToolUse/SubagentStart absent, SessionStart present, `internal/dispatchack` gone, the forbidden-marker scan) is untouched, and only the incidental "Claude must not activate hooks" assertion flipped — which I confirmed is load-bearing in its new direction.
- DONE: Judge the sentinel proof, the auto-compact inference, and the trust gap.
  **Sentinel: methodology sound.** The decisive feature is the negative control — same prompt, same method, broken hook → `NONE`; fixed hook → the exact 64-bit token. I enumerated the routes to that string without hook delivery and closed them: guessing is excluded by an exact 16-hex match; inference is excluded because the prompt does not contain the token; repo leakage is excluded because `git grep` finds the sentinel nowhere in the committed tree; and an operator-only channel is excluded because the model reproduced it in a turn with no tool calls, so it was in model context. Residual write-up gap, named rather than waved: the spike does not explicitly assert the model never read the disposable plugin tree pre-compaction, and the two runs were separate sessions, so the negative control does not formally exclude a run-2-only read. The plugin dir sat outside the session cwd per the spike's own env dump, so this is a stretch — but it is the honest caveat. **Auto-compact inference: sound in kind, unobserved, and it covers the higher-frequency case** — finding 2. **Trust gap: real, silent, safe** — finding 3.
- DONE: State plainly that this wires delivery, not obedience.
  It does, and the entity does not overreach. AC-4 scopes itself explicitly to delivery and states that obedience is not asserted or tested; `docs/runtime-support.md` and the gate test's own doc comment say the same. I credit exactly this: the reminder provably reaches the resumed model's context through the shipped plugin path, and nothing here shows a compacted First Officer acts on it. That remains a live-journey question.

### Findings

1. **Deferred risk — the Codex path gains an unverified precondition.** The new script adds an in-script `[ "$src" = "compact" ]` check the old Codex-only script never had. The `^compact$` matcher already exists on `main`, so the host-side filter predates this cycle and the in-script check is defence in depth rather than a new sole gate. But no captured Codex `SessionStart` payload exists anywhere in the repo, so if Codex honours its matcher yet omits `source` (or spells it differently), Codex now goes silent where it previously emitted. Low value at stake: `docs/runtime-support.md` records that a Codex `systemMessage` reaches only the operator-facing UI, not the model, and #595 owns Codex compaction recovery. **Promotes to material** if Codex's operator-facing reminder is relied on, or if #595 builds on this script. Cheap check: capture one Codex `SessionStart` payload and assert the field.
2. **Deferred risk — auto-compact is inferred, not observed, and it is the more dangerous case.** The reasoning is sound in kind: `source` enumerates session-start causes, the manual/auto distinction lives only on `PreCompact` (which this hook never reads), and the script has no branch on trigger, so no code path can behave differently. I accept it as reasoning. But manual `/compact` is the case an operator knows happened; auto-compact fires silently at threshold, which is precisely when a First Officer is least likely to suspect stale bindings. The gap is declared honestly rather than skipped. **Promotes to material** on any report of a hook miss after an auto-compaction. Cheap check: one auto-compact capture.
3. **Deferred risk — first-run trust is not ruled out.** The trust probe ran under a `HOME` where the plugin path showed "Last used: 2 days ago", so a genuine first-ever-encounter approval prompt is not excluded. If one exists, the hook does not fire on a new machine until approved. Degradation is silent and safe — compaction behaves exactly as it does today — so this costs the value, not correctness. Cheap check: one run under an isolated `HOME`.
4. **Polish.** The fallback-resolution test covers Claude-shaped and Codex-shaped but not both-set (precedence) or neither-set (the loud `rc=127` failure); both behave correctly, and adding the two rows is nearly free. The stale completion checklist carried the abandoned mechanism's four items into this dispatch and should be refreshed before the next cycle.

### Summary

The mechanism is new, so I validated it from scratch and executed everything rather than reading it — the lesson this cycle already paid for. The token fix is real and correct: I staged a disposable plugin copy, pulled the live template out of `hooks.json`, and ran it under hermetic Claude-shaped and Codex-shaped environments, confirming both resolve and emit, both-set resolves by precedence, neither-set fails loudly, and the pre-fix template fails under Claude with exactly the error string the spike recorded while still working for Codex — so the bug was Claude-only and the fix is additive. The new regression test genuinely executes the template rather than string-comparing it, and I proved it falsifiable by reverting the token and watching only the Claude subtest red. The gate matrix holds across thirteen hermetic rows — necessary discipline, since this repo's ambient shell really does export `SPACEDOCK_BIN` — and the naive `sed` turns out to be unspoofable by any JSON-valid payload. Codex packaging is intact, verified by running the real staging rather than reading the includes list. Five assertions broken by hand, each reding only its own test; the contractlint correction is genuine residue with the dispatch-ack invariant untouched. The sentinel proof stands on its negative control, and I closed the alternative routes to that string, including confirming the token appears nowhere in the committed tree. Full suite and `-race` green except the pre-existing `TestCodexResolveManifestAgainstInstalledHost`, which I re-confirmed fails identically on `main` today. Surface is +378 across 7 paths with a ~24-line shipped mechanism. This wires delivery, not obedience, and the entity says so itself without overreaching. No material finding. **Recommend PASSED**, with three deferred risks and their promote conditions above.

## Stage Report: implementation (cycle 5)

CORRECTION cycle, three prose defects on code/tests already approved. No mechanism, hooks.json, plugin.json, printf payload, or other test touched.

- DONE: DEFECT 1 — cut the mechanism narrative from `docs/runtime-support.md`.
  Replaced the 64-line "Compaction boundary reminder (Claude Code and Codex)" section (SHA, spike narrative, PLUGIN_ROOT bug story) with a 2-sentence "Compaction-boundary reminder hook" note: hooks.json is shared, a new host activating it inherits the reminder, and the host must supply its own plugin-root variable. No SHA, no spike, no host-specific naming. Commit `53a14d985`.
- DONE: DEFECT 2 — cut the script comment essay.
  `hooks/session_start_compact_reminder.sh` went from 18 comment lines to 2: shebang + one ABOUTME line naming only the SPACEDOCK_BIN gate's rationale (non-obvious, looked strippable). Removed the PreCompact-vs-SessionStart reasoning, the entity-file pointer, and the can't-force-the-next-tool-call caveat — all already live in this entity. Executable code (`[ -n ... ] || exit 0`, the `sed` extraction, the `printf` payload) is byte-identical to before, verified by diff against a pre-edit backup after a falsifiability round-trip.
  Commit `53a14d985`.
- DONE: DEFECT 3 — replace the test's byte-copy assertion with property checks.
  `session_start_compact_reminder_hook_test.go`'s `wantReminder` const (a verbatim copy of the payload, compared with `got != want`) is gone. The "reminded" branch now parses stdout as JSON and asserts `hookEventName == "SessionStart"`, `additionalContext != ""`, and `additionalContext` contains `status --boot` — the one substantive claim the payload makes. The gate-closed branch's empty-stdout assertion is unchanged. Proved both directions by hand: rewording the payload text (keeping "status --boot") left the test green; dropping the `status --boot` mention reproducibly reds it (`session_start_compact_reminder_hook_test.go:144: additionalContext does not name the boot command`). Script restored byte-identical after each probe (diff clean). Commit `53a14d985`.
- DONE: DEFECT 4 — lead with host signature parity; cut the gate matrix from 9 rows to 3.
  Captain, mid-cycle: "i care about the signature of the codex and claude being the same (session start + compaction), but instead i am seeing fuck load of fucktard tests." The invariant this entity delivers is HOST SIGNATURE PARITY — both plugin manifests activate the same `hooks.json`, exactly one `SessionStart`/`^compact$` entry, resolving to the same script under either host's plugin-root variable — and the test file buried it under 9 gate-matrix rows permuting shell built-ins. Reordered `TestSessionStartCompactReminderHookIsWired` (static) and `TestSessionStartCompactReminderPluginRootFallbackResolves` (dynamic) to lead the file with a joint doc comment naming the invariant, extracted their shared `hooks.json`-parsing into `sessionStartCompactCommand()`, and cut `TestSessionStartCompactReminderHookGate` from 9 cases to 3: one open case, plus the two closed cases with an external source (`SPACEDOCK_BIN` unset; `source != compact`). Dropped `SPACEDOCK_BIN`-set-but-empty, `source=resume/clear`, empty stdin, malformed non-JSON stdin, and non-string `source` — those exercised `sed`/`test(1)`'s own semantics, not this entity's invariant. Test names unchanged (both AC-2/Test-plan prose and this entity's own provenance trail cite them by name).
  Verified every remaining assertion is load-bearing by breaking it by hand and confirming only the right subtest reds, then restoring byte-identical (diff clean): a duplicate `hooks.json` command entry (reds the exactly-one check); reverting the fallback to the bare `${PLUGIN_ROOT}` token — reproduces the original shipped bug exactly, redding the Claude-shaped subtest while the Codex-shaped one still passes, matching the historical record; dropping the `SPACEDOCK_BIN` gate (reds the "no SPACEDOCK_BIN" row only); dropping the `source=compact` check (reds the "source=startup" row only). File: 288 → 185 lines (−36%). Commit `a30b963eb`.
- DONE: Report actual net surface.
  `git diff --numstat` against the prior cycle-4 HEAD, combined across both cycle-5 commits: `docs/runtime-support.md` −57 net (7 ins / 64 del), `hooks/session_start_compact_reminder.sh` −16 net (1 ins / 17 del), `internal/contractlint/session_start_compact_reminder_hook_test.go` −103 net (109 ins / 212 del — defect 3's property-check rewrite plus defect 4's reorder-and-cut). Combined **−176 net** across the 3 files.
  `go test ./...`: green except the same pre-existing, unrelated `TestCodexResolveManifestAgainstInstalledHost` (already failing on `main`, unrelated to this entity — reconfirmed after both commits). `go test ./... -race` run in full after `53a14d985` (same single pre-existing failure); `go test ./internal/contractlint/... -race` re-run after `a30b963eb`, clean. `gofmt -l ./cmd ./internal`: clean.

### Summary

Four prose/framing corrections, all verified rather than asserted. Defects 1-3: the runtime-support doc now tells a new-host author only what they need (inherits the hook, must supply their own plugin-root variable), the script reads as a 2-line header over 4 lines of shell instead of an essay, and the test's former byte-copy — which the same change authored on both sides, so it could only catch a typo — became three property checks plus a boot-command substring check. Defect 4: the test file's real problem was framing, not just the byte-copy — it buried the one invariant this entity delivers (host signature parity between Claude and Codex) under 9 rows of shell-built-in permutations. Reordered the two parity tests to lead, shared their JSON-parsing logic, and cut the gate matrix to 3 rows (288 → 185 lines, −36%). Every surviving assertion across all three tests was proven load-bearing by hand: six independent breaks (duplicate hooks.json entry, reverted plugin-root token reproducing the original bug exactly, dropped SPACEDOCK_BIN gate, dropped source=compact check, reworded payload text, removed boot-command mention), each redding only its own subtest, each restored byte-identical. Mechanism, `hooks.json`'s actual command, `plugin.json`, and the `printf` payload are untouched. Full suite green except the pre-existing unrelated Codex-resolve failure. Committed path-scoped on the CODE branch (`53a14d985`, `a30b963eb`); did not push, did not touch the PR per the dispatch.

## Stage Report: implementation (cycle 6)

CORRECTION cycle, one defect, one line. Mechanism untouched: `hooks.json`, both plugin manifests, the `CLAUDE_PLUGIN_ROOT` fallback, the script's executable lines, and all three tests are byte-identical to `a30b963eb`. `git diff --numstat` for the whole cycle is **1 insertion / 1 deletion in 1 file** (`hooks/session_start_compact_reminder.sh`).

- DONE: The injected additionalContext instructs re-binding at triggers then resuming the loop, and never names the Startup procedure
  Commit `b461c703d`. The payload now mirrors `first-officer-shared-core.md`'s `## Compaction continuity` **After** clause verbatim in its operative phrase — "re-satisfy each load precondition and state read at its existing trigger" — followed by "Then resume the loop where it stopped", and opens with "You are mid-loop: do NOT greet, re-present a session summary, or wait to be engaged again." The defect was real and traceable: shared core Startup step 3 invokes `«interaction.boundary»()`, whose interactive branch "present the summary … then STOP for input. Do NOT auto-dispatch", so the old text's "re-run the Startup procedure" instructed a compacted, already-engaged FO into a cold greet. Verified by executing the shipped script under a hermetic `env -i` and parsing its stdout: `Startup` absent, `startup` absent (case-insensitively clean), the re-satisfy clause present, `resume the loop` present. All four distrust items retained verbatim as substrings — gates-presented/check-readiness-not-memory, workers-alive, `SPACEDOCK_BIN` path, installed contract version — and `${SPACEDOCK_BIN:-spacedock}` is still emitted literally (confirmed: the probe env exported a real `SPACEDOCK_BIN` and the rendered text still shows the template, so the single-quoted `printf` is not expanding it). Payload 663 → 780 chars.
- DONE: Script stays six lines: no re-added in-script source check, no new test permutations
  `wc -l` = 6, unchanged. The diff is one `printf` line replaced by one `printf` line; lines 1-5 (shebang, ABOUTME, `SPACEDOCK_BIN` gate, `cat | sed` extraction, `[ "$src" = "compact" ]`) are untouched. No test file changed, so no permutation was added — the gate matrix is still the 3 rows from `a30b963eb` and the file is still 185 lines. **Naming a wording tension rather than silently resolving it:** the dispatch says "no re-added in-script `source` check or the `cat`", but both have been on this branch since `719398d11` and removing them would take the script to four lines, contradicting "stays six lines". I changed neither — nothing was added and nothing was removed — which satisfies both readings of the instruction and leaves cycle-4 validation finding 1 (the Codex-silence deferred risk from that in-script check) exactly where the validator left it, neither aggravated nor discharged. If the captain meant the check should be *gone*, that is a one-line follow-up, not something I should decide inside a prose-only correction.
- DONE: go test ./internal/contractlint green with the hook-wiring and CLAUDE_PLUGIN_ROOT-fallback tests unchanged
  `go test ./internal/contractlint/` green, including `TestSessionStartCompactReminderHookIsWired`, `TestSessionStartCompactReminderPluginRootFallbackResolves` (both subtests), `TestSessionStartCompactReminderHookGate` (3 rows), and `TestDispatchAckMachineryIsAbsent`; `-race` also green (3.271s). Falsifiability re-proven against the NEW string, not just inherited from cycle 5: rewriting the payload's boot command to `state --identify --json` reds exactly one subtest with `session_start_compact_reminder_hook_test.go:181: additionalContext does not name the boot command (status --boot)`, and nothing else; restored byte-identical (`diff` clean against a pre-probe backup). Gate-closed rows re-exercised on the new script under `env -i`: `SPACEDOCK_BIN` unset + `source=compact` → exit 0, empty stdout; set + `source=startup` → exit 0, empty stdout. `go test ./...` and `go test ./... -race` both fail only on `TestCodexResolveManifestAgainstInstalledHost`, which I re-confirmed fails identically on `main` at `6cb8f8211` — pre-existing and unrelated. `gofmt -l ./cmd ./internal` clean.
- DONE: Push so PR #738 updates in place, no new PR
  `git push origin spacedock-ensign/force-boot-at-compaction-boundary` — `b461c703d` on the entity's existing branch, so #738 updated in place. No PR opened.

### Summary

One defect, one line: the reminder told a compacted first officer to "re-run the Startup procedure", and Startup provably terminates in a greet-and-stop — so obeying the text would demote an engaged session to a cold one, which is what happened live in this session. The payload now says what the shared core's own compaction clause says: you are mid-loop, do not greet, re-satisfy each load precondition and state read at its existing trigger before the next workflow effect, then resume the loop. The four distrust items that scored 4/4 at a real boundary are retained verbatim. Nothing else moved — the six-line script's executable lines, `hooks.json`, both manifests, and all three tests are byte-identical to the approved cycle-5 state, and I proved the surviving `status --boot` assertion still bites the new wording rather than trusting cycle 5's account of it. One instruction tension is recorded rather than silently resolved: "no re-added in-script `source` check" versus "script stays six lines" — I touched neither, so the cycle-4 Codex deferred risk stands unchanged.

## Stage Report: validation (cycle 6)

- DONE: Execute the shipped script and grade its rendered payload: mid-loop, re-satisfy each load precondition and state read at its trigger, resume the loop; no greet and no reference to the Startup procedure
  Executed the committed script under `env -i PATH=… SPACEDOCK_BIN=/real/bin/spacedock` with `{"source":"compact"}` on stdin: exit 0, valid JSON, `hookEventName=SessionStart`, 780-char `additionalContext`. Graded the jq-DECODED text, not the source line: "You are mid-loop" present; "do NOT greet" present and the only occurrence of "greet" is that prohibition; "re-satisfy each load precondition and state read at its existing trigger" present, matching `first-officer-shared-core.md:164`'s operative phrase verbatim; "Before the next workflow effect" present; "Then resume the loop where it stopped" present; case-insensitive `startup` count = **0**. `${SPACEDOCK_BIN:-spacedock} status --boot --identify --json` is emitted literally even though the probe env exported a real `SPACEDOCK_BIN` — the single-quoted `printf` does not expand. Defect rationale independently confirmed at source, not accepted from the report: shared core line 19 routes the interactive `«interaction.boundary»()` branch to "present the summary … then STOP for input. Do NOT auto-dispatch", and line 39 says "A greet-and-stop boot loads NONE of these" — so the prior "re-run the Startup procedure" wording did instruct a compacted, already-engaged FO into a cold greet.
- DONE: Confirm the correction is one line and the mechanism is byte-identical to a30b963eb: hooks.json, both plugin manifests, the CLAUDE_PLUGIN_ROOT fallback template, all three tests, and the 185-line test file
  `git diff --numstat a30b963eb..b461c703d` = `1  1  hooks/session_start_compact_reminder.sh` — one insertion, one deletion, one path. Blob-hash comparison a30b963eb vs HEAD returns IDENTICAL for `hooks.json`, `.claude-plugin/plugin.json`, `.codex-plugin/plugin.json`, `session_start_compact_reminder_hook_test.go`, and `dispatch_ack_removal_test.go`. Template still `${CLAUDE_PLUGIN_ROOT:-${PLUGIN_ROOT}}/hooks/session_start_compact_reminder.sh`; matcher `^compact$`; exactly 1 command. Test file 185 lines at both revisions; script 6 lines with lines 1-5 hashing identically (`eed3b9ad880e`) at both. All three hook tests plus `TestDispatchAckMachineryIsAbsent` pass.
- DONE: Reproduce each of the four numbered ACs' cited evidence, and independently confirm the single go test ./... red is pre-existing and environment-dependent
  **AC 1 (Mechanism):** executed a 16-row hermetic matrix. Emits only on `SPACEDOCK_BIN` set + `source=compact`; silent (exit 0, empty stdout) on BIN unset, BIN set-but-empty, `source`=startup/resume/clear, near-misses `compaction`/`precompact`, empty stdin, non-JSON garbage, numeric `source`, absent `source` key, and both field-order spoof attempts; multiline JSON still emits. **AC 2 (Installed/once/resolves):** both manifests carry `hooks: "./hooks.json"`; fallback test green on both host shapes; on a throwaway copy, reverting to bare `${PLUGIN_ROOT}` reds ONLY the Claude-shaped subtest while Codex-shaped still passes — the original bug's asymmetry reproduced. **AC 3 (One script):** `hooks/codex_session_start_compact.sh` absent; injecting a second command yields `has 2 command(s), want exactly 1`. **AC 4 (Delivery; obedience declared untested):** cited evidence located at `force-boot-at-compaction-boundary/ideation-spike-evidence.md` §6 — sentinel `SPIKE-SENTINEL-133D580A5296E412` in `additionalContext`, negative control (broken hook → `NONE`), post-fix verbatim reproduction with no tool calls; I confirmed the sentinel appears nowhere in the committed tree, closing the leakage route. Delivery credited per the settled disposition; no obedience test sought, per AC 4's own declaration. **Falsifiability against THIS cycle's string:** stripping `status --boot` from the new payload reds exactly one subtest at `session_start_compact_reminder_hook_test.go:181`; restored byte-identical. **The red:** `TestCodexResolveManifestAgainstInstalledHost` fails in the worktree AND byte-identically on a throwaway `main@6cb8f8211` checkout with the same resolved path `~/.codex/plugins/cache/spacedock-local/spacedock/0.27.0-pre7/.codex-plugin/plugin.json`; that path **exists on disk**, so the test's "not installed" premise is false on this machine — pre-existing and environment-dependent, established from this run rather than inherited. `gofmt -l ./cmd ./internal` clean; `go test ./internal/contractlint/ -race` green.
- FAILED: `go test ./...` is not a single red on this machine — it has TWO
  `internal/ensigncycle` also fails, at 600.455s: a default 10-minute test timeout, not an assertion. Confirmed pre-existing by the same method as the Codex red — the package times out at 601.013s on the throwaway `main@6cb8f8211` checkout, and the branch touches no file under `internal/ensigncycle` (merge-base diff = 7 paths). A re-run at `-timeout 30m` also fails, at 1800.801s, so the package hangs rather than merely running slow — but it hangs identically on untouched `main`, so it is pre-existing environment behavior, not this change. Not caused by this entity; recorded because the dispatch's "single red" label is incomplete, not because it implicates the change.

### Findings

1. **Evidence defect, for the captain to weigh — no AC or test pins the payload's instruction CONTENT.** Proven by execution, not argued: I swapped the REJECTED cycle-5 payload (`a30b963eb`, the "re-run the Startup procedure" text) into a throwaway copy with everything else at HEAD, and the entire `internal/contractlint` suite passed **green**. The exact defect cycle 6 exists to fix is invisible to the gate. AC 1 nominally covers "the exact declared reminder JSON", but cycle 5 replaced the byte-copy with property checks, and "declared" has no canonical referent in the entity — the surviving content assertion is the `status --boot` substring, which the rejected text also satisfies. No value AC fails today (the shipped payload is correct, verified above), so this is not Material; it is the observation boundary, and closing it is a scope decision the captain owns.
2. **AC 1 text over-promises its test.** AC 1 claims coverage "including empty or malformed stdin" and byte-exact JSON; the trimmed 3-row test asserts neither. I covered the missing rows by execution and all behave correctly, so this is a prose/test divergence, not a behavior gap. Polish.
3. **Open disposition, recorded not resolved (per dispatch).** Script lines 4-5 (`cat | sed` extraction and `[ "$src" = "compact" ]`) are redundant with the host's own `^compact$` matcher and carry cycle-4 validation finding 1's Codex-silence deferred risk. Implementation correctly declined to decide, the FO's instruction being self-contradictory. I removed nothing and argue nothing away; the deferred risk stands exactly where cycle 4 left it, with its promote-to-material condition unchanged.
4. **Surface bookkeeping.** True surface from merge-base `f64c733ef` is 7 paths, +208/−6, net **+202**. The `## Expected surface` section still declares +378 (the cycle-4 figure); cycle 5's reported −176 reconciles it exactly (378−176=202). Stale prose, arithmetically consistent. Polish.

### Summary

The correction is exactly what it claims: one line, one file, mechanism byte-identical to `a30b963eb` by blob hash across `hooks.json`, both manifests, the fallback template, and all three tests. I graded the rendered payload by executing the shipped script rather than reading its source, and it satisfies every graded property — mid-loop, no greet, the shared core's re-satisfy-at-trigger clause verbatim, resume the loop, and zero occurrences of "startup" — and I confirmed at source that the old wording really did route a compacted FO into a greet-and-stop, so the defect was real. All four numbered ACs reproduce; the falsifiable assertions bite this cycle's string, not cycle 5's, proven by breaking them on a throwaway copy and restoring byte-identical. Two corrections to the dispatch's premises, both from this run's evidence: `go test ./...` carries **two** reds on this machine, not one, and the second (`internal/ensigncycle`, a 10-minute timeout) is pre-existing on `main` by the same throwaway-checkout test as the Codex red, which itself is environment-caused by a stale local codex cache that demonstrably exists on disk. The finding that matters for the gate is not a defect in the shipped bytes but in what can catch them: the rejected cycle-5 payload passes the full suite green, so nothing pins the instruction content to the shared core's compaction clause. No material finding. **Recommend PASSED**, with the content-AC gap surfaced for the captain's decision and the cycle-4 Codex deferred risk carried forward untouched.

## Stage Report: implementation (cycle 7)

Heading note, not a renumbering: the dispatch says "Cycle 6", which is the number of the REJECTION this round corrects. `## Stage Report: implementation (cycle 6)` is already occupied by the round that rejection judged, so this is cycle 7 — the same N-rejection/N+1-report pairing the cycle-5 rejection and the cycle-6 report already follow.

CORRECTION cycle, one defect, two deleted lines, commit `433f7a05a`. Whole-cycle `git diff --numstat b461c703d..HEAD` is `0 2 hooks/session_start_compact_reminder.sh` and `7 7 internal/contractlint/session_start_compact_reminder_hook_test.go` — a pure deletion in the script, and a same-size rewrite in the test. `hooks.json`, `.claude-plugin/plugin.json`, `.codex-plugin/plugin.json`, `dispatch_ack_removal_test.go`, and `docs/runtime-support.md` are IDENTICAL to `b461c703d` by blob hash.

- DONE: Delete script lines 4-5 so the script is four lines and source filtering belongs to the host matcher alone
  `wc -l` = 4: shebang, ABOUTME, the `SPACEDOCK_BIN` gate, the `printf`. The `printf` payload line hashes identically (`d2197fcfbc68`) to its pre-edit self, verified by hash rather than by eye because an intermediate edit of mine collapsed the space between `'%s\n'` and the payload argument — reverted before any commit, and the committed script diff is 0 insertions / 2 deletions, so no payload byte moved. Behavior confirmed by executing the committed script under `env -i` rather than by reading it: with `SPACEDOCK_BIN` set it emits the same 866-byte JSON for `source=compact`, `source=startup`, the nested-object payload, and empty stdin (exit 0 each); with `SPACEDOCK_BIN` unset it exits 0 with empty stdout. The script no longer reads stdin at all, which is the point — the host's `^compact$` matcher, pinned as exactly one entry by `TestSessionStartCompactReminderHookIsWired`, is now the only source filter.
- DONE: Rewrite the gate test's source=startup row to assert what the script now owns, keeping the SPACEDOCK_BIN rows unchanged
  Rows 1 and 2 (`launcher-marked + source=compact emits the reminder`, `no SPACEDOCK_BIN -> silent even on source=compact`) are byte-identical. Row 3 became `launcher-marked emits whatever the payload says: a nested "source" no longer suppresses it`, carrying the exact payload from the rejection line — `{"session_id":"s1","hook_event_name":"SessionStart","source":"compact","extra":{"source":"startup"}}` — with `wantReminded: true`. No row added; the file is still 185 lines. The test's doc comment was rewritten in place (7 lines, same as before) because it claimed "a non-compact source ... stay[s] silent", which the deletion makes false; it now says the script gates on `SPACEDOCK_BIN` alone and names the greedy-sed suppression the third row exists to catch. Nothing else in the file changed: the wiring test, the plugin-root fallback test, and the shared `sessionStartCompactCommand` helper are untouched.
- DONE: go test ./internal/contractlint green, and the nested-object payload that used to go silent now emits
  `go test ./internal/contractlint/` green — all three gate rows, both fallback subtests, the wiring test, and `TestDispatchAckMachineryIsAbsent`; `-race` green (4.075s). Falsifiability proven by execution on a throwaway `rsync` copy of the worktree, leaving the real tree untouched (hash-verified after): restoring BOTH deleted lines (the exact pre-fix script from `b461c703d`) makes the nested payload emit 0 bytes and reds exactly the new row, at `session_start_compact_reminder_hook_test.go:175: stdout did not parse as JSON ... (stdout: )` — the rejection's reproduction, now caught by a test. Restoring only `[ "$src" = "compact" ] || exit 0` reds two rows (`src` unset suppresses every payload). Precision on the dispatch's phrasing: restoring only the `src=$(cat | sed ...)` line does NOT red — with no branch left to consume it, that line is inert. So the falsifier bites the branch, and the branch cannot come back without the test noticing.
- DONE: Full-suite state, reported rather than inherited
  `go test ./...`: one red, `TestCodexResolveManifestAgainstInstalledHost` (`internal/cli`), the documented environment-dependent failure — the resolver returns `~/.codex/plugins/cache/spacedock-local/spacedock/0.27.0-pre7/.codex-plugin/plugin.json`, a path that exists on this machine, so the test's "not installed" premise is false here. Correction to cycle-6 validation's second red: `internal/ensigncycle` PASSED this run at 247.237s rather than hanging to the 10-minute timeout, so that failure is load- or contention-dependent, not a standing property of the package. `gofmt -l ./cmd ./internal` clean. `go test ./... -race` carries the same single red and no data race, with `internal/ensigncycle` green again at 289.887s.
- SKIPPED: Push and PR
  Per dispatch: committed on `spacedock-ensign/force-boot-at-compaction-boundary`, not pushed and no PR opened — a layer sits on this branch and the FO owns the restack. CI neither requested nor approved: the covering lane is the offline suite, and no live lane can observe a deleted shell branch.

### Summary

Two lines gone and one test row honest about what remains. The deleted branch re-implemented the host's `^compact$` matcher with a greedy `.*` that took the LAST `"source"` on the line, so a nested object silently suppressed the one message whose purpose is to not fail silently; the fix is deletion, not repair, because both hosts honour the matcher and the wiring test pins it as exactly one entry. The honest cost landed exactly where the rejection said it would: the `source=startup -> silent` row asserted the deleted behavior, so it became the nested-object row that reds if the branch returns — proven by restoring the pre-fix script on a throwaway copy and watching only that row fail for the right reason, with the real worktree hash-verified untouched. One prose divergence I did not fix, because AC text is the captain's: AC 1 still says the script emits "only when `SPACEDOCK_BIN` is set AND given `SessionStart` JSON with `source=compact` on stdin", which the script alone no longer does — the same over-promise cycle-6 validation raised as its finding 2, now one clause larger. Cumulative surface from merge-base `f64c733ef` is 7 paths, +206/-6, net **+200** against the +245 estimate (82%); the `## Expected surface` section's +378 is the stale cycle-4 figure.

## Stage Report: validation (cycle 7)

Heading follows the report it grades: `## Stage Report: implementation (cycle 7)` is the round under review, and `## Stage Report: validation (cycle 6)` is taken.

- DONE: Execute the shipped four-line script and confirm the nested-source payload now emits, and that the SPACEDOCK_BIN gate still closes
  11-row hermetic matrix under `env -i PATH=/usr/bin:/bin`, executing the committed script rather than reading it. `SPACEDOCK_BIN` set: the rejection's exact repro `{"source":"compact","extra":{"source":"startup"}}` exits 0 and emits the full 867-byte payload — before this cycle it emitted 0 bytes. So do `source=compact`, the full nested shape, `source=startup`, `source=resume`, empty stdin, non-JSON garbage, and an absent `source` key: all 867 bytes, exit 0, empty stderr. The script no longer reads stdin, which is the point. Gate closes on all three closed shapes: `SPACEDOCK_BIN` unset → 0 bytes exit 0 (with `source=compact` and with the nested payload), `SPACEDOCK_BIN=` empty → 0 bytes exit 0.
- DONE: Confirm the cycle is two deleted script lines plus one rewritten test row, with hooks.json, both manifests, the payload string, and the wiring and fallback tests byte-identical
  `git diff --numstat b461c703d..HEAD` = `0 2` script, `7 7` test file — nothing else. The script diff is a pure deletion of exactly `src=$(cat | sed …)` and `[ "$src" = "compact" ] || exit 0`; `wc -l` = 4. Payload line hashes `d2197fcfbc68c528` at both revisions, so no payload byte moved. Blob-hash IDENTICAL at `b461c703d` vs HEAD for `hooks.json`, `.claude-plugin/plugin.json`, `.codex-plugin/plugin.json`, `dispatch_ack_removal_test.go`, `docs/runtime-support.md`. Test file still 185 lines and lines 1–115 — the `sessionStartCompactCommand` helper, the wiring test, and the plugin-root fallback test — hash `aeb603d6fd008016` at both revisions. The 7/7 is one row plus a 6-line doc comment rewritten in place. Cumulative from merge-base `f64c733ef`: 7 paths, +206/−6, net **+200**.
- DONE: Reproduce the four numbered ACs' evidence and confirm the falsifier: restoring either deleted line reds the new row and nothing else
  All falsifiers run on a `git archive HEAD` throwaway checkout, never the worktree (worktree `git status` clean and HEAD `433f7a05a` afterward). **Falsifier, with one correction to the dispatch's phrasing:** restoring BOTH lines (the exact `b461c703d` script) reds exactly the new row and nothing else — `session_start_compact_reminder_hook_test.go:175: stdout did not parse as JSON … (stdout: )`, the rejection's silent suppression now caught by a test. Restoring ONLY `[ "$src" = "compact" ] || exit 0` reds TWO rows, because `src` is then unset and nothing emits at all. Restoring ONLY the `src=$(cat | sed …)` line reds NOTHING — with no branch to consume it, that line is inert. So "either line" is not right: the falsifier bites the branch, not the extraction, and the branch cannot return without the test noticing. Independently reproduced, matching the implementation report's own precision note. **AC 1 (Mechanism):** the `SPACEDOCK_BIN` half holds by the matrix above; the `source=compact` half no longer describes the script — see finding 2. **AC 2 (Installed/once/resolves):** both manifests carry `hooks: "./hooks.json"`; both fallback subtests green; reverting to the bare `${PLUGIN_ROOT}` token reds the wiring test and ONLY the Claude-shaped fallback subtest while the Codex-shaped one still passes — the original bug's asymmetry, reproduced. **AC 3 (One script):** `hooks/codex_session_start_compact.sh` absent; injecting a second command yields `has 2 command(s), want exactly 1`. **AC 4 (Delivery; obedience declared untested):** cited evidence present at `force-boot-at-compaction-boundary/ideation-spike-evidence.md` §6 — sentinel `SPIKE-SENTINEL-133D580A5296E412` in `additionalContext`, negative control (`NONE` on the `${PLUGIN_ROOT}`-broken run, with the verbatim `/bin/sh: /hooks/…: No such file or directory` hook error), post-fix verbatim reproduction with no tool calls, and `invoked source=compact spacedock_bin_set=yes`. Credited per the settled disposition; no obedience test sought. `go test ./internal/contractlint/` green, `-race` green (4.035s), `gofmt -l ./cmd ./internal` clean.

### Findings

1. **Deferred risk (evidence defect) — the judgment asked for: exactly-one-entry-at-`^compact$` is ONE-SIDED cover, not sufficient cover.** Proven by execution on the throwaway, not argued. Adding a SECOND `SessionStart` entry with matcher `"*"` naming the same script leaves the ENTIRE `internal/contractlint` suite **green** — `sessionStartCompactCommand` collects commands only from entries whose matcher already equals `^compact$`, so an added permissive entry is invisible to the count. In that state the reminder fires at every SessionStart (startup, resume, clear), and the script, having lost its own `source` check, has nothing left to stop it. The test does catch the opposite direction: widening the existing matcher to `"*"` reds with `has 0 command(s), want exactly 1`. So mutation of the entry is covered; addition of another is not. A second, narrower point on the same axis: `TestSessionStartCompactReminderPluginRootFallbackResolves` genuinely EXECUTES the plugin-root template through `/bin/sh`, while nothing in the repo executes the host's matcher semantics — the matcher is pinned by string equality alone. This cycle moved source filtering from a mechanism the suite runs to a mechanism the suite only spells, which is a real reduction in what the offline lane can prove. NOT material: no value AC fails, both shipped hosts honour the matcher on the settled record, and the trigger is a `hooks.json` edit that does not exist today. Promote-to-material: any second entry appearing in `hooks.json`'s `SessionStart` array, or a host that registers the script without matcher support. Remedy is ~3 lines inside the existing helper — count commands across ALL `SessionStart` entries and assert the sole entry's matcher is `^compact$`. Recommended, not applied; candidate bytes unchanged.
2. **Needs decision — AC 1's text now contradicts its own artifact.** AC 1 says the script emits "only when `SPACEDOCK_BIN` is set AND given `SessionStart` JSON with `source=compact` on stdin, and emits nothing (exit 0) for every other combination, including empty or malformed stdin." Executed: with `SPACEDOCK_BIN` set the script emits on `source=startup`, on empty stdin, and on non-JSON garbage. Cycle-6 validation logged this as Polish when it was a coverage gap; the deletion turns it into a direct contradiction of the shipped behavior. The captain ordered the deletion knowing the cost, and AC text is the captain's — implementation correctly declined to edit it — but it should not reach the gate as an AC its own artifact fails.
3. **Restated at the gate per the dispatch, unchanged by this cycle: no AC or test pins the payload's instruction CONTENT.** Cycle-6 validation proved by execution that the REJECTED cycle-5 payload passes the suite green; the only surviving content assertion is the `status --boot` substring, which the rejected text also satisfied. Two of the last three rejections (cycles 5 and 6) were invisible to the gate. Not material — the shipped payload is correct — but it is the observation boundary, and closing it is the captain's scope call.
4. **Polish — surface bookkeeping.** `## Expected surface` still declares +378, the cycle-4 figure. True cumulative from merge-base `f64c733ef` is 7 paths, +206/−6, net **+200** (82% of the +245 estimate); cycle 5's −176 and this cycle's −2 reconcile it exactly.

### Summary

The cycle is exactly what it claims: two deleted script lines, one rewritten test row, net −2, with the payload line, `hooks.json`, both manifests, and the wiring and fallback tests byte-identical to `b461c703d` by hash. The nested-source payload that used to emit 0 bytes now emits all 867, and the `SPACEDOCK_BIN` gate still closes on unset and empty. The falsifier holds with one correction to the dispatch's phrasing, independently reproduced: restoring BOTH lines reds exactly the new row and nothing else, restoring the branch alone reds two rows, and restoring the extraction alone reds nothing because it is inert without the branch — so the falsifier bites the branch, which is the thing that must not come back. Full offline suite carries one red, `TestCodexResolveManifestAgainstInstalledHost`, whose resolved `~/.codex/.../0.27.0-pre7/.codex-plugin/plugin.json` exists on disk dated Aug 17, before any of this branch's work — environment-caused and pre-existing; `internal/ensigncycle` passed this run at no timeout, confirming the cycle-7 report's correction to cycle-6 validation. On the FO's question I come down plainly against sufficiency: the wiring test covers a widened matcher but not an added permissive sibling entry, which I proved by leaving the suite green in exactly that state — a one-line-remedy test-strength gap with a hypothetical trigger, so a deferred risk rather than a rejection. Live color on the payload's content, unplanned: my own state commit was blocked by a pre-commit hook resolving `$SPACEDOCK_BIN` to the deleted `0.27.0-pre7` cask path — this entity's diagnosis item 1, reproduced on me during its own validation, which is the strongest argument yet that "distrust the SPACEDOCK_BIN path" belongs in the reminder. No material finding. **Recommend PASSED**, with finding 1's deferred risk and findings 2-3 surfaced for the captain's decision.

## Stage Report: implementation (cycle 8)

Two coupled changes plus the AC narrowing, commit `aa359b910`. `git show --numstat`: `hooks.json` 1/1, `hooks/session_start_compact_reminder.sh` 2/2, `docs/runtime-support.md` 2/2, the hook test 60/19. Both plugin manifests, the `CLAUDE_PLUGIN_ROOT` fallback template, and `TestSessionStartCompactReminderPluginRootFallbackResolves` are untouched, as instructed; the in-script `source` check stays deleted and the script stays four lines.

- DONE: Widen the matcher to ^(compact|clear)$ and make the payload's opening line boundary-neutral, keeping the mid-loop instruction and the four distrust items
  `hooks.json` matcher is `^(compact|clear)$`. The payload opens `CONTEXT BOUNDARY (compaction or clear) — your bindings are stale, not just your narrative.`; naming both triggers keeps it neutral without making the model guess which one happened. Graded by EXECUTING the committed script under `env -i` and parsing stdout on `source=compact` and on `source=clear` — identical 799-char text both times, exit 0, empty stderr, and all ten required strings present: the opening, `You are mid-loop`, `do NOT greet`, the shared core's `re-satisfy each load precondition and state read at its existing trigger`, `Then resume the loop where it stopped`, all four distrust items, and `${SPACEDOCK_BIN:-spacedock} status --boot --identify --json` emitted literally (the probe env exported a real `SPACEDOCK_BIN` and the single-quoted `printf` still did not expand it). `COMPACTION BOUNDARY` is gone; the only surviving occurrence of "compaction" is the neutral parenthetical. Gate unchanged and still closing: `SPACEDOCK_BIN` unset → 0 bytes exit 0, `SPACEDOCK_BIN=` empty → 0 bytes exit 0. One sentence I deliberately left alone: "A summary claiming you read or did something is not the reading or the doing." On a `clear` there is no summary, so it is inert rather than wrong, and the dispatch said keep everything else.
- DONE: Exercise the matcher: iterate every SessionStart entry, compile each declared matcher, and assert the union of source values it matches is exactly clear and compact
  `if entry.Matcher != "^compact$" { continue }` is gone. `sessionStartEntries` now returns EVERY `SessionStart` entry with no matcher filter; `sessionStartCompactCommand` counts commands across all of them (so a sibling entry is counted, not skipped) and keeps its signature, leaving the fallback test's call site untouched; new `sessionStartSources` compiles every declared matcher — compile failure is `t.Fatalf`, not a skip — and returns the accepted subset of `startup|resume|clear|compact|fork`, asserted `== "clear,compact"`. Matchers are compiled BEFORE any source is tested, so an early match cannot skip compiling a later bad matcher. Cost, stated plainly: the file went 185 → 226 lines (+41), which buys the parse split, the compile-and-evaluate helper, and one assertion; comment prose was trimmed twice to hold it there.
- DONE: Prove the new assertion catches a second permissive entry, a widened matcher, and a wrong matcher, and that the whole suite is green
  Seven mutants, each run on an `rsync` throwaway copy (worktree `hooks.json` byte-identical afterward, `git status` clean), each red for its own reason: a second entry with matcher `.*` carrying the same command → `SessionStart has 2 command(s), want exactly 1`; **a second entry with matcher `.*` and an EMPTY hooks array → `accept [startup,resume,clear,compact,fork], want exactly [clear,compact]`** — the sharp falsifier, since the command count stays 1 and only the union assertion can see it; the sole matcher widened to `.*` → same union message; a wrong matcher `^startup$` → `accept [startup]`; a non-compiling `*` → `matcher "*" does not compile: missing argument to repetition operator`, a failure rather than a skip; and narrowing back to `^compact$` → `accept [compact]`, so this cycle's widening is itself pinned and cannot be silently reverted. The exact state cycle-7 validation left GREEN — a second entry with matcher `"*"` naming the same script — is now RED. Restoring the shipped `hooks.json` returns green. Suite: `go test ./...` and `go test ./... -race` each carry ONE red, `TestCodexResolveManifestAgainstInstalledHost`, the environment-dependent pre-existing failure; `internal/ensigncycle` green in both (345.8s, 364.8s), no data race, `internal/contractlint` green (`-race` 4.896s), `gofmt -l ./cmd ./internal` clean.
- DONE: Rewrite AC 1, which the four-line script had made false
  AC 1 now describes the artifact: the script owns one gate, `SPACEDOCK_BIN`; it emits regardless of stdin because it does not read stdin; source filtering is the host matcher's job, asserted by AC 2. Marked as the captain-approved cycle-8 narrowing, matching the Cycle 7 ledger line's `AC narrowed`.
- DONE: Carry three more consequences of the widening, beyond the dispatch's literal list
  Flagging rather than burying, since the dispatch named only AC 1: change 2 also falsified AC 2 (`exactly ONE command under SessionStart/^compact$`), two `## Test plan` bullets (the gate matrix still listed `launcher-marked+{startup,resume,clear} → silent`, and the wiring bullet still said `^compact$`), and the `## Semantics that may change` runtime-behavior line. Each is updated to the shipped behavior, plus a new semantics bullet recording the matcher change; `docs/runtime-support.md`'s heading and mechanism sentence went from `SessionStart(compact)` to `SessionStart(compact|clear)` (no test or anchor references it — checked). Leaving these would have shipped the same defect class that got cycle 7 rejected: prose the artifact fails. Nothing in the historical stage reports or Feedback Cycles was edited.
- SKIPPED: PR, push, and CI
  Per dispatch: committed on `spacedock-ensign/force-boot-at-compaction-boundary`, not pushed, no PR opened or updated, no CI requested or approved — a layer sits on this branch and the FO owns the restack, the PR, and any run.

### Summary

The matcher is now behavior instead of a label, and it fires at `clear` too. The old helper looked its own expectation up by matcher string, which is exactly why a permissive sibling entry could be added without redding anything; the wiring test now compiles every declared matcher and asserts the accepted-source union over the host enum, which catches an added sibling, a widened matcher, a wrong matcher, a non-compiling matcher, and a revert of this cycle's own widening — six of the seven mutants named above red on the union alone, and the one state the previous validator proved green is now red. The widening carried a real prose cost that I paid rather than deferred: the payload's `COMPACTION BOUNDARY` opening is now boundary-neutral (graded by execution on both sources, 799 chars, all four distrust items and the re-satisfy clause intact), and AC 1, AC 2, two test-plan bullets, the declared-semantics list, and the runtime-support doc are updated to what the artifact actually does. The test file grew 41 lines, which is the honest price of an exercised matcher and is reported as such rather than framed as a wash. Cumulative surface from merge-base `f64c733ef` is 7 paths, +248/−7, net **+241** against the +245 estimate (98%). Residual, not closed by this cycle: the union assertion MODELS the host's matcher with Go's `regexp` rather than executing the host's own matching, so a host whose regex semantics differ from Go's is still unproven — the shipped matcher is explicitly anchored, so every reading agrees today.

## Stage Report: validation (cycle 8)

- DONE: Execute the shipped script and grade the rendered payload: boundary-neutral opening, mid-loop instruction, re-satisfy-at-trigger clause, and all four distrust items
  Executed the committed script under `env -i` and parsed stdout with a JSON parser rather than grep. 881 bytes out, 799-char `additionalContext`, exit 0, empty stderr — identical on all five source values and on empty stdin, non-JSON garbage, and an absent `source` key. Opens `CONTEXT BOUNDARY (compaction or clear)`; `COMPACTION BOUNDARY` is gone and "compaction" survives exactly once, in the neutral parenthetical. Present: `You are mid-loop`, `do NOT greet`, the shared core's `re-satisfy each load precondition and state read at its existing trigger`, `Then resume the loop where it stopped`, and all four distrust items (gates, workers, the SPACEDOCK_BIN path, contract version). `${SPACEDOCK_BIN:-spacedock} status --boot --identify --json` is emitted literally with a real `SPACEDOCK_BIN` exported — the single quotes hold and the backticks do not substitute. Rendered the cycle-7 and cycle-8 payloads and diffed them: the ONLY change is the opening clause; everything after the first ` — ` is byte-identical to the text the captain accepted.
- DONE: Prove the matcher is now exercised: a second permissive entry, a widened matcher, a narrowed matcher, and a non-compiling matcher each fail
  Nine mutants on a `git archive` throwaway, `hooks.json` restored to sha `ccf1ffc8…` after each; the worktree was never touched (`git status` clean, HEAD `aa359b910`, both artifact shas unchanged). RED, each for its own reason: a second entry `.*` carrying the command (`has 2 command(s), want exactly 1`); a second entry `.*` with an EMPTY hooks array (`accept [startup,resume,clear,compact,fork]` — the command count stays 1, so only the union assertion can see it); narrowed to `^compact$` (`accept [compact]`, so this cycle's widening cannot be silently reverted); widened to `^(compact|clear|resume)$`; the non-compiling `[` (a failure, not a skip); plus three shapes the dispatch did not name — the `matcher` FIELD deleted, a sibling entry with no `matcher`, and `matcher: ""` — each red, because Go compiles `""` to match-all, which is also how the host reads a missing matcher. GREEN: the unanchored `compact|clear`, which accepts the same union over the declared five (see finding 2). The exact hooks.json state cycle-7 validation left green is now red.
- DONE: Reproduce the four numbered ACs including AC 1 as narrowed at cycle 8, and confirm nothing outside the four changed files moved
  `git diff --name-only 433f7a05a..HEAD` is exactly `hooks.json`, the script, the hook test, and `docs/runtime-support.md`; both plugin manifests and `dispatch_ack_removal_test.go` are blob-IDENTICAL to `433f7a05a`. **AC 1 (as narrowed):** every clause reproduced — one conditional in the script; emits regardless of stdin across eight shapes; silent and exit 0 on unset and on empty; and "it does not read stdin at all" proven with a POSITIVE CONTROL rather than an absence — sharing one pipe between the script and a following `cat`, the shipped script leaves the bytes for `cat` while the pre-deletion `b461c703d` script consumes them. The test env is explicit, not inherited: proven by the ambient shell exporting a real `SPACEDOCK_BIN` (at the deleted `pre7` path) while the no-SPACEDOCK_BIN row still passes. **AC 2:** both manifests carry `./hooks.json`; one command; union `clear,compact`; and the template resolves — reverting to bare `${PLUGIN_ROOT}` reds the wiring string AND only the Claude-shaped fallback subtest, reproducing the live bug's asymmetry. **AC 3:** old path absent; a second command caught. **AC 4:** the §6 evidence is present (sentinel, the `NONE` negative control, the no-tool-calls reproduction) and credited per the settled disposition; no obedience test sought. `internal/contractlint` green. `go test ./...` and `-race` each carry ONE red, `TestCodexResolveManifestAgainstInstalledHost`, which I proved pre-existing rather than inheriting the claim: it fails identically at merge-base `f64c733ef` with no branch code present, caused by a `~/.codex/…/0.27.0-pre7/` cache dir dated Aug 17 23:03 — two days before this branch's first commit. No data race. `gofmt -l ./cmd ./internal` clean.

### Findings

1. **New evidence, and the one claim cycle 8 had not proven: delivery at `source=clear` is now confirmed live.** The settled delivery proof was compact-only, so the widening's own value rested on inference. Ran the §6 recipe against an instrumented disposable copy (shipped `hooks.json` unmodified, sha `ccf1ffc8…`; the sentinel and a stdin log added to the script copy only): Claude Code 2.1.226, `--plugin-dir`, fresh scratch cwd, `SPACEDOCK_BIN` exported. The host invoked the hook with `"source":"clear"` verbatim, and the next turn — no tool calls — reproduced `CLEAR-SPIKE-SENTINEL-7F31C0A94DE28B65` exactly. Cardinality: exactly ONE invocation across the whole session, and none at session start, so the host honours the anchored matcher. This retires most of the implementation report's declared residual that Go's `regexp` only MODELS the host's matcher: host and Go now agree on `clear` (in) and `startup` (out), with `compact` (in) from the cycle-4 spike. Session killed; nothing committed.
2. **Judgment asked for, answered against the dispatch's premise: the union assertion is UNDER-constrained, not over-constrained.** The dispatch asks whether "a host that adds a sixth source value would red this test." It would not — proven, not argued: adding `compact_auto` to the enum leaves the suite GREEN, because the anchored matcher cannot match any new value, and a host value the enum does not list is never evaluated at all. What DOES red is a rename or removal (`clear`→`clear_all` yields `accept [compact]`), which is correct strictness. So this is neither over-constraining nor a maintenance trap; it is blind in exactly this entity's own failure direction — a new context-loss source (`compact_auto`, `clear_all`) would silently not get the reminder, which is the silent non-firing the entity exists to prevent. The surviving unanchored-matcher mutant is the same blind spot. Enum provenance improved this cycle and is worth recording: the shipped `codex-cli 0.147.0` binary carries `startup resume clear compact` as its own SessionStart source enum — independent corroboration for four of five values, and confirmation that Codex has `clear` too — while my spike confirms `clear` on Claude; `fork` still has no repo-side or observed provenance on either host. Deferred risk (evidence defect); no value AC fails. Promote-to-material: either host publishing a new SessionStart source that means context loss. The remedy is not another regexp but a periodic check of the enum against the hosts.
3. **Judgment asked for, answered: AC 1 as narrowed describes the shipped artifact exactly, and cycle 7's material finding is CLOSED.** Every clause reproduced in the third checklist item above, including the one that needed a positive control to mean anything. One wording note, not a defect: AC 1's phrase "the declared reminder JSON" has no declaration anywhere but the script itself, so that word does no work — which is finding 5, not a new problem.
4. **Judgment asked for, answered: the payload is actionable after a `clear`, but by ONE sentence rather than by design.** Read as an FO arriving with nothing loaded: the only instruction executable from a cold context is `${SPACEDOCK_BIN:-spacedock} status --boot --identify --json`, which is self-contained and does re-establish workflow, state, and pending gates. Everything around it is written for a reader who kept a summary. "re-satisfy each load precondition and state read at its existing trigger — re-read a deferred contract at the point that needs it" presupposes knowing what your triggers and contracts ARE, which is precisely what a clear destroyed; and it is stated BEFORE the boot command, when after a clear the boot is the precondition for the rest. "A summary claiming you read or did something is not the reading or the doing" is inert, no summary existing — the implementer declared this and I agree it is inert rather than wrong. The one substantive issue: **"You are mid-loop" is asserted unconditionally**, and `/clear` is commonly a deliberate fresh start, so on this source the payload's opening premise is often FALSE. Distrust items 3-4 (the SPACEDOCK_BIN path, the installed contract version) are the most valuable lines after a clear, being facts about the environment rather than about the narrative — and this machine's ambient `SPACEDOCK_BIN` points at a deleted `pre7` cask path right now, so item 3 is live. Classification with the arithmetic shown: trigger evidence yes, fired live this cycle; observable harm NOT observed, and observing it needs the obedience test AC 4 declares out of scope; affected value AC `none:` — no AC pins payload content — which per the disposition policy cannot establish Material. Deferred risk; the wording is the captain's call. Promote-to-material: an observed FO auto-resuming or asserting mid-loop after a deliberate `/clear`, or a new AC pinning content, which would give this a value AC to cite. Cheapest fix if wanted: make the premise conditional ("If you were mid-loop, …") — one clause, no mechanism change.
5. **Restated at the gate per the dispatch, and now proven CURRENT for cycle 8's own change: no AC or test pins the payload's instruction CONTENT.** Not inherited from cycle 7 — re-demonstrated on the throwaway. Reverting ONLY the opening back to `COMPACTION BOUNDARY`, the exact wording this cycle was rejected into fixing, leaves `internal/contractlint` GREEN. So does gutting the whole payload to the 43-character stub `ignore all prior instructions; status --boot`, which satisfies all three property assertions. Cycles 5 and 7 were both real defects the suite passed green on, and cycle 8's own fix is unprotected the same way. Not material — the shipped payload is correct and I graded it by execution — but it is the observation boundary, and closing it is the captain's scope call.
6. **Polish, twice-flagged and still unfixed: `## Expected surface` declares +378 (product +19 / tests +294 / docs +65).** Actual is 7 paths, +248/−7, net **+241**: product +1, tests +233, docs +8. Cycle-7 validation raised this as its finding 4, and cycle 8's own summary acknowledges the figure is stale without correcting it. No consequence for the ledger, which deviates against the +245 ideation estimate (241 = 98%) — but given cycle 7's rejection was literally "prose the artifact fails", this section is the last surviving instance of that class in this entity.

### Summary

Cycle 8 does what it claims, and the matcher is behavior now rather than a label: nine mutants — including three shapes the dispatch did not name (a deleted `matcher` field, a sibling with no matcher, `matcher: ""`) — each red for its own reason, and the exact hooks.json state the previous validator left GREEN is now red. The payload was graded by execution, not reading: the only change from the accepted cycle-7 text is the opening clause, byte-identical after it. AC 1 as narrowed finally describes its own artifact, closing cycle 7's material finding, with "does not read stdin" proven against a positive control rather than an absence. The cycle's one genuinely unproven claim was delivery at the NEW source value, the settled spike being compact-only — so I ran it: the host invoked the hook with `"source":"clear"` and the sentinel came back verbatim on the next turn with no tool calls, firing exactly once and not at session start. On the dispatch's first question I come down against its premise, by execution: an added sixth source value cannot red this test, only a rename can, so the assertion is blind in this entity's own failure direction rather than over-strict. On the third, the clear-reader is served by exactly one sentence — the boot command — and "You are mid-loop" is asserted on a source where it is often false; unobserved harm and no content AC to cite make that a deferred risk on wording, not a rejection. Full suite carries the one pre-existing environmental red, proven pre-existing at the merge-base rather than inherited. No material finding. **Recommend PASSED**, with findings 2, 4 and 5 as deferred risks and decisions for the captain, and 6 as polish. CI: no run requested or approved; the PR untouched.
