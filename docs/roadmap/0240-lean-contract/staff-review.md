# 0240 lean-contract — sprint-wide preflight readiness review

## Mechanism note

This is an independent staff review. I did not write any of the 0240 work; my charge is to refute the FO's and the ideation-ensigns' assumptions, not to confirm them. The review covers the sprint as a whole (DoD coverage, wave sequencing, cross-member composition of the shared-file edits, cold-boot Commander drivability), the soundness of the two flagged-complex members (7d4 split-root debrief routing, k4 boot-write deferral), and an adversarial spike-refutation pass that built the strongest case that each of the three load-bearing spikes (k4 sweep-independence, 84 greet-no-Status-Viewer, f5 launcher-widening) is wrong before tracing the code to confirm or refute. Three lens reviews were merged into this verdict; the FO and the ideation-ensigns did not run it.

## Evidence base

Read in full: the sprint carve (`docs/roadmap/0240-lean-contract/index.md`) and all seven member bodies under `docs/dev/.spacedock-state/`. Traced against source: `skills/first-officer/references/first-officer-shared-core.md` (lines 7, 44–47, 80, 142, 154, 159–177, 180), `skills/first-officer/references/claude-first-officer-runtime.md:35`, `internal/contractlint/boot_resident_closure_test.go` (`bootResidentBodies`:20, `foReferenceCores`:33, `bodyReferenceRe`:45, the os.Stat walk:125–133), `internal/journeymetrics/readadoption.go` (`statusReadLaunchers`:13, `commandInvokesStatusRead`:23), and `docs/dev/.spacedock-state/_mods/pr-merge.md` (startup hook lines 20–23). z4's AC-4 byte baselines were taken as measured by the sprint-whole lens and corroborated by the in-body table (fn-binding-refinements.md:94–99).

Every load-bearing spike CONCLUSION survives refutation: the deferrals are boot-safe, f5's fold is sound, 7d4's routing reuses the shipped commit discipline. The material problems are not in mechanism design — they are in (a) DoD checklist text that contradicts ratified gate decisions, (b) a contradictory #6 registry scope, (c) an under-coded reachability surface for the deferred references whose guard gap the closure oracle structurally cannot see, and (d) three spike/spec records that reach the right answer through a wrong premise a gate-locking review must correct.

## VERDICT: SOUND-WITH-FIXES — drivable after the ten Material findings are folded; not cold-boot drivable as-is.

No BLOCKERS: no mechanism breaks boot, breaks a member outright, or invalidates the sequencing. But a cold-boot Commander driving the release from the DoD would mis-gate on bullets that contradict reality (M1, M2) and would implement z4 ambiguously (M3), so the gates must not lock until the Material set is folded.

## Material (must fix before gates lock)

A cross-cutting root unites M4/M5/M6: the contractlint closure oracle's coverage boundary stops at `bootResidentBodies`. It os.Stats `references/*.md` existence and checks `foReferenceCores` anchor-carriage, but it never walks the deferred reference files' prose and never matches a prose section-name pointer. Three of the ten Material items are that one gap seen from different angles.

1. **z4 / DoD bullet 7 (index.md:24) is unsatisfiable and contradicts the ratified AC-4 re-scope.** "Every FO contract file ≤ its v0.22.0 baseline" cannot hold for `claude-fo-dispatch.md` (already +508 B over baseline on `main`: 22489→22997, and z4 nets ~0 on it). The gate already ratified the fix at index.md:47 but the DoD text was never updated. Fix: edit the bullet to the two-part AC-4 measure (z4-own delta ≤ 0 vs origin/main per edited file; absolute ≤ v0.22.0 only for files z4 materially restructures) and exempt `claude-fo-dispatch.md`'s pre-existing overage.

2. **scr / DoD bullet (index.md:21) credits 0240 with #290's shipped work and omits scr's actual deliverable.** The TDD/"CODE only"/code-only markers are already absent — #290 delivered that seed (gate decision index.md:44). scr's ratified scope is the −49-char stage-name-parenthetical neutralization plus a NEW contractlint structural-absence regression guard. Fix: rewrite the bullet to scr's scope and name #290 as prior art, not a 0240 deliverable.

3. **z4 + 84 + k4 / #6 registry scope is contradictory.** z4 AC-8 (fn-binding-refinements.md:41) commits to replacing only `## Dispatch` + `## Merge and Cleanup` and preserving those two sections' load-points/greet-guards, yet #6 (lines 28, 79) plus 84's and k4's Coordination notes expect all four pointer sections folded. The two readings diverge on duplication and on which guards AC-8 must prove survive. Fix: pin one reading; if fold-all-four, amend AC-8 to require all four load-points + four greet-guards (status-query, write/new, dispatch, merge) survive — the closure tests prove filename resolution only, never that a folded section's done-when/guard survived.

4. **84 / `fo-status-viewer.md` is guarded for existence, not content-carriage.** AC-3 claims a status path "resolves ALL the moved guidance" but binds only to `TestBootResidentDeferredLoadPointsResolve`, which os.Stats existence. 84 registers nothing in `foReferenceCores`; k4 registers `fo-write-core.md` + a byte diff. Fix: register `fo-status-viewer.md → ["## Status Viewer","### Captain-Facing State Display","## Issue Filing"]` (symmetric with k4). This also makes 84 the first editor of `boot_resident_closure_test.go`, repairing k4's Coordination assumption (line 88) which is otherwise false — as planned 84 edits no test file, so k4 is the sole editor and the claimed Wave-2 test-file collision does not exist.

5. **84 + k4 / the dangling prose "(see FO Write Scope)" pointer is unguarded after the cut.** shared-core:47 (inside `## Status Viewer`, which 84 moves verbatim into `fo-status-viewer.md`) names a section k4 then DELETES. `bodyReferenceRe` (boot_resident_closure_test.go:45) walks only `bootResidentBodies` and matches only `references/X.md` tokens — so even k4 Edit 4's path-form conversion *inside* `fo-status-viewer.md` is never os.Stat'd, and a prose section-name pointer is invisible everywhere. The only net is k4's manual AC-3c inventory editing a sibling's file. Fix: make k4 Edit 4 target `fo-status-viewer.md`, convert the pointer to the `references/fo-write-core.md` path form, and add a coded grep over BOTH new reference files so DoD's "reachability proven as a real gate, not a prose claim" (index.md:20) actually holds.

6. **k4 / AC-2(b) "sole boot-resident mention" grep is contradicted by k4's own Edit 4.** Edit 4 rewrites `claude-first-officer-runtime.md:35` (verified in `bootResidentBodies`:22) to name `references/fo-write-core.md`, creating a SECOND boot-resident mention. The "sole mention" grep (defer-write-scope-id-styles.md:79) false-fails or gets silently waived. Fix: reword to "every boot-resident mention of fo-write-core.md is a deferred pointer/name, never a greet-time load," and note runtime:35 sits in `## Filing New Entities`, off the greet path.

7. **7d4 / declared-but-absent state checkout is an unhandled error path.** The split-root branch runs `git -C {state_checkout}` assuming the checkout exists; the `entity_dir_present == false` case (fresh clone / removed worktree, which the FO boot halt-gates at shared-core:159) yields opaque git errors, and the skill is user-invocable independent of an FO boot. No AC/fixture exercises it. Fix: read `entity_dir_present`/`entity_dir` from `status --boot --json` (internal/status/boot.go emits both), halt with "state not initialized" if absent (mirror state.ensure-ready), add a fixture.

8. **k4 / the load-bearing AC-2 spike misidentifies the boot-sweep write command.** Verified: `_mods/pr-merge.md` (lines 20–23) finalizes with `status --set … mod-block=`, `status --set … completed verdict=PASSED worktree=`, then `status --archive`; `merge guard` appears nowhere in the mod and is the DEFERRED terminal ceremony in `fo-merge-core.md`, never a boot path. k4's spike (line 70) and AC-2's `merge_guard_test.go` pin therefore exercise a non-boot command. The conclusion holds (status --set/--archive are equally binary-self-authorizing). Fix: re-point the spike trace + AC-2 pin to the runSet/runArchive guards, and correct gate-decision index.md:46, which records the same wrong command.

9. **84 / the greet-trace falsely claims `--boot` is not documented in the Status Viewer section.** shared-core:44 documents `--boot` INSIDE `## Status Viewer` (and :45 documents `--validate`), and the greet invokes `status --boot` via «state.boot» (shared-core:154), so 84's spike-#1 enumeration is false. The conclusion holds — the greet reaches no Captain-Facing DISPLAY rules and `--boot` is independently resident in Startup step 5, so the deferred file carries a DUPLICATE `--boot` bullet, not the sole copy. Fix the trace to make that the argument.

10. **f5 / AC-2 names the wrong env var and the exact-token map drops the contract-canonical quoted launcher.** Verified `statusReadLaunchers` = `{spacedock, sd, ${SPACEDOCK_BIN:-spacedock}}` and `commandInvokesStatusRead` keys on exact `tokens[i-1]` with no quote-stripping. AC-2 names `$SPACEDOCK` (the contract invariant at shared-core:7 is `${SPACEDOCK_BIN:-spacedock}`) and cannot recover the QUOTED `"${SPACEDOCK_BIN:-spacedock}"` the contract prescribes. The de-vacuum goal is met (`spacedock_launcher` alone makes the count > 0) but the metric undercounts the canonical form 82k's AC-1 depends on. Fix: normalize (strip quotes; canonicalize the SPACEDOCK_BIN family) instead of three literal tokens; add the quoted positive to `TestCommandInvokesStatusRead`.

## Polish (record for next sprint)

- **82k / carve:** index.md:33 calls Wave 1 "both edit the ensign core," but 82k Sites C (shared-core:229) and D (:100) edit the FO shared core, making 82k its first toucher; Site D (:100) is adjacent to z4 #4's target (:105). Note this and the adjacency so z4 re-anchors carefully.
- **82k + f5:** the live FO+ensign AC-1 gates the least-impactful change on a fragile single-digit metric. Pre-agree the degradation path (ship the trim; treat the ensign-aware metric as a forward-looking non-regression check) rather than discovering inconclusiveness at the gate.
- **scr:** the re-title and slug rename (gate decision index.md:44) are outstanding; rename the slug before implementation to clear the archived `ep0ra3z` `--resolve` ambiguity.
- **7d4:** "mirrors exactly that test" overstates the ClassifyState match (omits the absolute-path/`..` rejections at internal/status/state.go:49–55). Better: consume `entity_dir` from `status --boot --json` — which also closes the M7 absent-checkout gap in one fix.
- **f5:** the ~92% launcher-miss figure is corpus-dependent and the in-body breakdown sums to 285, not 482; state it as a range and reconcile, optionally excluding /tmp dev-build paths. Direction is robust.

## Sequencing assessment

The strict Wave 1 < Wave 2 < Wave 3 ordering plus the f5-predecessor is correct and load-bearing — the same-file contention on `first-officer-shared-core.md` is real (82k Sites C/D in Wave 1, 84 + k4 in Wave 2, z4 #4/#5/#6 in Wave 3), and z4 #6 binding the `references/*.md` paths that 84/k4 create genuinely forces z4 last (naming a Wave-2 file before it exists REDS the os.Stat walk, fn-binding-refinements.md:86). Two corrections: (1) the Wave-1 line understates 82k as the first toucher of the shared core (Polish above); (2) the Wave-2 coordination assumes 84 edits `boot_resident_closure_test.go` first, but as planned 84 edits no test file — k4 is the sole editor. Folding M4 (registering 84's reference in `foReferenceCores`) makes 84 the first test-file editor and restores the assumption, so M4 and the sequencing repair are the same edit.

## Riskiest claim to demonstrate first

"No reachability is lost, proven as a real gate, not a prose claim" (DoD, index.md:20) — the sprint's central deliverable — currently rests on os.Stat existence plus a manual cross-ref inventory, and the closure oracle structurally cannot see a dangling prose pointer in a deferred reference file (M4 + M5). Demonstrate this first, and cheaply: on a scratch branch, extend the oracle to walk the two new reference files (`fo-status-viewer.md`, `fo-write-core.md`) for any bare "FO Write Scope"/"ID Styles"/"Status Viewer" prose pointer lacking a resolving `references/*.md` token, then prove it RED-then-GREEN by planting a dangling "(see FO Write Scope)" in `fo-status-viewer.md`. That is minutes of work, it de-risks M4 and M5 together, and if it cannot be made to go RED on the planted pointer then the DoD's reachability gate is not a gate — which would invalidate the rest of the Wave-2/Wave-3 deferral work.