---
session-date: 2026-06-21
sprint: 0230-stable-finalization
first-commit: aa49853c
last-commit: 66950a14
duration: ~7h (2026-06-21 ~16:00 → 2026-06-22 ~00:30 PT)
---

# Session Debrief — 0230 stable finalization (drive phase) — 2026-06-21

The Commander drive of the lean v0.23.0 cut. Opened at 7 members; grew to 11 as a flake-landscape investigation and an M4 reject surfaced real work. **Token clawback delivered, value gate met, the e2e-gate flakes mostly proved to be over-strict tests.** Cut not yet fired — M4 (one-line fix), v9 (implementation), the pre-cut audit, release notes, and a final authoritative E2E remain.

## Shipped (merged to `main`)
- **y2** `fo-contract-token-cut` — [#426](https://github.com/spacedock-dev/spacedock/pull/426). FO contract token-clawback via call-site collapse; `«fn»` bodies authoritative, shared-core 28325 ≤ 28586. Folded in the no-broad-sweep prohibition (code-gated) + a re-added codex pre-wait emit imperative (audit-driven, code-gated).
- **ad** `trim-dispatch-adapter-prose` — [#428](https://github.com/spacedock-dev/spacedock/pull/428). The +5441B half: collapsed `«dispatch.build»` + dropped `«merge.guard»` narration; fo-dispatch-core 17446 ≤ 17488, fo-merge-core 7436 ≤ 8059; SB2 spans byte-intact, git-diff-ZERO to claude-fo-dispatch.md + shared-core.
- **z2t** `spacedock-marketplace-source-env` — [#424](https://github.com/spacedock-dev/spacedock/pull/424). Codex edge installs as `spacedock@spacedock-edge` (channel-via-marketplace-name); + a CI-fix bounce to isolate the codex-live baseline CODEX_HOME (was self-colliding).
- **x1** `ensign-runtime-binding-block-cleanup` — [#423](https://github.com/spacedock-dev/spacedock/pull/423). Codex/pi ensign adapters reduced to host-specific binding blocks; codex-ensign 1522 ≤ 2390, pi-ensign 1333 ≤ 1768.
- **7hc** `opus-rejection-flow-reviewer-routing-flake` — [#425](https://github.com/spacedock-dev/spacedock/pull/425). The "opus flake" was a TEST false-positive (body-blind assertion miscounting a mandatory shutdown_request). Fix = body-aware + team-aware reviewer-reuse assertion; test-only.
- **v5** `feedback-3-cycle-escalation-marker-flake` — [#427](https://github.com/spacedock-dev/spacedock/pull/427). Also a test false-positive: the exact escalation marker lives only in deferred README body the FO is told not to read. Fix grades the escalation BEHAVIOR; test-only.
- **25** `codex-rejection-flow-keepalive-diagnosis` — [#429](https://github.com/spacedock-dev/spacedock/pull/429). Third test false-positive: codex `«addressable-worker»` is non-deterministically absent → contract-correct fresh-dispatch; the absent-detector matched 0/10 live wordings. Fix = structural narration detector; test-only.
- **y8** `headless-no-conn-gate-discipline` — [#430](https://github.com/spacedock-dev/spacedock/pull/430). The ONE real model-discipline gap: sonnet (and opus) headless FO fabricates a "given-the-conn" grant + self-approves gates, + a zero-discover root FS sweep. Fix = falsifiable conn antecedent + tightened zero-discover detector; reduced-rate DETECTED lane (CI live test is the backstop). Proven opus 9/9 + sonnet 8/8, byte-neutral.
- **(#420)** edge-cask channel-switch — already in `main` before the sprint (regenerated by goreleaser at the cut).

## Filed (backlog)
- **v5** `feedback-3-cycle-escalation-marker-flake` — shipped same session (M8).
- **y8** `headless-no-conn-gate-discipline` — shipped same session (M9).
- **25** `codex-rejection-flow-keepalive-diagnosis` — shipped same session (M10).
- **z4q** `fn-binding-refinements` — next-sprint (untagged): promote reuse-condition-4 to a `«reuse.model-match»` «fn»; disambiguate `→ prose` on `«dispatch.next-action»`; add a `→ runtime-binding` pointer to worker.spawn/shutdown. From the «fn»-effectiveness audit (verdict: mostly-effective; the M2 "regression" was a verified false alarm).
- **3g8** `live-e2e-node-runtime-bump` — next-sprint (untagged): bump the live-E2E harness `setup-node node-version: "20"` (3 places in runtime-live-e2e.yml) off node 20; #359 fixed the action runtimes, not this CLI-harness pin. Needs a live-E2E re-validation (changes the CLI runtime).

## Decisions (captain)
- **Ideation:** took the 2026-06-20 sprint-wide staff review as the ideation gate-pass for the 6 original members (advance straight to implementation). v9 got its own focused ideation pass (added later, not in that review).
- **M1 scope:** call-site collapse — tighten the *callers* of the post-v0.22.0 `«fn»` bodies; bodies stay the single authoritative definition.
- **M1 audit concern:** fold the dropped codex pre-wait emit imperative back in AND code-gate it (chosen over merge-as-is).
- **M9:** no forge-proof binary gate is possible (the conn is the FO's own judgment; any signal it passes is forgeable) → accept the prose-floor-raise + CI-detection backstop as a reduced-rate detected lane. Validate on OPUS (the model the de-risk run showed actually fails), not just sonnet.
- **M10:** diagnose-first (real codex gap vs assertion false-positive) → it was the assertion; fix the test, not the FO prose.
- **Flake landscape:** file M9 (tag-blocker) + M10 (diagnose) as new members; fold the `filing` opus var-capture false-positive into v9; defer the node20 bump + the «fn» refinements to next sprint.
- **M4:** reject the advisory-only guard (a guard nothing runs isn't a gate); fold the out-of-scope `stable`-advances-to-wrong-SHA MAJOR into the same release.yml pass.

## Issues — Workflow
- **e2e-gate was flaky on 4 lanes; 3 were over-strict TESTS, 1 was real.** rejection-flow (opus+codex), feedback-3-cycle (sonnet), codex-keepalive all = over-strict assertions punishing contract-correct FO behavior (M7/M8/M10, all test-only fixes). Only gate-discipline (M9) was real model misbehavior.
- **The de-risk Runtime Live E2E on main (run 27920641440) was pivotal** — confirmed M7/M8/M10 delivered (codex/sonnet/pi/offline green) AND revealed OPUS is the model that fails gate-discipline (sonnet passes). Without it, M9 would have merged sonnet-proven on the model that already passed.
- **Per-PR merges happened on validation evidence, NOT a CI Runtime Live E2E** — E2E is `workflow_dispatch`+PR-gated (not auto on push to main) and is not a required check. The authoritative e2e-gate run (on the final tagged commit) has not yet been run with all members in.
- **M4 manifest-tag-gate shipped advisory-only** (a real binary nothing in CI invoked) — caught at validation, now CI-wired. Cycle-2 then rejected again for a one-line doc/code contradiction (see What's Next).

## Issues — Spacedock
- **Pre-existing zero-discover detector evasion** — `find <root> <root>/docs` short-circuits the scoping loop in `detectBroadSearchAtBoot` (byte-identical to main, not introduced by M9). Surfaced by the M9 adversarial audit. Candidate follow-up (not filed as a GH issue — internal).
- **release.yml workflow guards are syntactic-presence checks** — they miss `if: && false` / `continue-on-error` / RELEASE_COMMIT-reassignment un-wirings (class-wide, shared with the sibling e2egate guard). Noted by M4 cycle-2 validation as non-blocking.
- (Not filing GitHub issues this session — no captain approval requested for that.)

## Observations
- **The flake investigation paid for itself many times over** — it turned a vague "e2e-gate is flaky" into a precise map (3 test-bugs + 1 real gap + the node20 finding), and the de-risk run pinned the opus-vs-sonnet split that the sonnet-only M9 validation would have missed.
- **"A guard nothing runs isn't a gate"** (M4) and **"validate on the model that fails, not the one that passes"** (M9) were the two sharpest catches — both came from adversarial verification, not the happy path.
- **The `«fn»` binding program is genuinely effective** — ~12KB clawed while every load-bearing guarantee stays machine-pinned; the `→ shipped/prose/runtime-binding` arrow trichotomy is the standout legibility win.
- **A member `fe` `codex-edge-channel-marketplace-source` (validation, score 0.7) appeared in the sprint that this Commander did not dispatch** — likely a parallel session. RECONCILE OWNERSHIP before the cut (see handoff).

## What's Next (the cut is NOT yet fired)
1. **M4 `releasing-doc-pre-stamp-drift` (7y)** — validation REJECTED cycle 2. One-line fix: `docs/releasing.md:25` still says `git push origin main:refs/heads/stable`; cycle-2 code does `$RELEASE_COMMIT:refs/heads/stable`. Fix the stale prose → re-validate → merge. (The CI-wired gate + stable-advance fix are good.)
2. **v9 `fo-binary-launcher-invariant`** — ideation complete; present the ideation gate → implementation (launcher-invariant pin + code-gate over bare-`spacedock` FO examples + SPACEDOCK_BIN≠PATH regression + the folded-in `filing` var-capture assertion hardening) → validation → merge. Serializes on shared-core (253B headroom).
3. **`fe` `codex-edge-channel-marketplace-source`** — at validation; reconcile ownership (parallel session?) before the cut.
4. **VALUE-GATE CHECK** — re-confirm all FO+ensign bytes ≤ v0.22.0 after v9 (+ fe if it touches contract files). Currently MET on `main` 66950a14.
5. **Pre-cut antipattern audit** — independent reviewer over the assembled sprint, tag NOT yet fired.
6. **Write v0.23.0 release notes** — must cover pre.1/pre.2 content + the token clawback.
7. **Final authoritative Runtime Live E2E on main** — `workflow_dispatch` + approve the CI-E2E/-CODEX/-PI/-OPUS envs; confirm OPUS goes green with M9 in (the de-risk run was red on opus pre-M9). This is the e2e-gate the tag fires on.
8. **Cut** per the now-reconciled `docs/releasing.md`: stamp 0.23.0 into `.claude-plugin/plugin.json` + `.codex-plugin/plugin.json`, GREEN the commit, tag the e2e-green SHA, push to fire goreleaser (regenerates both casks incl #420, advances `stable` to the tagged SHA). Verify a fresh `brew install` launches.
