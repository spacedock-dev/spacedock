# Sprint 0201 — `yw` (readme-and-docs-architecture-cleanup) — preflight staff review

**Method:** independent adversarial review (not the FO, not the ideation ensign) of the sprint's sole doc-surface owner `yw`, re-checked against the real tree at the **`origin/main`** target (HEAD `55158746`, #343 doc-site land — the post-flip trunk the lead named; local `main` `3f941ba7` is four commits behind so all reads use `origin/main`) and the `next` working tree. Lenses: is `install_doc_test.go` actually a banned prose-grep on `main`; retarget-map page existence; AC external-provability; blast-radius of the test removal; over-reach/sequencing. Every retarget anchor checked with `git show origin/main:<page>` against the page's actual headings — not the entity's self-report.

**Verdict: NOT-READY** — two Material defects must be folded before the gate. The retarget map (the entity's central anti-collision deliverable) points three of the five members at `docs/site/get-started/install.md` and `command-reference.md` **sections that do not exist on the target branch** — its anchor descriptions match the 169-line `install-journey.md` (the file being deleted) rather than the slim canonical site pages on `main`. And `zrc`'s canonical home cites a **non-existent page** (`first-launch.md`). The test-removal mechanism (AC-2), the AC-1 grep claim, and the README structural diff (AC-3) all hold against real code — those refuted nothing.

## Material findings (fold before the gate)

**M1 — the retarget map's `install.md` anchors describe the deleted file, not the canonical site page; te/gp/gj are pointed at sections that don't exist on `main`.**
The entity's load-bearing premise — *"`docs/site/get-started/install.md` (on `main`) already carries `install-journey.md`'s content … the site page IS that content"* (body, Test plan + Branch-grounding) — is **false**. The site `install.md` on `origin/main` is a deliberately slimmed ~60-line page (`git show origin/main:docs/site/get-started/install.md`) with headings: `# Install Spacedock`, `## Launch`, `## Skills`, `## Sandboxing`, `## Troubleshooting`, `## Next`. The `next` `install-journey.md` (168 lines) has the rich sections the map cites: `## Install with Homebrew`, two `**Confirm it.**` steps, `## Use Codex or Pi instead`, `## Build from source (for development)`, `## Keep things in sync`, `## Command grammar`. None of those survive onto the site page — e.g. "Keep things in sync" content collapsed to a one-line `spacedock doctor` under `## Troubleshooting`; "Build from source" and the Codex/Pi prose are simply absent.

Consequently the retarget map points members at homes that aren't there:
- **te** → *"`docs/site/get-started/install.md` 'Keep things in sync' section"* — **no such section exists** on `install.md` (origin/main). It exists only on the deleted `install-journey.md:147`.
- **gp** → *"`install.md` 'Build from source' / a new 'Edge channel' note"* — **no 'Build from source' section** on `install.md` (origin/main); that content lives at `docs/site/contributing/build-from-source.md`, a page the map never names. (gp's *other* target, `contributing/releasing.md`, IS real — it is a symlink, mode `120000`, → `docs/releasing.md`, which carries the edge/plugin-manifest lines `docs/releasing.md:12-17`. That half holds.)
- **gj** → *"`command-reference.md` (the `--version` section — its example `spacedock 0.20.0 (contract 1)` gets the Sandbox line)"* and *"`install.md` two 'Confirm it' steps (the `spacedock --version` example output)"* — **neither exists**: `command-reference.md` (origin/main) has no `--version` *section* (only a one-line mention in the intro and a `## Launch`/`## Setup`/`## Workflow` body that explicitly defers flags to `--help`), and a grep for any `spacedock X.Y.Z (contract N)` example across all `docs/site/**/*.md` on origin/main returns **nothing**. gj would have to *author* a version-example block, not edit one.

This is not cosmetic: AC-4 asserts the proof is *"the canonical homes in the retarget map carry the behavior's wording"* and names these exact anchors as the checkable diff. With the anchors wrong, AC-4 is not externally checkable as written — a reviewer following it to `install.md`'s "Keep things in sync" finds no such heading. The anti-collision contract (the entity's whole reason to exist) rests on each member's content having a real, named home.
*Fix:* re-derive the retarget map against the **actual `origin/main` site pages**, not `install-journey.md`. Either (a) name the real homes that exist — te's sync hint → `install.md` `## Troubleshooting` or `command-reference.md` `## Setup` (which already says *"When `doctor` reports the plugin is out of date, refresh it with `spacedock install`"*); gp's from-source → `contributing/build-from-source.md`; gj's version block → a new sub-section yw creates in `command-reference.md` — and state that yw **creates** the missing sections rather than edits existing ones; or (b) scope AC-4 to "yw authors the named content on the page that owns each topic, creating sections where none exist," and list the real page per member. Until the homes are real, AC-4's git-show proof can't be run. → routed to `yw` ensign.

**M2 — `zrc`'s canonical home cites a page that does not exist on the target branch.**
The map's `zrc` row names *"`docs/site/get-started/first-launch.md` 'command grammar' if it enumerates the permission posture."* `docs/site/get-started/first-launch.md` is **absent on origin/main** (`git cat-file -e` fails); the get-started pages are `first-workflow.md`, `install.md`, `survey.md`. The "command grammar" content the entity is reaching for exists only on the deleted `install-journey.md:159` (`## Command grammar`); on the site it lives in `command-reference.md`'s `## Launch` section, not in any get-started page. So the `zrc` home is doubly wrong — wrong filename and wrong page for the content. (zrc's *primary* target, `command-reference.md` `## Launch`, IS real and is the right home; only the `first-launch.md` secondary is broken.)
*Fix:* drop the `first-launch.md` reference and land zrc's launch-posture wording in `command-reference.md` `## Launch` (the real grammar home). If a get-started "first launch" page is wanted it must be created, but that is out of yw's "does not restructure the site" scope (Out-of-scope §1) — so don't cite it. → routed to `yw` ensign.

## Polish (may ride implementation)

- **P1 (AC-2 / blast-radius — confirmed sound, no fold):** verified line-exact on origin/main. `readInstallJourney` is the only non-test-func symbol in `install_doc_test.go` and is referenced nowhere else (`git grep readInstallJourney origin/main -- internal/release` → only `install_doc_test.go:16,43,65`). `executableShellCommands` is defined at `workflow_exec_guard_test.go:441` and consumed by `channel_agreement_guard_test.go:40`, `e2egate_workflow_test.go:106`, and six sites in `workflow_exec_guard_test.go` — it stays. Deleting the whole file leaves zero dangling references; `go vet ./internal/release/` is already green, and removal drops only two independent passing test entry points plus a file-local helper, so it cannot break compilation or other tests. AC-2's "go test green after removal" holds at the mechanism level. Recorded as a confirmed non-defect.
- **P2 (install_doc_test.go IS a banned prose-grep on `main` — confirmed):** read `git show origin/main:internal/release/install_doc_test.go`. Both tests `os.ReadFile` `docs/site/get-started/install.md` and assert author-written substrings: `TestInstallJourneyDocumentsLinuxPath` requires a `curl … raw.githubusercontent.com/spacedock-dev/spacedock … | sh` line the author wrote; `TestInstallJourneyDoesNotOverclaimLinuxSandbox` rejects literal phrasings ("sandboxed on linux"). The expected values originate in the file under test — no independent source (the install.sh URL is not cross-checked against the actual script, the sandbox claim not against `internal/safehouse`). The entity's "repointed to the site page but still tautological" claim is **correct**: repointing the read from `install-journey.md` to the site page moved the tautology, didn't cure it. Removal claim stands. Confirmed non-defect.
- **P3 (AC-1 grep — confirmed):** `git grep -ln install-journey origin/main -- '*.md' '*.go' '*.y*ml'` (excluding archive/state/claude/roadmap) returns exactly one live hit, `internal/release/install_doc_test.go` (its own path/error strings), which AC-2 removes. The entity's AC-1 note predicting this exact single hit is accurate. The file is already absent on origin/main, so AC-1's file-absence half is already true there, as the body states.
- **P4 (AC-3 — confirmed real work):** README on origin/main carries `## Quick start` (`:80`), `## How it works` (`:105`), `## Usage` (`:132`) — so the structural-removal diff is genuine, checkable work, not a no-op. The Install section already links `[docs/site/get-started/install.md](docs/site/get-started/install.md)` (`:77`), so the entity's open "(A) published URL vs (B) in-repo link" decision is honestly surfaced — (B) is already partly in place. Non-blocking.
- **P5 (over-reach / two-wave sequencing — sound):** the wave-1 (structural cleanup: test-removal + README slim, AC-1/2/3, depends on nothing) → behaviors land → wave-2 (behavior-docs, AC-4) ordering is correct and avoids the "documenting unbuilt behavior" deadlock. No behavior goes undocumented: each of the five maps to a topic with a real owning page (modulo M1/M2's wrong anchors). Consolidating the doc surface to one writer is the right anti-collision call — the members' `install-journey.md` diffs would have collided on a deleted file. No fold; the *concept* is sound, only the anchor table (M1/M2) is wrong.
- **P6 (unmapped relevant page):** `docs/site/reference/sandbox.md` exists on origin/main and is the natural home for gj's "per-runtime sandbox state" and zrc's sandbox-posture wording, yet the map routes both to `command-reference.md` only. Not a defect, but when re-deriving the map (M1) consider `sandbox.md` as a/the home for the sandbox-status content — `install.md`'s `## Sandboxing` already just links it.

## What was checked and refuted nothing material

- **The test is a real banned prose-grep on the target** (P2) — confirmed against `origin/main`'s blob, removal justified.
- **The removal is clean** (P1) — `readInstallJourney` file-local, `executableShellCommands` shared and retained; `go vet ./internal/release/` green; no orphan.
- **AC-1 grep + AC-3 README diff** (P3, P4) — both externally checkable and reflect real tree state on `origin/main`.
- **gp's `releasing.md` home** is real via the `120000` symlink → `docs/releasing.md`, which carries the edge/plugin-manifest lines. Only gp's *`install.md` "Build from source"* anchor is wrong (M1).
- **zrc's `command-reference.md` `## Launch`** primary home is real; only the `first-launch.md` secondary is broken (M2).
