# Sprint 0201 — post-flip release model — preflight staff review

**Method:** independent adversarial review (not the FO, not the ideation ensigns) of the five ready members — `gj` (startup-sandbox-status), `gp` (marketplace-repo-and-pinned-channels), `te`/`tes` (install-refresh-and-upgrade-hint), `zrc` (non-sandboxed-launch-auto-mode), `8p` (brew-cask-agentsview-safehouse-deps). Every entity claim re-checked against the real tree at `internal/cli/{init.go,cli.go,frontdoor.go,host_exec.go}`, `internal/contract/contract.go`, `.goreleaser.yaml`, and the `internal/cli/*_test.go` oracle set — not the entities' self-reports. Lenses: design soundness, proof-gaps, tautological/prose-grep ACs, over-engineering, blast-radius, cross-entity coherence.

**Verdict: NOT-READY** — one Material defect must be folded before the ideation gate locks (`zrc`): an unconditional argv change on the unsandboxed path breaks at least five existing exact-argv front-door oracles, and no entity surface names the co-edit. The other four members (`gj`, `gp`, `te`, `8p`) refuted nothing material — their riskiest-mechanism claims hold against the real code and their ACs are non-tautological. A handful of polish items can ride implementation.

## Material findings (fold before the gate)

**M1 — `zrc`: the unsandboxed argv injection breaks ≥5 existing exact-argv oracles; no entity surface names the co-edit.**
`zrc` injects `--permission-mode auto` (claude) / `--ask-for-approval on-request` (codex) into the **unwrapped** launch arm (`frontdoor.go:299-307` claude, `:457-464` codex), gated only on `!wrap` and operator-flag-absence — verified those arms carry no such flag today. But the unwrapped arm is pinned by `equalArgv` exact-match in at least five existing tests, all of which run `lookFound` + no `.safehouse` (so they hit the unsandboxed path) and whose `want` argv has no permission flag:
- `internal/cli/frontdoor_test.go:108` `TestClaudeFrontDoorLaunchesOnCompatible` — `want := {"claude","--agent","spacedock:first-officer","-p","do the thing",wantBootstrapPrompt}`
- `internal/cli/safehouse_frontdoor_test.go:159` `TestClaudeNoSafehouseLaunchesPlain`
- `internal/cli/safehouse_frontdoor_test.go:317` `TestCodexNoSafehouseLaunchesPlainNoBypass`
- `internal/cli/frontdoor_stray_prompt_test.go:39` and `:70`
- `internal/cli/plugin_dir_frontdoor_test.go:64`

`zrc`'s ACs name `safehouse_frontdoor_test.go:159`/`:317` only as the oracle SHAPE to *mirror* for the new tests; the body has no blast-radius section and never states these existing exact-match oracles must be updated to carry the injected flag. An implementer who writes the new AC tests and ships will leave `go test ./internal/cli/` RED while believing the ACs passed — the same failure class as sprint 0200's M2. (Unlike `te`'s front-door hint — see below — this argv change is **unconditional** on the unsandboxed path, so it is NOT insulated by the `Version=="dev"` test sentinel; every unsandboxed exact-argv oracle breaks.)
*Fix:* add a "Implementation co-edit" line to `zrc` naming the five exact-argv oracles above (claude: `frontdoor_test.go:108`, `safehouse_frontdoor_test.go:159`, `frontdoor_stray_prompt_test.go:39/:70`, `plugin_dir_frontdoor_test.go:64`; codex: `safehouse_frontdoor_test.go:317`) as required updates carrying the injected flag, and broaden the test-plan success condition from "the new oracle asserts the flag" to "`go test ./internal/cli/` green over the whole package after the change." Under captain option (B) (no codex change) the codex oracle is untouched and only the four claude oracles move. → **routed to `zrc` ensign**

## Polish (may ride implementation)

- **P1 (`gj`):** the body's codex line (`startup-sandbox-status.md:60`) claims codex "spacedock enabled" enablement "reuses the same listing's status field." `codexEntryInstalled` (`host_exec.go:113-126`) parses only an `installed` / `not installed` status — there is **no** current codex enablement signal (the `(installed[, enabled])` parenthetical at `:107` is the documented *older* codex format, not parsed). gj's own `--version` example renders codex as bare `codex: installed` (no "spacedock enabled"), and no gj AC requires codex enablement (AC-4 is claude-only, AC-5 is the probe-error case). So the design is self-consistent at the AC layer; the body prose overstates what exists. Tighten the `:60` sentence to: codex renders install-state only (no enablement field exists to read), matching the example. Non-blocking.
- **P2 (`gj`):** AC-3's first-line regex `^spacedock .* \(contract \d+\)$` is the right guard for the load-bearing FO/ensign parse — verified `printVersion` (`cli.go:503-505`) emits exactly `spacedock %s (contract %d)` and the comment at `:499-502` marks the token load-bearing. The expected value (the regex) is test-supplied and the rendered first line is the output under test, so this is a legitimate behavioral guard, not a prose-grep. No change; recorded as a confirmed non-defect.
- **P3 (`zrc`):** AC-3's codex analog drives `--ask-for-approval untrusted` in the passthrough, but `--ask-for-approval` is NOT in `valueTakingHostFlags["codex"]` (`frontdoor.go:513-521`, which carries `--sandbox`/`-c`/`--profile` etc. but not `--ask-for-approval`). This does not affect argv assembly (zrc's own `passthroughHasFlag` helper reads the passthrough directly), but the stray-prompt advisory would mis-classify the successor token. If `zrc` injects `--ask-for-approval`, it should also add it to `valueTakingHostFlags["codex"]` so the advisory stays accurate. Implementation-detail; can ride.
- **P4 (`gj`↔`zrc` coherence):** the index frames `zrc` as "consuming gj's detection," but the two read **different** predicates — gj's three-way state is `(Present(dir)||flags) && Available`, while `zrc` reads the existing `wrap = Present(dir) || forceSafehouse || len(safehouseFlags)` (`frontdoor.go:294`), which does NOT check `Available`. They still compose correctly at the launch boundary: when `wrap` is true the launch aborts unless `Available` (`frontdoor.go:311`), so a proceeding `wrap==true` == gj's "enabled" and `!wrap` covers both of gj's non-sandboxed states — exactly zrc's intent. `zrc` is honest that it reads `wrap`, not gj's new code. No shared-file collision (gj touches `launchBanner`/`status`/`printVersion`; zrc touches the `runClaude`/`runCodex` argv assembly). Recorded as a confirmed non-defect; the "consumes gj" wording is a concept-share, not a code dependency — they can implement in parallel.
- **P5 (`te` blast-radius, confirmed safe):** `te`'s AC-4 makes `gateHost` print an upgrade hint on `Compatible` (`frontdoor.go:231-234` is silent today — verified). This does NOT break the existing exact-stderr/exact-argv compatible-launch oracles because the hint fires only when `cli.Version` is valid semver, and the test binary's `Version` is the `dev` sentinel (asserted at `launch_banner_wording_test.go:141`). `te` explicitly gates the hint off for non-semver `Version` and keeps the hint additive to the existing `Compatible` message (`contract.go:148`), preserving the verbatim `OK: ...` substring that `init_test.go:115` and the gate tests assert. Verified, no fold needed — noted so the implementer keeps the `dev`-suppression invariant.
- **P6 (`gp` sequencing, confirmed):** `gp`'s migration step declares `te`/`tes` a hard predecessor (the `installArgvSequence` repoint relies on a reliable re-pull). Verified the dependency is real: `runInit`'s codex arm short-circuits to doctor on `resolved != ""` (`init.go:47-52`) and never re-installs — exactly `te`'s Facet-1 bug — so a codex user migrating before `te` lands would silently no-op the repoint. The index already sequences `te` early. No change; the cross-entity ordering is sound.

## What was checked and refuted nothing

- **`gp`** — the decoupling spike (tag-pinned stable frozen while edge advances, one marketplace, two distinct resolved versions) is the riskiest mechanism and was exercised end-to-end against the real `claude` CLI with byte-level on-disk proof. AC-1 reproduces it as a host-gated behavior test; AC-2 is a git-tree invariant over two independently-authored branch trees (non-tautological); AC-3 drives the argv builder per channel. AC-2 was correctly scoped to the `source.ref` divergence the decouple removes, NOT a whole-tree fast-forward claim. The `marketplaceSource` change (`init.go:16`) and `@<devBranch>` shorthand removal are accurately located. No material defect.
- **`te`** — Facet 1 root cause verified line-exact: claude arm calls `ops.Install` unconditionally when `!check` (`init.go:30-39`); codex arm returns at the `if code != 0 || resolved != "" || check` guard (`init.go:49`) before any install. The "test codifies the bug" claim verified: `TestInitCodexInstallReadiness/compatible-installed` (`init_test.go:105-123`) asserts the present-compatible codex arm prints `OK` and bans add-prose — AC-1's plan to replace it is correct. Facet 2's silent-Compatible gate verified (`frontdoor.go:231-234`); `Compare` receives both display semvers (`contract.go:114-148`). The `compareVersion`-lives-in-`cli`-not-`contract` cycle hazard (`host_exec.go:170`) is correctly flagged in te's risk note. No material defect.
- **`8p`** — `.goreleaser.yaml` `homebrew_casks:` (`:94`) emits two casks (`spacedock` `:104`, `spacedock@next` `:132`) to `spacedock-dev/homebrew-tap`; both `caveats` blocks name `safehouse` + `agentsview` as "not installed by brew" (`:116-119`, `:142-145`). The `agent-safehouse.dev` vs code's `github.com/anthropics/safehouse` (`internal/safehouse/safehouse.go:14`) mismatch is real and correctly scoped out. The `dependencies: - cask: agentsview` add is sound; the goreleaser-schema + snapshot-render spike is the riskiest mechanism and was run. AC-2's caveats substring check is over goreleaser-rendered Ruby (a build artifact, not a model-read instruction file) with the expected value from the AC — not a banned prose-grep. No material defect.
- **`gj`** — the one unverified read (claude plugin `enabled`) is real: `pluginListEntry` (`host_exec.go:24-27`) reads `ID`+`InstallPath` only, NOT `enabled`, so adding the field is genuine new work, spiked live. The `--version` first-line invariant is load-bearing and AC-3 guards it correctly (see P2). Banner/boot changes are additive and the existing banner oracles are substring-based (`launch_banner_test.go:196`), so they survive. The only soft spot is the codex-enablement prose (P1). No material defect.
