# Fable seat delta vs the codex staff review — 0260-proportionality

## (a) Seat comparison

The codex report already covers 27 of the 51 fable findings — including every finding about the four DoD contradictions and the dispatch's missing seam table — but the fable seat adds a distinct layer codex never touched: the sprint's own bookkeeping state (gate frontmatter, uncommitted state branch, the drift-check rule) and several internal contradictions inside approved member bodies; 23 raw findings (18 after merging duplicates) survive verification as additions, 1 is refuted.

## (b) Verified additions

### Material — state and package integrity (fix before the Commander boots)

**1. z7's gate frontmatter is self-conflicting; the cold Commander selects the wrong attempt.**
`falsifiability-ladder.md:14` (`gates.current.attempt: gate-attempt:z7-ideation-3`) contradicts `falsifiability-ladder.md:18` (`current-attempt: gate-attempt:z7-ideation-4`), and attempts 1–3 each retain `application: {action: feedback, state: pending}` (`:34-38`, `:54-57`, `:73-76`) though their feedback was applied in cycles 2–5. The gate contract says `gates.current` selects the one attempt eligible for application (`gate-resolution-frontmatter-contract.md:237`) and that conflicting state is ineligible, fail closed (invariant 8, `:276-277`). Failure: a Commander following `gates.current` applies attempt 3's pending feedback and routes z7 — the member four others' wording depends on — back into ideation; a Commander that notices the conflict must fail closed and re-present, which `dispatch-sprint-execution.md:14` forbids. Disposition: fix-before-Commander — repair the pointer to attempt 4 and mark the stale applications; no captain decision needed, the approve at `:91-96` is unambiguous.

**2. 2ae's approval is not durably committed; HEAD carries a duplicate unapproved 2ae.**
`git status` in `docs/dev/.spacedock-state` shows ` D template-rigor-propagation.md` (deletion uncommitted) and `?? template-rigor-propagation/review/` (untracked). `git show HEAD:template-rigor-propagation.md` returns the pre-gate entity with the same id `2ae8r33r18g0w0g21559yc57` and no `gates:` block, while the committed `template-rigor-propagation/index.md` carries the approval whose room-ref is the untracked artifact (`index.md:26`). Failure: any fresh checkout of the state branch yields two entities with one id — the drivable-set query returns a duplicate 2ae, one copy unapproved — and the digest-bound briefing artifact does not exist to verify the approval. Disposition: fix-before-Commander — commit the deletion and the `review/` folder.

**3. The package's drift check, executed as written, voids two approvals and forbids the only remedy.**
`dispatch-sprint-execution.md:14` orders: on entity-content drift from `briefing.digest`, mark superseded and re-present — and in the same paragraph forbids re-asking any closed gate. But 85's expected-surface block was appended post-approval by the approval's own terms (`merge-guard-arm-not-a-stopping-point/index.md:42`, the block at `:68`), and z7 gained the post-approval captain-merged catalog addendum (`falsifiability-ladder.md:360-362`). Failure: a faithful Commander finds both drifted, must supersede, and cannot re-present; a Commander that skips the check abandons the only stale-approval guard. The "approval-directed edits are not drift" waiver exists only inside 85's note, never as a package rule. Disposition: fix-before-Commander — add the waiver rule to the dispatch and state which bytes the digest covers.

### Material — contradictions inside approved bodies (small folds)

**4. 2ae's Piece 4 is a paraphrase while its own AC-3 demands word-for-word carriage.**
Piece 4 drops "The decline is your licensed disposition, not a dodge:" and the three-part parenthetical ending in the promote-to-material condition (`template-rigor-propagation/index.md:125` vs `ensign-finding-triage-disposition.md:138`) and compresses the record/narrowing lines (`:127` vs 02av `:141,143`), while `:74` claims "carried VERBATIM … re-drafts nothing", AC-3 requires word-for-word match (`:196`), and the hard self-check trips on a re-drafted sibling wording (`:213`). Failure: implementation applies the approved text and fails its own AC-3 at validation — a guaranteed bounce on the member scheduled last, directly before the pre-cut audit — or a validator waves the divergence through, re-creating the disease the entity cures. Disposition: fix-before-Commander — make Piece 4 verbatim to 02av:137-143.

**5. 2ae plants the triage rule in the template's validation stage-def; 02av's dev realization is in implementation — commissioned workflows' in-stage rounds stay unprotected.**
2ae adds the consumption bullet to the template's validation stage-def (`template-rigor-propagation/index.md:121`; the SKILL.md pattern sentence likewise names only review-flavored stages, `:131`), but 02av deliberately lands the dev bullet in the implementation stage-def after `README.md:123` (`ensign-finding-triage-disposition.md:153`) because in-stage reviewer rounds — the e6j/task-91/incident-13 shape — happen during implementation. AC-3 checks wording and file set, not placement (`:196`). Failure: a workflow commissioned from the updated template carries triage only in validation packets; its implementation-stage roborev round receives no rule and the dutiful-fix class survives in every new workflow while 2ae's ACs pass. Disposition: fix-before-Commander — mirror 02av's placement (implementation stage-def, or both).

**6. 2ae's AC-2 proves refit propagation by validator-performed regeneration, not a live refit drive.**
The mechanism is refit-skill prose (Piece 8, `template-rigor-propagation/index.md:161-164`) and the named risk is a refit agent re-rendering only stage values, yet AC-2's verification is "regenerate the commissioned fixture README from the updated template and diff" at "Cheap, seconds, diff + line-count" (`:194`, `:202`) — explicitly contrasted with AC-1's live commission drives (`:201`). The sprint constraint requires every behavioral claim proven by a live drive (`index.md:45`), and the DoD wording is "a refit dry-run" (`index.md:40`). Failure: validation reproduces the ideation spike by hand, AC-2 greens, and a real refit agent later still emits only the version bump — the certified-fixed gap persists. Disposition: fix-before-Commander — reword AC-2's verified-by to a live refit-skill dry-run over the fixture. (This narrows the codex report's "template/refit mechanism held" verdict: the mechanism holds, its AC-2 evidence path does not.)

**7. The retired code-gate-only satisfier survives in `docs/dev/README.md:74` — no member owns removing it.**
The sentence z7 partially renames continues: "satisfy 'the contract says X' only with 'the binary or a test enforces X, and here is the run'" (`docs/dev/README.md:74`). z7's in-scope README change is the one-phrase rename only (`falsifiability-ladder.md:231`), and az's README edits touch the bullets and AC template, not line 74 (`anti-tautology-enforcement-and-template-gap/index.md:99-100`). z7's replacement clause accepts any of the first three checks including a one-off falsifiable exercise (`falsifiability-ladder.md:153`). Failure: after the sprint lands, the dev workflow's own proof-policy header still mandates the binary-or-test satisfier — the exact pressure behind incident 6's presence test — while the boot contract says the opposite. Disposition: fix-before-Commander — extend z7's README:74 edit to rewrite the whole sentence.

**8. bw's AC-1 offers "a small script or testlint case", contradicting its own 0-Go surface and the consent ruling.**
`feedback-cycle-record-command/index.md:172` ("formalized as a small script or testlint case") vs `:70` ("0 Go source files … any Go/product code … trips a reconfirm") and `:197` ("No Go unit tests, no product code in this cut"); `internal/testlint` does not exist; `index.md:48` gates new committed checks on explicit captain approval. Failure: an implementer taking the testlint arm at face value stands up a new committed Go check without approval, or trips bw's own reconfirm — either way a burned cycle. Disposition: fix-before-Commander — strike "or testlint case"; the check is a one-off script whose output lands in the validation report. Rides the codex finding-1 bw fold.

**9. The tolerance discipline the sprint rules on is not carried: three members have no tolerance and the "contract default" is never defined.**
The captain ruling requires "a written expected surface + tolerance" per gated ideation (`index.md:48`). 85 declares 1 file / ~2 lines with no tolerance (`merge-guard-arm-not-a-stopping-point/index.md:68`); ht declares 6 files / ~−135 LOC with no tolerance (`fix-tautological-output-grep-tests/index.md:72`); z7's byte AC requires only "measured and declared" with no bound (`falsifiability-ladder.md:239`). bw's shipped convention references "a declared tolerance (contract default, entity may override)" but no default value appears in any landing text, and the README ideation bullet asks for the surface only (`feedback-cycle-record-command/index.md:99,207,216,220`). Failure: when 85, ht, or z7 deviates, the deviation percentage has no threshold — the design-reset weigh, this sprint's headline guard, is undefined on the sprint's own members. Disposition: fix-before-Commander — one clause in bw's landing text naming the default (e.g. 2× unless the entity declares otherwise); rides the codex finding-1 fold.

### Material — reframe-merge wiring (fold into the codex finding-4 dispatch expansion)

**10. The feedback skill's frontmatter description still advertises "escalate on cycle 3" and is outside bw's declared surface.**
`skills/feedback-rejection-flow/SKILL.md:3` carries "track `### Feedback Cycles`, escalate on cycle 3" in the always-visible description; bw's surface is steps 2-3 plus two shared-core lines (`feedback-cycle-record-command/index.md:70,202-212`). Failure: the shipped high-stakes skill is self-contradictory (description prescribes the retired rule, body the new one) — a plausible material finding at the mandated detached adversarial audit, bouncing the co-edit gate. Disposition: fix-before-Commander — add the description line to bw's edit surface.

**11. The bw+02av merge trips required live lanes that assert the retired escalation shape, and no member may touch them.**
`docs/dev/README.md:78` makes the host live lanes REQUIRED green when the diff touches `skills/**/references/**` — bw edits `first-officer-shared-core.md`. The shipped `feedback-3-cycle-escalation` scenario runs under both runtimes (`claude_live_runner_test.go:162`, `codex_live_runner_test.go:71`) and asserts at least three bare `- Cycle N:` entries plus an escalation-at-third handoff (`shared_assertions_impl_test.go:44,117-131`); its fixture README mandates that exact format and stop-at-three (`shared_fixtures_test.go:186-190`). bw's new entry format (`- {timestamp} — …`) does not match the asserted regex. Honest caveat: the fixture self-instructs its format, so a workflow-obedient FO may keep the lane green — the red is plausible, not certain. But if it reds, the only fix is Go-test work outside every member's declared 0-Go surface, and no package document mentions these lanes. Disposition: fix-before-Commander — add a dispatch landing note: run the required lanes at this merge; treat an escalation-scenario red as a captain reconciliation decision, never a silent fixture edit.

**12. The binding gate-presentation directive points at the superseded /tmp wrapper; the working ritual lives only in optional reading.**
`index.md:47` directive 2 binds every driving FO to "single-file wrapper today at /tmp/subspace-present-float-skill"; the shaping debrief records that wrapper as superseded, floats paused after captain comprehension failures, and documents the proven replacement ritual (`_debriefs/2026-07-20-01-0260-shaping.md`, the float-procedure paragraph and finding 13). The dispatch lists the debrief only as "Session context if needed" (`dispatch-sprint-execution.md:10`), yet 2ae's binding validation-gate condition depends on presenting an artifact. Disposition: fix-before-Commander — update directive 2 (or make the debrief's presentation ritual a required boot read).

### Needs-decision

**13. The adopted re-anchor merge dropped its payload: the committed non-falsifiable live scenario has no owner.**
The second re-lock says "the AC-2 fixture absorbs the re-anchor scenario" (`index.md:6`). w0's defect — `internal/livescenario/ac2_reanchor.go` stays green when its own deliverable clause is deleted, a verified pre-tag major — was archived with "scope carried there" (`_archive/ac2-reanchor-scenario-falsifiable.md:23-25`) into 0qe, which was itself archived into z7's catalog carrying only 0qe's reviewer-side fixture (`_archive/ac2-design-proof-fixture.md`, final section; `falsifiability-ladder.md:360-362`). The Go-scenario repair appears in no sprint document; `ac2_reanchor.go` is still on disk. Failure: 0.26.0's goal line "the tautological-test estate is fixed and gated against recurrence" (`index.md:18`) ships with a known non-falsifiable committed check, and the archive stub's false "scope carried there" prevents rediscovery. Disposition: captain-decision — re-file for the next train, genuinely fold into z7's catalog, or record a decline; correct the archive record either way.

### Disproportionate (right, no sprint value breaks — recorded declines)

**14. ht's deletions drop the only binding between help output and the command/verb set.** The grouped-help literals (`internal/cli/cli_test.go`, the jargon-free test) and `TestHelpListsNewVerbs` (`internal/cli/verbs_test.go:94`) are the only tests asserting help lists the commands/verbs; retained siblings check form-identity and verb behavior only, and no AC-1 matrix mutation (`fix-tautological-output-grep-tests/index.md:90-97`) covers a dropped help row. The approved design explicitly adjudicated command-name rows as rewording-only, so decline; promote-to-material condition: an observed help-content regression.

**15. 02av's FO-delivery link is unobserved by any AC.** AC-1 hands the rule directly to the ensign (`ensign-finding-triage-disposition.md:162`); AC-3(b) conditions on a context already containing the block (`:168`). Accurate — but the body deliberately defers the guaranteed-delivery mechanism behind observed FO omission (`:71`); instrumenting it now is the over-building the sprint declines. Decline, with the entity's own promote condition standing.

**16. 02av's README bullet misattributes the four-class list to the validation stage-def.** The bullet (`:155`) and body (`:79`) cite the validation stage-def for Material/Deferred risk/Polish/Needs decision, but the README defines only the first two there (`docs/dev/README.md:148`, polish in passing at `:152`, no needs-decision); the four classes live at `.roborev.toml:46-50`. Harmless in practice — the bullet enumerates the classes inline. Decline; a two-word citation fix can ride the co-edit if convenient.

**17. bw ships the undefined compound "round state" into the boot-resident effect line.** `feedback-cycle-record-command/index.md:212`; the cycle-3 sweep's claim that it is existing vocabulary (`:264`) is false — `grep -rn "round state|round-state" skills/` returns nothing. It is plain words, so not a break of the no-minting ruling; decline, though the fold ("read the deviation and AC-drift") is free if the codex finding-1 bw fold is applied.

**18. z7's body still frames the closed +1055 budget as an open gate call.** `falsifiability-ladder.md:206,239,334-335,354` vs the closing resolution (`:92`) and the dispatch note (`dispatch-sprint-execution.md:24`). Doubly mitigated — the closure is in the same file's frontmatter and in the packet. Decline; annotate when the file is next touched. (Same class as codex 5.)

**19. 85's body argues in the banned vocabulary beyond the `group: ladder` field codex 5 flagged.** "falsifiability-ladder judgment rung" at `merge-guard-arm-not-a-stopping-point/index.md:85` and "judgment-rung per this sprint's falsifiability ladder" at `:94` — the coinage z7 swept and the captain banned. Decline as polish; sweep alongside codex 5's regroup.

**20. "Land bw and 02av together at a shared gate" names bookkeeping the gate notation does not define.** The schema is strictly per-entity (`gate-resolution-frontmatter-contract.md:91-108`); `dispatch-sprint-execution.md:23` and 02av `:128` both use the undefined term. A Commander can lawfully record one presentation as two per-entity attempts, so no blocker — decline, but one clarifying sentence is nearly free inside the codex finding-4 dispatch expansion.

## (c) Refuted candidates

- **az's AC-2 is the banned self-prose criterion** — refuted: AC-2's deliverable IS the decision record, so reading it for required components (single decision, named losing side, three grounds — `anti-tautology-enforcement-and-template-gap/index.md:120-121`) is direct artifact inspection that fails if any component is absent; the z7 clause targets prose review substituting for behavior observation, and the feared cross-check refusal has no path because the gate is already closed-approved (the falsifiable fragment is separately owned by AC-4's git check).

## (d) Duplicate-coverage map (fable findings in journal extraction order F1–F51)

| Codex finding | Fable duplicates | Note |
|---|---|---|
| 1 (reframe DoD vs bw) | F1, F8, F18, F27, F30, F32, F33, F45 | F8/F32/F33 add corroboration that the delegated live half (gq) is archived done since 2026-06-05; F30 is the index:6 "command +" staleness codex's closure condition 1 already catches |
| 2 (testlint DoD vs az) | F2, F9, F17, F19, F26, F34, F46 | F19 (no check asserts Edit D's absence) is mooted by codex 2's resolve-Edit-D-before-drive fold |
| 3 (index stale/unowned scope) | F16, F29, F40, F51 | F16 adds the `index.md:64` "contract-cleanups pair" wording — fold alongside |
| 4 (dispatch omits composition/proof wiring) | F10, F20, F31, F35, F36, F49 | F31/F36 sharpen the seam: 2ae's approved Pieces 1-2 after-texts are the pre-az wording of README:224/:76, so the fold must re-anchor 2ae's diffs, not merely "validate against landed text"; F49 adds that the table understates bw's surface as skill prose only |
| 5 (stale metadata) | F14, F42 | body-vocabulary extension recorded as addition 19 |
| 6, 7 | — | no fable duplicates |

Additions above: F43, F44, F47 (material 1–3); F22/F38, F12, F4 (material 4–6); F11/F39, F13/F21, F6+F25 (material 7–9); F15, F37, F48 (material 10–12); F28 (needs-decision 13); F3, F7, F41, F24, F23, F14-extension, F50 (disproportionate 14–20); F5 refuted. All 51 accounted for.
