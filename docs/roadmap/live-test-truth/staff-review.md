# Live-test-truth preflight staff review

**Reviewer:** Fable, supplied through the Captain on 2026-08-03.

**Verdict:** READY after the recorded folds.

## Evidence checked

- `3d` reproduced the AC2 false green at commit `2997da9a2`. The removed-policy run passed in 96.70 seconds.
- `3d` proved the durable two-way oracle. Its wrong branch failed in 148.38 seconds.
- `3d` exercised Codex activity resets through the real front door at commit `17d3f5a71`.
- `ys` reuses the banked spikes. It records why another ideation spike is not necessary.
- `15e` pins its maps to a source commit and CI run `30378538074`. It includes measured Pi cost evidence.

## First review round

| Finding | Class | Disposition | Evidence |
| --- | --- | --- | --- |
| The focus section named absorbed tasks. | Material | Fixed | The index now names `ys`, `3d`, and `15e`. |
| Absorbed records remained active. | Material | Fixed | State commit `e7426315b` archives all 12 records and preserves their reports. |
| Deferred member `rm` lost sprint membership. | Material | Fixed | `rm` is in the full query and absent from the drivable query. |
| `ys` contains the largest blast radius. | Material review focus | Accepted with controls | Its plan has seven reviewable landing steps. The final selector and guard land last. |
| The sprint selected Sol while the workflow declared `opus`. | Polish | Clarified | The index records the Codex `model: null` result and explicit Sol override. |

## Second review round

| Finding | Class | Disposition | Evidence |
| --- | --- | --- | --- |
| `ys` and `3d` both owned the AC2 repair. | Material | Fixed | `3d` owns the repair. State commit `0877e2888` leaves only wrapper removal in `ys`. |
| Shared surfaces had no inter-member order. | Material | Fixed | The order is `3d`, then `15e`, then `ys`. Index commit `7224ef482` records it. State commits `03e9b124d`, `abffb5ad2`, and `0877e2888` bind each seam. |
| Archive paths must preserve spike evidence. | Material | Fixed | Fresh gate references use the 12 `_archive` reports. |
| The quiet-budget values appeared inconsistent. | Material | Fixed | `3d` names the 60-second product value and the 30-second probe value separately. |

## Remaining release dependency

`rm` remains deferred and release-blocking. If its upstream product repairs miss `v0.27.0`, the Captain must move the train or change the release bar.

The planned SSOT link, reconciliation SHA, and path guard are not current evidence. `ys` must land them together before sprint completion.

