# 0221 layered-FO — sprint-wide preflight readiness review (0.22.1)

> This is the **2026-06-19 full-sprint readiness review**, superseding the 2026-06-16 no-2y-tranche per-member review (git preserves it). It refutes the sprint AS A WHOLE — does this carved set, in the planned order, cohere and deliver the 7-bullet Definition of Done, and can a fresh Commander cold-boot and drive it? It does NOT re-litigate any member's internal acceptance criteria (the ideation gate owns that); members appear only for how they compose.

## Verdict

**Gaps to close — not yet cold-boot drivable.** The carved set is strong and most of the riskiest design work is done (the w4 spike PASSED; 6re is built and merge-ready; rgq/mz/72 are gate-approved at ideation with their spikes run; 72's fail-safe flip and launcher surface — the prior review's two material gaps — are folded in). But three sprint-level holes block a clean drive:

1. **DoD bullet 1 (`next-action` dispatch driver) is UNOWNED in this sprint.** It moved to `0222-dispatch-driver` and is not carved as a 0221 member. The DoD as written cannot be satisfied by the carved set. Amend the DoD (the cleaner call — the w4 spike found `«next»` HOLDs, Haiku-operable as prose) or pull a member in.
2. **The sprint gate proof (`kt` haiku-drive-validation) is STALE on its load-bearing residency and verification premise.** Its body still assumes the retired 2.1.177 pin and an interactive-tmux-only team-tool world; the 0.22.0 merged floor (which this sprint rides on) changed the launch shape underneath it. The single artifact that closes the sprint is written against a world that no longer exists.
3. **A flagged 0.22.1 candidate — the `reconcile.go` headless-meta roster-discovery fix — is not carved, and it bears directly on `kt`'s launch shape.** Either carve it or record an explicit decision that `kt` runs without it.

Everything else (sequencing, the shared-core region contention, the verdict-route boundary) is sound or has an owned plan, with the caveats below. Close the three holes and the sprint is drivable.

## DoD coverage

The index's Definition of Done has seven bullets. Mapping each to the member that delivers it against the carved roster (`czw`, `3e`, `rgq`, `mz`, `72`, `6re` drivable; `kt`, `y2` deferred):

| DoD bullet | Owning member | Status |
| --- | --- | --- |
| 1. `next-action` dispatch driver (`spacedock dispatch next-action`) | **NONE in 0221** — descoped to `0222-dispatch-driver` (uncarved seed) | **UNOWNED — material gap** |
| 2. State-repo verbs (`state ready`/`sweep`/`commit`, verb-enforced rebase HALT) | `rgq` state-verbs (ideation, gate-approved, spike PASSED) | owned |
| 3. Finalize / merge-guard verb (`merge guard <slug>`) | `mz` merge-finalize (ideation, gate-approved, spike PASSED) | owned |
| 4. Model-aware delegation mechanism (`«fo.tier»`, L3 routing table, Haiku routes all gate verdicts) | `72` fo-tier-delegation (ideation, gate-approved) | owned (gate-verdicts row wired; 5 rows documented-but-deferred — honest) |
| 5. Prose-function restructure (cores as `«fn»` invocations) | `czw` prose-function-restructure (ideation) | owned |
| 6. Validating live Haiku drive (the sprint gate proof) | `kt` haiku-drive-validation (backlog, defer) | owned but **STALE premise** |
| 7. `v0.22.1` cut after a clean pre-cut antipattern audit | the cut step (membership-independent) | n/a |

**The material gap is bullet 1.** The DoD opens with "A `next-action` dispatch driver" as a hard ship condition, with a detailed return-schema contract (`{action, slug?, stage?, team_action?, reason}`, fully-qualified `team_action`). No 0221 entity owns it — the only `next-action` references in the carved bodies are `czw`'s `«dispatch.next-action»` guillemet prose-function (whose migration target is the unshipped `dispatch next-action` verb) and `rgq`'s out-of-scope note. `0222-dispatch-driver/index.md` carries the `next-action-driver` seed (uncarved) and notes the descope rationale explicitly: 0.22.1 is the judgment/safety track, `next-action` is mechanical-dispatch mechanization, and **the w4 spike found `«next»` HOLDs (stays prose, Haiku-operable) — so the validation does NOT depend on `next-action` being binarized.** That rationale is sound. But the DoD text was not amended to match the descope, so it now names a deliverable the carved set cannot produce. **Action: amend DoD bullet 1** to "the `«dispatch.next-action»` prose-function ships (guillemets, Haiku-operable per the w4 `«next»`-HOLD finding); the binary `dispatch next-action` is `0222`'s" — which is exactly what `czw` delivers. (Pulling the heavy, riskier `next-action` binary INTO this sprint would re-import the mode-2 drift-class risk the spike left NOT-EXERCISABLE and contradict the clean 0221/0222 split; do not.)

Bullets 2/3/4/5 are owned and gate-approved/built. Bullet 4's honest scope (only the gate-verdicts row wired live; the other five routing rows shipped as documented-but-deferred prose) matches the DoD's specific claim ("when the FO is Haiku and the workflow has `gate: true` stages, ALL gate verdicts route to level-3") — the DoD asks for the gate-verdicts guarantee, which is the wired row, so bullet 4 is satisfiable as scoped. Bullet 6 is owned by `kt` but rests on a stale premise (next section). Bullet 7 is the cut step.

## Sequencing & dependencies

The planned order is sound, with one hard precedence that the roster does not yet enforce and one stale blocker.

**The verb-core (`rgq`, `mz`) is independent and parallelizable.** Both ship new `internal/cli`/`internal/status` binary surface, both ran their riskiest-mechanism spike in ideation (the rebase-HALT 2-writer harness for `rgq`; the set→invoke→clear→backstop-refusal hand-drive for `mz`), and neither edits the boot-resident contract regions (the notation flip is explicitly `czw`'s). They can be built in either order or concurrently. Good.

**The boot-resident region order is `6re → 72 → czw`, and `czw` designed itself around it.** Three members edit the same three shared-core regions — `## Startup`, `## Completion and Gates`, `## FO Write Scope`:
- `6re` rewrites Startup step 4 (taxonomy via `status --read`). **Verified: `6re`'s branch (`spacedock-ensign/gate-extract-verbs`, tip `404385bb`) is rebased onto current `main` (merge-base = `main` tip `2c9609f8`; 4 commits ahead, ZERO behind)** — the brief's "behind main by 10" is stale; the branch carries the new step-4 wording (`first-officer-shared-core.md:16` on the branch) and is merge-ready pending its validation gate. `main`'s step 4 still carries the OLD "the boot JSON does not carry the stage taxonomy" wording, so the edit is in-flight, not landed.
- `72` inserts a Startup step-1.5 (`«fo.tier»()` arming), a verdict-route bullet in `## Completion and Gates`, and a `### Gate Verdicts` write-scope bullet — as RAW prose.
- `czw` re-expresses all of it into prose-function form and **explicitly plans to land LAST**, folding `72`'s step-1.5 in as a sixth declared prose-function (`«fo.tier»()`).

`czw`'s body resolves this collision the right way (restructure-last localizes the notation work to one member; landing it first would force `6re`/`72` to re-author in a notation they did not design, widening blast radius onto three members). **But the order is a design recommendation inside `czw`, not a roster-enforced precedence.** `czw` is `sprint-readiness: ready` and `6re` is still at `implementation` (unmerged) — nothing stops a Commander from dispatching `czw` before `6re` merges and `72` lands. The hard precedence the Commander must honor: **`6re` merges → `72` lands → `czw` re-expresses.** This is the one ordering that, if violated, forces rework AND risks `72`'s safety property being silently dropped in the re-expression (the exact risk `czw` cites).

**`kt` is correctly sequenced LAST** (`defer`, depends on every other member + 2y transitively). Its dependency list is right in shape — all four verb-core members + `72` + `6re` shipped, plus the team backend green — but **stale in substance** (see Composition, and the gate-proof note below).

**No order forces avoidable rework** beyond the `6re→72→czw` precedence. `3e` (wrong-root-guard) is an isolated test-harness parser fix with zero product/contract coupling — it can land anytime and is sequencing-neutral.

## Composition & wiring

The members fit together; the seams are real and mostly well-mapped. Two compositions need a Commander's attention.

**The verdict-route boundary across `6re` → `72` → `czw` is coherent and consistent.** `6re` ships deterministic EXTRACTION (`status --read --checklist`/`--ac-scan`) and explicitly never adjudicates (no `satisfied` field, the verdict routes to L3). `72` ROUTES the verdict: a level-2-only FO sends the extracted structure to the standing `level-3-judge`, fills present-gate's `Recommend` line from the L3-authored verdict, and records a durable `### Gate Verdicts` `decided-by: level-3-judge` line. `czw` keeps `«gate.assemble-verdict»` as a prose-function whose body names (a) `6re`'s extract sub-calls and (b) `72`'s L3 route — and recommends NOT binarizing the verdict. All three agree the verdict is irreducible judgment and the L3 route is the safety mechanism. Verified the anchors: present-gate's `Recommend {approve | reject}` line (`skills/present-gate/SKILL.md:18`) and assembly rule 5 (`:40`) are real; `6re`'s `status --read` modes are built on its branch. The boundary holds across all three.

**`72`'s standing-L3 residency vs. the spike's bare-Agent provenance is reconciled — but its proof now lives entirely in `kt`, which is stale.** The prior review's central composition finding (the spike proves the residency-AGNOSTIC routing BOUNDARY via a per-judgment bare `Agent(model=opus)` and a captured-stream oracle; `72` ships a STANDING `level-3-judge` mod + `SendMessage` graded on the durable `### Gate Verdicts` line) is now correctly handled: `72`'s rework explicitly disclaims borrowing the spike's drive and assigns its AC-1 standing-residency proof to `kt`. Verified `72`'s AC-3 anchor is real (`internal/dispatch/standing.go:15` `spawnModelEnum` includes `opus`; `spawn-standing-all` discovers a `model: opus` mod the same way as the live `comm-officer.md`). So the boundary transfers and the standing-residency proof is owned — **but it is owned by `kt`, whose residency premise is stale (below). If `kt`'s premise is wrong, `72`'s AC-1 has no valid proof.** This is why `kt`'s staleness is a sprint-level blocker, not just a per-member nit.

**`72`'s fail-safe flip and `SPACEDOCK_FO_MODEL` launcher surface (the prior review's two material gaps) are folded in.** Verified against the live tree: `frontdoor.go` still carries `--model` as opaque passthrough and exports only `SPACEDOCK_BIN` via `launchEnv` + an explicit `--env-pass SPACEDOCK_BIN` safehouse flag (no `SPACEDOCK_FO_MODEL` anywhere) — so the launcher work `72` now owns (parse `--model`, normalize, export `SPACEDOCK_FO_MODEL`, add `--env-pass`) is genuinely new and correctly claimed, not mis-described as trivial. The tier default is now fail-SAFE (unset/unresolvable → level-2-only → route). Both folds are sound; flagged here only because the new `SPACEDOCK_FO_MODEL` contract surface is a captain-ratify item the gate must bless.

**The spike's PROVISIONAL must-build list maps cleanly to the verbs being built.** The w4 spike's must-build set (`«state.commit»`, `«dispatch»`, `«gate»`, `«merge»`; `«boot»`/`«next»` HOLD) lines up with the carved verbs: `«state.commit»` → `rgq`; `«merge»` → `mz`; `«gate»` extraction → `6re`; `«next»` HOLDs → stays `czw` prose (and is why bullet-1's descope is safe). The one spike must-build with NO carved owner is `«dispatch»` (Haiku dropped `subagent_type` from the emitted Agent envelope 3/3 — the spike says the verb must EMIT the spawn, not hand the FO an envelope). That is the `dispatch build --request-file` / `next-action` producer work — **also descoped to `0222`** (`vp` dispatch-build-request-file). So the spike's `«dispatch»` must-build is unowned in 0221, same root cause as bullet 1. Consistent with the descope; flagged so the Commander knows the spike's full must-build list is split across 0221+0222, and the `kt` drive will exercise the still-prose `«dispatch»` on the real shape.

## Scope (missing / over)

**MISSING — the `reconcile.go` headless-meta roster-discovery fix (flagged 0.22.1 candidate, not carved).** The 2026-06-19 debrief flagged: on a headless `-p` merged host, the member record is `subagents/agent-*.meta.json`, NOT the `teams/session-*/config.json` interactive registry, so the FO's `reconcile` leadSessionId auto-discovery **degrades to git-only**. The debrief explicitly notes this "bears on `haiku-drive-validation`'s launch shape" and names a `reconcile.go` fix (read the meta.json path for headless roster discovery) as a 0.22.1 candidate. **Verified: no carved entity owns it** (no 0221/0222 entity references `meta.json` / `leadSessionId` / headless roster discovery; the only headless-titled entity, `team-mode-verdict-omission`, is a different concern folded into `mz`'s AC-4). If `kt` launches the Haiku FO headless `-p` on the merged floor (the floor the sprint rides on), reconcile roster-discovery degrades — exactly the loop step the gate proof must exercise. **Either carve this fix as a member (it bears on the gate proof) or record an explicit decision that `kt` drives without it** (e.g. `kt` runs tmux-interactive where the registry exists, or accepts git-only reconcile for the drive). Today it is neither carved nor decided.

**MISSING (stale, not absent) — `kt`'s residency/verification premise.** `kt`'s body states "Must run under the REAL (tmux) backend (m4), NOT the `-p` spike proxy — team tools are interactive-only post-2.1.178; team-tool availability is verified in CI on the pinned 2.1.177 (61, LANDED #395)." **Three facts in that sentence are now false on the live tree:** (a) the #395 keystone pin was RETIRED by 0.22.0's CI-unpin (`index.md:3`; `m4-readiness.md:55`); CI now floats on installer-`latest` (the merged floor). (b) `m4`'s shipped MERGED lane proves headless `claude -p` is team-enabled flag-free on the merged floor — named-background `Agent` + `SendMessage`, no tmux, no `TeamCreate` (`m4-readiness.md:13,38-43`; debrief Observations) — so "team tools are interactive-only" is the LEGACY-host truth, not the merged-floor truth the sprint rides on. (c) `61`/#395 is retired, not the live verification basis. `kt` is the artifact that CLOSES the sprint (and `72`'s AC-1); it is written against a world 0.22.0 replaced. **`kt` must be re-grounded** to the merged floor: decide the residency (headless-`-p` named-Agent+SendMessage, which `m4` proved works, vs. tmux-interactive) and the team-tool availability basis (verified on the unpinned/merged floor, not the retired pin) BEFORE it is carved out of `defer`. This is bundled with the reconcile.go decision above — both are "what is `kt`'s real launch shape on the merged floor."

**Over-scope: none material.** The carved set is tight. `y2` (fo-contract-token-cut) is correctly `backlog`/`defer` (a micro-optimization, not a DoD dependency). `3e` is small and isolated. `6re`'s consolidation into `status --read` (vs. a new `gate` command) and its reconciliation of task `82k` are well-bounded. No member over-reaches the Goal.

## Commander readiness

A fresh Commander can cold-boot the *roster* — the entities carry their spikes, ACs, doc-diffs, and (for `czw`/`72`/`6re`) the shared-region landing order. What a Commander CANNOT do today without tripping a hole:

- **Drive to the DoD as written** — bullet 1 names a verb no member ships. The Commander would either chase a phantom deliverable or have to make the descope call themselves mid-drive. Amend the DoD first.
- **Run the gate proof** — `kt` is `defer`/`backlog` with a stale launch premise and an undecided reconcile.go dependency. The single artifact that proves the sprint cannot be driven until its real merged-floor shape is settled. This is the highest-leverage close: every other member feeds `kt`, and `kt` feeds bullet 6 AND `72`'s AC-1.
- **Trust the roster's sequencing to self-enforce** — the `6re→72→czw` boot-region precedence lives in `czw`'s prose, not in `sprint-readiness`/`group` gating. A Commander reading only `--where 'sprint-readiness != defer'` sees `czw`, `72`, `6re` all `ready` with no ordering signal. The precedence must be made explicit (a roster note, a `depends-on`-style marker, or the Commander's run-plan) so the restructure does not land before the edits it re-expresses.

The verb-core (`rgq`/`mz`) and `3e` are genuinely drivable now. The boot-region trio is drivable IN ORDER. The gate proof is not, until re-grounded.

## Close before the Commander drives (ordered, owner-tagged)

1. **Amend DoD bullet 1 to match the `next-action` descope.** Reword to "the `«dispatch.next-action»` prose-function ships (Haiku-operable per the w4 `«next»`-HOLD finding); the binary `dispatch next-action` is `0222`'s," so the carved set can satisfy the DoD. Do NOT pull the binary in. *Owner: captain (DoD amend).*
2. **Re-ground `kt` (the gate proof) to the 0.22.0 merged floor.** Replace the stale "interactive-tmux-only / verified on pinned 2.1.177 (#395)" premise: decide the residency (headless-`-p` named-Agent+SendMessage, which `m4` proved works on the merged floor, vs. tmux-interactive) and base team-tool availability on the unpinned/merged floor, not the retired pin. This also closes `72`'s AC-1 standing-residency proof, so it must be settled before `72` can claim its safety property is provable. *Owner: ideation-rework (`kt`) + captain-ratify-at-gate (the residency decision).*
3. **Decide the `reconcile.go` headless-meta roster-discovery fix.** Either carve it as a 0221 member (it bears on `kt`'s launch shape — on a headless `-p` merged host, reconcile degrades to git-only) or record an explicit decision that `kt` drives without it. Bundle with item 2 — both are "`kt`'s real launch shape." *Owner: captain (carve-or-defer decision) + ideation if carved.*
4. **Make the `6re → 72 → czw` boot-region precedence roster-enforced, not prose-only.** A Commander reading the drivable query sees all three `ready` with no ordering signal; the precedence (6re merges → 72 lands → czw re-expresses) must be explicit so the restructure does not land before the edits it re-expresses and silently drop `72`'s safety property. *Owner: captain (sequencing) — record in the roster / run-plan.*
5. **Land `6re` (merge its validation gate).** It is built and rebased onto current `main` (4 ahead, 0 behind), and it is the first domino of the boot-region order — `72` and `czw` both build on its merged step-4 edit. Until it merges, `main`'s step 4 carries the old wording and `72`/`czw` have a moving target. *Owner: Commander (drive `6re` validation→merge first).*
6. **Ratify `72`'s new contract surfaces at its gate.** The `SPACEDOCK_FO_MODEL` launcher export + `--env-pass` plumbing (verified genuinely new — `frontdoor.go` exports only `SPACEDOCK_BIN` today) and the `### Gate Verdicts` durable-record write scope are new contract surfaces the gate must bless; the fail-safe tier default and the narrowed "only the gate-verdicts row is wired" claim are sound as folded. *Owner: captain-ratify-at-gate (`72`).*
7. **Confirm the spike's `«dispatch»` must-build split is intentional.** The w4 spike's `«dispatch»` must-build (Haiku dropped `subagent_type` 3/3) is unowned in 0221 — it is `0222`'s `vp`/`next-action` producer work, same root cause as bullet 1. The `kt` drive will exercise the still-prose `«dispatch»` on the real shape; confirm that residual risk is read at the gate, not assumed away. *Owner: captain-ratify-at-gate.*

The verb-core spikes (`rgq` rebase-HALT 2-writer harness; `mz` set→invoke→clear backstop hand-drive) both ran and PASSED in ideation, so the sprint does NOT owe the injected-conflict harness as a separate gate condition — the riskiest state/merge mechanisms are exercised. The remaining holes are sprint-composition holes (an unowned DoD bullet, a stale gate proof, an undecided launch-shape dependency, an un-enforced precedence), not unproven mechanisms.
