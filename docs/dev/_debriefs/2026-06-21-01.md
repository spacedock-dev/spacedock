---
session-date: "2026-06-21"
sequence: 1
first-commit: 7338d556
last-commit: f4994984
duration: ~3h
---

# Session Debrief — 2026-06-21 #1

FO session focused on a single release-blocking thread: fix the broken codex/claude **edge** plugin install, then cut `v0.23.0-pre.3` to bake it. Ran concurrently with a Commander driving the rest of the 0230 finalization tail (#432, `fo-binary-launcher-invariant`) in the shared checkout.

## Shipped
- **fea2** `codex-edge-channel-marketplace-source` — [#431](https://github.com/spacedock-dev/spacedock/pull/431). Fixed the broken codex/claude **edge** plugin install: a `channelMarketplaceSource(devBranch)` routes the edge channel to `spacedock-dev/marketplace@edge` (a new branch whose root manifest is *named* `spacedock-edge`, single `spacedock` entry → product repo ref `next`), so `codex plugin add spacedock@spacedock-edge` resolves; `SPACEDOCK_MARKETPLACE_SOURCE` override preserved verbatim. Host-neutral (fixes claude-edge too). Filed + shipped same session.

## Released
- **v0.23.0-pre.3** cut via the waiver path (set → cut → delete `SPACEDOCK_E2E_GATE_WAIVER`). Edge cask `spacedock@next` published at `0.23.0-pre.3`; stable cask correctly skipped (prerelease). Verified end-to-end against the *published* edge binary: `install --host codex` + `doctor` self-consistent (`0.23.0-pre`), no stale-version drift.

## Filed (backlog)
- **fea2** `codex-edge-channel-marketplace-source` — shipped same session.

## Peer context (Commander, concurrent — not this FO's work)
- **#430** `headless-no-conn-gate-discipline` and **#432** `releasing-doc-pre-stamp-drift` (`7y`) merged in this window. Commander now driving `fo-binary-launcher-invariant` (`v9`, cycle-2 validation PASSED).

## Decisions
- Drove codex-edge as the explicit TOP PRIORITY with **verify-the-mechanism-first** discipline — created and proved the `edge` branch + a real codex install end-to-end *before* touching the binary.
- Merged #431 on **deterministic-green** (build/install/offline + the validator's real codex+claude installs); live lanes **waived** per captain rather than waiting on deployment approval.
- pre.3 e2e-gate handled via the **waiver** escape hatch (set → cut → delete), not a green live-e2e for the commit.
- **Stood off `7y`/#432** as Commander-owned per captain; provided an independent assessment of #432 instead of driving it.

## Issues — Workflow
- pre.2 binary + a polluted `~/.codex`: `spacedock doctor --host codex` resolved a stale `0.19.9` from nested `.worktrees/` plugin.json files inside `cache/spacedock/spacedock/0.22.0/` (a past dirty-checkout/local-marketplace install). The **fixed** binary resolves the right plugin — confirmed live in an isolated `CODEX_HOME`. Not a release blocker; the pre.2 binary simply predates #424/#431.
- `git tag -F <file>` strips `## ` headers (git treats `#`-leading lines as comments) — the pre.3 tag annotation lost its section headers. Caught it; fixed the published release body with `gh release edit --notes-file`. Use `git tag --cleanup=verbatim` next time.

## Issues — Spacedock
- **codex cache-manifest resolver descends into nested install dirs** — `codexCacheManifest`/version resolution can land on a `.worktrees/**/.codex-plugin/plugin.json` when an install dir contains a dirty checkout, mis-reporting the version. Bites dirty-checkout / local-marketplace installs. Hardening candidate — not filed (pending captain approval).
- **Edge `@next` version-bump path drift** — `next-publish.yml` bumps the *product* repo's `.claude-plugin/marketplace.json`, but post-#431 codex reads the *standalone* `spacedock-dev/marketplace@edge` branch's manifest. The `@next` re-pull cadence version marker may no longer reach what codex reads. Reconcile candidate — not filed (pending captain approval).

## Observations
- Verified + documented the **local codex marketplace dogfood recipe** (workflow-proven, isolated `CODEX_HOME`): set `SPACEDOCK_MARKETPLACE_SOURCE` to a local marketplace dir, then `spacedock install --host codex`. The channel lives in the **marketplace name**, so a plain `go build` (edge/`devBranch=next`) binary needs the local marketplace named `spacedock-edge`; the entry name stays `spacedock` (must equal the plugin's own `plugin.json` name). Override is passed verbatim — no `@edge` appended. Documented at `docs/site/get-started/install.md:63-69` and the CI harness `docs/runtime-live-ci.md:94-101` / `writeCodexLocalMarketplace`.
- #432's `manifest-tag-gate` carves out pre-releases (`if: "!contains(github.ref, '-')"`, test-locked), so the pre.3 flow we used stays valid; only **stable** cuts must stamp-then-tag.

## What's Next
- **Stable v0.23.0 cut** — now unblocked (codex-edge done ✓, `7y`/#432 merged ✓). Must follow #432's **stamp-then-tag** ritual: stamp `.claude-plugin/plugin.json` + `.codex-plugin/plugin.json` to `0.23.0`, commit, and tag *that* commit — the `manifest-tag-gate` (goreleaser `needs:` it) enforces the match on stable tags, so the pre.3 "tag a `0.22.0`-manifest commit" shortcut will NOT work for stable. Release notes must cover pre.1/pre.2 content **plus** the token clawback.
- **#432 review** — an independent multi-lens review workflow was launched (now post-hoc, since #432 already merged); will surface any latent gate/test findings.
- **Two follow-ups to file** if approved: codex cache-manifest resolver hardening; edge `@next` version-bump path reconcile.
- **proposal-B refresh** (`docs/dev/_proposals/fo-contract-token-cleanup.md`) — carried in the handoff, untouched this session.
- Commander continuing `fo-binary-launcher-invariant` (`v9`).
