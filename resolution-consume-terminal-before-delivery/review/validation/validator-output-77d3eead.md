Validation stage complete. Summary of what was verified (all outputs observed by me, not report-trust):

1. **AC reruns, observed exit 0**: AC-1 `TestTerminalDeliveryFailureReworkRoundTrip`, AC-2 `TestTerminalSpendOnlyInDeliveryEnvelope` (5 legs: 3 delivery classes + terminal-`--set` refusal + idempotent re-consume), AC-3 `TestTerminalRetryLeavesAuthorityPending`, refusal matrix (incl. no-pending-application leg), envelope-atomics pair (`TestDeliveryEnvelopeIsOneAtomicCandidate`, `TestDeliveryEnvelopeFailureLeavesOriginalBytesIntact`), gates supporting tests. Full suite exit 1 with **exactly** the 2 pre-existing env failures, re-proven red identically on main @ 3522ee457.
2. **Movable baseline**: `/tmp/1w62-base` @ 3522ee457 — same shell-driven fixture: consume exit 0 → `status: done` + `state: consumed` with zero delivery proof; `merge guard --rework` exit 1. Hole still reproduces.
3. **Detached adversarial audit** on throwaway `/tmp/1w62-cand` @ 51af9bd16 — 5 claim-breaking edits, 5 catches, zero holes: old always-consume branch → AC-1/AC-2/AC-3 red; split envelope (bypassing the instrumented seam) → atomics red (AC-1 CLI green by design, as the AC text predicts); silent feedback-to fallback → exactly the no-feedback-to leg red; disabled sole-consumer `--set` refusal → exactly its AC-2 leg red; removed nil-approval `--rework` refusal → refusal matrix hard-red.
4. **Durable live drives** against the compiled candidate: consume→deliver and consume→rework→re-enter→deliver land correct on-disk end states (archived entity: done/passed/completed/consumed; attempt-1 superseded frozen, attempt-2 consumed; `gates.current` unchanged). `--rework` with no pending application refuses exit 1, bytes untouched.
5. **Surface re-measure from git**: +1382/−79 vs ≈+720 estimate (+87%, over the ±30% cap). Semantic core certified at +201 vs +205 declared; overrun fully attributed to the envelope seam relocation (+267 unbudgeted: envelope.go/+217, mutate.go/+40, model.go/+10) and AC fixtures (+834 vs +300). No out-of-design machinery: zero added lines with delivering/reversed/reversal/`--to`/`--by`/journal/compat/scanMods additions; gates io.go/operation.go/prepare.go untouched.

**Recommendation: PASSED** — no material/deferred/polish findings; the +87% overrun is gate-visible and decomposed (tolerance decision belongs to the gate). Stage report appended to the state-checkout entity, committed path-scoped (8b32ca5bd) and pushed via SSH. Worktree branch untouched.