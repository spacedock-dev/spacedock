# Gate review: Finding triage and the decline disposition, on advisory records (02av) — ideation, attempt 2

**What you are looking at:** the reframe you directed — the triage rule's record mechanism moved onto the recorder model. Three artifacts: this summary, the full design, and the recorder contract whose round-records section this task owns.

**Chosen direction:** the rule text you already approved stands unchanged (three classes; a finding neither fixed nor recorded is not triaged; narrowing a value claim is never the consumer's to make). What reframes is the record: the reviewer's round verdict and the ensign's triage are each ADVISORY resolutions — the decline names each declined finding with its class, why-not-material, and promote-when, the exact fields the landed validation taxonomy already requires. An advisory record can never advance status by construction; a design-reset or AC-narrowing graduates to a BINDING attempt — captain-owned — so the loop structurally cannot self-approve a reframe. The always-loaded cost is zero: the ~2 KB prose block the parked cut would have copied every round becomes a per-entity structured record.

**Boundary, answered honestly:** this task owns triage SEMANTICS only. The test that keeps it honest — "if the shape needed a new schema field, it would be plumbing" — was exercised: the spike constructed the concrete decline record against the frozen contract with zero schema change. The rounds plumbing is explicitly deferred (contract wording now says so, per the preflight fold); hand-authored interim.

**Fixtures are real:** the archived symlink finding and material control, plus the 0260 drive's actual recorded rounds; the offline classification check discriminates on them.

**Preflight:** three folds here (contract wording honesty, the duplicate-spec removal, a stale anchor) — all applied and verified; one spec home, no divergence.

**Recommend approve.** Checklist: 3 done, 0 skipped, 0 failed. This carries the moved release line: the seeded disproportionate finding produces a recorded decline and a zero-line diff in live replay.

**Decision:** approve = a pending advance to implementation. Revise = annotate. Hold = discuss.

---
*Companion artifacts: the full design (entity snapshot), the recorder contract.*
