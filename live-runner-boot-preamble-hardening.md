---
title: "Live-runner boot preamble hardening — a driven FO's boot fumbles can't reach the scenario under test"
status: validation
source: "0250 Commander session 2026-07-07 (captain-requested filing). Two same-day sonnet-lane instances of boot-phase nondeterminism failing scenarios UPSTREAM of their subject matter: (1) k7's PR run — TestLiveDefaultHeadlessStopsAtGate, the driven FO's `status --discover` returned empty twice in the fixture and it correctly stopped at no-workflow (rerun green); (2) zm's PR run — TestLiveClaudeSharedScenarios/smallest-sufficient-mechanism, the driven FO prefixed its boot with `cd /home/user` and operated outside the fixture root (caught by the runner's own claude_live_runner_test.go:130 diagnostic; opus variant green on the identical suite both times). Related same-day codex instance: shallow-boot's FO tried four wrong `state sweep` flag shapes (never `state sweep --workflow-dir .`), concluded the verb was broken, and improvised a partial manual advancement that skipped the archive — a call-shape fumble, not a deliverable defect. Each red costs a full lane run + FO triage + rerun; the flake tax erodes lane trust."
started: 2026-07-08T15:56:12Z
completed:
verdict:
score: 0.85
worktree: .worktrees/spacedock-ensign-live-runner-boot-preamble-hardening
issue:
id: sc592grb0w36q8ravzeya70r
pr: pr-merge:490
---

**Status as of 2026-07-08 (tightened after PR #488/#489 evidence): this is now actively blocking routine merges, not an occasional footnote.** PR #488 (`claude-result-extractor-first-vs-terminal` — a diff touching ONLY two Go test files, `internal/ensigncycle/claude_final_message_impl_test.go`/`claude_final_message_test.go`) failed its `claude-live (sonnet, CI-E2E)` lane on **5 consecutive attempts, 0 passes**, hitting **7 distinct scenario names** (self-evidence-merge-triage x2, keep-moving-posture, filing x3, TestLiveDefaultHeadlessStopsAtGate, smallest-sufficient-mechanism) — none with any coupling to the diff (confirmed via `gh pr diff --name-only` each time). A same-day cross-PR survey (`gh run list` across unrelated branches) found the sonnet live lane failing in 4 of 6 distinct runs checked (~33% pass rate today), and PR #489 (a completely unrelated diff) independently reproduced two of the same failure shapes on the SAME day, including one (wrong-root cd into the real CI checkout) now confirmed on BOTH sonnet and opus — this class is model-agnostic. Full evidence: 16 logged instances below, spanning PRs #481/#482/#488/#489.

Live scenario lanes let the driven FO's boot-phase nondeterminism (cwd wandering, discovery fumbles, call-shape guessing, contract-file hunting) fail scenarios before their subject matter runs — the assertion then reds on a preamble accident, indistinguishable in the checks summary from a real behavioral failure. Six confirmed sub-shapes so far: (1) wrong-root cd into a `/tmp` scratch dir, (2) wrong-root cd into the fixture's parent dir, (3) wrong-root cd into the CI runner's OWN spacedock checkout (`/home/runner/work/spacedock/spacedock`) — the most concerning, since the FO is then reading/grepping real production contract files instead of the fixture, (4) hanging past the 60s no-progress quiet budget while `find /`-hunting for a contract reference file not present in the minimal fixture, (5) `status --boot --identify` transiently returning a false-negative "no commissioned Spacedock workflow found" for a workflow that IS present (discovery race), (6) generic call-shape guessing (wrong `state sweep` flags). Direction for ideation: harden the preamble on both sides — (a) runner/prompt side: anchor the launch context explicitly (fixture root named in the drive prompt; runner pins/validates cwd before the scenario clock starts; possibly a boot-preamble retry distinct from scenario retry, since a boot fumble is not scenario evidence); (b) binary side (rider): `state ready` / `state sweep` / `status --boot --identify` tolerate the obvious flag shapes an FO guesses and close the discovery-race window, instead of exit-2ing or false-negativing a competent caller — cf. tv's default-to-stage precedent for meeting callers where they are. Constraint: hardening must NOT mask genuine boot-behavior regressions — scenarios whose SUBJECT is boot behavior (k7's greet drives) opt out. Acceptance sketch: value — a seeded boot-fumble drive (cd-away, discovery fumble, or contract-file hunt) either self-corrects or reds with a distinct preamble-failure diagnostic, never as the scenario's own assertion (baseline: PR #488's 5/5-fail, 7-scenario spread); mechanism — the runner guard + prompt anchor ship, with the flag-shape rider and the discovery-race fix (or their explicit rejection) recorded.

## Instance ledger (FO updates)

- Instance 3 (2026-07-07, PR #481 run 28852762853): claude-live OPUS — FO cd'd to the fixture's PARENT dir in the filing scenario (`cd /tmp/TestLive...filing...` instead of `.../001`), caught by the runner's wrong-root guard. The class is MODEL-AGNOSTIC (previously sonnet x2, now opus); the seed's framing should generalize. Same-run codex note for the 3v family ledger: a NEW failure shape, "foreground-wait watchdog typed stall" (scenario smallest-sufficient-mechanism, arm silent-after-wait, durable_progress=false) — an FO stall, not an oracle miss; may belong to this seed's hardening scope (watchdog/retry semantics) rather than 3v's oracle scope.
- Instance 4 (2026-07-07, PR #481 rerun, filing/opus — THE DEFINING SPECIMEN): transcript proves the wrong-root cd was a TRANSIENT, RECOVERED guess — the FO cd'd to the fixture parent, `spacedock new` fail-loudly refused ("no commissioned Spacedock workflow found... do NOT search"), the FO retried and `new` created 001/wire-the-thing.md with the correct id at the correct root. The runner's wrong-root guard keys on any boot-phase cd outside the fixture and reds DESPITE correct recovery and correct durable end-state. Design consequence for this seed: the guard should key on the DURABLE root/outcome (or tolerate binary-corrected recovery), except in scenarios whose SUBJECT is boot discipline. Two consecutive filing/opus reds on PR #481 were both this shape.
- Instances 5-6 (2026-07-07, PR #482 run): sonnet filing wrong-root again — this time cd to an unrelated /tmp/tmp.* scratch dir (boot wandering, new sub-shape); sonnet shallow-boot HUNG (1m no-progress quiet budget, killed) — the hang joins the watchdog/stall sub-class. Codex same run: filing "did not file via new" + the ready-one merge-guard-surface variant (3v family). All on a diff (release.yml/internal-release/docs) that no live-lane FO loads — pure family noise, zero diff coupling.
- Instance 7 (2026-07-08, PR #488 run 28937877624, `claude-live (sonnet, CI-E2E)`): `TestLiveClaudeSharedScenarios/self-evidence-merge-triage` HUNG (1m no-progress quiet budget, killed) — NEW sub-shape: not cd-wandering or flag-guessing but the driven FO searching for its own contract reference file (`find / -maxdepth 6 -iname "fo-merge-core.md"`, then an unbounded `find /` retry) after reading the merge-triage fixture, rather than proceeding on the fixture content alone. Upstream of the scenario's actual subject matter (a merge/triage judgment call with no binary guard). Diff under test (`internal/ensigncycle/claude_final_message_impl_test.go` + `_test.go`, entity `claude-result-extractor-first-vs-terminal`) has zero coupling to this scenario or the watchdog path — confirmed by reading `gh pr diff --name-only`. Re-ran the single lane per the FO's unrelated-flakiness branch; not routed through feedback.
- Instances 8-9 (2026-07-08, PR #488 rerun of run 28937877624, `claude-live (sonnet, CI-E2E)`, job 85872899892 — SAME lane's very next attempt, TWO NEW scenarios, neither the one that failed before): `keep-moving-posture` — boot command ran `cd /tmp/spacedock-live-plugin-746878277` (outside the fixture root), then HUNG (1m no-progress quiet budget) searching for `fo-dispatch-core.md`/`claude-fo-dispatch.md` via `find /` — the same self-orientation/contract-file-hunt sub-shape as Instance 7, compounded with wrong-root cd-wandering. `filing` — boot command ran `cd /home/runner/work/spacedock/spacedock` (the CI runner's OWN spacedock source checkout, not a fixture) and grepped its `internal/` tree for schema validation code — a new wrong-root variant (the FO wandered into the real repo checkout rather than a `/tmp` scratch dir). Both are the runner's own `claude_live_runner_test.go:130` wrong-root guard tripping, same diagnostic shape as Instances 3-6. Two unrelated scenarios failing on consecutive attempts of the same PR/lane, with zero diff coupling either time, is stronger evidence this class's failure rate on `claude-live (sonnet, CI-E2E)` is high enough that a single re-run should not be assumed sufficient to reach green — worth weighing when this entity is scored/picked up.
- Instances 10-11 (2026-07-08, PR #488 rerun #2 of run 28937877624, `claude-live (sonnet, CI-E2E)`, job 85891709073 — THIRD consecutive attempt, TWO MORE new failures): `TestLiveDefaultHeadlessStopsAtGate` (separate "Run live ensign cycle" step, not the shared-scenarios suite) — `status --boot --identify` returned exit 1 "no commissioned Spacedock workflow found" in the fixture even though `README.md`/`gate-stop.md` were present; the FO correctly reported zero-discovery and stopped per contract (not an FO defect) — this is the SAME failure shape as this entity's own founding source-field example ("k7's PR run", which self-corrected on rerun): a discovery-fixture race, not boot wandering. `filing` failed AGAIN (3rd consecutive attempt on this scenario across reruns, 2nd time via hang rather than wrong-root) — grepped `/tmp/spacedock-live-plugin-.../skills/first-officer/references/*.md` then broadened to `find / -iname "*fo-write-core*"` combined with a repo-wide `find /...xargs grep`, tripping the quiet budget again. THREE consecutive attempts of this PR's sonnet lane have now produced SIX distinct scenario failures (self-evidence-merge-triage, keep-moving-posture, filing x3, TestLiveDefaultHeadlessStopsAtGate) and ZERO passes on `claude-live (sonnet, CI-E2E)`, all traceable to this entity's scope, none to the PR's actual diff. `filing` failing on 3 of 3 attempts (2 different failure shapes) suggests it may be the highest-frequency offender in this class and worth a dedicated look when this entity is picked up.
- Instance 12 (2026-07-08, PR #488 rerun #3 of run 28937877624, `claude-live (sonnet, CI-E2E)`, job 85896767661 — FOURTH consecutive attempt, ZERO passes so far): `self-evidence-merge-triage` again (same scenario as Instance 7) — this time `status --boot --identify` itself returned "no commissioned Spacedock workflow found" (the SAME discovery-race false-negative as Instance 10, now reproducing on a SECOND, different scenario), and a follow-up `status --help --workflow-dir .` in the same session showed the entity WAS discoverable — confirming the false negative rather than a real absence. Then hung (no-progress quiet budget) after further exploration. **Cross-PR corroboration check**: surveyed `gh run list` for the "Runtime Live E2E" workflow across unrelated branches today (2026-07-08) — `claude-live (sonnet, CI-E2E)` failed in 4 of 6 distinct runs checked (this PR's run + 3 others: `fo-contract-path-portability-sweep`, `fo-write-core-product-edit-guard`, `codex-plugin-dir-skill-provider-shadowing`) and passed in 2 (`codex-plugin-dir-skill-provider-shadowing` earlier run, `codex-live-spawn-oracle-v2-family`) — a roughly 33% pass rate today across completely unrelated diffs, confirming this is a today-wide environmental degradation of the sonnet live lane, not anything specific to PR #488. At that base rate, 4 consecutive fails on one PR is unlucky but not statistically alarming (~20% chance); continuing to re-run remains the right call rather than treating this as a regression.
- Instances 13-15 (2026-07-08, PR #489 `fo-contract-path-portability-sweep` run 28949586687 — an UNRELATED diff/entity, `gh pr diff --name-only` confirms it touches `internal/contractlint/fo_contract_path_portability_test.go`, `internal/contractlint/fo_write_core_mutation_gate_test.go`, `internal/ensigncycle/fo_product_edit_guard_test.go`, `internal/release/journeydelta.go`, `skills/first-officer/references/claude-fo-dispatch.md`, `skills/fo-write-core/SKILL.md` — none overlapping PR #488's `claude_final_message_impl_test.go`/`claude_final_message_test.go`; note this entity's own `.spacedock-state` frontmatter read `status: ideation`, worktree empty earlier this session, so a live implementation-stage PR existing for it means a concurrent/peer actor advanced it since — not this FO's dispatch, report-only per the reconcile ownership rule): `claude-live (sonnet, CI-E2E)` job 85892565915 — `TestLiveDefaultHeadlessStopsAtGate` failed with the IDENTICAL "no commissioned Spacedock workflow found" discovery-race false-negative as Instances 10/12. `claude-live (claude-opus-4-8, CI-E2E-OPUS)` job 85892566044 — TWO failures in the same run: `filing` (wrong-root cd to the fixture's parent dir, the original Instance 3/4 shape) and `rejection-flow` (wrong-root cd to `/home/runner/work/spacedock/spacedock` — the CI runner's OWN spacedock checkout, not a fixture — the SAME shape as Instance 9's `filing` failure, except Instance 9 was sonnet and this is OPUS). This is the first direct confirmation that the "wandered into the real CI checkout" sub-shape is MODEL-AGNOSTIC (sonnet AND opus), not sonnet-specific, and independently corroborates the discovery-race sub-shape on a second, unrelated PR/diff the same day. Strengthens the case that this class is a today-wide environmental issue in the live-CI harness itself, not diff-specific, model-specific, or PR-specific.
- Instance 16 (2026-07-08, PR #488 rerun #4 of run 28937877624, `claude-live (sonnet, CI-E2E)`, job 85901868050 — FIFTH consecutive attempt on this PR, ZERO passes across all five): `TestLiveClaudeSharedScenarios/smallest-sufficient-mechanism` HUNG (1m no-progress quiet budget, killed) — a SEVENTH distinct scenario name to fail across this PR's five attempts (self-evidence-merge-triage x2, keep-moving-posture, filing x3, TestLiveDefaultHeadlessStopsAtGate, smallest-sufficient-mechanism), still zero coupling to the diff. FO paused the retry loop here per captain direction rather than continuing indefinitely. Final tally for PR #488: 5/5 sonnet-lane attempts failed, 16 logged instances of this class across 4 PRs today, 6 confirmed-distinct failure sub-shapes (wrong-root into /tmp scratch, wrong-root into fixture parent, wrong-root into the real CI checkout, contract-file find / hang, discovery-race false-negative, generic quiet-budget hang), 2 of 2 models affected. This is no longer an occasional flake footnote — it is actively blocking a routine, unrelated-diff PR from reaching a normal merge, at a today-observed sonnet-lane base pass rate of roughly 33%. Recommend this entity be picked up for ideation soon; see the rewritten problem statement above (bumped score) for the consolidated case.

## Proposed approach

The 16-instance ledger reduces to three failure mechanisms once the discovery "race" is diagnosed (see Spike record: it is NOT a race). Each is fixed at its root in the live-test harness and its fixtures; the binary is correct and gets no change.

**Root-cause map** (each of PR #488's 7 baseline scenarios traces to one of these):

1. **Wrong-root cd-wander** (instances 3-6, 8-9, 13-15; both models). Every drive prompt names the workflow only as `Workflow directory: .` (all 9 builders in `shared_fixtures_test.go`); the absolute root reaches the FO only via `cmd.Dir = workflowRoot` (`claude_live_runner_test.go:463`). Once the FO `cd`s before it boots, the `.` anchor is gone and it operates outside the fixture. The wrong-root detector (`detectWrongRootBoot`, wired at `claude_live_runner_test.go:518`) already reds this legibly — so the value-AC's "distinct diagnostic" half is already met for this shape; the gap is that it fails the scenario instead of reaching it.
2. **Contract-file find-hunt** (instances 7, 8, 11, 16). After reading the fixture the FO greps the filesystem for a contract reference file (`fo-merge-core.md`, `claude-fo-dispatch.md`) that lives in the `--plugin-dir` checkout, never the fixture, then stalls past the 60s no-progress budget. Today this surfaces as an OPAQUE `made no stream progress` stall, indistinguishable from a genuine mid-scenario hang. The claude runner does NOT run the existing `detectBroadSearchAtBoot` (only `zero_discover_live_test.go:112` does).
3. **Discovery false-negative** (instances 10, 12, 13-15). `status --boot --identify` returns "no commissioned Spacedock workflow found" and the FO correctly stops — failing the scenario as its OWN assertion. The Spike proves this is deterministic, not a race: only 2 of 10 fixture READMEs carry the production `commissioned-by: spacedock@` marker that discovery requires (`handlers.go:546`), so `--boot --identify` deterministically false-negatives on the other 8, while `--workflow-dir .` (which bypasses discovery) succeeds on all. The nondeterminism is in the FO's CHOICE between those two boot paths, not the binary's response.

**The fix — three changes, all in the harness/fixtures:**

- **(A) Anchor the drive prompt.** In the eight NON-boot-subject prompt builders (`gatePrompt`, `rejectionPrompt`, `escalationPrompt`, `mergeHookGuardPrompt`, `filingPrompt`, `mergeTriagePrompt`, `smallestMechanismPrompt`, `keepMovingPrompt`), replace `Workflow directory: .` with the ABSOLUTE fixture root the runner already knows (`workflowRoot`, threaded into the builder). A realism fix — production FOs are told their workflow path — that gives an anchor a stray `cd` cannot erase. It deliberately does NOT add "do not cd / do not search" imperatives: those duplicate the FO's own boot contract and would MASK a contract-adherence regression.
- **(B) Give the fixtures the production discovery marker.** Add `commissioned-by: spacedock@1` to every fixture README whose scenario expects a discoverable workflow (the 8 that lack it: gate, rejection, escalation, merge-hook, shallow-boot, merge-triage, keep-moving in `shared_fixtures_test.go`, plus `gateStopReadme` in `live_gate_stop_test.go`). Spike-proven to make `--boot --identify` resolve deterministically, so the FO's path choice can no longer produce a scenario-failing false-negative. `TestLiveZeroDiscoverReportsAndStops`'s fixture is the sole opt-out — its SUBJECT is non-discovery, so it stays markerless.
- **(C) Make the find-hunt red as a preamble fumble.** Wire the existing `detectBroadSearchAtBoot` into `claudeLiveRunner.run`'s failure path, immediately after the `detectWrongRootBoot` check (`claude_live_runner_test.go:518-526`), with a scenario-neutral message (the detector's current message is zero-discover-specific). A contract-file `find /` hunt then reds fast with "FO broad-searched the filesystem at boot" naming the sweep, instead of an opaque 60s stall. Does NOT mask — it keeps the fumble a RED, only legible and fast.

**Opt-out set (boot behavior is the SUBJECT — no anchor):** `TestLiveDefaultHeadlessStopsAtGate` (default-headless drive-to-gate), `shallow-boot` (no-TeamCreate-before-greet / S7b sweep), `TestLiveZeroDiscoverReportsAndStops` (report-and-stop). These keep `Workflow directory: .` so their orientation is tested unaided. gate-stop and shallow-boot still receive the marker (B): the marker makes the workflow findable at the CORRECT root without masking whether the FO reaches that root, and the wrong-root detector still catches a wander regardless of the marker.

**Binary side — explicit, justified scope-narrowing (no code change):**

- **REJECT** the seed's proposed `status --boot --identify` "close the discovery-race window" change. The Spike disproves the race: the false-negative is a deterministic fixture-realism gap (missing marker), the binary behaves correctly, and the fix belongs in the fixture (B).
- **DEFER** the `state sweep` / `state ready` flag-tolerance rider. It addresses only the single codex shallow-boot instance, not the measured PR #488 sonnet baseline; the four guessed flag shapes were never recorded, so we would design blind; and the tv default-to-stage precedent (`gate_extract.go:36`) covers defaulting an OMITTED arg (`state sweep --workflow-dir .` already works and already defaults via `ResolveWorkflowDir`) — it does not extend to accepting arbitrary WRONG flags. Follow-up: needs the exact guessed shapes captured before it can be designed.
- **DEFER** the boot-preamble RETRY (re-launch on a preamble-classified failure). Higher risk (misclassification could mask a real regression) and unnecessary — A+B+C address every scenario in the measured baseline. Held as the escalation lever if the live rate (AC-3) stays high after A+B+C land.

No user-visible / CLI / docs-site surface changes — all edits are to `internal/ensigncycle/*_test.go` harness and fixtures. No doc diff required.

## Acceptance criteria

- **AC-1 (value — eliminates the discovery false-negative class; CLI-measured, deterministic).** From every non-boot-subject fixture root, `spacedock status --boot --identify` resolves the workflow (exit 0, deep-boot) rather than the terminal "no commissioned Spacedock workflow found" — where the baseline is 8 of 10 fixtures false-negativing on that path (Spike-measured). The FO's nondeterministic choice between `--boot --identify` and `--workflow-dir .` can therefore no longer produce a scenario-failing false-negative, and the zero-discovery-subject fixture deterministically STAYS non-discoverable (guards over-application). *Test:* a Go test that builds each fixture and asserts the `--boot --identify` outcome (before: 2 discoverable; after: 10; zero-discovery fixture: 0). Baseline can move the wrong way — a fixture losing its marker, or the zero-discovery fixture gaining one, fails the test.
- **AC-2 (value — the find-hunt reds distinctly, never as the scenario's assertion; offline-measured, deterministic).** A boot stream carrying a contract-file `find /` hunt (the instance 7/8/11/16 shape) drives `claudeLiveRunner.run`'s failure path to fail with the broad-search preamble diagnostic naming the sweep, NOT the opaque `made no stream progress` stall and NEVER a scenario assertion. Baseline: today the same stream surfaces only as the opaque stall (or, on clean completion, as the scenario assertion against un-oriented state). *Test:* an offline Go test feeding a synthetic find-hunt stream through the wired classification path; the detector's own red/pass boundary is already covered by `broad_search_detect_test.go` (`repo_rooted_find_reds`, `find_path_arg_less_reds`).
- **AC-3 (value — end-value against the PR #488 baseline, rescoped to what the harness owns; live-measured, expensive).** Across the sonnet `claude-live` shared-scenario lane, on the non-boot-subject scenarios: wrong-root wander and discovery false-negatives (fixes A/B) recur at ZERO, and every contract-file find-hunt (fix C) reds fast and legibly via `classifyBootPreambleFailure` — NEVER as an opaque stall and NEVER as a downstream scenario assertion. Eliminating the find-hunt's live OCCURRENCE rate is explicitly OUT of this AC's scope: two rejection cycles (a validator rejection on the unmet original AC-3, then a captain-directed scope split after an opus advisory consultation) established that the hunt traces to a genuine gap in the FO contract itself (bare relative-path references to skill docs resolving against cwd instead of the plugin-dir root) — a contract-level fix, not a harness defect, and this harness cannot honestly own eliminating it. That fix is tracked separately by entity `fo-deferred-load-point-hunt-vs-skill-addressing` (short-id dp). Baseline: PR #488's 5-of-5 lane attempts failed, 7 distinct scenarios hit, 0 passes — all boot-preamble-class, with wrong-root wander and discovery false-negatives among the confirmed shapes this AC still holds to zero. *Test:* re-run the 8 non-boot-subject shared scenarios on the sonnet lane over ≥3 attempts; classify every red via `detectWrongRootBoot`/`detectBroadSearchAtBoot` — a wrong-root or discovery-false-negative red fails this AC, a classified find-hunt red does not. Runs LAST, after AC-1/AC-2 validate the mechanisms cheaply.

## Test plan

- **Offline / CLI (cheap — seconds, no model spend):**
  - AC-1 discovery test — Go test building each fixture and asserting `--boot --identify` discovery outcome. This is the Spike promoted to a repo test.
  - AC-2 classification test — offline Go test feeding a synthetic find-hunt stream through the runner's failure classification; reuses the already-green `broad_search_detect_test.go` for the detector boundary.
  - Regression guard — the existing offline negative-case detector tests (`wrong_root_detect_test.go`, `broad_search_detect_test.go`) reuse the prompt/fixture builders, so anchoring the prompts (A) and adding the marker (B) must keep them green. Run `go test ./internal/ensigncycle/... ./internal/status/...` (offline tags).
  - Cost: negligible; part of the standard offline lane that gates the live jobs.
- **Live workflow (expensive — model spend + CI env approval):**
  - AC-3 (rescoped) — the sonnet `claude-live` lane (`TestLiveClaudeSharedScenarios`, the 8 non-boot-subject scenarios) over ≥3 attempts, plus one `CI-E2E-OPUS` attempt to confirm the model-agnostic shapes (instances 3-4, 13-15): confirm wrong-root wander and discovery false-negatives recur at zero, and classify every red — a find-hunt classifying via fix C is NOT an AC-3 failure; its live occurrence rate is out of scope here and tracked by entity dp. Cost: full live-lane runs — the reason AC-1/AC-2 are paid first.
  - The opt-out boot-subject tests (`TestLiveDefaultHeadlessStopsAtGate`, shallow-boot, `TestLiveZeroDiscoverReportsAndStops`) confirm the anchor/marker changes did not mask their unaided-orientation subject — but "stay green" is not an unconditional promise for all three: `TestLiveDefaultHeadlessStopsAtGate` and `TestLiveZeroDiscoverReportsAndStops` should stay green, while shallow-boot may legitimately red under fix C's broad-search classifier (captain-confirmed 2026-07-08: a `find {fixture root}` self-orientation call is a real lean-boot discipline lapse this class of scenario exists to surface, not a detector bug).

## Spike record

**Riskiest mechanism:** whether the "discovery-race false-negative" (instances 10, 12, 13-15) is a binary race (needing a binary fix) or a deterministic fixture-marker mismatch (fixable in the fixture). The entire binary-side scope-narrowing rests on this.

**Exercised (CLI, no model):** built a minimal fixture mirroring `gateReadme`/`mergeTriageReadme` (no `commissioned-by`), git-init'd it, and ran `spacedock status --boot --identify` from it. Result — deterministic, not a race:
- WITHOUT the marker: 5/5 runs → exit 1 "no commissioned Spacedock workflow found …".
- `status --boot --workflow-dir . --json` (bypasses discovery), same markerless fixture: 3/3 → exit 0 boot JSON. This is the path the FO takes when the scenario PASSES; the failure is purely the FO choosing `--boot --identify` instead.
- AFTER adding `commissioned-by: spacedock@1`: 5/5 runs of `--boot --identify` → exit 0, deep-boot JSON.

**Conclusion:** the false-negative is a deterministic marker mismatch; adding the marker (fix B) eliminates it; no `status --boot --identify` binary change is warranted. This spike is AC-1's first test. Discovery predicate confirmed at `internal/status/handlers.go:546` / `discover_walkup.go:26` (`strings.HasPrefix(commissioned-by, "spacedock@")`).

**No spike needed for the other mechanisms:**
- Fix C rests on `detectBroadSearchAtBoot`, already offline-proven (`broad_search_detect_test.go`: `repo_rooted_find_reds`, `find_path_arg_less_reds`; plus the real-stream replays in `zero_discover_replay_test.go`); the change is only WIRING it into the claude runner's existing failure path.
- Fix A's effect on live model wander rate is inherently a live-measured claim — no cheap spike exists; it is validated by AC-3.

## Stage Report: ideation

- DONE: Concrete proposed approach covering both the runner/prompt-side fix and the binary-side fix (or an explicit, justified scope-narrowing decision) that replaces the current sketch with an implementable design
  `## Proposed approach`: fixes A (prompt anchor), B (fixture marker), C (wire `detectBroadSearchAtBoot`), each mapped to named files/lines; binary side explicitly narrowed — REJECT the discovery-race change (Spike-disproven), DEFER the flag-rider and the boot-preamble retry with justifications.
- DONE: Acceptance criteria including at least one value-measuring AC against an independent baseline (PR #488's 5/5-fail, 7-distinct-scenario spread logged in the Instance ledger) plus a test plan naming fixture/CLI/live-workflow costs
  AC-3 measures zero boot-preamble-class failures against PR #488's 5/5-fail / 7-scenario / 0-pass baseline; AC-1 (CLI/deterministic) and AC-2 (offline) added; `## Test plan` names offline/CLI (seconds, no model) vs live (model spend + CI env) costs.
- DONE: Riskiest-mechanism spike recorded (e.g., distinguishing a genuine boot-behavior regression from a preamble accident, or confirming how the discovery-race false-negative reproduces) or an explicit "no spike needed" with the proven mechanisms named
  `## Spike record`: CLI spike proved the discovery false-negative is a deterministic marker mismatch (5/5 fail without marker, 5/5 discover with it, `--workflow-dir .` bypasses), plus "no spike needed" for C (`detectBroadSearchAtBoot` already offline-proven) and A (live-only, validated by AC-3).

### Summary

Diagnosed the 16-instance ledger down to three root mechanisms and proposed a harness/fixture fix for each (prompt anchor, fixture discovery marker, wiring the existing broad-search detector into the claude runner), with the binary side explicitly narrowed out. The load-bearing spike disproved the "discovery race": it is a deterministic missing-`commissioned-by`-marker mismatch, so the fix belongs in the fixture and no `status --boot --identify` binary change is warranted. Key open judgment for the gate: whether naming the absolute fixture root in the drive prompt (fix A) is acceptable realism or unwanted hand-holding for the non-boot-subject scenarios — the opt-out set and the "no do-not-cd imperatives" rule are the guardrails against masking.

## Stage Report: implementation

- DONE: Fix A — absolute fixture root threaded into the 8 non-boot-subject drive-prompt builders, opt-out set left on "Workflow directory: ." unaided
  `gatePrompt`/`rejectionPrompt`/`escalationPrompt`/`mergeHookGuardPrompt`/`filingPrompt`/`mergeTriagePrompt`/`smallestMechanismPrompt`/`keepMovingPrompt` in `shared_fixtures_test.go` now take `workflowRoot string` and emit `"Workflow directory: "+workflowRoot`; all call sites in `claude_live_runner_test.go`, `codex_live_runner_test.go`, `livescenario_adapter_live_test.go` updated. `shallowBootPrompt`, `TestLiveDefaultHeadlessStopsAtGate`'s prompt, and `TestLiveZeroDiscoverReportsAndStops`'s prompt untouched (commit c5d43070).
- DONE: Fix B — `commissioned-by: spacedock@1` added to the 8 markerless fixture READMEs named in the Proposed approach, zero-discovery fixture stays markerless
  Marker added to `gateReadme`/`rejectionReadme`/`escalationReadme`/`mergeHookGuardReadme`/`shallowBootReadme`/`mergeTriageReadme`/`keepMovingReadme`/`gateStopReadme` (10 total markered READMEs now, confirmed via `grep -c`); `filingReadme`/`smallestMechanismReadme` already carried it. `gateStopReadme`/`gateStopEntity` moved from the live-tagged `live_gate_stop_test.go` into the default-tagged `shared_fixtures_test.go` so the offline AC-1 test can build every fixture without `-tags live`. `TestLiveZeroDiscoverReportsAndStops`'s `.gitkeep`-only root carries no README and is untouched.
  offline: `TestSharedScenarioFixturesAreDiscoverable` (10/10 subtests pass) + `TestZeroDiscoverFixtureStaysUndiscoverable` pass — see `boot_discovery_test.go`.
- DONE: Fix C — `detectBroadSearchAtBoot` wired into `claudeLiveRunner.run`'s failure path, offline/CLI suite green
  Added a pure `classifyBootPreambleFailure(stream, workflowRoot)` (wrong-root first, then broad-search) in `boot_preamble_classify_impl_test.go`, wired in at `claude_live_runner_test.go`'s former `detectWrongRootBoot`-only check, immediately ahead of the stall check. Generalized `detectBroadSearchAtBoot`'s three error messages off their zero-discover-specific "after a zero `status --discover`" phrasing to a scenario-neutral "boot-preamble filesystem sweep, not the scenario's own assertion" (`broad_search_detect_impl_test.go`) since the same detector now guards both the zero-discover branch and a normal-boot contract-file hunt.
  AC-1: `TestSharedScenarioFixturesAreDiscoverable`/`TestZeroDiscoverFixtureStaysUndiscoverable` (`boot_discovery_test.go`, new). AC-2: `TestClassifyBootPreambleFailureBroadSearch`/`...WrongRootTakesPriority`/`...CleanBootPasses` (`boot_preamble_classify_test.go`, new). Regression guard: `go test ./internal/ensigncycle/... ./internal/status/...` green (offline, no `-tags live`), `-race` green, `go build`/`go vet` green both with and without `-tags live` (confirms every live-tagged call site compiles against the new prompt signatures). AC-3 (live-measured, expensive) is validation's to run, not implementation's — offline/CLI mechanisms are the stage-owed proof here per the Proposed approach's cost ordering.

### Summary

Implemented all three fixes (A/B/C) from the ideation-stage Proposed approach on branch `spacedock-ensign/live-runner-boot-preamble-hardening` (commit c5d43070): the 8 non-boot-subject drive prompts now anchor on the absolute fixture root, the 8 markerless fixture READMEs now carry the discovery marker (with `gateStopReadme`/`gateStopEntity` relocated to the default-tagged fixtures file so it's testable offline), and the claude live runner now classifies a boot-preamble broad-search fumble as a distinct, fast diagnosis via a new `classifyBootPreambleFailure` alongside the existing wrong-root check. New offline tests cover AC-1 and AC-2 directly; the full offline `ensigncycle`/`status` suites plus `-race`, `go build`, and `go vet` (both build-tag configurations) are green. AC-3 (the live sonnet-lane measurement against the PR #488 baseline) is deliberately left to validation, per the Proposed approach's stated cost ordering (offline/CLI mechanisms paid first, live last).

### Feedback Cycles

**Cycle 1 (validation gate, REJECTED on AC-3).** AC-1 and AC-2 fully proven, offline and live (4 live attempts: 3 sonnet + 1 opus, 33 scenario-runs; wrong-root wander and discovery false-negatives never recurred). AC-3 as written ("ZERO boot-preamble-class failures... on the non-boot-subject scenarios") is not met: the contract-file-hunt sub-class — which fix C classifies correctly but, by explicit design, does not prevent — persists at 33% (8/24) across 3 sonnet attempts, concentrated in `filing` (0/3 pass). This is exactly the condition the ideation stage's own Proposed approach named as the trigger for its explicitly DEFERRED boot-preamble-retry escalation lever ("Held as the escalation lever if the live rate (AC-3) stays high after A+B+C land").

Routed back to implementation: add the already-designed, previously-deferred boot-preamble-retry mechanism (or an equivalent behavioral fix reducing the plugin-dir contract-file-hunt rate) — fixes A and B are correct and effective and should not be re-litigated. Also carry forward two prose corrections while in the body: AC-3's "5 non-boot-subject scenarios" should read 8 (confirmed via `claudeScenarioRunners()`); AC-1's "before: 2 discoverable; after: 9" should read 10. The Test Plan's "opt-out boot-subject tests... must stay green" line needs qualifying: shallow-boot legitimately reds under fix C (captain-confirmed, 2026-07-08 — a real lean-boot discipline lapse, not a detector bug, and outside AC-3's measured set since shallow-boot is a boot-subject opt-out) — the line should note this rather than promise unconditional green. PR #490 (https://github.com/spacedock-dev/spacedock/pull/490, run 28986231001) carries the full live evidence trail and stays open, unmerged, pending this rework.

## Stage Report: validation

- DONE: Independently reproduce AC-1/AC-2 offline evidence yourself (TestSharedScenarioFixturesAreDiscoverable, TestZeroDiscoverFixtureStaysUndiscoverable, TestClassifyBootPreambleFailure* subtests) plus full go build/go vet/go test -race, both with and without -tags live
  All green in the worktree. `go build`/`go vet` clean with and without `-tags live`. `go test ./...` and `go test -race ./...` (no tags) fully green. `go test -race -tags live` (fake HOME so credential-gated live tests self-skip cleanly, per `isolatedClaudeEnv`'s `$HOME`-only probe) was clean except: 3 Pi-live tests failing on a missing `pi-subagents` npm install (reproduced byte-identical against `main` with the same fake-HOME setup — confirmed pre-existing/environmental, unrelated to this diff) and one flaky timing-sensitive unrelated test (`TestSonnetTeamDeleteHangReplay`, in `streamwatch_regression_test.go`, untouched by this diff) that passed in isolation and on immediate rerun. `TestSharedScenarioFixturesAreDiscoverable` (10/10 subtests), `TestZeroDiscoverFixtureStaysUndiscoverable`, and `TestClassifyBootPreambleFailure{BroadSearch,WrongRootTakesPriority,CleanBootPasses}` all pass. Nit: AC-1's ideation prose says "before: 2 discoverable; after: 9" but the actual/correct count is 10 (10/10 subtests pass) — harmless undercount in the AC text, not a functional gap.

- DONE: AC-3 (live-measured): run the sonnet claude-live lane on the 5 non-boot-subject shared scenarios over >=3 attempts plus one CI-E2E-OPUS attempt, classify every red via detectWrongRootBoot/detectBroadSearchAtBoot against the PR #488 baseline (5/5-fail, 7-scenario, 0-pass in the Instance ledger)
  Pushed the branch and opened PR #490 (https://github.com/spacedock-dev/spacedock/pull/490), CI-E2E/CI-E2E-OPUS approved by CL, run 28986231001. Correction: `claudeScenarioRunners()` has 8 non-boot-subject scenarios in `TestLiveClaudeSharedScenarios`, not 5 (AC-3's "5" is a prose miscount) — measured all 8. Opus (CI-E2E-OPUS, 1 attempt): 9/9 scenarios pass, zero preamble-class failures, zero failures of any kind. Sonnet (CI-E2E, 3 attempts): 16/24 non-boot-subject scenario-runs pass (66.7%), 8/24 fail (33.3%). Every one of the 8 reds classifies via `classifyBootPreambleFailure` -> `detectBroadSearchAtBoot` (the contract-file-hunt sub-class — e.g. `grep -rn "spacedock new" /tmp/spacedock-live-plugin-.../skills/`, `find .../skills/first-officer -iname "*merge*"`) — NEVER wrong-root wander, NEVER a discovery false-negative, NEVER an unclassified opaque stall, NEVER a downstream scenario-assertion masquerading as preamble. Per-scenario across 3 attempts: filing 0/3 pass, keep-moving-posture 1/3, rejection-flow 2/3, self-evidence-merge-triage 2/3, smallest-sufficient-mechanism 2/3, gate-guardrail/feedback-3-cycle-escalation/merge-hook-guardrail 3/3 clean. shallow-boot (boot-subject opt-out, outside AC-3's measured set) failed 2/3, same class, via a `find {exact fixture root} -maxdepth 4` self-orientation call — raised to team-lead/CL; captain's call: this is a legitimate first-time catch of a genuine lean-boot discipline lapse (the FO's contract says `status --boot --identify` alone suffices; it shouldn't need `find` at its own root), not a detector bug, and does not block AC-3. It does contradict the Test Plan's "opt-out boot-subject tests... must stay green" line — that promise didn't anticipate fix C legitimately catching pre-existing shallow-boot behavior for the first time (previously masked because only zero-discover routed through this detector); the Test Plan line needs correcting, not the code or the fixture.

- DONE: Detached adversarial audit on a throwaway checkout, never the implementation worktree: this diff touches internal/ensigncycle/*_test.go, a named high-stakes CI/live-test-harness surface per the workflow Proof policy
  Cloned to a sibling directory (`git clone --no-local`, never the implementation worktree), fetched and checked out commit c5d43070 on a throwaway branch. Mutation-tested AC-1 (removed the `commissioned-by` marker from `gateReadme`) and AC-2 (short-circuited `classifyBootPreambleFailure`'s broad-search branch to `nil`) — both offline tests correctly red on the mutation, confirming they have teeth, not tautologies. Confirmed the opt-out set (shallow-boot, gate-stop's/zero-discover's drive prompts) correctly stayed unanchored while shallow-boot/gate-stop still gained the B marker. Non-blocking note: the WIRING of `classifyBootPreambleFailure` into `claudeLiveRunner.run`'s failure path (as opposed to the extracted function in isolation) has no offline regression test of its own — matches the pre-existing pattern for `detectWrongRootBoot`'s wiring (not a new gap this diff introduces); the live AC-3 run is what actually exercises it, which it did, 9 times, always correctly.

### Summary

AC-1 and AC-2 are fully proven, both offline (independently reproduced, mutation-tested with teeth on a throwaway checkout) and by direct live corroboration: across 4 live attempts (3 sonnet + 1 opus, 33 scenario-runs total), wrong-root wander and discovery false-negatives — the PR #488 baseline's dominant failure modes — NEVER recurred, and every one of the 8 sonnet contract-file-hunt reds was classified fast and legibly rather than as an opaque stall or a false scenario assertion, which is AC-2's exact value claim, now live-confirmed. AC-3 as WRITTEN ("ZERO boot-preamble-class failures... occur on the non-boot-subject scenarios") is NOT met: the contract-file-hunt sub-class — which fix C classifies but, by explicit design, does not prevent — persists at 33% (8/24) across 3 sonnet attempts, concentrated in `filing` (0/3 pass). This is precisely the condition the ideation stage's own Proposed approach flagged as the trigger for its explicitly DEFERRED boot-preamble-retry escalation lever ("Held as the escalation lever if the live rate (AC-3) stays high after A+B+C land"): 33% is high, and that lever was pre-authorized for exactly this measured outcome.

**Recommendation: REJECTED on AC-3.** Fixes A and B are correct and effective — zero recurrences of their targeted failure classes across every live attempt in this validation — and should not be re-litigated. Fix C's diagnosability mechanism is proven correct and valuable, live-confirmed 8/8 times it fired. But the entity's own stated AC-3 bar (zero preamble-class failures on the non-boot-subject scenarios) is not met, and the shipped diff has no behavioral fix for the remaining contract-file-hunt rate — only its classification. Route back to implementation to add the already-designed, previously-deferred boot-preamble-retry mechanism (or an equivalent behavioral fix reducing the plugin-dir contract-file-hunt rate), which ideation pre-authorized for this exact condition. Also carry forward two prose corrections for the next implementation/ideation pass: AC-3's "5 non-boot-subject scenarios" should read 8, and AC-1's "after: 9" should read 10. The Test Plan's opt-out-must-stay-green line needs qualifying per the shallow-boot finding above (captain-confirmed non-blocking). PR #490 (https://github.com/spacedock-dev/spacedock/pull/490) carries the full live evidence trail (run 28986231001) and should stay open, unmerged, pending the AC-3 rework.

## Stage Report: implementation (cycle 2)

- DONE: Boot-preamble-retry mechanism (or equivalent behavioral fix) implemented to reduce the plugin-dir contract-file-hunt rate, with offline/CLI tests proving the mechanism itself; the live AC-3 re-measurement is validation's job next cycle, not this one
  `claudeLiveRunner.run` (`claude_live_runner_test.go`) now loops per-launch via a new `launchAttempt` helper, retrying a launch whose stream `classifyBootPreambleFailure` flags with a fresh relaunch. The retry decision is the pure `shouldRetryBootPreamble(scenarioName, attempt, preambleClassified)` policy in the new default-tagged `boot_preamble_retry_impl_test.go`: retries up to `bootPreambleMaxAttempts` (3) total, ONLY on a classified preamble fumble (never a generic stall or a scenario assertion, which the classifier never flags), and NEVER for a boot-subject scenario (`isBootSubjectScenario`; shallow-boot is the only one reachable through this runner) — so the captain-confirmed shallow-boot lean-boot-discipline red is never retried away. Each attempt's stream archives under its own `claude-stream.attempt{N}.jsonl`; the canonical `claude-stream.jsonl` reflects the last attempt. Offline proof: `boot_preamble_retry_test.go` (`TestShouldRetryBootPreambleRetriesUpToBudget`, `...NeverRetriesACleanBoot`, `...OptsOutBootSubjectScenarios`, `TestIsBootSubjectScenario`), all passing. Commit 949fa2d1, pushed to PR #490's branch (head now `949fa2d1`).
- DONE: Two prose corrections applied: AC-3's scenario count 5->8, AC-1's fixture count 9->10
  `## Acceptance criteria` AC-1's *Test* clause now reads "after: 10"; AC-3's *Test* clause now reads "re-run the 8 non-boot-subject shared scenarios". `## Test plan`'s live-workflow AC-3 line also corrected to "the 8 non-boot-subject scenarios". The "5"/"9" strings still present elsewhere in the body are inside the ideation/validation Stage Report sections (historical quotes of what needed fixing) and were deliberately left untouched — those reports are not rewritten after the fact.
- DONE: Test Plan's "opt-out boot-subject tests... must stay green" line reworded to reflect the captain-confirmed shallow-boot finding
  Reworded to split the three: `TestLiveDefaultHeadlessStopsAtGate`/`TestLiveZeroDiscoverReportsAndStops` should stay green; shallow-boot may legitimately red under fix C's classifier (a real lean-boot discipline lapse it exists to surface, not a detector bug) and is deliberately excluded from the new retry, so that red is never masked.

### Summary

Implemented the boot-preamble-retry lever cycle-1 validation authorized: `claudeLiveRunner.run` now retries a launch through a fresh `launchAttempt` when its stream classifies as a boot-preamble fumble, bounded by the pure, offline-tested `shouldRetryBootPreamble` policy (up to 3 attempts, never for the boot-subject shallow-boot scenario). Applied the two requested prose corrections (AC-3 5->8, AC-1 9->10) and reworded the Test Plan's opt-out-green line to reflect the captain-confirmed shallow-boot finding. `go build`/`go vet`/`go test`/`go test -race` all green with and without `-tags live`; new offline tests (`TestShouldRetryBootPreamble*`, `TestIsBootSubjectScenario`) pass. Pushed to PR #490's branch (commit 949fa2d1) for the next validation cycle's live AC-3 re-measurement.

### Feedback Cycles

**Cycle 2 (validation gate, REJECTED — a correctness defect in the retry mechanism itself, discovered live).** Offline reproduction and the adversarial audit of `shouldRetryBootPreamble` (the pure decision policy) both fully passed. But the first live re-measurement attempt surfaced a state-contamination bug in how that policy is WIRED into `claudeLiveRunner.run`/`launchAttempt`: retries reuse the SAME `workflowRoot` with no reset between attempts, and `classifyBootPreambleFailure` scans the ENTIRE stream (not an early boot prefix), so a broad-search can trip AFTER the FO has already done real, durable work (git commits, entity edits, worker dispatches). That attempt is discarded and retried anyway. Concrete evidence (downloaded and read the actual CI artifacts, not just the summary): on sonnet's `keep-moving-posture`, attempt 1 ran the full scenario to completion — 3 sub-agents dispatched, `approved-gate`/`ready-one`/`ready-two` committed to done+archived, `questioned` re-shaped, the gate presented — before a `find .../references -maxdepth 1` call (early in the transcript, but well before any state mutation) triggered a retry. Attempt 2 booted against the now-already-mutated `workflowRoot`, correctly found nothing left to do on the three archived entities, and re-presented `questioned`'s gate. The scenario assertion, checked only against the LAST attempt's stream, then failed with "the FO did not advance the approved entity... after the captain's approval" — which is false; it did, in attempt 1. The retry discarded a genuine pass and manufactured a false negative. `self-evidence-merge-triage` independently corroborated the same contamination pattern (a real `git commit` in attempt 1 before a mid-run retry trigger) but happened to still pass because its assertion reads final entity state rather than "did this run's transcript show action X" — confirming the exposure is general to any transcript/action-based assertion, not scenario-specific.

Root cause is a wiring/integration defect, not a decision-logic defect: `shouldRetryBootPreamble` itself is correct and mutation-tested with teeth (both the unbounded-retry and boot-subject-opt-out-removed mutations were caught). The bug is that the pure policy's precondition — "a retry gets a clean slate" — was never true in the shipped wiring, and no offline test could have caught it (the retry tests are pure-function, no I/O). Two candidate fixes, left for implementation to pick and defend: (a) reset/re-provision `workflowRoot` before each retry attempt, or (b) scope classification and retry-eligibility to an early boot prefix, stopping the process before it does any state-mutating work rather than scanning the whole completed transcript.

Given this defect makes any live rate collected under it untrustworthy, validation halted after 1 sonnet + 1 opus attempt rather than spend the remaining budget on data from a mechanism now known to be unsound (team-lead independently re-verified the defect by reading `claude_live_runner_test.go:442-456` and concurred). Minor, non-blocking nit carried forward: `.run()`'s failure message unconditionally appends "(persisted across the boot-preamble retry budget)" whenever `preamble != nil`, even for `shallow-boot` (the boot-subject opt-out), which never actually retries — the wording implies multiple attempts were tried when only one ever ran.

**Cycle 3 (captain-directed scope split, NOT a validator rejection).** Following cycle 2's REJECTED (the retry's state-contamination defect), an opus advisory consultation was run on the underlying contract-file-hunt behavior itself rather than on the retry's wiring bug. It root-caused the hunt to a genuine gap in the FO contract: bare relative-path references to `fo-dispatch-core.md`/`fo-merge-core.md`/`fo-smallest-sufficient-mechanism.md` resolve against the FO's cwd rather than the `--plugin-dir` checkout root, and fail to resolve in the live-CI harness's cwd — a 1:1 match against every find-hunt instance in the ledger. Fixing the live occurrence rate of the hunt is therefore a contract-level fix, out of this entity's declared harness-only scope by construction; the captain judged that two rejection cycles kept correctly routing back to "add more harness" only because the entity's own AC-3 asked the harness to own a rate it structurally cannot move. The captain split that work to a new entity, `fo-deferred-load-point-hunt-vs-skill-addressing` (short-id dp), and directed this entity's scope back to strictly harness-honest: back out the buggy retry (commit 949fa2d1) entirely rather than debug its wiring further, and rewrite AC-3 to describe only what fixes A/B/C actually prove — wrong-root wander and discovery false-negatives at zero, and the find-hunt classifying fast/legibly rather than eliminating its rate. This entity hands off cleanly to entity dp for the contract-level fix; no further AC-3 rate work belongs here.

## Stage Report: validation (cycle 2)

- DONE: Independently reproduce the new boot-preamble-retry mechanism's offline evidence yourself (TestShouldRetryBootPreambleRetriesUpToBudget, ...NeverRetriesACleanBoot, ...OptsOutBootSubjectScenarios, TestIsBootSubjectScenario) plus full go build/go vet/go test -race, both with and without -tags live
  All 4 new tests pass; re-ran `TestSharedScenarioFixturesAreDiscoverable`/`TestZeroDiscoverFixtureStaysUndiscoverable`/`TestClassifyBootPreambleFailure*` as a regression guard, still green. `go build`/`go vet` clean with and without `-tags live`. `go test ./...` and `go test -race ./...` (no tags) fully green. `go test -race -tags live` (fake HOME, same technique as cycle 1) clean except the same 3 pre-existing/environmental Pi-live failures (missing `pi-subagents` npm install, unrelated to this diff).

- FAILED: AC-3 re-measurement (live, sonnet claude-live lane, >=3 attempts): confirm the retry reduces the contract-file-hunt failure rate from cycle 1's 33% (8/24) baseline, and confirm shallow-boot's captain-confirmed red is never masked by the retry (isBootSubjectScenario opt-out)
  Pushed commit 949fa2d1 to PR #490's branch; the push auto-triggered run 28992390800, CI-E2E/CI-E2E-OPUS approved by CL. Completed 1 sonnet + 1 opus attempt, then HALTED (see the state-contamination defect above) rather than spend 2 more live attempts on data from a mechanism now known unsound. shallow-boot's opt-out held correctly (single attempt both models, no retry attempted — the boot-subject exclusion works as designed). What DID measure cleanly: wrong-root wander and discovery false-negatives (fixes A/B) still never recurred. What did NOT measure cleanly: the contract-file-hunt rate itself is now confounded by the contamination bug — `keep-moving-posture`'s sonnet red is a false negative caused by the retry, not a genuine unresolved fumble, so cycle 1's 33% baseline cannot yet be honestly compared against a cycle-2 number.

- DONE: Detached adversarial audit on a throwaway checkout targeting the new retry logic specifically (e.g. mutate shouldRetryBootPreamble to retry unboundedly or to retry a boot-subject scenario) and confirm the offline tests catch it
  Fresh throwaway checkout (sibling directory, never the implementation worktree), commit 949fa2d1. Mutation 1 (unbounded retry — dropped the `attempt < bootPreambleMaxAttempts` check): `TestShouldRetryBootPreambleRetriesUpToBudget` correctly reds. Mutation 2 (boot-subject opt-out removed): `TestShouldRetryBootPreambleOptsOutBootSubjectScenarios` correctly reds. Both confirm the pure policy function has real teeth. This audit's scope (pure-function mutation) is exactly why it could not have caught the state-contamination defect above — that bug lives in the I/O wiring around the policy, not the policy's own decision logic; the two are independent claims and this cycle proves both must be checked separately.

### Summary

Cycle 2's offline work is clean: the retry policy is correct, mutation-tested with teeth, and does not regress AC-1/AC-2. But the first live attempt at AC-3 re-measurement surfaced a genuine correctness defect the offline tests structurally could not reach — the retry shares a mutable `workflowRoot` across attempts with no reset, so a late-firing classification on an already-completed, already-successful run discards real evidence and replays against contaminated state, producing at least one confirmed false-negative red (`keep-moving-posture`) and one likely-silent instance that only passed by luck (`self-evidence-merge-triage`). This is precisely the kind of defect this stage exists to catch independently rather than rubber-stamp the implementer's self-report, per team-lead's dispatch framing — team-lead independently re-verified the root cause by reading the source directly and concurred.

**Recommendation: REJECTED.** Route back to implementation with the defect and both candidate fixes (reset `workflowRoot` per attempt vs. scope classification/retry-eligibility to an early boot prefix before state-mutating work begins) — implementation's call which to take. AC-3 live re-measurement (the 2 remaining sonnet attempts + a clean rate comparison against cycle 1's 33% baseline) is deliberately deferred to the next validation cycle once the mechanism is trustworthy; spending that budget now would only produce more contamination-tainted data. Carry forward the minor message-wording nit (the "(persisted across the boot-preamble retry budget)" text is misleading for `shallow-boot`'s never-retried single attempt).

## Stage Report: implementation (cycle 3)

- DONE: Revert commit 949fa2d1 via `git revert` (not reset/force-push) on the PR 490 branch, keeping c5d43070's fixes A/B/C fully intact; confirm go build/go vet/go test -race clean afterward, both with and without -tags live
  `git revert --no-edit 949fa2d1` on the worktree branch produced commit bcd23274, cleanly removing `launchAttempt`/`bootPreambleMaxAttempts`/`isBootSubjectScenario`/`shouldRetryBootPreamble` and both new retry test files, restoring the single-attempt `claudeLiveRunner.run` from c5d43070 with `classifyBootPreambleFailure` still wired at its failure path (confirmed via `git show --stat` and a direct grep — only that one call site remains). `go build`/`go vet` clean with and without `-tags live`; `go test -race ./internal/ensigncycle/... ./internal/status/...` (no tags) green; `go test -tags live -run '^$' ./internal/ensigncycle/...` compiles clean. `TestSharedScenarioFixturesAreDiscoverable` (10/10), `TestZeroDiscoverFixtureStaysUndiscoverable`, and `TestClassifyBootPreambleFailure*` (AC-1/AC-2's tests, from c5d43070) re-confirmed still passing post-revert. Pushed to PR #490's branch (head now `bcd23274`).
- DONE: Re-scope AC-3 in the entity body from "ZERO boot-preamble-class failures... on non-boot-subject scenarios" to what the harness can honestly own: wrong-root wander and discovery false-negatives (fixes A/B) recur at zero, and every find-hunt reds fast/legibly via classification (fix C), never as an opaque stall or a false scenario assertion — eliminating the find-hunt's live occurrence rate is explicitly out of scope, tracked separately by entity fo-deferred-load-point-hunt-vs-skill-addressing (short-id dp, a contract-level fix, not a harness fix). Update the Test Plan's AC-3 live-workflow line and the opt-out-green line to match
  `## Acceptance criteria` AC-3 rewritten to hold only wrong-root wander + discovery false-negatives to zero and require find-hunts to classify (not vanish), naming entity dp as the owner of the occurrence-rate fix. `## Test plan`'s AC-3 live-workflow line rewritten to match (confirm zero + classify, not "reduce the rate"); the opt-out-green line's stale reference to the now-reverted retry's `isBootSubjectScenario` exclusion removed, restored to describing only the classifier-driven shallow-boot caveat (still accurate — fix C classifies shallow-boot's single attempt exactly as before the retry existed).
- DONE: Record this as Feedback Cycle 3 in the entity body's ### Feedback Cycles: captain-directed scope split (not a validator rejection) after an opus advisory consultation root-caused the find-hunt behavior to a contract-level gap outside sc5's declared harness-only scope; cite entity dp as where that work continues
  Appended under the cycle-2 `### Feedback Cycles` section (the most recent one), naming the opus consultation's root cause (bare relative-path skill-doc references resolving against cwd instead of `--plugin-dir`), the captain's scope-split rationale, and entity `fo-deferred-load-point-hunt-vs-skill-addressing` (dp) as the fix's new owner.

### Summary

Backed out the buggy boot-preamble-retry mechanism cleanly via `git revert` (commit bcd23274) rather than debug its state-contamination wiring defect further, per the captain's scope-split direction: an opus advisory consultation traced the underlying contract-file-hunt to a genuine FO-contract gap (relative skill-doc references resolving against cwd, not `--plugin-dir`) that this harness-only entity cannot honestly fix, and that work now belongs to the new entity `fo-deferred-load-point-hunt-vs-skill-addressing` (dp). Fixes A/B/C from c5d43070 remain fully intact and verified green (offline build/vet/test/-race, with and without `-tags live`; AC-1/AC-2's own tests re-confirmed). AC-3 and its Test Plan lines are rewritten to describe only what this harness actually proves — wrong-root wander and discovery false-negatives at zero, find-hunts classifying fast/legibly rather than vanishing — with the occurrence-rate question explicitly handed off to entity dp. PR #490 (https://github.com/spacedock-dev/spacedock/pull/490) now sits at commit bcd23274, ready for a validation pass scoped to the rescoped AC-3.

## Stage Report: validation (cycle 3)

- DONE: Confirm the revert (bcd23274) is clean and complete: fixes A/B/C's actual code is genuinely unchanged from c5d43070 — diff the relevant files/tree against c5d43070 yourself, don't just trust the implementer's git show summary
  `git diff c5d43070 bcd23274` over both `internal/ensigncycle/` and the FULL repo tree returns byte-zero output. Stronger than a diff: `git rev-parse c5d43070^{tree}` and `git rev-parse bcd23274^{tree}` are the IDENTICAL tree object hash (`607a58f1cd48ee272ea1eccfd231aa3619bf8117`) — a mathematical guarantee no file anywhere in the repo differs, not just an absence-of-diff-output inference. `grep -rn "shouldRetryBootPreamble|bootPreambleMaxAttempts|isBootSubjectScenario|launchAttempt" internal/ensigncycle/` and `ls internal/ensigncycle/ | grep -i retry` both confirm zero hits — the retry files/symbols are fully gone, not merely disabled.
- DONE: Confirm the rescoped AC-3 and Test Plan lines accurately describe what cycle-1's already-gathered live evidence proved (4 attempts: 3 sonnet + 1 opus, 33 scenario-runs; zero wrong-root wander; zero discovery false-negatives; 8/8 find-hunts classified fast/legibly) — if the revert is genuinely byte-identical to c5d43070 on the relevant surface, cycle-1's evidence honestly re-applies and no new live spend should be needed; only re-run live if a concrete reason the old evidence doesn't cleanly cover the reverted state is found
  Read the rescoped `## Acceptance criteria` AC-3 and `## Test plan` lines against my own cycle-1 report (still in this file, cycle-1's Stage Report section): the rescoped bar is exactly the strict subset of what cycle-1 already measured — zero wrong-root wander (cycle-1: 0/33), zero discovery false-negatives (cycle-1: 0/33), every find-hunt classifies via `classifyBootPreambleFailure` rather than an opaque stall or a masquerading scenario assertion (cycle-1: 8/8 sonnet reds, all correctly classified this way). Given the tree-hash identity above, the code that produced cycle-1's evidence IS the code shipping now — no NEW claim exists that cycle-1's evidence doesn't already cover, so no concrete reason for fresh live spend. Considered and rejected as non-concrete: live-model/tool drift between cycle-1 (2026-07-08) and cycle-3 (2026-07-09) is the same generic noise the entity's own ≥3-attempt sampling design already exists to average over, not a structural gap specific to this revert. No PR trigger or CI environment approval was needed this cycle — a first for this entity's validation history.
- DONE: Confirm entity dp is correctly cited as the owner of the find-hunt occurrence-rate fix, and that AC-1/AC-2 remain intact with their own tests still passing
  `docs/dev/.spacedock-state/fo-deferred-load-point-hunt-vs-skill-addressing.md` exists, id `dpwp415wfzj6yrcwbs0krrea` (short-id "dp" matches), title/source precisely match this entity's citation and framing (the bare-relative-path-vs-plugin-dir root cause, split from this entity 2026-07-09 per captain direction). AC-1/AC-2: `go build`/`go vet` clean with and without `-tags live` (fresh run, post-revert); `TestSharedScenarioFixturesAreDiscoverable` (10/10 subtests), `TestZeroDiscoverFixtureStaysUndiscoverable`, `TestClassifyBootPreambleFailure{BroadSearch,WrongRootTakesPriority,CleanBootPasses}` all pass (fresh `-count=1` run).

### Summary

The revert is clean by the strongest evidence available — identical tree object hashes, not just an empty diff — so fixes A/B/C are byte-for-byte the exact code cycle-1 already validated live. The rescoped AC-3/Test Plan text is an honest, strict subset of what cycle-1's 4-attempt/33-scenario-run evidence already proved (zero wrong-root wander, zero discovery false-negatives, 8/8 find-hunts classified fast/legibly), so that evidence re-applies without qualification and no fresh live spend was warranted — the first validation cycle on this entity that closed without a CI trigger. Entity dp is correctly filed and cited as the occurrence-rate fix's new, separate owner. AC-1/AC-2's own tests re-confirmed green post-revert.

**Recommendation: PASSED.** Fixes A/B/C ship as validated in cycle 1 (0 wrong-root/discovery-false-negative recurrence across every live attempt gathered across this entity's history; every find-hunt classifies correctly). AC-3 as rescoped is met by evidence already on record. The retry mechanism's contamination bug (cycle 2) is fully backed out, confirmed by tree-hash identity rather than trusted by report. The occurrence-rate question is honestly handed off to entity dp rather than closed out here under false pretenses. No outstanding blockers for this entity.
