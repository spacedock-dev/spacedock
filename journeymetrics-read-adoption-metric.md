---
title: Measure journeymetrics read adoption and trim the redundant dispatch-prompt hint
status: validation
source: "captain (2026-06-16, 0204 sprint) — e6a's status --read adoption was proven WORKING once (AC6 single live drive). No ongoing verification that real FO/ensign sessions call --read; the six contract sites are wording, and wording-present is not behavior. journeymetrics already parses every journey transcript but records ToolCallsByName by tool NAME only — `status --read` is a Bash subcommand, invisible today as a generic Bash call."
score:
sprint: 0204-structured-reads
sprint-readiness: ready
issue:
id: hf4jmbksapyg2d9s0zj85wca
started: 2026-06-16T20:02:50Z
worktree: .worktrees/spacedock-ensign-journeymetrics-read-adoption-metric
mod-block: merge:pr-merge
pr: "#392"
---

## Scope note
This task is a measure-then-trim unit. It **supersedes and absorbs `read-hint-adoption-bloat-trim`** (the 4x backlog entity): the metric this task builds is the standing before/after monitor for the trim, so the two ship as one. Part 1 builds the journeymetrics `--read` adoption detection; part 2 trims the redundant site-6 dispatch-prompt `--read` hint in `build.go` and proves adoption does not regress using the metric from part 1. The 4x entity can be closed as absorbed once this lands.

## Problem
**Part 1 — no standing adoption measurement.** e6a's `status --read` adoption is proven to work exactly once (AC6's single live FO drive during e6a validation). There is no standing, behavioral check that real FO/ensign sessions actually invoke `--read`. The instruction lives in six contract sites; that is wording, not observed behavior.

`internal/journeymetrics` parses every journey transcript and records `ToolCallsByName` — but keyed by tool NAME only (`toolCallsByName[block.Name]++` in `claude.go:75`; the codex side does the same in `codex.go:52`). A `status --read` invocation is a Bash subcommand, so it is counted as a generic `Bash` call and the `--read` flag is invisible. A scoped `Read` (one carrying `offset`/`lines`) is counted identically to a full-file `Read`.

**Part 2 — redundant per-dispatch instruction bloat.** The `--read` guidance reaches the ensign in three places it sees every dispatch: `ensign-shared-core.md:18`, `ensign-shared-core.md:92`, AND the `internal/dispatch/build.go` site-6 dispatch-prompt hint (`build.go:552-556`, the "For later re-reads of the body during work…" line) that is appended to EVERY dispatch prompt — the line repeated across all 13 dispatch goldens (confirmed: `grep -l "For later re-reads of the body during work" internal/dispatch/testdata/golden/*.txt` returns exactly 13). Site-6 largely reinforces sites 4/5, which the ensign already loads via its `Skill` call. Heavier instruction is not stronger proof; it is per-dispatch prompt bloat. The captain notes `status --read` adoption is ALREADY observably happening in the project's token/boot-forensics output under the current 6-site instruction, so the trim is de-risked: the metric is the standing monitor, not a hypothetical gate.

## Spike result
**No spike needed: the Bash `tool_use` command STRING is already on disk in the transcripts journeymetrics parses; the parser merely discards it.**

The riskiest assumption was whether the Bash command args (not just the tool name) are reachable in the stream-json transcript. They are, proven against committed real captures:

- Real ensigncycle capture `internal/ensigncycle/testdata/shallow-boot-greet.stream.jsonl` carries `{"type":"tool_use",...,"name":"Bash","input":{"command":"spacedock status --boot --json"}}`. Other real captures carry `spacedock status --discover`, `spacedock status --workflow-dir … --boot`, etc. The `input.command` string is present and complete.
- Real `Read` tool_use blocks carry `input.file_path` (e.g. `internal/ensigncycle/testdata/sonnet_teamdelete_marker_continues.stream.jsonl`). A scoped `Read` carries `offset`/`lines` alongside it in the same `input` object — the same object the parser drops.
- The committed journeymetrics fixtures (`testdata/claude_terminal_split.stream.jsonl`, `testdata/claude_no_terminal.stream.jsonl`) already carry `"input":{"command":"…"}` on their Bash blocks, confirming the format the parser consumes.

The reason `--read` is invisible today is purely that `claudeContent` (`claude.go:214-218`) unmarshals only `Type`/`ID`/`Name` and never reads `input`. The fix is to unmarshal `input` and inspect it — no new data source, no runtime handoff, no on-disk format change. Codex is symmetric: `tool_call.started` carries `arguments.cmd` (e.g. `{"cmd":"go test ./..."}` in `testdata/codex-exec.jsonl`), currently discarded.

## Proposed approach

### Part 1 — the metric
Extend the claude transcript parser (and mirror on codex) to surface a per-journey `status --read` adoption count, and a scoped-`Read` count, on the journey `Record`.

**Detection (claude.go):**
1. Extend `claudeContent` to unmarshal the tool-use `input` as `json.RawMessage` (kept raw so the struct stays a thin decode of the block; interpretation lives in small helpers).
2. For a `tool_use` whose `Name == "Bash"`, decode `input.command` and increment a `StatusReadCalls` counter when the command string contains a `status --read` invocation. Detection matches the launcher-agnostic forms — `spacedock status --read`, `sd status --read`, `${SPACEDOCK_BIN:-spacedock} status --read`, and a bare `status --read` — by testing for the `status` subcommand followed (in any flag order) by a `--read` token in the command string. The detector is a single helper `commandInvokesStatusRead(cmd string) bool` so the matching rule is unit-testable in isolation and shared by both runtimes.
3. For a `tool_use` whose `Name == "Read"`, decode `input` and increment a `ScopedReadCalls` counter when `offset` OR `lines` is present and non-zero (a full-file Read carries neither).
4. Dedup both counters on the same `toolID` key the existing `toolCallsByName` dedup uses (`claude.go:67-75`), so a repeated stream delta does not double-count — matching the existing tool-call dedup discipline.

**Surfacing on the record:** add two integer fields to `Observation` and `Record` (`StatusReadCalls`, `ScopedReadCalls`) with `omitempty` JSON tags (`status_read_calls`, `scoped_read_calls`). `BuildRecord` copies them from the observation (`record.go:53-55`), exactly as `ToolCallsByName` flows today. No ledger change is required: `AggregateLedger` preserves each `Record` verbatim inside `ScenarioLedgerEntry.Observations`, so the per-release ledger surfaces the counts per observation, and successive release ledgers give the over-time series. "Over time" is the sequence of per-release ledgers, not a new aggregate field — this is the smallest change that satisfies the requirement.

**Codex (codex.go):** mirror the Bash-arg detection against `tool_call.started`'s `arguments.cmd`, reusing the shared `commandInvokesStatusRead` helper, and surface `StatusReadCalls` on `CodexCharacterization`. Codex has no first-class `Read` tool (file reads go through the shell), so `ScopedReadCalls` is claude-only; the codex characterization carries the Bash-derived `status --read` count only. Note that FO/ensign `--read` adoption is principally a Claude-runtime concern (the FO/ensign runtimes drive Claude Code), so the codex mirror is for completeness and symmetry, not the primary signal.

### Part 2 — the trim
Remove the redundant site-6 dispatch-prompt hint and prove adoption does not regress, using the metric from Part 1 as the before/after monitor.

1. **Trim:** delete the `parts = append(...)` block at `build.go:552-556` (the "For later re-reads of the body during work — e.g. to find the append point for your stage report — use `… status --read … --json` …" line) and its now-orphaned rationale comment fragment at `build.go:539-540`. The first whole-spec entity-read instruction (`build.go:546-551`) stays — only the trailing repeat-read hint is removed. Sites 4 and 5 (`ensign-shared-core.md:18`, `:92`), which the ensign loads via its `Skill` call, remain the contract for repeat-read behavior.
2. **Regenerate goldens:** `go test ./internal/dispatch -run TestBuild -update` regenerates the checked-in dispatch goldens from native output (the `-update` flag is wired in `internal/dispatch/golden_harness_test.go`). The trim removes the repeated hint line from all 13 affected goldens; the diff is exactly that line's deletion across those files. Re-run `go test ./internal/dispatch/...` and confirm green.
3. **Adoption-not-regress proof (behavioral, non-tautological):** the metric from Part 1 is the monitor. The existing `//go:build live` harness in `internal/ensigncycle/journey_metrics_live_test.go` already drives real Claude FO/ensign scenarios, calls `journeymetrics.ParseClaudeJSONL`, and `EmitRecord`s to `$SPACEDOCK_JOURNEY_METRICS_DIR/shared-scenarios` — so once Part 1 lands, `status_read_calls`/`scoped_read_calls` flow into those records with no new harness. Capture the **pre-trim baseline** by running the live ensign journey on `main` (the current 6-site instruction the captain says already shows adoption in boot-forensics output) and record the emitted `status_read_calls`/`scoped_read_calls`. After the trim, run the same live drive and assert the measured counts are **not lower** than the baseline. Baseline source is concrete and cited: the journeymetrics records emitted by the live ensigncycle harness, the same artifacts `cmd/spacedock-release` aggregates into the release ledger. This is behavioral (it reads the measured counts), not a prose-grep over instruction files.

### Per-journey ledger field decision (checklist item 1)
The exact fields that surface `--read` adoption over time:
- `Record.status_read_calls` (int, omitempty) — count of Bash tool calls whose command invokes `status --read`. This is the primary adoption signal: it directly counts the behavior the six contract sites are trying to induce.
- `Record.scoped_read_calls` (int, omitempty) — count of `Read` tool calls carrying `offset`/`lines`. This is the secondary/corroborating signal: the ensign-shared-core contract steers agents to `status --read` *in order to* then issue a scoped `Read`, so scoped-Read volume is the downstream evidence that the hint changed read behavior, not just that the flag was typed.

Both are surfaced; `status_read_calls` is the headline metric for the redundant-instruction-site trim decision.

## Why
Part 1 is the durable behavioral proof that the e6a adoption sticks. Part 2 is the payoff: with the metric as a standing monitor, the redundant per-dispatch hint can be trimmed with evidence rather than blind, recovering prompt bloat on every dispatch while proving the trimmed site was not load-bearing. Measure, then trim — as one unit, so the trim never ships without its monitor.

## Documentation impact
None. Part 1 adds two `omitempty` integer fields to the emitted journey-metric records and the release ledger JSON — an internal release artifact, not a documented user surface. Part 2 removes a line from the generated dispatch prompt; the dispatch prompt is generated by `build.go`, not a docs-site-described surface, and the goldens are the spec for it (regenerated, not hand-documented). No doc diff is owed for either part.

## Acceptance criteria

### Part 1 — metric
- **AC-1** — A journey record surfaces a count of `status --read` Bash invocations (`status_read_calls`) and a count of scoped `Read` calls carrying `offset`/`lines` (`scoped_read_calls`). Tested by a journeymetrics unit test over a fixture stream-json transcript that contains one `status --read` Bash `tool_use` and one scoped `Read` `tool_use`, asserting both counts are 1; and a perturbation fixture with a plain `spacedock status` Bash call and a full-file `Read` (no offset/lines), asserting both counts are 0. Non-tautological by perturbation: deleting the detection in `claude.go` makes the first fixture's assertion fail.
- **AC-2** — The `status --read` detector matches the launcher-agnostic command forms and rejects non-`--read` `status` calls. Tested by a table-driven unit test on `commandInvokesStatusRead`: positives include `spacedock status --read PATH`, `sd status --read PATH`, `${SPACEDOCK_BIN:-spacedock} status --read PATH`, and `spacedock status --json --read PATH` (flag-order independence); negatives include `spacedock status`, `spacedock status --boot`, `spacedock dispatch show-stage-def` (a non-status command), and `echo 'status --read'` is accepted-or-rejected per the chosen rule and asserted either way so the boundary is pinned.
- **AC-3** — The counts dedup across repeated stream deltas. Tested by a unit test feeding a multi-delta transcript that repeats the same `status --read` Bash `tool_use` block (same tool id) across two `assistant` rows — mirroring the real multi-delta shape already covered by `TestParseClaudeTurnsMergesToolUseAcrossDeltas` — asserting `status_read_calls == 1`, not 2.
- **AC-4** — The codex characterization surfaces `status_read_calls` from `tool_call.started` `arguments.cmd`. Tested by a unit test over a codex fixture containing a `tool_call.started` whose `arguments.cmd` invokes `status --read`, asserting the count is captured, and zero when the command is a plain `status`.

### Part 2 — trim
- **AC-5** — The site-6 dispatch-prompt re-read hint is removed from `build.go` and from every generated dispatch prompt. Verified behaviorally by the dispatch golden suite, not by grepping `build.go`: after the trim, `go test ./internal/dispatch -run TestBuild -update` regenerates the goldens and the only diff is the deletion of the "For later re-reads of the body during work…" line from the 13 goldens that carried it (the count is pinned in the report: 13 before, 0 after).
- **AC-6** — The dispatch golden suite is regenerated and green after the trim. Verified by `go test ./internal/dispatch/...` passing against the regenerated goldens (no `-update`), proving the committed goldens match native output post-trim.
- **AC-7** — Measured `status --read` adoption does not regress after the trim. Verified behaviorally via the Part-1 metric: a pre-trim baseline of `status_read_calls`/`scoped_read_calls` is captured from a live FO/ensign drive on `main` (baseline source: journeymetrics records emitted by the `//go:build live` `internal/ensigncycle/journey_metrics_live_test.go` harness to `$SPACEDOCK_JOURNEY_METRICS_DIR/shared-scenarios`), and the same live drive after the trim yields counts not lower than the baseline. The baseline value is recorded in the task body / validation report at capture time so the comparison is auditable. No prose-grep over instruction files.

## Test plan

### Part 1 — metric (fixture-driven Go unit tests)
- **What verifies it:** Go unit tests in `internal/journeymetrics` (`claude_test.go`, `codex_test.go`), driving the existing exported parsers `ParseClaudeJSONL` and `CharacterizeCodexExecJSONL` over new committed `testdata` fixtures, plus a table-driven test of the `commandInvokesStatusRead` helper. No new test framework; extends the established fixture-driven pattern.
- **Fixtures needed:** two new claude stream-json fixtures (one WITH a `status --read` Bash call + a scoped `Read`, one WITHOUT — plain `status` + full-file `Read`) and one codex fixture (or an extension of `codex-exec.jsonl`) with a `status --read` shell call. Fixtures are hand-authored in the proven format confirmed by the spike; the multi-delta dedup fixture follows the shape committed in `claude_test.go`.
- **Cost/complexity:** Low. Pure parser extension over in-repo fixtures; sub-second `go test ./internal/journeymetrics/...`. The mechanism (command string reachable in the transcript) is already proven by committed real captures, so a fixture exercise is sufficient proof of the parse behavior.

### Part 2 — trim (golden regen + adoption comparison)
- **What verifies it:** the dispatch golden suite (`go test ./internal/dispatch/...`) for the prompt-shape change, and the Part-1 metric for the adoption-not-regress claim.
- **Steps:** (1) On `main`, confirm the baseline: `go test ./internal/dispatch/...` green and 13 goldens carry the hint line. (2) Capture the pre-trim live adoption baseline via the live ensign journey harness (`go test -tags live ./internal/ensigncycle/...` with `SPACEDOCK_JOURNEY_METRICS_DIR` set), record `status_read_calls`/`scoped_read_calls`. (3) Apply the trim to `build.go`, regenerate goldens with `-update`, confirm the diff is only the hint-line deletion across the 13 files, re-run `go test ./internal/dispatch/...` green. (4) Re-run the live drive, assert measured counts not lower than the recorded baseline.
- **Cost/complexity:** Low for the golden regen (sub-15s, already exercised green at baseline). The live adoption comparison is the only live-cost step — gated on Part 1 being merged so the counts exist; it is a validation-stage activity, not a precondition for the parser or the golden change. If a same-session live drive is impractical at validation time, the captain's existing boot-forensics observation that adoption is already happening under the 6-site instruction is the documented prior; the metric makes the post-trim check a one-command comparison going forward.
- **Out of scope:** does not touch sites 4/5 (`ensign-shared-core.md:18`, `:92`) — only the redundant site-6 dispatch-prompt hint. Does not add a CLI command to display the counts — they live in the release ledger JSON read by `cmd/spacedock-release`.

## Stage Report: ideation

- DONE: Pin the detection design in `internal/journeymetrics` (claude.go + codex.go): detect `status --read` in the Bash `tool_use` command string AND/OR count scoped `Read` calls carrying `offset`/`lines`; decide the exact per-journey ledger field that surfaces `--read` adoption over time.
  Design recorded in `## Proposed approach` Part 1 and `### Per-journey ledger field decision`: extend `claudeContent` to decode `input` (raw), add `commandInvokesStatusRead` helper shared by both runtimes, surface `Record.status_read_calls` (primary) + `Record.scoped_read_calls` (corroborating) flowing through `BuildRecord` and preserved verbatim per-observation by `AggregateLedger` — over-time = the per-release ledger sequence, no new aggregate field.
- DONE: AC-1's journeymetrics unit test must be NON-TAUTOLOGICAL by perturbation: a fixture transcript WITH a `status --read` Bash call + a scoped `Read` asserts the count is captured; a fixture WITHOUT them asserts zero; removing the detection makes the test fail.
  AC-1 specifies exactly this WITH/WITHOUT fixture pair (counts 1/1 vs 0/0) plus the explicit perturbation clause; AC-2/AC-3/AC-4 pin the matcher boundary, delta-dedup, and codex mirror.
- DONE: Verify the riskiest assumption and record the spike result: confirm the Bash `tool_use` command STRING is actually reachable in the transcript journeymetrics parses.
  `## Spike result` records "no spike needed" with proof from committed real captures (`shallow-boot-greet.stream.jsonl` carries `spacedock status --boot --json` in `input.command`; codex `arguments.cmd` carries the shell command) — the string is on disk; the parser only discards it because `claudeContent` omits `input`. Additionally exercised the end-to-end detection in a throwaway Go program over real-capture-shaped lines: `status_read_calls=2, scoped_read_calls=1` as designed (then removed the throwaway).
- DONE: Fold `read-hint-adoption-bloat-trim` (4x) into this task as Part 2; absorb its trim design.
  Read the 4x entity; added `## Scope note` (this task supersedes 4x), Part-2 problem statement, Part-2 proposed approach (trim `build.go:552-556` site-6 hint, regen 13 goldens via `-update`, prove adoption-not-lower via the metric), and AC-5/AC-6/AC-7. Grounded the trim: located the exact site (`build.go:552-556`), confirmed exactly 13 goldens carry the hint line (`grep -l`), found the `-update` regen flag (`golden_harness_test.go:18`), and found the live monitor path (`journey_metrics_live_test.go` already EmitRecords from `ParseClaudeJSONL`), so AC-7's baseline source is concrete and the metric needs no new harness to serve as the before/after monitor.

### Summary

Expanded the ideation per captain decision to a single measure-then-trim unit absorbing `read-hint-adoption-bloat-trim`. Part 1 (unchanged) builds the journeymetrics `--read` detection: two per-journey `Record` fields (`status_read_calls` primary, `scoped_read_calls` corroborating) via a shared `commandInvokesStatusRead` helper, claude + codex, surfaced through the existing ledger; riskiest assumption proven (no spike needed) against committed real captures and an exercised detection run. Part 2 (folded in) trims the redundant site-6 dispatch-prompt hint (`build.go:552-556` — confirmed in exactly 13 goldens), regenerates goldens with `-update`, and proves adoption does not regress using Part 1's metric as the standing before/after monitor (baseline = records emitted by the existing `//go:build live` ensigncycle harness; the captain's boot-forensics observation that adoption already happens under the 6-site instruction de-risks it). Seven ACs: AC-1..4 (metric, non-tautological), AC-5..7 (trim: site removed/goldens regen+green/adoption-not-lower) — all behavioral, no prose-grep. Verified the dispatch + journeymetrics suites green at baseline before designing.

## AC-7 adoption baseline — recorded fallback (live comparison handed to validation)

A same-session live before/after drive was **not run at implementation**; the documented fallback is recorded here for audit, and the live comparison is handed to validation. Concrete rationale:

- **The "pre-trim baseline on `main`" the AC describes is unobtainable today.** `main` carries neither Part-1 field, so a live drive on `main` emits no `status_read_calls`/`scoped_read_calls` — it cannot produce a metric baseline. A meaningful pre-trim baseline requires Part-1's parser *with* the pre-trim prompt, which is this branch at `HEAD~1` (commit `2b49ea9d`, metric landed, trim not yet applied), not `main`.
- **A credible before/after needs two heavy live drives, which the entity assigns to validation.** Each `TestLiveClaudeSharedScenarios` scenario is "an independent multi-minute live claude journey" (the runner comment cites ~27m serially for four on opus); a pre-trim drive at `2b49ea9d` plus a post-trim drive at `233a231c` is two such runs. The entity's own Test plan (Part 2, Cost/complexity) states the live comparison "is a validation-stage activity, not a precondition for the parser or the golden change."
- **The trim is de-risked by the documented prior.** The trimmed site-6 hint is redundant with sites 4/5 (`ensign-shared-core.md:18`, `:92`), which the ensign loads via its `Skill` call every dispatch — those remain the contract for repeat-read behavior. The captain's boot-forensics observation that `status --read` adoption is already happening under the current 6-site instruction is the recorded prior.
- **Local live auth IS present** (`~/.claude/benchmark-token` non-empty), so the deferral is a stage-ownership decision, not a missing-credential block. The post-trim live comparison is mechanically a one-command run for validation now that the metric exists.

**Validation handoff (one-command before/after):** with `SPACEDOCK_JOURNEY_METRICS_DIR` set, run the `//go:build live` harness against pre-trim `2b49ea9d` to record the baseline `status_read_calls`/`scoped_read_calls`, then against post-trim `233a231c` (HEAD); assert the post-trim counts are **not lower** than the baseline. Baseline source: the journeymetrics records the harness EmitRecords to `$SPACEDOCK_JOURNEY_METRICS_DIR/shared-scenarios` (the same artifacts `cmd/spacedock-release` aggregates).

## Stage Report: implementation

- DONE: Part 1 (metric): implement exactly the design — `claudeContent` decodes the tool-use `input` (raw `json.RawMessage`), a shared `commandInvokesStatusRead` helper (launcher-agnostic; flag-order independent), `Record.status_read_calls` (Bash-arg) + `Record.scoped_read_calls` (Read with offset/lines) as `omitempty` ints flowing through `BuildRecord`, deduped on the existing `toolID` key, and the codex mirror on `CodexCharacterization` (Bash-arg only). Prove AC-1..AC-4 with the fixture/table unit tests, NON-tautological.
  Commit `2b49ea9d`. `internal/journeymetrics`: `readadoption.go` (helpers `commandInvokesStatusRead`/`readInputIsScoped`/`bashCommand`/`codexCommand`), `claude.go`/`codex.go`/`types.go`/`record.go` wiring. AC-1 fixtures `claude_read_adoption.stream.jsonl` (1/1) + `claude_no_read_adoption.stream.jsonl` (0/0); AC-2 table (positives spacedock/sd/${SPACEDOCK_BIN}/bare + `--json --read`; negatives plain/--boot/non-status/quoted-echo); AC-3 multi-delta dedup (==1); AC-4 codex from `arguments.cmd` (1 with-read, 0 plain). Non-tautological proven: forcing the status-read detector to `false` flips AC-1 to `status_read_calls=0` FAIL; restoring passes. Codex `status_read_calls` surfaces via the preserved `record.CodexCharacter` (no extra BuildRecord copy needed). Scoped-read detects the real Read fields `offset`/`limit` (the on-disk scoping fields; the spec's "offset/lines" prose names the same line-range concept).
- DONE: Part 2 (trim): delete ONLY the site-6 re-read hint at `build.go:552-556` plus its orphaned comment fragment; KEEP the whole-spec entity-read and sites 4/5. Regenerate the dispatch goldens; the ONLY diff must be that hint line's deletion across exactly the 13 goldens that carried it (13 → 0). Prove AC-5/AC-6 via the golden suite, behaviorally.
  Commit `233a231c`. Removed the `parts = append(...)` hint block and the hint-describing comment fragment; the now-unused `readPath` local was removed with it; the whole-spec entity-read (`Read the entity file at …`) stays; sites 4/5 untouched. `go test ./internal/dispatch -run TestBuild -update` → diff is exactly 13 goldens, 26 deletions, every removed line a blank separator or a "For later re-reads of the body during work…" variant, zero additions. Post-trim `grep -l` count: 0 (was 13). AC-6: `go test ./internal/dispatch/...` green without `-update`.
- DONE: AC-7 adoption-not-regress: capture a pre-trim baseline … RECORD it for audit; if a same-session live drive isn't feasible, record that explicitly + the documented fallback and hand the post-trim live comparison to validation. All deterministic suites green.
  Recorded the fallback above (`## AC-7 adoption baseline`): the "baseline on `main`" is unobtainable because `main` lacks the metric; a credible before/after is two multi-minute live drives the entity itself assigns to validation; the trim is de-risked by sites 4/5 + the captain's boot-forensics prior; local benchmark-token IS present so this is a stage-ownership deferral, not a credential block. Validation handoff is a one-command before/after (pre-trim `2b49ea9d` vs post-trim HEAD). Deterministic suites: `go test ./internal/contractlint ./internal/dispatch/... ./internal/journeymetrics/...` green; `go test ./...` green (exit 0, 15 pkg ok, 0 failures).

### Summary

Part 1 surfaces `status --read` adoption (`status_read_calls`) and scoped-`Read` volume (`scoped_read_calls`) on the journey `Record` by decoding the tool-use `input` the parser previously discarded — a launcher-agnostic shared helper, deduped on the existing `toolID` key, mirrored on the codex characterization (Bash-arg only). Part 2 trims the redundant site-6 per-dispatch re-read hint from `build.go` and regenerates exactly the 13 affected goldens (the only diff being that hint's deletion, 13 → 0), with sites 4/5 retained as the repeat-read contract. AC-1..AC-6 are proven behaviorally and non-tautologically by the unit and golden suites; AC-7's live before/after comparison is recorded as a documented fallback and handed to validation (a one-command pre/post drive), since the metric-bearing baseline cannot exist on `main` and the comparison is two heavy live drives the entity assigns to the validation stage. All deterministic suites green.

### Feedback Cycles

#### Cycle 1 — validation detached adversarial audit (throwaway checkout `/tmp/jm-audit-*`, never the implementation worktree)

Refuted little material; one minor metric over-count edge to note, plus one load-bearing AC-7 finding that reframes the live proof.

- **Detector false-positive surface (minor, non-blocking).** Table-probed `commandInvokesStatusRead` against 13 adversarial inputs. Correctly REJECTS the cases that matter: quoted echo, `grep "status --read"`, `sed 's/status --read/X/'`, git commit message, path-embedded `/var/status`, `--read=value` (single token, not `--read`), abs-path launcher `/usr/local/bin/spacedock`, `./sd`. Correctly ACCEPTS real invocations after `&&`/`;`/pipe and an env-prefixed `spacedock`. The one false-positive: a *commented* command `# spacedock status --read /x` matches (the tokenizer ignores a leading `#`). Materiality: low — an agent does not issue a standalone commented Bash tool call to "invoke" a read, and the metric over-counts (never under-counts safety). Not a trim risk. Recorded, not blocking.
- **`scoped_read_calls` keyed on the REAL Read fields.** Probed `readInputIsScoped`: true for `offset` only / `limit` only / both; false for full-file, explicit-zero `offset`/`limit`, a (non-existent) `lines` field, empty, and malformed input. The detector keys on `offset`/`limit` — the actual Read tool fields — not on the spec's prose word "lines". Correct.
- **Trim removed NOTHING beyond site-6.** `233a231c` touches exactly build.go + 13 goldens (zero other files); the build.go diff deletes only the site-6 `parts = append(...)` block, its rationale comment fragment, and the orphaned `readPath` local; the whole-spec entity-read (`Read the entity file at … for the full spec`) is retained in all 13 goldens (`grep -l` = 13); sites 4/5 (`ensign-shared-core.md`) and `agents/` are byte-identical to `main`. Re-running `-update` produces zero further diff (goldens match native output). No hidden prompt change in the regen.
- **AC-7 measurement mismatch (load-bearing — reframes the live proof, does NOT block the trim).** The credential is LIVE (a cheap one-shot `claude -p` spot-check via the benchmark-token returned `result/success`, not 401 — the implementer's stale-401 note is obsolete). BUT the live harness as wired cannot produce a meaningful before/after for THIS trim: `emitClaudeScenarioMetrics` parses `result.stream`, which is the **FO front-door** `spacedock claude -p` stream. The trimmed site-6 hint lives in the **dispatched ensign's** prompt and steers the **ensign's** repeat-reads — and the ensign runs as a separate team-agent session whose transcript is NOT captured or parsed. Empirically confirmed: parsing four real FO front-door captures (`sonnet_teamdelete_*`, `shallow-boot-greet`; 18-23 Bash calls + 3-4 Reads each) through the metric yields `status_read_calls=0`/`scoped_read_calls=0` in every one. So the prescribed live before/after would compare `0 == 0` regardless of the trim. The two ~27m drives would burn live budget measuring the wrong agent's reads. The trim is still safe on the redundancy argument; the *measured* monitor simply does not yet observe the agent it was built to watch.

## Stage Report: validation

- DONE: Reproduce AC-1..AC-6 in the worktree: `go test ./internal/journeymetrics/...` green and `go test ./internal/dispatch/...` green without `-update` (AC-6).
  Both green in the worktree (`internal/journeymetrics` ok; `internal/dispatch` ok, 13.6s, no `-update`); `go build ./...` clean.
- DONE: AC-1 the 1/1 WITH + 0/0 WITHOUT fixtures; AC-2 launcher-agnostic + flag-order matcher table with negatives; AC-3 multi-delta dedup ==1; AC-4 codex from `arguments.cmd`.
  Read both fixtures (WITH: real `status --read` Bash + scoped Read offset/limit; WITHOUT: plain status + full-file Read) and `readadoption_test.go`; the table pins positives (spacedock/sd/${SPACEDOCK_BIN}/bare + `--json --read`) and negatives (plain/--boot/non-status/quoted-echo); dedup and codex tests present and green.
- DONE: Confirm AC-1 is non-tautological (forcing the detector to false flips status_read_calls to 0 and FAILs).
  In the throwaway checkout, injected `return false` into `commandInvokesStatusRead` → AC-1 FAILs `status_read_calls = 0, want 1`; separately forced `readInputIsScoped` false → FAILs `scoped_read_calls = 0, want 1`; reverted → green. Both detection halves non-tautological.
- DONE: Confirm the AC-5 trim diff is EXACTLY the site-6 re-read hint removed across the 13 goldens (zero additions; whole-spec entity-read and sites 4/5 retained) — verified via the golden DIFF, not by grepping build.go.
  `233a231c` = build.go + exactly 13 goldens, 36 deletions / 0 additions; each golden loses only the hint line + its blank separator; build.go removes only the site-6 append block + comment + orphaned `readPath`; whole-spec read retained (`grep -l` = 13); sites 4/5 + `agents/` byte-identical to `main`; re-`-update` yields zero further diff.
- DONE: Run the DETACHED adversarial audit on a THROWAWAY checkout; try to REFUTE (detector false-pos/neg, scoped-read fields, trim over-removal). Record findings as a `### Feedback Cycles` entry.
  Throwaway worktree `/tmp/jm-audit-*` (removed after); findings recorded in `### Feedback Cycles` Cycle 1. Refuted nothing that blocks: one minor metric over-count (`#`-commented command), scoped-read correctly keyed on `offset`/`limit`, trim removed nothing beyond site-6.
- DONE (assessed, not measured): AC-7 adoption-not-regress — make an explicit honest PASS/REJECT call; flag if measured proof is needed before merge.
  PASS on safety, with a load-bearing reframe. Credential is live (one-shot spot-check returned success, not 401). The prescribed measured before/after is INVALID as wired: the metric parses the FO front-door stream while the trim targets the dispatched ensign's separate, uncaptured session — four real FO captures all register `status_read_calls=0`/`scoped_read_calls=0`, so the drive would compare 0==0. The trim is safe on the redundancy argument (sites 4/5 loaded by the ensign's Skill call every dispatch remain the repeat-read contract) + the captain's boot-forensics prior. Measured proof is NOT needed before merge, but the harness should later parse the ensign sub-agent transcript for the metric to actually monitor the trimmed surface.

### Summary

PASSED. AC-1..AC-6 reproduced behaviorally and non-tautologically: journeymetrics + dispatch suites green, AC-1 perturbation-proven (forcing either detector false FAILs), AC-5 trim verified via the golden diff (build.go + exactly 13 goldens, 0 additions, whole-spec read and sites 4/5 retained, regen reproducible). Detached adversarial audit on a throwaway checkout refuted nothing that blocks — one minor metric over-count edge (a `#`-commented `status --read` matches; over-count not safety risk) and the trim demonstrably removed nothing beyond site-6. AC-7 is a PASS on the trim's safety but a REFRAME of its proof: the live before/after the entity prescribes cannot move because the metric reads the FO front-door stream while the trim targets the dispatched ensign's separate uncaptured session (four real FO captures all measure 0/0). The trim is safe via sites 4/5 + the boot-forensics prior; measured proof is not needed before merge, but the metric does not yet observe the agent it was built to watch — recommend a follow-up to fold the ensign sub-agent transcript into the parsed metric so the standing monitor is real.
