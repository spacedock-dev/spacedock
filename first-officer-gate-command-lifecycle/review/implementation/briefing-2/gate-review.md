# Implementation correction gate: recorded lifecycle proof boundary

## Capability and reviewed change

The deferred lifecycle topology and deterministic gate journey are within their approved budgets, but the cycle-3 report remains 1 DONE and 2 FAILED because three live-proof mechanisms are impossible or disproportionate and one Pi harness is genuinely incomplete.

## Evidence

- Claude loaded the copied command-only mutant, reconstructed the required eligibility call, emitted all six events, consumed the approval, and dispatched. The behavior stayed correct; deleting explanatory prose as well would manufacture failure rather than isolate the command.
- The shipped route extractor and route-deletion controls cover every logical entry. Requiring every route to execute live on every host adds model cost without attacking a distinct host binding.
- Codex live produced correlated durable child output, but its public JSON stream exposes no spawn handle/result. Private rollout files are not a supported oracle.
- Pi returned a native async handle and completed later, but the harness graded and cleaned up before child completion.
- Claude's positive path reached real spawn/output; the last run failed only on the now-corrected reason overconstraint.
- The durable `8n` readiness projection is implemented and passing validation on PR #561.

## Recommendation and decision

Recommendation: **revise within implementation**. Keep every value AC, replace the command-only prompt mutant with copied-body provenance plus deterministic six-event deletion controls, use one representative live route per host plus source-derived route coverage, split Codex proof across live durable correlation and offline native handle parsing, fix Pi async waiting, rerun Claude, and require `8n` merged before validation.

Decision: authorize the bounded proof correction under the existing 650 behavioral-line and 59 live-line caps.
