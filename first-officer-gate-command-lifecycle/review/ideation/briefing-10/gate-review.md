# Ideation gate: rebuild the minimum recorded-gate v1 contract

Capability/change: Rebuild from accepted tip `13d70249`, preserving only the product lifecycle: bind and commit, present one semantic root review under conn, record exact authority, consume and commit, then route exactly once.

Test and evidence: Real CLI and Git tests own state/authority barriers, reviewer rejection and Captain mappings have deterministic controls, host-specific extractors own root/order/multiplicity, and existing live journeys own visible review plus durable outcome without common transport forensics.

Reviewed snapshot: state commit `54af3140`; prototype tip `3c535105` will be preserved under an explicit local archival ref; PR #565 remains at `13d70249`.

Findings:

- Material: none after independent staff repair.
- Deferred: preserve the distinct initial-stage drive-to-gate test; inspect semantic extractors at the deterministic checkpoint; Pi failure diagnostics remain intentionally ordinary.
- Polish: treat `+250/-489` as a planning target within aggregate ranges, not a measured midpoint; baseline byte sizes were one byte low.

Recommendation: approve deterministic clean-branch implementation. Live host work remains blocked on a second staff review of the actual code.

Decision ask: approve to archive the prototype ref, create a new local branch at `13d70249` without reset/force, and implement within the 17-path obligation ranges; revise to change the retained product obligations; hold for a named prerequisite.
