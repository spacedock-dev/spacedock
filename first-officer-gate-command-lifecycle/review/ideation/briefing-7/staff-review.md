# Independent staff review: cycle 20 repair

Verdict: **APPROVE**

## Material

None. The duplicate-root finding is closed: AC-6 preserves multiplicity, the test plan requires a count-two failure, extractors may not silently return the last match, and the obligation table carries the same exact-one proof burden.

## Deferred risk

The explicit duplicate fixture is Codex-shaped while Claude and Pi use separate extractors. Implementation must honor the plural requirement; duplicate-root arms in their existing focused tests are appropriate if extraction is not shared. The durable transcript excerpt is sufficient to audit the mechanism even if the temporary JSONL disappears.

## Polish

The revised eight-file arithmetic is correct: `+65/-31` incremental and `+160/-45` intended total. The `+175` additions hard stop leaves 15 lines of tolerance and introduces no ninth file or new mechanism. Earlier cycle-20 figures are superseded by the appended repair report.
