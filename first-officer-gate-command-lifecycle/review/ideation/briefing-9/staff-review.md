# Independent staff review: cycle 25

Verdict: **APPROVE**

## Material

None. The design restores Cycle 9’s host-specific transport split, treats Codex wait as ordering evidence only, removes assignment SHA from proof, requires persistent Pi retention before launch, corrects deletion arithmetic, and adds lossless JSON edge cases.

## Deferred risk

- Resolve destination symlinks before containment checks and cover a symlinked cleanup-owned path.
- Name the common field as dispatch observation, not completion.
- Git ancestry proves timing and durability on Codex, not exact-child authorship; native fixtures own transport.

## Surface judgment

Seven existing repair files plus the admitted Pi owner are sufficient. The unchanged Codex evidence parser remains a proof dependency, not a ninth repair file. The deletion checkpoint is cumulative `≤370`; final cumulative additions target `≤455`, tolerate `≤465`, and hard-stop at `+480`.
