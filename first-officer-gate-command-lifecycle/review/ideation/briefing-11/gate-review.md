# Ideation gate: amend one proof-file range

Capability/change: Widen only `internal/ensigncycle/recorded_gate_lifecycle_test.go` from `+70..120` to `+70..135` additions; keep its deletion range and every other design boundary unchanged.

Test and evidence: Independent review inspected the dirty correction and found the extra 15 lines irreducible direct proof for six public-CLI/Git Captain mappings, production parsing, UTF-8 directives, and durable route outcomes.

Reviewed snapshot: state commit `1b2abc25`; clean implementation base `72302472`; dirty correction remains uncommitted and no live host has run.

Findings:

- Material: none for the conditional range amendment.
- Condition: remove the redundant per-case bind reparse and measure exactly `+135/-152` before commit.
- Deferred: live hosts remain blocked on the same staff reviewer’s final deterministic approval.

Recommendation: approve the one-file amendment only.

Decision ask: approve to resume implementation under the exact `+135/-152` condition; revise to change the proof; hold for a named prerequisite.
