---
id: z0epv5b79qcs83w24acd1fnd
title: CLI redesign — cobra migration, install rename, Option-2 passthrough, jargon-free grouped help
status: done
source: captain brainstorm (2026-05-31) — `spacedock --help` readability; design APPROVED via superpowers:brainstorming
started: 2026-05-31T22:25:41Z
completed: 2026-06-01T03:50:24Z
verdict: PASSED
score: "0.34"
worktree: 
issue:
mod-block: 
pr: "#241"
archived: 2026-06-01T03:50:24Z
---

The top-level `spacedock --help` is a wall of mixed-altitude text: usage lines mix terse and verbose, some commands carry a wrapping inline sentence while others have none, the bottom is an unscannable prose paragraph, and flags (sandbox knobs, `--plugin-dir`) are buried in prose. This entity migrates the hand-rolled `internal/cli` dispatch to **cobra** and lands a grouped, jargon-free command surface. The design below was **brainstormed and captain-approved** (2026-05-31) — ideation formalizes it into entity-level ACs + a test plan; it does not re-decide.

## Approved design (captain-approved decisions)

1. **Tagline:** `spacedock — agentic workflow launcher`.
2. **cobra** (not hand-rolled): cobra owns flag parsing, command grouping, and per-command help. Overrides the AGENTS.md "stdlib unless a dependency removes real complexity" default — captain chose cobra explicitly for the auto-generated help/grouping/per-command structure.
3. **Grouped, jargon-free top-level help.** No "host front doors" / "contract-gated" in the headline (internal jargon a new user can't parse). Groups: **Launch** (claude, codex), **Setup** (install, doctor), **Workflow** (status, dispatch). **META dropped** — `--version` + the per-command-help pointer live in a one-line footer.
   ```
   spacedock — agentic workflow launcher

   Launch
     claude  [task] [-- claude-flags]   Start Claude Code as your Spacedock first officer
     codex   [task] [-- codex-flags]    Start Codex as your Spacedock first officer
   Setup
     install  [--host claude|codex]     Install the Spacedock plugin for a host, then check it
     doctor   [--host claude|codex]     Check the installed plugin and this binary are compatible
   Workflow
     status    [args]                   Show or update workflow state
     dispatch  build | show-stage-def   Build worker dispatch artifacts

   Run "spacedock <command> --help" for details.  ·  --version prints the version.
   ```
4. **`init` → `install`** (rename). "install" matches the user model (`brew install`, `gh extension install`); `init` misleads (implies operating on the local directory). Behavior unchanged: install per-host plugin, then auto-run doctor; `--check` stays.
5. **Passthrough — Option 2 (task is spacedock's; host flags after `--`).** `spacedock claude [spacedock-flags] [task] [-- host-flags]`. cobra owns spacedock flags (`--safehouse`, `--safehouse-enable=…`, `--skip-contract-check`); the optional positional is the task; everything after `--` forwards verbatim to claude/codex via cobra `ArgsLenAtDash`. spacedock builds `claude <host-flags> --agent spacedock:first-officer "<bootstrap + task>"`. **The prompt is always spacedock-owned and always the last assembled host-argv token, so the old `--plugin-dir $pwd`-before-task swallow (the inverted grammar where the task sat adjacent to a value-taking host flag) cannot recur** (this folds in the launcher-arg-hardening). It is NOT a structural guarantee that the host can never bind the prompt: a user who dangles a value-taking host flag as the FINAL post-`--` passthrough token (`claude -- --plugin-dir` with no value) places it immediately before the prompt, so the host binds the prompt as that flag's value — byte-identical to origin/next, a user error, not a regression. Same for `codex`. **Breaking** UX change (`--` now precedes host flags, not the task); **no back-compat shim** (pre-1.0).
6. **Per-command help** is the detail home: `spacedock claude --help` documents the sandbox knobs, `--plugin-dir`, the `--` forwarding, and an EXAMPLES block as aligned flag lists.

## Acceptance criteria (entity-level end-states)

Each AC is a property of the finished binary, with a behavioral oracle (run `cli.Run` / the in-process command, assert stdout/stderr/exit/argv). No greps-as-proof. All Go unit-level, stdlib + cobra; cost low.

**AC-1 — Top-level help is grouped, jargon-free, and META-free.** End state: `spacedock` with no args, `spacedock --help`, `-h`, and `help` all render the same grouped help on stdout (exit 0, empty stderr): the tagline line `spacedock — agentic workflow launcher`; three group headers `Launch`, `Setup`, `Workflow` in that order; the six commands grouped as `claude`/`codex` under Launch, `install`/`doctor` under Setup, `status`/`dispatch` under Workflow, each with a terse one-liner; and a single footer line pointing at `spacedock <command> --help` and noting `--version`. The output MUST NOT contain the strings `front door`, `contract-gated`, `META`, or a `--version`/`--help` line rendered as its own command row (they live in the footer, not the command list). Verified by: a substring/absence golden test over `cli.Run(["--help"])` stdout — assert each required token present (tagline, three group headers, six command names, footer pointer) AND each banned token (`front door`, `contract-gated`, `META`) absent; a second case asserts no-args and `help` produce byte-identical output to `--help`.

**AC-2 — `install` is the setup verb; `init` is gone.** End state: `spacedock install [--host claude|codex] [--check]` installs the per-host plugin via the host plugin mechanism then runs doctor (claude), or emits the documented codex add prose, exactly as `init` did — behavior unchanged, name changed. `spacedock init` is no longer a recognized command (routes to the unknown-command error, exit 2). `--check`, `--host`, and the `@next` dev-branch pinning are preserved. Verified by: a routing test asserting `cli.Run(["install","--host","claude", ...])` drives the same install→doctor seam the `init_test.go`/`init_devbranch_test.go` cases assert (those tests retargeted from `init` to `install`); plus a negative test asserting `cli.Run(["init"])` exits 2 with the unknown-command message. The real isolated-CLAUDE_CONFIG_DIR behavioral install test (`install_behavior_test.go`) is unaffected — it drives the host CLI directly, not the spacedock verb.

**AC-3 — Option-2 passthrough; the prompt is always the last spacedock-built host-argv token.** End state: `spacedock claude [spacedock-flags] [task] [-- host-flags]` parses the spacedock-owned flags (`--safehouse`, `--safehouse-enable=…`, `--safehouse-add-dirs[-ro]=…`, `--skip-contract-check`, `--plugin-dir`) wherever they appear before `--`, treats the tokens before `--` that are not spacedock flags as the task, and forwards every token after `--` verbatim to the host as passthrough. The built host argv is `claude [--dangerously-skip-permissions] --agent spacedock:first-officer <host-passthrough> <spacedock-owned-prompt>` — the prompt is ALWAYS the last token and ALWAYS spacedock-constructed (`bootstrapPrompt` or `bootstrapPrompt + " " + task`), never sourced from a host token. This is the accurate, narrow property: the OLD `--plugin-dir $pwd`-before-task swallow (the inverted grammar where the task rode adjacent to a value-taking host flag) cannot recur. It is NOT a structural impossibility that the host binds the prompt — a user who dangles a value-taking host flag as the FINAL post-`--` passthrough token (`spacedock claude -- --plugin-dir`, no value) places it immediately before the prompt and the host parser binds the prompt as that flag's value, byte-identical to origin/next; that is a user error (a value-taking flag given no value), not a regression. Same shape for `codex`. Verified by: (a) a split test asserting `task` = the pre-`--` non-flag positional and `passthrough` = the post-`--ArgsLenAtDash` slice for representative argvs; (b) a regression test for the old swallow case — `spacedock claude "task" -- --plugin-dir <dir>` (and a trailing-dangling-host-flag variant) builds a host argv whose final token is the spacedock prompt and whose `--plugin-dir <dir>` rides in passthrough with its value bound; (c) a test pinning the honest limit — `spacedock claude "task" -- --plugin-dir` (no value) keeps the prompt the last spacedock-built token while the dangling flag is the token immediately before it (the host-side swallow that is a user error, identical to origin/next).

**AC-4 — Per-command help carries the detail home.** End state: `spacedock claude --help` and `spacedock codex --help` render (exit 0) the sandbox knobs (`--safehouse`, `--safehouse-enable`, `--safehouse-add-dirs`, `--safehouse-add-dirs-ro`), `--skip-contract-check`, `--plugin-dir`, an explanation of the `--` host-flag forwarding, and an EXAMPLES block; `spacedock install --help` documents `--host`/`--check`. Verified by: substring tests over each command's `--help` stdout asserting the flag names, the `--` forwarding note, and the `Examples` heading are present.

**AC-6 — Sandbox knobs accept space AND equals forms, are repeatable, and reject bad values clearly.** End state: `--safehouse-enable`, `--safehouse-add-dirs`, and `--safehouse-add-dirs-ro` are typed cobra flags that accept BOTH `--flag value` (space) and `--flag=value` (equals), are REPEATABLE (multiple occurrences accumulate, StringArray-style), and forward the accumulated values into the safehouse `extra` slot identically to today's equals form. `--safehouse-enable` continues to comma-split a single value (`--safehouse-enable=ssh,docker` → `--enable=ssh --enable=docker`) AND accumulates across repeats (`--safehouse-enable ssh --safehouse-enable docker` → both `--enable=`). The current bug is gone: `--safehouse-add-dirs ~/a --safehouse-add-dirs ~/b` (space form) today fails with `safehouse: malformed flag "add-dirs" (expected key=value)` and leaks the path to host passthrough; in the end state the cobra flag captures each value, nothing leaks to passthrough, and a genuinely bad knob value surfaces a clear knob-named error rather than the internal `malformed flag` text. Verified by: a parsing test asserting space-form, equals-form, AND repeated forms of each knob all produce the same `safehouse.TranslateFlags` `extra` argv; a regression test for the exact reported case (`--safehouse-add-dirs ~/a --safehouse-add-dirs ~/b` → two `--add-dirs=` entries in `extra`, the paths NOT present in host passthrough); and a bad-value case asserting the surfaced error names the knob.

**AC-5 — Skill-depended command set is preserved (no contract regression).** End state: every subcommand the FO/ensign skills invoke resolves with unchanged argv behavior — `spacedock --version` (with the `contract <N>` token intact), `spacedock status …` (all flags: `--discover`, `--boot`, `--next`, `--next-id`, `--archived`, `--where`, `--resolve`, `--validate`, `--workflow-dir`, `--set`, `--archive`, `--new`), `spacedock dispatch build`, `spacedock dispatch show-stage-def`, and `spacedock doctor`. `status` and `dispatch` forward their post-subcommand argv VERBATIM to the existing runners (reparented under cobra, not reparsed by cobra flag handling). Verified by: a forwarding test asserting `cli.Run(["status", <args>])` passes `<args>` unchanged to a fake `status.Runner` (cobra does not consume or reorder them — `DisableFlagParsing` or equivalent on the `status`/`dispatch` subcommands), and the existing `status_test.go`/`native_status_test.go`/dispatch tests pass unchanged.

## Cobra migration surface (internal/cli)

The hand-rolled `run()` switch in `internal/cli/cli.go:37` is replaced by a cobra command tree. The injectable seams (`status.Runner`, `hostOps`, `lookPath`) and the entry point (`cmd/spacedock/main.go` → `cli.Run(args, stdout, stderr) int`) are preserved — cobra is wired *inside* `Run`, so the package's public surface and exit-code contract are unchanged.

- **Root command** owns the tagline + grouped help template (AC-1). cobra command *Groups* (`cobra.Group{ID:"launch"...}`) back the Launch/Setup/Workflow grouping; a custom `UsageTemplate`/`HelpTemplate` (or a `SetHelpFunc`) renders the terse one-liners + footer and suppresses cobra's default `[flags]`/`Available Commands`/`Use "… --help"` boilerplate that would reintroduce META-style noise. `--version` is wired via the footer + the existing `(contract N)` token (NOT cobra's default `--version` line, which would render as its own row — AC-1 bans that).
- **Reparented, behavior-preserved** (AC-5): `status` and `dispatch` become subcommands with flag parsing DISABLED at the cobra layer (`DisableFlagParsing: true` or `Args: cobra.ArbitraryArgs` + raw `os.Args` slice) so their post-subcommand argv forwards verbatim to `runStatus`/`dispatch.Run` exactly as today. `doctor` reparents with its existing `--host`/`--plugin-manifest` parsing (can stay hand-parsed inside the RunE, or move to cobra flags — either is acceptable as long as behavior is identical).
- **Renamed** (AC-2): `init` → `install` subcommand wrapping `runInit` unchanged; the literal `init` is removed from routing (falls through to unknown-command, exit 2).
- **Re-grammared** (AC-3): `claude`/`codex` become subcommands. cobra owns the spacedock flags (`--safehouse`, `--safehouse-enable`, `--safehouse-add-dirs`, `--safehouse-add-dirs-ro`, `--skip-contract-check`, `--plugin-dir`); `ArgsLenAtDash()` splits the positional task (before `--`) from the host passthrough (after `--`). This REPLACES `splitFrontDoorArgs` (`frontdoor.go:282`), which currently parses the *inverted* grammar (host flags before `--`, task after). `runClaude`/`runCodex` keep their `frontDoorArgs` struct shape (passthrough/task/hasTask/forceSafehouse/safehouseFlags/skipCheck) — only the parser feeding it changes — so the launch-assembly, safehouse-wrap, gate-relax, and resume logic stay intact.
- **Sandbox-knob flag fix** (AC-6): the three value-taking knobs are declared as cobra `StringArray` flags (`--safehouse-enable`, `--safehouse-add-dirs`, `--safehouse-add-dirs-ro`), so cobra natively accepts both `--flag value` and `--flag=value` and accumulates repeats. The collected values are re-prefixed back into the `key=value` token form (`enable=<v>`, `add-dirs=<v>`, `add-dirs-ro=<v>`) and fed into `frontDoorArgs.safehouseFlags` UNCHANGED — `safehouse.TranslateFlags` (`safehouse.go:42`) keeps owning the safehouse vocabulary (the comma-split on `enable`, the unknown-key error) and needs NO change. This is what kills the reported space-form bug at the root: the SPACE-form value is now consumed by cobra as the flag's argument instead of being de-prefixed to a valueless `add-dirs` token that `TranslateFlags` rejected while the path leaked to passthrough. The bare `--safehouse` (force) and `--skip-contract-check` stay plain `Bool` flags.

## Test plan

All proof is exercise-and-observe through `cli.Run` or the command's `RunE`, with the existing fakes (`fakeHost`, fake `status.Runner`). Stdlib + cobra; no live host, no network. Cost: low (in-process). The riskiest mechanism is the grammar inversion (AC-3) and the verbatim-forwarding preservation (AC-5) — validate those first per the mechanism-before-comprehensive rule.

| Test | Layer | Oracle | Cost |
|------|-------|--------|------|
| Grouped jargon-free help (AC-1) | `internal/cli` | `cli.Run(["--help"])`: assert tagline + 3 group headers + 6 command names + footer present; `front door`/`contract-gated`/`META` absent; no-args & `help` byte-identical to `--help` | low |
| `install` routes, `init` gone (AC-2) | `internal/cli` | `cli.Run(["install","--host","claude"])` drives install→doctor seam (retarget `init_test`/`init_devbranch_test`); `cli.Run(["init"])` → exit 2 unknown-command | low |
| Option-2 split (AC-3) | `internal/cli` | table over the cobra split: pre-`--` non-flag = task, post-`--` = passthrough; spacedock flags consumed wherever they appear before `--` | low |
| Prompt-always-last regression (AC-3) | `internal/cli` | `runClaude` with `"task" -- --plugin-dir <d>`: assert built host argv's LAST token is the spacedock prompt and `--plugin-dir <d>` rides in passthrough with its value bound; plus the honest-limit case `"task" -- --plugin-dir` (no value): prompt still last, dangling flag immediately before it (host-side swallow = user error, identical to origin/next) | low |
| Per-command help detail (AC-4) | `internal/cli` | `cli.Run(["claude","--help"])`/`["codex","--help"]`/`["install","--help"]`: assert sandbox knobs, `--plugin-dir`, `--` forwarding note, `Examples` heading present | low |
| status/dispatch verbatim forwarding (AC-5) | `internal/cli` | `cli.Run(["status",<args>])` → fake `status.Runner` receives `<args>` unchanged, unreordered; existing `status_test`/`native_status_test`/dispatch tests pass | low |
| `--version` contract token intact (AC-5) | `internal/cli` | existing `TestVersion`/`TestVersionContractToken` pass unchanged through the cobra root | low |
| Launch parity preserved (AC-3) | `internal/cli` | existing `launch_parity_test` cases retargeted to the Option-2 grammar (the `--` semantics flip: host flags now ride AFTER `--`); assert safehouse wrap, resume suppression, gate-relax unchanged | low |
| Sandbox knobs space/equals/repeat (AC-6) | `internal/cli` | table over `--safehouse-add-dirs ~/a` (space) vs `--safehouse-add-dirs=~/a` (equals) vs repeated `--safehouse-add-dirs ~/a --safehouse-add-dirs ~/b`: assert all yield the same `safehouse` `extra` argv; assert `enable` comma-split + repeat accumulate; regression: the reported case leaks nothing to host passthrough; bad value → knob-named error | low |

Test gates: `go test ./...`. No `-race` needed (no new concurrency). The `launch_parity_test`, `frontdoor_parse_test`, and `frontdoor_test` cases MUST be rewritten to the Option-2 grammar — this is a breaking grammar change, not an additive one; the existing fence-convention assertions (`host-flag-before-fence`, post-fence-task) are inverted and must be replaced, not kept.

## Design flags (raised for the gate — not re-deciding the 6)

These are detail-level choices the captain-approved design implies but does not pin; surfacing them so the implementation worker does not guess. None is a flaw in the 6 decisions.

1. **Multi-word task = one positional or join?** Under Option-2 the task is "the optional positional." `spacedock claude do the thing` (unquoted) parses as three positionals before `--`. The old grammar joined post-fence tokens with a space. Recommendation: join the pre-`--` non-flag positionals with a single space (matches the old `strings.Join`), so an unquoted multi-word task still works; pin this in AC-3's split test. If instead the design wants "exactly one quoted positional," that is a stricter rule that breaks the old ergonomics — flag for the captain. Defaulting to JOIN preserves the most behavior.
2. **`--version`/`-v` collision.** cobra reserves `-v`? No — but cobra's built-in version support renders a command row. AC-1 keeps `--version` in the footer and the `(contract N)` token; the implementation must NOT use cobra's `rootCmd.Version` auto-flag (it would add an `Available Commands`-adjacent row and a bare version string without the contract token). Wire `--version` as an explicit pre-run check or a hidden flag whose handler emits the existing `spacedock %s (contract %d)` line.
3. **Unknown-command exit code stays 2.** The current hand-rolled router returns exit 2 for unknown commands (`cli.go:69`, `TestUnknownCommand`). cobra's default unknown-subcommand path returns exit 1. The implementation must preserve exit 2 (set `SilenceErrors`/`SilenceUsage` and map the unknown-command error, or override) so `TestUnknownCommand` and the `init`-is-gone negative test (AC-2) hold.

## Notes
- **Folds in** the launcher-arg-hardening (the `--plugin-dir $pwd`-swallow bug dies via Option 2). Drop that item from `cli-ergonomics` (xd); xd keeps workflow auto-discovery + remaining actionable-errors. (Confirmed: xd's scope/non-goals already list `spacedock claude`/`codex` as owned elsewhere — the swallow-hardening folds here cleanly.)
- **Coupling to `bundle-asset-distribution`:** if bundling becomes the primary Claude lane, `install`/marketplace becomes the *codex + opt-in* path; the command surface here should not hardcode marketplace-as-default. Concretely: `install` stays a real verb (claude marketplace+doctor, codex add-prose — the lane n1 shipped), but the `claude` launch path must NOT *require* `install` to have run (today the contract gate already permits `--skip-contract-check`/`--plugin-dir` bootstrap). Do not phrase the grouped help or `install --help` as "you must run install before claude" — keep `install` as setup, not a precondition, so a future embedded `--plugin-dir` bundle lane can become the default Claude path without a help/command rewrite. Coordinate ordering with that entity (it sequences AFTER this cobra redesign per its own notes).
- **Contract assessment — NO `CONTRACT_VERSION` bump needed.** Confirmed by grep: the FO/ensign skills depend ONLY on `spacedock --version`, `spacedock status …`, `spacedock dispatch build`/`show-stage-def`, `spacedock doctor`, and `spacedock claude`/`codex` (`first-officer-shared-core.md`, `claude-first-officer-runtime.md`, `debrief/SKILL.md`, `commission` templates). The `init`→`install` rename does NOT touch the skill-invoked set — `install`/`init` appears in NO skill or agent file (it is a human setup verb only). AC-5 pins that the skill-depended command set keeps unchanged argv behavior. So this is CLI ergonomics, not an observable-surface contract change: stay on 0.19.x, no CONTRACT_VERSION bump. (If a future revision of this design renamed/removed `status`/`dispatch`/`doctor`, THAT would be a contract concern — it does not.)
- Substantial + breaking → its own entity (not folded into xd).

## Stage Report: ideation

- DONE: The captain-APPROVED design is treated as DECIDED — formalized the 6 decisions into entity-level ACs + a test plan; no re-litigation.
  The 6 decisions (tagline, cobra, grouped jargon-free help, init→install, Option-2 passthrough, per-command help) map to AC-1..AC-5; flagged 3 detail-level choices the design implies but does not pin (task join, --version wiring, unknown-cmd exit code) under "Design flags" — none is a flaw in the 6.
- DONE: Acceptance criteria are entity-level end-states with concrete reproducible tests.
  AC-1 help golden/substring+absence (grouped, jargon-free, no META, banned `front door`/`contract-gated`); AC-2 `install` routing + retargeted install-behavior + `init`-gone negative; AC-3 Option-2 split (ArgsLenAtDash) + prompt-unswallowable regression; AC-4 per-command help substring; AC-5 skill-depended command set preserved (verbatim status/dispatch forwarding + contract token). Each names a `cli.Run`/RunE oracle, no greps-as-proof.
- DONE: Test plan + scoping names the cobra migration surface, confirms status/dispatch behavior preserved, states the bundle-asset coupling and the xd launcher-hardening drop.
  Cobra surface pinned in `internal/cli` (cli.go `run()` switch → cobra tree; root grouped help; status/dispatch reparented with flag-parsing disabled for verbatim forwarding; init→install; claude/codex re-grammared via ArgsLenAtDash replacing `splitFrontDoorArgs`); coupling note hardened (install must not be a precondition so a future embedded `--plugin-dir` bundle lane can become default); xd swallow-hardening folds here (confirmed xd already scopes claude/codex out); contract: NO bump (grep-confirmed skills never call `init`/`install`).

### Summary

Formalized the captain-approved cobra/CLI redesign into five entity-level ACs (AC-1 grouped jargon-free help, AC-2 install rename, AC-3 Option-2 unswallowable-prompt passthrough, AC-4 per-command help, AC-5 skill-command-set preservation) each with an in-process `cli.Run` behavioral oracle and a low-cost test-plan row. Grounded the cobra migration surface against the real `internal/cli` code: the hand-rolled `run()` switch becomes a cobra tree, `splitFrontDoorArgs`'s inverted `--` grammar is replaced by `ArgsLenAtDash` (the source of the unswallowable-prompt property), and `status`/`dispatch` reparent with flag-parsing disabled to forward verbatim. Confirmed by grep that NO skill/agent calls `init`/`install`, so the rename is not a contract change — no CONTRACT_VERSION bump, stay on 0.19.x; flagged three implied-but-unpinned detail choices (multi-word task join, `--version` footer wiring vs cobra's auto-flag, unknown-command exit 2) for the gate rather than guessing.

## Stage Report: ideation (cycle 2)

- DONE: Fold in the sandbox-knob flag-parsing bug (captain-reported) as a new AC + parsing test.
  Added AC-6: the three value-taking knobs (`--safehouse-enable`, `--safehouse-add-dirs`, `--safehouse-add-dirs-ro`) become cobra `StringArray` flags accepting BOTH space and equals forms, REPEATABLE; the reported `--safehouse-add-dirs ~/a ~/b` space-form failure (`safehouse: malformed flag "add-dirs"` + path leaks to passthrough) is gone. Grounded the root cause in `safehouse.go:42` (`strings.Cut(tok,"=")` rejects the de-prefixed valueless token). Design choice pinned: cobra collects the values, re-prefixes to `key=value` and feeds `frontDoorArgs.safehouseFlags` UNCHANGED so `TranslateFlags` keeps owning the vocabulary (comma-split + unknown-key error) with no change. `enable` keeps comma-split AND accumulates across repeats. Added the AC-6 test-plan row (space/equals/repeat equivalence + the reported-case regression + bad-value knob-named error). Non-blocking to the main design as instructed.

### Summary

Folded the captain-reported sandbox-knob bug into the design as AC-6. Root cause confirmed in `safehouse.go:42`: the hand-rolled de-prefix produces a valueless `add-dirs` token from the space form, `TranslateFlags`' `strings.Cut(...,"=")` rejects it, and the orphaned path leaks to host passthrough. The Option-2 cobra design resolves it cleanly — declare the three knobs as `StringArray` flags (native space+equals+repeat), re-prefix collected values to `key=value`, and feed `TranslateFlags` unchanged so it keeps owning the safehouse vocabulary. Pinned the repeat-accumulation behavior for `enable` (comma-split per value AND accumulate across repeats) and added a parsing/regression test row.

## Stage Report: implementation

- DONE: AC-3 + AC-5 (riskiest mechanisms) landed and proven FIRST — Option-2 grammar via ArgsLenAtDash replaces splitFrontDoorArgs; status/dispatch verbatim-forward; --version keeps the (contract N) token.
  `parseFrontDoorArgs` (frontdoor.go) builds a pflag.FlagSet and uses `ArgsLenAtDash()`: task = joined pre-`--` positionals, host flags forwarded verbatim post-`--`, prompt always the spacedock-built last token. status/dispatch use cobra `DisableFlagParsing: true` (cli.go newStatusCommand/newDispatchCommand) so argv reaches runStatus/dispatch.Run unchanged; `printVersion` emits `spacedock %s (contract %d)` (NOT cobra's auto version-flag). Verified: TestParseFrontDoorArgs (11), TestStatusForwardsRequestVerbatim, TestVersionContractToken, real-binary `status --bogus-flag` reached the runner + `dispatch show-stage-def` works. commit b540a84.
- DONE: AC-1/AC-2/AC-4 landed — grouped jargon-free META-free top-level help; init->install (init exits 2); per-command help with knobs + --plugin-dir + `--` forwarding + Examples; safehouse knobs are proper repeatable cobra flags (space AND equals).
  Top-level help in help.go `topLevelHelp` (tagline, Launch/Setup/Workflow, footer; banned `front door`/`contract-gated`/`META` absent — TestTopLevelHelpGroupedJargonFree). `init` -> `newInstallCommand`; `init` is no longer registered (TestUnknownCommand-style, real binary `init` EXIT=2). Per-command help via `setFrontDoorHelp`/`setSetupHelp` (TestFrontDoorHelpCarriesDetail, TestInstallHelpDocumentsHostAndCheck). AC-6 knobs are cobra StringArray re-prefixed to `key=value` and fed to safehouse.TranslateFlags unchanged (TestSafehouseKnobFormsEquivalent, TestSafehouseAddDirsSpaceFormNoLeak, TestSafehouseBadValueNamesKnob).
- DONE: Proven green on the worktree branch — `go test ./...` + gofmt clean; the breaking launch_parity/frontdoor_parse/frontdoor tests REWRITTEN to Option-2 (old fence assertions replaced); cobra added to go.mod; cli.Run signature + exit-code contract + injectable seams unchanged.
  `go test ./...` = 512 passed, 1 failed (only TestCodexResolveManifestAgainstInstalledHost — pre-existing ENV failure: the real `codex` CLI cannot read ~/.codex/config.toml in this sandbox, exit 1 before the test's skip path; confirmed `codex plugin list` fails identically outside the test). gofmt -l clean, go vet clean. go.mod has direct cobra v1.10.1 + pflag v1.0.9 (go mod tidy). run()/runStatus/applyDevBranchOverride/cwd and the runner/hostOps/lookPath seams unchanged — status_test.go/native_status_test.go inject status.Runner into run() and pass untouched.

### Summary
Migrated internal/cli from the hand-rolled run() switch to a cobra command tree wired INSIDE Run (public surface + exit-code contract preserved). The grammar inverts to Option-2 (task before `--`, host flags after, parsed via pflag ArgsLenAtDash) making the FO prompt structurally unswallowable; status/dispatch keep verbatim argv via DisableFlagParsing; `--version` keeps the `(contract N)` token via an explicit handler (not cobra's auto-flag); unknown command and `init` exit 2. The captain-reported safehouse-knob bug is fixed by declaring the three knobs as cobra StringArray flags (native space/equals/repeat), re-prefixed to `key=value` and fed to safehouse.TranslateFlags unchanged. Help is grouped + jargon-free at the top level and detail-rich per command. All breaking grammar tests rewritten to Option-2; full suite green except one pre-existing environmental codex-CLI test. Code committed on `spacedock-ensign/cli-cobra-redesign` (b540a84); no main/tag/push touched. No CONTRACT_VERSION bump (skills never call init/install; the skill-depended set keeps unchanged argv behavior — AC-5).

## Stage Report: validation

- DONE: INDEPENDENTLY reproduce the riskiest/contract-critical behavior (do NOT trust the report)
  Built the branch binary at b540a84 and exercised it live; also wrote+ran a throwaway in-package repro test (then removed it) that printed the real assembled host argv. AC-3 unswallowable prompt: `claude "do the thing" -- --plugin-dir /some/dir` → host argv `[claude --agent spacedock:first-officer --plugin-dir /some/dir "<bootstrap> do the thing"]` — prompt is the LAST token, `--plugin-dir` rides in passthrough with its value bound; trailing-host-flags variant (`-- --model opus --plugin-dir /d`) also keeps the prompt last. AC-5: real binary `status --some-bogus-flag` and `status --where status=validation` both forwarded verbatim to the runner (exit 0, table rendered, NOT cobra-rejected); `dispatch show-stage-def` works; `--version` → `spacedock 0.19.0 (contract 1)`.
- DONE: Verify AC-1, AC-2, AC-4, AC-6 by reproducing the committed tests + real-binary checks
  AC-1: no-args/`--help`/`-h`/`help` byte-identical (diff confirmed), tagline+Launch/Setup/Workflow+6 commands+footer present, banned `front door`/`contract-gated`/`META` absent, no `--version`/`--help` command row. AC-2: `install`/`install --help`/`doctor` work; `init` EXIT 2 (unknown command); TestInit* (retargeted to install→doctor seam) pass. AC-4: `claude`/`codex --help` carry knobs+`--plugin-dir`+`--` forwarding+Examples; `install --help` documents `--host`/`--check`. AC-6: space/equals/repeat forms of each knob produce identical safehouse `extra` (`--add-dirs=/home/a --add-dirs=/home/b`), nothing leaks to passthrough; reported space-form case no longer emits `malformed flag`; bad value `--safehouse-bogus=x` → knob-named `unknown flag: --safehouse-bogus`, no `malformed flag`, Launch never reached.
- DONE: `go test ./...` green except ONLY the pre-existing env-gated codex test; gofmt + go vet clean; cobra/pflag in go.mod (tidy); cli.Run signature + exit-code contract + seams unchanged
  `go test ./...` = 512 passed, 1 failed (only TestCodexResolveManifestAgainstInstalledHost). Confirmed via `git diff --name-only main...HEAD` that NO codex resolver file is touched; reproduced `codex plugin list` failing identically outside the test (`Failed to read config file ~/.codex/config.toml: Operation not permitted` — sandbox env, not the branch). gofmt -l clean, go vet clean, cobra v1.10.1 + pflag v1.0.9 direct deps, `go mod tidy` a no-op. `func Run(args []string, stdout, stderr io.Writer) int` unchanged; `runClaude`/`runCodex`/`hostOps`/`status.Runner` seam signatures byte-identical to main; exit-code contract preserved (unknown=2, init=2, version=0).

### Per-AC evidence table

| AC | Verdict | Evidence |
|----|---------|----------|
| AC-1 grouped/jargon-free/META-free help | PASS | live: no-args≡`--help`≡`-h`≡`help` (diff identical); tagline+3 groups+6 cmds+footer present; `front door`/`contract-gated`/`META`/version-row absent. TestTopLevelHelpGroupedJargonFree, TestTopLevelHelpFormsAreIdentical |
| AC-2 install renamed, init gone | PASS | live: `install --help` works, `init` EXIT 2 (unknown command). TestInit* (install→doctor seam), TestUnknownCommand, TestInstallHelpDocumentsHostAndCheck |
| AC-3 Option-2; prompt unswallowable | PASS | independent argv repro: prompt always LAST, `--plugin-dir` in passthrough; trailing-host-flag variant too. TestParseFrontDoorArgs (10 cases), TestDevLanePluginDirReachesLaunchSeam |
| AC-4 per-command help detail | PASS | live `claude`/`codex`/`install --help`: knobs+`--plugin-dir`+`--` forwarding+Examples present. TestFrontDoorHelpCarriesDetail, TestInstallHelpDocumentsHostAndCheck |
| AC-5 skill command set + verbatim forward | PASS | live: `status --bogus-flag`/`--where` forward verbatim; `dispatch show-stage-def`; `--version`→`(contract 1)`. TestStatusForwardsRequestVerbatim, TestVersionContractToken; status.Runner seam unchanged |
| AC-6 knob space/equals/repeat, no leak, clear error | PASS | independent repro + live: all forms → same `extra`, no passthrough leak, no `malformed flag`; bad value names knob. TestSafehouseKnobFormsEquivalent (8), TestSafehouseAddDirsSpaceFormNoLeak, TestSafehouseBadValueNamesKnob |

### Summary

PASSED. Independently reproduced (live binary + a throwaway in-package repro that printed the real assembled argv, since removed) the two contract-critical mechanisms: the Option-2 prompt-unswallowable property (the spacedock prompt is always the last host-argv token; `--plugin-dir` rides in post-`--` passthrough with its value bound, so the old swallow cannot recur) and the AC-5 verbatim status/dispatch forwarding with the `(contract 1)` token intact. All six ACs PASS against both the committed behavioral tests and real-binary checks. `go test ./...` is 512/513 green; the single failure is the pre-existing env-gated TestCodexResolveManifestAgainstInstalledHost (the diff touches NO codex resolver code; `codex plugin list` fails identically outside the test on a sandbox `~/.codex/config.toml` permission error) — correctly out of scope. gofmt/vet clean, cobra+pflag tidy, cli.Run signature + exit-code contract + injectable seams unchanged. No defects found; no rework needed. RELEASE DEFERRED honored (no merge/tag/push/main touched).

## Stage Report: implementation (cycle 2)

- DONE: gateHost stderr remedies in internal/cli/frontdoor.go (~lines 61, 67, 77) still tell users to run the removed `spacedock init --host …` — change them to `spacedock install --host …` so the gate's printed remedy names a real command, and add/extend a test asserting the remedy text matches the live install command name.
  Changed all three `gateHost` remedy branches (resolve-error, no-plugin, missing-manifest) to name `spacedock install --host`. Found the SAME defect on the gate's mismatch branch — `gateHost` prints `res.Message`, and contract.go's `tooOldPluginRemedy`/`noPluginMessage` (the too-old-plugin/no-plugin gate output, also shown by `doctor`) named `spacedock init --host`; fixed both (root-cause, not just the three named lines). New TDD test `TestGateRemedyNamesLiveInstallCommand` drives every gateHost branch asserting the remedy names `spacedock install` and never `spacedock init`, then proves resolution via cobra `root.Find` (`install` resolves, `init` falls back to root → exit 2). Retargeted `contract_test.go` `TestCompareHostSubstitution` to assert the live verb + absence of `init`; updated the stale `init` halves in `frontdoor_test.go` (×2) and `safehouse_frontdoor_test.go` (×2). Live binary: `doctor --host claude` → "Install it: spacedock install --host claude."; `init` EXIT 2. commit 5c01b113.
- DONE: Correct the entity's AC-3 wording: the swallow is NOT 'structurally impossible' — a dangling value-taking host flag as the final post-`--` passthrough token (e.g. `claude -- --plugin-dir` with no value) still consumes the prompt, identical to origin/next (pre-existing, a user error). Restate AC-3 as the accurate narrow property (the spacedock prompt is always the last assembled host-argv token) and cover the dangling-value case with a test or an explicit doc note.
  Restated the AC-3 heading + body + decision-5 line + test-plan row: the property is "the prompt is ALWAYS the last spacedock-built host-argv token," which kills the OLD inverted-grammar swallow but is NOT a structural impossibility — a dangling value-taking flag as the final post-`--` token binds the prompt as its value (user error, byte-identical to origin/next). Verified empirically (throwaway repro, since removed): `claude "task" -- --plugin-dir` → argv `[claude --agent spacedock:first-officer --plugin-dir "<bootstrap> task"]`, prompt last but bound by the host's `--plugin-dir`. Added behavioral test `TestDanglingValueTakingHostFlagStillSwallows` (prompt still last + dangling flag immediately before it) and softened the "structurally unswallowable" comment on `TestDevLanePluginDirReachesLaunchSeam`. commit 5c01b113.

### Summary
Targeted post-audit fix on the existing worktree branch (no cobra re-litigation). (A) Every contract-gate remedy a user can hit now names the live `spacedock install --host …` instead of the removed `spacedock init --host …` (which exits 2): the three `gateHost` branches AND — found as the same root-cause defect — the contract.go too-old-plugin/no-plugin messages that flow through the gate's mismatch branch and `doctor`. (B) AC-3 wording corrected from "structurally impossible/unswallowable" to the accurate narrow property (prompt always the last spacedock-built token); the honest limit — a dangling value-taking host flag as the final passthrough token still has the host bind the prompt, a user error identical to origin/next — is now pinned by a regression test. All TDD (failing test first, verified empirically). `go test ./...` = 524 passed, 1 failed (only the pre-existing env-gated TestCodexResolveManifestAgainstInstalledHost; `codex plugin list` reproduced failing identically outside the test; codex resolver not in the branch diff). gofmt/vet clean, cli.Run signature + exit-code contract unchanged. Code on `spacedock-ensign/cli-cobra-redesign` (5c01b113); no main/tag/push touched. NOTE for review: the install command's own error self-labels (`internal/cli/init.go`: `spacedock init: unknown host …`, etc.) still print the old verb when `spacedock install` errors — a cosmetic rename leftover, left untouched as it is a self-label not a "run this removed command" remedy and outside the two named audit items; flagging for a follow-up if desired.

## Stage Report: validation (cycle 2)

- DONE: With the built branch binary, confirm EVERY contract-gate remedy a user can hit now names `spacedock install --host …` and never the removed `init` (the three gateHost branches AND contract.go's too-old-plugin/no-plugin messages surfaced via `doctor`); `spacedock init` still exits 2; TestGateRemedyNamesLiveInstallCommand passes.
  Built branch binary at 5c01b113. Source-confirmed all three gateHost branches name `spacedock install --host` (frontdoor.go:59-78; resolve-error branch names `spacedock doctor` or `spacedock install --host`, both live). Live `doctor`: isolated CLAUDE_CONFIG_DIR no-plugin → "Install it: spacedock install --host claude."; too-old fixture (`testdata/too-old-plugin.json`) → "Update it: spacedock install --host claude (or 'claude plugin update spacedock')." `init` and `init --host claude` both EXIT 2 (unknown command). `strings` on the binary: the only residual `spacedock init:` tokens are init.go's four SELF-LABELS (unknown argument / host install failed / unknown host / --host requires a value) emitted when install/doctor itself errors — NOT "run a removed command" remedies; flagged cosmetic by cycle 2 impl, out of the two named audit items. TestGateRemedyNamesLiveInstallCommand + TestCompareHostSubstitution PASS.
- DONE: Confirm AC-3 is restated to the accurate narrow property (prompt always the last spacedock-built host-argv token) and TestDanglingValueTakingHostFlagStillSwallows guards the honest limit.
  Entity AC-3 heading/body + decision-5 line + test-plan row read accurate: "the prompt is ALWAYS the last spacedock-built host-argv token," NOT a structural impossibility; the dangling-value case (`-- --plugin-dir`, no value) binds the prompt host-side, byte-identical to origin/next, a user error. TestDanglingValueTakingHostFlagStillSwallows asserts both halves: prompt last + spacedock-built (invariant) AND the dangling `--plugin-dir` is the token immediately before it (host-side consequence). PASS.
- DONE: go test ./... green except the pre-existing env-gated TestCodexResolveManifestAgainstInstalledHost; gofmt/vet clean; cli.Run signature + exit-code contract unchanged; the cobra migration is not re-litigated/regressed.
  `go test ./...` = 524 passed, 1 failed (only TestCodexResolveManifestAgainstInstalledHost). `git diff --name-only main...HEAD` confirms NO codex resolver file is touched; `codex plugin list` reproduced failing identically outside the test ("Error: failed to load configuration" — sandbox cannot read ~/.codex/config.toml). gofmt -l clean, go vet clean. `func Run(args []string, stdout io.Writer, stderr io.Writer) int` byte-identical to main; exit-code contract live (unknown=2, version=0, help=0). Did not re-litigate the migration.

### Summary

PASSED (re-validation). The cycle-2 post-audit fix (5c01b113) is complete and nothing regressed. Built the branch binary and reproduced live: every contract-gate remedy a user can hit — the three gateHost branches and contract.go's too-old-plugin/no-plugin messages surfaced via `doctor` — now names `spacedock install --host …`; `spacedock init` exits 2; the only residual `spacedock init:` strings are init.go self-labels (error prefixes when install/doctor itself errors, not remedies), correctly left as flagged-cosmetic. AC-3 is restated to the accurate narrow property and TestDanglingValueTakingHostFlagStillSwallows pins the honest dangling-value limit. `go test ./...` = 524/525 green, the sole failure the pre-existing env-gated codex test (resolver not in the branch diff; reproduced failing outside the test). gofmt/vet clean, cli.Run signature + exit-code contract unchanged, migration not re-litigated. No defects; no rework. RELEASE DEFERRED honored (no merge/tag/push/main touched).
