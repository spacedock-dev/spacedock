---
id: zse4a3ds0x19gpdcjh7anhgs
title: Segregate Claude-specific runtime coupling (context-budget, team/standing) from the generic contract
status: done
source: sprint — FO/captain (native-dispatch-helper scope split)
score: "0.32"
worktree: 
started: 2026-05-30T22:10:50Z
mod-block: 
pr: "#246"
completed: 2026-06-01T06:03:03Z
verdict: PASSED
archived: 2026-06-01T06:03:03Z
---

The `claude-team` helper carries a runtime-coupled surface that `native-dispatch-helper` deliberately scopes OUT of the native dispatch path: `context-budget` (reads Claude Code's `~/.claude/.../agent-*.jsonl` transcripts + team `config.json`), and the standing-teammate / team subcommands `spawn-standing`, `list-standing`, `show-standing` (emit Agent() specs, probe team membership, enumerate `_mods/`). These are Claude-Code-specific and not on the initial-dispatch critical path — but the FO still shells to `python3 + claude-team` for **every ensign-reuse decision** (`context-budget`), the feedback-rejection flow, and standing-teammate spawn. So a "Python-free self-hosted" handoff is, today, only Python-free for *initial dispatch*; the steady-state FO loop is not.

This entity owns that surface and the contract reorganization it implies. Two coupled questions:

1. **Subcommand disposition.** For each runtime-coupled `claude-team` subcommand (`context-budget`, `spawn-standing`, `list-standing`, `show-standing`, and `build`'s `_mods`-enumeration branch that decides whether to emit a `show-standing` fetch line), decide: reimplement native (in a Claude-Code-specific package, NOT the generic dispatch/status core), keep a thin shim, or keep Python — and justify against the cost (transcript-format coupling, per-host layout) vs the benefit (a truly Python-free FO loop).
2. **Claude-vs-generic contract split.** The operating contract is split today into a generic core (`first-officer-shared-core.md`) and host runtime adapters (`claude-first-officer-runtime.md`, `codex-first-officer-runtime.md`). Determine whether Claude-Code-specific assumptions (transcript-budget reads, team/standing semantics, on-disk team layout) have leaked into the generic core, and if so, relocate them into the Claude runtime adapter (or a Claude-specific module) so the generic core is host-neutral. The native binary's package boundaries should mirror this: generic dispatch/status logic in host-neutral packages, Claude-Code coupling behind a clear seam.

## Acceptance criteria

Sharpened by ideation 2026-05-30 (see `## Ideation design`). Every AC is an end-state property of the finished entity, falsifiable by an oracle external to this entity body. No doc-only ACs.

**AC-1 — The five runtime-coupled subcommands resolve through the native binary's `spacedock dispatch` surface (in `internal/claudeteam`, not the generic dispatch/status core), each at parity with the vendored Python oracle.**
End state: `spacedock dispatch context-budget`, `list-standing`, `show-standing`, `spawn-standing` are no longer rejected as deferred (today `internal/dispatch/dispatch.go` returns exit 2 for them), and `spacedock dispatch build` emits the `_mods`/`show-standing` fetch line.
Oracle: the three-channel parity harness (`internal/dispatch/parity_harness_test.go` idiom — native in-process vs vendored `claude-team` exec'd with pinned hermetic `HOME`, byte-compare stdout/stderr/exit after the documented `claude-team`→`spacedock dispatch` fetch-prefix rewrite) extended with a fixture `~/.claude` tree (team `config.json` + `agent-*.meta.json` + `agent-*.jsonl`) for `context-budget` and a `_mods`-bearing workflow fixture for the standing subcommands and the `build` branch. Each subcommand's success path AND its loud-failure paths (missing jsonl, missing mod, missing `standing: true`, bad model enum) are at parity. SPIKE-A pre-validated the context-budget extraction parity (85183 == 85183 on a frozen snapshot).

**AC-2 — The FO ensign-reuse path has a defined, tested behavior when no context budget is available, on both hosts.**
End state: reuse-condition-0 is a *capability* in the generic contract, not a hardcoded Claude command. When the budget probe reports the worker over budget → fresh-dispatch; when the budget source is unavailable → fresh-dispatch (fail-safe, never silent-reuse); when the runtime declares no budget probe at all (Codex, established by SPIKE-C) → condition-0 is satisfied and the remaining reuse conditions decide. "Unavailable" is three concrete `spacedock dispatch context-budget` exit-1 conditions, each fail-safe identically: (a) **missing jsonl** — `find_subagent_jsonl(name)` finds no `agent-*.jsonl` for the named agent (oracle: "no subagent jsonl found"); (b) **unreadable/empty jsonl** — the jsonl exists but yields no assistant entry carrying `usage` (oracle: "no assistant entries with usage"); (c) **agent-not-in-team-config** — `lookup_model(name)` finds no team `config.json` member with that name (oracle: "no team config found for member"). All three exit non-zero with no `reuse_ok: true`, so the FO fresh-dispatches; none ever silent-reuses.
Oracle: a behavior fixture drives `spacedock dispatch context-budget` against a fixture `~/.claude` tree once per condition — (a) named agent's jsonl absent, (b) jsonl present but all-zero/usage-free, (c) agent absent from the team `config.json` members list — and asserts each exits non-zero and emits no `reuse_ok: true` (the FO fresh-dispatches). The native and vendored-Python paths are byte/struct-compared on all three via the AC-1 parity harness. The no-probe-runtime case (Codex) is host-qualified prose covered by the AC-3 prose oracle. This closes the steady-state-Python question with an observable contract.

**AC-3 — The generic operating-contract core (`first-officer-shared-core.md`) and the generic Go packages (`internal/dispatch`, `internal/status`) carry no UNQUALIFIED Claude-Code-specific runtime coupling; the coupling lives in the Claude adapter / `internal/claudeteam`.**
End state: every named Claude-runtime helper command/function in the generic core sits inside an explicitly host-qualified span (the `X on Codex, Y on Claude` shape already at `first-officer-shared-core.md:206`); the concrete commands move to `claude-first-officer-runtime.md`; `internal/dispatch` + `internal/status` source contains no `~/.claude`/home-rooted team/transcript reads. **This requires relocating three PRE-EXISTING `~/.claude` reads that already violate the invariant before this entity's subcommand work begins** (see the host-neutrality-seam relocation in Decision 2): `internal/status/boot.go probeTeamState` (reads `~/.claude/teams`, called by `gatherBoot`, feeds the boot `TEAM_STATE` JSON), `internal/dispatch/helpers.go recentTeamEvidence` (reads `~/.claude/teams` via `os.UserHomeDir`, called by `build.go`'s bare-mode WARN), and the `build.go` WARN string literal naming `~/.claude/teams/*/config.json`. The generic boot/build paths take a host-supplied team-state probe (a callback or interface the Claude package fills and the Codex/bare path leaves nil), so `internal/status`/`internal/dispatch` source carries no `.claude` literal. The boot `TEAM_STATE` section's text and the bare-mode WARN move with their probe into the Claude seam.
Oracle (two complementary structural invariants over real files, NOT a substring count):
- prose-side: a test parses the markdown structure of `first-officer-shared-core.md` and fails if a Claude-only helper token (`claude-team`, `context-budget`, `spawn-standing`, `list-standing`, `show-standing`, `member_exists`, `lookup_model`) appears in a generic-algorithm span that lacks a host qualifier — falsifiable by re-introducing the line-142 unqualified `claude-team context-budget` step and watching it fail.
- code-side: a test over `internal/dispatch` + `internal/status` source (via `go/parser`) asserts no `.claude` path-join / home-rooted transcript-read string literals — falsifiable by adding such a read and watching it fail. This oracle FAILS today (pre-implementation) on `probeTeamState`, `recentTeamEvidence`, and the `build.go` WARN literal; making it pass is the host-neutrality-seam prerequisite, not incidental cleanup.

**AC-4 — `context_limit_for_model` resolves the opus context window by a forward family rule (opus minor ≥ 7 → 1M), not a per-release exact-name list, in both the native implementation and the vendored Python oracle.**
End state: `claude-opus-4-8` (no `[1m]` suffix) → 1_000_000; `claude-opus-4-6` → 200_000; `[1m]` suffix → 1_000_000; a hypothetical `claude-opus-4-10` → 1_000_000 without a code change. The live false-negative (every opus-4-8 ensign read against the 200k denominator) is gone.
Oracle: a fixture `~/.claude` tree stamping the agent `claude-opus-4-8` (no suffix, as the real team `config.json` does for ensigns) at a sub-600k resident count asserts `context_limit == 1_000_000` and `reuse_ok == true`; a boundary table asserts the per-model expectations above. SPIKE-B pre-validated the rule against the current buggy Python.

## Test gates

- `go test ./...`
- The extended three-channel parity harness for `context-budget`, `list-standing`, `show-standing`, `spawn-standing`, and `build`'s `_mods` branch (native vs vendored Python over `~/.claude` + `_mods` fixtures).
- The AC-2 budget-unavailable fallback behavior fixture.
- The AC-3 host-neutrality invariants (prose-side markdown-structure parse + code-side `go/parser` over the generic packages — the code-side oracle must go RED on the pre-existing `probeTeamState`/`recentTeamEvidence`/`build.go`-WARN `~/.claude` literals before the seam relocation and GREEN after).
- Boot `TEAM_STATE` and build bare-mode-WARN parity preserved across the team-state-probe relocation (the existing boot/build harness, asserting the injected-probe seam does not change observed output on a fixture `~/.claude` tree).
- The AC-4 1M family-rule fixture + boundary table.

## Notes

Sequences after `native-dispatch-helper` (`7w8w5nsa5mbc807b3jb88psv`), which defines the native dispatch surface and the package seam this entity extends. native-dispatch-helper's `build` is scoped to NON-`_mods` workflows (the self-hosted `docs/dev` path); this entity owns `build`'s `_mods`/`show-standing` fetch-line branch + the standing subcommands, closing the `_mods` parity gap.

Per the captain's note: if a design choice here rests on an unverified mechanism (e.g. exact transcript-budget parity, or whether a host-neutral seam is achievable without per-host branching), ideation must SPIKE it — run the smallest end-to-end exercise first and record the behavioral evidence — rather than assert it.

Relation to the deliverable-principles proposal (`docs/dev/_proposals/encoding-deliverable-principles.md`): that proposal's contract edits and this entity's contract reorganization both touch the operating-contract reference files; coordinate so they don't collide, and prefer the vendored-copy-first authoring direction documented there.

**Context-budget 1M-detection bug (found 2026-05-30, fold into this entity's scope).** `context_limit_for_model` returns the 200k DEFAULT for `claude-opus-4-8` ensigns: the `EXTENDED` list has `claude-opus-4-7` but NOT `4-8`, and spawned ensigns drop the `[1m]` suffix in their team-config/jsonl `model` field (only the lead keeps `claude-opus-4-8[1m]`). But since opus **4-7**, ensigns run the 1M window REGARDLESS of the dropped suffix (the suffix was only load-bearing back on 4-6). Result: `reuse_ok` false-negatives every opus-4-8 ensign at the wrong 200k denominator (a 159k-resident ensign reads as 80% when it is ~16% of 1M). Fix: a forward FAMILY RULE — parse the opus minor version and return 1M for minor ≥ 7 — so the mapping never goes stale on the next release; do NOT depend on the `[1m]` suffix or a per-release exact-name list. Applies whether context-budget stays Python or is reimplemented native. Behavioral test: a fixture stamping `claude-opus-4-8` (no suffix) asserts `context_limit` == 1_000_000 and `reuse_ok` true at a sub-600k resident count.

## Ideation design (2026-05-30)

### SPIKE evidence (forensic, run before designing)

Two unverified mechanisms gated this design; both spiked against real artifacts before any decision was locked.

**SPIKE-A — context-budget transcript parity (native Go vs Python).** Drove a throwaway stdlib-Go reimplementation of the three context-budget primitives (`find_subagent_jsonl` → `extract_resident_tokens` → `context_limit_for_model`) against the same real `~/.claude/.../subagents/agent-*.jsonl` this very ensign is writing, then against a *frozen snapshot* of it (the live file grows between reads — peak-resident is a moving target by design, not a parity defect).
- Frozen-snapshot resident extraction: Python `extract_resident_tokens` = **85183**, Go backward-scan = **85183** (exact match). `extract_runtime_models` = `['claude-opus-4-8']` both sides.
- Real usage shape confirmed: assistant entries carry `usage.{input_tokens, cache_creation_input_tokens, cache_read_input_tokens}`; resident = their sum; backward scan skips the trailing all-zero entry an overflow-dead ensign leaves. Plain JSONL, no Claude-proprietary encoding — stdlib `encoding/json` line-decode reproduces it.
- Verdict: native context-budget parity is **achievable and cheap**. No format surprise. The riskiest path is de-risked.

**SPIKE-B — the 1M-detection family rule, across the version boundary.** Implemented the forward family rule in Go (`^claude-opus-4-(\d+)`, minor ≥ 7 → 1M; `[1m]` suffix → 1M; else 200k) and ran the boundary cases against the CURRENT (buggy) Python:

| model | CURRENT Python | family rule (correct) |
|---|---|---|
| `claude-opus-4-8` | 200000 **(BUG)** | 1000000 |
| `claude-opus-4-8[1m]` | 1000000 | 1000000 |
| `claude-opus-4-7` | 1000000 | 1000000 |
| `claude-opus-4-6` | 200000 | 200000 (correct — 4-6 needs the suffix) |
| `claude-opus-4-10` (hypothetical) | 200000 (would go stale) | 1000000 (forward-safe) |
| sonnet / haiku | 200000 | 200000 |

- Reproduced the live bug end-to-end: `claude-team context-budget --name {this-ensign}` returns `context_limit: 200000` while the ensign genuinely runs the 1M window. Confirmed the root cause empirically: the real team `config.json` stamps the ensign `claude-opus-4-8` (no suffix) while the lead keeps `claude-opus-4-8[1m]`, and the real jsonl `model` field is `claude-opus-4-8` (no suffix).
- Verdict: the family rule fixes the false-negative and never goes stale; `4-6` correctly stays 200k (minor < 7), so the rule is not over-broad.

**SPIKE-C — is a host-neutral seam achievable without per-host branching?** Read the Codex FO runtime adapter end to end. Finding: the **Codex adapter has no context-budget mechanism and no standing-teammate section at all**. Its reuse flow decides reuse on "addressable + shared reuse conditions pass" via `send_input`/`wait_agent`; there is no transcript-reading budget probe, because Codex has no `~/.claude/.../agent-*.jsonl` to read. context-budget and standing-teammates are **structurally Claude-only capabilities**, not generic capabilities that Claude happens to implement first.
- Verdict: a host-neutral seam IS achievable, and the seam is a *capability boundary*, not a per-host `if claude:` branch. The generic core expresses reuse-condition-0 as "if the runtime adapter provides a context-budget probe and it reports `reuse_ok: false`, dispatch fresh"; the Claude adapter fills the probe, the Codex adapter declares it absent (condition-0 vacuously satisfied). This is the same pattern the contract already uses at `first-officer-shared-core.md` line 207 (`send_input on Codex, SendMessage on Claude teams`).
- Scope of what SPIKE-C proves (sharpened cycle 2): SPIKE-C establishes the *capability/FEATURE* boundary — Codex genuinely lacks the context-budget and standing-teammate features, so reuse-condition-0 and standing-discovery must be expressed as capabilities the adapter may or may not provide. It does NOT, by itself, prove the Go *PACKAGE* boundary (that `~/.claude` reads quarantine cleanly into `internal/claudeteam`); that is a separable design assertion validated by AC-3's code-side `go/parser` oracle, which the host-neutrality-seam relocation (Decision 2) must make true. The two are independent: the feature boundary motivates the seam; the package oracle enforces it.

### Decision 1 — Subcommand disposition

Drives the native binary's `spacedock dispatch` surface; mirrors the package seam `native-dispatch-helper` established (generic dispatch/status logic in host-neutral packages; Claude coupling behind a clear seam). The disposition is **native reimplementation behind a Claude-specific package**, NOT in the generic `internal/dispatch` core.

| subcommand | disposition | rationale | oracle |
|---|---|---|---|
| `context-budget` | **native**, in a new Claude-specific package (`internal/claudeteam`), folding the 1M family-rule fix | SPIKE-A proved cheap parity; this is the per-ensign-reuse hot path the entity exists to de-Python; transcript format is stable JSONL. Coupling (reads `~/.claude`) is real but quarantined to the Claude package — it does NOT enter `internal/dispatch`/`internal/status`. | parity test: native vs vendored Python over a fixture `~/.claude` tree (team `config.json` + `agent-*.meta.json` + `agent-*.jsonl`), byte/struct-compare the JSON envelope and exit code |
| `list-standing` | **native**, same Claude package | pure `_mods/*.md` frontmatter enumeration (`standing: true`), no transcript coupling; needed at FO boot before members exist; trivially native | parity test over a `_mods` fixture: native vs Python newline-delimited sorted paths |
| `show-standing` | **native scaffolding, Claude-rendered body** | the composition (`enumerate_declared_standing_teammates` + render) is runtime-neutral but the rendered body is Claude-specific SendMessage routing prose; native keeps the body string verbatim from the Python oracle so parity holds byte-for-byte | parity test over a `_mods` fixture including the `## Routing Usage` body-extraction path |
| `spawn-standing` | **native**, same Claude package | emits an Agent() spec JSON + `member_exists` team-config probe; closes the last steady-state Python shell-out (feedback/standing spawn); model-enum + heading-order validation port directly | parity test: already-alive path (fixture team config has the member) and spec-emit path (member absent), plus the loud-failure cases (missing mod, missing `standing: true`, bad model enum) |
| `build`'s `_mods`/`show-standing` fetch-line branch | **native**, extending `internal/dispatch/build.go` | `native-dispatch-helper` deferred exactly this branch to this entity (see `build.go:60-61`). The branch is `enumerate_declared_standing_teammates(workflow_dir, team_name)` → if non-empty, append a `spacedock dispatch show-standing` fetch line. The enumeration helper is runtime-neutral and can live in `internal/dispatch` (it is pure `_mods` parsing); the Claude-specific `show-standing` *body* it points at lives in the Claude package. | extend the existing three-channel parity harness with a `_mods`-bearing fixture: assert native build emits the standing fetch line iff Python does, byte-identical after the documented `claude-team`→`spacedock dispatch` fetch-prefix rewrite |

**Why native for all five, not keep-Python or shim:** the entity's whole reason to exist is that the steady-state FO loop is not Python-free even though initial dispatch is. Keep-Python on `context-budget` would leave the per-reuse hot path shelling to `python3 + claude-team` forever; that's the status quo this entity removes. A shim buys nothing over native here — the logic is small and the parity test is the same cost either way. The transcript-format coupling cost (the cited downside of native) is contained by the package boundary: it lives in `internal/claudeteam`, never in the generic dispatch/status core, so the host-neutrality invariant (Decision 2) still holds.

**Package placement:** `enumerate_declared_standing_teammates` (pure `_mods` frontmatter parsing, the `build` branch needs it) is runtime-neutral → `internal/dispatch`. Everything that reads `~/.claude` (context-budget, `member_exists`, `lookup_model`) or renders Claude SendMessage prose (`show-standing` body) → `internal/claudeteam`. This keeps `internal/dispatch` and `internal/status` free of `~/.claude` reads.

### Decision 2 — Claude-vs-generic contract split (AC-3)

The generic `first-officer-shared-core.md` carries Claude-Code-specific runtime coupling today. Concrete leaks identified (real-file evidence, vendored copy in this checkout; line numbers re-verified cycle 2 — the cycle-1 citations were each off by one):

| location | leak | relocation |
|---|---|---|
| line 142 (reuse condition 0) | `run \`claude-team context-budget --name {ensign-name}\`. If \`reuse_ok\` is false, skip to fresh dispatch` — names a Claude-only binary as an unconditional algorithm step | generic core states the *capability*: "if the runtime adapter provides a context-budget probe and it reports the worker over budget, dispatch fresh; if the adapter declares no budget probe, this condition is satisfied." Concrete `spacedock dispatch context-budget` invocation lives in `claude-first-officer-runtime.md`. |
| line 206 (feedback rejection flow, step 4) | same `claude-team context-budget` named invocation | same relocation; generic core references the capability, Claude adapter owns the command. (Distinct from the already-qualified line-207 `send_input on Codex, SendMessage on Claude teams` — that one is a NOT-leak, see below.) |
| line 117 (prose-polish routing) | `Check team-config membership via \`member_exists\`` + `Workers dispatched via \`claude-team build\`` | generic core describes "check the runtime's team-membership predicate"; `member_exists` (a Claude helper name) and the `claude-team build`/`spacedock dispatch build` command move to the Claude adapter |
| line 325 (`## Standing Teammates`) | `The FO discovers each at boot (via \`list-standing\`)` — names a Claude subcommand in the section lede | see the concept-vs-mechanics rewrite below; `list-standing` moves to the Claude adapter (already there, line 34) |
| lines 327-330 (`## Standing Teammates` concept bullets) | `## Hook: startup` parsing / `_mods/{name}.md` layout / SendMessage / `TeamDelete` / `subagent_type`/`team_name` config — Claude-only declaration + routing mechanics interleaved INTO the cross-runtime concept bullets | see the concept-vs-mechanics rewrite below — this is a rewrite, not a line-range move |

NOT leaks (already correctly host-qualified — do not touch): line 207 (`send_input on Codex, SendMessage on Claude teams`), line 327 mid-bullet (`Because Claude teams are per-captain-session ... other runtimes re-derive scope`), line 349 (`on Claude Code: ... file-staleness safety net`). The fix follows the pattern these lines already establish.

**`## Standing Teammates` — concept-vs-mechanics rewrite (M-2, NOT a line-range relocation).** The section today (lede + four bullets: first-boot-wins, team-scope lifecycle, routing contract, declaration format) already leads with the cross-runtime *concept*, but each bullet then interleaves Claude *mechanics* as if they were generic. The rewrite cleaves each bullet along the concept/mechanics seam:
- **stays in the generic core (concept, host-qualified):** a standing teammate is a long-lived team-scoped specialist; spawn is deferred to first dispatch (first-boot-wins); lifecycle is captain-session/team-scoped and the teammate dies with the team; routing is by-name, best-effort, non-blocking with a 2-minute timeout; one declaration per teammate. The lede's "discovers each at boot" stays but drops the parenthetical `(via list-standing)`, becoming "discovers each at boot via the runtime adapter's standing-discovery step."
- **moves to the Claude adapter's standing section (mechanics):** `list-standing` as the boot discovery command; `_mods/{name}.md` on-disk layout; the `## Hook: startup` section grammar (`subagent_type`, `name`, `model` enum, `team_name` placeholder); the `## Agent Prompt` last-section rule; `TeamDelete` as the teardown trigger; SendMessage as the concrete routing call. These are the Claude realization of the four concepts; the Codex adapter realizes (or omits) them differently.

**Host-neutrality seam — relocate the three PRE-EXISTING `~/.claude` reads (M-1, the cycle-1 blocker; FO-confirmed broader than the review cited).** AC-3's code-side oracle ("`internal/dispatch` + `internal/status` carry no home-rooted team/transcript reads") FAILS today, before any subcommand work, because three reads already sit in the generic packages:

| location | pre-existing leak | relocation |
|---|---|---|
| `internal/status/boot.go:131-163` `probeTeamState` | reads `~/.claude/teams/*/config.json` mtimes; called by `gatherBoot:206`; result feeds the boot JSON `TEAM_STATE` section (`teamPresent`/`teamHint`) | `gatherBoot` takes a host-supplied team-state probe (a func/interface value). The Claude seam (`internal/claudeteam`) supplies the `~/.claude`-reading implementation; the generic path leaves it nil → `TEAM_STATE: present: false`. The "claude runtime supports it" hint string (`boot.go:294`) moves with the probe. |
| `internal/dispatch/helpers.go:137-162` `recentTeamEvidence` | reads `~/.claude/teams/*/config.json` mtimes via `os.UserHomeDir`; called by `build.go:115` to gate the bare-mode WARN | the WARN's evidence check moves behind the same host-supplied probe seam; the generic `build.go` calls the injected predicate, the Claude package owns the `~/.claude` read. |
| `internal/dispatch/build.go:116-120` | the bare-mode WARN string literal names `~/.claude/teams/*/config.json` | the WARN text moves into the Claude seam with `recentTeamEvidence`; generic `build.go` emits a host-neutral "no recent team evidence" advisory (or defers the whole advisory to the Claude package). |

The seam shape is one host-supplied team-state probe consumed by both the boot path and the build bare-mode advisory. This is the same capability-boundary pattern as the context-budget probe (Decision 1), not a per-host `if claude:`. After relocation, a `go/parser` scan of `internal/dispatch` + `internal/status` source finds no `.claude` literal — the oracle passes.

**Native package boundary mirrors the prose split:** generic dispatch/status logic stays in `internal/dispatch` + `internal/status` (no `~/.claude` reads in their source); Claude coupling (context-budget transcript reads, `member_exists`, standing-body rendering, team-state probes) lives in `internal/claudeteam`. NOTE: the boundary is about source LITERALS, not the transitive import graph — the binary as a whole still reads `~/.claude` through the Claude package, and the CLI/router layer may import `internal/claudeteam` to wire the five subcommands and the injected probes. The oracle checks that no `.claude` path-join or home-rooted read literal appears in the `internal/dispatch`/`internal/status` source, which the injected-probe seam satisfies.

**The host-neutrality invariant (AC-3 oracle — a structural check over real artifacts, NOT a substring grep).** Two complementary oracles:

1. **Prose-side (over the vendored contract files):** a test parses `first-officer-shared-core.md` and asserts that every reference to a *named Claude helper command or helper function* (`claude-team`, `spacedock dispatch context-budget`, `context-budget`, `spawn-standing`, `list-standing`, `show-standing`, `member_exists`, `lookup_model`) appears ONLY inside a span that is explicitly host-qualified — i.e., either the same sentence/bullet names a non-Claude runtime alternative (the `X on Codex, Y on Claude` shape at line 206) OR the reference lives under the Claude adapter file, not the generic core. The invariant is "no *unqualified* Claude-runtime command in the generic core," resolved by parsing the markdown structure (which bullet/section a token sits in and whether that span carries a host qualifier), not by `grep -c claude`. A bare mention inside an explicitly Claude-qualified clause is allowed; an unqualified algorithm step that names a Claude-only command fails.
2. **Code-side (over the real Go packages):** a test asserts `internal/dispatch` and `internal/status` import-graph and source contain no `~/.claude`/`os.UserHomeDir`-rooted team/transcript reads — those reside only in `internal/claudeteam`. Mechanizable via `go/parser` over the package files (no transcript-path string literals; no `.claude` path joins) — a structural property of the source, falsifiable by adding such a read and watching it fail.

### Decision 3 — Context-budget 1M fix (folded in)

Replace `NATIVE_1M_MODELS` exact-name set + `[1m]`-substring with the forward family rule in BOTH the (native) implementation and the vendored Python oracle (so parity holds): `[1m]` suffix → 1M; else parse `claude-opus-4-(\d+)`, minor ≥ 7 → 1M; else 200k. SPIKE-B validated the boundary. Behavioral oracle: a fixture `~/.claude` tree stamping the ensign `claude-opus-4-8` (no suffix) at a sub-600k resident count asserts `context_limit == 1_000_000` and `reuse_ok == true` — the exact false-negative the bug produces today.

### Decision 4 — Decomposition: host-neutrality seam as a PREREQUISITE entity (M-3)

The review flagged the entity as too large for one stage: as written it bundles six semi-independent workstreams (5 native subcommands + the `build` `_mods` branch + the contract reorg + the 1M fix + the M-1 host-neutrality seam) behind one integration point. **Recommendation: split the host-neutrality seam out as a prerequisite entity that lands FIRST**, then this entity carries the subcommands + contract reorg + 1M fix.

| entity | scope | why first / why it stands alone |
|---|---|---|
| **PREREQUISITE — `internal/claudeteam` seam + team-state-probe relocation** | create `internal/claudeteam`; relocate `probeTeamState` (boot `TEAM_STATE`) and `recentTeamEvidence` (build bare-mode WARN) behind a host-supplied team-state probe; refactor `gatherBoot`/`runBuild` to take the injected probe; make AC-3's code-side `go/parser` oracle pass on `internal/dispatch`+`internal/status`. | This is the riskiest contract uncertainty (does the injected-probe seam preserve boot/build parity AND clear the `.claude`-literal oracle?). Per the "validate the riskiest path first" discipline, it should land and prove the seam BEFORE five subcommands pile onto it. It is independently shippable: the five subcommands still resolve via Python until the main entity lands, and parity for boot/build is the existing harness. It establishes the package + the probe seam every subcommand then plugs into. |
| **MAIN (this entity) — 5 subcommands + `build` `_mods` branch + contract reorg + 1M fix** | reimplement context-budget/list-standing/show-standing/spawn-standing native in the now-existing `internal/claudeteam`; extend `build.go`'s `_mods`/`show-standing` branch; the `first-officer-shared-core.md` leak relocations + `## Standing Teammates` rewrite; the 1M family rule. | once the seam exists, each subcommand is a self-contained native-vs-Python parity unit behind it; the contract reorg is prose; the 1M fix is local. No longer six workstreams behind one integration point — the integration point (the package + probe seam) already exists and is proven. |

Rationale for prerequisite-not-absorb: the seam is the one piece whose failure invalidates the rest (if `~/.claude` reads can't quarantine cleanly, AC-3 is unsatisfiable and the whole package boundary is in question), and it is the smallest end-to-end exercise of the riskiest contract. Landing it first pays the small bill before the comprehensive run. If the FO/captain prefers a single entity, the seam MUST still be sequenced as the first implementation milestone with its own AC-3-code-side green checkpoint before subcommand work — the decomposition into a separate entity is the recommendation, but the non-negotiable is that the seam is validated before the subcommands stack on it.

### Implementation-stage worktree disposition

This entity's implementation stage SHOULD run worktree-backed (it ships Go code across `internal/claudeteam`, `internal/dispatch`, `internal/status`, `internal/cli` plus the vendored Python oracle and two contract reference files — a multi-file code change that wants branch isolation). Under split-root, the worktree isolates CODE only; the entity body and stage reports stay in the `.spacedock-state` checkout per the split-root contract. The prerequisite entity (if split out) likewise runs worktree-backed. Ideation (this stage) is non-worktree — state-checkout only.

### Coordination (operating-contract collision avoidance)

Three entities touch the operating-contract reference files. Declared touch-points so the FO can sequence them without collision, vendored-copy-first per `encoding-deliverable-principles.md`:
- **this entity:** `first-officer-shared-core.md` `## Standing Teammates` (full section, concept-vs-mechanics rewrite) + the **reuse-condition-0 paragraph (line 142)** and the **feedback-rejection step 4 (line 206)** inside `## Completion and Gates`/`## Feedback Rejection Flow` + the **prose-polish routing note (line 117)**. Plus `claude-first-officer-runtime.md` (gains the relocated commands).
- **deliverable-contract-hardening / `encoding-deliverable-principles.md`:** edits a DIFFERENT paragraph of `## Completion and Gates` (the AC-coverage cross-check) and `## Probe and Ideation Discipline`. Same section as my reuse-condition edit, different paragraphs — sequence, do not parallelize, on `## Completion and Gates`.
- **spacedock-packaging:** adds an FO-Startup step-0; if that lands in `## Startup`/boot prose it does not overlap my `## Standing Teammates`/`## Completion and Gates` edits.

### Independent staff review

This ideation warrants an **independent staff review** before the gate. Per the stage definition's staff-review trigger, the design spans (a) a native/Python parity contract over real `~/.claude` transcripts, (b) a host-neutrality invariant that must be a structural check rather than a grep, and (c) a multi-entity operating-contract edit collision — each individually the kind of "native parity / contract reorganization" complexity the FO is told to route for review. The review should check: the parity-fixture realism (does the fixture `~/.claude` tree reproduce the suffix-drop the live bug needs?), the host-neutrality invariant's resistance to gaming (can it pass while a leak survives?), and the package-boundary code-side oracle's soundness.

**Re-affirmation on the cycle-2 revised design (post-NEEDS-REWORK).** The independent staff review recommendation STANDS — the cycle-2 rework strengthens the case for it rather than weakening it: the M-1 host-neutrality seam adds a third Go-package refactor (inject a team-state probe into `gatherBoot`/`runBuild` without breaking boot/build parity) whose correctness the review should verify, and the M-3 decomposition decision (prerequisite-vs-absorb) is exactly the kind of right-sizing judgment a staff reviewer should sanity-check. The cycle-2 review should additionally confirm: (1) the injected team-state-probe seam preserves boot `TEAM_STATE` and build bare-mode-WARN parity while clearing the `.claude`-literal code-side oracle (the M-1 fix is sound, not just relocated cosmetically); (2) the three AC-2 budget-unavailable conditions are exhaustive of the oracle's exit-1 paths (no fourth silent-reuse path slips through); (3) the decomposition recommendation's prerequisite/seam ordering is the right cut. SPIKEs A/B/C stand from cycle 1 and were not re-run.

## Stage Report: ideation

- DONE: Decide the runtime-coupled-subcommand disposition (context-budget, spawn-standing, list-standing, show-standing, and build's _mods/show-standing fetch-line branch): per subcommand, native vs thin-shim vs keep-Python, justified against cost vs benefit. SPIKE the riskiest unverified mechanism and record the behavioral evidence.
  `## Ideation design` → Decision 1 table: all five native, in a Claude-specific `internal/claudeteam` package (the runtime-neutral `_mods` enumeration stays in `internal/dispatch`). SPIKE-A proved native-vs-Python context-budget parity on a frozen snapshot of this ensign's own real jsonl (resident 85183 == 85183, model `claude-opus-4-8` both sides).
- DONE: Design the Claude-vs-generic contract split (AC-3): identify Claude-Code-specific leaks in the generic core, relocate them into the Claude runtime adapter, and define the host-neutrality invariant as a check over real artifacts. FOLD IN the context-budget 1M-detection fix.
  Decision 2 enumerates five concrete leaks in `first-officer-shared-core.md` (lines 141, 205, 116, 324, 327-329) with relocations and three not-leaks (already host-qualified); the invariant is a two-part structural oracle (markdown-structure parse + `go/parser` over the generic packages), not a grep. 1M fix folded in as Decision 3 + AC-4, SPIKE-B-validated.
- DONE: Rewrite the seed ACs as behavioral end-state properties, each naming an exercise-and-observe oracle — NO doc-only ACs. State whether ideation warrants an independent staff review.
  AC-1..AC-4 rewritten as end-state properties, each naming a concrete oracle (extended three-channel parity harness over `~/.claude`+`_mods` fixtures; budget-unavailable fallback fixture; two-part host-neutrality invariant; 1M family-rule fixture+boundary table). `### Independent staff review` recommends YES with three review focuses.

### Summary

Spiked all three risks before locking any decision. SPIKE-A: native Go reproduces Python's context-budget resident-extraction exactly (85183==85183 on a frozen snapshot of the live jsonl). SPIKE-B: the forward family rule fixes the live `claude-opus-4-8` false-negative and stays correct across the version boundary (4-6 stays 200k, hypothetical 4-10 → 1M). SPIKE-C (the decisive one): reading the Codex FO adapter showed context-budget and standing-teammates are *structurally* Claude-only — Codex has neither — so the host-neutral seam is a capability boundary, not a per-host `if`. Disposition: all five subcommands go native behind `internal/claudeteam`, removing the steady-state Python shell-out the entity exists to kill, while the generic dispatch/status core stays free of `~/.claude` reads. Recommending an independent staff review given the parity-contract + host-neutrality-invariant + multi-entity-contract-collision span. No worktree (state-checkout ideation); no test gates run this stage (no code shipped — implementation is the next stage).

## Stage Report: ideation (cycle 2)

Rework driven by the FO's independent staff review (verdict NEEDS-REWORK). Core thesis (native subcommands behind `internal/claudeteam`; capability-boundary seam per SPIKE-C) was sound and is preserved; this cycle makes AC-3 satisfiable and right-sizes the entity. SPIKEs A/B/C not re-run.

- DONE: Resolve M-1 (FO-confirmed broader): the pre-existing `~/.claude` reads that make AC-3 unsatisfiable.
  Verified all three against source: `internal/status/boot.go:131-163` `probeTeamState` (reads `~/.claude/teams`, called by `gatherBoot:206`, feeds boot `TEAM_STATE` JSON), `internal/dispatch/helpers.go:137-162` `recentTeamEvidence` (reads `~/.claude/teams` via `os.UserHomeDir`, called by `build.go:115`), and `build.go:116-120`'s WARN string literal. Added a "Host-neutrality seam" subsection to Decision 2 (relocate all three behind a host-supplied team-state probe injected into `gatherBoot`/`runBuild`) and folded the relocation into AC-3's end state; AC-3 code-side oracle now explicitly noted as RED pre-relocation / GREEN after. New test gate added for boot/build parity across the relocation.
- DONE: Decide M-3 decomposition with a recommendation.
  Added Decision 4: RECOMMEND splitting the host-neutrality seam into a PREREQUISITE entity that lands first (riskiest contract path; independently shippable; establishes the package + probe seam the subcommands plug into), leaving this entity as 5 subcommands + `build` `_mods` branch + contract reorg + 1M fix. Non-negotiable fallback if kept single: the seam is validated (AC-3 code-side green) before subcommand work stacks on it.
- DONE: Restructure `## Standing Teammates` as concept-vs-mechanics (M-2; line-325 not 324 fixed; rewrite not move), sharpen AC-2 to three named budget-unavailable conditions (M-5), re-affirm the staff review.
  Re-verified all `first-officer-shared-core.md` citations against the real file — every cycle-1 line number was off by one (context-budget 142/206 not 141/205; `member_exists` 117 not 116; `list-standing` 325 not 324; `## Hook` mechanics 327-330; not-leaks 207/349). Decision-2 leak table corrected; added an explicit concept-vs-mechanics cleave for `## Standing Teammates` (concept stays host-qualified, `list-standing`/`_mods`/`## Hook: startup` grammar/`TeamDelete`/SendMessage move to the Claude adapter). AC-2 now names the three exit-1 conditions grounded in the oracle's `cmd_context_budget` (missing jsonl / unreadable-or-empty jsonl / agent-not-in-team-config), each uniformly fail-safe fresh-dispatch, each parity-tested. Fixed the SPIKE-C polish note (proves FEATURE boundary, not PACKAGE boundary — the latter is AC-3's separable oracle). Stated the implementation-stage worktree disposition (worktree-backed, CODE-only under split-root). Re-affirmed the independent-staff-review recommendation with three cycle-2 focuses.

### Summary (cycle 2)

The cycle-1 design's thesis was correct but AC-3 was unsatisfiable: three pre-existing `~/.claude` reads (`probeTeamState`, `recentTeamEvidence`, `build.go`'s WARN literal) already sit in the generic `internal/status`/`internal/dispatch` packages, so the code-side oracle is RED before any subcommand work. Cycle 2 folds their relocation behind a host-supplied team-state probe into Decision 2 + AC-3, making the invariant achievable. Recommended decomposing the host-neutrality seam into a prerequisite entity that lands first (validate the riskiest contract path before five subcommands stack on it). Sharpened AC-2 to three named oracle-grounded budget-unavailable conditions, rewrote `## Standing Teammates` as a concept-vs-mechanics cleave, corrected every off-by-one line citation against the real file, and fixed the SPIKE-C feature-vs-package framing. No worktree (state-checkout ideation); no code shipped this stage.

## Stage Report: implementation

- DONE: Reimplement the four runtime-coupled subcommands (context-budget, list-standing, show-standing, spawn-standing) native in internal/claudeteam (the seam package landed via #244) + extend internal/dispatch/build.go's _mods/show-standing fetch-line branch — each at PARITY with the vendored Python oracle via the extended three-channel parity harness (success AND loud-failure paths: missing jsonl, missing mod, missing standing:true, bad model enum).
  context-budget + MemberExists live in internal/claudeteam (the ~/.claude reads); the runtime-neutral _mods parsing + list/show/spawn orchestration in internal/dispatch (no .claude literal). All routed through dispatch.Run. Parity harness extended over a fixture ~/.claude tree (contextbudget_parity_test.go: success + 3 exit-1 paths) and _mods fixtures (standing_parity_test.go: list/show success+empty, spawn spec-emit/already-alive + 6 loud-failures incl missing-mod/not-standing/missing-prompt/missing-model/bad-enum/trailing-heading, build _mods branch). SHA 3a108754.
- DONE: Fold in AC-4: the context-budget 1M opus-family-rule (opus minor >=7 -> 1M; [1m] suffix -> 1M; else 200k) in BOTH the native impl and the vendored Python oracle so parity holds — kills the claude-opus-4-8 false-negative.
  Forward family rule (regex `^claude-opus-4-(\d+)`, minor >= 7) in claude-team `context_limit_for_model` and native `contextLimitForModel`. Behavioral oracle TestContextBudget1MFalseNegativeGone: opus-4-8 (no suffix) at 159k resident reads context_limit 1_000_000 / usage_pct 15.9 / reuse_ok true; boundary table TestContextLimitForModelBoundary covers 4-6/4-7/4-8/4-10/4-100/sonnet/haiku. SHA 3a108754.
- DONE: AC-3 contract reorg per zs Decision 2 (corrected citations): relocate the Claude-only leaks in first-officer-shared-core.md (reuse-condition-0 ~142, feedback-rejection ~206, member_exists ~117, list-standing ~325 + reuse-condition-4 lookup_model ~146) into claude-first-officer-runtime.md + rewrite ## Standing Teammates as concept-vs-mechanics; keep the seam's code-side host-neutrality oracle GREEN; add a direct internal/claudeteam unit test. go test ./... green except pre-existing env codex; gofmt/vet clean.
  Prose-side oracle (prose_neutrality_test.go) RED on the 8 unqualified leaks pre-relocation, GREEN after; code-side TestNoClaudeHomeReadsInGenericPackages stayed GREEN throughout; direct claudeteam unit test (contextbudget_test.go: boundary table + 20.0 pyFloat envelope + team-scoped MemberExists). Adapter context-budget/list-standing/spawn-standing moved to `spacedock dispatch` form + gained a declaration-and-routing-mechanics subsection. SHA d06c6a00. go test ./...: 580 passed, 1 failed (TestCodexResolveManifestAgainstInstalledHost — the codex host CLI itself errors `failed to load configuration` in this env, touches none of my files).

### Summary

Landed the five runtime-coupled subcommands native behind the dispatch surface and reorganized the FO operating contract to be host-neutral, building on the #244 seam (not recreating it). context-budget + the MemberExists/team-config reads sit in internal/claudeteam (the Claude seam); the runtime-neutral _mods parsing and the list/show/spawn orchestration sit in internal/dispatch (zero `.claude` literal, so the code-side oracle stays green); show-standing's Claude SendMessage render is a data-driven function in claudeteam. The AC-4 1M opus family rule landed in both the native impl and the vendored Python oracle, killing the claude-opus-4-8 false-negative. AC-3 relocated all Claude-only helper tokens out of the generic core into the Claude adapter — verified by a new prose-side markdown-structure oracle that went RED on all 8 leaks (incl. the reuse-condition-4 `lookup_model` the cycle-2 leak table under-counted) and GREEN after. Every subcommand is byte-for-byte parity-tested native-vs-Python over fixture `~/.claude` and `_mods` trees on success AND loud-failure paths. The steady-state FO loop (per-reuse budget check, feedback-rejection, standing spawn) is now Python-free. go test ./... green except the pre-existing env codex host test; gofmt/vet clean.

### Deliberate existing-test inversion (FO-acknowledged)

The prerequisite seam (#244) shipped `internal/dispatch/no_native_subcommands_test.go` as its AC-P3 deliverable: an EXPLICIT "until this entity lands them" placeholder guard. The seam established only the `internal/claudeteam` package + the team-state probe — no subcommands — so the guard asserted the temporary deferral.

- **What the guard asserted (deleted/inverted):** `TestDeferredSubcommandSetUnchanged` pinned the `deferredSubcommands` set to exactly `{context-budget, list-standing, show-standing, spawn-standing}`; `TestBuildEmitsNoStandingFetchLine` asserted build emits ONLY the show-stage-def fetch line even with a `_mods/` standing teammate present. `internal/dispatch/guard_test.go::TestDeferredSubcommandGuard` asserted each of the four subcommands exits non-zero with a `claude-runtime-segregation`-naming deferral diagnostic.
- **Why obsolete-by-design:** this entity's charter IS to land those four native subcommands + the build `_mods`/show-standing branch. The guard's own docstrings name `claude-runtime-segregation` as the entity that removes the deferral. Once the subcommands route native, every assertion in those guards is false by intent, not by regression.
- **What they became (coverage preserved, not dropped):** `no_native_subcommands_test.go` → renamed `native_subcommands_routing_test.go` with `TestRuntimeCoupledSubcommandsRouteNative` (the four subcommands route to native handlers — invoked flagless they return the native usage error, NOT the deferral diagnostic) + `TestBuildEmitsStandingFetchLineUnderMods` (build emits BOTH show-stage-def AND `spacedock dispatch show-standing` under a `_mods` standing teammate) + `TestBuildOmitsStandingFetchLineWithoutMods` (the branch is conditional). The obsolete `TestDeferredSubcommandGuard` was removed from `guard_test.go`; its sibling `TestUnknownSubcommandGuard` + `TestRequiredFlagGuard` (still valid) stay. Net: the routing table and the build-branch conditionality are still pinned by behavioral tests, now asserting the positive end state.

### Other deliberate deviation

Folded reuse-condition-4 `lookup_model` into the AC-3 relocation though the cycle-2 leak table (Decision 2) omitted it. The AC-3 oracle token list explicitly includes `lookup_model`, so leaving it unqualified at shared-core line 146 would have failed the invariant. Rewrote condition 4 to "resolve the worker's model through the runtime's model-for-member lookup" (capability) and documented the concrete read in the Claude adapter's context-budget section.

## Stage Report: validation

- DONE: INDEPENDENTLY reproduce the 5 ACs on the built branch binary (AC-1 four subcommands route NATIVE + byte/struct-parity with the vendored Python oracle on success AND loud-failure paths; AC-4 1M opus-family-rule in BOTH oracles; AC-2 budget-unavailable fail-safe on the three exit-1 conditions).
  Built `cmd/spacedock` from the worktree (SHA d06c6a00). Out-of-band (not the in-process harness): ran the built binary vs `python3 skills/commission/bin/claude-team` over a hand-built fixture ~/.claude tree — context-budget opus-4-8/opus-4-6, all three exit-1 paths, list/show/spawn-standing (spec-emit, already-alive, 5 loud failures), and the build _mods branch — every channel (stdout/stderr/exit) byte-identical after the documented `claude-team`→`spacedock dispatch` fetch rewrite. context-budget opus-4-8 no-suffix at 159k → context_limit 1_000_000 / usage_pct 15.9 / reuse_ok true. The three AC-2 conditions each exit 1 with no `reuse_ok` (fail-safe). The extended parity harness (contextbudget_parity_test.go + standing_parity_test.go + build_parity_test.go) is a genuine three-channel test driving real python3 exec, not mocks — all green.
- DONE: AC-3 host-neutrality both oracles green and FALSIFIABLE; all leaks relocated incl reuse-condition-4 lookup_model (9th); test-inversion covers what the removed no_native_subcommands_test.go guarded.
  Both oracles GREEN. FALSIFIED both: injected `$HOME/.claude/teams` literal into internal/dispatch/helpers.go → code-side TestNoClaudeHomeReadsInGenericPackages RED (named file:line); injected an unqualified `claude-team context-budget` reuse step into shared-core → prose-side TestSharedCoreHasNoUnqualifiedClaudeHelpers RED (flagged both claude-team + context-budget tokens); reverted both, GREEN again, worktree clean. Raw grep confirms the generic core's ONLY remaining Claude-helper token is `context-budget` at line 142, inside an explicitly Codex-qualified span (`Codex declares none; Claude supplies one`); claude-team/spawn-standing/list-standing/show-standing/member_exists/lookup_model all absent (relocated to the adapter). reuse-condition-4 rewritten to "runtime's model-for-member lookup" (capability), documented at adapter line 179. Test-inversion sound: TestDeferredSubcommandSetUnchanged→TestRuntimeCoupledSubcommandsRouteNative (same 4 route native, exit-2 native-usage not "deferred"); TestBuildEmitsNoStandingFetchLine→TestBuildEmitsStandingFetchLineUnderMods + TestBuildOmitsStandingFetchLineWithoutMods (stronger: both branches pinned). `deferredSubcommands` symbol fully gone; TestUnknownSubcommandGuard + TestRequiredFlagGuard retained. No coverage lost.
- DONE: go test ./... green except pre-existing env codex; gofmt/vet clean; Python-oracle 1M edit did not break other oracle parity; relocated commands read coherently in the adapter and the generic core is host-neutral.
  `go test ./...`: only failure is TestCodexResolveManifestAgainstInstalledHost — verified pre-existing (fails identically on the main checkout: `codex` host CLI errors `Failed to read config file ~/.codex/config.toml: Operation not permitted`), env-gated, touches none of this entity's files; internal/cli passes with it skipped. gofmt -l clean; go vet clean. The oracle's `context_limit_for_model` is consumed only by cmd_context_budget (no other subcommand), and the full Parity suite (build/showstagedef/cycle2/contextbudget/standing) is green — the 1M edit broke no other parity. Adapter coherently owns context-budget/list-standing/show-standing/spawn-standing + the model-for-member read; the generic core's Standing Teammates section is a clean concept-vs-mechanics cleave.

### Summary

PASSED. Independently reproduced all five ACs against the built branch binary — not the report, not only the in-process harness: ran the binary head-to-head with the vendored Python oracle over hand-built ~/.claude and _mods fixtures and got byte-identical stdout/stderr/exit on every subcommand, every success path, and every loud-failure path (after the one documented fetch-prefix rewrite). The AC-4 opus-4-8 1M false-negative is gone in both the native impl and the Python oracle (boundary table reproduced in Python directly: 4-6→200k, 4-7/4-8/4-10/4-100→1M, sonnet/haiku→200k; no residual exact-name set). AC-3's two structural oracles are both green AND genuinely falsifiable (each goes RED when I re-introduce the exact leak it guards, GREEN on revert). The deliberate test-inversion preserves coverage (the positive end-state replaces the temporary deferral guard, with the build branch now pinned by two tests) and the reuse-condition-4 lookup_model fold is correct (the AC-3 token list requires it). Full suite green except the pre-existing env-gated codex host test; gofmt/vet clean.

NON-BLOCKING (recommend a follow-up, does NOT affect any AC): `internal/dispatch/build.go` carries two now-stale comments describing the pre-implementation state — the ABOUTME line 2 (`...matching the vendored claude-team build oracle (non-_mods)`) and the `runBuild` docstring lines 59-63 (`Scoped to non-_mods workflows: it emits the single show-stage-def fetch line and never the standing-teammate fetch line. Matches cmd_build minus the _mods/standing branch (deferred to the sibling claude-runtime-segregation)`). This entity IS claude-runtime-segregation and landed the _mods/show-standing branch (build.go:384, verified emitting the fetch line in-process and out-of-band), so these comments now contradict the code and violate the evergreen-comment rule. Behavior is correct and tested; only the comments are stale.

## Stage Report: implementation (cycle 2)

Rework driven by the adversarial audit (CONCERNS): [A-1] non-ASCII JSON byte-parity divergence + [A-2] the coverage gap that let it ship. The audited-clean cycle-1 work (contract reorg, 1M family rule, subcommand routing, test-inversion, host-neutrality oracles) was left untouched.

- DONE: [A-1] Fix the non-ASCII JSON parity divergence with ONE escape-non-ASCII helper applied to all three indent=2 emitters.
  New `internal/claudeteam/pyjson.go`: `EmitPythonJSON` (encoder + `EscapeNonASCII`) reproduces Python `json.dumps(v, indent=2)+print()` byte-for-byte, escaping every rune Python's `ensure_ascii=True` escapes but Go's encoder leaves raw — exhaustively verified as exactly DEL (U+007F) + all >= U+0080, surrogate pairs above the BMP. Wired into `contextbudget.go` emitJSON, `build.go` emitBuildJSON, `standing.go` emitSpawnJSON; the comments now truthfully say "matching Python json.dumps … including its ensure_ascii escaping". Also routed the spawn-standing already-alive `name` (a `json.Marshal` sibling that diverged identically) through `EscapeNonASCII`. The mixed_models_warning em-dash was a LIVE (not latent) divergence — its fixed text carries U+2014.
- DONE: [A-2] Close the coverage gap with non-ASCII parity cases, each red-without-fix / green-with-fix verified.
  `TestContextBudgetWarningNonASCIIParity` drives BOTH the mixed-models and config-fallback warning paths (each warning's fixed text carries an em-dash); `TestSpawnStandingParitySpecNonASCIIPrompt` drives spec-emit with an em-dash Agent Prompt (the FO forwards spec.prompt verbatim); `TestBuildParityNonASCIITitle` drives build with an em-dash entity title (lands in `description`). Each asserts native==Python BYTE-identity via the three-channel harness AND a guard that the raw bytes carry `—` (not a raw em-dash), so the parity assertion can't pass by comparing raw==raw. `TestEscapeNonASCIIMatchesPythonEnsureASCII` unit-tables the helper against the live python3 over em-dash/smart-quotes/arrow/accent/astral-emoji/DEL/NBSP/tab-nl/HTML-chars. Confirmed each parity test goes RED when its emitter's fix is reverted, GREEN on restore.
- DONE: Full gates green; built-binary out-of-band confirmation.
  `go test ./...`: 586 passed, 1 failed — only `TestCodexResolveManifestAgainstInstalledHost`, verified pre-existing (fails identically at committed HEAD d06c6a00 without my changes: env codex host CLI errors "failed to load configuration"), env-gated, touches none of my files. The full parity+non-ASCII suite (contextbudget/standing/build/escape) green: 96 passed across dispatch+claudeteam. Host-neutrality oracles still green (3 passed) — the new helper is in claudeteam, no `.claude` literal entered internal/dispatch. gofmt -l clean; go vet clean. Out-of-band: built `cmd/spacedock` and ran it head-to-head with the vendored Python oracle on the mixed-models path — byte-identical, em-dash escaped to `—`.

### Summary (cycle 2)

The audit's A-1 was correct and slightly broader than cited: the three indent=2 emitters AND the spawn-standing already-alive `json.Marshal` name all emit raw UTF-8 where Python's `json.dumps` (default `ensure_ascii=True`) escapes to `\uXXXX`, and the mixed_models_warning divergence is live today (em-dash in fixed text), not merely latent. One helper, `EmitPythonJSON`/`EscapeNonASCII` in `internal/claudeteam/pyjson.go`, now drives all four sites; an exhaustive rune scan proved the escape set is exactly DEL + all non-ASCII, with correct surrogate pairs for astral chars, so the "matching Python json.dumps" comments are now unconditionally true. A-2's coverage gap is closed with three byte-identity parity cases (context-budget both warnings, spawn-standing prompt, build title) plus a helper unit table, each verified red-without-fix. This is the BOOTSTRAP fix (match the oracle while it exists); the deliberate raw-UTF-8 move is a documented POST-oracle item for parsing-modernization, NOT done here. Full suite green except the pre-existing env-gated codex host test; gofmt/vet clean.

## Stage Report: validation (cycle 2)

Re-validation of the A-1/A-2 fix (commit 112acfa8) after the prior cycle-1 PASSED + adversarial-CLEAN verdict. Scope per dispatch: ONLY the non-ASCII byte-parity restoration and the fix's additivity; the cycle-1 contract reorg / 1M rule / subcommand routing / test-inversion / host-neutrality were NOT re-litigated.

- DONE: INDEPENDENTLY confirm the A-1 non-ASCII byte-parity is RESTORED across all four emitters + RED-without-fix spot-check.
  Built `cmd/spacedock` from the worktree (HEAD 112acfa8) and ran the binary head-to-head with `python3 skills/commission/bin/claude-team` OUT-OF-BAND (not the in-process harness) over hand-built `~/.claude` + `_mods` fixtures: context-budget mixed-models warning, context-budget config-fallback warning, spawn-standing spec-emit (em-dash Agent Prompt = the FO-forwarded `spec.prompt`), spawn-standing already-alive (em-dash member name), and build (em-dash entity title → `description`). All five `cmp`-byte-identical native==oracle (after the documented `claude-team`→`spacedock dispatch` fetch rewrite); the em-dash escaped to `—` in BOTH, no raw em-dash in either. Spot-check: reverted `internal/claudeteam` `emitJSON` to the raw encoder → `TestContextBudgetWarningNonASCIIParity` went RED on both subtests with the exact A-1 divergence (native raw `—` vs oracle `—`, escape-guard firing); restored → GREEN; worktree clean (`git status --porcelain` empty, HEAD unchanged).
- DONE: Confirm the cycle-1 audited-clean work is UNTOUCHED by the fix (additive only).
  `git diff --name-only d06c6a00..112acfa8` is exactly 8 files: the new `internal/claudeteam/pyjson.go` + `pyjson_test.go`, the four emitter call-sites (`contextbudget.go`, `build.go`, `standing.go`), and three parity tests (`build_parity_test.go`, `contextbudget_parity_test.go`, `standing_parity_test.go`). NO contract reference file (`first-officer-shared-core.md`/runtime adapters), no `context_limit_for_model`/family-rule code, no routing-table test, no prose/code host-neutrality oracle, no boot.go/helpers.go seam. Per-file hunk headers confirm the source edits are ONLY the three emitter functions + the one already-alive `json.Marshal` name escape — no stray logic edits.
- DONE: go test ./... green except the pre-existing env-gated codex test; full parity harness green incl new non-ASCII cases; host-neutrality code-side oracle still green; gofmt/vet clean.
  `go test ./...`: only failure is `TestCodexResolveManifestAgainstInstalledHost` — verified pre-existing (fails IDENTICALLY on the main checkout: `Failed to read config file ~/.codex/config.toml: Operation not permitted`), env-gated, touches none of this entity's files; `internal/cli` passes with it skipped. Full parity+escape suite (dispatch+claudeteam) green; the four A-1 cases (`TestBuildParityNonASCIITitle`, `TestContextBudgetWarningNonASCIIParity` ×2 subtests, `TestSpawnStandingParitySpecNonASCIIPrompt`) + the live-python3 helper unit table (`TestEscapeNonASCIIMatchesPythonEnsureASCII`) all PASS. Host-neutrality oracles all GREEN (`TestNoClaudeHomeReadsInGenericPackages` confirms the helper landed in `internal/claudeteam` with no `.claude` literal entering `internal/dispatch`/`internal/status`; prose-side + adapter-ownership also green). gofmt -l clean (changed files + whole tree); go vet exit 0.

### Summary (cycle 2)

PASSED. The A-1 non-ASCII byte-parity is independently confirmed RESTORED on the built branch binary across all four emitters: ran the binary head-to-head with the vendored Python oracle out-of-band over fixture `~/.claude` and `_mods` trees and got byte-identical stdout/stderr/exit on every non-ASCII path the dispatch named (context-budget both warnings, spawn-standing spec prompt, spawn-standing already-alive name, build title), with the em-dash escaped to `—` in BOTH and no raw em-dash in either. The RED-without-fix claim holds: reverting the emitter's `EmitPythonJSON` route makes the parity test fail on exactly the em-dash divergence, GREEN on restore, worktree clean. The fix is additive — `git diff d06c6a00..112acfa8` is precisely the new `pyjson.go` helper + its unit test + the four emitter call-sites + three parity tests, with no touch to the cycle-1 contract reorg, 1M family rule, subcommand routing, test-inversion, or host-neutrality oracles (per-file hunk headers verified). Full suite green except the pre-existing env-gated codex host test; the full parity harness (incl. the new non-ASCII cases) green; the code-side host-neutrality oracle still green (helper is quarantined in `internal/claudeteam`); gofmt/vet clean.

The NON-BLOCKING build.go stale-comment follow-up noted in the cycle-1 validation report is unchanged by this fix (build.go's ABOUTME line 2 + the `runBuild` docstring still describe the pre-_mods-branch state) — still non-blocking, still a recommended follow-up, does not affect any AC.
