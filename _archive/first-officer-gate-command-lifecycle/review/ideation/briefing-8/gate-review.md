# Ideation gate: one truthful unreleased-v1 gate path

Capability/change: Remove prototype marker and unbound-review behavior. A gate becomes reviewable only after a canonical Briefing is bound and committed; without decision authority the First Officer presents it once and holds the open attempt.

Test and evidence: Existing structured host command events are paired with the existing wrapper’s successful exit and state HEAD, so narration or failed commits cannot prove durability. Independent controls mutate the exact Briefing tuple and opaque delegated directive. Codex’s corrected journey remains green; Claude and Pi must pass unchanged supported journeys with complete retained evidence.

Reviewed snapshot: state commit `e11471a2`; local product tip `08675f02` remains unpushed; PR #565 remains at remote tip `13d70249`.

Findings:

- Material: none after independent staff review.
- Deferred: Pi CLI/package skew needs live proof; repeated commands require ordered stream-to-wrapper matching plus open-state HEAD; `+510` is a ceiling.
- Polish: describe the existing Pi package root as non-mutating and compatibility-probed, not filesystem read-only.

Recommendation: approve. The 17-file surface removes nine concrete prototype assumptions and reuses the existing wrapper; it adds no compatibility layer, recorder field, second harness, host lifecycle, or eighteenth file.

Decision ask: approve to repair the existing branch within `+297/-160` incremental intent and a `+510` total-additions hard stop; revise to change the v1 path or proof boundary; hold for a named prerequisite.
