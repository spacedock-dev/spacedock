# Ideation gate: preserve host-specific proof and retain Pi failures

Capability/change: Keep one common consume/build/dispatch-observed/descendant-effect contract while letting each host supply only the observation its public surface supports. Make exact delegated authority lossless JSON data and require Pi evidence to persist outside test cleanup before launch.

Test and evidence: Codex uses successful build, completed-wait ordering, and descendant durable effect without claiming worker completion. Claude/Pi retain richer matched events where exposed. Deterministic controls cover JSON escaping, symlinked cleanup roots, invalid retention destinations, ordered ancestry, and host-specific failure cases before one Pi run.

Reviewed snapshot: state commit `716c7d72`; local product tip `ce436505` remains unpushed; PR #565 remains at remote tip `13d70249`.

Findings:

- Material: none after independent staff repair.
- Deferred: resolve symlinks before containment checks; call the common result dispatch observation; do not infer exact-child authorship from Codex Git ancestry.
- Polish: the expected delta belongs to Cycle 25, not Cycle 24.

Recommendation: approve. The eight-file repair restores the binding Cycle-9 proof split, fixes evidence retention, and requires deletion before expansion without adding a second harness or production machinery.

Decision ask: approve to implement after cumulative additions fall to `≤370`, with final target `≤455`, tolerance `≤465`, and hard stop `+480`; revise to change the proof split; hold for a named prerequisite.
