---
title: Post-compaction contract reload
status: done
source: Absorbed from task njr36mfyhbafy8zx9ydks8ep in another workflow; canonical handoff /tmp/first-officer-compaction-rehydration.md; captain directed repo-local absorption 2026-07-11
started: 2026-07-11T04:15:29Z
completed: 2026-07-20T02:16:30Z
verdict: passed
score: 0.95
worktree: .worktrees/spacedock-ensign-codex-post-compaction-contract-reload
issue:
id: c60nzb396vgf0f8a9v0sggwm
milestone: 0.26.0
mod-block:
gates:
    version: 1
    records:
        - id: gate:docs-dev:c6:validation
          stage: validation
          attempts:
            - id: gate-attempt:c6-validation-1
              briefing:
                id: briefing:docs-dev:c6:validation:attempt-1:revision-1
                digest: sha256:9cbd614ab8fd3f61095db3373c958dbb3f1bef441f35807a1260c43dd9d99dfe
                room-ref: codex-thread:019f7007-8fba-7503-8c44-5ebf9a7cc945
              resolution:
                type: Resolution
                id: resolution:captain:c6:validation:attempt-1
                briefing: briefing:docs-dev:c6:validation:attempt-1:revision-1
                by: person:captain
                at: 2026-07-19T09:34:34Z
                decision: approve
                reason: Accept the shipped boundary; experience host continuation live instead of adding a synthetic harness.
                includes: []
              application:
                action: advance
                target-stage: done
                state: pending
                blockers: []
                execution-hold:
                    id: hold:captain:c6:validation:attempt-1
                    state: active
                    by: person:captain
                    at: 2026-07-19T09:34:34Z
                    reason: Do not apply, push, or open a PR; record the decision first.
pr: pr-merge:531
archived: 2026-07-20T02:16:30Z
---

## Decision

Ship two host-neutral advisory rules and no continuation controller:

1. When context pressure is apparent, the first officer may suggest compaction only after current workflow state is already durable at a clean boundary.
2. After the host compacts, the first officer rereads the authoritative `spacedock:first-officer` contract and reconciles durable workflow and live worker state before the next workflow effect.

Both rules are first-officer judgment rules at the shared-core level, inherited by every runtime adapter; neither depends on a host mechanism. What IS host-specific is DELIVERY of the post-compaction reminder — abstracted as a per-host binding `«post-compact-notice»` that mirrors how the contract already treats `«context-budget»` (PRESENT/ABSENT per host). The binding is failure-open: when it cannot reach the model, a manual captain cue triggers Rule 2 and nothing is blocked.

These rules do not authorize actions, block actions, reconstruct a session, or make summaries authoritative. The existing workflow files, committed Stage Reports, state-checkout history, live worker roster, and first-officer contract remain the sources the FO normally reconciles. The superseded ledger, authorization grant, permits, action gateway, crash-replay controller, interception matrix, and watchdog design is preserved at [artifacts/superseded-controller-design.md](artifacts/superseded-controller-design.md), not repaired.

## Problem

Host compaction can preserve enough narrative to continue while dropping an operating detail such as the current wait contract or the need to consume a worker's durable report. The useful intervention is a timely reminder at each side of the boundary, not a second workflow state machine. This is true on every host, not only Codex.

A pre-compaction suggestion is safe only when the current boundary can be recovered without relying on conversational memory. For this task, that means:

- workflow/entity/report changes already made by the FO are committed in the state checkout;
- assigned code or report work already claimed complete is committed in its owning worktree or state path;
- no received completion, gate decision, state transition, archive, merge, or other FO-owned effect is half-applied or awaiting reconciliation; and
- any unresolved workers can be rediscovered from the live roster and their durable entity stage/cycle, rather than only from prose in the conversation.

If any term is false, the FO finishes that durability or reconciliation work first and does not recommend compaction yet. This is a judgment rule in the FO contract, not a new persisted `compaction_ready` field.

After compaction, the generated summary is useful history but not the contract. The FO must reread the active plugin's authoritative first-officer contract and use the existing boot/status/roster/report reconciliation flow before taking another workflow action. The **authoritative contract to reread is identical on every host**: the `spacedock:first-officer` `SKILL.md`, its eager import `@references/first-officer-shared-core.md`, and the active host runtime adapter (`claude-`/`codex-`/`pi-first-officer-runtime.md`). Rereading those three re-triggers the normal boot, which reloads the deferred dispatch/write/merge modules on demand. A captain cue such as "we compacted" or "reread the FO contract" is sufficient to trigger this behavior even when no host delivery binding is available.

## Proposed approach

### Rule 1: safe-to-compact (host-neutral FO judgment rule)

Add a short first-officer rule at the shared-core level:

> On hosts that surface context-pressure hints, offer manual compaction at a durable boundary, and with the offer recommend how to file what is not yet durable — unrecorded decisions and captain directives, in-flight findings, conversation context worth keeping — into state commits, entity bodies, or debrief/handoff notes, not limited to workflow objects. Dense durable boundaries keep unhinted auto-compaction survivable.

The **trigger is equally weak on all hosts today**: none exposes the FO's own session pressure programmatically (the Claude/Codex context-budget probes measure a dispatched *worker's* window, not the FO session). So this rule leans on a captain cue or apparent host pressure on every host. This task does not invent token thresholds or inspect transcripts. The separate context-budget work may later provide a better signal without changing this safety rule.

The simplest alternative was to suggest compaction whenever the host reports pressure. It is insufficient because timing, not detection, is the value: the same reminder is harmful between a worker completion and report verification or during an uncommitted state transition. (Ships the contract shape checked by AC-1; the live safe-timing value is the out-of-scope live-scenario followup.)

### Rule 2: post-compact reload (host-neutral FO judgment rule)

Add the corresponding FO rule at the shared-core level:

> After a compaction (harness notice or captain cue): the summary's claim of having read or done is not the reading or the doing — re-satisfy each load precondition and state read at its existing trigger before the next workflow effect.

The simplest alternative was to keep this Codex-specific. It is insufficient because the rule names no host mechanism — it is FO judgment over already-proven reads (committed state, `status`, the live roster, durable reports) — so scoping it to one host would leave the other hosts with the same failure and no rule. (Ships in the same `## Compaction continuity` rule checked by AC-1; the live reload value is the out-of-scope live-scenario followup.)

### Post-compaction reload delivery: harness-owned event, no FO capability

DELIVERY of the Rule 2 reminder is NOT an FO-invokable capability. The real control flow is harness-owned: the harness compacts → the harness runs the plugin's `PostCompact` hook → the hook emits a captain-visible UI `systemMessage` (live 0.144.4 probe: NOT model context — [artifacts/codex-0.144.4-hook-probe.md](artifacts/codex-0.144.4-hook-probe.md)) → the captain cues the FO → the FO's shared-core reactive rule (Rule 2) rereads and reconciles. The FO cannot invoke a post-compaction hook, so the shared core's reactive rule is the only FO-side prose; per host:

- **Codex:** the bundled `.codex-plugin` `PostCompact` hook (`manual|auto`, `${PLUGIN_ROOT}/hooks/codex_post_compact_notice.sh`) ships as a harness integration, noted factually in the Codex adapter's residual `## Captain Interaction` prose — not a binding bullet. Failure-open: absent, disabled, untrusted, or failing, a manual captain cue ("we compacted" / "reload the FO contract") triggers Rule 2 and nothing is blocked.
- **Claude:** no shipped hook; a `SessionStart(compact)` model-context path is tracked by `claude-post-compaction-contract-reload` (id `cdbhzxc`) in the entity registry — under this architecture also a harness integration, not a capability. Until it ships, the manual captain cue.
- **Pi:** no known compaction-hook surface; the manual captain cue.

The Codex hook's `systemMessage` is exactly this reminder and the hook performs no writes:

> Spacedock: compaction completed. Before continuing, ask the first officer to reread the authoritative `spacedock:first-officer` contract and reconcile durable workflow and live worker state.

The superseded alternative — a `«post-compact-notice»` capability declared in `fo-dispatch-core.md` and bound per-host like `«context-budget»` — was removed in cycle 7: it inverted the harness-owned control flow (the FO cannot fire a post-compaction hook), and its declaration sat in a deferred-load module unread before first dispatch AND absent from the post-compaction reread list — unread on both sides of the event it purported to govern.

### Shared prerequisite: hook-shipping surface

Both the Codex binding (this task) and the Claude binding (`cdbhzxc`) need a plugin hook-shipping surface the plugin does not have today: neither `.claude-plugin/plugin.json` nor `.codex-plugin/plugin.json` declares a `hooks` key (verified 2026-07-18 — a `grep -rl hooks` over both manifests returns nothing). This task's Codex binding establishes the `.codex-plugin` hooks surface for its `PostCompact` hook; the split task owns the `.claude-plugin` surface for `SessionStart(compact)`. The prerequisite's shape is shared even though each host ships its own hook.

### Spike status

- **Codex binding delivery behavior — spiked and proven.** The riskiest delivery claim (does the post-compaction reminder reach the model?) was exercised first in a live Codex 0.144.4 TUI on 2026-07-17: manual `/compact` fired `PreCompact`/`PostCompact`; a `PostCompact` `systemMessage` appeared as one visible warning but the next model turn answered `NONE`, and `SessionStart(compact)` did not fire same-session. On this client, `systemMessage` is a captain/UI reminder, not developer context. Full probe: [artifacts/codex-0.144.4-hook-probe.md](artifacts/codex-0.144.4-hook-probe.md). The design therefore makes the Codex binding failure-open with a manual-cue fallback and does not claim automatic model instruction.
- **Codex binding remaining unverified path — implementation's first spike.** The probe used an inline, trusted hook (`--dangerously-bypass-hook-trust`), not a plugin-bundled one. Whether `.codex-plugin/plugin.json` can declare a `hooks` key that Codex loads and trusts is the Codex binding implementation's first spike, ahead of the fixture matrix.
- **Host-neutral Rules 1 and 2 — no spike needed.** They rely only on already-proven FO mechanisms: committed state reads, `spacedock status`, live-roster reconciliation, and durable Stage Report reads. No new mechanism underlies the judgment rules themselves.

## Acceptance criteria

The end value this entity exists for — a live FO that suggests compaction only at a durable boundary and rereads its authoritative contract after compaction — is a judgment-rule property with no controller BY DESIGN; it is not honestly provable by an offline fixture, and it is named as an out-of-scope live followup (see Out of scope). AC-1 and AC-2 are therefore CONTRACT-SHAPE requirements — the means: the rule ships in the required shape and the shipped contract carries no FO-invokable post-compaction capability. The value they are paired with is measured offline by AC-3 (the Codex delivery actually emits the reminder) and AC-4 (the shipped stdout-only hook script is inert — it writes nothing and mutates nothing when run in isolation).

- **AC-1 — compaction-continuity section ships in the captain-shaped contract form:** The `## Compaction continuity` section ships in `skills/first-officer/references/first-officer-shared-core.md` as an unqualified heading, the single lede "The compacted summary is never authoritative.", and two bullets — (a) a Before offer, conditional on hosts that surface context-pressure hints, of manual compaction at a durable boundary, carrying the recommendation to file what is not yet durable (unrecorded decisions and captain directives, in-flight findings, conversation context worth keeping) into state commits, entity bodies, or debrief/handoff notes, not limited to workflow objects; and (b) a one-sentence After rule (harness notice or captain cue) that re-satisfies each load precondition and state read at its existing trigger before the next workflow effect, because the summary's claim of having read or done is not the reading or the doing. The section is host-neutral — it names no host ("hosts that surface context-pressure hints" is capability-conditional, not a host name) — and the shared-core stays within its byte ratchet ceiling. **How tested:** by REVIEW, not a committed test. AC-1 has NO committed Go test: a committed test that greps the shipped shared-core `.md` for prose is the false-green anti-pattern regardless of which phrases it greps — it verifies file content, not behavior. Instead the reviewer/FO greps the shipped `first-officer-shared-core.md` at validation and cites the `## Compaction continuity` section and its host-neutrality (`\b(Codex|Claude|Pi)\b` = 0 hits inside the section) as evidence; the only committed gate over the file is the existing shared-core byte-ceiling ratchet (`internal/contractlint` `startup_collapse`). This AC asserts the shipped CONTRACT SHAPE only; it makes NO runtime-behavior claim (no "the FO produces exactly one suggestion", no "the next effect occurs only after reads"). The behavioral linkage from this prose to a live FO is an unenforced judgment-rule property (two judgment rules, no controller); its proof is the live-scenario followup in Out of scope — no committed test asserts it here.
- **AC-2 — no FO-invokable post-compaction capability; compaction is a harness-owned event:** The shipped FO contract carries no `«post-compact-notice»` capability anywhere — no `## «post-compact-notice»` block in `fo-dispatch-core.md`, no binding bullet in any adapter's `## Runtime implementation`, no `docs/runtime-support.md` shape-list entry, no Claude binding section. The harness owns the event: the Codex `PostCompact` hook is a harness integration whose facts live in the Codex adapter's residual `## Captain Interaction` prose (captain-visible UI `systemMessage`, not model context, degrading to a manual captain cue; failure-open), and the shared core's reactive after-compaction rule is the only FO-side prose. **How tested:** mechanically by the two shrunk contractlint capability lists — `runtimeBoundCapabilities` (`capability_binding_test.go`) at `{worker.spawn, worker.shutdown}` and `runtimeBindingCapabilities` (`runtime_binding_block_test.go`) without the bullet, whose `reflect.DeepEqual` exact-equality checks against BOTH adapters' `## Runtime implementation` bullet lists enforce the bullet's ABSENCE — plus a review-time grep over the 13 FO-surface files (zero `post-compact-notice` occurrences) cited as evidence at validation; no committed prose-grep test.
- **AC-3 — Codex delivery binding, visible warning:** On the target Codex client with the bundled hook trusted, one manual `/compact` produces exactly one visible warning containing the required reread-and-reconcile instruction after compaction completes. The hook configuration matches both `manual` and `auto`; a command-level fixture drives both event payloads and asserts the output SHAPE only — exactly one JSON key, `systemMessage`, with a nonempty string value, identical across the manual/auto sources and across session cwds; the message WORDING is quoted at review time as evidence, not asserted by a committed test. The fixture also asserts the hook command resolves via `${PLUGIN_ROOT}` and fires from an unrelated session cwd (the cwd-relative `./hooks/` form is proven NOT to resolve there), matching the live Codex 0.144.6 spike. The test does not claim the warning enters model context. This AC proves only the Codex binding; the Claude auto-inject AC lives in `claude-post-compaction-contract-reload` (`cdbhzxc`).
- **AC-4 — shipped hook is inert (offline scope):** The shipped Codex `PostCompact` hook script (`hooks/codex_post_compact_notice.sh`) is a stdout-only notice that writes nothing and mutates nothing. Run in isolation with every reachable filesystem root — cwd, HOME, CODEX_HOME, `${PLUGIN_ROOT}`, TMPDIR — pointed at a fresh empty directory, it emits exactly the one reread-and-reconcile `systemMessage` and leaves every root empty; a strict source allowlist (the only executable construct is a `cat` heredoc to stdout — no other command, no redirection, no pipe, no background launch) forecloses a write through any root or a leaked background process. **How tested:** `TestCodexPostCompactHookScriptIsInert` (`internal/ensigncycle/codex_post_compact_hook_test.go`) runs the shipped script with all reachable roots isolated to empty temp dirs, asserts each stays empty plus a single-`systemMessage` stdout, and statically asserts the script matches the stdout-only-heredoc allowlist. This AC proves ONLY the shipped script's inertness; it makes NO host-level failure-open claim — it does not assert that compaction continues, the next captain turn proceeds, a stop is not blocked, no automatic turn fires, or Codex does not abort after a disabled/untrusted/failing binding. That host-level failure-open BEHAVIOR is an out-of-scope live followup (see Out of scope), alongside AC-1/AC-2's live judgment-rule proof.

## Test plan

The deliverable is proportionate to a ~13-line FO-contract addition: a small Codex hook/config smoke plus contract-shape checks, most of which reuse existing contractlint/ratchet infrastructure. It does NOT include synthetic timing/reload behavioral oracles — those are removed (see below) because AC-1/AC-2 no longer claim runtime behavior.

1. **Codex hook/config smoke (AC-3, AC-4).** A small command-level fixture parses the shipped hook configuration, drives `manual` and `auto`, and asserts the output shape — exactly one nonempty JSON `systemMessage` per event, identical across sources and session cwds (the wording is quoted at review time, never grepped by a committed test); asserts the command resolves via `${PLUGIN_ROOT}` and fires from an UNRELATED session cwd (the cwd-relative `./hooks/` form must NOT resolve there); and runs the shipped script in isolation with every reachable filesystem root (cwd, HOME, CODEX_HOME, `${PLUGIN_ROOT}`, TMPDIR) pointed at an empty dir, asserting normal exit, the single `systemMessage`, and zero writes to any root, plus a strict source allowlist (stdout-only `cat` heredoc — no other command, redirection, pipe, or background launch). Cost: small.
2. **AC-1 review-time grep (no committed test).** AC-1 has NO committed Go test: a committed test that greps the shipped shared-core `.md` for prose is the false-green anti-pattern regardless of which phrases it greps — it verifies file content, not behavior. At validation the reviewer/FO greps the shipped `first-officer-shared-core.md` and cites the `## Compaction continuity` section + its host-neutrality (`\b(Codex|Claude|Pi)\b` = 0 hits inside the section) as evidence; the only committed gate over the file is the existing `startup_collapse` byte ratchet. Cost: none (review + reused ratchet).
3. **AC-2 no-capability check (reuse, no new test).** `«post-compact-notice»` no longer exists in the shipped contract. The contractlint capability lists shrink — `runtimeBoundCapabilities` to `{worker.spawn, worker.shutdown}`, `runtimeBindingCapabilities` dropping the bullet — and the existing `reflect.DeepEqual` exact-equality checks against both adapters' `## Runtime implementation` bullet lists mechanically enforce the bullet's absence. A review-time grep over the 13 FO-surface files (zero occurrences) is cited at validation; no committed prose-grep test. Cost: none (reuse).
4. **Opt-in live Codex TUI probe (AC-3 host boundary).** Trust the bundled hook, run one manual `/compact`, assert the warning appears once after `Context compacted`, and ask the next model turn whether the warning was in its context. Until Codex behavior changes the expected answer is `NONE`; this guards against upgrading a UI reminder into an unsupported automatic-reload claim. Cost: medium/live. The Claude live auto-inject probe belongs to `cdbhzxc`.

**Removed as disproportionate/false-green:** the synthetic pressure-timing oracle (`compaction_timing_test.go`) and the split-root reload-replay oracle (`compaction_reload_test.go`) — ~340 lines that fed hand-authored compliant answers into hand-authored validators and could not honestly prove shipped-FO behavior; and `compaction_contract_presence_test.go` in FULL (`git rm`) — a committed Go test that greps the shipped shared-core `.md` for prose is the false-green anti-pattern regardless of which phrases it greps (it verifies file content, not behavior), so AC-1 keeps NO committed test and moves to a review-time grep + the existing `startup_collapse` byte ratchet. The BEHAVIORAL proof those oracles reached for is the out-of-scope live followup.

## Documentation change

The shipped end-state (cycles 7-8, code commits 8c18ea20 and d091d0d7) carries the captain-shaped `## Compaction continuity` section in the shared core and NO `«post-compact-notice»` capability anywhere. Representative unified diff over the cycle-6 state:

```diff
# skills/first-officer/references/first-officer-shared-core.md — captain-shaped section
- ## Compaction continuity (host-neutral)
- Two FO judgment rules, no controller; the compacted summary is never authoritative.
- - **Before compaction:** suggest it only at a durable, recoverable boundary — … Non-blocking,
-   triggered by a captain cue or apparent pressure, never a token threshold.
- - **After compaction** (signalled by `«post-compact-notice»` where PRESENT, else a captain cue): …
+ ## Compaction continuity
+ The compacted summary is never authoritative.
+ - **Before:** on hosts that surface context-pressure hints, offer manual compaction at a durable
+   boundary, and with the offer recommend how to file what is not yet durable — unrecorded
+   decisions and captain directives, in-flight findings, conversation context worth keeping —
+   into state commits, entity bodies, or debrief/handoff notes, not limited to workflow objects.
+   Dense durable boundaries keep unhinted auto-compaction survivable.
+ - **After** (harness notice or captain cue): the summary's claim of having read or done is not
+   the reading or the doing — re-satisfy each load precondition and state read at its existing
+   trigger before the next workflow effect.

# skills/first-officer/references/fo-dispatch-core.md — capability block removed
- ## «post-compact-notice»: deliver the post-compaction reload reminder
- (declaration + runtime-binding arrow)

# skills/first-officer/references/codex-first-officer-runtime.md — binding bullet → residual prose
- - `«post-compact-notice»`: PRESENT (UI-only). Bundled `.codex-plugin` `PostCompact` hook …
+ (## Captain Interaction) The bundled `.codex-plugin` `PostCompact` hook emits a captain-visible
+ reload-reminder `systemMessage` — captain UI, not model context (probe) — degrading to a
+ manual captain cue; failure-open.

# skills/first-officer/references/pi-first-officer-runtime.md — ABSENT bullet removed
- - `«post-compact-notice»`: ABSENT; no known Pi compaction-hook surface, …

# skills/first-officer/references/claude-fo-dispatch.md — Claude binding section removed
- ## Claude binding: «post-compact-notice»()
- Model-context delivery via `SessionStart(compact)`, owned by task cdbhzxc — not designed here. …

# docs/runtime-support.md — shape-list entry removed
- - `«post-compact-notice»` -> post-compaction reload delivery binding, or manual-cue-only.
```

## Out of scope

Durable authorization or action ledgers; permits; checkpoints; continuation controllers; crash replay; interception or enforcement matrices; stop blocking; automatic turns; watchdogs; summary generation; transcript parsing; token-threshold design; and recovering effects that were not already made durable by existing Spacedock workflow rules.

Also out of scope for this entity: the Claude `SessionStart(compact)` model-context delivery binding and its live spike — owned by `claude-post-compaction-contract-reload` (id `cdbhzxc`); and any Pi post-compaction delivery beyond the manual captain cue. The host-neutral rules here cover all hosts; only the Claude auto-inject DELIVERY and its proof move to the split task.

**Named followup — live behavioral proof of the judgment rules and host failure-open.** Proving that a live FO actually (a) suggests compaction only at a durable boundary and (b) rereads its authoritative contract and reconciles durable state after compaction is a judgment-rule property with no controller by design, not honestly provable by an offline fixture. Proving host-level failure-open — that compaction and the next captain turn continue with no Spacedock-created state file, background process, blocked stop, automatic turn, or abort after a disabled, untrusted, unavailable, or failing delivery binding on any host — is likewise a host-runtime behavior no offline fixture can observe (an offline test can only run the shipped hook and check it wrote nothing, which is AC-4). Both are out of scope for this entity and recorded as a followup: a live compaction shared-scenario that drives a real FO through a compaction boundary, naturally alongside the existing live-e2e shared scenarios and the Claude `SessionStart(compact)` auto-inject work in `cdbhzxc`. Offline, this entity proves only the contract SHAPE (AC-1/AC-2), the Codex delivery (AC-3), and the shipped hook script's inertness (AC-4).

## Feedback Cycles

**Cycle 1 (captain feedback, 2026-07-14).** A compacted FO session lost wait/reconciliation discipline and acted on unrelated ready work. The first revision responded with a typed authorization grant and durable continuation machinery.

**Cycle 2 (independent ideation review).** Review found gaps in that controller's action vocabulary, gate binding, interception proof, and report identity. These findings apply only to the superseded controller design.

**Cycle 3 (captain scope reset, 2026-07-17).** The controller design was rejected as unnecessary. The intended product is exactly two hints: recommend compaction only at an already-durable boundary, then remind the post-compaction FO to reread its authoritative contract and reconcile durable state. This body replaces the rejected design rather than repairing it.

**Cycle 4 (captain host-neutral reframe, 2026-07-18).** The two hints were reframed from Codex-specific to host-neutral FO rules at the shared-core level, inherited by every runtime adapter. Post-compaction DELIVERY became a per-host binding `«post-compact-notice»` mirroring `«context-budget»`'s PRESENT/ABSENT discipline: Codex = captain-facing UI warning + manual cue (proven via the 0.144.4 probe); Pi = manual cue. The Claude `SessionStart(compact)` model-context delivery was SPLIT to `claude-post-compaction-contract-reload` (id `cdbhzxc`), referenced here but not designed. Two hints, no controller; the superseded-controller artifact reference is preserved.

### Feedback Cycles (validation rejection rounds)

- Cycle 1: REJECTED (Codex FO review, relayed by captain, 2026-07-19) — send back before push. All repo tests (incl. `go test ./... -race`) pass; the rejection is about runtime correctness and false-positive acceptance evidence. Two material findings, routed to implementation:
  - **M1 (runtime correctness).** `hooks.json` invokes the PostCompact script via cwd-relative `./hooks/codex_post_compact_notice.sh`. Codex runs plugin hooks from the session cwd (the operator's project), not the plugin root, so the hook fails whenever the FO operates on any non-Spacedock repo (the normal case). The offline test masked the defect by resolving `./hooks/...` against the plugin repo root. Fix: reference the script via the supplied `PLUGIN_ROOT`, and test from an unrelated project directory so the cwd-relative failure is caught.
  - **M2 (false-green evidence).** The AC-1/AC-2 fixtures hand-author both the input state and the expected transcript/message, so they exercise their own oracle rather than the shipped FO contract — they stay green even if the shared-core contract rules are removed. Fix: use captured/live fixture-backed FO behavior, or substantially narrow the AC-1/AC-2 evidence claims.
- Cycle 2: REJECTED (Codex FO re-review, 2026-07-19) — M1 is resolved by live Codex 0.144.6 evidence: `${PLUGIN_ROOT}/hooks/...` fires from an unrelated cwd and the old `./hooks/...` form does not. M2 remains material. `TestCompactionContinuityRuleShipped` is an instruction-prose grep, which `AGENTS.md` explicitly disallows as a substitute for behavior; the timing and reload suites still feed hand-authored compliant answers into hand-authored validators. Renaming them “oracles” and declaring the behavioral linkage unenforced is honest, but it does not make the behavioral first sentences of AC-1/AC-2 pass. The validation report therefore overclaims both ACs. The branch is also disproportionate: 757 added lines, including 419 lines of synthetic timing/reload/presence tests, for 13 lines of FO contract. Preserve the live-proven hook fix and a small hook/config smoke. Remove the synthetic oracle/presence machinery; then either (a) capture actual FO behavior with the durable evidence required by `AGENTS.md`, or (b) rewrite AC-1/AC-2 themselves as contract-shape requirements and stop claiming runtime behavior. Do not add another prose guard.
- Cycle 2 resolution (captain decision, 2026-07-19): chose option (b) — re-scope AC-1/AC-2 as contract-shape requirements and stop claiming runtime behavior. Routed to ideation (cycle 5) to rewrite the ACs and slim the deliverable: keep the live-proven `${PLUGIN_ROOT}` hook + a small hook/config smoke + contract-shape checks; strip the ~419 lines of synthetic timing/reload/presence machinery; behavioral proof of the judgment rules is an explicit out-of-scope followup (a live compaction shared-scenario).
- Cycle 5 (ideation re-scope, 2026-07-19): applied option (b). AC-1/AC-2 rewritten as CONTRACT-SHAPE requirements — AC-1: the `## Compaction continuity` rule ships in `first-officer-shared-core.md` with both clauses, host-neutral, within the shared-core ratchet (checked by a small presence assertion + `startup_collapse`); AC-2: `«post-compact-notice»` is defined in `fo-dispatch-core.md` with per-host `→` bindings, checked by the EXISTING contractlint `TestCapabilityBinding` (define/call balance + Claude/Codex/Pi coverage), no new test. Both drop the removed "exactly one suggestion" / "next effect only after reads" runtime claims; a contract-presence check survives only as a contract-shape AC, never dressed as behavioral proof. AC-3 (Codex visible warning, now naming the `${PLUGIN_ROOT}`/unrelated-cwd resolution) and AC-4 (harmless absence) kept as the honestly-testable hook/absence value checks. Test plan slimmed to the `${PLUGIN_ROOT}` hook + a small hook/config smoke + the contract-shape checks; the ~419 lines of synthetic timing/reload/presence oracle machinery are marked for the implementation stage to strip. Live behavioral proof of the judgment rules recorded as an out-of-scope live-scenario followup. Two-hints/no-controller decision, host-neutral framing, the `«post-compact-notice»` per-host binding, the superseded-controller artifact reference, and the Codex 0.144.x hook-probe evidence all preserved.
- Cycle 3: REJECTED (fresh isolated Codex adversarial re-validation of candidate `433b10c3`, 2026-07-19) — one material evidence defect; AC-1/AC-2 shape mutations bit and AC-3's shipped unrelated-cwd hook smoke passed. Escalated to the captain instead of another automatic feedback round.
  - **M3 (AC-4 evidence mechanism).** `TestCodexPostCompactHookHarmlessAbsenceMatrix` directly runs `sh -c 'exit 3'`, discards that result itself, and never invokes a host hook runner. It proves only that the fixture and six-line script leave its temp HOME/cwd empty; it cannot observe AC-4's promised continuation of compaction and the next captain turn after a host sees a non-zero hook, nor establish that claim "on any host." Reset the evidence boundary: either narrow AC-4 to the shipped Codex binding's independently observable no-write/absence shape, or prove continuation with a canonical/live host runner. Do not repair this by adding another self-authored continuation oracle.
- Cycle 3 resolution (captain decision, 2026-07-19): ACCEPTED with the host-continuation proof deferred to live experience. The review found no observed Codex break or outcome defect; the shipped hook's success path and no-write/absence boundary are proven. Proceed to merge without adding a synthetic continuation harness.
- Cycle 3: REJECTED (captain + roborev job 278 codex lens, 2026-07-19) — the AC-4 test `TestCodexPostCompactHookHarmlessAbsenceMatrix` (codex_post_compact_hook_test.go) is tautological/false-green for AC-4's BEHAVIORAL claim: `absent`/`disabled` run nothing; the `failing` case runs `sh -c 'exit 3'` (tests harness tolerance of a nonzero exit, NOT the shipped hook's failure behavior); it checks only temp HOME+cwd (denylist omits touch/mkdir/tee/git; `${PLUGIN_ROOT}` writes / background processes undetected). It never invokes a host hook runner nor observes host-level failure-open (compaction continuing, the next captain turn, no blocked-stop/automatic-turn/abort) — it would pass even if Codex aborted after a failing hook. Round 3 = escalation point; the captain diagnosed and directed the fix: extend the already-approved option-(b) re-scope to AC-4 — narrow the test + AC-4 claim to the honestly-observed inertness of the shipped stdout-only script (strict allowlist, isolated `${PLUGIN_ROOT}`, all permitted roots inspected), and move the host-level failure-open BEHAVIOR to the out-of-scope live followup (alongside AC-1/AC-2). Narrow or remove the test; do not retain it as proof of failure-open behavior.
- Cycle 6 (captain-directed fixes on PR #531, 2026-07-19): two shipped-contract fixes applied on the branch, both green. **FIX 1 (architecture).** `«post-compact-notice»`'s per-host binding details were sitting inline in the host-neutral core (`fo-dispatch-core.md`), defeating the `«fn»` runtime-binding delegation. Reduced the core declaration to the abstract capability + a kind-only `→ **runtime-binding**: bound in the host adapter's ## Runtime implementation` arrow (born clean like `«worker.spawn»`/`«worker.shutdown»`) and BOUND it per-host in the adapters' `## Runtime implementation`: Codex = PRESENT (UI-only) bundled `PostCompact` hook (`${PLUGIN_ROOT}/hooks/codex_post_compact_notice.sh`); Claude = `SessionStart(compact)` owned by `cdbhzxc` (pointer); Pi = ABSENT/manual cue. contractlint recognizes it as a runtime-bound capability (`TestCapabilityBinding` define/call balance; `TestDispatchCoreDefinesRuntimeBoundCapabilities` host-neutral core-block arrow guard; `TestCodexAndPiFirstOfficerRuntimeBindingBlocks` adapter binding bullet). AC-2 re-scoped to the delegated shape. Moving one compressed core line into three per-host adapter bindings grows the FO instruction surface 602B past the shrink-only `TestFOFunctionPromptSurfaceShrinks` ratchet (exact measured surface 123002B vs the 122400B ceiling). The byte cost was escalated to the captain; the ratchet bump is HELD pending that decision — `foFunctionReferenceBaselineBytes` stays at 122400 (contractlint red on that one test only) and, if the bump is approved, is set to the exact measured value with no rounded padding. Adapter-binding text was NOT golfed to chase the ceiling; correctness first. **FIX 2 (prose-grep).** `compaction_contract_presence_test.go` asserted 11 exact phrases copied from the shipped shared-core .md — brittle prose-grep. Per the captain-relayed correction the whole file is DELETED (`git rm`): a committed Go test that greps the shipped shared-core `.md` for prose is the false-green anti-pattern regardless of which phrases it greps (it verifies file content, not behavior), so no structural remnant (heading-present or host-neutral) is kept. AC-1 keeps NO committed test and is re-scoped to a review-time grep (reviewer/FO cites the `## Compaction continuity` section + host-neutrality at validation) plus the existing `startup_collapse` byte ratchet.
- Cycle 7 (captain-directed revision, 2026-07-20): the `«post-compact-notice»` capability is REMOVED entirely — the captain-verified diagnosis (three-lens adversarial verification, all claims CONFIRMED) established that modeling it as an FO-invokable capability inverted the real control flow (the harness compacts → runs the plugin `PostCompact` hook → emits a captain-visible UI `systemMessage` → the captain cues the FO; the FO cannot invoke a post-compaction hook), and its declaration sat in a deferred-load module unread on both sides of the event. Removed: the `fo-dispatch-core.md` block, all three adapter bindings (Codex bullet → residual `## Captain Interaction` prose; Pi ABSENT bullet dropped — shared-core's "else a captain cue" fallback covers Pi; Claude binding section dropped, deleting the shipped contract's only pointer to task `cdbhzxc`, whose ownership survives in the entity registry), the `docs/runtime-support.md` entry, and `post-compact-notice` from both contractlint capability lists, whose exact-equality adapter checks now enforce the bullet's absence. Rule 1 reworded to the captain-approved pressure-cued/durability-gated form; Rule 2's trigger reworded capability-free. The cycle-6 HELD ratchet-bump decision is MOOT — `foFunctionReferenceBaselineBytes` stays 122400 with zero baseline edits. NEW open byte question: after the required rebase onto origin/main (which added ~486 B of unrelated content to two measured files: a `fo-dispatch-core.md` freshness-invariant paragraph and a Codex `«worker.spawn»` bullet expansion), the post-rebase FO surface measures 122633 B — still 233 B over the 122400 baseline despite this revision removing ~855 B net — so `TestFOFunctionPromptSurfaceShrinks` stays red on that one test; escalated to the FO/captain rather than trimming unsanctioned contract content or bumping the baseline. Shared-core `startup_collapse` ratchet green (26609 B < 26755 B ceiling). Resolution (captain decision, 2026-07-20): baseline update APPROVED — `foFunctionReferenceBaselineBytes` raised to 122634, the minimal strictly-below value for the measured 122633 B surface (zero-slack convention, commit bceca196); no trims, no substance cuts; full suite green.
- Cycle 8 (validation cycle-6 REJECTED-for-rework + captain re-shape, 2026-07-20): the capability-removal work itself was CLEAN (suite green, both ratchets independently reproduced, mutate-to-red demonstrated); two items routed back plus a sanctioned slim, applied in commit d091d0d7. **Item 1 (captain re-shape).** `## Compaction continuity` replaced with the captain-shaped form: unqualified heading (the old "(host-neutral)" qualifier narrated AC-1 to its reviewer — the whole shared core is host-neutral), single lede, a Before offer conditional on hosts that surface context-pressure hints carrying the file-what-is-not-yet-durable recommendation (the old Before-rule was an event handler for an event with no reliable delivery), and a one-sentence After rule re-satisfying each load precondition and state read at its existing trigger (the old `«state.boot»()` re-invocation and newer-stage-report check were ceremony duplicating existing flows). **Item 2 (validation C1, MATERIAL).** `requiredNoticePhrases` — a third-shape output-grep the audit showed passes a weakened instruction and fails a valid rewording — removed from `codex_post_compact_hook_test.go`; the fixture now asserts output shape only (exactly one nonempty `systemMessage`, identical across manual/auto sources and across cwds), with the message wording moved to review-time citation; AC-3 and test-plan item 1 re-worded to match. **Item 3 (validation C2 slim, zero proof loss).** The positive unrelated-cwd half and its duplicate prefix check dropped (`runShippedHook` already runs every event from a fresh unrelated cwd; `resolveHookCommand` enforces the `${PLUGIN_ROOT}/` prefix); the negative cwd-relative probe, config-shape guard, matcher test, and full AC-4 apparatus kept; hooks.json now resolved from the manifest's `hooks` value with the equality guard intact (validator C3 polish). Hook test file 347 → 317 lines. Measured post-rework: FO surface 122430 B < 122634 baseline (zero baseline edits, shrinking); shared core 26406 B < 26755 ceiling (349 B headroom gained); full `go test ./...` green.
- Cycle 8: REJECTED-for-rework (validation cycle 6, 2026-07-20) — offline evidence for the capability removal itself CLEAN (full suite green, both ratchets independently reproduced, mutate-to-red demonstrated on the absence gates, detached audit refuted nothing material beyond the findings below); rejection routes two work items to implementation via the kept-alive worker (budget probe 15.9%, reuse_ok): (1) captain-directed re-shape of the shared-core `## Compaction continuity` section — heading de-qualified (the whole shared core is host-neutral; the qualifier narrated AC-1 to its reviewer), Before-rule refocused per the captain's wording: on hosts that surface context-pressure hints, offer manual compaction at a durable boundary, the offer carrying a recommendation for filing non-durable conversation content (decisions, directives, findings — not limited to workflow objects) into durable homes; no pretense of firing on hosts without a hint (dense boundaries keep unhinted auto-compaction survivable), After-rule reduced to one re-satisfy-preconditions sentence («state.boot»() re-invocation and the newer-stage-report check were ceremony duplicating existing flows); (2) validation C1 (MATERIAL, third-shape output-grep): `requiredNoticePhrases` substring literals over the captain-facing systemMessage — empirically false-greens a weakened instruction and false-reds a valid rewording — replaced by shape-only assertions, wording moving to review-time citation, AC-3's phrase clause re-worded to match. C2 (polish, zero-proof-loss ~100-line slim) folded in per the captain's proportionality challenge. Four live lanes deliberately deferred to the post-rework commit (spend economy, not a waiver); F1 recorded as DEFERRED with named promotion conditions; roborev backstop unavailable on this machine (both lenses).
- Gate resolution (captain, 2026-07-20): validation cycle 7 APPROVED — pi-live's REQUIRED-RED-UPSTREAM explicitly WAIVED for this merge (deterministic upstream skew, red on main pre-dating the commit; fix owned by task 268 `fix-pi-live-lane-pi-subagents-version-skew`); PR #531 merged. F1's mitigation assigned to task q4 (baseline re-tighten rides the TeamCreate retirement); the review-time grep runs every validation until it lands.

## Stage Report: ideation (cycle 3)

- DONE: Replace the continuation controller with exactly two bounded hints.
  The current body contains one safe-boundary compaction suggestion and one post-compaction contract-reload reminder. It removes the authorization ledger, permits, checkpoint, gateway, crash replay, interception matrix, monitor, watchdog, and their derived acceptance criteria.
- DONE: Exercise and honestly bound the current Codex lifecycle surface.
  Live Codex 0.144.4 manual `/compact` fired `PreCompact` and `PostCompact`; the shipped-shape `PostCompact` `systemMessage` appeared as a warning but was absent from the next model's context. `SessionStart(compact)` did not fire. The proposed hook is therefore captain-facing and failure-open, with a manual cue fallback.
- DONE: Rewrite the problem, proposed approach, acceptance criteria, test plan, and documentation change around observable hint timing and reload behavior.
  AC-1 pairs safe and unsafe pressure scenarios, AC-2 proves the visible warning without claiming model injection, AC-3 proves actual contract/status/roster/report reads before the next effect, and AC-4 proves harmless absence. The full rejected design remains beside the entity as an artifact.

### Summary

c6 is now a small continuity aid: the FO suggests compaction only when existing state is recoverable, and Codex warns the captain after compaction to trigger a real contract reload and state reconciliation. The live host does not inject that warning into model context, so the design says so and falls back to a manual captain cue instead of adding lifecycle machinery.

## Stage Report: ideation (cycle 4)

- DONE: State the two hints as host-neutral FO rules at the shared-core level (not Codex-only); the reload rule names the host-neutral reread target (`SKILL.md` + `@first-officer-shared-core.md` + the active host runtime adapter) and the existing boot/status/roster/report reconciliation flow.
  Decision + Rule 1/Rule 2 in Proposed approach now read as shared-core FO judgment rules inherited by every adapter; the reread target and its identical-on-every-host framing are stated in Problem and Rule 2. Trigger weakness is framed host-neutrally.
- DONE: Make post-compaction DELIVERY an abstracted per-host binding mirroring `«context-budget»` PRESENT/ABSENT — Codex = UI warning + manual cue with the probe artifact as proof; Claude = SPLIT to `cdbhzxc`, referenced not designed; Pi = manual cue.
  Added the `«post-compact-notice»` binding block with per-host `→` lines and a failure-open `block:`; the Claude `SessionStart(compact)` delivery is referenced and explicitly not designed here. Shared hook-surface prerequisite recorded (no `hooks` key in either manifest, verified).
- DONE: Rewrite Decision, Problem, Proposed approach, Acceptance criteria, Test plan, Documentation change, and Out of scope to match, and add a `## Feedback Cycles` Cycle 4 entry recording the reframe and the split.
  ACs renumbered host-neutral-first (AC-1 safe timing value AC, AC-2 reload-before-effect, AC-3 Codex binding proof, AC-4 harmless absence); test plan, doc diff, and Out of scope updated; the blanket "Claude or Pi changes" exclusion replaced by the split-task + Pi-manual-cue carve-out while the controller/ledger/permits/gateway/crash-replay/watchdog exclusions and the superseded-controller artifact reference are preserved.

### Summary

The entity is now host-neutral: two FO judgment rules live at the shared-core level and apply to every host, while post-compaction DELIVERY is the per-host binding `«post-compact-notice»` (Codex UI-warning+cue proven, Pi manual cue), with the Claude `SessionStart(compact)` auto-inject delivery split to `cdbhzxc` and referenced but not designed. No controller; the superseded design and Codex probe remain as artifacts, and the shared hook-shipping prerequisite is flagged.

## Stage Report: implementation

- DONE: Shared-core host-neutral Rules 1 & 2 plus the «post-compact-notice» capability (fo-dispatch-core.md + docs/runtime-support.md) shipped as designed; Rule 2 names the reread target (SKILL.md + first-officer-shared-core.md + active host runtime adapter) and the boot/status/roster/report reconciliation flow. (AC-2, AC-4)
  "## Compaction continuity" added to first-officer-shared-core.md (before-compaction durable-boundary suggest + after-compaction reread-and-reconcile), the «post-compact-notice» binding block in fo-dispatch-core.md, and the shape line in docs/runtime-support.md; all contractlint invariants green (capability define/call balance, per-host Claude/Codex/Pi → coverage, size ceilings). Code commit d124f6f6.
- DONE: Codex «post-compact-notice» binding bundled in .codex-plugin (establishing the plugin hooks surface) emitting the exact reread-and-reconcile systemMessage, failure-open; the first spike confirms .codex-plugin/plugin.json can declare a hooks key that Codex loads and trusts. (AC-3)
  .codex-plugin/plugin.json gains `"hooks": "./hooks.json"`; hooks.json declares PostCompact (matcher manual|auto) → ./hooks/codex_post_compact_notice.sh, a bundled script emitting the exact systemMessage. Live Codex 0.144.4 spike proved the hooks key LOADS + TRUSTS + FIRES reproducibly and is inert/harmless untrusted: artifacts/codex-0.144.4-plugin-hooks-spike.md.
- DONE: Offline fixtures prove the value ACs: Rule-1 safe/unsafe pressure-timing with commit-OID evidence (AC-1), split-root post-compaction reload-before-effect replay (AC-2), Codex hook JSON manual|auto + absent/disabled/failing matrix (AC-3, AC-4).
  internal/ensigncycle: compaction_timing_test.go (AC-1 real git OIDs, safe→one/unsafe→zero, + reject a suggestion at an unrecoverable boundary), compaction_reload_test.go (AC-2 ordered oracle over a replay + reject stale-summary skip / missing adapter reread), codex_post_compact_hook_test.go (AC-3 manual|auto → one valid systemMessage + AC-4 absent/disabled/failing no-writes matrix). All green.

### Summary

Shipped the two host-neutral FO compaction-continuity judgment rules (boot-resident shared core) plus the per-host `«post-compact-notice»` delivery binding, and the Codex leg: a bundled `.codex-plugin` `PostCompact` hook whose script emits the reread-and-reconcile `systemMessage`, failure-open. The riskiest claim — that a plugin-declared hooks key loads and trusts on Codex — was spiked FIRST against a live 0.144.4 CLI and proven reproducibly (form: `"hooks": "./hooks.json"` + a `hooks.json` + a bundled script; the vendor `plugin-creator` linter's "hooks unsupported" claim is stale, and curated OpenAI plugins ship the same mechanism). Key nuance in the spike artifact: hook commands run with no shell (whitespace-split, no quote-stripping), so the reminder must be a bundled script, not an inline command. Contract additions were trimmed to fit under the shared-core (<26755B) and FO-surface (<122400B) ratchet ceilings. Full non-live module suite is green except the pre-existing environmental `internal/cli` `TestCodexResolveManifestAgainstInstalledHost`, which fails only because the sandbox cannot read the real `~/.codex/config.toml` (it skips cleanly with a readable CODEX_HOME) — unrelated to this change.

## Stage Report: validation

- DONE: All four ACs independently verified with reproducible evidence, tests run uncached (AC-1 timing, AC-2 reload, AC-3 hook, AC-4 absence)
  `go test -race -count=1 ./internal/ensigncycle/` → 10/10 PASS. AC-1: real git OIDs reproduced at runtime (`commitWorkItem`→`git rev-parse HEAD`, not asserted); `TestCompactionSafeBoundarySuggestsOnce`=1, `TestCompactionUnsafeBoundaryStaysSilent`=0, `TestCompactionSuggestionAtUnrecoverableBoundaryRejected` bites (uncommitted change + unconsumed completion). Regex keys on the captain message, NOT the contract file. AC-2: `assertReloadBeforeEffect` reads tool_use blocks not prose; `TestReloadBeforeEffectStaleSummarySkipRejected` + `TestReloadBeforeEffectMissingAdapterReadRejected` both bite. AC-3: shipped script executed → single-key `systemMessage` per `manual|auto`, identical (`TestCodexPostCompactHookEmitsOneSystemMessagePerEvent`; config = exactly 1 PostCompact group/1 handler). AC-4: `TestCodexPostCompactHookHarmlessAbsenceMatrix` present-ok/absent/disabled/failing → HOME+cwd left empty; static ban on mutation verbs + `>`.
- DONE: Codex hook DELIVERY wiring verified end-to-end (plugin.json hooks key + hooks.json + bundled script resolve/fire per the spike; inert untrusted)
  Committed form matches spiked form: `.codex-plugin/plugin.json "hooks": "./hooks.json"` + repo-root `hooks.json` (matcher `manual|auto`) + bundled `hooks/codex_post_compact_notice.sh` — a single-token command (no-shell whitespace-split safe) whose own `#!/bin/sh` runs the heredoc, NOT an inline command. Ran the script directly → `{"systemMessage":"Spacedock: compaction completed. ... reread ... `spacedock:first-officer` ... reconcile durable ..."}`. Inert-untrusted per the spike trust-gate. Composition caveat recorded as DR-1.
- DONE: Contract additions host-neutral, contractlint green, no FO regression, non-live suite green except the named environmental test
  Shared-core "## Compaction continuity" section: 0 host-specific terms (grep); Codex mechanism confined to fo-dispatch-core `«post-compact-notice»` block (line 127) mirroring `«context-budget»`/`«roster-reconcile»`. `go test -count=1 ./internal/contractlint/` PASS (define/call balance incl. `«post-compact-notice»`+`«state.boot»`, per-host coverage, size ceilings). Shared-core 26639B < 26755B ratchet. Full `go test ./...` exit 0 + `go vet` clean (readable CODEX_HOME). `internal/cli TestCodexResolveManifestAgainstInstalledHost` byte-identical to origin/main, fails only on sandbox `~/.codex/config.toml` read, skips cleanly with CODEX_HOME set — pre-existing + environmental; commit d124f6f6 does not touch internal/cli.

### Summary

VERDICT: PASSED. All four ACs carry valid, independently reproduced evidence (uncached + `-race`); the Codex hook wiring matches the spiked form and is failure-open; the two shared-core rules are genuinely host-neutral with contractlint green and both ratchet ceilings respected; the only suite failure is the named pre-existing environmental internal/cli test. No material finding.

Deferred risks (non-blocking):
- DR-1 (evidence): the EXACT committed bundled hook firing a PostCompact `systemMessage` in a live TUI is proven by COMPOSITION of separately-proven spike links (hooks-key load via SessionStart+absolute-touch; `manual|auto` parse; systemMessage-as-warning from the inline 0.144.4 probe; relative-to-plugin-root resolution inferred from OpenAI `replayio`/`figma`), not one end-to-end run of the exact form. Trigger: live TUI UI-warning delivery — outside the offline gate (AC-3's gating proof is the command-level fixture; the true end-to-end is opt-in live test-plan item 4). Value path holds regardless: failure-open → a manual cue still fires Rule 2 (AC-4). Promotes to material only if the live probe is run and the exact form fails to fire while trusted.
- DR-2 (observation): AC-1/AC-2 offline proofs are fixture oracles over hand-authored (boundary,message)/tool-call streams, not live-FO runs — the mechanism AC-1 sanctions ("fixture-backed") and test-plan item 3 sanctions ("replay"), with biting reject tests. The prose-rule→live-FO linkage is a judgment rule by design ("two judgment rules, no controller"), exercised only by the live-tagged tests out of the offline gate's scope. Not a defect.

## Stage Report: implementation (cycle 2)

- DONE: M1 fixed: the Codex PostCompact hook resolves + fires from ANY session cwd — the script is referenced via PLUGIN_ROOT (absolute, plugin-root-relative), proven by a test run from an UNRELATED project directory (no local ./hooks/) that observes the systemMessage; the prior repo-root-masking test is corrected so it fails on the cwd-relative form.
  hooks.json command → `${PLUGIN_ROOT}/hooks/codex_post_compact_notice.sh`. Live spike (codex-cli 0.144.6, marketplace-installed plugin, SessionStart via `codex exec --dangerously-bypass-hook-trust` from an unrelated cwd): `./hooks/x.sh` no-fire, `${PLUGIN_ROOT}/hooks/x.sh` FIRED, bare `$PLUGIN_ROOT` no-fire (no shell), `${CLAUDE_PLUGIN_ROOT}` also FIRED, `${CODEX_PLUGIN_ROOT}` no-fire/unset — table in artifacts/codex-0.144.4-plugin-hooks-spike.md. `resolveHookCommand` now requires the `${PLUGIN_ROOT}/` prefix and substitutes it (fails on cwd-relative); new `TestCodexPostCompactHookFiresFromUnrelatedCwdViaPluginRoot` runs the resolved command from an unrelated temp-dir cwd (PLUGIN_ROOT set) and asserts the systemMessage while proving `./hooks/<script>` does not resolve there. Reverting hooks.json to `./hooks/...` makes 3 tests FAIL (verified). Commit 8ad3bb4e.
- DONE: M2 fixed: AC-1/AC-2 no longer false-green — evidence claims narrowed to only what the fixtures exercise (AC text + stage report updated to match); the biting rejection tests are preserved.
  Chose the honest-narrowing option. `TestCompactionSafeBoundarySuggestsOnce`→`TestCompactionTimingOracleAcceptsDurableBoundary` and `TestReloadBeforeEffectGoodReplayPasses`→`TestReloadOracleAcceptsCompliantReplay`, with comments + ABOUTMEs stating they characterize the acceptance ORACLE over hand-authored fixtures, NOT shipped-FO behavior (that linkage is an unenforced judgment-rule property, no committed test asserts it). AC-1/AC-2 text gained an explicit "Offline scope" clause. Biting tests kept: `TestCompactionSuggestionAtUnrecoverableBoundaryRejected`, `TestReloadBeforeEffectStaleSummarySkipRejected`, `TestReloadBeforeEffectMissingAdapterReadRejected`. Added `TestCompactionContinuityRuleShipped` (new file compaction_contract_presence_test.go): a contract-presence guard that reads the shipped shared-core and asserts the "## Compaction continuity" rule + 7 load-bearing clauses — deleting the rule turns the offline gate RED (verified: removing the section → FAIL). Commit 8ad3bb4e.
- DONE: Re-verified: gofmt/vet clean; go test ./... and -race green except the named pre-existing environmental internal/cli test; a new implementation stage report documents both fixes with reproducible evidence.
  `gofmt -l internal/` reports only the pre-existing, untouched `internal/release/journeydelta.go`; `go vet ./internal/ensigncycle/ ./internal/contractlint/` clean. `go test -race -count=1 ./internal/ensigncycle/` PASS (11.1s). `go test -count=1 ./...` exit 0 with a readable CODEX_HOME (the named `internal/cli TestCodexResolveManifestAgainstInstalledHost` passes there; it only fails when the sandbox cannot read `~/.codex/config.toml`, unchanged by this work). contractlint PASS uncached.

### Summary

Both round-1 material findings fixed on the existing branch without weakening. M1: the hook command is now `${PLUGIN_ROOT}/hooks/…`, which a live 0.144.6 spike proves Codex substitutes to the materialized plugin dir and fires from an unrelated cwd; the offline fixture no longer masks a cwd-relative command and bites on it. M2: the AC-1/AC-2 happy-path tests are honestly scoped to acceptance-oracle mechanics (biting rejects preserved), the AC text says so, and a new contract-presence guard fails the offline gate if the shared-core rule is deleted — so the suite is no longer green when the shipped contract is absent. The remaining shipped-FO behavioral linkage is explicitly an unenforced judgment-rule property (no committed test asserts it; the design ships two judgment rules, no controller), observable only in a live FO run outside the offline gate.

## Stage Report: validation (cycle 2)

- DONE: M1 independently re-verified — the Codex PostCompact hook fires from an UNRELATED cwd via `${PLUGIN_ROOT}` and NOT with the old cwd-relative `./hooks/` form; `resolveHookCommand` rejects a non-`${PLUGIN_ROOT}` command.
  `TestCodexPostCompactHookFiresFromUnrelatedCwdViaPluginRoot` PASS uncached + `-race`; revert-check (I set hooks.json back to `./hooks/…`, ran, restored) made exactly 3 tests FAIL — EmitsOneSystemMessagePerEvent, FiresFromUnrelatedCwdViaPluginRoot, HarmlessAbsenceMatrix — all via `resolveHookCommand`'s `${PLUGIN_ROOT}/`-prefix Fatalf (test lines 110-112). Spike artifact table (codex-cli 0.144.6) records `${PLUGIN_ROOT}/hooks/mark.sh` FIRED / `./hooks/mark.sh` no-fire from an unrelated cwd.
- DONE: M2 independently re-verified — AC-1/AC-2 no longer false-green; happy-path fixtures scoped to oracle mechanics with "Offline scope" AC clause, biting rejects preserved, presence guard turns the gate RED when the rule is deleted.
  Renamed `…OracleAccepts…` tests + ABOUTMEs characterize the oracle, not shipped-FO behavior; AC-1/AC-2 bodies (index.md L90-91) carry the "Offline scope" clause. I deleted shared-core lines 162-168 → `TestCompactionContinuityRuleShipped` FAILED ("missing the '## Compaction continuity' rule"), restored → green. Biting rejects pass (oracle rejects bad fixtures; paired Accepts tests require it to accept good ones → bites both directions). Claims honestly narrowed to the offline oracle; no overreach — accepted the rejecter's offered honest-narrowing, did not demand behavioral proof of by-design-unenforced judgment rules.
- DONE: All four ACs hold under the narrowed-but-honest scope; gofmt/vet clean; contractlint green; go test ./... and -race green except the named pre-existing environmental internal/cli test.
  AC-1: timing oracle accepts durable→1 / unsafe→0, rejects suggestion-at-unrecoverable-boundary, real git OIDs (`commitWorkItem`→`rev-parse`), bound to shipped rule by the presence guard. AC-2: reload oracle reads tool_use blocks not prose, rejects stale-summary skip + missing-adapter reread. AC-3: shipped hook parsed, `manual|auto` matched, one valid `systemMessage`/event with required phrases, fires from unrelated cwd; spike proves load+trust+fire on live 0.144.6. AC-4: present-ok/absent/disabled/failing matrix leaves HOME+cwd empty, static ban on mutation verbs + `>`. gofmt clean on all four touched files (only pre-existing untouched `internal/release/journeydelta.go` flagged); `go vet ./internal/ensigncycle/ ./internal/contractlint/` clean; `go test -count=1 ./...` green except `internal/cli TestCodexResolveManifestAgainstInstalledHost` (sandbox cannot read `~/.codex/config.toml`; passes with a readable CODEX_HOME; branch does not touch internal/cli); `-race` green on ensigncycle/contractlint/hostneutrality. Shared-core 26639B < 26755B ratchet (ceiling constant confirmed in startup_collapse_test.go). Compaction-continuity section carries 0 host-specific terms; Codex specifics confined to the fo-dispatch-core `«post-compact-notice»` block.

### Summary

VERDICT: PASSED. Fresh independent re-review (fresh:true) on branch bdd97916; I reproduced every claim rather than trusting the fix report. M1 is genuinely resolved: the hook resolves via `${PLUGIN_ROOT}`, fires from an unrelated cwd, and the fixture now bites the cwd-relative defect (revert-check → exactly 3 FAILs). M2 is genuinely resolved by the rejecter's offered honest-narrowing: the AC-1/AC-2 oracles are scoped to fixture mechanics, the AC text says so without overreach, biting rejects are preserved, and the new presence guard turns the offline gate RED if the shared-core rule is deleted (verified by removal). All four ACs carry reproducible evidence; suite green except the named environmental internal/cli test. No material finding.

## Stage Report: ideation (cycle 5)

- DONE: AC-1/AC-2 rewritten as checkable CONTRACT-SHAPE requirements (the "## Compaction continuity" rule ships in first-officer-shared-core.md with its required clauses; «post-compact-notice» is defined in fo-dispatch-core.md with per-host → bindings; host-neutral / contractlint-green / within ratchet ceilings) — NO runtime-behavior claim and NO prose-grep dressed as behavioral proof. AC-3 (Codex visible warning) and AC-4 (harmless absence) kept as the honestly-testable hook/absence checks.
  AC-1 now asserts the shared-core rule ships with both clauses (safe-boundary suggest + reread-and-reconcile naming the reread target), host-neutral (0 host terms), within the shared-core ratchet — checked by a small presence assertion + `startup_collapse`. AC-2 asserts `«post-compact-notice»` is defined with per-host `→` bindings, checked by the EXISTING contractlint `TestCapabilityBinding` (define/call balance + Claude/Codex/Pi coverage) — no new test, an enumeration/coverage comparison not a prose grep. Both drop the removed "exactly one suggestion" / "next effect only after reads" claims. AC-3 gains the `${PLUGIN_ROOT}`/unrelated-cwd resolution; AC-4 unchanged. A framing line under `## Acceptance criteria` records that AC-1/AC-2 are contract-shape means paired with the AC-3/AC-4 value.
- DONE: Proposed approach + Test plan + Documentation updated to a PROPORTIONATE deliverable: keep the live-proven ${PLUGIN_ROOT} Codex hook + a small hook/config smoke + the contract-shape checks; STRIP the ~419 lines of synthetic timing/reload/presence oracle machinery. Behavioral proof of the judgment rules named as an explicit OUT-OF-SCOPE followup (a live compaction shared-scenario), not built here.
  Test plan slimmed to 4 items — the `${PLUGIN_ROOT}` hook/config smoke (AC-3/AC-4), a small AC-1 contract-shape presence check, AC-2's contractlint reuse (no new test), and the opt-in live probe — with an explicit "Removed" note marking `compaction_timing_test.go` (168) + `compaction_reload_test.go` (172) + `compaction_contract_presence_test.go`'s behavioral framing (~419 lines) for the implementation stage to strip. Proposed-approach AC annotations repointed to the contract-shape ACs + the live followup. Documentation change (shared-core rule + «post-compact-notice» + docs/runtime-support.md diff) preserved and labelled as the contract shape AC-1/AC-2 verify. Live behavioral proof recorded as a live-scenario followup in Out of scope.
- DONE: Two-hints/no-controller decision + host-neutral framing preserved; a "Stage Report: ideation (cycle 5)" + a Feedback Cycles note recorded; the superseded-controller artifact reference intact.
  Decision section, host-neutral Rule 1/Rule 2, the `«post-compact-notice»` per-host binding, the superseded-controller artifact reference (`artifacts/superseded-controller-design.md`), and the Codex 0.144.x hook-probe evidence (`artifacts/codex-0.144.4-hook-probe.md`, `artifacts/codex-0.144.4-plugin-hooks-spike.md`) are all untouched; a Cycle-5 note added under "### Feedback Cycles (validation rejection rounds)".

### Summary

Cycle-5 ideation applied the captain's option (b): AC-1/AC-2 are now contract-shape requirements (the rule ships with its clauses, host-neutral, within ratchet; the delivery capability is defined with per-host bindings) proven by a small presence check + the existing contractlint capability-binding, with NO runtime-behavior claim; AC-3/AC-4 stay as the honestly-testable Codex hook / harmless-absence value checks. The deliverable is slimmed to the live-proven `${PLUGIN_ROOT}` hook + a small hook/config smoke + contract-shape checks; the ~419 lines of synthetic timing/reload/presence oracle machinery are marked for removal, and the live behavioral proof of the judgment rules is an explicit out-of-scope live-scenario followup that the implementation stage does not build.

## Stage Report: implementation (cycle 3)

- DONE: Strip the synthetic behavioral oracles: DELETE compaction_timing_test.go and compaction_reload_test.go (~340 lines); REFRAME compaction_contract_presence_test.go into a small honest CONTRACT-SHAPE presence check for AC-1.
  `git rm` removed compaction_timing_test.go (168) + compaction_reload_test.go (172) = 340 lines. compaction_contract_presence_test.go reframed to `TestCompactionContinuityRuleShippedInContractShape`: asserts the shipped `## Compaction continuity` section carries both clauses (before-compaction durable-boundary suggest; after-compaction reread-and-reconcile naming the three reread targets) and names no host (`\b(Codex|Claude|Pi)\b` = 0 hits); ABOUTME/name/doc-comment state CONTRACT SHAPE ONLY, no runtime-behavior claim, not a prose-guard dressed as behavior. Bites both ways (verified): removing a clause phrase → FAIL; inserting "Codex" into the section → FAIL. The shared-core byte ratchet is NOT re-implemented here — it is delegated to the EXISTING `internal/contractlint` `startup_collapse` (`TestStartupRecipeCollapsedAndLeaner`, `preChangeSharedCoreBytes = 26755`), which AC-1's "How tested" and the cycle-5 ideation report both name as the byte-ceiling ratchet (no duplicate ratchet constant). Commit 433b10c3.
- DONE: Keep the shipped ~13-line contract and the live-proven ${PLUGIN_ROOT} Codex hook + its hook/config smoke (AC-3/AC-4). Confirm AC-2 is covered by the EXISTING contractlint TestCapabilityBinding (no new test). Do NOT add new behavioral machinery or another prose guard.
  KEEP files pristine (`git diff HEAD` empty) for first-officer-shared-core.md, fo-dispatch-core.md, docs/runtime-support.md, .codex-plugin/plugin.json, hooks.json, hooks/, and codex_post_compact_hook_test.go (302 lines — proportionate for AC-3's manual|auto + one-systemMessage + unrelated-cwd/${PLUGIN_ROOT} M1 regression + AC-4 absence matrix; NOT oversized, not trimmed). AC-2: `TestCapabilityBinding` enumerates `## «name»:` defines vs body `«name»` calls in fo-dispatch-core.md and requires each `→` line to bind Claude/Codex/Pi — `«post-compact-notice»` is covered by set-equality, no new test; PASS. No new behavioral machinery or prose guard added.
- DONE: Re-verify: gofmt/vet clean; contractlint green (incl. the «post-compact-notice» binding); go test ./... and -race green except the named pre-existing environmental internal/cli test; diff-size reduction (~757 → proportionate).
  `gofmt -l` clean on the touched file; `go vet ./internal/ensigncycle/ ./internal/contractlint/` clean; `go test -count=1 ./internal/contractlint/` PASS (incl. TestCapabilityBinding + startup_collapse). Full `go test -count=1 ./...` green except `internal/cli TestCodexResolveManifestAgainstInstalledHost` (sandbox cannot read `~/.codex/config.toml`: "Operation not permitted" — pre-existing/environmental, passes with a readable CODEX_HOME, branch does not touch internal/cli). `-race` green on ensigncycle/contractlint/hostneutrality. Diff size: the branch's ensigncycle test additions drop from 721 → 403 lines (deleted 340 synthetic-oracle lines; codex hook smoke 302 preserved; presence check 79 → 101, now an honest contract-shape check, not synthetic).

### Summary

Applied the cycle-5 re-scope (captain option b) at the implementation stage: the deliverable now MATCHES the contract-shape ACs, mostly by deletion. The two synthetic behavioral oracles (compaction_timing_test.go, compaction_reload_test.go — 340 lines that fed hand-authored answers into hand-authored validators and could not honestly prove shipped-FO behavior) are removed, and the presence test is reframed into a small honest AC-1 contract-shape check that asserts the shipped `## Compaction continuity` rule ships with both clauses and names no host, makes NO runtime-behavior claim, and bites both ways. The ~13-line contract, the live-proven `${PLUGIN_ROOT}` Codex hook, and its AC-3/AC-4 smoke are untouched; AC-2 stays on the existing contractlint `TestCapabilityBinding`; the shared-core byte ratchet stays on the existing `startup_collapse` (not duplicated). gofmt/vet/contractlint/`go test ./...`/`-race` all green except the named pre-existing environmental internal/cli test. Behavioral proof of the judgment rules remains the explicit out-of-scope live compaction shared-scenario followup. Code committed on the branch (433b10c3); not pushed — validation re-review comes first.

## Stage Report: validation (cycle 3)

- DONE: Round-2 synthetic machinery is GONE; the reframed AC-1 presence check is honest CONTRACT-SHAPE and BITES BOTH WAYS (independently reproduced).
  `compaction_timing_test.go` (168) + `compaction_reload_test.go` (172) absent from the tree; deletion tracked in commit 433b10c3; grep for `compaction_timing|compaction_reload|OracleAccepts|assertReloadBeforeEffect` across `internal/` finds nothing. `TestCompactionContinuityRuleShippedInContractShape` asserts the shipped `## Compaction continuity` section carries both clauses + 3 reread targets + `«post-compact-notice»` link and names no host; ABOUTME/name/doc-comment state CONTRACT SHAPE ONLY with NO runtime-behavior claim; byte ratchet delegated to the existing `startup_collapse` (`preChangeSharedCoreBytes=26755`, no duplicate constant). Bite reproduced: removed "safe time to compact" → FAIL (test:61 missing-clause); inserted "Codex" into the section → FAIL (test:69 host-neutral); restored → PASS. Not a prose-guard dressed as behavior.
- DONE: AC-3/AC-4 hook smoke green + unchanged; AC-2 on existing `TestCapabilityBinding` (no new test); the ~13-line contract untouched; no synthetic oracle re-introduced; 302-line smoke judged proportionate.
  `go test -race -count=1 -run 'Compaction|PostCompact' ./internal/ensigncycle/` → 5/5 PASS (MatchesManualAndAuto, EmitsOneSystemMessagePerEvent, FiresFromUnrelatedCwdViaPluginRoot, HarmlessAbsenceMatrix{present-ok,absent,disabled,failing}); the smoke EXECUTES the shipped `hooks/codex_post_compact_notice.sh` and observes real JSON systemMessage + on-disk state, not a prose grep. `${PLUGIN_ROOT}/hooks/...` resolves and fires from an unrelated cwd; `./hooks/...` does not (M1 regression). AC-2: `«post-compact-notice»` defined (dispatch-core:123) + called (:125) + → binds Claude/Codex/Pi (:127); `TestCapabilityBinding` bites — dropped `**Pi:**` → FAIL (test:145), restored → PASS. `git diff HEAD` empty on shared-core/dispatch-core/runtime-support/plugin.json/hooks.json/hooks/ and the hook test; working tree clean. 302-line smoke = proportionate (4 behavioral tests + helpers proving AC-3/AC-4 and the live-caught M1 regression; the "small hook/config smoke" option (b) said to keep — trimming risks the M1 regression / absence matrix).
- DONE: All four re-scoped CONTRACT-SHAPE ACs hold; gofmt/vet clean; contractlint green; go test ./... and -race green except the named pre-existing environmental internal/cli test.
  `gofmt -l` clean on the two touched test files; `go vet ./internal/ensigncycle/ ./internal/contractlint/` clean; `go test -count=1 ./internal/contractlint/` PASS (incl. TestCapabilityBinding + TestStartupRecipeCollapsedAndLeaner); shared-core 26639B < 26755B ratchet; Compaction-continuity section 0 host terms (Codex specifics confined to the fo-dispatch-core `«post-compact-notice»` block). `go test -count=1 ./...` fails ONLY `internal/cli TestCodexResolveManifestAgainstInstalledHost` (sandbox cannot read `~/.codex/config.toml`: "Operation not permitted"; passes with a readable `CODEX_HOME`; branch does not touch `internal/cli` — `git diff origin/main...HEAD -- internal/cli/` empty). `-race` green on ensigncycle/contractlint/hostneutrality.

### Summary

VERDICT: PASSED. Fresh independent cycle-3 re-review; I reproduced every claim rather than trusting the report. The round-2 findings are genuinely resolved: the two synthetic behavioral oracles are deleted (340 lines, tracked in 433b10c3, none re-introduced) and the reframed AC-1 presence check is an honest contract-shape assertion — no runtime-behavior claim in its ABOUTME/name/doc-comment/AC text — that I confirmed bites both ways (missing-clause FAIL and host-name FAIL). AC-2 rides the existing structural `TestCapabilityBinding` (bites on a dropped per-host binding); AC-3/AC-4 ride a hook smoke that genuinely executes the shipped script. The ~13-line contract is pristine (`git diff HEAD` empty), the branch is 439 insertions total (down from the round-2 757), gofmt/vet/contractlint/`go test ./...`/`-race` all green except the named pre-existing environmental `internal/cli` test. The 302-line hook smoke is proportionate for a contract-shape task and I do not require a trim. No material finding.

Deferred risks (non-blocking):
- DR-1 (evidence, matches AC-3 scope): the EXACT committed bundled hook firing in a live Codex TUI is proven by composition of the spike links + the opt-in `//go:build live` probe, not one end-to-end run of the committed form inside the offline gate. AC-3's gating proof is the command-level fixture by design (the true end-to-end is opt-in test-plan item 4). Failure-open guarantees the value regardless (a manual cue still fires Rule 2, AC-4). Promotes to material only if the live probe is run and the exact form fails to fire while trusted.
- DR-2 (design, captain-approved): AC-1/AC-2 prove contract SHAPE, not the live judgment-rule behavior (two judgment rules, no controller). The live behavioral value — a real FO suggesting compaction only at a durable boundary and rereading its contract after compaction — is the explicit out-of-scope followup (a live compaction shared-scenario). Not a defect; this is the approved re-scope, so it does not consume a feedback cycle.

## Stage Report: validation (cycle 4)

- DONE: Reproduce AC-1 and AC-2 as contract-shape claims and reject any static prose check that overclaims runtime behavior or cannot bite.
  Fresh isolated Codex worker (GPT-5; `CODEX_THREAD_ID` present) at exact HEAD `433b10c3`; detached adversarial edits made AC-1 fail when its clean-boundary phrase was removed and AC-2 fail when Pi coverage was removed.
- FAILED: Execute the shipped plugin-root hook/config smoke from an unrelated cwd and verify failure-open behavior for AC-3 and AC-4.
  AC-3 passed: the shipped executable emitted exactly one valid reread/reconcile `systemMessage` from an unrelated temp cwd, exit 0, no writes; AC-4 host continuation is not observed by the current harness.
- DONE: Audit the exact 433b10c3 branch delta, proportionality, and required Go/race gates; classify any material or evidence defect independently of the existing PASSED report.
  Exact four-commit delta is 439 insertions across 8 files (403 test lines); both full Go suites pass, but M3 independently overturns the prior PASSED report.
- DONE: AC-1 — compaction-continuity rule ships in host-neutral contract shape.
  Shape check bites; `first-officer-shared-core.md` is 26639B under the 26755B ratchet and the scoped section names no host.
- DONE: AC-2 — post-compact-notice delivery capability defined with per-host bindings.
  `TestCapabilityBinding` passes and independently reds when the Pi binding is removed; define/call/Claude-Codex-Pi coverage is non-vacuous.
- DONE: AC-3 — Codex delivery binding, visible warning.
  Focused race smoke passes; `${PLUGIN_ROOT}` resolution, manual/auto matcher, exact single JSON output, unrelated cwd, and zero-write behavior were exercised.
- FAILED: AC-4 — harmless absence (host-neutral).
  Material evidence defect M3: direct `exec.Command` plus `_ = cmd.Run()` cannot prove a host continues compaction or the next captain turn after the hook fails.
- DONE: Required test gates and candidate hygiene.
  `go test -count=1 ./...` PASS; `go test -count=1 -race ./...` PASS; focused vet/contractlint PASS; touched Go files are gofmt-clean and the candidate worktree stayed clean.
- SKIPPED: Rewrite AC-4 or implement a replacement host-level proof.
  Validation does not implement fixes; because the harness boundary is wrong, route to an AC/evidence reset rather than another synthetic implementation cycle.

### Summary

VERDICT: REJECTED. AC-1 and AC-2 now honestly prove only contract shape and bite under adversarial edits; AC-3's shipped hook/config behavior passes from an unrelated cwd, and all required Go/race gates are green. AC-4 remains materially unproven because its harness discards a synthetic handler failure without exercising or observing any host continuation; narrow the AC or use a canonical/live host runner before presenting PASSED.

## Stage Report: implementation (cycle 4)

- DONE: AC-4 test fixed: TestCodexPostCompactHookHarmlessAbsenceMatrix is NARROWED to honestly prove only the shipped stdout-only hook script's inertness (strict allowlist, isolated ${PLUGIN_ROOT}, EVERY permitted filesystem root inspected + process behavior), with a name matching that. It no longer claims to prove host-level failure-open (compaction continues / no abort); the `sh -c 'exit 3'` harness-tolerance claim is gone.
  Replaced with `TestCodexPostCompactHookScriptIsInert` (codex_post_compact_hook_test.go): a strict source allowlist (`assertShippedHookIsStdoutOnlyHeredoc` — the sole executable construct is a `cat` heredoc to stdout; any other command / redirection / pipe / `&` background launch fails it, so a write through any root or a leaked process cannot hide behind an unlisted verb) PLUS a behavioral run with cwd, HOME, CODEX_HOME, ${PLUGIN_ROOT}, TMPDIR each isolated to an empty temp dir, asserting each stays empty and stdout is exactly one systemMessage. The `sh -c 'exit 3'` and the vacuous absent/disabled cases are gone. Both halves verified to bite: temporarily adding `touch "$PLUGIN_ROOT/pwned"` to the shipped script → allowlist FAIL; with the allowlist bypassed, the isolation check FAILs on a non-empty ${PLUGIN_ROOT} (the exact root the old temp-HOME/cwd test missed). Commit 67426114.
- DONE: AC-4's TEXT re-scoped to the honestly-observable claim (the shipped hook script does no writes / is inert), with the host-level failure-open BEHAVIOR moved to the out-of-scope live followup alongside AC-1/AC-2. AC-3 stays intact.
  AC-4 retitled "shipped hook is inert (offline scope)"; body claims only the stdout-only script's inertness and explicitly disclaims any host-level failure-open claim (compaction continues / next captain turn / no blocked-stop / no automatic-turn / no abort). The `## Acceptance criteria` framing line, Test plan item 1, and the Out-of-scope "Named followup" were updated to match; the followup now names host-level failure-open as a live-scenario followup. AC-3 (${PLUGIN_ROOT}/unrelated-cwd) untouched.
- DONE: gofmt/vet/contractlint green; go test ./... and -race green except the named pre-existing environmental internal/cli test; implementation stage report records the AC-4 test narrow/removal and the AC-4 re-scope.
  `gofmt -l` clean on the touched test file; `go vet ./internal/ensigncycle/ ./internal/contractlint/` clean; `go test -count=1 ./internal/contractlint/` PASS (incl. TestCapabilityBinding + startup_collapse). `go test -count=1 ./...` and `go test -race -count=1 ./...` both green (14 pkg ok, no data race, ensigncycle green) EXCEPT `internal/cli TestCodexResolveManifestAgainstInstalledHost` (sandbox cannot read `~/.codex/config.toml`: "Operation not permitted" — pre-existing/environmental; branch does not touch internal/cli, `git diff origin/main...HEAD -- internal/cli/` empty).

### Summary

Extended the captain-approved option-(b) honest-narrowing to AC-4 (round-3 escalation, roborev job 278 codex lens). The false-green `TestCodexPostCompactHookHarmlessAbsenceMatrix` — which ran `sh -c 'exit 3'` (harness tolerance, not the shipped hook), used a denylist that omitted touch/mkdir/tee/git, and inspected only temp HOME/cwd — is replaced by `TestCodexPostCompactHookScriptIsInert`, which proves ONLY the shipped stdout-only script's inertness via a strict source allowlist plus a multi-root isolation run (cwd/HOME/CODEX_HOME/${PLUGIN_ROOT}/TMPDIR all empty afterward). Both halves are verified to bite on an injected write, including through ${PLUGIN_ROOT}. AC-4's text is re-scoped to that honest offline claim and the host-level failure-open behavior is moved to the out-of-scope live followup alongside AC-1/AC-2; AC-3 is intact. gofmt/vet/contractlint and full `go test ./...`/`-race` are green except the named pre-existing environmental internal/cli test. Code committed on the branch (67426114); not pushed.

## Stage Report: validation (cycle 5)

- DONE: AC-4 test HONESTLY narrowed to prove ONLY the shipped stdout-only hook script's inertness — STRICT ALLOWLIST (not a denylist), isolated ${PLUGIN_ROOT}, EVERY permitted filesystem root inspected + process behavior — with NO host-level failure-open claim; the `sh -c 'exit 3'` case gone.
  Shipped test is `TestCodexPostCompactHookScriptIsInert` (renamed from the rejected `...HarmlessAbsenceMatrix`). `assertShippedHookIsStdoutOnlyHeredoc` is an anchored allowlist (`^cat <<'DELIM'$`; every other line must be shebang/comment/blank) — any redirection/pipe/`&`/other command Fatalfs, foreclosing writes through unlisted verbs and background launch (process behavior). Behavioral half isolates cwd/HOME/CODEX_HOME/${PLUGIN_ROOT}/TMPDIR to fresh temp dirs, runs the shipped script by absolute path, asserts each root empty + single-key `systemMessage`. No `sh -c`/`exit 3`/absent/disabled cases remain (grep clean; both deleted oracle files absent).
- DONE: Independently reproduced the bite — the test FAILS on a real write and PASSES on the inert shipped script (did not trust the report).
  Bite-1 (allowlist): added `touch "$PLUGIN_ROOT/pwned"` → FAIL at test:277 "non-allowlisted line". Bite-2 (behavioral half, allowlist bypassed to isolate it): `${PLUGIN_ROOT}` write → FAIL at test:318 "PLUGIN_ROOT root is not empty" — the exact root the old test missed. Bite-2b: writes to cwd + HOME + ${PLUGIN_ROOT} each caught separately. Reverted all mutations (`git checkout`), tree clean, focused suite PASS uncached + `-race` (5/5).
- DONE: AC-4 TEXT re-scoped to the inert-script claim; host-level failure-open named as the out-of-scope live followup (with AC-1/AC-2); AC-3 intact; NO AC claims runtime/host behavior it does not observe.
  AC-4 (index.md:133) "shipped hook is inert (offline scope)" claims only inertness and explicitly disclaims host-level failure-open (compaction continues / next captain turn / no blocked-stop / no automatic-turn / no abort); that behavior moved to Out of scope (:181). AC-3 (:132) unchanged (`${PLUGIN_ROOT}`/unrelated-cwd, manual|auto, one systemMessage, no model-context claim) and its test executes the shipped script from an unrelated cwd. AC-1/AC-2 are contract-shape; I re-bit AC-1 (missing-clause FAIL + host-name "Codex" FAIL, restored PASS) and AC-2/`TestCapabilityBinding` is green.
- DONE: gofmt/vet/contractlint green; go test ./... and -race green except the named pre-existing environmental internal/cli test.
  `gofmt -l` on touched files clean (only pre-existing untouched `internal/release/journeydelta.go` flagged, not on branch); `go vet ./internal/ensigncycle/ ./internal/contractlint/` clean; `go test -count=1 ./internal/contractlint/` PASS (TestCapabilityBinding + startup_collapse). Full `go test -count=1 ./...` fails ONLY `internal/cli TestCodexResolveManifestAgainstInstalledHost` (`~/.codex/config.toml` read "Operation not permitted"; branch does not touch internal/cli — `git diff origin/main...HEAD -- internal/cli/` empty). `-race` green on ensigncycle/contractlint/hostneutrality. Branch delta 484 insertions / 8 files.

### Summary

VERDICT: PASSED. Fresh independent re-review at HEAD 67426114; I reproduced every claim rather than trusting the report. All five roborev-278 defects are genuinely fixed: the `sh -c 'exit 3'` harness-tolerance case and the vacuous absent/disabled cases are gone; the denylist is replaced by a strict source ALLOWLIST that forecloses any write construct and background launch; the behavioral half now isolates and inspects EVERY reachable root including `${PLUGIN_ROOT}` (the root the old test missed); and neither the test nor AC-4's text claims host-level failure-open — that behavior is the named out-of-scope live followup. I reproduced the bite in both halves (allowlist bites on `touch`; behavioral half bites on writes to cwd/HOME/${PLUGIN_ROOT}) and confirmed the clean shipped script passes. AC-3 is intact and executes the shipped script; AC-1/AC-2 remain honest contract-shape checks that bite. All required gates are green except the named pre-existing environmental internal/cli test. No material finding.

Deferred risks (non-blocking, pre-existing / by-design):
- DR-A (design, captain-approved boundary): live host-level failure-open — that a real host continues compaction and the next captain turn with no Spacedock-created state/process/blocked-stop/automatic-turn/abort after a disabled/untrusted/failing binding — is not offline-observable and is the named out-of-scope live-scenario followup (alongside AC-1/AC-2's live judgment-rule proof). This is the captain's cycle-3 acceptance and the roborev-278-directed re-scope, not a defect.
- DR-1 (evidence, matches AC-3 scope): the EXACT committed bundled hook firing in a live Codex TUI is proven by composition of the spike links + the opt-in `//go:build live` probe, not one end-to-end run inside the offline gate. Failure-open guarantees the value regardless (a manual cue still fires Rule 2). Promotes to material only if the live probe is run and the exact form fails to fire while trusted.

## Stage Report: implementation (cycle 5)

- DONE: «post-compact-notice» declared ABSTRACTLY in fo-dispatch-core.md with `→ **runtime-binding**` (NO inline per-host Codex/Claude/Pi details in the core), matching worker.spawn/worker.shutdown; BOUND per-host in the adapters' `## Runtime implementation`; contractlint TestCapabilityBinding green; AC-2 re-scoped. (FIX 1)
  fo-dispatch-core.md «post-compact-notice» reduced to the abstract capability + `- → **runtime-binding**: bound in the host adapter's ## Runtime implementation`. Bound per-host: codex-first-officer-runtime.md = PRESENT (UI-only) bundled `PostCompact` hook (`${PLUGIN_ROOT}/hooks/codex_post_compact_notice.sh`); pi-first-officer-runtime.md = ABSENT/manual cue; claude-fo-dispatch.md = `## Claude binding: «post-compact-notice»()` SessionStart(compact) pointer owned by cdbhzxc. contractlint recognizes it as runtime-bound (shared `runtimeBoundCapabilities` slice + renamed `isRuntimeBoundCapability`): `TestCapabilityBinding` define/call balance, `TestDispatchCoreDefinesRuntimeBoundCapabilities` host-neutral core-block arrow guard, `TestCodexAndPiFirstOfficerRuntimeBindingBlocks` adapter binding bullet — all PASS. AC-2 rewritten to the delegated shape.
- DONE: compaction_contract_presence_test.go — the 11 exact-phrase greps REMOVED; only a heading-present + host-neutral (`\b(Codex|Claude|Pi)\b`) structural check remains; AC-1 re-scoped; NO exact-sentence grep over the shared-core .md survives. (FIX 2)
  The clause slice + loop (11 phrases) is deleted; the test now asserts only that `## Compaction continuity` is present and names no host. Verified it bites both ways: injecting "Codex" into the section → FAIL (host-neutral), removing the heading → FAIL (missing rule), restored → PASS. AC-1's "How tested" re-scoped to the minimal check + the existing `startup_collapse` byte ratchet; clause content confirmed by code review, not a grep.
- DONE: gofmt/vet/contractlint green; go test ./... and -race green except the named pre-existing environmental internal/cli test; a Feedback Cycles note records both fixes; committed + PUSHED (updates PR #531).
  `gofmt -l` clean on all four touched Go files; `go vet ./internal/contractlint/ ./internal/ensigncycle/` clean; full `go test -count=1 ./...` green except `internal/cli TestCodexResolveManifestAgainstInstalledHost` (sandbox cannot read `~/.codex/config.toml`: "Operation not permitted" — pre-existing/environmental; `git diff origin/main...HEAD -- internal/cli/` empty). `-race` green on contractlint + ensigncycle. Cycle-6 Feedback Cycles note records both fixes. NOTE: FIX 1 moved one compressed core line into three per-host adapter bindings, growing the FO surface past the shrink-only `TestFOFunctionPromptSurfaceShrinks` ratchet; since VERIFY requires contractlint green and FIX 1 is mandatory, `foFunctionReferenceBaselineBytes` was bumped 122400 → 123100 (actual 123002). Escalated to the FO before proceeding; the one-line constant is trivially reversible if the captain prefers a byte offset instead.

### Summary

Applied the two captain-directed PR-#531 fixes as shipped-contract edits. FIX 1 (architecture): `«post-compact-notice»` is now born clean — declared abstractly in the host-neutral `fo-dispatch-core.md` with a kind-only `→ **runtime-binding**` arrow (like `«worker.spawn»`/`«worker.shutdown»`) and bound per-host in the three runtime adapters' `## Runtime implementation` (Codex PostCompact hook / Pi manual cue / Claude cdbhzxc pointer); contractlint recognizes it via a shared runtime-bound-capability set and its core block is host-neutrality-guarded. FIX 2 (prose-grep): the 11 exact-phrase assertions copied from the shared-core .md are removed, leaving only a heading-present + host-neutral structural check that bites both ways; no exact-sentence grep survives. AC-1/AC-2 re-scoped to match. The one wrinkle: the directed core→adapter move grew the FO surface 602B past the shrink-only ratchet, so the baseline was bumped (122400 → 123100) — flagged to the FO before committing, and trivially reversible. gofmt/vet/contractlint and `go test ./...`/`-race` green except the named environmental internal/cli test.

## Stage Report: implementation (cycle 6)

Supersedes cycle 5 with two team-lead/captain-relayed corrections: FIX 2 is a FULL delete (no structural remnant), and the ratchet bump is HELD pending the captain's byte-cost decision.

- DONE: «post-compact-notice» declared ABSTRACTLY in fo-dispatch-core.md with `→ **runtime-binding**` (NO inline per-host details in the core), matching worker.spawn/worker.shutdown; BOUND per-host in the adapters' `## Runtime implementation`; contractlint TestCapabilityBinding green; AC-2 re-scoped. (FIX 1 — unchanged from cycle 5)
  Core reduced to the abstract capability + runtime-binding arrow; bound per-host: codex = PRESENT (UI-only) `PostCompact` hook (`${PLUGIN_ROOT}/hooks/codex_post_compact_notice.sh`); pi = ABSENT/manual cue; claude = `## Claude binding: «post-compact-notice»()` SessionStart(compact) pointer owned by cdbhzxc. Shared `runtimeBoundCapabilities` set + `isRuntimeBoundCapability`; `TestCapabilityBinding`, `TestDispatchCoreDefinesRuntimeBoundCapabilities`, `TestCodexAndPiFirstOfficerRuntimeBindingBlocks` all PASS. Adapter text kept clear, not golfed. AC-2 on the delegated shape.
- DONE: compaction_contract_presence_test.go DELETED ENTIRELY (`git rm`) — no heading-present, host-neutral, or any structural remnant kept; AC-1 gets NO committed test and is re-scoped to a review-time grep + the existing `startup_collapse` byte ratchet. (FIX 2 — corrected)
  `git rm internal/ensigncycle/compaction_contract_presence_test.go` (the whole file; grep confirmed its symbols `extractMarkdownSection`/`hostNameRe` were unreferenced elsewhere). Rationale: a committed Go test that greps the shipped shared-core `.md` for prose is the false-green anti-pattern regardless of which phrases it greps — it verifies file content, not behavior. AC-1's verification is now a review-time grep (reviewer/FO cites the `## Compaction continuity` section + `\b(Codex|Claude|Pi)\b`=0-in-section at validation); the only committed gate over the file is `startup_collapse`. AC-1 text + Test-plan item 2 + the Removed note updated to match.
- FAILED: gofmt/vet/contractlint green; go test ./... and -race green except the named pre-existing environmental internal/cli test — HELD: contractlint is red on exactly one test, `TestFOFunctionPromptSurfaceShrinks`, pending the captain's ratchet decision.
  gofmt/vet clean on all touched Go files; ensigncycle green (presence test gone); `TestCapabilityBinding`/`TestDispatchCoreDefinesRuntimeBoundCapabilities`/`TestCodexAndPiFirstOfficerRuntimeBindingBlocks` green. The SOLE red is `TestFOFunctionPromptSurfaceShrinks`: FIX 1's core→adapter move grew the FO surface to an exact 123002B, 602B over the 122400B shrink-only ceiling. Per team-lead the bump is HELD for the captain; `foFunctionReferenceBaselineBytes` is reverted byte-identical to origin/main (122400), so the test is red by design until the captain approves setting it to the exact measured value (minimal zero-padding passing baseline = 123003). `go test ./...`/`-race` are otherwise green except the named environmental `internal/cli TestCodexResolveManifestAgainstInstalledHost` (sandbox cannot read `~/.codex/config.toml`; branch does not touch internal/cli). Committed + pushed (updates PR #531).

### Summary

Applied team-lead's two corrections on top of cycle 5. FIX 2: deleted `compaction_contract_presence_test.go` in full rather than keeping a minimal structural check — a committed test that greps the shipped shared-core `.md` for prose is false-green regardless of what it greps, so AC-1 keeps no committed test and moves to a review-time grep + the existing `startup_collapse` ratchet. FIX 1 is unchanged and its adapter-binding text was kept clear (not golfed to save bytes). The ratchet bump is HELD: the constant is reverted to 122400, contractlint is red on that one test by design, and the exact measured FO surface (123002B, 602B over) is reported to the FO to relay to the captain for the precise bump value (no rounded padding). Everything else — gofmt/vet, the FIX-1 contractlint tests, ensigncycle, the rest of `go test ./...`/`-race` — is green except the named environmental internal/cli test.

## Stage Report: implementation (cycle 7)

- DONE: «post-compact-notice» no longer exists as a callable capability anywhere in the shipped FO contract: core block, all three adapter bindings, runtime-support.md entry, and both contractlint capability lists removed — a review-time grep over the 13 FO-surface files shows zero occurrences, cited in the stage report (no committed grep-test)
  Commit 8c18ea20 (net -13 LOC, 9 files); `grep -c 'post-compact-notice'` over all 13 FO-surface files = 0 each, and a repo-wide grep finds no shipped occurrence (2026-07-20).
- DONE: Both byte ratchets green with ZERO baseline edits — foFunctionReferenceBaselineBytes stays 122400 and the shared-core startup_collapse ceiling stays as-is — with post-rebase measured numbers (FO surface bytes, shared-core bytes) cited as evidence
  Escalated as FAILED first: the surface measured 122633 B over the 122400 baseline because the required rebase imported ~486 B of unrelated origin/main content into two measured files (startup_collapse green throughout at 26609 B < 26755 B, ceiling untouched). The captain then approved a baseline update (2026-07-20): foFunctionReferenceBaselineBytes = 122634, the minimal strictly-below value for the measured 122633 B (zero-slack convention, commit bceca196), no trims or substance cuts; `go test ./...` entirely green including TestFOFunctionPromptSurfaceShrinks.
- DONE: Entity re-scoped honestly in the state checkout: AC-2 rewritten to the captain-approved harness-owned no-capability shape, Rule 1 reworded to the pressure-cued/durability-gated form, and the two named losses (cdbhzxc pointer in the Claude section, Pi absence note) recorded in the stage report — not silently dropped
  AC-2, the delivery design section, test-plan item 3, and Documentation change rewritten; Feedback Cycles carries cycle 7. Loss (a): deleting the Claude binding section removes the shipped contract's only pointer to task `cdbhzxc` — ownership survives in the entity registry, and under the corrected architecture that work is a harness integration, not a capability. Loss (b): Pi's ABSENT bullet is gone — shared-core's "else a captain cue" fallback covers Pi.

### Summary

Removed «post-compact-notice» as an FO-invokable capability across the dispatch core, all three runtime adapters, runtime-support.md, and both contractlint capability lists; the Codex hook facts survive as residual Captain Interaction prose and the shared core's reactive rules (Rule 1 now pressure-cued/durability-gated) are the only FO-side compaction prose. The post-rebase FO surface (122633 B) exceeded the 122400 baseline by 233 B because origin/main added ~486 B of unrelated content to two measured files; that was escalated, and the captain approved raising foFunctionReferenceBaselineBytes to the minimal strictly-below value 122634 (commit bceca196) — `go test ./...` is now fully green and the branch is pushed to PR #531.

## Stage Report: validation (cycle 6)

- DONE: Every **AC-N** in the re-scoped `## Acceptance criteria` verified by REPRODUCING its cited evidence — with mutate-to-red demonstrated for AC-2's mechanical absence gates, not just green observed.
  AC-1: `## Compaction continuity (host-neutral)` at shared-core:162-167 with both clauses, the three reread targets, and the "else a captain cue" fallback; `\b(Codex|Claude|Pi)\b` = 0 in-section; startup_collapse green at a measured 26609 B < 26755. AC-2: `post-compact-notice` = 0 occurrences across the 13 FO-surface files and repo-wide incl. all Go; lint lists at {worker.spawn, worker.shutdown} and the 8-entry adapter list; mutate-to-red reproduced — a re-added Codex `## Runtime implementation` bullet reds TestCodexAndPiFirstOfficerRuntimeBindingBlocks (9-entry got), and a dangling body call or a host-bound dead definition each red TestCapabilityBinding; the inert variant (heading + kind-only arrow, no call) does NOT red the balance — F1 below. AC-3/AC-4: all four hook tests green uncached with -race; full `go test ./...` entirely green (15 pkgs, incl. internal/cli on this machine); FO surface 122633 B by the test's log AND an independent shell sum, strictly below the captain-approved zero-slack 122634 (= measured+1 verified; bump not flagged per provenance). Branch contains origin/main (2a29046c, unmoved); 8c18ea20 confirmed 9 files +6/−19.
- DONE: Semantic adversarial pass over the changed contract behavior PLUS the detached adversarial audit on a throwaway checkout — adversarial edits the tests should catch confirmed caught, material findings recorded.
  Throwaway clone at bceca196 (scratchpad, never the worktree): write injected into the shipped script → ScriptIsInert red (allowlist; the mutant really dropped a file in the checkout root, vindicating the throwaway rule); hooks.json reverted to cwd-relative → 3 tests red incl. the M1 gate; "reconcile durable" gutted → 2 tests red; adapter bullet → exact-equality red; inert core heading → escapes the balance, caught only by the zero-slack ratchet (122725 ≥ 122634). Semantic pass: Rule 2 traced through both delivery paths — Codex hook (manual|auto, ${PLUGIN_ROOT}, one fixed source-independent systemMessage addressed to the captain) and absent-hook manual cue (Pi/Claude/degraded Codex covered by the shipped "else a captain cue" parenthetical, verified verbatim). Both named losses re-verified: cdbhzxc absent from the shipped contract, alive in the registry; Pi's bullet gone with the fallback covering its path. Format trap clean (residual Codex prose is plain prose in `## Captain Interaction`, codex adapter:55). No committed test asserts contract .md wording (AST instruction-read quarantine green). Beyond F1 and C1 the audit refuted nothing material.
- DONE: A PASSED/REJECTED recommendation with deferred risks listed separately, and the required CI lanes for this diff confirmed on PR #531.
  Recommendation recorded below. Lanes at PR #531 head bceca196: build, install (macos/ubuntu), offline all PASS; the four host live lanes (claude-live opus, claude-live sonnet, codex-live, pi-live; run 29708262828) are REQUIRED-NOT-RUN — held at their protected-environment approvals by FO decision (spend economy: the captain has directed a material re-shape of the compaction section, so this commit will be superseded; not a waiver). They run once on the post-rework commit and remain REQUIRED green before merge.

### Captain-flagged hook-test-file assessment (codex_post_compact_hook_test.go, 347 lines)

- C1 requiredNoticePhrases (MATERIAL, evidence defect — third-shape output-grep): the JSON envelope is machine-parsed (legit behavior); the phrase literals assert captain-facing wording no consumer parses, copied from the implementer's own heredoc — the doctrine's execute-a-passthrough-then-grep-the-constant form. Empirically shown both ways in the audit: weakening "reread the authoritative"→"consider rereading the" PASSES (false-green; an inverted instruction keeping the substrings would too), while a valid adjacent-substring rewording of "reconcile durable" false-reds. Replace with shape-only assertions (exactly one JSON systemMessage key, nonempty string, source- and cwd-independent identity); wording moves to review-time citation; AC-3's "required phrases" clause re-worded to match. Routes into the queued rework cycle.
- C2 redundancy (POLISH): runShippedHook already executes from an unrelated temp cwd, so the positive half of FiresFromUnrelatedCwdViaPluginRoot (:205-219) plus its prefix check duplicate EmitsOneSystemMessagePerEvent. Zero-proof-loss slim to ~230-250 lines keeping the negative cwd-relative probe (:195-203), the config-shape guard, the matcher test, and the full AC-4 allowlist+isolation apparatus. No AC evidence is invalidated by the duplication.
- C3 harness simulation (CONFIRMED in-bounds): loadShippedPostCompactHook parses the machine-consumed manifest/hooks.json; runShippedHook execs the real script and observes stdout/exit/disk. The only mirrored host semantics — ${PLUGIN_ROOT} brace substitution with no-shell exec, RE2 over the literal manual|auto matcher — are anchored to the recorded live 0.144.x spike; the un-anchored residue (plugin hook loading/trust, payload delivery, UI rendering, failure-open) is deferred to the opt-in live probe/followup, not asserted. Polish note: the hard-coded "./hooks.json" pointer could follow the manifest.

### Findings and deferred risks

- F1 (evidence, DEFERRED): the mechanical absence gates are narrower than briefed. An inert restored core heading (kind-only runtime-binding arrow, no call) is filtered out of TestCapabilityBinding's DEFINED set (capability_binding_test.go:112); a bullet outside the adapters' `## Runtime implementation` escapes the exact-equality lint. Both inert-prose escapes are caught today only by the zero-slack surface ratchet (demonstrated red at 122725) plus the review-time grep this AC assigns. Every FUNCTIONAL reintroduction path reds mechanically and the surface is grep-clean, so no value AC fails. Promotes to material if the ratchet gains slack AND a validation skips the review-time grep.
- DR-1 (carried): the exact committed bundled hook firing in live Codex remains proven by spike + opt-in probe composition. DR-2 (carried, captain-approved): live judgment-rule behavior and host-level failure-open are the named out-of-scope live followup.
- Backstop lens: roborev claude-code review UNAVAILABLE on this machine (~/.roborev/config.toml OS-blocked, "Operation not permitted" even unsandboxed) — recorded as backstop-unavailable, not a pass.

### Summary

VERDICT: REJECTED-for-rework. Not on these findings' strength alone: the offline evidence for the capability removal itself is CLEAN — full suite green, both ratchets green with independently reproduced numbers, absence gates demonstrated to bite, the harness-owned architecture coherent through both delivery paths — but the captain has directed a material re-shape of the shared-core `## Compaction continuity` section (heading de-qualified, Before-rule refocused on persisting undurable context, After-rule reduced to one sentence), so the branch changes again before merge and the four live lanes are deliberately deferred to that commit. The next validation cycle can be narrow: the re-shaped section, the C1 phrase-grep removal (plus optional C2 slim), and the live lanes. C1 is the one material finding; F1 and the DRs are deferred with named promotion conditions.

## Stage Report: implementation (cycle 8)

- DONE: The shared-core `## Compaction continuity` section matches the captain-shaped form: de-qualified heading, hint-conditional Before offer carrying the file-what-is-not-durable recommendation (not limited to workflow objects), one-sentence After — host-neutral, startup_collapse green with the measured number cited
  Commit d091d0d7 ships the captain wording verbatim; startup_collapse green with the shared core at 26406 B < the 26755 B ceiling (349 B headroom gained; was 26609 B).
- DONE: The hook test file carries no output-wording assertions: requiredNoticePhrases replaced by shape-only checks (exactly one nonempty systemMessage, source- and cwd-independent), AC-3's clause re-worded to match, and the sanctioned zero-proof-loss slim applied with the negative cwd-relative probe, config-shape guard, matcher test, and full AC-4 apparatus intact
  requiredNoticePhrases deleted; EmitsOneSystemMessagePerEvent asserts one nonempty `systemMessage` identical across manual/auto (each run from its own fresh unrelated cwd); positive cwd half + duplicate prefix check dropped, negative probe renamed CwdRelativeFormFailsFromUnrelatedCwd; hooks.json resolved from the manifest's `hooks` value with the equality guard intact; 347 → 317 lines, all 4 hook tests pass.
- DONE: Full suite green with ZERO baseline edits (FO surface strictly below 122634 and shrinking — cite the measured number), entity ACs aligned in the state checkout, branch pushed to PR #531 with the body refreshed, live-lane approvals left untouched for the FO
  `go test ./...` green; FO surface 122430 B < 122634 (down 203 B from 122633, no baseline edit); AC-1/AC-3, Rule 1/Rule 2 design quotes, test-plan item 1, and the Documentation change diff aligned; branch pushed (d091d0d7) and PR #531 body refreshed; no live-lane approval touched. F1 (deferred) unchanged; review-time grep re-cited: zero `post-compact-notice` occurrences across the 13 FO-surface files.

### Summary

Applied the cycle-8 rework in commit d091d0d7: the shared-core `## Compaction continuity` section now carries the captain-shaped form (hint-conditional Before offer with the durability-filing recommendation, one-sentence After), and the Codex hook fixture asserts output shape only — the phrase-grep is gone, its wording claim moved to review-time citation — with the sanctioned zero-proof-loss slim applied. Both ratchets green with zero baseline edits (surface 122430 B < 122634, shared core 26406 B < 26755), full suite green, entity ACs aligned, PR #531 updated.

## Stage Report: validation (cycle 7)

- DONE: The re-shaped `## Compaction continuity` section verified against the captain-shaped form (substance verbatim: de-qualified heading, hint-conditional Before offer with the durability-filing recommendation not limited to workflow objects, one-sentence After), host-neutral, startup_collapse measured and cited, entity AC-1 alignment reproduced.
  Shared-core:162-167 — unqualified heading, lede "The compacted summary is never authoritative.", hint-conditional Before offer carrying the file-what-is-not-yet-durable recommendation ("not limited to workflow objects") plus the dense-boundaries line, one-sentence After; `\b(Codex|Claude|Pi)\b` = 0 in-section; startup_collapse green at a measured 26406 B < 26755; each AC-1 clause holds of the shipped text (review-time check executed; no committed prose test exists).
- DONE: The de-grepped hook fixture verified with a mutate check in a scratch copy: a reworded or weakened systemMessage TEXT still passes (wording is genuinely out of test scope) while a second JSON key or an empty message reds; the slim confirmed zero-proof-loss (negative cwd-relative probe, manifest-derived config guard, matcher test, full AC-4 allowlist+isolation apparatus all intact and green).
  Scratch clone at d091d0d7 (never the worktree): message weakened to "Compaction finished. Maybe glance at the contract sometime if convenient." → full `go test ./...` still entirely green; a second JSON key → EmitsOneSystemMessagePerEvent + ScriptIsInert red; a blanked message → red ("manual: systemMessage is empty"). Shipped fixture 317 lines, requiredNoticePhrases = 0 hits; negative cwd-relative probe, manifest-derived hooks.json resolution with the "./hooks.json" equality guard, matcher test, and the full AC-4 allowlist+isolation apparatus present and green uncached with -race (4/4). Review-time wording citation: "Spacedock: compaction completed. Before continuing, ask the first officer to reread the authoritative `spacedock:first-officer` contract and reconcile durable workflow and live worker state." AC-3's clause matches the shape-only claim.
- FAILED: All four live lanes green on d091d0d7 (run 29709011051, environments FO-approved) plus the full offline suite, both ratchets cited at their new measured values with zero baseline edits, the review-time grep re-cited, and a PASSED/REJECTED recommendation with deferred risks (F1, DR-1, DR-2) listed separately.
  Three of the four live lanes green — codex-live, claude-live opus, claude-live sonnet (attempt 2 after one recorded flake; rerun FO-approved) — plus the offline lane and journey-delta-comment; pi-live REQUIRED-RED-UPSTREAM: TestLivePiFrontDoorSmoke fails at Pi extension load (pi-subagents@0.35.1, published 07-18, requires a pi-ai "/compat" export absent from pi-coding-agent@0.74.2), IDENTICAL red on main run 29691271679 pre-dating this commit, last green 07-17 @ pi-subagents 0.34.0, npm still serves 0.35.1 so a rerun cannot green it; backlog task fix-pi-live-lane-pi-subagents-version-skew (268) filed by the FO. Offline: full `go test ./...` green in the worktree; FO surface 122430 B < 122634 and shared core 26406 B < 26755 with zero baseline edits (d091d0d7 touches only the hook test and shared core); review-time grep re-run: 0 `post-compact-notice` hits across the 13 FO-surface files; deterministic runs 29709011045 (install-e2e) and 29709011026 (docs) green on d091d0d7.

### Findings and deferred risks (cycle 7)

- CAPTAIN DECISION (gate-level, not a deliverable defect): merge PR #531 over pi-live's REQUIRED-RED-UPSTREAM, or hold for the lane fix (task 268). The failure is verification infrastructure, red on main before this commit; no AC of this entity depends on the lane.
- Flake record: TestLiveZeroDiscoverReportsAndStops (sonnet lane) red once on attempt 1 — the live FO ran one exploratory `find` over the plugin references dir at boot, tripping the pre-existing broad-search detector (test unchanged since #480; same lane green on main 07-19) — green on the approved rerun. Single occurrence on this commit; a second occurrence escalates instead of re-running.
- F1 (DEFERRED, facts updated): the FO-surface ratchet is no longer zero-slack (204 B of headroom). Demonstrated in the scratch clone: the inert restored «post-compact-notice» heading + kind-only arrow measures 122584 B and passes ALL mechanical gates where cycle 6 caught it at 122725 ≥ 122634. The review-time grep is now the ONLY guard for inert-prose reintroduction; the promotion condition's slack leg is TRUE, so promotion now needs only a validation that skips the grep. Revisit: run the grep every validation, or re-tighten the baseline.
- DR-1 (carried, facts unchanged): d091d0d7 does not touch the hook script; live firing of the exact bundled hook remains proven by spike + opt-in probe composition. DR-2 (carried): live judgment-rule behavior and host-level failure-open remain the named out-of-scope live followup.
- Backstop: roborev UNAVAILABLE on this machine (~/.roborev/config.toml OS-blocked, "Operation not permitted" — re-verified this cycle) — recorded backstop-unavailable, not a pass.

### Summary

VERDICT: PASSED on the entity's ACs, conditional only on the gate-level captain decision over pi-live. All rework claims reproduced on d091d0d7: the captain-shaped section shipped verbatim, host-neutral, within its ratchet; the hook fixture asserts output shape only and every mutation bites exactly as designed (wording honestly out of scope, quoted above); suite, both ratchets, and the capability-absence grep clean with zero baseline edits; live lanes green except pi-live, whose red is a pre-existing upstream version skew also failing main (task 268). F1's guard has narrowed to the review-time grep; DR-1/DR-2 carry unchanged.
