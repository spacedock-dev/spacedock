Validation (cycle 2) complete for `resolution-consume-terminal-before-delivery`. Stage report committed path-scoped (`4547257d9`) and pushed via SSH to `spacedock-state/dev`.

**Result: recommend PASSED**, with 2 deferred evidence-defect holes recorded:

- **ACs re-run with observed exits** (worktree @ b4daa0791): CLI terminal suite exit 0 (journey, retry isolation, refusal matrix incl. digest-stale byte-clean, sole-consumer set refusal, golden); gates seam atomics `TestFinalizeTerminalApprovalOneLockedReplacement` + delivery op tests exit 0; `go test ./...` exit 1 with exactly the 2 env failures, re-proven identically red on throwaway main @ 3522ee457; `-race` clean on gates/status/ensigncycle (cli red only on the same 2 env failures); gofmt clean.
- **Architecture from the diff**: internal/status mutates no gates subtree; the two ops ride `writeEntityDocument`'s locked compare-before-replace with only the variadic verdict/completed extension; legacy path is `ErrNoGateRecord`-only (merge.go:553).
- **Detached adversarial audit** (/tmp/1w62-cadv): envelope-split → caught by `TestFinalizeTerminalApprovalOneLockedReplacement`; digest-skip → caught by `TestTerminalDeliveryRefusalsByteClean/digest-stale-authority` (CLI additionally shielded by merge.go's own classification layer); **lock removal → uncaught (hole 1)**; **mod-block-clear-before-classify reorder → uncaught (hole 2)** — the digest-stale fixture's mod-block is empty at refusal, so "refusal before the mod-block clear" ordering is pinned in source but not by any test. Both are evidence defects only; shipped code is correct, classified deferred risk.
- **Surface re-measure**: 23 files, +1417/−78 from git — the implementation report's "22 files" named as a one-line accounting defect (its +499/−45 core tally also diverges; my independent recomputation: core +500/−37 vs declared +205). Zero out-of-design machinery in diff bytes.
- **Live drive**: consume→rework→re-enter→regate→consume→sentinel→finalize in /tmp/1w62-live; archived entity carries status=done + verdict + completed + `pr: pr-merge:7` retained + superseded/consumed attempts. Movable baseline re-proven: main @ 3522ee457 consumes straight into `done` with zero delivery proof and has no `--rework`.