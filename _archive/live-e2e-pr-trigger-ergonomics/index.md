---
id: q12h3wny9k4ceq5xq4rnzn1t
title: live-e2e PR trigger ergonomics — drop the label gate, rely on the environment-approval gate (or auto-label)
status: done
source: CL (2026-05-31) — "labeling pr requiring e2e live on creation, otherwise they don't trigger… or mimic old main's ci-setting where they're always pending and we explicitly approve env"
started: 2026-06-01T05:04:22Z
completed: 2026-06-01T05:22:18Z
verdict: PASSED
score: "0.30"
worktree: 
issue:
mod-block: 
pr: "#245"
archived: 2026-06-01T05:22:19Z
---

Make the live-runtime e2e run on PRs without a manual per-PR step. Today `runtime-live-e2e.yml`
(on `next`) gates the live job with `if: workflow_dispatch OR PR-carries-'run-live-e2e'-label`, **on
top of** the CI-E2E* environment required-reviewer approval. So a PR that isn't labeled at creation
never queues the live job at all — the operator has to remember to add the label, or manually
dispatch. Two stacked gates (label + environment) do one job's worth of work.

## The two paths CL named

1. **Mimic old main: always-pending + explicit env approval (recommended).** Drop the `run-live-e2e`
   label condition from the live job's `if`. Every PR to `next` then queues the live job straight
   into `waiting` on its CI-E2E* environment; the maintainer approves the deployment for the PRs
   that should burn budget and ignores/rejects the rest. The environment required-reviewer gate
   already provides the budget safety the label was standing in for, so nothing runs a paid model
   unapproved. The secret-free `offline` job stays the always-run gate. This is exactly the
   "always pending, we explicitly approve env" model.
2. **Auto-label on PR creation (lower blast radius).** Keep the label gate but have the `pr-merge`
   mod apply the `run-live-e2e` label when it opens the PR (`gh pr create --label run-live-e2e`),
   removing the manual step while preserving the current architecture. Falls back to documenting the
   labeling requirement for hand-opened PRs.

## Acceptance criteria

**AC-1 — A PR to `next` queues the live job without any manual labeling step.**
Verified by: open a PR (or simulate the `if` evaluation) and confirm the live job reaches `waiting`
on its environment gate with no label applied — under path 1, by asserting the workflow `if` no
longer references the label; under path 2, by asserting the mod applied the label at PR-open.

**AC-2 — No paid model runs without an explicit approval.**
Verified by: the live job still declares `environment: CI-E2E*` and stays in `waiting` until a
required reviewer approves; assert the environment gate is intact after the change.

**AC-3 — The secret-free offline gate still runs on every PR.**
Verified by: the `offline` job (no environment, no secret) remains unconditional and is the PR's
real pass/fail gate.

## Test plan

Primarily a workflow-YAML change validated by reading the rendered `if`/`environment` conditions and
a dry PR (or `act`/`gh workflow view`); plus, for path 2, a `pr-merge` mod behavioral assertion that
`--label run-live-e2e` is passed. Ideation should pick path 1 vs 2 (FO lean: path 1 — fewer moving
parts, matches the model CL described, env gate already enforces budget safety), and confirm the
fork-PR security model (`pull_request` not `pull_request_target`; same-repo-or-no-secrets) is
unchanged by dropping the label condition.

## Decision (ideation): path 1

**Chosen: path 1 — drop the label clause; rely on the per-variant environment-approval gate.**
This is CL's always-pending-approve-env model, and the empirical footprint analysis below makes it
strictly cheaper and lower-risk than path 2.

### Why path 1 over path 2 (grounded in the actual files)

- **Blast radius is one file nobody else is editing.** The `run-live-e2e` label appears ONLY in
  `.github/workflows/runtime-live-e2e.yml` (and its per-worktree copies) — no mod, no Go code, no
  other workflow references it (`grep -rn run-live-e2e` confirms). Dropping the clause strands no
  other consumer. Every in-flight worktree copy of the file (`audit-host-neutrality-seam`,
  `phaseA-pr`, `claude-runtime-segregation`, `audit-cli-ergonomics`, `k6-pr`) is byte-identical to
  `origin/next` — no concurrent session is mid-edit on it. The two entities the FO flagged as
  possible collisions do NOT edit this file: `split-root-collaboration-remote-resume` (sc) touches
  `dispatch/helpers.go` / `status/handlers.go` / commission only; `roborev-validation-hook` merely
  *rides* `runtime-live-e2e.yml` as a proven pattern (its PR-JOB extension is a separate
  `roborev init gh-action`-generated file). So path 1 is genuinely DISJOINT and lands concurrently
  with both — no serialize-at-implementation needed.
- **Path 2 collides on a hot file.** Path 2 must edit `mods/pr-merge.md:51` (the `gh pr create`
  line), which jx/f2/merge-ceremony work is also touching — a real hot-file collision path 1 avoids.
  Path 2 is also semantically awkward here: `pr-merge.md:51` opens PRs with `--base main`, but the
  live-e2e trigger is `pull_request: branches: [next]`, so auto-labeling the `main`-based per-entity
  PRs would not even target the lineage that runs the live job.

### The exact `runtime-live-e2e.yml` edit (NEXT lineage)

Target baseline = `origin/next:.github/workflows/runtime-live-e2e.yml` (the PR-triggered,
label-gated, two-variant matrix — NOT the `main` `workflow_dispatch`-only single-env file; see
"main-vs-next divergence" below).

1. **Delete the live-job `if:` line.** Today:

   ```yaml
   if: ${{ github.event_name == 'workflow_dispatch' || contains(github.event.pull_request.labels.*.name, 'run-live-e2e') }}
   ```

   Remove it entirely. With no `if`, the `claude-live` matrix runs on BOTH declared triggers
   (`workflow_dispatch` and `pull_request: branches: [next]`). The `on:` block has only those two
   triggers, so no narrower replacement condition is needed — deleting the line is the whole change.

2. **Update the two comments that describe the now-removed label gate so they describe the code as
   it is** (the file's SECURITY MODEL header and the per-job comment above the `if`). Specifically:
   - Top-of-file header: drop the sentence "The live job is additionally label-gated: it runs only
     on a manual dispatch or a PR carrying the `run-live-e2e` label." and rewrite the trailing
     sentence so the gate it names is "same-repo-or-no-secrets + the per-variant environment
     approval" (drop "+ the label").
   - The `claude-live` per-job comment: replace the "runs only on an explicit opt-in … or a PR
     carrying the `run-live-e2e` label … this job adds the label gate ON TOP OF the per-variant
     environment approval" text with prose stating that every PR to `next` (and every manual
     dispatch) queues the live matrix, and each variant pauses in `waiting` on its CI-E2E* /
     CI-E2E-OPUS environment until a maintainer approves THAT deployment.

   No code comment may claim a label gate after the `if` is gone.

### Behavioral acceptance (path-1-specific, refines AC-1..AC-3)

- A PR whose base is `next`, with NO label applied, queues both `claude-live` matrix legs into
  `waiting` on their respective environments (CI-E2E for sonnet, CI-E2E-OPUS for claude-opus-4-8).
  Verified by asserting the rendered live-job `if` no longer references the label (it is absent),
  and `pull_request: branches: [next]` is retained.
- No paid model runs unapproved: each leg still declares `environment: ${{ matrix.environment }}`
  (CI-E2E* required-reviewer gates), intact after the edit. Verified by reading the matrix +
  `environment:` block post-change.
- The `offline` job is unchanged: NO environment, NO secret, runs unconditionally on every PR as the
  real pass/fail gate. Verified by confirming the `offline` job body and its lack of an `if`/`environment`.
- Fork-PR security model unchanged: trigger stays `pull_request` (NOT `pull_request_target`), so
  GitHub withholds repo secrets from fork PRs; same-repo-or-no-secrets is preserved. Dropping the
  label condition does not touch the trigger or `permissions:`. Verified by confirming the `on:`
  block and `permissions: contents: read` are unchanged.

### main-vs-next divergence (checklist item 3)

`main` and `next` carry structurally different versions of this file:

- **`main`:** `workflow_dispatch`-only, single `environment: CI-E2E`, `inputs.model` (default haiku),
  NO `pull_request` trigger, NO label gate, NO matrix.
- **`next`:** `workflow_dispatch` + `pull_request: branches: [next]`, two-variant matrix
  (sonnet→CI-E2E, claude-opus-4-8→CI-E2E-OPUS), label-gated live `if`.

This edit targets the **NEXT lineage only** — it has no meaning on `main`, which lacks the
`pull_request` trigger and the label clause entirely. Implementation branches from `next` (or the
integration branch that carries the matrix), edits the `next`-shaped file, and the change merges
forward with the rest of the `next` lineage; it must NOT be applied to the `main`-shaped file.

### Staff review

Not warranted. This is a localized, single-file workflow-YAML edit (delete one `if` line + reconcile
two comments) on a lineage whose security model is already documented in-file. FO view (no review)
stands.

## Stage Report: ideation

- DONE: Decide path-1 vs path-2; confirm path-1 preserves the CI-E2E* env gate, the secret-free offline gate, and the fork-PR security model
  Chose path-1 (see "Decision (ideation): path 1"). `grep -rn run-live-e2e` shows the label lives ONLY in this workflow file; env gate (CI-E2E/CI-E2E-OPUS matrix), unconditional offline job, and `pull_request` (not `_target`) trigger all preserved by dropping only the `if` label clause.
- DONE: Specify the EXACT runtime-live-e2e.yml edit for path-1 with behavioral AC
  Delete the live-job `if:` line (the only two `on:` triggers are workflow_dispatch + pull_request:[next], so no replacement condition needed); reconcile the SECURITY MODEL header + per-job comment so neither claims a label gate. Behavioral ACs (no-label PR → both matrix legs `waiting`; no unapproved paid run; offline unconditional; fork-PR model intact) written under "Behavioral acceptance".
- DONE: Reconcile main-vs-next divergence; flag sc serialize; state staff-review need
  Edit targets NEXT lineage only (main is workflow_dispatch-only single-env, lacks the trigger+label). sc serialize is NOT needed: split-root-collaboration-remote-resume edits Go files (helpers.go/handlers.go), not this workflow; roborev-validation-hook only RIDES the pattern via a separate generated file. All in-flight worktree copies are byte-identical to origin/next — path-1 is genuinely disjoint and lands concurrently. Staff review: not warranted (localized single-file YAML edit).

### Summary

Resolved ideation in favor of path-1: drop the `run-live-e2e` label clause from the `claude-live` job's `if` on the NEXT-lineage `runtime-live-e2e.yml`, leaving the per-variant CI-E2E*/CI-E2E-OPUS environment-approval gate as the sole budget control (CL's always-pending-approve-env model). Grounded the recommendation empirically: the label is referenced in no file but this workflow, every in-flight worktree copy is identical to `origin/next`, and the FO-flagged collisions (sc, roborev) do not actually edit this file — so path-1 is disjoint and needs no serialization. Corrected the FO's "sc also edits this file" note: it does not. The exact edit is delete one `if:` line plus reconcile two now-stale comments; no staff review warranted.

## Stage Report: implementation

- DONE: Path-1 edit in .github/workflows/runtime-live-e2e.yml (NEXT lineage): drop the `contains(...'run-live-e2e')` clause from the claude-live job's `if` so the live job runs on workflow_dispatch OR any pull_request to next; update the stale header-comment sentences; do NOT touch any other job/file
  Worktree commit 34275c28: deleted the live-job `if:` line entirely (the only two `on:` triggers are workflow_dispatch + pull_request:[next], so no replacement condition needed). Reconciled three comments — SECURITY MODEL header, the `pull_request` inline comment, and the per-job comment — so none claims a label gate. Diff: 1 file, 11 insertions / 12 deletions; `grep -ni 'label\|run-live-e2e'` on the file → none.
- DONE: Confirm path-1 preserves the safety model: claude-live still declares `environment: CI-E2E*`; the secret-free `offline` job still runs unconditionally; trigger stays `pull_request` (NOT pull_request_target); behavioral AC that a no-label PR reaches the live job's `waiting` env-approval state and offline runs
  Asserted via `yq` against the rendered file: `claude-live.has("if")` → false (gate gone); `claude-live.environment.name` → `${{ matrix.environment }}` (matrix legs sonnet→CI-E2E, opus→CI-E2E-OPUS intact); `offline.has("if")`/`has("environment")` → false/false (unconditional, secret-free, AC-3); `on` keys → workflow_dispatch + pull_request (branches:[next]); `permissions` → contents: read. AC-1: with `if` absent the job is runnable on a no-label PR to next, and because it declares a required-reviewer `environment` GitHub holds it in `waiting` until approval (AC-2) — offline runs immediately as the real gate.
- DONE: Validate the YAML parses (yq/actionlint if available); note the main-vs-next divergence (main stays workflow_dispatch-only single-env — do NOT touch main's copy); commit on the worktree branch; entity body + stage report to the .spacedock-state checkout
  YAML parses clean under `yq` (actionlint/act not installed in this env; yq used as the structural validator). main's copy left untouched — `git show main:.github/workflows/runtime-live-e2e.yml` confirms it remains the workflow_dispatch-only single-env shape (no pull_request trigger / matrix); only the next-shaped worktree copy was edited. Code committed on worktree branch `spacedock-ensign/live-e2e-pr-trigger` (34275c28); entity body + this report committed path-scoped to the state checkout.

### Summary

Implemented path-1: removed the `run-live-e2e` label clause from the `claude-live` job's `if` in the NEXT-lineage `runtime-live-e2e.yml` and deleted the line entirely (the `on:` block declares only workflow_dispatch + pull_request:[next], so no replacement condition is needed). Reconciled all three stale comments — the SECURITY MODEL header, the `pull_request` inline comment, and the per-job comment — so none claims a label gate. Verified via `yq` that the safety model is intact: the per-variant CI-E2E*/CI-E2E-OPUS environment-approval gate remains (AC-2), the secret-free `offline` job stays the unconditional PR gate (AC-3), the trigger stays `pull_request` not `pull_request_target` (fork-PR model), and a no-label PR to `next` now reaches the live job's `waiting` env-approval state (AC-1). main's structurally-different copy is untouched. Code on worktree branch (34275c28).

## Stage Report: validation

- DONE: Verify the runtime-live-e2e.yml edit (NEXT lineage, branch spacedock-ensign/live-e2e-pr-trigger): claude-live if-condition DROPPED the contains(...run-live-e2e...) label clause; job still declares environment: CI-E2E*; secret-free offline runs unconditionally; trigger stays pull_request (NOT pull_request_target)
  Read the rendered yml + asserted via `yq` at HEAD 34275c28: `.jobs.claude-live | has("if")` → false (entire job-level if/label gate gone — the ONLY remaining `if:` in the file is the `always()` artifact-upload step); `.jobs.claude-live.environment.name` → `${{ matrix.environment }}` with matrix legs sonnet→CI-E2E + claude-opus-4-8→CI-E2E-OPUS intact; `.jobs.offline` has no `if`/`environment`/`env` (unconditional + secret-free); `.on` keys → `[workflow_dispatch, pull_request]`, `.on.pull_request.branches` → `[next]`, `.on | has("pull_request_target")` → false; `.permissions` → `{contents: read}`; `.jobs.claude-live.env.ANTHROPIC_API_KEY` → `${{ secrets.ANTHROPIC_API_KEY }}` (secret confined to the gated job).
- DONE: Behavioral AC: a no-label PR to next reaches the claude-live waiting env-approval state, no paid model runs without approval; header-comment label-gate prose updated; main's copy UNTOUCHED
  Rendered-if reasoning (AC-1/AC-2): the workflow fires on a PR targeting `next`; `claude-live` carries no job-level `if`, so it is selected with no label predicate; `needs: offline` gates it on the offline pass; the per-variant required-reviewer `environment` holds each matrix leg in `waiting` until a maintainer approves — so a no-label PR queues both legs into `waiting` and no `ANTHROPIC_API_KEY`-bearing step runs pre-approval. Prose reconciled: `grep -ni 'run-live-e2e\|label'` on the file → NONE (header SECURITY MODEL sentence, the `pull_request` inline comment, and the per-job comment all rewritten to "same-repo-or-no-secrets + per-variant environment approval"). main UNTOUCHED: my branch HEAD (34275c28) is off origin/next and is NOT an ancestor of origin/main; `git diff origin/next...HEAD` touches only the next-lineage file (blob 198a73ce→e1450928); origin/main's `runtime-live-e2e.yml` is a different `pull_request_target`/matrix blob (54b7b842) my diff never touches.
- DONE: YAML parses (yq/actionlint if present); no other job/file changed (git diff origin/next...HEAD = only runtime-live-e2e.yml)
  `yq '.' runtime-live-e2e.yml` → PARSE OK (actionlint + act not installed in this env; yq is the structural validator). `git diff --stat origin/next...HEAD` → exactly one file, `.github/workflows/runtime-live-e2e.yml`, 11 insertions / 12 deletions; no other job or file in the diff. Spot-check (validation-stage cheap-check before trusting the live job): `TestLiveEnsignCycle` exists at `internal/ensigncycle/live_test.go:55` (the live job's `-run` target is real); `go test ./internal/ensigncycle/` without `-tags live` → "no tests to run" for the live test (compiles OUT of the offline secret-free surface, AC-3), and `go test -tags live -list` shows the live surface compiling IN — the build-tag toggle that keeps the offline gate secret-free works.

### Recommendation: PASSED

All three checklist items and AC-1/AC-2/AC-3 verified with evidence; the deliverable matches the path-1 spec exactly. One non-blocking observation for the FO: both the ideation body ("main: workflow_dispatch-only, single environment: CI-E2E, NO pull_request trigger, NO matrix") and the implementation report describe `main`'s copy of this file as workflow_dispatch-only single-env — but `origin/main`'s actual `runtime-live-e2e.yml` (blob 54b7b842) is a `pull_request_target` matrix-shaped workflow (CI-E2E / CI-E2E-OPUS / CI-E2E-CODEX, pytest/make). This stale main-description does NOT affect the verdict: the requirement is "main's copy UNTOUCHED," which holds — the lineages are disjoint and my next-lineage diff never touches main. Flagging only so the workflow's main-vs-next divergence note can be corrected later if it matters.

### Summary

Validated the path-1 deliverable: the `claude-live` job's label-gate `if` is fully removed (no job-level `if` remains; only an `always()` artifact step), so every PR to `next` (and every workflow_dispatch) queues the live matrix with no manual labeling — AC-1. The per-variant CI-E2E*/CI-E2E-OPUS required-reviewer environment gate is intact and `ANTHROPIC_API_KEY` stays confined to that gated job, so no paid model runs pre-approval — AC-2. The secret-free `offline` job remains unconditional with no environment/secret as the real PR pass/fail gate, and the live-tagged test compiles out of the default surface — AC-3. Trigger stays `pull_request` (not `_target`) and `permissions` stays read-only (fork-PR model intact). YAML parses clean under `yq`; `git diff origin/next...HEAD` is exactly one file (11+/12-) and main's structurally-different copy is untouched. Recommend PASSED, with a non-blocking note that the workflow's "main is workflow_dispatch-only single-env" divergence description is stale vs origin/main's actual pull_request_target shape.
