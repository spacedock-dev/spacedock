---
title: Own the rejection-flow FO adherence residuals
status: backlog
source: "Blind shape-attribution analysis 2026-08-16 (_debriefs/2026-08-16-03-rejection-shape-attribution.md); captain approved filing"
id: 4rgdwbg3bj9jnyrk4nptjk6r
---

## Superseded at filing

Captain ruling (2026-08-16): this and run-rejection-journey-in-team-mode
(zqb683j8jth0tyr2eme231e2) are the same thing — the residuals were observed under the
bare invocation that entity removes, so behavior must be re-measured inside its
team-mode proof loop. Content folded there verbatim; archived unstarted.

## Problem

With all three stack fixes in place (inline state commit, rewritten skill, recognizer quoting), two real FO adherence failures remain on the composed tree, proven from preserved run streams:

1. The FO records both validation rounds, then ends the journey without ever invoking `gate prepare` — violating the rewritten skill's step-5 done-condition ("exactly one fresh open gate has been prepared and presented"). Observed in composed-tree loop run 1. The fixture's "leave the entity nonterminal" does not excuse it: a prepared open gate is nonterminal, and the green runs end exactly there.
2. The codex FO fresh-dispatches the fix worker while the followup surface is live and every reuse condition passes — the route the codex adapter calls the load-bearing exception to casual fresh dispatch. Observed in composed-tree loop run 3 (spawns=2, with `--advance` used both before and after in the same stream).

These are the only red causes that survive the harness repairs, and no entity owns them: any future rerun provision or XFAIL citation for this journey has nothing active to cite.

## Proposed approach

Ideation decides between: (a) targeted hardening of the skill or adapter text at the exact point each deviation enters, with falsifying evidence per mode; or (b) owner-only — this entity stands as the active owner of the residual rate, evidence attached, until N clean cadence runs retire it. Either way this entity holds the canonical evidence pointers: loop run-1 and run-3 streams under the repair entity's evidence/ dir and the job scratch ledgers.

## Out of scope

- Harness/grading changes (owned by run-rejection-journey-in-team-mode).
- The already-landed skill and binary fixes.
