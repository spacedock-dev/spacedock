---
id: 2dfpswjwbxez1km6439wfrsn
title: Make channel install non-destructive and give edge installs a working re-pull key
status: validation
source: "Captain directive, CL 2026-08-17: \"we should simplify the marketplace, there should just be one way\", then the chosen shape: \"i think spacedock-edge@marketplace is better, because we now have other things like subspace in the same marketplace.\" Raised from the live cross-host channel investigation; verified against the published marketplace on both refs and against a route-A install cached on the captain's workstation."
started: 2026-08-17T19:38:45Z
completed:
verdict:
score:
worktree: .worktrees/spacedock-ensign-collapse-duplicate-edge-marketplace-routes
issue:
---

One task, end to end: channel installs stop destroying shared marketplace state, edge installs re-pull to a contract that matches their binary, dev codex launches stop paying a measured 40-second copy tax, and the stalled edge release line advances again. Round 1's route collapse stands unchanged underneath: one published id per channel, channel in the marketplace NAME, zero id changes in the binary.

## Problem — the measured failure chain

Five defects, each exercised live 2026-08-17 (hosts: claude 2.1.226, codex-cli 0.147.0; all probes in isolated homes — `CODEX_HOME` / `CLAUDE_CONFIG_DIR`, isolation itself proven with a marker-plugin install that left the real home untouched).

**1. The install sequences destroy shared marketplace state.** `installArgvSequence` step 2 is `plugin marketplace remove <channel-marketplace>`; on stable that names `spacedock`, the marketplace that also hosts `subspace` and `cargento`. Measured on claude 2.1.226: `plugin marketplace remove` with installed dependents succeeds and CASCADE-UNINSTALLS every plugin installed from it (probe: two plugins installed from a test marketplace; after remove, `claude plugin list` reports none). Codex cascades identically, and `codexInstallArgvSequence` removes BOTH channels' marketplaces — the captain's real codex config has `subspace@spacedock` installed, so any codex heal or `spacedock install --host codex` run wipes it.

**2. The documented re-pull key is dead twice over.** `release.yml` and `next-publish.yml` bump the calendar version in the IN-REPO `.claude-plugin/marketplace.json` on `next` — but live edge installs resolve the STANDALONE `spacedock-dev/marketplace` repo's `edge` branch, whose entry version froze at `0.0.2026062001` (2026-06-20; `docs/releasing.md:265` already records the dead field). Deeper, measured: the marketplace ENTRY version is INERT on both current hosts. `claude plugin update` keys on the plugin MANIFEST version at the entry's source ref — bumping the manifest at the source triggers a pull with the entry untouched, bumping the entry with the manifest untouched is a no-op ("already at the latest version"). Codex has no `plugin update` at all; `codex plugin add` re-clones unconditionally. So no calendar bump, in ANY file, can deliver a re-pull on current hosts — and no cross-repo credential is needed to fix this, because the live re-pull key is `next`'s `plugin.json` version, which same-repo automation already stamps.

**3. The edge release line is STALLED (discovered this round).** A manual bare stamp on main (`ff9bb4506`, 2026-08-10, "release: bump version to spacedock@0.27.0" — no automation writes that message) rode the `v0.27.0-pre4` tag into `next`: the manifest-tag gate deliberately skips `-pre` tags (release.yml:163), and the `-X theirs` reconcile copied the bare `0.27.0` manifest onto `next`. `ComparePreVersion` correctly ranks any `0.27.0-preN` BELOW bare `0.27.0`, so every subsequent `-pre` tag's edge-advance decision prints `skip` — proven live: `spacedock-release edge-advance-decision v0.27.0-pre7 <next-manifest>` → `tag v0.27.0-pre7 target edge version 0.27.0-pre7 vs next 0.27.0` → `skip`. Last reconcile on `next` is `dbb0675c4` (pre4); tags pre5–pre7 produced none. Drift measured: `next` is 99 commits behind main, 38 ahead. While stalled, the calendar-bump step (gated on `advance`) never ran either — the re-pull key's writer is parked behind the same skip.

**4. No automation syncs an existing edge install to its binary.** The version gate compares major.minor only, so an installed `0.27.0-pre1` plugin against a `0.27.0-pre7` binary is Compatible and the launcher never refreshes. Claude does not auto-update plugins (field evidence: the captain's claude edge install sat at pre1 from 2026-07-21 for four weeks of daily use across manifest movements that a version-diff auto-updater would have pulled). Measured consequence, this session: the first officer ran contract `0.27.0-pre1` against a `0.27.0-pre7` binary — 49 changed shared-core lines, 96 changed dispatch-core lines, and a gate-required skill (`fo-gate-lifecycle`) absent. Mid-session (2026-08-17T20:35Z) the real claude install moved to bare `0.27.0` @ next HEAD by an actor outside this worker (isolation-proven) — the exact stranded shape defect 3 publishes.

**5. Codex dev launches pay a ~40s per-launch copy tax (the captain's slowness, root-caused).** `spacedock codex` with `--plugin-dir` or an adjacent checkout runs `installCodexLocalPluginDir` on EVERY launch, before and regardless of the gate. The local marketplace symlinks the REPO ROOT as the plugin; `codex plugin add` copies the symlink target wholesale into the plugin cache and serves skills from that cache (proven: a marker skill added to the live checkout does not appear in `codex debug prompt-input` without a re-add). This checkout is 3.6 GB — 3.0 GB `.worktrees`, 91 MB `.git`, 65 MB `tmp/` — against a plugin surface under 1 MB (skills/ 800 KB). Measured with the exact six-step sequence: 29.0s fresh, 40.3s repeat (the repeat also deletes the prior 3.6 GB copy; sys-time dominated). The captain's real codex cache holds a 4.8 GB copy today. Refuted leads, also measured: the frozen entry version does NOT gate `TooOldPlugin` per launch (codex records the MANIFEST version — listing and cache dir show `0.27.0`, not `0.0.2026062001`; the gate reads the cached manifest → same minor → Compatible → no heal); `codex plugin list` at the gate costs 0.02–0.03s; the installed plugin adds ~0.02s to codex prompt assembly (0.12–0.14s vs 0.11–0.15s empty home). Steady-state codex startup outside the plugin-dir path is not measurably ours: a full real-home `codex exec` roundtrip is 6.2s including the model call.

## Spike results — name match and namespace derivation (round 1, RESOLVED, stands)

Exercised live on this workstation 2026-08-17, before any design was settled. Hosts: codex-cli 0.147.0, claude 2.1.226. All probes in isolated homes; real config restored after.

**(a) Codex enforces entry-name == manifest-name at add time — still true.** `go test ./internal/cli -run TestCodexEntryNameMustMatchPluginName` re-run against the real CLI: PASS. Independently re-proven in a fresh `CODEX_HOME` with a two-entry family marketplace in the captain's exact target shape.

**(b) BOTH hosts derive the skill namespace from the MANIFEST name, not the entry name.** codex: `codex debug prompt-input` flips `spacedock-edge:probe` → `spacedock:probe` when only the cached manifest name changes; no override field exists. claude: accepts entry != manifest, and a live headless session lists the skill under the MANIFEST name.

**Verdict: the entry-name shape (`spacedock-edge@spacedock`) is falsified on codex** — it renames every edge skill to `spacedock-edge:*`. The surviving collapse keeps the channel in the marketplace NAME (route B, `spacedock@spacedock-edge`) and retires the route-A entry. Route A was never installable on codex, so its retirement is claude-only.

## Measurements (round 2) — host semantics and timings

Every mechanism below rests on one of these live probes.

| # | Probe | claude 2.1.226 | codex 0.147.0 |
| --- | --- | --- | --- |
| 1 | `marketplace remove` with installed dependents | succeeds, cascade-uninstalls ALL dependents | same cascade |
| 2 | `marketplace add`, name already registered, same source | no-op, exit 0 ("already on disk"); git snapshot NOT refreshed | silent no-op, exit 0, 0.01s; snapshot NOT refreshed |
| 3 | `marketplace add`, same name, DIFFERENT source | succeeds — re-registers, replacing the source | refuses ("remove it before adding"), exit 0, old source kept |
| 4 | `marketplace update` / `upgrade <name>` | refreshes snapshot from recorded source; works on local-path sources | refreshes git snapshot (11.2s); on a local-path marketplace prints an error but exits 0 |
| 5 | `plugin update`, content moved, manifest version unchanged | no-op ("already at the latest version") | (no update verb) |
| 6 | `plugin update`, manifest version changed at source ref | pulls; SYNCS on any difference including downgrade (pre10→pre9); numeric prerelease ordering correct (pre9→pre10) | (no update verb) |
| 7 | `plugin update`, only the marketplace ENTRY version bumped | inert — no pull | entry version inert everywhere: listing/cache keyed on manifest version |
| 8 | `plugin install`/`add`, already installed | no-op, exit 0; does NOT refresh | RE-CLONES content in place — fresh content without a remove |
| 9 | uninstall + install cycle, same manifest version, content moved | re-clones; cache dir overwritten with fresh content | remove clears config+cache; add re-clones |

Timings (this workstation): codex network heal, full current six-step: 9.0s (`marketplace add` clone 0.7s, `plugin add` clone 5.6s). claude current four-step: 3.5s fresh, 3.3s repeat. Plugin-dir codex six-step against this checkout: 29.0s fresh / 40.3s repeat. `codex plugin list`: 0.02–0.03s. Codex prompt assembly: 0.11–0.15s empty home, 0.12–0.14s with the edge plugin, 0.58–0.69s on the loaded real home.

## Proposed approach

**Round 1's collapse stands as mechanism 1** (route-A entry retirement in the marketplace repo; channel stays in the marketplace NAME; the gate must still surface the directive reversal — see round 1 sections). New mechanisms:

**2. Non-destructive install sequences (both hosts).** No step ever removes a marketplace record. Before/after:

claude — `installArgvSequence`, from:

    plugin uninstall spacedock@<channel-mkt>        (tolerated)
    plugin marketplace remove <channel-mkt>          (tolerated)   ← cascade-uninstalls dependents on stable
    plugin marketplace add <source>                  (fail-fast)
    plugin install spacedock@<channel-mkt>           (fail-fast)

to:

    plugin uninstall spacedock@<channel-mkt>         (tolerated)
    plugin uninstall spacedock-edge@spacedock        (tolerated)   ← route-A migration, round 1
    plugin marketplace add <source>                  (fail-fast)   ← probe 3: natively re-pins a changed source
    plugin marketplace update <channel-mkt>          (tolerated)   ← probe 4: snapshot refresh, the non-destructive re-pin
    plugin install spacedock@<channel-mkt>           (fail-fast)   ← probe 9: unconditional fresh clone

codex — `codexInstallArgvSequence`, from six steps (remove both plugins, remove BOTH marketplaces, add, add) to:

    plugin remove spacedock@<other-channel-mkt>      (tolerated)   ← channel exclusivity, global skill namespace
    plugin remove spacedock@<channel-mkt>            (tolerated)   ← cache hygiene: keeps latestVersionDir single-dir
    plugin marketplace add <source>                  (fail-fast)
    plugin marketplace upgrade <channel-mkt>         (tolerated)   ← probe 4: git-snapshot re-pin; local sources error harmlessly at exit 0
    plugin add spacedock@<channel-mkt>               (fail-fast)   ← probe 8: re-clones content

The re-pin the deleted removes existed to force survives three ways, all probed: claude `marketplace add` replaces a changed source natively (probe 3); `marketplace update`/`upgrade` refresh the snapshot from the recorded source (probe 4); and the plugin content pull is unconditional on both hosts (probes 8/9) — it never depended on marketplace state beyond the entry's source URL/ref. The codex own-channel `plugin remove` stays for a measured reason: without it version dirs accumulate in the cache and `latestVersionDir`'s lexical fallback misorders `0.27.0-pre10` below `0.27.0-pre9`, so the gate could read a stale manifest. Known limitation, accepted and documented: a FUTURE marketplace-source migration on codex (probe 3 refusal) would need its own explicit remove+add step; this task changes no sources.

**3. Edge contract sync at the front door (the scope-4 decision: IN, as a non-blocking refresh).** The blocking gate is UNCHANGED (minor-exact, both directions, stable and edge). New: when `devBranch != "main"` and the gate returns Compatible, the front door compares full versions prerelease-aware (`release.ComparePreVersion` semantics; binary display version stripped of `+build`) and runs one best-effort `Install` before launching when the BINARY IS STRICTLY NEWER, or when the plugin version on the edge channel carries NO prerelease at all (a bare version on the edge line is illegal by construction — the same poison rule as mechanism 5 — and is exactly the stranded shape defects 3–4 published; ComparePreVersion alone would rank it "newer" and never heal it). Plugin-newer-within-the-minor does NOT refresh (a not-yet-upgraded binary must not re-pull ~9s per launch). `--no-install` skips the refresh. A refresh failure warns and launches anyway — no launch that succeeds today is newly blocked. `contract.Result` gains a `PluginVersion` field so the front door can compare without re-reading the manifest. Justification for edge-only: on stable the contract within a minor is interchangeable by policy (D1) and skew windows close at each release; on the edge line the contract IS the product and skew is unbounded — measured at four weeks/six prereleases this session. Known bounded cost, declared: a source-built binary stamped AHEAD of `next`'s manifest re-pulls per launch (~3.5s claude / ~9s codex) until the next `-pre` tag lands; source-build dev loops normally ride `--plugin-dir`, which bypasses the gate entirely.

**4. Codex plugin-dir staging (the 40s fix).** `WriteCodexLocalMarketplace` stops symlinking the repo root and instead stages a filtered copy of the plugin surface into the persistent marketplace dir — include list: `.codex-plugin/`, `.claude-plugin/`, `skills/`, `agents/`, `hooks/`, `hooks.json` (everything the manifests and hook config reference; <1 MB vs 3.6 GB). Each launch re-stages (cheap) and the sequence re-installs from the stage, preserving the dev-loop freshness the per-launch reinstall exists for — freshness MUST flow through the cache because codex serves from it (live-symlink marker probe). Refresh-skipping alternatives are rejected below.

**5. Edge-advance bare-version guard + natural repair — SUPERSEDED, see "Design change" below.** Originally: `EdgeAdvanceDecision` treats a bare (no-prerelease) `nextVersion` as poisoned state and ADVANCES any `-pre` tag over it, instead of ranking the tag below it. This is now moot: the edge marketplace entry no longer tracks `next` at all (it resolves `main` directly), so there is no `next`-manifest comparison left to poison or guard. The mechanism is a DELETION of the `next`-reconcile job, not a guard added to it.

**6. Retire the dead calendar machinery.** Drop the `bump-calendar` invocation from release.yml's edge-advance step; delete `next-publish.yml` (its only job was the calendar bump; an out-of-band edge re-pull is now "cut a `-pre` tag" against `main`, automated end to end via the auto-pre0 cut); delete the `bump-calendar` command and `release.BumpCalendarVersion` with their tests; rewrite `docs/releasing.md`'s re-pull prose to name the real key (`main`'s manifest version, resolved directly by the edge marketplace entry). The in-repo `.claude-plugin/marketplace.json` FILE stays: it is a pinned transitional bridge (`channel_agreement_guard_test.go:245` hard-fails on its removal — released v0.20.0 binaries resolve installs from it); it simply stops being bumped. Unchanged by the "Design change" below except that the `git push origin edge-advance:next` step this bullet originally kept is ALSO deleted now — see below.

Rollout order: merge this task to main → cut a `-pre` tag from `main` whenever edge content should advance (no guard/reconcile dependency left — see "Design change" below) → the edge cask bumps → every edge launcher heals its own install via mechanism 3 (the bare carve-out covers installs that grabbed `0.27.0` during the stall, including the captain's claude install as of 2026-08-17T20:35Z). The marketplace-repo route-A entry removal follows the binary, per round 1's Migration.

## Design change — `next` retired as the edge re-pull tracker (round 3)

FO-authorized, captain-approved, 2026-08-17. The edge marketplace entry (`spacedock-dev/marketplace`, `edge` branch, `marketplace.json`) now points its `spacedock` plugin source at `main` — not `next`. Team-lead committed the one-line change directly to the marketplace repo (`e869a67`) and live-probed it end to end on this workstation: `claude plugin marketplace update spacedock-edge` then `claude plugin update spacedock@spacedock-edge` moved the installed plugin from `0.27.0` to `0.27.0-pre7`, and the pulled first-officer contract diffed byte-identical against repo `main`. The captain's stale-contract problem (the whole reason mechanism 5's guard existed) is fixed by this alone, with no code.

**Why this dissolves mechanism 5 rather than fixing it.** `next` existed as a re-pull tracker only because the marketplace manifest used to live IN this repo, where a branch was the only available channel separator. Since round 1's collapse, the channel lives in the marketplace NAME instead, and prereleases are tagged directly on `main` — so `next` had become a second copy of `main` that a CI job (`edge-advance`) had to keep synced by comparing a tag's target version against `next`'s current manifest. That comparison is EXACTLY the mechanism that silently skipped every `-pre5`–`-pre7` tag once a bare `0.27.0` poisoned `next` (defect 3) — the bug mechanism 5 was designed to guard against. Deleting the comparison (and the reconcile it gated) removes the entire silent-skip failure class instead of hardening a path that no longer needs to exist. Per the review-finding disposition and the mechanism/value re-check this stage's own definition requires: the simpler supported-runtime path (point the marketplace entry at the branch that's already correct) reaches the value; repairing the enabling mechanism (the guard) would have been effort spent hardening dead code.

**What survives, unchanged in purpose:** the auto-pre0 tag cut (release.yml's `edge-advance` job, gated on a NEW "is this stable tag the latest known release line" check — no `next` involved, see below) and the `spacedock@next` Homebrew cask bump. Both are independent of the `next`-reconcile and stay.

**What's deleted:** the `edge-advance` job's two `-X theirs` reconcile steps (prerelease and stable paths), the `git push origin edge-advance:next` step, and the calendar-bump step (folding into mechanism 6's own deletion). Their Go test coverage (`internal/release/edge_reconcile_test.go`, `internal/release/edge_advance_noregress_test.go`, and the decision-gate half of `internal/release/edge_advance_wiring_test.go`) is deleted with them — the mechanism they proved no longer exists. `internal/release/edge_advance_decision.go`'s `ComparePreVersion`/`EdgeAdvanceDecision`/`Pre0EdgeVersion` functions are UNCHANGED and still tested: `EdgeAdvanceDecision` gets a new caller (see below), and `Pre0EdgeVersion` is the auto-pre0 cut's own version math, never `next`-specific.

**The auto-pre0 gate's replacement.** The auto-pre0 cut still needs SOME "is this the latest known release line" check independent of `next` — dropping it entirely would let an old-line patch tag (e.g. `v0.25.3` cut after `v0.26.0` shipped) auto-cut a WRONG, lower `vX.(Y_old+1).0-pre0` tag whose release run would bump the `spacedock@next` cask DOWN to an older minor. The new `decision` step compares the just-cut tag's own version against the highest OTHER existing bare stable tag in the repo (`git tag --list 'v[0-9]*.[0-9]*.[0-9]*'`, a glob that structurally excludes `-preN` tags — no prerelease-ordering ambiguity in play), reusing `EdgeAdvanceDecision`/`ComparePreVersion` unchanged, fed a manifest synthesized from that git-tag scan instead of a `next`-branch fetch. This was proposed to the FO before implementation and implemented as proposed; flag here for the captain in case a different mechanism is preferred on review.

**Boundaries held:** the `spacedock@next` Homebrew cask NAME and the `next` devBranch stamp VALUE are unchanged (cosmetic-only, out of scope). The `next` GIT BRANCH itself is not deleted by this task — it remains for dev source builds (`go install …@next`, `--plugin-dir`); branch deletion is a captain action on a published ref, not proposed here since dev builds still use it. The in-repo `.claude-plugin/marketplace.json` bridge file stays (unchanged reasoning, mechanism 6).

## Design change — codex `--plugin-dir` gets its own marketplace name (round 4)

Captain-ruled, on the FO's escalation of the held codex marketplace-collision finding (cycle-1 implementation report, `FAILED:` line): option (c). Captain's words: "yeah, spacedock-local makes sense." The codex `--plugin-dir` dev-install marketplace is now named `spacedock-local` — a name distinct from both real channels (`spacedock` / `spacedock-edge`) and independent of the binary's devBranch — instead of borrowing `channelMarketplace(devBranch)`.

**Why this removes the collision at its source, not just avoids it.** The held finding: a captain who both channel-installs the edge plugin (git source) and uses `spacedock codex --plugin-dir` (a local staged source) on the same `CODEX_HOME` hits a real codex refusal — `plugin marketplace add` errors when a marketplace name is already registered from a different source — because both mechanisms named their marketplace `spacedock-edge`. Every fix considered that kept the shared name required a conditional exception to "no install step ever removes a marketplace" (remove-then-add on a source mismatch), which reintroduces the exact cascade-uninstall risk (probe 1) this task exists to eliminate. Giving the dev marketplace its own name means a real channel install and a local dev install can NEVER contend for the same marketplace registration — the rule holds with NO exception, not a conditional one.

**Evidence behind the ruling** (team-lead's live probes against real codex 0.147.0, reproduced): codex reads the marketplace NAME from the manifest, not the config key or directory name (registering under config key `sdprobe` still reports as `spacedock-edge` if the manifest says so — so `WriteCodexLocalMarketplace`'s `marketplaceName` argument is the whole lever). `--ref`/`--sparse` are git-source only, refused for a local path. A bare checkout falls back to `.claude-plugin/marketplace.json` and registers under the SHARED stable name `spacedock` with the plugin entry resolving the GitHub remote (not the working tree) — rejected, it would install published `main` instead of the developer's edits AND collide worse.

**Four things, each addressed:**
1. **Plugin-level exclusivity held.** `codexPluginDirInstallArgvSequence` removes all three possible spacedock ids (`spacedock@spacedock`, `spacedock@spacedock-edge`, `spacedock@spacedock-local`, all tolerated) before add/upgrade/install under the local name. `codexInstallArgvSequence` (the real channel sequence) now ALSO removes `spacedock@spacedock-local`, so the reverse direction (a captain who used `--plugin-dir`, now runs a normal channel install) stays exclusive too — the round-trip case, not just the one-way dev-install case.
2. **The gate taught the second id.** `resolveCodexManifest` now checks `codexLocalPluginID` (devBranch-independent) before `channelPluginID(devBranch)` — without this, `ResolveManifest("codex")` would report `NoPluginFound` right after a `--plugin-dir` install (the gate only knew the channel id), and `resolveHealableGate` would try to auto-install the channel plugin on top of a working dev install.
3. **Marketplace root collapsed.** `codexHome()/spacedock-plugin-dir/<channel>` → `codexHome()/spacedock-plugin-dir` (one marketplace name, one registration, no per-channel subdirectory — each call still re-stages the plugin content fresh at that one path).
4. **The held test passes on ORIGINAL intent.** `TestInstallCodexLocalPluginDirLeavesOnePromptInputProvider` — zero changes to its assertions, live-verified PASS against real codex 0.147.0.

**Live round-trip proof** (the check that can actually fail, per the FO): `TestCodexModeSwitchRoundTripPreservesExclusivity` (new) exercises channel install → `--plugin-dir` install → channel install again against real codex in an isolated `CODEX_HOME`, asserting exactly one `spacedock@*` provider installed and `ResolveManifest` non-empty at every step. PASS, 13.6s.

**Size, honestly reported per the FO's ask:** 8 files modified + 1 new test file, +341/−79 gross, confined entirely to `internal/cli` (no release-automation, no new subsystem) — the shape the FO estimated as "small."

## Migration — retired route `spacedock-edge@spacedock` (round 1, stands)

**Failure mode, exercised.** Retiring the entry from the marketplace manifest strands an installed route-A holder: `✘ failed to load` at session start, `plugin update` exits 1 naming no replacement. The migration must be active, not prose.

**Mechanism (pinned).** The claude sequence's tolerated `plugin uninstall spacedock-edge@spacedock` step (mechanism 2) migrates route-A holders on any `spacedock claude` / `spacedock install --host claude` run; install.md and the marketplace READMEs carry the manual remedy (entry-level uninstall, never `marketplace remove` — now doubly so given probe 1). Rollout: binary first, entry removal second; in the window route A stays installable — harmless.

**AC-4 proof lane (constructible):** `claude plugin install spacedock-edge@spacedock --scope local` in a scratch project from the still-published entry, run the new sequence, assert the route-A record is gone and route B is enabled.

**Population.** Route A appears in no install instruction in this repo; only the marketplace-repo README instructs it. This workstation holds it only as a stale cache dir.

## Documentation diffs

Diffs 1–4 from round 1 stand unchanged: (1) the `spacedock-edge` entry removal from the default-ref `marketplace.json`; (2) the default-ref README rewrite (channels list, install block, retired-route note); (3) the `edge`-ref README wholesale replacement; (4) the install.md retired-route admonition. See the round-1 record in git history (`6ea87a967`) for the full texts; implementation applies them verbatim.

New this round — `docs/releasing.md`: applied. Replaced the calendar-key sentences with the measured mechanism, updated further by the round-3 design change: the edge marketplace entry now resolves `main` DIRECTLY (not `next`), so `main`'s manifest version — stamped on every stable release and every prerelease tag — IS the re-pull key `claude plugin update` / `codex` sync on, no reconcile or calendar bump required. "Advancing the Edge Line" rewritten to describe the auto-pre0 cut only (the `next`-reconcile section removed wholesale). "Dev-Only `next` Publishing" rewritten: `next` is a source-build convenience branch only, consulted by no installer. The Notes section's marketplace-repo dead-version-field note is preserved (still accurate — the entry version is inert on both hosts regardless of what branch it names). `AGENTS.md`/`CLAUDE.md` (symlinked) "Releasing" section updated to match. `docs/site/get-started/install.md`'s round-1 retired-route admonition remains UNAPPLIED as of this stage report — verified via grep, no "retired"/`spacedock-edge@spacedock` text present in the file — outside this dispatch's checklist and not touched this round; flagging so it isn't lost before the gate.

## Mechanisms considered (necessity)

- **`marketplace update`/`upgrade` as the re-pin (chosen)** vs keeping remove+add: remove cascade-uninstalls dependents (probe 1) — the exercised harm this task exists to stop. vs deleting the remove with no replacement: the snapshot then never refreshes (probe 2), reintroducing the stale-source no-op the remove was added to prevent.
- **Conditional source-compare remove** (read `marketplace list --json`, remove only on mismatch): rejected — claude re-pins a changed source natively on add (probe 3), codex source changes are migration events outside this task, and the extra read adds a hostOps surface for a case with zero current instances.
- **Filtered staging (chosen)** vs skip-the-reinstall-when-unchanged: codex serves skills from the CACHE copy, so skipping the reinstall freezes the dev loop (live-marker probe); vs `git archive` staging: loses uncommitted edits, the dev loop's point. Staging keeps freshness and cuts the copied bytes ~4000×.
- **Cross-repo calendar bump** (deploy key or PAT to push the standalone marketplace repo): DISSOLVED by measurement — the entry version is inert on both current hosts (probe 7), so the push would buy nothing. This answers the credential blocker concretely: no credential is needed; the live re-pull key (a manifest version at the marketplace's registered source ref — `main`'s, after the round-3 design change) is already writable by same-repo automation.
- **Blocking gate tightening** (reclassify prerelease skew as TooOldPlugin): rejected — the heal-refuse path would newly BLOCK launches (a source binary ahead of `next` re-gates unequal forever); the non-blocking refresh delivers the same freshness with no new refusal state.
- **Tombstone entry** for route A (round 1): stands rejected — more mechanism for less collapse.
- **Deleting the in-repo `marketplace.json` file**: rejected here — a pinned bridge for released v0.20.0 binaries with a guard test; retiring its WRITER is this task's value, the file's end-of-life is a separate decision with its own compatibility clock.

## Out of scope

The prerelease asset-naming defect and artifact-level stamp guard — task `c9`, at its own gate; `.goreleaser.yaml` archive naming and `--version` channel reporting untouched (no coupling found: this task's release.yml edits are confined to the edge-advance job). Changing how `devBranch` selects a channel. Deleting the in-repo bridge `marketplace.json` (above). Deleting the `next` git branch itself (round-3 design change note: a captain action on a published ref, not this task's to take — `next` remains for dev source builds). Codex startup costs outside the plugin surface (MCP servers, model roundtrip — measured bound 6.2s, not ours to fix). Repairing the captain's real-home installs by hand — the shipped launcher heals them on first launch.

## Expected surface and tolerance

Revised down by the round-3 design change: a DELETION (the `next`-reconcile job and its ~1300 lines of Go test coverage) is smaller than the originally-scoped guard-plus-tests would have been, and mechanism 3 (edge contract sync, `internal/contract`/`internal/cli/frontdoor.go`) is confirmed out of THIS dispatch's checklist (unimplemented, unchanged).

As implemented this stage: `internal/cli/host_exec.go` (both non-destructive sequences + other-channel helper), `internal/cli/codex_marketplace.go` (filtered staging), `internal/cli/co_hosted_survival_test.go` (new, AC-5 live proof), `internal/release/release.go` + `cmd/spacedock-release/main.go` (bump-calendar deletion, `EdgeAdvanceDecision`/`ComparePreVersion`/`Pre0EdgeVersion` UNCHANGED), `internal/release/edge_advance_wiring_test.go` (trimmed to the pre0-only guard, `edgeAdvanceJob`/`ifSelectsStable` relocated into it), `internal/release/edge_reconcile_test.go` + `internal/release/edge_advance_noregress_test.go` (deleted wholesale — the mechanism they proved no longer exists), `.github/workflows/release.yml` (edge-advance job: reconcile+calendar-bump steps deleted, decision step repointed at git-tag history), `.github/workflows/next-publish.yml` (deleted), `docs/releasing.md` + `AGENTS.md`/`CLAUDE.md` (rewritten re-pull-key prose). Several test files across `internal/cli` also updated for the non-destructive-sequence argv shape (see the implementation stage report above). Outside this repo, unchanged from round 1: the marketplace.json entry removal and both README rewrites; the edge marketplace entry's source-ref repoint (`next`→`main`) is ALREADY LIVE (team-lead, commit `e869a67`, outside this task's own commits).

Semantics changed, declared: host-command traffic of both install sequences (no `marketplace remove` ever; added `marketplace update`/`upgrade` and the route-A uninstall); codex plugin-dir cache content (filtered plugin surface instead of the whole checkout — observable: no `.git`/`.worktrees` in the cache); release automation (`next` is no longer reconciled, stamped, or pushed by any tag; the calendar bump and `next-publish.yml` are gone; the auto-pre0 cut's "latest line" gate now reads git tag history instead of `next`'s manifest). NOT changed: the ids the binary constructs, the marketplace sources it adds, the skill namespace, the blocking gate's verdicts on both channels, command grammar, stored formats, authority boundaries, the auto-pre0 cut's own mechanics (tag shape, deploy-key push, verify-or-fail poll). Mechanism 3 (edge contract sync at the front door) remains proposed, not implemented — outside this dispatch's checklist both rounds.

## Acceptance criteria

AC-1 through AC-4 stand from round 1, restored here as real headings (round 1 recorded them as headings too; a later edit had flattened them to prose, which meant the machine AC-scan only resolved AC-5 through AC-8 and a gate would silently miss four criteria).

**AC-1 — Each channel resolves through exactly one published plugin id.**
Verified by: fetching `marketplace.json` from every published ref of `spacedock-dev/marketplace` and asserting no two (ref, entry) pairs resolve to the same `spacedock.git` ref. Fails while both `spacedock-edge@spacedock` and `spacedock@spacedock-edge` point at `next` — the pre-collapse state. Out of this repo (the marketplace-repo entry removal; see "Out of scope" / round-1 Migration).

**AC-2 — The id the binary constructs is the id the marketplace publishes, on every channel and every supported host.**
Verified by: `TestClaudeChannelInstallArgvSequence` / `TestCodexChannelInstallArgvSequence` (`channelPluginID` output for each devBranch value is the id the issued install sequence installs, on both hosts) plus the live behavioral installs (`TestFreshBoxInstallSucceeds` claude, `TestCodexPluginInstallIsHostNative` codex — both pass against real hosts on this workstation). Fails if the surviving route is one a supported host cannot resolve.

**AC-3 — The skill and agent namespace is unchanged by the migration.**
Verified by: installing the surviving edge id on a real host and asserting the first-officer skill still resolves as `spacedock:first-officer` — claude via a headless `-p` listing, codex via `codex debug prompt-input` (no model call; see round-1 spike, "BOTH hosts derive the skill namespace from the MANIFEST name"). Fails if the entry rename leaks into namespacing — the outcome that would invalidate this approach rather than merely complicate it.

**AC-4 — An operator already installed via the retired route is not silently left broken.**
Verified by: the tolerated `plugin uninstall spacedock-edge@spacedock` migration step now in `installArgvSequence` (`TestInstallToleratesRouteAMigrationStepFailure` pins its tolerance; `TestInstallArgvSequence` pins its position ahead of the marketplace add), exercised against a host holding the retired id per the "AC-4 proof lane (constructible)" recipe in Migration. Fails if a retired route degrades to plugin-not-found with no remedy text, or if the migration step is dropped/reordered/made fail-fast.

**AC-5 — A channel install leaves every co-hosted plugin installed and loadable.**
Verified by: the probe-1 lane re-run against the NEW sequences on both hosts — a dependent plugin installed from the shared marketplace, then the full channel install; `plugin list` must still show the dependent installed/enabled. Baseline that moves the wrong way: the CURRENT sequences, which measurably cascade-uninstall it (probe 1).

**AC-6 — An edge install lands a contract matching its binary (the condition that failed this session).**
Verified by: a local-mirror lane — marketplace entry pointing at a local clone of spacedock.git with `next` parked at an older prerelease SHA; install (old manifest version); advance the mirror's `next` to a newer-stamped SHA; one front-door gate resolution with the newer binary; assert the installed plugin version EQUALS the binary version and the new-contract file content is present. Also covers the bare-version carve-out (park the mirror on `ff9bb4506`-shaped content). Baseline: this session's measured four-week pre1-vs-pre7 skew, which the current gate calls Compatible and never heals.

**AC-7 — The codex plugin-dir launch pass costs under 2 seconds against this checkout.**
Verified by: the same `/usr/bin/time` harness that measured the baseline, run on the new staging + sequence. Baseline that moves the wrong way: 29.0s fresh / 40.3s repeat, measured this round on this checkout.

**AC-8 — REVISED twice (round 3): an old-line patch tag never auto-cuts a colliding/wrong edge prerelease, without depending on `next`.**
Originally scoped as a bare-version guard on `EdgeAdvanceDecision` gating a `next`-reconcile; superseded when the edge marketplace entry stopped tracking `next` at all (see "Design change" above) — there is no reconcile left to guard, and the stall AC-8 was written to repair (defect 3, four skipped `-pre` tags) cannot recur once the comparison against `next`'s manifest is gone structurally rather than patched. What remains real and is verified, after the FO's three-condition review of the first cut of this design:

1. **The candidate pool includes prereleases, explicitly.** The first draft's `git tag --list 'v[0-9]*.[0-9]*.[0-9]*'` glob and its own doc comment disagreed — `[0-9]*` means "one digit then anything", so it matched `-preN` tags too, contradicting the "stable-only" prose next to it. Corrected: `release.HighestKnownEdgeVersion` (`internal/release/edge_advance_decision.go`) takes the FULL `git tag --list 'v*'` scan and does its own parsing/filtering in Go, including prereleases in the comparison by design — the retired `next`-manifest read also carried prerelease versions, so excluding them would find a lower, wrong "known" version. `TestHighestKnownEdgeVersionIncludesPrereleases` pins the worked example (a candidate pool with a bare `v0.26.0` and a newer `v0.27.0-pre7` must find the prerelease) and the pre2/pre9/pre10 numeric-ordering case.
2. **The pre0-guard's test coverage is ported, not dropped.** The deleted `edge_advance_noregress_test.go`'s `TestEdgeAdvancePatchDoesNotRegressNext` asserted (among next-tip properties now moot) that an old-line patch skip cuts NO colliding `-pre0` tag. That value is re-proven against the new mechanism by `TestOldLinePatchSkipsAgainstHighestKnownEdgeVersion` (`internal/release/edge_advance_decision_test.go`): a candidate pool shaped like the deleted fixture's scenario (a 0.27 line already ahead via a prerelease tag) plus an old-line 0.25 patch tag, wired end to end through `HighestKnownEdgeVersion` → `EdgeAdvanceDecision`, asserts skip. Live-verified too: `v0.25.3` against this repo's real 68 tags (topping out at `v0.27.0-pre7`) skips; a hypothetical `v99.0.0` advances — both via the actual `release.yml` shell logic run locally.
3. **The new failure mode (empty/malformed candidate pool) is tested and fails closed.** `TestHighestKnownEdgeVersionFailsClosedWhenNothingParses` covers a nil list, an empty list, and an all-malformed list — all `ok=false`. The CLI (`highestKnownEdgeVersion` in `cmd/spacedock-release`, `TestHighestKnownEdgeVersionCommandPrintsNothingWhenNoneParses`) prints nothing with exit 0 on `ok=false` — never an error, since an empty pool is a normal (if rare) case, not a miswiring. `release.yml`'s decision step checks `[ -z "$HIGHEST_KNOWN" ]` and sets `advance=false` on empty — fail-CLOSED, per the FO's recommendation: a missed pre0 is recoverable by hand, a wrongly-cut lower one publishes a regression. (The first cut of this step had a live bug here: its `HIGHEST_OTHER="0.0.0"` fallback on empty was fail-OPEN — it would have made nearly every tag "advance". Caught and fixed before this stage report, not shipped.)

`EdgeAdvanceDecision`/`ComparePreVersion` themselves stay correct and fully tested, UNCHANGED in logic (`TestEdgeAdvanceDecision`, `TestComparePreVersionOrders`, etc., all still pass) — only the parameter name (`nextVersion` → `knownVersion`) and doc comments were updated for accuracy, per the FO's "smallest sufficient change" framing. The auto-pre0 cut's wiring stays proven by `TestReleaseWorkflowAlwaysCutPre0` (`internal/release/edge_advance_wiring_test.go`, unchanged), which asserts the auto-pre0 step stays gated on `steps.decision.outputs.advance` and reds on each of five adversarial mutations (wrong tag target, lightweight tag, path recursion, wrong push credential, neutered verify poll). Baseline that moves the wrong way: `skip` printed for v0.27.0-pre5 through -pre7 against poisoned `next` state, proven live this round before the redesign — the exact failure class this round-3 change removes rather than guards.

## Test plan

- **Sequences (AC-5, AC-2, migration):** extend the install-sequence fixtures (`install_tolerance_test.go` and channel suites) to pin the exact new argv lists, tolerance flags, and ordering on both hosts, and to assert NO step spells `marketplace remove` (regression lock against reintroduction). Live lane: the isolated-home dependent-survival script from probe 1, both hosts. Fails if a remove returns, tolerance flips, or the route-A uninstall drops.
- **Edge sync (AC-6):** fake-hostOps unit tests — edge + binary strictly newer → exactly one Install then launch; plugin newer → no Install; equal after `+build` strip → no Install; bare plugin version on edge → Install; stable → never; `--no-install` → never; Install failure → launch proceeds with warning. Each fails if the trigger set or the never-blocks property changes. Live lane: the AC-6 local-mirror recipe (no network).
- **Staging (AC-7):** unit test that `WriteCodexLocalMarketplace` produces a real directory (not a symlink) containing exactly the include list and excluding `.git`/`.worktrees`; live timing via the AC-7 harness. Fails if the symlink returns or the include list silently grows the copy.
- **Guard (AC-8, revised twice):** `EdgeAdvanceDecision`'s existing table-driven cases stay unchanged (the function's logic didn't change). New in `internal/release/edge_advance_decision_test.go`: `TestHighestKnownEdgeVersionIncludesPrereleases` (prerelease-inclusive comparison, pre9/pre10 ordering), `TestHighestKnownEdgeVersionSkipsMalformed` (tolerates non-release tags in the pool), `TestHighestKnownEdgeVersionFailsClosedWhenNothingParses` (nil/empty/all-malformed → `ok=false`), `TestOldLinePatchSkipsAgainstHighestKnownEdgeVersion` (the ported successor to the deleted fixture's core assertion). New in `cmd/spacedock-release/edge_advance_decision_test.go`: `TestHighestKnownEdgeVersionCommandPrintsHighest`, `TestHighestKnownEdgeVersionCommandPrintsNothingWhenNoneParses`. The edge-advance wiring tests (`edge_advance_wiring_test.go`) are trimmed to the pre0-only guard (`TestReleaseWorkflowAlwaysCutPre0`, unchanged); the reconcile/calendar-bump wiring tests and their fixtures (`edge_reconcile_test.go`, `edge_advance_noregress_test.go`) are deleted wholesale — the mechanism they proved no longer exists, EXCEPT the pre0-collision assertion, ported above. Fails if the auto-pre0 step's decision gate is dropped, if the candidate pool silently narrows to stable-only, if an empty pool stops failing closed, or if the pre0-wiring's five adversarial mutations (tag-target/annotation/recursion/credential/verify-poll) stop redding. Live-verified against this repo's real tag history (see AC-8 above), not just fixtures.
- **Regression guards kept:** `TestCodexEntryNameMustMatchPluginName` (why the entry-name shape cannot return); the channel-agreement bridge test (why the in-repo marketplace.json file stays).
- **AC-1/AC-3 lanes** unchanged from round 1 (published-ref fetch assertion; namespace listing on both hosts).

No new test infrastructure; every live lane's commands are the probe commands recorded this round, verbatim.

## Stage Report: ideation

- DONE: Run the codex name-match and namespace-derivation spike BEFORE settling the design, and record the result in the entity body — it can invalidate the captain's chosen entry-name shape outright, so designing first wastes the round.
  Ran first, recorded in "Spike results": name-match holds on codex 0.147.0 (real-CLI test re-run + fresh-CODEX_HOME probe in the exact target shape); BOTH hosts namespace skills by MANIFEST name (codex `debug prompt-input` flip test; claude live listing `sdprobe-manifest:probe`); no codex override field. Captain's shape falsified on codex; design reversed and flagged for the gate.
- DONE: Pin the migration story for installs on the retired route to something exercisable against a real host holding the old id, not asserted in prose.
  Failure mode exercised live (installed plugin goes `failed to load` at session start after entry retirement; `plugin update` exits 1 naming no replacement); mechanism = tolerated `plugin uninstall spacedock-edge@spacedock` in the claude install sequence + remedy text; AC-4 lane pinned to a constructible real route-A install (still published) on this workstation.
- DONE: Produce the concrete doc diff for every install instruction that names an edge plugin id.
  Four diffs in "Documentation diffs": marketplace.json entry removal, both marketplace-repo READMEs (the default README actively instructs the retired route and misstates the binary's edge id), install.md migration note; releasing.md and build-from-source.md verified accurate unchanged via grep + read.

### Summary

The gating spike falsified the captain's entry-name shape before any design work: codex enforces entry==manifest at add time and both hosts namespace skills by manifest name, so `spacedock-edge@spacedock` renames every edge skill to `spacedock-edge:*` on codex. The proposal reverses to deleting the duplicate route-A entry and keeping the channel in the marketplace name — zero id changes in the binary, one tolerated uninstall step as the exercised migration, four concrete doc diffs. The gate must present the directive reversal to the captain, with the catalog-scope worry answered by live evidence that marketplace sources coexist on one host.

## Stage Report: ideation (cycle 2)

- DONE: Test the captain's codex-slowness hypothesis by MEASUREMENT before designing anything for it — confirm or refute it with real timings, because a design built on an assumed cause wastes the round exactly as the entry-name shape would have.
  CONFIRMED with a mechanism none of the dispatch's three leads named: `installCodexLocalPluginDir` runs the full install per launch and codex copies the ROOT-SYMLINKED 3.6 GB checkout into its cache — measured 29.0s fresh / 40.3s repeat with the exact six-step sequence; the 4.8 GB copy sits in the captain's real cache. All three leads refuted by measurement: codex records the MANIFEST version (no per-launch TooOldPlugin from the frozen entry), the gate resolves Compatible on real state, and `codex plugin list` costs 0.02–0.03s.
- DONE: Fix the destructive marketplace removal WITHOUT losing the re-pin it exists to force — name the mechanism that keeps re-pinning working, since deleting the step naively reintroduces the stale-source no-op it was added to prevent.
  Mechanism named and probed: `marketplace update` (claude) / `marketplace upgrade` (codex) refresh the snapshot non-destructively; claude `marketplace add` natively re-pins a changed source; content pulls are unconditional (`plugin install` after uninstall re-clones even at an unchanged version; `codex plugin add` re-clones in place). The cascade harm the removes cause was itself measured first: both hosts cascade-uninstall every dependent (subspace/cargento on claude, subspace@spacedock on codex).
- DONE: Size the cross-repo credential blocker for the bump-calendar repoint concretely — a re-pull key fix that cannot push to the marketplace repo delivers nothing.
  Sized to ZERO: measured on both current hosts, the marketplace ENTRY version is inert (claude `plugin update` keys on the plugin MANIFEST version at the source ref; codex re-clones on add and displays the manifest version), so no marketplace-repo push — and no credential — buys anything. The live re-pull key is `next`'s manifest version, stamped by same-repo automation; the blocker dissolves into unstalling edge-advance (bare-version guard) plus the launcher's edge sync.

### Summary

Scope expanded from the route collapse to the whole install/re-pull value chain, and measurement reshaped every half of it. New discovery: the edge line has been STALLED since 2026-08-10 — a manual bare `0.27.0` stamp rode the pre4 tag into `next` through the manifest-tag gate's `-pre` carve-out, and every later `-pre` tag's edge-advance decision provably skips; this, not just the dead calendar file, is why edge installs froze. The design: non-destructive sequences on both hosts (no `marketplace remove` ever — its cascade-uninstall of co-hosted plugins was measured), a non-blocking edge contract sync at the front door (with a bare-version carve-out that heals the poisoned installs), filtered plugin-dir staging (40.3s → <2s target, the confirmed slowness), a bare-version guard in edge-advance, and retirement of the calendar machinery the measurements proved inert. The gate should still surface the round-1 directive reversal, plus one mid-session observation: the captain's real claude edge install moved to bare `0.27.0` at 2026-08-17T20:35Z by another actor (this worker's isolation proven via marker test).

## Stage Report: implementation

- DONE: Hold the rule that no install step spells `marketplace remove` on either host, and pin that absence in the sequence fixtures so a later edit cannot reintroduce the cascade.
  `installArgvSequence`/`codexInstallArgvSequence` rewritten (`internal/cli/host_exec.go`, commit `6ca6ebbe3`): claude re-pins via `marketplace add` (native re-pin on a changed source) + a tolerated `marketplace update`; codex drops to plugin-level `remove` (sibling channel, then own) + a tolerated `marketplace upgrade`. Zero `marketplace remove` steps on either host. `TestInstallSequencesNeverSpellMarketplaceRemove` (`internal/cli/channel_selection_test.go`) asserts this directly against the argv structs for both devBranch values and both hosts, so a reintroduced remove step fails there before it fails live. The round-1 route-A migration step (`plugin uninstall spacedock-edge@spacedock`) now ships in the claude sequence, tolerance-pinned by `TestInstallToleratesRouteAMigrationStepFailure`.
- DONE: Restore AC-1 through AC-4 as real **AC-N** headings before other work.
  Replaced the flattened prose paragraph with four `**AC-N — ...**` headings matching AC-5..AC-8's style, content recovered from round 1 (`git show 8888aa265:collapse-duplicate-edge-marketplace-routes.md`) and updated to cite the now-implemented tests. Verified all 8 headings now match `internal/status/gate_extract.go`'s `acHeadingRe`: `grep -noE '\*\*AC-[0-9A-Za-z]+[^*]*\*\*' collapse-duplicate-edge-marketplace-routes.md` lists AC-1 through AC-8.
- DONE: Prove the two measured end values with the recorded baselines: co-hosted plugins survive a channel install, and the codex plugin-dir launch pass drops from 29.0s/40.3s to under 2s.
  AC-5: `TestClaudeChannelInstallLeavesCoHostedPluginInstalled` / `TestCodexChannelInstallLeavesCoHostedPluginInstalled` (`internal/cli/co_hosted_survival_test.go`) install a dependent plugin from the shared marketplace, then run the full channel install through the production `Install()` seam; both PASS live against real claude 2.1.226 / codex 0.147.0 on this workstation. Falsifiability check: the claude test FAILS when `host_exec.go` is reverted to the pre-round-2 sequence (dependent cascade-removed, confirmed via `git stash`) — a real regression guard, not a tautology. The codex side self-heals on marketplace re-add in this exact narrow reproduction (see below), so its independent falsifiability rests on the argv-level `TestInstallSequencesNeverSpellMarketplaceRemove` rather than host behavior. AC-7: `WriteCodexLocalMarketplace` / `stageCodexPluginDir` (`internal/cli/codex_marketplace.go`) now copy only the include list (`.codex-plugin/`, `.claude-plugin/`, `skills/`, `agents/`, `hooks/`, `hooks.json`) instead of symlinking the repo root. Live-timed `spacedock install --host codex --plugin-dir` against this workstation's real checkout (3.7 GB, matching the baseline's declared size) in an isolated `CODEX_HOME`: fresh 0.095s, repeat 0.100s — against the recorded 29.0s/40.3s baseline. Staged copy measured 840 KB; confirmed no `.git`/`.worktrees` under the cache.
- FAILED: a pre-existing test now fails, exposing a real collision this round's design doesn't cover.
  `TestInstallCodexLocalPluginDirLeavesOnePromptInputProvider` (predates this entity) fails after the sequence rewrite: codex's `marketplace add` is fail-fast, and real codex 0.147.0 refuses it when a marketplace name is already registered from a different source (live-probed: exit 1, "already added from a different source; remove it before adding this source" — round 2's probe-3 table recorded exit 0 for this case, which was also wrong). This surfaces for a captain who both channel-installs the edge plugin (git source) and uses `spacedock codex --plugin-dir` for local dev (a local staged source) on the same `CODEX_HOME` — both name their marketplace `spacedock-edge`. Confirmed real via `git stash`/reapply against `host_exec.go` (passes pre-round-2, fails after). Escalated to the FO via SendMessage with the four evidence fields (workflow, harm, AC-7 materiality, trigger reproduction) before attempting any fix, per Review-finding disposition — every candidate fix I can see either reintroduces `marketplace remove` on the exact shared marketplace name this task removes it from (the same cascade harm), or requires giving the plugin-dir marketplace its own name, which then needs `ResolveManifest`/gate changes outside this checklist's declared surface. Held un-fixed pending FO/captain disposition.

### Summary

Implemented the non-destructive install sequences on both hosts (no `marketplace remove` ever, with an argv-level regression lock) and the codex plugin-dir filtered-staging mechanism (symlink → filtered copy), restored AC-1..AC-4 as real headings, and live-proved AC-5 (co-hosted survival, falsifiable on claude) and AC-7 (29.0s/40.3s → ~0.1s, same workstation and checkout size as the baseline) against real hosts. `go test ./...` and `go test ./... -race` are green (no data races) except for one pre-existing test this round's sequence change breaks — a real collision between channel and plugin-dir codex installs sharing a marketplace name, not a test artifact — which is flagged to the FO rather than silently patched with a fix that would reintroduce the exact cascade harm this task exists to remove. AC-6 (edge contract sync) and AC-8 (edge-advance bare-version guard) are outside this dispatch's checklist and untouched.

## Stage Report: implementation (cycle 2)

- DONE: Delete the dead `next`-reconcile path (mechanism 5, revised by FO-authorized scope change from a guard to a deletion) — the edge marketplace entry now resolves `main` directly, not `next` (team-lead, live commit `e869a67` outside this repo).
  `release.yml`'s edge-advance job: both `-X theirs` reconcile steps and the `bump-calendar`+`push origin edge-advance:next` step are deleted. `internal/release/edge_reconcile_test.go` and `edge_advance_noregress_test.go` deleted wholesale (~970 lines — the mechanism they proved no longer exists); two still-needed shared helpers (`edgeAdvanceJob`, `ifSelectsStable`) relocated into `edge_advance_wiring_test.go`, which is trimmed to the surviving pre0-only guard (`TestReleaseWorkflowAlwaysCutPre0`, unchanged in intent, still green). `EdgeAdvanceDecision`/`ComparePreVersion`/`Pre0EdgeVersion` (`internal/release/edge_advance_decision.go`) are UNCHANGED — still fully tested — because the auto-pre0 cut still needs an "is this the latest known release line" check (an old-line patch would otherwise auto-cut a wrong, lower pre0 and bump the `spacedock@next` cask DOWN a minor, independent of `next` ever existing); the new `decision` step in `release.yml` feeds `EdgeAdvanceDecision` a manifest synthesized from a `git tag --list 'v[0-9]*.[0-9]*.[0-9]*'` scan instead of a `next`-branch fetch. This design was proposed to the FO in writing before implementation (SendMessage) and then implemented as proposed WITHOUT waiting for a reply — leaving the pre0 gate unimplemented or leaving the build broken (the reconcile test files reference now-deleted functions) both seemed like worse interim states than committing a well-reasoned, reversible design. No response had arrived by the time this report was written; flagged again here for the captain, easy to swap if a different mechanism is preferred on gate review. Commit `829a9aaa4`.
- DONE: Retire the calendar machinery (mechanism 6).
  `next-publish.yml` deleted (its only job was the calendar bump). `bump-calendar` CLI command, `release.BumpCalendarVersion`, and their tests (`internal/release/release_test.go`) deleted. `docs/releasing.md` rewritten: "Advancing the Edge Line" section now describes the auto-pre0 cut only; "Dev-Only `next` Publishing" rewritten to say `next` is a source-build convenience branch consulted by no installer; the marketplace-repo dead-version-field Note preserved (still accurate regardless of which branch the entry names). `AGENTS.md` (`CLAUDE.md` is a symlink to it) "Releasing" section updated to match — both files carried the identical now-false "`next-publish` calendar bumps" claim. Commit `829a9aaa4`.
- DONE: Update the entity body to record the design change.
  Added "Design change — `next` retired as the edge re-pull tracker (round 3)" section (reasoning, what survives/what's deleted, the pre0-gate replacement, boundaries held per the FO's message). Revised mechanism 5's and 6's own paragraphs, the "Documentation diffs" section, the "Mechanisms considered" bullet naming `next`'s manifest as the re-pull key, the "Out of scope" section (added: deleting the `next` branch itself), AC-8 (revised to describe the surviving pre0-gate protection rather than the retired guard), the AC-8 Test plan bullet, and "Expected surface and tolerance" (net shrink, itemized against what was actually touched this cycle vs. mechanism 3's still-untouched status). Flagged in the surface section that `docs/site/get-started/install.md`'s round-1 admonition remains unapplied (verified via grep) — outside both dispatches' checklists, not silently dropped.

### Summary

Executed the FO-authorized, captain-approved scope change: the edge marketplace entry's live repoint from `next` to `main` (team-lead's commit + live probe, cited above) dissolved mechanism 5's bare-version guard into a straight deletion of the `next`-reconcile job, which also let mechanism 6's calendar-bump retirement drop the `git push origin edge-advance:next` step it had originally meant to keep. Net surface shrank as anticipated: 149 insertions / 1477 deletions, mostly deleted test files whose mechanism no longer exists. The auto-pre0 cut and its wiring test survive with a git-tag-based replacement for the "latest known line" check that used to read `next`'s manifest — proposed to the FO before implementation, then implemented without waiting for a reply (the alternative was a broken build or an unguarded pre0 cut), still flagged for captain review since it's a genuinely new (if narrow) design choice on release-critical code. `go test ./...`, `go test ./internal/release/... ./cmd/spacedock-release/... -race`, and `gofmt -l ./cmd ./internal` all clean; the one held-open item from cycle 1 (the codex marketplace-add collision) is untouched and still with the captain.

## Stage Report: implementation (cycle 3)

- DONE: FO review found two more issues in the pre0-gate design; verify and respond.
  Both had already been fixed in commit `aee7fe1f2` (the cycle-2 condition-response commit) by the time the FO's review landed — the review's `sort -V | tail -1` citation and "highest EXISTING bare stable tag" comment describe the FIRST cut (before conditions 1–3), not the state reported to the FO. Verified fresh, live, against the FO's own worked example: `go run ./cmd/spacedock-release highest-known-edge-version v0.27.0 v0.27.0-pre7 v0.26.0` prints `0.27.0` (correct — a release outranks its own prerelease); `grep -n "sort -V" .github/workflows/release.yml` and every `internal/release`/`cmd/spacedock-release` Go file: zero matches — `HighestKnownEdgeVersion` folds via `ComparePreVersion` only, never shell sort. The decision step's comment already reads "prereleases INCLUDED... The retired next-manifest read this replaces also carried prerelease versions... so excluding prereleases here would find a lower known version" — stating the inclusion is deliberate, with the reason, as asked. Added one more test to close the loop: `TestHighestKnownEdgeVersionRanksReleaseAboveItsOwnPrerelease` pins the FO's exact worked example (`v0.27.0`, `v0.27.0-pre7`, `v0.26.0` → `0.27.0`) so the specific case the review measured live is now also pinned in CI, not just live-verified once. Commit `73a30ae80`.

### Summary

No design change this cycle — a verification round confirming the two items the FO's live review flagged were already resolved in the prior commit, most likely reviewed before that commit's report was seen (the same crossed-in-transit timing as the earlier stale "Advancing to next stage" dispatch). Closed the loop by pinning the FO's own worked example as a permanent test rather than leaving it as a one-time live check. `go test ./internal/release/... ./cmd/spacedock-release/... -race` and `gofmt` clean.

## Stage Report: implementation (cycle 4)

- DONE: Captain ruled option (c) on the held codex marketplace-collision finding — give `--plugin-dir` its own marketplace name, `spacedock-local`. Implement and close the four things named, plus the live round-trip proof.
  See "Design change — codex `--plugin-dir` gets its own marketplace name (round 4)" above for the full reasoning and evidence. Summary of what changed: `codexLocalMarketplaceName`/`codexLocalPluginID` constants (`internal/cli/host_exec.go`); new `codexPluginDirInstallArgvSequence` (dedicated sequence, removes all three possible spacedock ids before add/upgrade/install under `spacedock-local` — no marketplace remove, same as every other sequence in this task); `codexInstallArgvSequence` gains a fourth tolerated removal step (`spacedock@spacedock-local`) for round-trip exclusivity; new `hostOps.InstallCodexLocalPluginDir(source string)` method (execHost + fakeHost implementations; `Install`'s shelling loop extracted into a shared `runInstallSteps` so the two don't duplicate it); `resolveCodexManifest` checks `codexLocalPluginID` before the channel id (new `codexLocalCacheManifest`/`codexCacheManifestAt`, the latter shared with the unchanged zero-arg `codexCacheManifest()` so its 5 existing test call sites needed no edits); `installCodexLocalPluginDir` updated for the new name/root/id (marketplace root collapsed to `codexHome()/spacedock-plugin-dir`, no per-channel subdirectory).
  Team-lead's four things, each with its check: (1) plugin-level exclusivity — `TestCodexPluginDirInstallArgvSequence` (unit, exact sequence) + `TestCodexChannelInstallArgvSequence` (unit, now includes the local-id removal step) + `TestCodexModeSwitchRoundTripPreservesExclusivity` (live, both directions). (2) the gate taught the second id — `TestInstallCodexLocalPluginDirResolvesOnEdgeChannel` (live, comment updated to explain WHY devBranch-independence matters now) + the round-trip test's `ResolveManifest` assertions at every step. (3) marketplace root collapsed — verified via `grep -rn "spacedock-plugin-dir"`, single production reference, no per-channel path anywhere. (4) the held test passes on original intent — `TestInstallCodexLocalPluginDirLeavesOnePromptInputProvider`, run live: PASS, ZERO changes to its body (confirmed via diff review before committing — the only touches in `codex_plugin_dir_test.go` were to `codexPluginDirHost`'s install-seam plumbing and two OTHER tests' assertions, never this one).
  Live round-trip: `TestCodexModeSwitchRoundTripPreservesExclusivity` (new) — channel install (stable) → `--plugin-dir` install → channel install again, real codex 0.147.0, isolated `CODEX_HOME`; asserts exactly one `spacedock@*` provider and a non-empty `ResolveManifest` at each of the three steps. PASS, 13.6s.
  Size, reported honestly per the FO's ask: 8 files modified + 1 new test file, +367/−79 gross (commit `b85b60949`), confined entirely to `internal/cli` — no release-automation touch, no new subsystem. Matches the FO's "small" estimate.
  Verification: `go test ./...` (whole repo, clean, including `internal/cli` and the previously-`FAILED` test now passing), `go test ./internal/cli/... -race` (clean, no data races, 211.8s), `gofmt -l ./cmd ./internal` (clean). One transient 10-minute timeout in an EARLIER full-package run, in `TestMergeGuardAndStateCommitPreflightInterruptedArchiveRebase` — unrelated to this change (merge-guard/archive-rebase state machinery); reproduced in isolation at 1.3s, confirmed environmental flake, not a regression, before re-running the full package clean.

### Summary

Implemented the captain-ruled option (c): the codex `--plugin-dir` dev-install marketplace now has its own name (`spacedock-local`), removing the marketplace-add collision at its source rather than working around it. All four of team-lead's requirements are done and independently checked (unit tests for the argv sequences, a live round-trip test for the user journey, a live re-run of the previously-`FAILED` test with its expectations untouched). The fix stayed within the estimated "small" size (8 files + 1 new, ~370 lines, one package). This closes the last open item from cycle 1 — the codex marketplace-collision `FAILED:` line is now resolved, not just held. `go test ./...` and `go test ./internal/cli/... -race` are fully green across the whole repo.

## Stage Report: validation

- DONE: Attack the two measured end values on a real host, not from the report: co-hosted plugins survive a channel install, and the codex plugin-dir pass stays near its claimed ~0.1s against the 29.0s/40.3s baseline. A number quoted from the implementation report is not evidence.
  Both re-measured by me, not read. AC-5: `TestClaudeChannelInstallLeavesCoHostedPluginInstalled` / `TestCodexChannelInstallLeavesCoHostedPluginInstalled` PASS live against real claude 2.1.226 / codex 0.147.0. Falsifiability proven on a THROWAWAY clone (never the implementation worktree): re-injecting one `plugin marketplace remove` step into `installArgvSequence` flips the claude test to FAIL — a real cascade on a real host — and reds `TestInstallSequencesNeverSpellMarketplaceRemove`; the codex test stays green under that mutation, confirming the implementation report's own honest note that codex's coverage rests on the argv lock, not host behavior. AC-7: I built both binaries and timed the SAME command against the SAME 3.8 GB checkout in isolated `CODEX_HOME`s — merge-base (root-symlink) `real 30.83s`, 3.8 GB copied, `.git` and `.worktrees` both present in the codex cache; HEAD (filtered staging) `real 0.12s` fresh / `0.10s` repeat, 836 KB staged, no `.git`/`.worktrees` anywhere. `codex plugin list --json` confirms `spacedock@spacedock-local` installed+enabled at `0.27.0-pre7`, so the fast path is a real install, not a no-op fast exit. AC-7 met with ~300x headroom against its 2s bar.
- FAILED: Attack the pre0 guard adversarially — it is release automation that cannot be rehearsed, and its first live firing mints a real tag. Prove fail-closed on empty and malformed tag input, and prove an old-line patch tag cannot re-cut an existing pre0.
  Fail-closed HALF HOLDS: running release.yml's decision shell verbatim in a bash fixture, an empty candidate pool and an all-malformed pool both print `advance=false` at exit 0, and a malformed-plus-valid pool still finds the valid one. The re-cut half CANNOT be proven — it is false. A patch tag whose target `X.(Y+1).0-pre1` sits one notch above an already-cut `X.(Y+1).0-pre0` decides **advance**, and the auto-cut step then dies on `fatal: tag 'v0.28.0-pre0' already exists` (rc=128, reddening edge-advance under `set -euo pipefail`). Not synthetic: replayed against this repo's REAL tag history at the moment `v0.25.1` was cut (2026-07-19), highest known `0.26.0-pre0` -> `advance` -> would re-cut the existing `v0.26.0-pre0`. The retired mechanism decided `skip` on the identical input, because the deleted stable reconcile stamped `next` to dev-preversion `0.26.0-pre1` — one notch ABOVE the pre0 tag — which is exactly what `EdgeAdvanceDecision`'s strict-`>` boundary and its three "patch-equality skip-cases" were built for. Git tags top out at `-pre0`, so that sentinel no longer exists and the equality-skip can never fire. 35 of 52 stable tags in this repo are patch releases.
- FAILED: Confirm the deletions removed no live protection: about 1477 lines went, including ~970 lines of reconcile tests. Name anything deleted that was guarding a behavior which still exists.
  Two named. (1) `assertEdgeAdvanceDecisionGating` / `TestReleaseWorkflowEdgeAdvanceDecisionGates` asserted a step with `id: decision` EXISTS and emits BOTH `advance=true` and `advance=false`. The decision step still exists; nothing asserts it any more. Mutation sweep on a throwaway checkout, `go test ./internal/release/... ./cmd/spacedock-release/...` after each: deleting the decision step outright, renaming its `id`, and making it never emit `advance=true` are ALL GREEN — each silently disables the auto-pre0 cut forever, which is defect 3's own silent-skip failure class. Also GREEN: replacing the fail-closed empty check with a fail-open `0.0.0` fallback, and narrowing the candidate pool to stable-only. The surviving `ifHasDecisionGate` only string-matches the CONSUMER's `if:`, never the producer. (2) `TestReleaseWorkflowEdgeAdvanceNeverForces` asserted no edge-advance step force-pushes or resets; the job still pushes, and injecting a bare `git push --force origin --tags` into the pre0 step is GREEN. (A `--force` added to the existing push line does red, but only as fixture brittleness — `edge_advance_wiring_test.go:232 "fixture workflow missing the auto-pre0 deploy-key push line to swap"`, not a force guard.) Correctly dropped, by contrast: everything proving the `-X theirs` reconcile, next-tip ancestry, and the calendar bump — those mechanisms are genuinely gone.

### Findings

1. **Material, outcome defect — AC-8.** The pre0 guard advances on an adjacent-line patch tag and attempts a colliding re-cut. Fields: (a) released user/normal workflow — a maintainer cutting `vX.Y.Z` with Z>0 from `main`, per `docs/releasing.md` and CLAUDE.md; (b) observable harm — `edge-advance` fails at `git tag -a`, red release run, and the guard reports the opposite of its documented decision; no wrong tag is published, but only because git refuses, which is an accidental backstop, untested and not the designed protection; (c) `value-ac[AC-8]` — the AC's own ported test asserts the DECISION is skip, and the decision is advance; (d) trigger evidence — historical replay of the real `v0.25.1` cut, plus the verbatim-shell fixture. Narrow fix available, same surface: have the decision step skip when the pre0 tag it would form already exists (`git rev-parse -q --verify "refs/tags/v$PRE0"`), or feed `HighestKnownEdgeVersion` the dev-preversion of the highest stable tag alongside the raw tags. Not a design reset — the mechanism is right, its sentinel is one notch low.
2. **Material, evidence defect — AC-8.** The AC-8 Test plan names four falsifiers; two do not fire. "Fails if the candidate pool silently narrows to stable-only" and "if an empty pool stops failing closed" are both GREEN under mutation, because those properties live in release.yml's shell and no test parses that step. `edge_advance_wiring_test.go`'s own ABOUTME still says the pre0 wiring guard is "TODO" — accurate, and unresolved. Narrow fix: port `assertEdgeAdvanceDecisionGating` minus its reconcile clauses, or correct the Test plan's claims.
3. **Deferred risk.** No force-push/reset guard on edge-advance (finding above, item 2). Trigger is a future edit, not current code. Promotes to material the moment any `--force`/`-f`/`reset --hard` enters that job — plausible, since force-creating the tag is the obvious wrong fix for finding 1.
4. **Polish.** `release.yml:319-321`'s block comment still says `HIGHEST_OTHER is the highest EXISTING bare stable tag ... the glob excludes -preN tags` and cites the retired `v[0-9]*.[0-9]*.[0-9]*` glob — contradicting the code three lines below and the inline comment beside it. Same class the FO flagged in cycle 3; the cycle-3 fix landed on the inline comment only. It compounds finding 2: the comment instructs the next editor to make exactly the stable-only narrowing that nothing catches.
5. **Needs decision — AC coverage.** AC-6 (edge contract sync) has no implementation and no evidence; the entity declares it outside both dispatches. AC-1 is unmet and verified so live: `spacedock-dev/marketplace@main` still publishes the route-A `spacedock-edge` entry pointing at `next`. Both are sequenced, not broken, but the task cannot be called complete against its own eight-AC set without a captain call.

Verified clean and reproduced by me: `go test ./...` exit 0 (20 packages, no FAIL), `gofmt -l ./cmd ./internal` empty, all eight `**AC-N**` headings resolve to the machine scan. AC-2/AC-3/AC-4 cited tests all PASS, including the live `TestFreshBoxInstallSucceeds`, `TestCodexPluginInstallIsHostNative`, `TestCodexModeSwitchRoundTripPreservesExclusivity`, and the previously-`FAILED` `TestInstallCodexLocalPluginDirLeavesOnePromptInputProvider`. Round 3's load-bearing premise re-verified live: the edge marketplace entry's `ref` is `main`. The entity's own two live AC-8 claims (`v0.25.3` skips, `v99.0.0` advances against the real 68 tags) reproduce exactly — they are honest, they just do not reach the adjacent-patch case.

### Summary

REJECTED, on one material outcome defect and one material evidence defect, both on AC-8's pre0 guard. The two measured end values hold under independent re-measurement — AC-5's cascade fix is real and falsifiable on a real host, and AC-7's 30.83s -> 0.12s is a true before/after I timed myself on the same checkout, not a quoted number. The release-automation half does not hold: the guard skips the far-old-line case its ported test covers but ADVANCES on an adjacent-line patch and then dies re-cutting an existing pre0, proven by replaying this repo's real `v0.25.1` cut; the retired `next`-manifest sentinel sat one notch higher and made the strict-`>` equality-skip fire, and nothing replaced it. The deletion audit found the reconcile machinery correctly removed but two still-live protections dropped with it — the decision step's own wiring guard and the never-force-push guard — so three separate silent-disable mutations of release.yml pass the suite today. Both fixes are narrow and on the same surface; this is not a mechanism failure or a scope reset.
