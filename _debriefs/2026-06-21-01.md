---
session-date: 2026-06-21
sequence: 1
first-commit: "b7ecd04a"
last-commit: "aa49853c"
duration: ~full session (edge-cask fix, 0230 commission, token-clawback measure, codex-edge gap, process gates)
---

# Session Debrief — 2026-06-21 #1

Started as the 0221-layered-fo FO (land merge-guard, cut a release). Evolved into: fixing the broken edge brew install, shaping + commissioning the **0230 stable-v0.23.0 finalization** sprint, measuring the token clawback complete, and discovering the codex-edge install is broken end-to-end. A Commander drove most of 0230 in parallel in the shared checkout.

## What landed
- **#420** — edge cask channel-switch fix (merged, `b7ecd04a`). Root cause: goreleaser defaults a cask's `binary` stanza to the cask NAME, so the edge cask emitted `binary "spacedock@next"` while the archive ships `spacedock` → symlink-source-missing → install failed. Captain chose **Option B** (node@18 pattern): edge installs the `spacedock` command, both casks `conflicts_with` each other, via goreleaser's first-class `conflicts` field + `binaries:["spacedock"]`.
- **0230 sprint** shaped, gate-reviewed (readiness workflow + staff review), value-anchored, commissioned. Dispatch package landed direct on main (`docs/roadmap/0230-stable-finalization/`, `aa49853c`). A Commander drove 5/6 members to done; member 4 (`releasing-doc-pre-stamp-drift`, the consolidated releasing.md reconciliation) at validation. **Value gate MET** — FO + ensign resident contract all ≤ v0.22.0; full per-runtime FO contract below v0.22.0 (claude −282 tok, codex −234, pi −936 vs the original +2434/+2962/+3744 bloat).
- **Filed** (in-house): `fo-entity-authoring-docs`, `fo-smallest-sufficient-mechanism`, `live-test-stream-tee-ci-stdout-noise`, `status-where-robust-and-discoverable` (subsumes GitHub #314).

## The headline catch
**Codex edge install is broken**, and `done`-marked z2 (`spacedock-marketplace-source-env`) didn't actually deliver it: the binary builds `spacedock@spacedock-edge` (needs a marketplace NAMED `spacedock-edge`), but the standalone repo has one marketplace named `spacedock`, and the binary adds the same bare source for both channels. z2's AC-1 passed against the synthesized live-lane marketplace, which **masked the real-repo gap**. Fix (captain's choice): an `edge` branch in `spacedock-dev/marketplace` (root `name: spacedock-edge`) — cleaner than a second repo — plus a per-channel edge `marketplaceSource` in the binary; verify the real install; then cut pre.3. This is the value-gate lesson recurring: "passed a fixture" ≠ "the install works."

## Process lessons (captain drilled these — now encoded as gates)
- **Smallest sufficient mechanism.** Repeated over-reach: a dynamic workflow + a dispatched worker to edit ~7 markdown files I already had the content for; a PR (with CI lanes) for a roadmap strategy doc the convention commits directly; a `find`/grep loop instead of `spacedock status --where status=ideation` (one call). Each time an escape hatch (Ultracode "use Workflow," dispatcher "never touch a file," PR-discipline over-generalized). Converted into a forcing gate (`fo-smallest-sufficient-mechanism`): name why the cheaper rung can't do it before climbing; Ultracode raises answer-thoroughness, not mechanism-weight.
- **Use the native query, not raw shell** — and `--where` has real gaps (no field validation → silent match-all per #314; excludes archived). Filed as `status-where-robust-and-discoverable`.
- **Ideating via a workflow ≠ done** — refined ACs but left entities at `backlog`; the stage transition (`→ ideation`) is the other half. Same family as shipping a stale package doc.
- **Begin with the end / verify the END.** The cask fix (verified the version bump, not the install) and the codex-edge gap (z2 passed a fixture) are both this miss.
- **Shared-checkout race** is constant: peers/Commander drive in the same working tree (branch-cycling, reverting direct-to-main into PRs, advancing 0230 members under me). Discipline held: report-only, never reset/force; isolated worktrees for commits; roadmap prose direct to main.

## Open threads → /tmp/handoff-codex-edge-0230-2026-06-21.md
1. Codex-edge fix (`@edge` branch + binary follow-up) → pre.3 → verify the real install. **(top priority; pre.3 held on it)**
2. 0230: member 4 merges → cut lean v0.23.0 (release notes cover pre.1/pre.2 + the clawback).
3. Proposal-B refresh — `fo-contract-token-cleanup.md` rewritten evergreen, remaining opportunities only.
