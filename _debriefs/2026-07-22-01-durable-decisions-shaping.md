# Debrief: durable-decisions shaping (2026-07-20 → 2026-07-22, same FO session as 0260 shaping)

Durable authorities: docs/roadmap/durable-decisions/{index.md, staff-review.md, staff-review-codex.md, dispatch-sprint-execution.md}; the four member entities' gates records; the contract at durable-gate-approval-pending-blockers/gate-resolution-frontmatter-contract.md (sha 681b2348...). This file captures what lived only in conversation. The running conduct/float-finding ledger from the same session is _debriefs/2026-07-20-01-0260-shaping.md (findings 1-15 + conduct findings).

## The arc in one paragraph

3k split from four-products-in-one-coat to the resolution-only recorder across nine gate attempts (two captain scope cuts: the four-way split, then applications out to h1; a captain-directed fold absorbing the triage reframe's round-record shape; a render war ending in two frameless diagrams; the codex-seat reconciliation retiring the gate-review architecture from the spec). xb reframed from a Go command to an overridable present-gate channel at the captain's decoupling question. h1 shipped the application layer and DECLINED the blocker half on the record. 02av reframed triage dispositions onto advisory resolutions with a zero-schema-change spike. Two preflight seats (fable READY-AFTER-FOLDS, codex NOT READY) — every material finding closed in durable state; the captain rulings from that round (recording identity, digest domains, authorization-only consumption, the named subspace release condition) live in the index Constraints.

## Float mechanism (the working ritual, superseding the 0260 findings 12-13 procedure)

Multi-artifact briefing packages work via DIRECT `subspace-tui --review-v1` inside a blocking zellij float — the launcher is preserved at _evidence/gate-float-interim/review-package-zellij (was /tmp-only). Learned this arc: briefing.json REQUIRES a context block (probe caught it); the review-v1 result envelope wraps as {"resolution":{...}} and a left-open float returns {"open":{...}} (review-v1-open-result) — the leave-open path retains cleanly now; the provider writes briefing.review.jsonl beside the package; results are room-resident from the first byte (the fix after a dead launcher's trap unlinked the captain's approval — finding 15). Probe-first remains the ritual; one recorded deliberate skip (same toolchain proven twice that session).

## Session mechanics findings not yet owned anywhere

- The Bash tool's persistent cwd repeatedly broke repo-root-relative commands after any `cd` into the state checkout (FOUR separate incidents now — the fourth occurred while writing this very debrief, creating a phantom nested docs/ tree inside the state checkout, removed). Conduct rule: absolute paths or a leading `cd` to the root in EVERY state-touching command.
- Piping spacedock commands through `| tail`/`| head` in `&&` chains masks non-zero exits (the near-miss where three sweeps sat uncommitted) — the pipeline's exit is tail's.
- `spacedock new`'s multi-workflow ambiguity error is correct but `--id-seed` errors misleadingly when --workflow-dir resolution fails from a wrong cwd.

## Open at capture

- 0.26.0: with the owning Commander — captain's refit-delta review + archive record, then the tag. Both pre-cut seats stored in the 0260 folder.
- The 0270 Commander boots on dispatch-sprint-execution.md after the 0.26.0 tag (captain's timing).
- Standing agents comm-officer + claude-md-auditor: alive since 2026-07-20, release decision with the captain. /tmp/claude-md-audit-report.md STILL unsaved (personal file, captain applies by hand).
- The precut auditor's scratchpad artifacts (lure transcripts, bw replay, 2ae drives incl. the captain's refit-delta review artifact) are tmp-ephemeral — the owning Commander harvests or they die.
- Sprint-close mining for 0260 runs after the tag (second manual run; recipe in the 0260 forensics).
