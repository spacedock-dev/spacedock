---
session-date: 2026-06-16
sequence: 2
first-commit: 726086fa
last-commit: f9462a3a
duration: ~7h
---

# Session Debrief — 2026-06-16 #2

The `0204-structured-reads` Commander session. Drove the 0.20.4 read-cost sprint: **all five read-cost members landed** (the `status --read` backbone + four reductions), pulled the team-mode-harness flake-owner and two verification follow-ups into the sprint, fixed a red `main`, and archived the 0.20.3 boot-forensics verdict.

## Shipped
- **e6a** `status-section-reader` — [#386](https://github.com/spacedock-dev/spacedock/pull/386). `spacedock status --read` returns a file's frontmatter + a section-heading→offset map so callers read one section via `Read(offset,limit)`, not the whole file. Rebased off a 25-commit-stale base (dropped the redundant README slim a9e669ae); AC6 live FO drive (offset 209/lines 14, 14 of ~205 body lines) + detached adversarial audit.
- **0q** `status-source-not-default` — [#387](https://github.com/spacedock-dev/spacedock/pull/387). SOURCE dropped from the default human table and the bare `--json` envelope, restored only via `--fields source` / `--all-fields` (Direction A, gate-ratified). Doc-only `command-reference.md` conflict resolved at merge (no CI re-run).
- **48** `commission-templates-defer-to-contract` — [#388](https://github.com/spacedock-dev/spacedock/pull/388). The three commission templates restructured to lead-with-the-end + defer universal rules to the FO/ensign contract; AC-1 live commission+rejection drive proved the defer composes (durable REJECTED end-state). claude-live skipped as irrelevant to commission templates.
- **j7** `status-set-staleness-echo` — roadmap decision, no code. The Read-then-`status --set` whole-file cache-write echo does NOT reproduce on Claude Code 2.1.178 (5-arm controlled spike); terminalized PASSED via `--force` (no-code outcome; FO-contract reason-line flagged for a follow-up).
- **6rt** `ci-log-read-summary` — [#389](https://github.com/spacedock-dev/spacedock/pull/389). Live-CI test steps emit clean per-package stdout + archive the full `-json` detail in one run (pinned prebuilt gotestsum v1.13.0, sha256-verified install, fails closed). The `t.Log` stream firehose is structurally suppressed (0x stdout / 40x in the `--jsonfile`, proven 6245B→39B).

## Filed (backlog, 0204)
- **ezf** `runtime-neutral-dispatch-core-cleanup` — pulled into 0204; make the host-neutral dispatch core genuinely runtime-neutral (it still carries Claude/team-only language Codex/Pi FOs now load).
- **hf** `journeymetrics-read-adoption-metric` — measure actual `status --read` usage in FO/ensign journeys (the behavioral verification of e6a's adoption; journeymetrics counts by tool name, `--read` is a Bash subcommand it can't see today).
- **4x** `read-hint-adoption-bloat-trim` — trim the redundant `--read` instruction sites (ensign-shared-core :18/:92 + the build.go:553 dispatch hint) once journeymetrics measures usage. Depends on hf (measure-then-trim).
- **m4** `live-team-mode-terminal-harness` — pulled into 0204 from backlog; pty/tmux live team-mode harness. Ideation PASSED + spiked (see What's Next).

## Non-PR commits (main + state, workflow-only)
- `e411c28d` (main) **fix(proposals):** quoted a bare `date: 2026-06-15` in `fo-contract-token-cleanup` frontmatter — unbroke `TestMigrationCheckFixturesParseConsistently`, which had been RED on main since another session's `e3eb35b5` landed (bare YAML date auto-timestamped → reader/direct parse diverged).
- `3d88ba17` (main) **roadmap(0203):** archived the v0.20.3 boot-forensics verdict — post-cut boot cost is structural (FO contracts + gate output), nothing materially avoidable.
- All other session state commits are routine stage-machine churn rolled up in the shipped PRs above.

## Decisions
- **0q Direction A** (gate-ratified): `source` opt-in everywhere; accepted the breaking `--json` default change; followed the captain-approved `--fields` de-dupe oracle-divergence precedent (native-only goldens).
- **48 AC-2 paraphrase limitation ACCEPTED** (not a blocker): the contractlint single-source guard catches realistic verbatim/near-verbatim doctrine drift; a deliberate paraphrase is an infinite space more markers can't close, and a structural absence-check would false-positive on legitimate slot prose. Measure-then-trim.
- **6rt AC-5 = pinned prebuilt gotestsum** — ruled out `go run gotestsum` (toolchain fragility: no pinned version builds on both CI Go 1.22 and modern local Go) and the stdlib grep (it could not strip the `t.Log` firehose — FO caught this; the ensign proved gotestsum `--format pkgname` suppresses it structurally). Declined a setup-go bump (out of scope).
- **m4: reference-only on spacedock-gym** (borrow the tmux mechanism, no cross-module import / no shared lib), a `liveDriver` interface seam, and the pty driver runs the existing `sharedRuntimeScenarios()` + fixtures + `gradeTerminalTeardown` verbatim. **m4 is the team-mode-coverage home, NOT the `TestLiveEnsignCycle` flake fix.**
- The **claude-live flake** is a budget/await issue, NOT a regression — identical code + pinned CC 2.1.178 passed on #385 and on isolated re-runs; concurrent live runs (9 at once) aggravated it. Mitigation: serial isolated re-runs; merge code members on their relevant/deterministic lanes when the flaky lane is unrelated (48) or the deliverable is offline-proven (6rt).

## Issues — Workflow
- **`TestLiveEnsignCycle` claude-live flake** — non-deterministic budget/await timeout on the headless-bare drive (CC 2.1.178 dropped headless team tools → the FO goes bare). Mitigated by serial isolated re-runs. Its root-cause fix is a separate budget/await item; m4 gives team-mode coverage a live home and unblocks `re` (verdict-omission). **Gates the v0.20.4 clean-CI cut decision.**
- **Red main** from a bare-date proposal frontmatter (another session's `e3eb35b5`) — fixed (`e411c28d`). Recurrence risk: proposal frontmatter dates must be quoted or full-Z.
- **Stale `index.lock`** (another session's crashed git, ~4 min old) blocked main commits — cleared (git-sanctioned).

## Issues — Spacedock
- Minor: `spacedock new` / the migration-check could warn on a bare YAML `date:` (auto-timestamped → parse divergence) at authoring time rather than only failing CI later. Not filed.

## Observations
- The `--read` backbone is dogfoodable immediately (used live this session to locate entity sections).
- **Serial isolated live-CI re-runs** are the practical flake mitigation (concurrency aggravates it). Per the 48 insight, a path-filter on the live workflow (skip the FO-cycle live lanes for template/doc-only changes) would save pointless flaky runs — candidate CI-efficiency follow-up.
- The split-root state branch is shared by concurrent sessions (shaping-FO, 0205 Commander); path-scoped commits + pull-rebase kept it clean, but commit logs interleave.

## What's Next
- **m4 (ideation PASSED, at gate) — APPROVE to implementation.** The captain confirmed the riskiest mechanism (a pty drive exposes team tools + stays resident; gym-tested + spike-proven). Dispatch m4 implementation in a worktree: the `liveDriver` seam + `ptyLiveDriver` + the two resurrected team-mode live tests (comm-officer roster injection, bounded-teardown marker), sharing scenarios/fixtures/grader. Encode the AC-5 spike hazards (no-pipe launch, FO-pane-by-title, stable-idle send).
- **v0.20.4 cut decision (open):** all read-cost members merged. Decide whether the cut waits on a flake-root-cause budget/await fix (re/e3z) or cuts with the serial-re-run caveat; then run the pre-cut antipattern audit and stamp 0.20.4.
- **Backlog (0204):** ezf (host-neutral core cleanup), hf (journeymetrics `--read` metric) → then 4x (bloat-trim), re (verdict-omission, unblocked by m4), e3z (bare-mode-baseline).
- **Cross-session:** flag the owning session to quote proposal-frontmatter dates (the bare-date will re-break the migration-check otherwise).
