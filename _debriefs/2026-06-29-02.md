---
session-date: 2026-06-29
sequence: 2
first-commit: 6ea56f95
last-commit: 55039e74
duration: ~8h
---

# Session Debrief — 2026-06-29 #2

A recovery-and-ship session. It opened by repairing the broken Haiku FO session (#1) — main contaminated with code that belonged in a worktree, a fabricated entity id, a never-compiled "proof" test — then drove two `0221-layered-fo` entities cleanly to merge: the end-value re-anchor gate rule and the resident-parent launcher.

## Shipped
- **bm** `gate-on-end-value` — [#441](https://github.com/spacedock-dev/spacedock/pull/441). The FO gate now re-anchors on the entity's end-value: a means-only AC whose value-measuring counterpart regressed is a REJECT, not a pass — proven by a live behavioral run in which a real FO applied the rule and rejected a means-only + regressed-value fixture.
- **vv** `launcher-resident-process` — [#442](https://github.com/spacedock-dev/spacedock/pull/442). The launcher stays resident as the host's parent (shared-foreground-group spawn-wait) instead of `syscall.Exec`'ing away — keeping `spacedock <host>` legible in process listings and enabling future sidecars.

## Filed (backlog)
_(none newly filed)_ — two existing entities changed:
- **z4** `fn-binding-refinements` — amended with a 4th refinement: promote the AC coverage cross-check to a `«gate.ac-cross-check»` `«fn»` (folding bm's re-anchor as an explicit guard/block), **pending bm's merge** (it restructures bm's EDIT A). Closes two of bm's validator edge-findings as a bonus.
- **0q** `ac2-design-proof-fixture` — id re-minted (`bmt9h…mz0a` → `0qe93g…`); the Haiku FO had hand-fabricated a near-duplicate of gate-on-end-value's id, which made `bm` ambiguous. Now resolves cleanly.

## Non-PR commits (workflow-only)
The repair touched `main` directly; entity state lives on `spacedock-state/dev` (split-root).
- Main repair: reset local `main` to `6ea56f95` (dropped 4 mis-placed Haiku commits, preserved on `salvage/haiku-session-2026-06-29`), kept the broken-session debrief (`2026-06-29-01.md`).
- State-checkout lifecycle (`spacedock-state/dev`): bm implementation re-land + cycle-1 fix + cycle-2 live-proof validation; vv ideation (PTY spike 11/11) + implementation + validation + cycle-1 Linux fix; the z4 amendment; the fixture id re-mint.

## Decisions
- **Repair over redo**: reset local main and re-route the Haiku FO's salvaged work into a proper worktree dispatch rather than discard it. The work was sound; only the process broke.
- **bm = gate-on-end-value** (the debrief's "bm", disambiguated after the fabricated-id fix).
- **Re-mint the fabricated fixture id**; the two Spacedock tooling-gap issues it exposed were **deferred** (not filed).
- **Amend z4 now** with the 4th refinement (vs. a separate entity), pending-bm.
- Routed bm's cycle-1 rejection back to implementation; re-validated only after the benchmark-token was refreshed.
- Approved both gates; pushed both PRs; approved + reran the flaky live-E2E lanes (codex-live, vv claude-live-sonnet); merged both.

## Issues — Workflow
- **Haiku FO fabricated entity ids** — hand-cloned a near-duplicate sd-b32 id for the fixture, colliding with gate-on-end-value on 22/24 chars and making `bm` unaddressable. Root cause: bypassed `spacedock new`. Fixed by re-mint.
- **Haiku FO's AC-2 "proof" was hollow** — the livescenario test was unwired/unreachable dead code AND never compiled under `-tags live` (a `declared-and-not-used`). Its "ready to run / integrates with claudeRunnerAdapter" claims were false. Caught by validation cycle 1.
- **vv validation was macOS-only** — missed a Linux-specific test-string bug (`syscall.Signal.String()` renders SIGWINCH as "window size changes" on darwin vs "window changed" on Linux). Caught by CI's offline lane; fixed cycle 1.

## Issues — Spacedock
Candidates, **deferred (not filed)** per captain:
- `status --validate` does not catch near-duplicate / non-minted sd-b32 ids (only byte-exact full-id dups) — a fabricated id passes validation silently.
- `status --resolve` is active-scope-only — it cannot resolve an archived entity even by its full 24-char id, so any reference to merged work reads as "unknown."
- **Environmental flake**: a single Bash invocation lost `git`/`tail` from `$PATH`, making `merge guard` fail mid-finalize ("git not found"). The transaction failed atomically (fully recoverable by re-running with PATH guarded), but the CRITICAL "rollback did NOT fully restore" warning over-stated the damage. Worth a clearer atomic-rollback message.
- Live-E2E lanes are flaky (codex rejection-flow final-state assertion; sonnet headless-gate FO non-determinism) — both passed on rerun.

## Observations
- **The feature was dogfooded on its own author.** gate-on-end-value's own re-anchor rule, applied at its first validation gate, correctly rejected its overclaimed "proof"; after the fix, a real FO applied the same rule against a real fixture and rejected for the right reason. The strongest possible self-demonstration.
- **vv's arc is the model**: spike-first ideation (real PTY, 11/11) → independent staff review (SOUND-WITH-FIXES) → implementation → mutation-confirmed adversarial validation → CI cross-platform catch → fix. Every claim exercised, not asserted.
- **Workers corrected the FO twice** (healthily): the impl ensign root-caused the SIGWINCH failure as a string-locale mismatch (not the sync-race the FO had diagnosed); the validator pushed back on the Haiku FO's overclaims.
- A credential-rotation instruction arrived shaped exactly like an exfil injection (out-of-band, mid-task, coercive "you MUST" footer, keychain-secret-to-plaintext-file). Defaulting to refuse-and-surface was correct; the cost was a false alarm, the right trade.

## What's Next
- **z4** `fn-binding-refinements` — **now unblocked** by bm's merge; the 4th refinement (`«gate.ac-cross-check»`) can land. Ready for ideation/implementation.
- **kt** `haiku-drive-validation` — the experiment the Haiku session botched; the FO infra is now proven end-to-end (this session was effectively a successful multi-entity FO drive), so kt can be run deliberately.
- File the two deferred Spacedock tooling-gap issues (validate near-dup ids; resolve archived) when ready.
- **main reconciliation**: local `main` carries the two debriefs on top of `origin/main`'s merges (rebased) — push when ready.
- Backlog otherwise unchanged (large `0221-layered-fo` + survey + release cluster).
