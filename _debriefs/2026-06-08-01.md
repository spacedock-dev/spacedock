---
session-date: 2026-06-08
sequence: 1
first-commit: 22a05e8c
last-commit: 3d303fd9
duration: multi-turn (2026-06-06 boot → 2026-06-08), Claude FO
---

# Session Debrief — 2026-06-08 #1 — README flip-prep (PR #322), survey signal-correction, the sprint-construct verdict, and a safehouse env bug

A long Claude first-officer session. Almost everything is IN FLIGHT (nothing merged this session): the headline is the reader-first + decision-first README reconciliation landing as **PR #322** (captain-steered + hardened by a 3-reviewer ICP bounty), plus the survey signal-correction cluster, the qa binary/version UX, the brew cask cleanup, and several filings + design conclusions. `next` HEAD unchanged at `74e3f824` (0.19.6) — the PR is open, not merged.

## Inherited at boot (pre-session, undebriefed)
The prior session (no debrief) landed before this one booted: **19** orient-workflow-discovery (#309), **rbjk** spacedock-claude-auto-install-plugin (#311), **4q** sweep-guard quarantine, **z6** pi-stage-dispatch (#300, Codex), **#312**, and the **0.19.6 release cut** (`74e3f824`). The /tmp handoff this session started from (session #04) was stale against all of that.

## Shipped
None merged this session. nb's PR #322 is OPEN (awaiting captain review + merge).

## Filed (backlog)
- **xn** `survey-signal-correction` — combined fix for the survey dogfood cluster (#318/#319/#320 + #317); in flight (validation).
- **78** `brew-cask-message-cleanup` — trim the Homebrew cask caveats; in flight (implementation, FO-verified).
- **v3** `ship-linux-binaries` — Linux build (one-line goreleaser add; CI already builds on ubuntu) + a `curl | sh` installer; dispatch before 0.20.0. Mirrors GitHub #321.
- **wv** `mkdocs-material-docs-site` — MkDocs Material docs site + Pages publish; fast-follow to nb.
- **5h0** `ban-readme-substring-assertions` — replace #315's README-substring packaging test with a behavioral seam test when it lands on main.
- **e6** `readme-real-workflow-example-link` — add a "see a real workflow" README link post-flip (the `docs/plans/` paths #315 linked are flip-fragile).

## In flight (resume these)
- **nb** `readme-main-flip-reconciliation` — **validation, PR #322 open, `mod-block=merge:pr-merge`.** Deliverable = README.md + docs/install-journey.md + docs/releasing.md (de-flipped to steady-state; flip runbook owned by pj) @ code `46092693`, entity @ `3d303fd9`. Reader-first → decision-first reframe (captain direct steering) → 3-reviewer ICP bounty + captain triage. AC-1..AC-9 PASSED (cycle 3). **Next: captain reviews #322 → on merge, FO clears mod-block → terminalize done/PASSED → archive → remove worktree/branch → close #213/#220/#315 → tear down the nb cohort.** e2e left out (docs-only); captain does reviews.
- **xn** `survey-signal-correction` — **validation gate** (cycle-1 zsh `nomatch` fix landed @ `e59c0861`; query-smoke 5/5 with proven teeth). NO bundled bin/ — recommended SQL in `skills/survey/references/queries.sql` + SKILL.md prose (supersedes #309); lighter onboarding proof bar (query-smoke + live drive). **Next: captain's AC-3 live drive (re-run `/spacedock:survey` via `--plugin-dir` to the worktree on a real repo) → approve gate → merge.** impl ensign held alive (feedback-to).
- **qa** `spacedock-binary-missing-install-journey` — **ideation gate presented** (expanded to the whole binary/version/upgrade UX: binary-missing journey + show-VERSIONS-not-"contract" message at `contract.go:156` + binary-needs-install/upgrade). Spiked: manifest version is a one-field read; binary version threaded as a param (no `contract`→`cli` cycle); `--version`'s `(contract N)` token kept. **Next: captain approve ideation gate → implementation (worktree; contract.go = high-stakes → validation gets a detached audit).**
- **78** `brew-cask-message-cleanup` — **implementation done + FO-verified** (caveats trimmed to a 3-line safehouse/agentsview recommend; xattr fallback dropped — postflight auto-clears; `goreleaser check` exit 0; `go test` 1141/16) @ `caca3d51`. **Next: validation (dispatch fresh) → merge.**
- **v3** `ship-linux-binaries` — backlog; **captain may dispatch its ideation before the flip.**
- **27** `agy-runtime-support` (implementation) + **z6** `pi-stage-dispatch` (validation, #300 MERGED un-advanced) — **Codex peers; do NOT touch/terminalize** (state-race boundary).

## Decisions
- **nb README, captain-steered throughout.** reject-the-garbage (maintainer-voiced) → reader-first rewrite → **decision-first reframe** ("the decision is the unit") → ICP bounty triage. Captain calls: "native sandbox integration" not "built in"/zero-wrapping (overclaim); list Codex/Pi without the "experimental" caveat ("users will report"); **mention Hermes (openclaw-like harnesses via skill systems)** — grounded; **de-flip `releasing.md` to steady-state** (one-time flip runbook owned by pj). Merge on the offline gate, **e2e skipped**, captain does reviews. **Supersedes #213/#220/#315.**
- **Survey cluster → ONE task, not 1-1** with the issues (reports close after it ships). **#316 (detect-spacedock incumbent + audit/meta-loop flip) OUT** (serves the spacedock-incumbent minority). **No bundled bin/** (captain: prose + recommended-SQL reference, supersedes #309). **Lighter onboarding proof bar** (report efficacy monitored by a separate project). The ideation **spike corrected the premise** (the #318 bug is `scan-project`'s own `basename(pwd)` key diverging, not agentsview fragmenting → `dirname(git rev-parse --path-format=absolute --git-common-dir)`).
- **Sprint/roadmap construct (from spacedock-gym): ship as contrib skill + commission template, DEFER builtin.** Adversarial design workflow (5 advocates/5 critics/judge): hybrid 6.5 (winner) · skill-only 5.5 · defer 5 · template-only 4 · builtin 2.5. Key: the sprint-membership query the builtin case wanted ALREADY EXISTS (`status --where sprint=X --where 'sprint-readiness != defer'`). Do-nots: no CONTRACT_VERSION bump, no sprint recognizer, no `--sprint-validate` DoD gate (domain-specific + binds to FO-authored frontmatter = tautological). Graduation = a divergence-capable, project-agnostic predicate proven across a 2nd project. Cross-project scan: **gym-only (N=1)** → defer stands. **Recorded → `docs/dev/_proposals/sprint-roadmap-construct.md`.**
- **qa whole, no carve-out** (captain). The 0.20.0 flip is NO contract change, so a 0.19.6→0.20.0 upgrade stays Compatible — the skew/"contract"-jargon path is NOT triggered by the flip; qa is UX hardening, not flip-blocking.
- **Linux binaries:** code already builds + tests on Linux (CI = ubuntu offline gate); gap = darwin-only release config + a distribution channel → recommended a universal `curl | sh` installer (goreleaser archive naming is already script-ready).
- **Brew two-channel (next + main):** the tap is stable-cask-only today. Concrete design given: a `spacedock@next` versioned cask (`conflicts_with` the stable one) + a **per-channel `devBranch` stamp** (stable→main, edge→next, so each channel's binary installs its own channel's plugin) + a next-publish goreleaser flow. Prereq: next must publish pre-built artifacts. Separable from the flip. NOT yet filed.
- **Upgrade path NOT fully resolved:** plugin-missing-auto-install (rbjk #311) + binary-absent-abort done; the show-versions message + the binary-missing journey + skew-recovery confirmation = qa (in flight); 44 (bundle) deferred. The flip itself doesn't exercise the skew path (no contract change).

## Issues — Workflow
- **Deliverable drift past validation** (recurring): nb kept moving after each validation pass (trim, releasing.md de-flip, sandbox fix) under live captain steering. Handled by FO delta-checks of each post-validation delta (not full re-validations) + an AC-3 refresh so the entity matches the shipped 3-doc deliverable. Watch this pattern when the captain steers an ensign directly via Shift+Down.
- **zsh `nomatch` glob trap** bit twice in FO bash this session (a `*` glob with no match aborts under zsh) — the same class as the survey §1 bug. Quote/`find` over bare globs in FO shell.
- **GitHub #321** (Linux binaries) was mis-filed as a public issue when the captain meant a LOCAL task — local `v3` is the tracker; #321's disposition (close vs keep as public report) is pending.

## Issues — Spacedock
- **safehouse strips `SPACEDOCK_BIN` (HIGH — NOT YET FILED).** A safehouse-wrapped `spacedock claude/codex` launch loses the launcher-injected `SPACEDOCK_BIN`: `launchEnv` (frontdoor.go:55) injects it via `resolvedLauncherBin`, but the real safehouse sandbox sanitizes the env and drops it, so the session falls back to the PATH binary, not the dev/worktree binary that launched it. Confirmed from inside a safehouse-wrapped gym Commander session (the gym `.safehouse` adds the v1 dir → auto-wrap). The smoke test `TestSafehouseShapePreservesSpacedockBinToInnerChildFixture` gives FALSE confidence — it uses a fake `exec "$@"` safehouse that preserves env; the real sandbox doesn't. Fix: forward `SPACEDOCK_BIN` through the safehouse env allowlist + rebuild the smoke test against the real safehouse (or a sanitizing shim). Immediate workaround: `export SPACEDOCK_BIN=<dev binary>` in the wrapped session. **File this.**
- Already filed this session: **jh** (dispatch-build flag/file runtime docs vs accepted-binary skew), **gf** (split-root state sync needs a no-origin local mode), **qy** (root `go test` fails on debrief frontmatter) — all backlog.

## Observations
- **The detached/independent review keeps earning its keep.** The survey staff review caught a broken `--git-common-dir` formula the green spike had shipped (M1); the 3-reviewer ICP README bounty triangulated the real top issues (3/3 on the install contradiction, 2/3 on the native-sandbox overclaim + the unbacked flagship claims). Independent eyes catch what a self-green pass structurally can't.
- **Captain-steered ensigns + the comm-officer standing teammate worked well** for the README — many fast polish round-trips; the FO stayed out of the content and handled gating/merge/triage. The cost is drift-past-validation (above).
- **"Build works on Linux ≠ shipped on Linux"** and **"flip keeps contract 1 ≠ no upgrade UX gap"** — two places where the easy assumption hid the real work (distribution; the binary-missing/skew journey).

## What's Next
**Captain's open queue (the merge slate):**
- nb PR #322 — review → merge (FO closes #213/#220/#315 + terminalizes).
- xn survey — AC-3 `--plugin-dir` re-test → approve gate → merge.
- qa — approve ideation gate → implementation.
- 78 brew-cask-cleanup — dispatch fresh validation → merge.
- v3 ship-linux-binaries — dispatch ideation (before the flip).

**To file (FO, on captain nod):**
- The **safehouse `SPACEDOCK_BIN`** bug (Spacedock issue).
- The **brew two-channel + per-channel `devBranch` stamp + next-publish** flip release-mechanics task (absorbs the goreleaser `devBranch=next→main` retarget).
- The **`spacedock:roadmap` skill + commission sprint template** build task (from the `_proposals/` decision record), post-flip.
- GitHub **#321** disposition (close vs keep).

**The capstone:** **pj** `main-flip-0200-marketplace` — cut 0.20.0 on main + flip the marketplace, once the slate (nb/survey/qa/brew/linux) is in and the flip release-mechanics (goreleaser per-channel stamp) is built. Carried backlog/ideation: p2, vc, bw, j9, e3z, ev, 7q, ng, 5w, e30, 82, ey, 95.

**Peer coordination:** Codex owns the pi-line (27 agy in implementation; z6 #300 merged-un-advanced) — do NOT rebase their worktrees or terminalize their entities.
