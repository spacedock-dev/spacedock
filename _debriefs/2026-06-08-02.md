---
session-date: 2026-06-08
sequence: 2
first-commit: f5c2c1cc
last-commit: 46794291
duration: multi-turn, ~13h wall (2026-06-07 21:11 → 2026-06-08 10:13 PT), Claude FO
---

# Session Debrief — 2026-06-08 #2 — cut 0.19.7, dogfooded the sprint/roadmap construct (twice), shaped 0.19.8

Drove the inherited merge slate to a real release — **spacedock 0.19.7 cut** (5 PRs) — while dogfooding the sprint/roadmap construct end-to-end: **019x-pre-flip-cleanups** (a pragmatic single-session FO drive → 0.19.7) and **0198-pre-flip-hardening** (the faithful two-tier — FO shapes/ideates/preflights/packages, the Commander drives — shaped + packaged + handed off). Two independent staff reviews (pre-drive readiness + post-sprint antipattern) earned their keep.

## Shipped (sprint 019x → 0.19.7)
- **nb** `readme-main-flip-reconciliation` — [#322](https://github.com/spacedock-dev/spacedock/pull/322). Reader-first + decision-first README + install-journey + steady-state releasing (closed #213/#220/#315).
- **78** `brew-cask-message-cleanup` — [#323](https://github.com/spacedock-dev/spacedock/pull/323). Trim the Homebrew cask caveats — drop the xattr note, shrink the safehouse wall.
- **xn** `survey-signal-correction` — [#324](https://github.com/spacedock-dev/spacedock/pull/324). Survey reads ground-truth signals via `references/queries.sql` + SKILL.md (no bundled `bin/`).
- **e30** `dispatch-path-teamname-sanitize` — [#325](https://github.com/spacedock-dev/spacedock/pull/325). Sanitize `team_name` in the dispatch-file path (Option-B `teamNamePattern`) + fix the bare-mode comment.
- **qy** `internal-status-test-hygiene` — [#326](https://github.com/spacedock-dev/spacedock/pull/326). Make `go test ./...` pass from the repo root (migration-check `_debriefs` + contractlint `.claude` skips).
- **spacedock 0.19.7 cut** — `a35c7002`, annotated tag `v0.19.7` (3 manifest bumps; Release workflow green).

## Filed (backlog)
- **kb** `migration-check-prune-state-walk` — prune `.spacedock-state` in the migration-check walk (the 019x post-sprint audit's one Material finding) + drop orphaned survey fixtures. **[0198 active]**
- **z9** `codex-plugin-auto-install` — front-door Codex plugin auto-install, the analog of #311 (codex-cli 0.137.0 adds `plugin marketplace add` + `plugin add`). **[0198 active, ideation approved]**
- **nzb** `gate-release-on-e2e` — gate the release/flip on the live e2e suite. **[deferred → post-flip]**
- **vh** `survey-skill-correctness-pass` — one survey-skill pass on the corrected agentsview git-root model + codex-cwd + scaffold-fact + sandbox-probe; **consolidates** 69/1p/4t. **[deferred → post-flip; design banked]**
- **69** `survey-codex-cwd-workaround`, **1p** `survey-scaffold-state-the-fact`, **4t** `survey-agentsview-detect-under-sandbox` — superseded into `vh`.
- Roadmap construct artifacts (main repo): `docs/roadmap/README.md` + `019x-pre-flip-cleanups/` + `0198-pre-flip-hardening/` (index + staff-review + commander-dispatch). Closed survey issues **#317–320**.

## Non-PR commits (workflow-only)
- Roadmap construct + both sprint folders (main repo): `8d785cc3`, `9f7b3b66`, `bb75a132`, `2d4a9f19`, `81295097`, `59d1f303`.
- State: the sprint carve/tag/defer/consolidate commits + the per-entity merge ceremonies. All other state commits are routine stage churn, rolled up in the shipped PRs above.

## Decisions
- **Sprint/roadmap construct — convention-only dry run (option B), dogfooded twice.** 019x = pragmatic single-session FO drive (I drove all stages). 0198 = the FAITHFUL two-tier (FO shapes/ideates/preflights/packages + gets gate approvals; the **Commander** drives implementation) — CL corrected the role-discipline mid-session ("leave dispatch to the commander"). Zero new binary code (frontmatter + the native `--where` query). Graduation needs the construct proven across ≥2 sprints + this 2nd project — now in hand.
- **0.19.7 = the 019x pre-flip-cleanups release. A sprint per release is the natural cadence** (019x→0.19.7, 0198→0.19.8, 0200→flip).
- **jh (version-skew): NO contract bump pre-first-release** (captain). The 0.19.4-vs-current flag-form skew self-resolves once the first release (0.20.0) sets the contract-1 baseline (flag form included). jh deferred.
- **B2 — agentsview keys `project` by GIT-ROOT basename, not cwd basename.** xn's just-shipped scoping model (SKILL.md:64 + `queries.sql` folded_keys) teaches the wrong premise. `vh` corrects it: drop `folded_keys` (structurally always 1), invert the cwd-union rationale (it excludes same-basename SIBLING repos), Codex blank-cwd → a flagged presence count, not a silent union.
- **0198 (qa-led):** `qa` (binary/version UX) + `z9` (codex auto-install) ideation gates **APPROVED**; `nzb` (e2e-gate) + `vh` (survey) **DEFERRED post-flip**; `jh`/`5h0` deferred. Active drive set: **qa / z9 / kb**.
- **z9 channel-tracking via the shared `devBranch`** (`frontdoor.go:49`, today `"next"`). The flip MUST retarget `devBranch` `next→main` (the per-channel stamp / 2-channel work) — a **flip dependency**, not a 0198 task.
- **2-channel brew design** (stable@main + edge `spacedock@next` `conflicts_with` it; per-channel `devBranch` stamp; next-publish flow) — recalled, **still UNFILED** (flip-release-mechanics).
- **Commander = a separate top-level session.** The subagent probe confirmed it empirically: a subagent has no `Agent` tool and can't create its own team — so it cannot dispatch.
- **Pre-drive staff review + post-sprint antipattern audit are complementary** — both review points; the construct should carry both.

## Issues — Workflow
- qy shipped a symptom fix (the post-sprint audit's Material) → `kb` files the proper tree-prune (0198).
- **entity-vs-task language (CL):** the Commander/FO prose uses generic "entity/entities" instead of the workflow's declared `entity-label` ("task"). Operating + captain-facing prose should localize to the label. **[improvement candidate]**

## Issues — Spacedock
- **The two-tier Commander handoff is underspecified.** How an ideation-gate approval is recorded for the Commander (this session: I left `qa` at `status=ideation` and recorded the approval in the package prose); the Commander needs a separate session (subagent can't dispatch). Candidate: a binary/contract affordance for the gate-approved→Commander handoff. **Not filed.**
- **entity-label localization** — operating/captain-facing prose should resolve the README `entity-label`/`entity-label-plural`; the shared contract keeps generic "entity." **Not filed** (CL's observation).
- **4t — survey agentsview install-detect false-negatives under sandbox** (`command -v` stat/access denied by Seatbelt vs `agentsview --version` execve allowed). Filed → folded into `vh`.
- **jh — runtime docs outpace the accepted binary at the same contract** (the dispatch-build flag form). Deferred — self-resolves at the first release.
- **Carried, still OWED (not addressed this session):** the **safehouse `SPACEDOCK_BIN` strip** bug (HIGH); **GitHub #321** disposition.

## Observations
- The independent review keeps earning its keep: the 019x preflight refuted my over-cautious e30/jh collision flag + found readiness gaps; the post-sprint audit caught qy's symptom-fix (a cross-cutting inconsistency per-entity validation structurally can't see); the 0198 preflight caught the B2 survey-model contradiction + the DoD#4-unowned gap.
- I over-drove 019x (drove all stages as FO); CL corrected to the faithful two-tier for 0198. The construct's payoff is the clean conn-to-drive handoff.
- I dropped the 0198 folder's `staff-review.md` + `dispatch-sprint-execution.md` at carve time (CL caught it) — the commission template should scaffold all three at carve.
- Consolidation: 3 coupled survey tasks on one file + one shared model → 1 task (`vh`). CL's "supersede with one task" was right; it also collapsed three live-drives into one.

## What's Next
**0198 Commander handoff (immediate):** boot a SEPARATE Commander session on
`docs/roadmap/0198-pre-flip-hardening/dispatch-sprint-execution.md` to drive `qa` → `z9` + `kb`
→ cut **0.19.8**. Captain-gated within the drive: qa's live drive, z9's detached adversarial
audit, the 0.19.8 release cut.

**The capstone — `pj` `main-flip-0200-marketplace`.** Depends on: 0198 landed + the
**2-channel / `devBranch` retarget** (next→main, so z9/#311 install the main plugin) + `nzb`
(e2e-gate, the gate on-branch before the tag).

**Post-flip:** `vh` (survey pass, design banked), `nzb` (e2e-gate), the **`spacedock:roadmap`
skill graduation** (the construct → contrib skill + commission template — fold in the
all-three-files-at-carve fix + the entity-label localization).

**To file (FO, on captain nod):** the **2-channel + per-channel `devBranch` stamp + next-publish**
task (flip-release-mechanics; z9's post-flip dependency); the **entity-label localization**.
Carried: the **safehouse `SPACEDOCK_BIN`** bug; **#321** disposition.

**Peer coordination:** Codex owns `27` (agy-runtime-support) + `z6` (pi-stage-dispatch, #300
merged-un-advanced) — do NOT rebase their worktrees or terminalize them.
