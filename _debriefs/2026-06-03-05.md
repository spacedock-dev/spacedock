# Debrief 2026-06-03 — enforce strict first-officer boundary

## Trigger

During `pi-runtime-frontdoor` validation follow-up, the main Pi session read a REJECTED validation report and directly edited product code in `.worktrees/spacedock-ensign-pi-runtime-support/internal/ensigncycle/pi_live_runner_test.go` instead of routing the rejection back to an implementation worker.

## Boundary that should be enforced

The main session is the Spacedock first officer. It should inspect, triage, dispatch, validate, and update workflow state, but should not directly edit or commit product/source files. A validation rejection should become an implementation-worker handoff or a captain decision, not an inline parent-session fix.

## Follow-up

Add process/tool enforcement later:

- Add explicit FO role-boundary text to `AGENTS.md` and/or the first-officer skill.
- Add a harness/tool guard that denies main-session `edit`/`write` outside FO-owned state/report paths unless explicitly overridden.
- Define the rejection loop: classify rejection, record triage, dispatch a narrow implementation worker, then dispatch independent validation.

## Current cleanup note

A direct product diff may remain in the implementation worktree from the violation. Treat it as uncommitted fix material that needs either worker adoption or rollback before finalizing `pi-runtime-frontdoor`.
