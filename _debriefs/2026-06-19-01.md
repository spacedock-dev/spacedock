---
session-date: 2026-06-19
sequence: 1
first-commit: cf286eea
last-commit: 2c9609f8
duration: ~8h (2026-06-18 16:00 → 2026-06-19 00:10 PT, incl. long async CI + ensign waits)
---

# Session Debrief — 2026-06-19 #1

Shaping-FO session that became a **Commander run under the captain's conn** and **cut `v0.22.0`** — the Claude Code 2.1.178+ merged-team-model migration + the CI unpin. Drove `m4` + `ci-unpin` through implementation → validation → a combined PR #390 → merge → release, on a live legacy-team-host session (a real both-semantics demonstration).

## Shipped
- **m4** `live-team-mode-terminal-harness` — [#390](https://github.com/spacedock-dev/spacedock/pull/390). A both-semantics live team-mode harness: a merged-semantics lane (headless `claude -p`, no tmux — named background Agent, no TeamCreate, `subagents/agent-*.meta.json` member oracle, `team-lead` completion) + capability skip-gates so exactly one team lane runs per Claude version + a folded `claude-fo-dispatch.md` contract correction.
- **rr** `ci-unpin-claude-version` — [#390](https://github.com/spacedock-dev/spacedock/pull/390). Unpin live-e2e from 2.1.177 to installer-`latest` (DISABLE_AUTOUPDATER freeze-for-run); delete the 2.1.177 pin guard for a float guard with a re-pin adversarial twin; rewrite the legacy removal-trigger guard to "consumer retired, path retained."

Both landed via the **combined PR #390** (squash-merge `7afe339b`), cut as **`v0.22.0`** (tag published, Homebrew cask bumped, `stable` branch advanced). The e2e-gate was cleared via a recorded `SPACEDOCK_E2E_GATE_WAIVER` (since unset).

## Filed (backlog)
- **2e** `bare-mode-coverage` — explicit forced-bare scenario + the `-p`-assumes-bare audit; sequenced after pin-retirement (the merged floor makes headless `-p` team-enabled by default, so bare loses its incidental coverage).
- **rr** `ci-unpin-claude-version` — shipped same session.
- **3e** `wrong-root-guard-compound-boot` — the opus wrong-root-guard false positive; the `v0.22.0` e2e-gate-waiver follow-up.

## Non-PR commits (workflow-only)
- `cf286eea` m4 readiness package (both-semantics seam, legacy-lane probe, merged-lane spec).
- `18033c7b` fold the headless-registry contract finding into m4 (captain — contract correction rides in m4's PR, not a separate task).
- `69dd3a44` ratify command-surface routing principles (ACCEPTED, reviewer rubric); shelve `dwd` command-surface-gate.
- `f96f4a1a` release: bump version to spacedock@0.22.0.
- `2c9609f8` reconcile version labels post-cut (m4+unpin SHIPPED v0.22.0; layered-FO → 0.22.1).
- Concurrent (not this FO): `1d691b45` + `0b51a11a` `using-claude-team` re-architect around the merged worker back-channel (#396) — the contract this session's cut built on.

## Decisions
- **Re-scope (captain):** the m4+unpin cut, originally 0.20.5, was **renamed to `v0.22.0`**; the `0205-layered-fo` sprint bumped from 0.20.6 → **0.22.1**. 0.22.0 unpins CI so it runs the merged-team floor.
- **Legacy TeamCreate = best-effort in code** (`using-legacy-claude-team`), retired when STABLE Claude catches up — NOT fixed in CI, NOT a separate pinned job.
- **m4 findings folded INTO m4 (captain):** the `claude-fo-dispatch.md` line-7 contract correction rode in m4's PR, not a separate task.
- **Combine into one PR (captain):** m4 + ci-unpin co-edit `runtime-live-e2e.yml` and are co-dependent for the live proof → combined onto PR #390; one unpinned-CI run proves both.
- **Merge on the sonnet proof (captain):** the opus `claude-live` red was the pre-existing wrong-root-guard false positive (the deliverable passed on both models); merged via admin override on the sonnet + deterministic + merged-lane green.
- **e2e-gate waiver (captain):** recorded `SPACEDOCK_E2E_GATE_WAIVER` (auditable reason) for the same opus flaky guard to publish; unset after the e2e-gate passed.
- **Verb gates `mz` + `rgq` APPROVED at ideation (captain)** — they STAY at `ideation` (gate-approved) for the 0.22.1 Commander, not advanced/shipped this session.
- **Principles ratified** (ACCEPTED as the reviewer rubric); `dwd` command-surface-gate **shelved** (de-scoped).
- **FO given the conn** to drive the merge → terminalize → tag → publish.

## Issues — Workflow
- **Opus wrong-root-guard false positive:** `bootPathArgs` (`wrong_root_detect_impl_test.go:106`) splits a compound boot `cd X; ls; cat README` on whitespace and glues the trailing `;` onto the cd target, so `isUnder` fails though the FO booted the correct root. Latest-opus chains its boot command; pinned-2.1.177 and sonnet did not, so it surfaced only on the unpinned floor. Filed as `wrong-root-guard-compound-boot`; recorded in the 0.22.0 e2e-gate waiver. Test-harness only, zero product impact.
- **Headless-`-p` merged host writes no `teams/config.json`:** the member record is `projects/<cwd>/<session-id>/subagents/agent-*.meta.json` (the `teams/session-*/config.json` registry is the INTERACTIVE artifact). `claude-fo-dispatch.md` line 7 was corrected (folded into m4). Consequence: the FO's `reconcile` leadSessionId auto-discovery **degrades to git-only on a headless `-p` merged host** — a `reconcile.go` fix (read the meta.json path for headless roster discovery) is flagged as a **0.22.1 candidate** (bears on `haiku-drive-validation`'s launch shape).

## Issues — Spacedock
- `gh pr edit --title/--body` hits a "Projects (classic) deprecated" GraphQL error that silently swallows the title mutation; `gh api -X PATCH repos/.../pulls/N` (REST) works. Minor tooling friction; gh-side, not spacedock. Not filed.
- The marketplace manifest's `version` display field lags at `0.20.0` while `source.ref: stable` tracks the (now-advanced) `stable` branch — delivery is correct, the display field is cosmetic and is `qp` (steady-state-runbook) territory. Not touched.

## Observations
- **The FO's own session was a LEGACY-team host** (`ToolSearch(select:TeamCreate)` matched) even though the local `claude` binary is 2.1.181 (merged) — a live demonstration of exactly why m4 needs both semantics. The session's team-tool surface is fixed at launch and can diverge from the binary version. The dispatch ran the legacy `TeamCreate` path throughout.
- **Validate-the-riskiest-mechanism-first paid off:** the m4 ensign probed the merged dispatch BEFORE writing assertions and caught the spec's auto-team-registry premise being false on headless `-p` — corroborated by the FO's own earlier `-p` trials (no `teams/` dir created). Saved a wasted validation cycle on wrong assertions.
- **The merged team model (.178+) works flag-free headless:** named background `Agent` + `SendMessage`, no `TeamCreate`, no `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` flag (reproduced live this session + by #396).
- **Coupled deliverables that co-edit a CI file and share one live proof are cleanest combined into one PR** with a single green run — neither m4 nor ci-unpin could be live-green alone.

## What's Next
**0.22.1 — the `0205-layered-fo` sprint on the merged floor (a Commander drives):**
- `mz` `merge-finalize` (gate-approved), `rgq` `state-verbs` (gate-approved) — the verb core; approvals recorded here for the Commander, entities still at `ideation`.
- `72` `fo-tier-delegation` (gate-approved), `6re` `gate-extract-verbs` (built, branch unmerged), `czw` `prose-function-restructure` (needs ideation), `kt` `haiku-drive-validation` (LAST, the sprint gate proof).
- `reconcile.go` headless meta.json roster discovery — 0.22.1 candidate (the headless-reconcile-degradation; bears on `kt`'s launch shape).

**Cleanup / follow-ups:**
- `3e` `wrong-root-guard-compound-boot` — small test-harness fix so the next live-e2e is clean-green (no waiver).
- `2e` `bare-mode-coverage` — forced-bare scenario, tied to pin-retirement.
- Orphan `27` `agy-runtime-support` — still parked at implementation, no sprint; needs a disposition call.
- Marketplace `version`-field cosmetic lag — `qp` runbook territory.
