---
session-date: 2026-06-12
sequence: 1
first-commit: 3a694742
last-commit: a4f56f1e
duration: ~multi-day (sim-dated 2026-06-08 → 2026-06-12)
---

# Session Debrief — 2026-06-12 #1

Shaping-FO session across the **post-0.20.0-flip** period. The flip itself shipped under the Commander (debrief #2). This session: confirmed the flip landed clean (and backed off `pj`), made the **post-flip branch-model decision (Model B, sprint 0201)** grounded by a research workflow, merged the docs-site materialization (#342) + terminalized the docs site (wv), captured a **second round of survey feedback**, and **intaked + dispatched an interactive ensign for the reviewer-feedback docs revision (#343)** — in-flight at session end. Ran the latter half under an **SSH-push outage**, rerouting git network through gh's HTTPS credential helper.

## Shipped
- **wv** `mkdocs-material-docs-site` — [#341](https://github.com/spacedock-dev/spacedock/pull/341) (merged by captain); FO terminalized PASSED + archived + cleaned the worktree this session.
- **PR #342** `docs site: materialize the 20 [TODO]-stub pages` — [#342](https://github.com/spacedock-dev/spacedock/pull/342), squash-merged to `main` by the FO after a fresh-venv `mkdocs build --strict` (green). Standalone PR, not a tracked entity.

## Filed (backlog)
- **te** `install-refresh-and-upgrade-hint` — `spacedock install` left a tag-fresh 0.20.0 binary on a stale 0.19.8 plugin with no upgrade hint (the "0.19.8 thing").
- **gp** `marketplace-repo-and-pinned-channels` — Model B core: separate marketplace repo + tag-pinned stable / edge-on-next channels.
- **ez** `stamp-then-tag-release-ritual` — invert the release ordering so a tag's commit manifest matches its tag (the v0.20.0→0.19.9 mismatch).
- **h5** `survey-output-polish` — 2nd-round survey catches (Codex workstream null-signal; scratch-preamble leak) + cluster refinements.
- **5x** `survey-lens-honesty` — author feedback: own the recent-window snapshot + an honest partial-corpus caveat.
- **pt0** `docs-site-feedback-revision` — **#343 intake**, at `implementation`, **in-flight** (interactive ensign, captain-steered).

## Non-PR commits (workflow-only)
Process docs + the research artifact (FO owns `docs/roadmap/`):
- `a4f56f1e` roadmap(0201): post-flip release model — Model B decision recorded. **On `next`, NOT pushed** (SSH outage).
- Plugin-distribution research workflow `wcdgsgd88` (9 agents, adversarially verified) — grounded the Model B decision; report relayed to captain, not committed.
- Author-followup reply drafted (sharpen the survey classifier) — handed to captain, awaiting the author's answers.

**Parallel writers (NOT this FO — noted for reconciliation):** `vv` launcher-resident-process seed (captain, 06-10); `69`/`4t`/`1p` archived (captain `--force`); the Commander's `pj` terminalization + 4 post-flip follow-ups (`tw`/`w6`/`qp`/`6a`) + debrief #2.

## Decisions
- **Post-flip branch model = Model B** (captain, grounded by `wcdgsgd88`): the **stable channel serves the latest `v*` tag** (not branch HEAD); `main` becomes the **trunk** (normal/UX work lands via PR); `next` = edge served at HEAD; **stamp-then-tag** release ritual; **separate marketplace repo** kills the main/next `source.ref` divergence; **seamless migration** via the next binary's auto-repointing install (rides `te`'s refresh fix). Locked in principle, all details settled, roadmap doc written — **pending captain review + scope-lock**, then ideate `gp`/`ez`.
- **Docs deploy moves to the Astro landing site** (`spacedock-landing`); the mkdocs `docs.yml` Pages job is descoped (dead-weight cleanup candidate). Closes the parked `/llms.txt` project-Pages-subpath concern.
- **The survey is the single-repo prototype of the 0.21.x decision-frontier** — its NEEDS-YOU/BACKLOG triage + decided/shipped/superseded cross-check is the ready-room in nascent form. The survey-polish cluster and 0.21.x are the same conversation.
- **Backed off `pj`** — Commander-owned; the flip's outward steps were already on the remote at boot (tag-the-green-tip invariant verified: `v0.20.0`→`6accd320`, `archive/v0`→OLD_MAIN).

## Issues — Workflow
- The **"message sent mid-turn / while interrupted reports success but never lands"** race recurred (comm-officer routing) during interactive steering of the #343 ensign — the same friction flagged in the boot handoff. Re-send resolves it; the ensign briefly mistook the best-effort polish channel for a blocker (corrected: it's non-blocking by contract).

## Issues — Spacedock
- **SSH push down mid-session** (`ssh-agent` unreachable: "Operation not permitted") — blocks `git fetch`/`push` over SSH. Worked around via `git -c credential.helper='!gh auth git-credential'` over HTTPS (gh token). Environmental, but argues for the `gf` local-mode degrade + a documented gh-HTTPS fallback. Not filed.
- **Reconcile `E` class is pre-flip logic** — it assumes local `main` tracks `origin/next` and flags "main carries N commits not on next"; post-flip `main` legitimately diverges (stable line), so `E` fires every boot and its remedy ("reset main→next") would **destroy the flipped main**. The FO did NOT act on it. Candidate fix (folds into 0201/`w6` territory). Not filed.
- **CI gates only trigger on PRs to `next`, not `main`** (`install-e2e.yml`/`runtime-live-e2e.yml` declare `branches: [next]`) — so #342 merged to `main` ungated. Harmless for docs, but **a hard requirement for Model B** (main-as-trunk must gate on `main` PRs). Fold into `gp`/0201.

## Observations
- **The research workflow earned its keep.** It established that Claude Code's marketplace `source` supports `ref` (tag) **and** `sha` pinning, that the `version` field is the update cache key, and that Anthropic's own curated `claude-plugins-official` sha-pins 125/125 entries — grounding Model B empirically instead of by assertion. One claim was adversarially refuted (the "only Homebrew serves branch-HEAD" aside) before it could mislead.
- **The flip's tag-the-green-tip left a tag→manifest mismatch** (`v0.20.0`'s commit says plugin 0.19.9; the 0.20.0 stamp landed 2 commits later) — harmless under HEAD-serving, fatal under tag-serving. Exactly why `ez` (stamp-then-tag) is a Model-B prerequisite. A clean case of the ritual lagging the real machinery.
- **gh-HTTPS is a complete fallback** for git network ops when `ssh-agent` dies — fetch and push both work via the gh token; worth codifying.
- Stale local `main` (the old v0 line, `8c069d95`) fed a research agent a bogus "main 0.12.1"; fast-forwarded local `main` to `origin/main` to stop the recurring confusion — but that's also what surfaced the pre-flip `E`-class issue above.

## What's Next

**⚠️ Push backlog (SSH outage) — flush first:** unpushed state commits `04a3619f` / `918a3a2f` / `433c3b0c` / `43db5d55` (+ this debrief), and the roadmap doc `a4f56f1e` on `next`. Push via the gh-HTTPS route (`git -c credential.helper='!gh auth git-credential' push https://github.com/spacedock-dev/spacedock.git <branch>`) or once the key is restored. #342 already landed on `main`.

**In-flight — finish #343 (pt0 `docs-site-feedback-revision`):** the interactive implementation ensign was parked at session end. **Next FO: respawn it from `/tmp/spacedock-docs-site-feedback-phase2-handoff.md`** to finish the Phase-2 revision, then route to `validation` → merge #343 to `main`. The deliverable is committed locally on the `docs-site-feedback` branch (worktree `.worktrees/docs-site-feedback`); push the updated branch (gh-HTTPS) when the captain's satisfied.

**0201 release model:** captain reviews the roadmap doc → scope-lock → ideate `gp` (marketplace-repo) + `ez` (stamp-then-tag). Fold in the CI-on-main-PRs and reconcile-`E` findings. Note the overlap with the parallel-filed `tw`/`w6`/`qp` (next-release-line / marketplace-repo-decouple / release-runbook) — dedupe these into the 0201 sprint.

**0.21.x decision-abstraction shaping** (grounded in `~/git/spacedock-research/spaceterm/spacedock-ui-research/`): the decision-event model + ready-room + `orient`. The survey-polish cluster is its single-repo prototype. The captain's bands: 0.20.x cleanup/UX, 0.21.x decision abstraction, 0.22.x dynamic UI via mcp-app (the research says **defer the rich UI, start cheap**).

**Survey-polish cluster:** `zb` · `zw` · `za` · `h5` · `5x` + the author-followup reply (pending the author's answers, which fold into the classifier seeds).

**Other backlog:** `5ar` cleanups (Node-action bump), `44`/`5w` distribution+notarize, `xp` cross-session FO↔Commander comms, `gf` state-sync local-mode degrade.
