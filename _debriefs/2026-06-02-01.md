---
session-date: 2026-06-02
sequence: 1
first-commit: 9cffe173
last-commit: 54c8b621
duration: ~6h (2026-06-01 19:04 → 2026-06-02 01:02, state-checkout span; wall-clock longer incl. release + CI waits)
---

# Session Debrief — 2026-06-02 #1

Resumed from the session-04 handoff (sprint done, v0.19.2 released). Synced the diverged local `main` to canonical `origin/next`, then ran a focused **0.19.3** patch: the shipped codex front-door regression, the `--plugin-dir` dev-workflow + live-e2e-net restore, and the split-root ORPHANS misreport — **cut + published v0.19.3**. Then hardened the live-e2e net (per-stage timeouts), which surfaced + diagnosed the real live-e2e flakiness (the FO stalls headless). Closed a pile of stale-but-shipped issues. FO held the conn (gates, pr-mod pushes, CI-E2E env approvals, merges) per the session-03 delegation; CL drove scope + the genuine forks.

## Shipped
- **5p** `codex-plugin-list-parse-drift` — [#258](https://github.com/spacedock-dev/spacedock/pull/258). `spacedock codex` reported "no installed codex plugin found" even when installed — `codexEntryInstalled` grepped the paren form but real codex prints the comma/table form; fixed with field-based parse-tolerant matching + captured-real-format tests.
- **at** `orphan-status-drift` — [#259](https://github.com/spacedock-dev/spacedock/pull/259) (closes spacedock-dev/spacedock#251). `status --boot` ORPHANS misreported present split-root code worktrees as missing — resolved worktree existence against the code repo root (`FindGitRoot(definitionDir)`), the same class `f2` fixed for scanMods.
- **jg** `front-door-plugin-dir` — [#260](https://github.com/spacedock-dev/spacedock/pull/260). `spacedock claude --plugin-dir` (before `--`, the captain's primary dev workflow) was rejected by the cobra migration, AND the live-e2e net had been silently dead since that migration — promoted `--plugin-dir` to a repeatable StringArray + restored the live test.
- **37** `live-e2e-per-stage-timeouts` — [#261](https://github.com/spacedock-dev/spacedock/pull/261). Replaced the live test's monolithic 12m timeout + `CombinedOutput()` with a streaming per-step ≤60s no-progress (quiet) watcher (Go port of upstream `FOStreamWatcher`) — fails fast + localized + streamed, no more 10m black-box.
- **RELEASED v0.19.3** — tag pushed → `release.yml` (darwin arm64/amd64 + GitHub Release + homebrew-tap cask + self-stamp `next`). Content: 5p + jg + at (#258/#260/#259).

## Filed (backlog)
- **6b** `terminal-guard-rejected-consistency` — the `verdict=rejected` exemption is honored in code (handlers.go:163) but not the contract prose, and `--set`/`--archive` are asymmetric (mutate.go:228). 0.19.4 status-lane.
- **1x** `code-cleanups-0193` — sprint-end Minor cleanups (release regex clobber, statecommit-phrase assertion, StandingTeammate dedup, prose-marker brittleness). Advanced to ideation then parked un-dispatched (see What's Next).
- **n3** `frontmatter-hash-quoting` — `status --set` doesn't quote YAML-ambiguous values (leading-`#`, internal `: `), so the pr-mod's `pr=#N` is comment-stripped (breaks merge-detection + the terminal guard) and colon-bearing `source:` values break GitHub's frontmatter render. Caught LIVE this session.
- **v1** `encode-deliverable-principles` — encode the 4 principles + template ergonomics into README + FO/ensign contract + status guards; the detached-audit formalization folds in here (captain decision). Advanced to ideation then parked un-dispatched.
- **3g** `live-e2e-transcript-artifact` — the CI uploads only the binary + gh truncates the log, so 38's streamed transcript is lost on a failed run. Ideation DONE (sound). Merge FIRST (prerequisite for `hd`).
- **hd** `headless-fo-drive-flake` — the FO intermittently doesn't drive the cycle headless (`-p`/teams); ideation found a candidate root cause: an undocumented single-entity-`-p`-vs-team-availability precedence contradiction. Ideation DONE (sound), investigation-first, gated on `3g`.

## Non-PR commits (workflow-only)
- `c817da04` (code repo) **merge: origin/next (0.19.2 + #237–#257) into main** — reconciled the diverged local `main`; conflicts resolved to canonical origin/next; one non-textual duplicate-symbol artifact in `parity_harness_test.go` restored to canonical.
- State-checkout: `seed:`/`dispatch:`/`advance:`/`mod-block:`/`pr:`/`archive:` churn for 5p/at/jg/37 + the 6 filings — rolled up in the Shipped/Filed sections above.
- `fix(state):` re-quoted `pr=#258/#259` and the colon-bearing `source:` of three active entities (the n3 bug's live workaround).

## Decisions
- **Sync via merge, not reset** (captain): local `main` was 8 superseded-concurrent-session commits ahead / 26 canonical behind; merged origin/next preserving both parents, resolving conflicts to canonical. (The 8 local commits were redundant drafts of work already PR'd.)
- **Focused 0.19.3** (captain pivot): from the initial "full bucket" to just the codex fix + `--plugin-dir` + orphan-status-drift; the rest deferred to 0.19.4.
- **Merge-on-proof over the live-e2e flake**: the CI-E2E live jobs are non-deterministic (one model per run flakes — the FO doesn't drive). Merged 5p/at/jg/37 on offline-green + one-model-full-cycle + validation + detached-audit, treating live-e2e as the informational net it is (it caught the real `--plugin-dir` regression). Approved CI-E2E env-gates under the conn.
- **`--plugin-dir` fix folded into 0.19.3 to green the 2 PRs** (captain): jg restored the live-e2e net + the captain workflow; merged first, then rebased #258/#259 onto it.
- **Per-stage timeouts, long timeout banned** (captain): no individual timeout > 60s; port the upstream FOStreamWatcher per-stage pattern. 38's key call: the ≤60s budget is a NO-PROGRESS (quiet) bound (resets on stream activity), reconciling "≤60s" with live stages that take minutes.
- **Stale-issue triage** (captain): verified each in shipped code, then closed `#230/#207/#189/#163` (s0), `#225/#217/#223` (3q), `#63` (dispatch build), `#158` (dup of #204), `#251` (by #259). Open issues 40 → ~24.
- **Audit-formalization folds into `v1`** (captain), not a separate entity.
- **pr-quoting bug → fast-follow 0.19.4** (captain "file it"), not folded into 0.19.3; live state worked around by quoting.
- **Follow-up ordering** (captain): `3g` (transcript artifact) merge FIRST; `hd` (FO-drive-flake investigation) gated on it.

## Issues — Workflow
- `v1` + `1x` are at `status: ideation` but were never dispatched (advanced when "start those," then parked on the 0.19.3 pivot) — they have NO ideation report. Next session must DISPATCH their ideation, not gate-approve. (Honest state quirk; documented in the handoff.)

## Issues — Spacedock
- **`status --set` doesn't quote YAML-ambiguous frontmatter values** (leading-`#`, internal `: `) → the pr-mod's `pr=#N` is silently comment-stripped (s0/#254's strip) breaking merge-detection + the terminal guard, and colon-bearing `source:` values break GitHub frontmatter render. Tracked as **n3** (0.19.4). Not separately GH-filed (tracked as the entity; captain aware).
- **CI uploads only the binary, not the transcript** → 38's streamed diagnostic transcript is lost on a failed run (compounded by gh log truncation). Tracked as **3g**.
- **Headless FO-drive flake** + the undocumented **single-entity-`-p`-vs-team-availability precedence** contradiction in the FO contract. Tracked as **hd**.
- **Stale duplicate mod**: `.spacedock-state/_mods/pr-merge.md` (version 0.12.1, old code-sha audit-link instruction) is a leftover; `f2` re-homed the canonical mod to `docs/dev/_mods/pr-merge.md` (with jx's state-sha fix) but didn't remove the old copy. Cleanup candidate.
- **Archived entities show `status: validation`** (not `done`) — the pr-mod terminalize sets `completed`/`verdict` but not `status=done`, per the contract/mod. Cosmetic audit-display quirk.

## Observations
- **The detached adversarial audit earned its keep again** — on every high-stakes cycle (5p/jg/37). Refuted nothing *material* this session, but CONFIRMED correctness independently AND caught real hardening items on 37 (a leaked child on an early-step trip; the "chatty-stuck" quiet-budget gap). Read-only refutation on a detached checkout remains non-negotiable for front-door / status / contract / CI changes.
- **The live-e2e flakiness dive corrected its own framing** — the "opposite models flaked" symmetry was partly an artifact of early stale-binary runs; the real issues were the FO-drive nondeterminism + an unreachable-12m-timeout misconfig. The investigation explicitly fixed diagnosability (37) BEFORE proposing a runtime change (hd deferred).
- **37 validated in production**: the sonnet flake on #261 failed in ~70s (vs the old 10m silent panic), streamed the transcript, and the `defer poller.kill()` killed `claude` — the fix working on a real flake. opus drove the full cycle green, proving the code.
- **The pr=#N comment-strip is a nasty silent regression** (s0's strip vs the pr-mod's `#N` convention, both shipped in 0.19.2) — caught live when jg's terminalize misfired and #258/#259 went invisible to merge-detection.
- Two ensigns correctly pushed back / self-corrected (5p ideation: "untested"→"tests wrong format" + latestVersionDir already-fixed; 38 ideation: terminalize/archive are post-exit FS, not stream events) — the gate process surfaces good adversarial honesty.

## What's Next
**0.19.4 — ordered (the live-e2e reliability chain):**
1. **3g** `live-e2e-transcript-artifact` — ideation DONE + sound (CI-only tee + upload; pipefail-exit-preservation proven offline). **APPROVE-ON-RESUME → advance to implementation → merge FIRST** (prerequisite for hd).
2. **hd** `headless-fo-drive-flake` — ideation DONE + sound (H1: the single-entity-`-p`-vs-teams precedence contradiction; investigation-first, AC-3 forbids a speculative runtime change before a captured transcript). After 3g lands + ≥3 stalls captured → advance the data-gathering + root-cause.

**0.19.4 — status-lane bucket (serialize the impls — one status worktree at a time):**
- **n3** `frontmatter-hash-quoting` (the pr/source quoting bug — high value, fixes the pr-mod silently).
- **6b** `terminal-guard-rejected-consistency`.
- **2a** `require-external-proof-guard` (the P1 code guard; coordinates with v1).

**Needs DISPATCH (not gate — no report exists):**
- **v1** `encode-deliverable-principles` (+ the audit-formalization) and **1x** `code-cleanups-0193` — at `status: ideation` but un-dispatched; dispatch their ideation.

**Parked / gated (do not dispatch without CL):**
- **5w** `notarize-macos-release` — blocked on CL's Apple Developer ID cert.
- **02** `retire-python-oracle` → **zj** `yaml-parser-migration` — post-bootstrap chain.
- **ng** `roborev-validation-hook` (CL-parked), **44** `bundle-asset-distribution` (CL-dropped), **7h** `release-notes-local-summary` (other session's lineage).

**Housekeeping:** remove the stale `.spacedock-state/_mods/pr-merge.md` 0.12.1 duplicate (canonical is `docs/dev/_mods/pr-merge.md`).
