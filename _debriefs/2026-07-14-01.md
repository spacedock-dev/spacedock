---
session-date: 2026-07-14
sequence: 1
first-commit: abf48da46af23ec2b30b4623a1cd3f389259db4f
last-commit: c747006dccd4493797d1c161c4a0ba23bc8f7fd7
duration: ~23h01m
---

# Session Debrief — 2026-07-14 #1

The 0.25 session shipped the neutral Claude candidate installation, established Roborev-first review discipline, and drove three difficult follow-up lanes through repeated adversarial review. The session also exposed first-officer hydration, approval-routing, and Codex worker-lifecycle gaps that are now captured as workflow tasks.

## Shipped

- **2h** `claude-live-installed-candidate-binary` — [#505](https://github.com/spacedock-dev/spacedock/pull/505). Installs the exact checked-out Claude live candidate outside the repository-shaped checkout. All required CI passed; remote merge `23c32369` landed; the task finalized and archived without a local merge.

## Filed (backlog)

- **3k** `durable-gate-approval-pending-blockers` — Persist approvals independently from unresolved dispatch blockers.
- **4h** `semantic-adversarial-pre-review-gate` — Add the Roborev-derived semantic adversarial pass to this workflow and the shipped development template.
- **hc** `portable-state-checkout-roborev-followup` — Repair the prematurely finalized split-root portability work.
- **d1** `claude-env-scrub-permission-mode` — Preserve Claude auto permissions when subprocess credential scrubbing is enabled; paused for the 0.25 focus.

## Non-PR commits (workflow-only)

State transitions and reports that do not belong to a shipped PR:

- `a6a3945e` corrected qwp's premature local-merge finalization. The merge never shipped.
- `4872939e`, `f0e8ddab` shaped the portable-checkout repair and trust-anchor follow-up.
- qwp review/rework records through cycles 1–10: `3a26bbc2`, `9f73859c`, `22d83252`, `85964150`, `df913799`, `b38317b7`, `c4225c27`, `7aaeb9ae`, `dcb608a3`, `48f638bb`.
- e6j Roborev rejection records through cycle 7: `57db2067`, `0f80c646`, `25c7ad65`, `7d284e13`, `8e69e4b4`, `0b19aa3c`.
- Lazy-loading design and review records: `a3bdda0e`, `724f62f5`, `d16a6b25`, `be5d6087`, `3ee6ef3c`, `f244fe1f`, `c747006d`.
- `0c4c2776` completed Roborev setup-skill ideation.
- `9030af39` revised the post-compaction authorization contract.
- `e5734cba`, `1e9dd50f` shaped and corrected the Claude scrub probe.
- `b2d0d022` archived 2h after PR #505 merged.

Routine `state:` transitions are omitted. PR-associated code commits are rolled into #505 above.

## Decisions

- Roborev must pass before downstream validation, branch publication, PR updates, or CI.
- Roborev findings are sent back unless they require genuine reframing.
- No local merges.
- 00 and c6 belong to 0.26.0, not 0.25.0.
- Issue #504 is paused while the 0.25 lanes finish.
- “Float” means use the Subspace TUI approval-gate mechanism, not invent a milestone.
- The standing comm officer was terminated temporarily to free a worker slot for 1k.
- Component-specific Roborev rules belong in `AGENTS.md`; general review behavior belongs in review guidelines.
- Implementation should perform a semantic adversarial pass before requesting review.

## Issues — Workflow

- qwp was prematurely finalized after an invalid local merge attempt. The attempt was removed and never pushed; the archived record now points to the active follow-up.
- qwp exposed successive repository-boundary, symlink, metadata, and mutation-order defects through ten Roborev cycles.
- e6j exposed repeated publication, registration-identity, generation, and writer-race defects through seven completed Roborev cycles; cycle 8 is implementing.
- 1k's first two reviews found stale eager-loading documentation and false-green behavioral oracles; cycle 3 implementation is complete locally and awaits validation.
- The initial #504 compatibility probe was invalid: it did not exercise normal boot, `/permissions` produced no durable evidence, and the secret-printing command was correctly refused.
- Roborev range jobs invoked from the main checkout display `branch: main` even when the stored `git_ref` targets an unpushed worktree head. The range is correct, but the attribution is misleading. Not filed externally.

## Issues — Spacedock

- Post-compaction sessions need deterministic contract hydration, including the 300-second Codex wait rule. Not filed externally; captured by **c6**.
- Codex lacks a real worker-shutdown and roster-reconciliation binding; completed/addressable workers can occupy the three child handles and queue unrelated work. Not filed externally; captured by **jv**.
- Standing teammate injection can consume scarce concurrency before first routed use. Not filed externally; captured by **s8**.
- A reused worker once answered the captain's status question instead of its pointer-file assignment. The completion was rejected and the exact dispatch was resent. Not filed externally.
- The Roborev setup skill led the first officer toward searching and `/tmp` configuration instead of `roborev quickstart` and the existing task draft. Not filed externally; captured by **00**.
- The Subspace float initially appeared invisible because `subspace-tui` was not on `PATH` and child stderr was hidden. Not filed externally.
- Approval persistence and dispatch blockers remain conflated. Not filed externally; captured by **3k**.

## Observations

The strongest reusable pre-review heuristic from this session is to trace changed values through every representation and lifecycle phase; exercise adjacent empty, terminal, repeated, reordered, Unicode, EOF, size, visibility, and layout variants; validate complete records atomically; inspect scaling and allocation risks; and ask how a test could pass while observable behavior is wrong.

Compaction must restore operating authority and workflow mechanics, not merely summarize task content. A captain interruption returns control; it does not complete, fail, or redispatch a worker.

Roborev's exact stored range is authoritative. Its checkout-derived branch label is not evidence that `main` or an unrelated PR is under review.

## What's Next

- **hc/qwp:** cycle-10 implementation is complete locally at `720b29b2`; verify its report and advance to exact-range Roborev-first validation. No remote branch or PR exists.
- **1k:** implementation cycle 3 is complete locally at `664d64c3`; verify its report and advance to exact-range Roborev-first validation.
- **e6j / PR #503:** implementation cycle 8 is active. Do not update the PR or engage CI until Roborev passes.
- **4h:** continue ideation for the semantic adversarial workflow/template gate.
- **00 and c6:** retain in ideation for 0.26.0.
- **d1 / #504:** remain paused.
- **2h / #505:** complete and archived.
