---
session-date: 2026-06-04
sequence: 7
first-commit: 1b95e34f
last-commit: ed705246
duration: ~5h
---

# Session Debrief — 2026-06-04 #7

The `at`→**0.19.5** wave. Started by finishing `at` (#285) and merging the backlog it had blocked, then drove the whole next-wave + a captain-initiated token-efficiency/contract-decomposition line, and **cut and shipped 0.19.5** (10 PRs since v0.19.4). Highlight: every high-stakes change went through a detached adversarial audit — each caught a real test-strength hole the green suite alone missed.

## Shipped (10 PRs → 0.19.5)
- **atw** `non-interactive-teardown-exit` — [#285](https://github.com/spacedock-dev/spacedock/pull/285). Scoped the live gate to the teardown marker grade; the gate-guardrail sonnet flake was a 60s→2m budget fix (sonnet slower than opus, not #55297).
- **n3** `frontmatter-hash-quoting` — [#275](https://github.com/spacedock-dev/spacedock/pull/275). Frontmatter writer quotes YAML-ambiguous values (leading-#, internal colon).
- **2a** `require-external-proof-guard` — [#277](https://github.com/spacedock-dev/spacedock/pull/277). Refuse to close a task whose only proof is re-reading its own write-up.
- **am** `ensigncycle-streamwatch-test-only` — [#288](https://github.com/spacedock-dev/spacedock/pull/288). Rename streamwatch.go → _test.go; drop 658 LOC of test-only code from the release binary.
- **6b** `terminal-guard-rejected-consistency` — [#289](https://github.com/spacedock-dev/spacedock/pull/289). `--set`/`--archive` agree on verdict=rejected terminalization. (Audit found an --archive over-widening coverage hole; fixed cycle 1.)
- **ep** `ensign-contract-dev-leakage` — [#290](https://github.com/spacedock-dev/spacedock/pull/290). Re-home dev discipline out of the universal ensign core + split 2a's external-proof classifier (injected vocab). (Audit found a span-list kernel-vocab hole; fixed cycle 1.)
- **zd** `extract-team-orchestration-skill` — [#291](https://github.com/spacedock-dev/spacedock/pull/291). Extract the generic team lifecycle into a reusable `using-claude-team` skill; FO runtime adapter 305→224 lines. Spike disproved `@`-include, proved `Skill()` cross-load.
- **p4** `live-verification-gate` — [#292](https://github.com/spacedock-dev/spacedock/pull/292). Gate runtime-observable ACs on a resolvable live-run citation + promote the `livescenario` primitive. (Audit found the untested ci-run 404 discriminator; fixed cycle 1.)
- **nz** `frontdoor-prompt-after-dash-guard` — [#294](https://github.com/spacedock-dev/spacedock/pull/294). Advisory warning on a stray prompt after `--` (argv unchanged). (Two feedback cycles: --plugin-dir injected-prefix misfire on the value-flag then subcommand path; fixed structurally.)
- **n1a** `live-cycle-end-state-determinism` — [#295](https://github.com/spacedock-dev/spacedock/pull/295). Absolutize entity_path (1a) + team_name-key the dispatch file (1b) to kill stale-pointer reuse; re-promote TestLiveEnsignCycle end-state assertions to hard (post-1b 5/5, #55297 0/5).

## Filed (backlog) — the 0.19.6 token-efficiency + determinism line
- **p2** `pr-complete-binary-command` — ideation-approved. The post-merge ceremony as a binary command. **Captain naming decision: `spacedock dispatch advance {slug} --to done`** (terminal case of dispatch advance, unifies with roadmap #2), NOT `dispatch complete`. Boundary: command=payload, pr-merge mod shrinks to PR-open, reconcile --act=trigger. AC-5 folds in `pr-lifecycle-from-boot` as a live test.
- **vc** `reconcile-act-deterministic-completion` — roadmap #6, the deterministic TRIGGER for p2 (Class C `--act` runs `dispatch advance --to done`); depends on p2.
- **t3** `gate-presentation-skill-extraction` — ideation-approved. Gate Presentation → lazy `present-gate` skill (captain-endorsed).
- **a9** `feedback-rejection-flow-skill-extraction` — ideation-approved. Feedback Rejection Flow → lazy skill.
- **e3z** `bare-mode-coverage-baseline` — ideation-approved. **Baseline live run: bare mode WORKS** (orphaning hypothesis disproven — the FO invokes using-claude-team unconditionally to *decide* team availability). Deliverable = `dispatch-mode {team,bare}` variant axis + a standing live bare lane. Note: single-entity `-p` reaches bare via a different bypass.
- **e30** `dispatch-path-teamname-sanitize` — n1a audit polish (validate team_name before the fs path; fix a bare-mode comment).

(Dropped mid-session: `contract-token-budget-guard` — captain judged a hard per-file token cap a blunt instrument. The umbrella `fo-shared-core-decomposition` was split into t3/a9/p2 per captain direction.)

## Non-PR commits (workflow-only)
State transitions and scaffolding that don't belong to a PR:
- Feedback-cycle entries authored by the FO for 6b, ep, nz (×2), p4, n1a — each routing a detached-audit or validation finding back to implementation.
- p2 ideation cycles 2 + 3 (boundary resolution; deterministic-invocation resolution) and the `dispatch advance --to done` naming decision committed to p2 + vc.
- The 6 filed-entity seeds (and the 2 dropped/split).
- Debrief #07 (this file). All other session commits roll up in the 10 shipped PRs above.

## Decisions
- **Live-verification A/C call:** ep/zd/p4 each have a runtime-observable "live drive on the swept contract" AC that can't run pre-merge (skills register at boot; swept contract is only live post-merge). Captain-approved: ship on static + adversarial-audit evidence, close the live AC via a post-merge fresh session / p4's primitive. Both A (post-merge confirm) and C (fold into p4) ship now; only B (contrived pre-merge drive) would delay.
- **Token-efficiency = two levers** (captain-initiated): (1) binary-command migration (ceremony→binary, e.g. p2), (2) contract decomposition into lazy spacedock-owned skills (judgment→skill, e.g. zd/t3/a9). The superpowers comparison quantified it: `first-officer-shared-core.md` (~9,730 tok) is ~6× the median superpowers skill and loads eagerly every boot. Roadmap refreshed (`docs/dev/_proposals/binary-simplification-roadmap.md`).
- **`dispatch advance {slug} --to done`** (not `dispatch complete`): the command drives the entity to its end state; the pr-merge mod can't deterministically *inject* it (prose), so the deterministic trigger is `reconcile --act` (#6), not the mod.
- **Bare mode is NOT broken** by the zd decomposition (live baseline disproved the hypothesis) — but has no standing live coverage (e3z filed).
- **n1a 1b = team_name keying** (option B) over content-hash (non-deterministic tempdir broke goldens) or defer (re-promotion determinism depends on it).
- **0.19.5 release notes** captain-approved (tighter/user-facing); tag cut single-`-m` to match the v0.19.4 clean `cat-file` extraction precedent.

## Issues — Workflow
- **Ensign stage-report path divergence** ("two entity files") — root-caused to `dispatch build` echoing a relative entity_path verbatim while absolutizing workflow_dir. Fixed by n1a 1a.
- **Dispatch-file collision** — `/tmp/spacedock-dispatch/{worker_key}-{slug}-{stage}.md` is a global that collides across concurrent FOs / back-to-back runs (stale-pointer read race). Fixed by n1a 1b (team_name keying). Residual polish: team_name unvalidated into the path (e30).

## Issues — Spacedock
- **Release blocked by an unsatisfiable gate** (from #284 cost-ledger): `release.yml`'s "Download latest journey metrics artifacts" hard-requires a *successful Runtime Live E2E run on `next`*, but that workflow only runs on PRs + workflow_dispatch — never auto on `next`. First release since #284 → goreleaser failed. **Unblocked manually:** workflow_dispatch'd live-e2e on `next` (approved 3 envs), then re-ran the release. **Needs a real fix** (0.19.6, coordinate w/ Codex peer): auto-run live-e2e on `next` push, or have the release trigger it, or make the metrics step non-fatal. Not filed yet.
- **Tag-body extraction inconsistency:** `AnnotatedTagArgs` (the release tool) uses two `-m` for `%(contents:body)`, but `release.yml` extracts via `git cat-file tag | awk` (after first blank), which *includes* the "Release X" subject. v0.19.4 was cut single-`-m` to avoid it. The tool and CI disagree. Worth reconciling. Not filed.

## Observations
- **Detached adversarial audits earned their cost every time.** 6b (--archive over-widening), ep (kernel-vocab span-list), p4 (untested 404 discriminator), nz (--plugin-dir misfires), n1a (golden-regen + collision probes) — each high-stakes audit found a real test-strength hole that the validator's green suite missed. The pattern (validate → detached refute → route material findings back) is paying off.
- **The live-verification cluster is real and mostly built:** scenario spec (foundation) → p4 (citation gate + primitive) → 4n (cost ledger, peer) → n1a (determinism). What's missing: populate the 5 roadmap scenarios + CI-wire the live primitive (currently the `livescenario` live test runs once, not in CI).
- **Captain steering shaped the session heavily** — the token-efficiency line, the bare-mode probe, the dispatch-advance naming, the A/C live-AC call were all captain-initiated mid-flight; the FO's job was to turn each into filed/dispatched work.

## What's Next
**0.19.6 line (filed, parked — all serialize on first-officer-shared-core except p2's mod work):**
- Contract decomposition: **t3** (present-gate skill), **a9** (feedback-rejection skill), **p2** (`dispatch advance --to done` + the Merge-and-Cleanup collapse) → then **vc** (#6 reconcile --act).
- **e3z** bare-mode standing live lane + variant model; **e30** team_name sanitize polish.
- **Release-gate fix** (the live-e2e-on-next blocker) — file + coordinate with the Codex peer; do this BEFORE the next release or every cut needs the manual workflow_dispatch dance.
- The **5 scenario-roadmap scenarios** (`docs/specs/scenario-testing-principles.md`): pr-lifecycle-from-boot (folded into p2), dispatch-before-wait, split-root-bootstrap-resume, live-metrics-artifact-capture, feedback-reuse-boundary.
**Codex peer line (coordinate, don't author):** pi-runtime (#293 shipped; z6 pi-stage-dispatch-uses-build-artifact in flight), 4n cost-ledger (#284 shipped).
**Backlog:** 44 bundle-asset-distribution, 5w notarize-macos-release (still relevant — cask install hits Gatekeeper), nz/ng/7q/fc/zd-follow-ons.
