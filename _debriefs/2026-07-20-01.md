---
session-date: 2026-07-20
sequence: 1
first-commit: 2148c80c77fb22827c7d683daf9af40d067f1469
last-commit: 8d9924b4829a2f4a5c64c54689154323a520208e
duration: ~14h
---

# Session Debrief — 2026-07-20 #1

This follow-up closes omissions from the previous debrief and records the 0.25.1 release, c6's final architecture, RoboRev changes, 3k/Subspace dogfood, and 0.26 planning.

## Shipped

- **zbx** `auto-load-adjacent-spacedock-plugin` — [#524](https://github.com/spacedock-dev/spacedock/pull/524). Local launchers select their adjacent Spacedock checkout while additional plugins remain separate. Carry-forward omitted from the previous debrief.
- **rt8** `codex-fresh-dispatch-context-isolation` — [#532](https://github.com/spacedock-dev/spacedock/pull/532). Codex dispatch payloads explicitly use `fork_turns: "none"`.
- **c6** `codex-post-compaction-contract-reload` — [#531](https://github.com/spacedock-dev/spacedock/pull/531). Ships host-neutral compaction-continuity rules and a Codex `PostCompact` captain-visible warning.
- **Release** — [v0.25.1](https://github.com/spacedock-dev/spacedock/releases/tag/v0.25.1) cut from `main`, including #524, #532, #526, and reviewer-reuse fixes.

## Filed (backlog)

- **krd** `subspace-gate-attempt-presentation-integration` — separates Subspace transport/controller behavior from 3k's durable gate-state design.
- **268** `fix-pi-live-lane-pi-subagents-version-skew` — restore deterministic pi-live green.
- **q4** `retire-legacy-teamcreate-and-back-channel-naming` — remove the dead TeamCreate path and rename inter-agent communication.
- **02av** `ensign-finding-triage-disposition` — let workers decline correct but disproportionate findings against declared stakes.
- **z7** `falsifiability-ladder` — prefer the cheapest check that can genuinely fail.
- **js6** `stakes-declaration-read-through` — investigated and parked after its consumers appeared already served.
- **2ae** `template-rigor-propagation` — propagate accumulated proof and proportionality guidance through commission/refit.
- **ve** `feedback-design-reset-routing` — filed, then absorbed into `feedback-cycle-record-command`.

## Non-PR commits (workflow-only)

- `2a29046c` released Spacedock 0.25.1.
- `530af7bb`, `a274f75e`, `8d9924b4` shaped and re-locked the 0.26 proportionality sprint.
- `7e639926` added the tracked local `.roborev.toml` two-member `branch_final` panel and strengthened validation/test-proof classification. These local-main commits were not yet on `origin/main` at the confirmed boundary.

All c6 and rt8 implementation commits are rolled up into their PRs above.

## Decisions

- c6's post-compaction event is harness-owned, not an FO-invokable capability. The hook warns the captain; a captain cue triggers the FO's authoritative reread and reconciliation.
- c6's synthetic and prose-grep tests were removed or narrowed. The final inertness test proves only the shipped script's no-write behavior. The prompt-surface baseline was raised to the exact approved value, `122634`.
- c6 was accepted without another synthetic continuation harness; the host behavior is better evaluated through live use.
- rt8 was fast-tracked as a small 0.25.1 correctness release with a correspondingly small adapter test.
- The 0.25.1 Pi gate was waived after the same external `pi-subagents` compatibility failure reproduced independently of the release diff. The underlying skew received local task 268.
- RoboRev remains a pilot, not formally adopted workflow policy. The panel now combines Codex correctness and Claude product lenses, with Codex synthesis.
- A probe is one instance-specific question tied to a briefing. Probe history/results belong to the briefing provider; Spacedock owns only the durable gate/attempt/Briefing/Resolution/application binding.
- Subspace presentation/controller integration belongs in krd, outside 3k.

## Issues — Workflow

- No tasks are currently dispatchable; the useful next actions are gate decisions and incomplete-review recovery.
- PR #522 remains open without a product-value decision. The current recommendation was to close it unless reading `dispatch build --help` is shown to be part of the real FO journey.

## Issues — Spacedock

- 3k Briefing 7 ended with a nonterminal `open` result and created no review log or Resolution. Presentation is incomplete. Tracked locally by krd; no GitHub issue filed.
- Although krd was filed, 3k's current AC-7 still contains `followup_task` and blocking Subspace transport mechanics. The latest requested split has not been fully folded into the specification.
- c6's archived experimental gate frontmatter still says its application is `pending` with an active execution hold even though the task is terminal. This is useful evidence for 3k's eventual reducer/application lifecycle.
- Subspace dogfood repeatedly exposed empty floats, controller EOF/open behavior, and missing ProbeResult/delta rendering. Tracked locally by krd; no GitHub issue filed.
- Binary/skill `dispatch build` form drift remains tracked locally by `jha`.
- The long pinned launcher/`$SD_DEV` ergonomics discussion produced no durable task.
- RoboRev experiment evidence from jobs 41, 94, 97, 215, and 278 has not yet been folded into task 00.

## Observations

- RoboRev's test-proof gate now explicitly asks what production mutation must make each test fail. This is materially stronger than merely prohibiting "tautological tests."
- The product lens remains necessary: correctness review can validate a mechanism without questioning whether an agent encounters or needs it.
- 3k dogfood clarified the durable abstraction, but transport friction obscured the review. The separation into durable gate state versus provider-owned presentation should remain strict.
- One FO should own workflow mutations and dispatch. A second harness/model contributes a read-only branch review or lens; it should not independently advance the same task.
- Installing c6 after this session's compaction cannot test the hook retroactively. Plugin hooks must be loaded when a new Codex session starts.

## What's Next

1. Start a fresh Codex session with the now-installed c6 snapshot, run `/compact`, verify exactly one warning, cue the FO, and confirm contract/state/roster reconciliation precedes any workflow effect. Restore `spacedock-edge` afterward.
2. Revise 3k to remove the remaining Subspace transport mechanism, retain only its durable seam, produce a new briefing revision, and present the semantic probe delta separately.
3. Drive krd as the independent Subspace integration task.
4. Decide whether to close PR #522.
5. Fold the RoboRev pilot evidence into task 00 and decide formal adoption.
6. Review the Codex follow-ups: `bc` at validation, `6cc3` at ideation, worker reuse, and worker shutdown/roster reconciliation.
7. Resolve Pi version skew task 268.
8. Continue the 0.26 proportionality sprint's pending ideation gates.
