---
session-date: 2026-06-05
sequence: 3
first-commit: c05f2c11
last-commit: 9ae182da
duration: ~5h
---

# Session Debrief — 2026-06-05 #3 — Two ships, the gq reuse+timeout saga, and an API-overload reckoning

Terminalized the two CI-approved PRs from #02 (wm #303, hwk #306); drove gq (feedback-nonhappy-live-coverage) through a long multi-cycle reuse-assertion + timeout-discipline saga that is Codex-live-confirmed but Claude-live-blocked by an ongoing Claude API overload; advanced the Codex peer's z8 #305 on captain direction; and filed the 0.19.6 closeout slate (release-gate fix, main-flip 0.20.0, binary/version UX, orient discovery) plus re-scoped the hwk follow-up to net-removal.

## Shipped
- **wm** `fo-auto-continues-after-stage-completion` — [#303](https://github.com/spacedock-dev/spacedock/pull/303). The FO must auto-continue after a completed non-gated stage (behavioral-only proof; detached audit done in #02). Squash-merged + terminalized this session.
- **hwk** `tautological-test-remediation` — [#306](https://github.com/spacedock-dev/spacedock/pull/306). Remediate 56 tautological instruction-file tests (demote-not-delete + re-bind) + a standing AC-3 proof-policy sweep; the match axis ("ingest⇒declare") closed and the reader axis honestly scoped (forked). 3 validations + 2 detached audits; merged on green after a sonnet flake re-run.
- **z8** `pi-live-ci-runtime-scenarios` — [#305](https://github.com/spacedock-dev/spacedock/pull/305) (Codex peer's pi-line). Terminalized + archived on captain direction ("advance z8").

## Filed (backlog)
- **bqqr** `release-gate-job-separation-fix` — the 0.19.6/0.20.0 cut blocker: `release.yml` hard-requires a Runtime-Live-E2E on the branch that never auto-runs; fix = job-separation (sibling journey-ledger job goreleaser doesn't `needs:`) + tag-body OPTION B. Coordinate with the Codex peer (4n owns the journey-ledger consumer). Hard prerequisite for the main-flip.
- **pj6k** `main-flip-0200-marketplace` — the capstone: once all-green, cut 0.20.0 on main + flip the marketplace (next→origin/main). Captain-clarified: 0.20.0 = first actual release, NO contract change; bundling deferred.
- **qak5** `spacedock-binary-missing-install-journey` — binary/version onboarding UX: absent → install hint + `spacedock claude` launch journey; present-but-incompatible → show skill+binary VERSIONS (not "contract" jargon) per captain directive.
- **19fh** `orient-workflow-discovery` — bring the prototype orient skill's agent-history workflow discovery (workstreams + OPEN decisions + interruption stats, from agentsview) into spacedock as the front-door to commission.
- **4q** `sweep-guard-reader-axis-invert` — RE-SCOPED to NET-REMOVAL (captain: "net removal, not more crap to mark crap or detect crap"): remove the heavy reader-axis detection machinery, keep the match-axis core, rely on the detached audit as the reader-axis backstop.
- **n2** `unify-stall-watchdog-streamwatcher` — filed then CLOSED same session (verdict=rejected/superseded — gq unified the two stall mechanisms onto the existing streamWatcher; the 60s budget held on a clean opus drive).

## Non-PR commits (workflow-only)
State-checkout commits this session were the routine dispatch/advance/feedback churn for gq + hwk plus the filings above; the two ships are captured by their PR links. Code-side, the Codex peer's pi-line also landed #298 (fc) + #299 (d2) + #304 (qj) on `next` (peer-owned, coordinate-not-author).

## Decisions
- **hwk Option C (captain):** ship the match-axis closure + 56 demotions now with the guard honestly scoped; fork the reader-axis robustness — rather than keep hardening an enumeration-based guard the audit kept defeating. Then the **net-removal directive**: the follow-up (4q) must NET-REMOVE the reader-axis machinery, not add more detection code.
- **Basket-timeout ban (captain):** long basket/binary timeouts are banned; per-stage timeouts ≤60s unless a captain-signed "very good reason." Drove gq's per-stage stall-watchdog (kill on 60s of stream silence), replacing the 40m basket; the 40m became a documented loose CLI backstop behind the watchdog.
- **gq unify at 60s, not 120s — then the overload reckoning:** the FO oscillated 60s↔120s three times on an offline stream-gap metric; the worker held the empirical-framing line. A clean opus drive + the Codex run held at 60s, so the clean DRY unify-at-60s shipped. The authoritative opus re-drive then false-killed at 60s — but the captain correctly suspected API overload, and the stream confirmed it (529 + 500×2 + 19 retries). So 60s is NOT proven too-tight; the false-kill was an overload artifact. CARRIED.
- **z8 advancement (captain-directed):** terminalize the peer's merged-but-un-advanced z8 #305.
- **main-flip clarifications (captain):** flip = next→origin/main (main as release branch + marketplace source); "tested" = all green; 0.20.0 = first actual release, no contract change; bundling deferred; and the CLI must not say "contract" to users — show skill + binary versions on incompatibility.

## Issues — Workflow
- **qj `pi-runtime-readiness-intercom-prereqs` #304 merged but may be un-advanced** — flagged to the captain/peer; not terminalized by this FO (peer-owned pi-line). Next session / the peer should advance it.

## Issues — Spacedock
- **Claude API overload breaks the live lanes (529 / 500 / retries).** The opus rejection-flow live re-drive false-killed because the no-progress stall-watchdog cannot distinguish an API-overload backoff stall (>60s of no stream output during SDK retry) from a genuine hang. Two faces: (a) live tests are flaky under Claude API overload; (b) the watchdog needs a policy for overload backoff (raise budget / detect 529+retry and pause / accept re-run-on-overload). Not filed (needs a clean-baseline decision first).
- **Async SendMessage crossover thrash.** The FO's successive 60s↔120s messages crossed with the worker's commits, causing the worker to revert/re-revert (ping-pong). Eventually fixed by FREEZE + isolated measurement in a detached worktree. A coordination hazard when an FO changes its mind faster than message round-trips settle.

## Observations
- **The detached adversarial audit earned its keep, again and repeatedly** — hwk cycle-2 (the match-axis allow-list), hwk cycle-3 (M-A/B/C reader-axis evasions), gq (M1+M2: the reuse assertions false-passed a fresh-dispatch — the exact violation they guard). Each time it caught what the validation's own green structurally missed. The thesis holds: a self-check tests the shapes you thought of; the independent refuter finds the rest.
- **Measure, don't assume — twice over.** The FO's 60s-too-tight conclusion rested on an offline stream-gap "overestimate" theory AND a single lucky clean run; the captain's "check for API overload" catch exposed the false-kill as an infra artifact. The watchdog firing was real data, but the CAUSE needed checking before concluding. Don't draw a budget conclusion from an overload-contaminated sample.
- **FO indecision is expensive.** The 60s↔120s oscillation cost cycles and thrashed the worker. The fix that worked: FREEZE the worker, measure the contested option in isolation (a detached checkout), then issue ONE final instruction. Adopt that pattern earlier next time.
- **Standing-guard machinery grows heavy under adversarial hardening.** hwk shipped +2412 lines (≈63% the standing sweep); the captain's "net removal not more crap to detect crap" is the corrective — prefer the detached audit as the backstop over an ever-growing static detector.

## What's Next
**gq (in flight, the priority):** the reuse fix + timeout unify is Codex-live-confirmed + offline-sound (1166/15 green, HEAD f21a126b, unpushed). Owed: a Claude-live re-drive of opus `rejection-flow` on a CLEAN (non-overloaded) API to confirm the strengthened `assertClaudeReviewerReuse` on a genuine reuse; decide the watchdog-vs-overload policy against that clean baseline (do NOT raise 60s on the overload sample); then push f21a126b's successor to #302 + a detached re-audit of the strengthened assertion → merge. Rotate the benchmark-token; check 529/overload before trusting any opus failure.

**0.19.6 closeout slate:** bqqr (release-gate fix, coordinate with the Codex peer 4n) → pj6k (main-flip 0.20.0) once all-green. 4q (net-removal), qak5 (binary/version UX), 19fh (orient discovery).

**Carried from #02 / backlog:** ev3e (fo-halt/sync/journey live drives), p2 (pr-complete binary command) + vc, bw (feedback-cycle-record), j90 (lazy-teamcreate boot), e3z (bare-mode baseline), eykb (proof-policy to scaffolding), qy (root go-test hygiene), 5w/5p/s0cq (release/marketplace), 7q/cq/ng + backlog.

**Peer coordination:** the Codex peer owns the pi-line (z6 #300 still OPEN; qj #304 merged-but-maybe-un-advanced; cq). Coordinate the release-gate journey-ledger consumer (4n).
