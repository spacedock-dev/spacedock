# Debrief: 0260 shaping session (2026-07-20, pre-compaction capture at ~65% context)

FO session record. Durable state of authority: the sprint index (docs/roadmap/0260-proportionality/index.md), the state checkout entities and their gates frontmatter, _evidence/0260-agent-derail-forensics/. This file captures what lived only in conversation.

## Captain rulings issued via float annotations (fleet-wide, binding)

1. Every gated ideation declares a written expected surface (files/LOC or the workflow's unit) plus tolerance; later stages judge deviation against it.
2. No minted terminology anywhere — identifiers, overloaded system vocabulary, coined compound abstractions, and metaphor names (the ordering rule's own former name was the fifth catch). Plain words; system ids only where the notation binds them.
3. Building a new check or enforcement process requires EXPLICIT captain approval and normally its own entity.
4. Prose-greps over instruction files: legal as one-off validation-time evidence pasted into a report; illegal as committed tests.

## Session operating directives (persist to successor sessions)

Assume the sprint members' intended behavior directly in FO conduct (notation, estimates, no minting, cheapest-check ordering, triage posture). Present gates via the Subspace float with the design as artifact; record resolutions in gates frontmatter. Both also recorded in the sprint index Constraints.

## Gate state (also durable in each entity's gates frontmatter)

- 85, ht: APPROVED, advance-to-implementation pending (Commander applies). Provider results + briefings retained in each entity's review/ideation/ room. Workers released.
- z7 (retitled "Prefer the cheapest check that can fail"): second revise in progress — five wording annotations (drop the jargon term; bullet the ordering; last-resort build needs explicit approval + own entity + less dev-specific wording; fix the inline file reference; carry the prose-grep evidence-vs-test nuance).
- bw: revise in progress — command-existence question (presumption: entry-format CONVENTION first, command deferred), landing-spot review AC, roborev-shape AC, prose-grep AC reframing, plus AC-6 (per-round AC-digest drift detection) recommended-include.
- Lane plan: after bw's gate → dispatch az; after z7's gate → dispatch 02av then 841; template member (2ae) last on settled wording; 0qe gate judged on banked ideation.

## Subspace float wiring (WIP skill at /tmp/subspace-present-float-skill) — dogfood findings

1. Version pin was stale vs installed 0.9.0 (fixed by captain grant: EXPECTED_VERSION bump).
2. Override path cannot accept a release newer than the pin.
3. Result-validation failure path deletes the private result area instead of retaining it; the leave-open/hold decision's result shape is the untested branch that failed validation.
4. One transient "Zellij did not return exactly one terminal pane ID" zero-spawn failure; error names neither query nor output; succeeded on retry with identical state.
5. Single-file wrapper forces gate-summary+entity concatenation; multi-artifact briefing packaging is designed (3k) but absent from subspace-tui 0.9.0's surface. Captain flagged packaging should be separate artifacts.
Also 3k adapter datum: provider resolution binds its own envelope briefing id; workflow recording normalizes to the attempt briefing id after digest validation — the id-mapping question is real and belongs to 3k's implementation.

## Open items

- CLAUDE.md audit: complete at /tmp/claude-md-audit-report.md (EPHEMERAL — captain to save), 20 items; items 1/20 apply in their interim forms since the stakes member is parked; auditor agent still alive pending release.
- Main repo: several unpushed commits (index re-locks, remedy commit); captain decides push.
- Captain's own "3k · ideation · approve?" subspace pane may still be open.
- Post-compaction FO boot: reload spacedock:first-officer skill (+ dispatch cores at first dispatch), then this file + the sprint index restore session context.

## Live roster at capture

z7 worker (revising), bw worker (revising), comm-officer (standing, idle), claude-md-auditor (idle, unreleased).

## Update at compaction test (2026-07-20 ~06:20, captain will manually trigger compaction)

Gate wave complete except one. APPROVED with pending advances (Commander applies): 85, ht, bw (prose-only round convention), z7 (check-ordering, budget re-baselined +1055, lure catalog with 5 scenarios), az (no-detector decision; Edit D audit-widening still awaits its own explicit captain yes/no), 841 (contractlint runtime retirements). 0qe merged into z7's lure catalog as scenario five (reviewer-side trap) and archived. IN FLIGHT: 02av placement rework — the triage rule text is approved-shaped, delivery moves trigger-scoped (feedback-context path + stage-def bullet; always-loaded core gets at most a pointer). NEXT after 02av: dispatch 2ae (template propagation, the last ideation) on settled wording; then sprint-wide review completion check + Commander package.

Float/presentation procedure (learned the hard way, findings 6-11): the WIP wrapper at /tmp/subspace-present-float-skill is superseded — the native path is `subspace-tui present --transport zellij -- --review-v1 --actor person:reviewer --approver agent:first-officer <briefing.json>`, with the briefing package (briefing.json + gate-summary artifact + FROZEN entity snapshot artifact — an entity cannot self-bind, record the digest of a snapshot copy) living inside the entity folder at review/<stage>/briefing-N/. --review-v1 is undocumented in usage but real. Old subspace skills recovered from spacedock-subspace git history (ce8fa0d^ and 617d3af^: using-subspace-tui, review-by-subspace-tui) document present's transports, Ctrl+O leave-open with retained package paths, and --resume. Floats paused this session after captain comprehension failures — gate summaries must be plain-language, orientation-first; chat presentation with three-sentence summaries worked. Captain rulings all recorded in sprint index Constraints.

Float finding 12 (post-compaction, 2ae gate): `spacedock-subspace/scripts/subspace-tui-float` + PATH subspace-tui 0.9.0 + zellij transport, single-file advisory mode, failed at launch with `present-child protocol ended early: EOF` (exit 1, no recovery path emitted). Reproduces the blank-float defect; the `.agents/skills/subspace-r-working-copy` skill names `scripts/review-local-zellij` (stderr kept attached to the float PTY) as the repair, but that launcher is absent from the tree and the local build reports version `dev`, not the expected 0.9.0. Float artifact retained at `template-rigor-propagation/review/ideation/briefing-1/gate-summary.md` (sha256 4760be51f28b83d92b5671119ab26916113c4b470d44603377a2a88cc2800448); gate fell back to chat.

Float finding 13 (RESOLUTION of the blank-float defect): the working float path is `spacedock-subspace/.agents/skills/subspace-r-working-copy` — its `scripts/review-local-zellij` (in the SKILL's own scripts dir, not the repo's) runs the TUI DIRECTLY in a blocking Zellij float (no present-child protocol, stderr on the pane PTY, stdout captured to a private result file). Requirements: `SUBSPACE_TUI_BIN` = exact-tip local build (`go build -o /tmp/subspace-tui-tip ./cmd/subspace-tui`; source default version is `dev`, so pass `SUBSPACE_TUI_EXPECT_VERSION=dev`), live `ZELLIJ_SESSION_NAME`/`ZELLIJ_PANE_ID`, jq. Proven twice (probe + 2ae gate, both valid results, digest-bound). Ritual: probe with a throwaway .md FIRST — the mechanism check costs seconds; skipping it cost one dead one-shot gate float (finding 12).

## Shaping close (2026-07-20 ~10:35)

Preflight staff review COMPLETE as a two-seat panel: codex seat (staff-review.md, committed 7a186a01) + fable seat (staff-review-fable-delta.md, 5b5e8945; produced by six banked lens agents + one merge agent after the captain stopped a 110-agent verification fan-out — that incident is now z7's lure scenario six and the authoring-time amendment's fixture). All material folds captain-approved and FO-applied under an edit-directly grant; independent closure pass verified CLOSED after two rounds (residues: the uncommitted codex seat; the advisory-digest convention — chat-recorded gate digests hash an uncommitted working state, so drift checking diffs the body at the attempt's recording commit, never re-hashes; now stated in all four amended attempt notes and the Commander package drift check). Gate states: 85/bw/z7/2ae carry superseding fold attempts (latest pending advance each); az carries the Edit D approved resolution record; ht/02av/841 unchanged. Two next-train tasks filed: roborev-sprint-review-panel (7wv), ac2-reanchor-live-scenario-repair (1az). Sprint index lifecycle: all Shape boxes checked. Pending on the captain: push main (12+ ahead), the five untracked scratch paths (item 6 of the closure report), saving /tmp/claude-md-audit-report.md, releasing the comm-officer + claude-md-auditor standing agents. The Commander package (dispatch-sprint-execution.md) is the cold-boot entry.

Live roster at compaction: 02av ideation worker (mid-rework), comm-officer (standing, idle — one undelivered polish request outstanding), claude-md-auditor (idle, unreleased; report at /tmp/claude-md-audit-report.md — EPHEMERAL, captain still to save). Unpushed main commits remain (captain decides push). POST-COMPACTION FIRST ACTIONS: reload spacedock:first-officer skill; read this debrief + docs/roadmap/0260-proportionality/index.md; do not re-ask settled gates (they are in each entity's gates frontmatter); await 02av's Done.

Float finding numbering note (2026-07-21): finding 14 was skipped in the live session — the sequence runs 12, 13, 15. Retained as-recorded to avoid breaking references (3k's attempt-7 resolution cites finding 15); the gap is deliberate. Finding 15 = the captain's gate approval destroyed by a dead launcher's unlinked scratch dir (orphaned-pane incident); the interim fix is the room-resident result path in the package launcher.

Conduct finding (2026-07-21, captain catch): sixth minting recurrence — the preflight seat's letter-coded finding scheme was ADOPTED and propagated by the FO into worker instructions, entity state, and the committed review summary, despite the standing bare-ordinal ruling and despite the identical shape being the FIRST recorded live example in the check-ordering task's evidence list. Swept to class + bare ordinal (three entities + the review doc); the FO's own commit messages retain the codes as immutable history. Wording input for the check-ordering clause at its validation: the no-minting rule must bind CONSUMPTION of upstream minted schemes (a reviewer's, a tool's), not only the FO's authoring — adoption is how every recorded propagation actually happened.

Conduct finding (2026-07-21, captain catch): the shaping FO fired a pre-cut audit into the actively-driven 0260 sprint on an UNVERIFIED handoff inference — the archived member states plus the Commander debrief's "Remaining lifecycle" section were read as drive-complete, without checking whether the Commander session was live (it was: running the lure drives itself, with a codex pre-cut already complete). Same shape as the Commander's own conduct finding 8: an outcome reported/assumed without verification — and a violation of the workflow README's cross-session ownership rule ("other FO sessions stay out of an actively driven sprint's members"). Mitigation: the in-flight auditor was narrowed within minutes to read-only static checks with the lure drives skipped, reporting as a delta against the codex seat. Contract input: the ownership rule's graduation trigger (a machine-readable sprint owner, tracked by xp) is the mechanical fix; until it lands, "is the drive-owner session live" is a question the FO must ASK the captain, not infer from state.

Conduct rule (2026-07-22, from the recorder's undefined-subcommand gap — binds this FO's gate presentations from now on): the gate AC cross-check gains a standing look — any expected surface naming `cmd/` work or a new CLI verb demands the concrete interface doc diff (usage line, flags, exit codes; the stage-def's existing command-surface rule) BEFORE the gate presents; "N verbs, ~LOC" is an estimate token, not a design. Root cause recorded honestly: the rule existed in the dev README's ideation stage-def; nine gate presentations ran without applying it because no AC claimed the command surface, so the cross-check never triggered — absence generates no contradiction, which is also why both preflight seats missed it. The preflight's new implementability lens (docs/roadmap/README.md) is the sprint-level guard for the same class.
