---
session-date: 2026-06-30
sequence: 1
first-commit: 5ecf93f9
last-commit: ba18936d
duration: multi-day (CI/codex-flake waits spanned 2026-06-22 → 06-30)
---

# Session Debrief — 2026-06-30 #1

Drove the 0230 stable-finalization sprint's remaining members to merge and took v0.23.0 to the edge of the stable tag: M4 (release machinery), v9 (launcher invariant), and a newly-discovered contract-version bump all landed; pre.4 was cut + shaken out; the value gate was waived for one regression; a pre-tag spot-audit of two parallel-merged members surfaced two benign majors (now filed). The tag itself is deferred to a handoff — ship the race fix, then cut.

## Shipped
- **7y** `releasing-doc-pre-stamp-drift` (M4) — [#432](https://github.com/spacedock-dev/spacedock/pull/432). Reconcile docs/releasing.md to the live e2e-gate machinery + CI-enforced manifest-tag-gate + stable→tagged-SHA advance (cycle-3, rebased over fe's same-file edits).
- **v9** `fo-binary-launcher-invariant` — [#433](https://github.com/spacedock-dev/spacedock/pull/433). Pin one resolved launcher per session + a contractlint lint banning bare-`spacedock` helper invocations (cycle-2; the detached audit caught a real line-132 escape + a lint hole, both closed).
- **7h3** `contract-version-bump-v2` — [#443](https://github.com/spacedock-dev/spacedock/pull/443). Bump the skill↔binary contract version 1→2 so a v0.23.0 plugin cleanly rejects an out-of-date binary (surfaced mid-cut by the captain's skew question).
- **fe** `codex-edge-channel-marketplace-source` — [#431](https://github.com/spacedock-dev/spacedock/pull/431). Per-channel marketplace source (edge→marketplace@edge); terminalized at boot (merged by a parallel owner just before this session).
- Parallel (other sessions): **bm** [#441](https://github.com/spacedock-dev/spacedock/pull/441) gate-on-end-value (FO contract), **vv** [#442](https://github.com/spacedock-dev/spacedock/pull/442) launcher-resident (front-door spawn-vs-exec) — both shipped by a "repaired Haiku session"; spot-audited pre-tag (see Issues).

## Filed (backlog)
- **3p** `launcher-signal-forward-race-fix` — fix the #442 launcher data race (forwardHostSignals goroutine reads `cmd.Process` before `cmd.Start()` writes it); sprint 0230, **GATES the v0.23.0 cut**.
- **kr7** `minor-version-compat-coupling` — replace the hand-maintained contract integer with minor-version coupling (self-maintaining skill↔binary compat); follow-up that would have prevented the 7h3 skew.
- **w07** `ac2-reanchor-scenario-falsifiable` — strengthen #441's AC-2 re-anchor live scenario (currently non-falsifiable / steerable-by-narration).

## Non-PR commits (workflow-only)
Entity state transitions + the cut artifacts (incl. this debrief) live on the `.spacedock-state` branch; shipped code is rolled up in the PRs above. **`v0.23.0-pre.4`** was tagged + published from `6ea56f95` as a shake-out (no PR) and validated end-to-end.

## Decisions
- Captain approved v9 ideation, M4 cycle-3 route, fe validation; then gave the FO **the conn toward the cut** (auto-approve gates/merges/ci-env).
- **Cut pre.4** (`v0.23.0-pre.4`) from `6ea56f95` to shake out M4+v9 on real published artifacts — validated M4's release machinery end-to-end (e2e-gate → goreleaser → cask) + the published binary/edge-cask.
- **Skew fix:** the captain's question ("what happens to a 0.22 binary + upgraded 0.23 skills?") exposed that the contract version stayed dead at `1` across 0.20/0.22/0.23 despite the skills gaining hard verb dependencies (`state commit/ready/sweep`, `merge guard`) → a 0.22 binary boots clean then breaks cryptically. Chose the **integer bump (7h3)** for this cut; filed **minor-coupling (kr7)** as the durable follow-up.
- **Value gate WAIVED for the tag:** #441 grew `first-officer-shared-core` +595 B over the v0.22.0 baseline (29181/28586). Captain chose to **include #441/#442 + skip the clawback** ("get it back in z4 and others") rather than block or revert.
- After the spot-audit (0 ship-blockers, 2 benign majors), captain chose to **file the race fix + hand off** the ship-then-cut rather than fix in-session.

## Issues — Workflow
- **Value-gate violation:** #441 pushed shared-core 595 B over baseline; waived for this tag, clawback deferred (kr7/z4 + others). Release notes corrected to drop the now-false "every FO file at/under baseline" claim.
- **Local-main divergence:** parallel sessions committed 2 debriefs (`e1b27014`, `0031f770`) directly to the shared local `main`, diverging it from origin. The cut runs from `origin/main`; those debriefs await their owner's rebase+push. Lesson: parallel sessions should NOT commit debriefs to the shared local main during a cut (this debrief goes to the state branch by design).
- **Provenance:** #441/#442 came from a "repaired Haiku session" after a "broken FO/worktree isolation, results invalid" session — weighted heavily in the spot-audit.

## Issues — Spacedock
- **Codex live-lane flakiness:** `TestLiveCodexSharedScenarios/rejection-flow` foreground-wait liveness stall is a reduced-rate flake — the v9 PR + the pre.4 e2e each needed re-runs (pre.4 took 3 codex attempts). Re-run-on-red works but is slow + costly. Candidates: `3g8` live-e2e-node-runtime-bump + the codex-keepalive hardening.
- **/tmp reaped mid-session:** the multi-day span (CI/codex waits, 06-22→06-30) let the OS reap `/tmp` scratch (helper scripts, the v0.22.0 verification binary) — recreatable, but long-running cuts shouldn't park artifacts in `/tmp`.
- **Cross-version skew is invisible to the pre-cut audit** (which reviews the diff, not cross-version compat). The captain's targeted question caught the 7h3 skew; a dedicated old-binary-vs-new-skills check would catch it automatically.

## Observations
- Parallel sessions merging to `main` DURING a cut moved the target (cut commit + value gate) under us — #441/#442 landed after the pre-cut audit + pre.4, forcing a re-value-gate + a spot-audit. A "freeze `main` during the cut" convention would help.
- The detached adversarial audit earned its keep repeatedly: it caught v9's line-132 lint escape, and the spot-audit reproduced #442's data race under `-race` where the shipped tests stayed green.

## What's Next
**The v0.23.0 stable tag is one fix + the cut ceremony away** — see the handoff prompt (`/tmp/0230-cut-handoff-prompt.md`). Sequence:
1. **Ship the race fix (`3p` `launcher-signal-forward-race-fix`)** — dispatch → validation (detached audit) → merge. Gates the cut.
2. **Cut v0.23.0** from the contract-2 `main`: stamp `0.23.0` into both plugin manifests → push → green that exact commit (Runtime Live E2E; expect codex re-runs) → annotated `v0.23.0` tag with the drafted release notes → push (goreleaser publishes + advances `stable`) → verify the tagged manifest + a fresh `brew install`.

Cut readiness: pre-cut audit ✅ clean (+ #441/#442 spot-audited) · value gate ⚠️ WAIVED (shared-core +595 B, deferred) · pre.4 published ✅ · contract bump (#443) on `main` ✅ · release notes drafted+corrected. Follow-ups filed: `kr7` (minor-coupling), `w07` (AC-2 falsifiable); still to file: the pre-cut audit's 2 majors (stale `docs/dev/_mods/pr-merge.md` cross-ref; codex-keepalive assertion steerable).
