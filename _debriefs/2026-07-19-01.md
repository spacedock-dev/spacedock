---
session-date: 2026-07-19
sequence: 1
first-commit: 601c3f53
last-commit: 235e76365bf82742abf76e62f4513e1024c6876e
duration: ~2d (2026-07-17 → 2026-07-19)
---

# Session Debrief — 2026-07-19 #1

A long multi-day session dominated by the `codex-post-compaction-contract-reload` (c6) saga — three false-green validation rejections resolved into a contract-shape re-scope, then a post-approval architectural catch — plus two clean ships and a verified sweep that turned the recurring tautological-test problem into filed enforcement work.

## Shipped
- **6v** `replace-reviewer-reuse-oracle-with-structured-evidence` — [#516](https://github.com/spacedock-dev/spacedock/pull/516). Replaces inference-based reviewer-reuse grading with structured native-handle correlation. Merged after investigating its red CI → confirmed an *unrelated* sonnet boot broad-search flake (test-only diff; opus + all offline lanes green; `main` unprotected).
- **26** `dispatch-build-help-input-mode-drift` — [#526](https://github.com/spacedock-dev/spacedock/pull/526). Makes `dispatch build --help` document both input modes, the exact stdin-vs-flag/file selection rule, and an accepted flag/file `--advance` example (direction (a), parser byte-unchanged). Shipped after a mid-flight rework: a brittle help-output grep test (caught by the captain) was reverted; behavioral coverage stayed on the execute-the-examples test.

## In-flight (PR-pending)
- **c6** `codex-post-compaction-contract-reload` — [#531](https://github.com/spacedock-dev/spacedock/pull/531) OPEN. Two host-neutral compaction-continuity FO rules (safe-to-compact + reread-after-compaction) + a failure-open Codex `PostCompact` delivery hook. **Reworking:** move `«post-compact-notice»` host bindings out of the host-neutral core into the runtime adapters, and delete the 11-phrase prose-grep presence test. **Blocked on a captain decision (HELD):** whether to bump the FO-surface shrink-ratchet (~602 bytes) that the correct decoupled architecture crosses.

## Filed (backlog)
- **f4qwzrd5** `live-boot-contract-file-hunt-flake` — the sonnet claude-live boot broad-search flake (FO runs `find /` for a skill file → no stream progress → 60s watchdog kill); root cause of #516's red. #490's retry lever mitigates, doesn't prevent.
- **cdbhzxc** `claude-post-compaction-contract-reload` — Claude `SessionStart(compact)` model-context delivery for the post-compaction reload (split from c6); spike-confirmed on claude-sonnet-4-6/200k that the hook stdout reaches the post-compaction model.
- **hthnpaag** `fix-tautological-output-grep-tests` — 8 adversarially-confirmed tautological output-grep tests (the "third shape": real sink + hand-written literal asserting rendered output wording no consumer parses).
- **ekmsd9c7** `anti-tautology-enforcement-output-grep-shape` — extend anti-tautology enforcement to the third shape; mechanical (reuse the existing `internal/contractlint` AST infra) + no carve-outs; records the current tension.

## Decisions
- **c6 option-(b) contract-shape re-scope.** After two false-green rejections (Codex FO relayed by captain), chose to rewrite AC-1/AC-2 as contract-shape requirements and stop claiming runtime behavior; the behavioral judgment-rule proof became a named out-of-scope live followup.
- **Merged #516 over the sonnet-flake red** — unrelated boot flake, test-only diff; and merged #526 after the help-grep revert.
- **Killed the compaction presence test entirely — "grep it at review, not in a test."** A contract-shape AC is verified by a review-time grep cited as evidence, never a committed grep-test over a shipped `.md` (that checks file content, not behavior — regardless of "runtime-loaded"/"contract-shape" framing).
- **AC-4 narrowed to honest inertness** (round-3 rejection, captain + roborev job 278): the hook-absence test proves only the shipped stdout-only script writes nothing, not host-level failure-open.
- **`«post-compact-notice»` host bindings → adapters** (post-approval architectural catch): host detail belongs in the runtime adapters' `## Runtime implementation`, not the host-neutral core; the core declares abstractly via `→ runtime-binding`.
- **FO-surface ratchet bump: HELD** (parked pending decision).

## Issues — Workflow
None workflow-specific.

## Issues — Spacedock
_(candidates; not filed as GitHub issues this session — offer stands)_
- **Sonnet boot broad-search flake** — FO `find /`s for a skill/contract file at boot; #490's retry lever mitigates but doesn't prevent. Tracked: `live-boot-contract-file-hunt-flake`.
- **Tautological-test recurrence + Claude-review leniency** — a prose-grep test shipped and passed **4** Claude validation reviews; roborev's *codex* lens caught what the *claude* lens missed. The proof-discipline is prose and decays; the mechanical infra (`contractlint`) exists but was not applied — carve-outs were rationalized instead. Tracked: `fix-tautological-output-grep-tests`, `anti-tautology-enforcement-output-grep-shape`.
- **Roborev degraded in-sandbox** — codex agent can't read `~/.codex/config.toml`; worktree `.roborev` snapshot writes are "operation not permitted" → the `branch_final` panel can't run locally; only the claude-code lens works.
- **FO-surface shrink-ratchet vs `«fn»`-decoupling tension** — the byte-count shrink-only ratchet (`foFunctionReferenceBaselineBytes`) penalizes the correct decoupled form (core pointer + per-host adapter bindings) over a compressed-but-wrong core-inline line; a raw metric can't distinguish decoupling from bloat.

## Observations
- **Mechanical > prose for discipline, and the infra already exists.** Prose rules against tautological tests were applied leniently 4× in one session; only structural lint (reuse `contractlint`) + scoped mutation testing are not-prose-driven, at the cost of partial coverage or compute. The "carve-out rationalization" (runtime-loaded / contract-shape / honestly-scoped) is the failure mode to end.
- **c6 dogfooded its own Rule 1 live** — the "what needs to be true before compaction" exchange was the FO applying c6's safe-to-compact durable-boundary judgment to this session. The behavioral value needs **no hooks** (hooks only auto-deliver the reminder; the FO behavior is cue-triggerable), which de-risks the deferred live followup.
- **My Claude validation lens is too lenient on false-green** — it passed the tautology class 4× (c6 r1/r2/r3 + the help-grep); roborev's codex lens + the captain caught them. The fix is a validation dispatch that reproduces the bite (mutate → red), plus roborev as a required backstop.

## What's Next
- **c6 #531** — unblock the ratchet decision (HELD) → the worker lands both fixes (adapter placement + prose-grep strip) → re-validate → merge (modifies the shipped FO contract). The behavioral live-proof is a deferred followup (no hooks needed for it).
- **This session's seeds** — `fix-tautological-output-grep-tests`, `anti-tautology-enforcement-output-grep-shape`, `live-boot-contract-file-hunt-flake`, `claude-post-compaction-contract-reload`.
- **Related prior backlog** — `tautological-test-fixes` (4 assertion-free/mirror, still unshipped, and its `testlint-assertion-free-gate` gate), `anti-tautology-enforcement-and-template-gap`, `contractlint-prose-function-backstop-retirement`, `contractlint-mixed-structural-boundary`.
