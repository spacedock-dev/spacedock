---
session-date: 2026-06-09
sequence: 1
first-commit: 6ead4fa6
last-commit: 6accd320
duration: ~7h
---

# Session Debrief — 2026-06-09 #1

Shaping-FO session across the 0.19.9 cut and the 0.20.0 launch. The Commander drove the pre-flip cohort + the pre-flip mechanics; the FO unblocked the cut, cut **0.19.9** (released, darwin+linux), carved + packaged the **0200-flip** sprint, shaped the docs site to a PR, captured two survey-feedback rounds, and the **flip itself is now in progress** (`pj` at implementation).

## Shipped
- **0.19.9 release** — cut + published (`v0.19.9`, darwin amd64/arm64 + **linux** amd64/arm64 + checksums). FO cut it here after clearing the migration-check cut-blocker.
- **v3** `ship-linux-binaries` — [#332](https://github.com/spacedock-dev/spacedock/pull/332). Linux binaries + a `curl|sh` install path.
- **th** `safehouse-preserves-spacedock-bin` — [#334](https://github.com/spacedock-dev/spacedock/pull/334). Front door re-asserts `SPACEDOCK_BIN` past safehouse's env-scrub.
- **jm** `entity-label-localization` — [#333](https://github.com/spacedock-dev/spacedock/pull/333). Layer-1 operating-voice label localization.
- **f1** `migration-check-share-walk-helper` — [#331](https://github.com/spacedock-dev/spacedock/pull/331). Migration-check shares the production walk composition.
- **47rx** `survey-codex-and-sandbox-followups` — [#335](https://github.com/spacedock-dev/spacedock/pull/335). Survey body pass (Codex section, sandbox caveat, G anti-tautology floor).
- **yq** `frontdoor-launch-ux` — [#336](https://github.com/spacedock-dev/spacedock/pull/336). Honest auto-install message + pre-launch workflow banner.
- **nzb** `gate-release-on-e2e` — [#337](https://github.com/spacedock-dev/spacedock/pull/337). Release-time `e2e-gate` job goreleaser `needs:` (pre-flip).
- **cmx** `frontdoor-launch-banner-ux` — [#338](https://github.com/spacedock-dev/spacedock/pull/338). Launch-banner rewrite to consistent first-officer framing (pre-flip).
- **k6d** `two-channel-release-devbranch-stamp` — [#339](https://github.com/spacedock-dev/spacedock/pull/339). Per-channel devBranch stamp + single-target plugin-manifest stamp (pre-flip).

## Filed (backlog)
- **cmx** `frontdoor-launch-banner-ux` — shipped same session (#338). Banner UX: FO-framing flip, status-command overload, multi-workflow limbo.
- **5ar** `pre-cut-audit-cleanups-0199` — the 4 recorded 0.19.9 pre-cut non-blockers + the time-sensitive Node-20 actions bump (#5).
- **zby** `survey-knowledge-work-archetype` — survey feedback (anonymized): lead with workdir-attributed Codex count; name a knowledge-work archetype.
- **zwg** `survey-iteration-framing-and-branch-attribution` — survey feedback (anonymized): reframe the commission offer toward iteration/exploration; branch-aware work-by-area.
- **wv** `mkdocs-material-docs-site` — the public MkDocs Material docs site; **PR #341 open** (PR-pending, awaiting captain merge).

## Non-PR commits (workflow-only)
Roadmap process docs + the release + the flip-in-progress (FO owns `docs/roadmap/`):
- `6ead4fa6` roadmap(0198): quote session-date scalar — the migration-check cut-blocker fix that unblocked the 0.19.9 tag.
- `a84018d0` roadmap(0200-flip): carve the main-flip sprint (kickoff).
- `bffb8b42` + `432cda52` roadmap(0200-flip): preflight staff review (NOT-READY, 5 material) then resolution (folded + verified, READY).
- `2ffe9c9b` release: bump version to spacedock@0.19.9 (the cut).
- `85404bdd` + `929cac74` roadmap(0200-flip): package the pre-flip pair, then add cmx to the Commander package.
- `f174e71d` roadmap(0200-flip): pre-cut antipattern audit — SHIP-CLEAR.
- `c6f70e45` debrief: 0200-flip Commander drive #1 (Commander's record).
- `504acc1b` roadmap(0200-flip): flip-drive cold-boot prompt for pj.
- `ac476b51` flip(fold): reconcile release docs to post-flip main + branch-local marketplace ref (AC-4) — **the flip is underway** (captain-driven).
- `6accd320` fix(journeymetrics): tolerate non-JSON stderr lines.

## Decisions
- **Cut 0.19.9 here (not the Commander).** The FO cleared the cut-blocker (unquoted `session-date` scalar tripping `TestMigrationCheckFixturesParseConsistently`) and cut from `next` — confirmed the pre-flip stable lane is still `next`, not `main` (`docs/releasing.md` is post-flip-aspirational; the real machinery cuts from `next`).
- **0200-flip = nzb + k6d + cmx (Commander pre-flip) → pj (the flip, driven here).** Captain split: the pre-flip mechanics land on `next` via a cold-boot Commander; the outward flip runs here, captain-gated at every step. cmx added to the drivable set mid-drive.
- **Docs first, as a releasable shell.** wv ships structure + honest `[TODO]`s now, prose later; the content-sourcing/no-duplication policy and the deploy domain are parked.
- **Agent distinction in the docs:** AGENTS.md is for an agent *developing the repo* (Contributing); an agent *using the product* reads the docs, covered by `/llms.txt` — so the "For agents" head-link surface was cut and AGENTS.md re-homed under Contributing.
- **Survey feedback is captured anonymized** — corpus content stays out of the shared state repo; only the behavioral feedback is recorded.

## Issues — Workflow
None blocking. The repeated "second message races the ensign's turn" pattern recurred (folds sent while an ensign was mid-turn were missed until re-sent) — a coordination friction, not a bug.

## Issues — Spacedock
- **`mkdocs-llmstxt` / llms.txt convention vs project-Pages subpath:** the canonical `/llms.txt` only resolves at a domain root; under a `/spacedock/` project-Pages base it's at `/spacedock/llms.txt` and the convention (and a14y) miss it. Deploy-topology decision (custom/org domain) parked. Not filed.
- **Survey feedback** (zby, zwg) are spacedock-survey improvements, captured as backlog entities rather than GitHub issues (richer + anonymized in-repo).

## Observations
- **The preflight staff review earns its keep, again.** The 0200-flip preflight review came back NOT-READY with 5 material findings — one (the `journey_workflow_test.go` byte-literal anchors breaking on the `needs: e2e-gate` edge) empirically verified by a scratch patch (0→14 failures). Folded before the gate locked.
- **The carve completeness sweep caught 5 ownerless flip surfaces** before pj ideation (archive-tag-fires-goreleaser, channel-aware stamp, marketplace-ref-paired-test, doc drift, existing-user migration) — folded into pj's checklist.
- **Reproduce-don't-trust validation** on the docs site (fresh-venv `mkdocs build --strict`) + the ensign's own idempotency + topology catches were the difference between a clean shell and a broken one.
- **The flip is the riskiest outward step of the whole line** — the tag-the-green-tip invariant (green run = force-pushed main tip = tagged commit, all one SHA; freeze `next` step 3→7) is the load-bearing discipline; it's encoded in pj's runbook + the flip-drive prompt.

## What's Next
**In progress — finish the flip (`pj`, here + captain):**
- `pj` is at `implementation`; AC-4 doc-reconciliation already folded (`ac476b51`). Remaining per pj's 9-step runbook + `docs/roadmap/0200-flip/flip-drive.md`: fold the marketplace ref+paired-test commit, freeze `next` + record `$PREPARED`, live e2e on the prepared tip (green + headSha re-verify), archive `origin/main` as **`archive/v0`** (non-`v*`), guarded `--force-with-lease` flip, tag **`v0.20.0`**, post-flip calendar-bump + verify. Captain-gated at every outward step. NO contract change.
- **Watch:** the Node-20 actions bump (`5ar` #5, ~June 16) should land before the flip cut runs `release.yml`.

**Awaiting captain:**
- **wv** docs site — **merge PR #341** → the pr-merge hook auto-terminalizes + archives. (Then: stop the leftover preview server + clean the worktree's untracked build artifacts.)

**Backlog clusters:**
- **Survey polish** — `zby` + `zwg` + `za` (a coherent survey-skill pass; the iteration/exploration framing is the meatier thread).
- **Cleanups** — `5ar` (Node bump time-sensitive; checksum-gate test-strength; gofmt; hasGitEntry comment).
- **Post-flip** — the docs deploy domain (`/llms.txt` topology), distribution (`44` bundle / `5w` notarize), `xp` cross-session FO↔Commander channel, the docs-site content-sourcing policy.
