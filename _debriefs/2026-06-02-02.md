---
session-date: 2026-06-02
sequence: 2
first-commit: e1beda26
last-commit: ab8b157d
duration: ~5h (state-commit span; wall-clock longer incl. CI-E2E + live-e2e waits)
---

# Session Debrief — 2026-06-02 #2

The 0.19.4 build-out session: shipped 8 entities to `next`, and the detached adversarial audit earned its keep on every high-stakes cycle. The capstone was the `3g`→`hd` chain — `3g`'s session-JSONL artifact surfaced the long-standing headless-FO-drive flake's root cause on the first captured live run (the FO couldn't find `spacedock` on its PATH → #262's binary-absent abort; plus a non-discoverable fixture), and `hd` fixed it: the live-e2e now drives on BOTH models. Ran alongside a peer **codex FO** sharing the split-root state (it drove `r0`/`f4`/`4n`); path-scoped commits + pull-rebase kept the two FOs collision-free.

## Shipped
- **5v** `binary-absent-fo-bootstrap` — [#262](https://github.com/spacedock-dev/spacedock/pull/262). FO contract gate: binary-absent now emits a runnable install hint (`brew`/`go build`) instead of routing to the missing binary's `doctor`.
- **1x** `code-cleanups-0193` — [#263](https://github.com/spacedock-dev/spacedock/pull/263). 0.19.x cleanups + the `init→install` rename drift + unknown-subcommand silent-exit (folded in from the captain's live install-path test).
- **7h** `release-notes-local-summary` — [#264](https://github.com/spacedock-dev/spacedock/pull/264). Local LLM-summarized release notes via tag-body → goreleaser `--release-notes` (re-homed from a diverged branch).
- **v1** `encode-deliverable-principles` — [#266](https://github.com/spacedock-dev/spacedock/pull/266). Re-homed the deliverable-principles + detached-audit disciplines to the dev README + commission template (dev-specific, NOT the universal FO contract); dropped the grep self-proof tests.
- **3g** `live-e2e-transcript-artifact` — [#267](https://github.com/spacedock-dev/spacedock/pull/267). Archives the agent session JSONL (under a per-job `CLAUDE_CONFIG_DIR`) on a failed live run — the `hd` diagnostic.
- **rx** `prune-tautological-prose-tests` — [#268](https://github.com/spacedock-dev/spacedock/pull/268). Pruned/demoted 6 tautological grep-over-prose integration tests (the banned P2 antipattern), each disposition mutation-proven.
- **hd** `headless-fo-drive-flake` — [#271](https://github.com/spacedock-dev/spacedock/pull/271). FIXED the headless-FO-drive flake: built `spacedock` on the FO PATH + a discoverable live fixture; the live-e2e now drives on both models.
- **02** `retire-python-oracle` — [#270](https://github.com/spacedock-dev/spacedock/pull/270). Deleted the vendored Python oracle + VendorRunner; graduated the parity suite to native goldens; `--next-id` is now a logical (path-independent) assertion, not a byte-frozen golden.

## Filed (backlog)
- **kj** `split-root-onboarding-command` — a `spacedock state new` one-liner to birth a split-root workflow on an existing repo (from the captain's recce-repo test).
- **qs8** `ensign-lifecycle-reconcile` — harden FO reconciliation: teardown agents at terminal + supersede-shutdown + a state-keyed `reconcile` helper (no lingering ensigns / stale branches / un-advanced PRs).
- **5v** `binary-absent-fo-bootstrap`, **rx** `prune-tautological-prose-tests` — shipped same session.
- **r0** `codex-runtime-adapter` — filed + ideated by this FO; the codex peer FO took it over (now its validation).

## Non-PR commits (workflow-only)
~140 state-checkout commits this session (incl. the codex peer's churn). Surfaced non-PR:
- Feedback cycles: `2ff362b5` (binary-absent M1/M2), `4df48dd6` (1x AC-6), `3ca14ee5`+`e6b52e31` (v1 cycle-1 tests + cycle-2 reshape), `fce02066` (7h dead-guard), `f74f28f4`+`fb4f1478`+`66e18875` (02 cycles 1/2/3), `84fa9426`+`a60de62f` (3g re-target + cycle-2), `eaa35634` (hd cycle-2).
- Ideation reports: `9264e5bc` (v1), `563b30bb` (1x), `90612bdb` (binary-absent), `9d7082b5` (02).
- Seeds: `96bc0881` (binary-absent), `8bf3aea2` (split-root-onboarding), `02ef7dd9` (ensign-lifecycle-reconcile).
All other session commits roll up into the shipped PRs above (or the codex peer's entities).

## Decisions
- **Install-path slice (captain):** fold the rename-drift + unknown-subcommand silent-exit CODE fixes into `1x`; file the FO-contract binary-absent gap as its own entity (`binary-absent`).
- **v1 reshape (captain):** the deliverable principles are DEV-SPECIFIC, not universal FO (a non-dev workflow's AC proof is an artifact/metric/review, not a test) → re-home to dev README + commission template; the grep self-proof tests are the banned P2 antipattern → delete; behavioral teeth → `2a` as a WORKFLOW-OPT-IN guard.
- **`--next-id` (captain):** byte-parity with the old Python implementation is NOT the goal — only LOGICAL SOUNDNESS (format + determinism + a path-independent derivation vector).
- **`hd` before `02` (captain):** `hd` fixes the live-e2e PATH (tested + shipped) FIRST, then `02` rebases onto it for PROPER live CI (a real FO-driving cycle), rather than merging `02` offline-only.
- **CI-E2E approval (captain prompt):** approving the live-cycle env gate is part of the merge-on-proof bar (offline + ONE live-model cycle + validation + audit); the FO had been skipping the live cycle. Restored for `02` (drove on both models with the oracle removed).
- **Peer boundary:** `r0`/`4n`/`f4` are the codex peer FO's — hands-off; coordinate only via shared split-root state.

## Issues — Workflow
- Live-e2e was DETERMINISTICALLY red (the FO never drove): `spacedock` not on the FO subprocess PATH + the live fixture lacked `commissioned-by:` → `status --discover` found 0 workflows → FO exited before TeamCreate. FIXED by `hd`. #262's binary-absent gate surfaced it as a loud, correct abort (exposing the env bug, not causing it).

## Issues — Spacedock
- **Lingering ensigns** — the FO leaked 3 agents (no teardown step at terminal in `## Merge and Cleanup`; no supersede-shutdown rule; shutdown rules key off drift-prone session memory). Filed `ensign-lifecycle-reconcile` (`qs8`). Not separately GH-filed (tracked as the entity).
- **Reactive rebase / stale local `main`** — in-flight branches reconciled only at merge (02 hit #263 late, the pre-merge rebase caught it); local `main` carried prior-session merge-bubbles + the FO binary ran ~19 commits stale (affecting `dispatch build`/`status`). Discipline adopted: reset `main`→`origin/next` (NOT merge — merging makes the bubble) + rebuild `./spacedock` at boot/after-merge. Folds into `qs8`.
- **Tautological grep-over-prose tests** (~23% of `skills/integration`) shipped in prior work — pruned via `rx`; the class is unfixable by a denylist (v1's cycle-1 re-audit proved it 5×).
- (Carried) `n3` frontmatter-hash-quoting still forces the `pr=#N` quoting workaround.

## Observations
- **The detached adversarial audit was the session MVP — it caught a real defect validation PASSED on every high-stakes cycle:** #262 (M1 skip-on-zero + M2 disclaimer test holes), `1x` (AC-6 exit-code regression), `7h` (dead `[ ! -s ]` release guard), `v1` (grep tests unsalvageable — 5 inversions stayed green), `02` (a path-dependent golden TWICE — absolute checkout path, then macOS `/private`), `hd` (the 2nd discovery blocker — caught before a wasted live run). Unifying lesson: **`looks-right-here` ≠ `runs-right-everywhere`** — `native==golden` ≠ `golden==reproducible-oracle`; two-paths-same-OS ≠ OS-independent; offline + valid-YAML ≠ a real CI run. The audit's lever is running in a DIFFERENT environment (different path / detached / Linux-CI) than validation.
- **The `3g`→`hd` chain paid off exactly as designed:** the jsonl artifact turned an intermittent "the FO won't drive" mystery into a concrete, evidence-grounded test-infra fix on the first captured run — and it was a clean honest abort, not a stall, so no speculative runtime change was needed.
- **`v1` was corrected by its own principles at two levels** (grep-proof banned; the constraint is dev-specific) — the strongest evidence the principles bite.
- **Two-FO collaboration on shared split-root state** (this Claude FO + a codex peer FO) worked cleanly via path-scoped commits + pull-rebase. No collisions.
- **AC framing matured this session:** runtime-observable ACs (jsonl-archived, FO-drives, CI-startup) are honestly marked by-construction-pending-live and confirmed on a real CI run — not claimed from offline.

## What's Next — 0.19.4 sprint
0.19.4 is the release accumulating on `next`; this session landed 8 entities toward it. The remaining 0.19.4 followups for next sprint:

**Recommended next (unblocked by `02`):**
- **`zj` `yaml-parser-migration`** — ideation APPROVED, implementation was parked on `02` landing (now merged). Dispatch its implementation: migrate the frontmatter reader to `yaml.v3`/`yaml.Node`, keep the hand-rolled mutator seam + the durable unknown-field/key-order byte-preservation contract; documented divergences from the retired Python quirks. **Note its writer-quoting CONVERGES with `n3`** — sequence/coordinate them.

**Status-lane bucket (serialize — one `internal/status` impl worktree at a time):**
- **`n3` frontmatter-hash-quoting** — HIGH value; fixes the pr-mod's `pr=#N` comment-strip (the live `pr='"#N"'` quoting workaround until it lands) + colon-bearing `source:` GitHub render.
- **`6b` terminal-guard-rejected-consistency**.
- **`2a` require-external-proof-guard** — the CODE guard for `v1`'s external-proof principle; MUST be **workflow-opt-in** (per v1's design note — the constraint is dev-specific, not universal).

**Hardening backlog (0.19.4):**
- **`qs8` ensign-lifecycle-reconcile** — teardown-at-terminal + supersede-shutdown + the reconcile helper (folds in the reset-`main`→`next`/rebuild-binary discipline).
- **`kj` split-root-onboarding-command** — the `spacedock state new` helper.

**Release:** cut **0.19.4** when the sprint's followups land (follow `docs/releasing.md`; the marketplace.json `bump-calendar` step is now documented — run it).

**Parked / peer (not 0.19.4 this FO):** `ng` roborev-validation-hook (parked); the codex peer FO's `r0` codex-runtime-adapter, `f4` codex-live-ci, `4n` journey-cost-ledger.
