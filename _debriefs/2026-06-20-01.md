---
session-date: 2026-06-20
sequence: 1
first-commit: "326b18e3"
last-commit: "7501cc4f"
duration: ~5h (2026-06-20 01:20 → 06:35 UTC, incl. long live-CI + ensign waits)
---

# Session Debrief — 2026-06-20 #1

Layered-FO sprint, dispatch-driven. Cleared the v0.22 cut-blockers (vk/mvc/ga), then the session pivoted into a deep back-channel investigation: discovered the worker↔FO back-channel is gated behind an experimental flag, re-homed the pi capstone (b2) into 0221 and reframed it to capabilities-as-«fn», and enabled + dogfooded the live back-channel (mid-run steering). 5 entities shipped, 3 filed, 1 in flight.

## Shipped
- **vk** `migration-check-raw-scalar` — [#410](https://github.com/spacedock-dev/spacedock/pull/410). Migration-check compares RAW scalar frontmatter values (un-reds `main`; fixes the auto-typed-date false failure). Test-only.
- **mvc** `haiku-loop-premise-spike` — [#411](https://github.com/spacedock-dev/spacedock/pull/411). Credential-gated live regression lane proving a Haiku FO holds the mechanical dispatch loop (CAN-HOLD across N=3).
- **ga** `strip-deferred-tier-vocabulary` — [#403](https://github.com/spacedock-dev/spacedock/pull/403). Strip deferred-72 tier vocab from «gate.assemble-verdict»; KEPT the structural-absence guard test.
- **66** `claude-launch-agent-teams-flag` — [#412](https://github.com/spacedock-dev/spacedock/pull/412). `spacedock claude` exports `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` into the claude child env (ACs narrowed to env-plumbing; deterministic merge).
- **b2** `pi-back-channel-dispatch` — [#409](https://github.com/spacedock-dev/spacedock/pull/409). Capabilities-as-«fn» reframe of the shared dispatch contract + detect-based Claude `addressable-worker` binding. Re-homed 0223→0221 (Approach A only; Approach B pi-intercom wiring stays 0223). Merged clean on all 4 live lanes.

## Filed (backlog)
- **adk7** `trim-dispatch-adapter-prose` — further cumulative cut of the per-adapter operational prose now that «fn» `→` lines consolidate present/absent (sequence after b2; HARD constraint: preserve await/reuse/guardrail contract — SB2 risk).
- **7h** `opus-rejection-flow-reviewer-routing-flake` — live opus FO intermittently routed the cycle-2 rejection re-review to the impl worker (fix≠reviewer violation); sonnet passed the same contract → model-variance, not a b2 regression. ACs: establish N≥3 frequency, then accept-re-run or harden the prose.
- **xd** `merge-guard-owns-arm-and-finalize-under-pr` — merge guard should own auto-arm + finalize-from-detected-MERGED (eliminate FO hand-rolling). **In flight** — implementation running at session close (steered mid-run with the Codex-FO premature-finalize finding).

## Non-PR commits (workflow-only)
State + scaffolding that didn't flow through a code PR:
- pr-merge mod fix: PR base `next`→`main` (CL-authorized direct `_mods/` edit; the mod was stale, all real PRs target main).
- `_reviews/0221-fo-review-pr409-capability-refactor.md` — the 0221 cross-sprint review of #409 + addendum (capabilities-as-«fn» restructure + naming + M1 flag-gate root cause), for the pi commander.
- Feedback cycles: b2 cycle-1 (0221 takeover → «fn» reframe) + cycle-2 (detect-based binding); launcher-flag cycle-1 (ACs narrowed after validation REJECT).
- Recovered: machine ENOSPC mid-finalize → `git gc` (.git 167M→23M, freed ~3.5Gi).

## Decisions
- **b2 re-homed 0223→0221** (CL-directed). 0221 owns the shared capability contract (Approach A); Approach B (pi-intercom wiring) stays 0223. Sprint mixing is bookkeeping — fix it in release notes, not git.
- **launcher-flag: keep the harmless export, narrow the ACs** to env-plumbing only. Validation (live e2e) falsified the "enables the back-channel" claim — `env -u` still exposes SendMessage; the authoritative enabler is `~/.claude/settings.json` (`env.CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` + `teammateMode: auto`).
- **ga: KEEP the deferred-tier structural-absence test.** The README proof policy explicitly permits structural-absence checks in `contractlint` (consistent with the shipping `claudeModelTokens`/`claudeTeamImperative` siblings) — it is not the banned prose-grep antipattern (expected value is external: "member 72 deferred"). Handoff's "delete it" was overruled.
- **Release cut: ONE combined `v0.23.0`** (Pi runtime support + FO contract maturation), notes grouped by THEME not sprint. Main is a shared trunk; a release versions the trunk. Pi = new host functionality → minor bump.
- **merge-guard: dispatch the fix + dogfood it** — build the binary from its worktree and use the new `merge guard` verb to run its own merge ceremony (validation-in-use).

## Issues — Workflow
- Recurring FO finalize bug (mine): the archive-move commit used a newline-bearing `$PATHS` variable as one pathspec → `git commit` failed silently (ga, b2). Caught + fixed both with explicit-path commits. Use explicit paths, not a variable, for rename commits.

## Issues — Spacedock
- **Worker↔FO back-channel (`SendMessage`/`TeamCreate`) is gated behind the experimental flag `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, OFF by default** — the FO contract (`claude-fo-dispatch.md`) assumed it always-present. A fresh session has named-but-not-addressable workers; completion falls back to `task_notification`, teardown to `TaskStop`. Root-caused via transcript forensics (same `agentSetting` sessions split purely on the flag; version-correlation was a red herring). Mitigations this session: launcher sets the flag (#412), b2 binding is now detect-based (#409), and the flag was set in `~/.claude/settings.json` (`+ teammateMode: auto`) — after which mid-run steering worked live.
- **`merge guard` premature-finalize bug** (Codex FO): with `pr:` set to an OPEN PR it returned `signal: finalized` / `status: done` and archived — it keys off `pr:`-presence, NOT verified-MERGED. Archived a task before its PR landed. Steered into the `merge-guard` worker mid-run; new AC + test added (must return blocked/await-pr unless a local MERGED signal is set).
- **`merge guard` doesn't auto-arm under `merge: pr`; `state sweep` won't finalize from a non-armed state** → FO hand-rolled the arm→push→PR→merge→clear→terminalize→archive ceremony 5×. Filed (`merge-guard-owns-arm-and-finalize-under-pr`).
- **Completion-signal name mismatch: dispatch build emits `SendMessage(to="team-lead")` but the runtime lead is `"main"`** → every worker's completion `SendMessage` fails ("no agent named team-lead"), falling back to `task_notification`. UNFILED (recommend filing).
- pr-merge mod base was stale (`next` → fixed to `main`).
- **Shared-state-checkout race:** the `.spacedock-state` working tree is shared on-disk with the concurrent 0223 peer session; their git ops silently reverted the FO's *uncommitted* writes. Mitigated by set-and-commit atomically in one shell call.
- **Empty-id peer entity blocked boot** (`sd-b32` validation is fatal on a missing id) — recurring shared-branch hazard (handoff #38, now firing on boot/`--next`/`--help`).
- **Machine ENOSPC** mid-session (`/System/Volumes/Data` 417/460Gi) — recovered ~3.5Gi via `git gc`; machine-level cleanup needed (mostly outside our workflow).

## Observations
- **Mid-run steering dogfooded** — the FO steered the running `merge-guard` worker via `SendMessage` (the back-channel this session enabled), injecting the Codex-FO premature-finalize finding. First successful live FO→worker steer.
- **Transcript forensics** settled the back-channel mystery empirically (per-session tool enumeration + the `agent-setting` + flag correlation) rather than by guessing.
- **Cross-sprint reality:** the 0223 pi cohort (#405–#409) is substantially on main; releases version the trunk, not the sprint — handle attribution in notes, not by splitting tags.

## What's Next
- **Session opener (in flight): merge-guard.** When implementation finishes, build the binary from `.worktrees/spacedock-ensign-merge-guard-owns-arm-and-finalize-under-pr`, then **dogfood** `merge guard` to run its own validation→merge ceremony (assume the behavior fixed, per CL). Verify the premature-finalize gate holds.
- **Release cut `v0.23.0`** — draft combined notes (Pi runtime support; FO contract maturation; fixes), grouped by theme.
- **Backlog:** `trim-dispatch-adapter-prose` (after b2/merge-guard), `opus-rejection-flow-flake`, file the `team-lead→main` completion-signal fix, b2 **Approach B** (pi-intercom wiring, 0223).
- **Peer:** `status-validate-determinism` (a1) is stranded armed (`mod-block: merge:pr-merge`) — the 0223/Codex FO's to resolve.
