# Rejection-journey shape attribution — blind two-sided analysis (2026-08-16)

Provenance: FO-run workflow after the captain's bare-mode ruling. Side A derived the
contract-determined shape from source only (blind to streams); side B extracted observed
shapes from the five preserved run streams only (blind to contract); a synthesizer joined
them, re-verifying disputes against the worktree sources and raw streams directly.
Synthesis verdict verbatim below; A's derivation (with its one corrected error) appended.

---

## Synthesis verdict

All disputes are now resolved against the worktree sources and the raw streams. Final report:

---

**Preliminary: which sources govern.** A's citations do not match `/Users/clkao/git/spacedock-research/spacedock-v1` at HEAD — they match the checkout the runs actually tested: `/Users/clkao/git/spacedock-research/spacedock-v1/.worktrees/spacedock-ensign-repair-codex-rejection-round-recording` (the repair-loop script builds `spacedock-under-test` from that worktree). I verified the load-bearing texts there: `assertWorkerLifecycle` (lines 119-213) is byte-identical between worktree and main; `FindSectionSpans` (`internal/status/section_read.go:94-111`) is identical — exact-text heading match, `len(matches) != 1` returns nil spans plus the "matches %d headings" error, never a partial span; and the fixture's rework instruction "Then append a second `## Stage Report: implementation` section" is identical in both (only the Feedback Cycles paragraph differs between worktree and main). A's SKILL.md quotes match the worktree's five-step version.

**The one decisive error in A.** A derived "exactly 2 implementation spawns" from the Claude bare-mode adapter (`claude-fo-dispatch.md:9,111-113`) and from the ABSENT branch of `codex-first-officer-runtime.md:10`. These runs are Codex runs, and the Codex adapter makes `«addressable-worker»` conditional on the live surface: PRESENT when `followup_task` is live. It was live: I extracted every `dispatch build` from all five streams, and every run delivered advance packages (`--advance`, prompt "Advancing to next stage: …") that never incremented the harness spawn counter — loop run-3 used `--advance` for both validation advances (indices 41, 76) in the same run it fresh-spawned the rework. A non-`spawn_agent` turn-starting route was therefore live and in use, so `«addressable-worker»` was PRESENT, every reuse condition in `fo-dispatch-core.md:48-53` passes (context-budget ABSENT is satisfied per `codex-first-officer-runtime.md:28`; the fixture declares no `fresh: true`, no worktree, no model), and `feedback-rejection-flow/SKILL.md` step 1 determines REUSE of the existing handle for the fix — spawns stays 1. On this surface the contract-determined implementation spawn count is 1, not 2. A's "spawns != 1 CONTRADICTS" verdict is wrong; the conditional A ignored sits inside A's own citation of `codex-first-officer-runtime.md:10`. (A's bare-mode reasoning does govern the Claude `-p` branch; the runner swaps in `assertImplementationWorkerLifecycle` only for `codexAsLiveDriver` at `claude_live_runner_test.go:399`.)

**1. Per-run attribution**

Loop run-1 (RED rejection-gate-not-prepared): real FO adherence failure (i), not a line-209 clause. The lifecycle composite passed. SKILL step 5's done-condition is "exactly one fresh open gate has been prepared and presented, and the flow stops"; the FO never invoked `gate prepare` ("entity has no gates record"; FO final words: "closing the now-unneeded worker without advancing to `done` or consuming any gate"). The fixture prompt's "stop after … the second validation passes; leave the entity nonterminal" does not excuse it — a prepared open gate is nonterminal (`awaiting-captain`), which is exactly the end state of the two green runs.

Loop run-3 (RED, spawns=2): real FO adherence failure (i), with one stated caveat. The rework at stream index 59 was a second spawn-style build (`--feedback-reflow`, no `--advance`, full `name`/`$spacedock:ensign` envelope) while the same run advanced workers via the followup route at 41 and 76. With the route live and reuse conditions passing, SKILL step 1 says "Reuse the existing handle … otherwise shut down and fresh-dispatch" — fresh-dispatching the fix target deviated from the determined route, and the codex adapter calls feedback rejection "the load-bearing exception to casual fresh dispatch." Caveat: whether that specific handle was still listed/addressable at the rework moment cannot be proven — the graded rollout streams were not preserved (B's caveat 1, which I confirm: `_codex-home` dirs are empty). If the handle had genuinely died, fresh dispatch would be the contract's fail-safe branch and this red would be a harness over-pin; the balance of evidence (followup used immediately before and after) says adherence failure.

Prerebase run-3 (RED, report=selector "Stage Report: implementation" matches 2 headings): harness defect (ii). This ensign followed the fixture's letter exactly — a second section headed exactly `## Stage Report: implementation` — and `FindSectionSpans` returns nil spans plus the ambiguity error on 2 matches, so `reportErr != nil` and `len(spans) != 1` both fire against the fixture-scripted shape. A and B agree here and both are verified. (This run separately published `--round validation/2`, which the worktree SKILL step 2 forbids — "there is no second publication after the reviewer re-run" — but that is not what the red graded.)

**2. The greens.** Yes, run-2 and run-4 literally satisfy every clause of line 209: exactly one `spawn_agent`-style implementation build, the tracked worker's "Done:" before the first `status=validation`, and a unique exact heading. But both satisfactions are revealing, and the second is not to the harness's credit:

1. The section clauses pass only because the rework ensign deviated from the fixture: it titled its second report `## Stage Report: implementation (cycle 2)` instead of the instructed exact duplicate. All four loop-run ensigns happened to disambiguate; the one ensign that obeyed the fixture literally (prerebase run-3) was failed. There is no latest-wins behavior in `FindSectionSpans` to thank — the greens are luck of model phrasing. The assert over-counts failure against the scripted shape.
2. spawns==1 is a genuine single dispatch, not counter blindness — run-3 proves a second spawn-style build is counted, and the advance route is a different tool that correctly does not increment `spawn_agent`. But the topology it certifies is a single worker chain: one agent implemented, reviewed (advance to validation), fixed (advance back to implementation), and then re-reviewed its own fix (advance to validation again — verified in both greens' streams: one spawn plus three `--advance` builds). That last hop violates SKILL step 4's explicit ban ("Ask that reviewer to re-review the updated entity state, never to validate its own fix work") and the very shape the runner comment at `claude_live_runner_test.go:404-412` says the contract requires ("not the impl worker serving as its own validator"). The Codex branch swapped the reviewer-flow assert out entirely (line 399), so nothing checks reviewer topology. Bluntly: the assert under-counts — it grades green a run that breaks the rejection flow's reviewer-independence rule — and it over-counts on the section clauses. Ironically, loop run-3's failed shape (fresh fix agent, chain kept as reviewer) is closer to the reviewer-separation intent than the greens are.

**3. Verdict per clause of `claude_runtime_helpers_test.go:209`**

1. `spawns != 1` — correct for this scenario, but fragile. Correct because followup was live and reuse is then the determined fix route. Fragile three ways: the contract's fail-safe branches (handle lost, `fresh: true`, worktree birth, model mismatch) make spawns=2 fully conforming, and the clause would misfire on them; the counter increments on ANY `spawn_agent` whose arguments merely contain the substring "implementation" (a fresh cycle-2 reviewer package quoting feedback-to context would falsely trip it); and it silently accepts the self-validation topology (see 2 above) — it counts spawns, not roles.
2. `completed < 0` — correct; mildly fragile (codexWorker tracks only the most recent counted spawn's `task_name`, and completion is the literal substring "Done:").
3. `validation < 0` — correct; the FO must emit `status --set rejection-task status=validation` (observed in every stream); the `custom_tool_call` detection channel is host plumbing, not contract.
4. `completed >= validation` — correct; `fo-dispatch-core.md:23` ("Do not advance to validation until `«completion-signal»` arrives and the stage report passes the completion gate") orders impl-1 completion strictly before the first `status=validation`.
5. `reportErr != nil` — contradicts the determined shape. The fixture scripts two identical headings; `FindSectionSpans` hard-errors on 2 matches. A is right; verified.
6. `len(spans) != 1` — contradicts the determined shape, same root cause (nil spans on ambiguity; even a tolerant reader would count 2).
7. `!strings.Contains(…, "- DONE:")` — content requirement matches the fixture, but it is unreachable on a fixture-literal entity (short-circuit at clauses 5-6), and even on the greens it inspects only the cycle-1 section — the rework report's DONE item is never checked.

**4. Minimal correction.** Clauses 5-7 are wrong; the fix must make fixture and assert agree. The minimal assert-side correction consistent with both the fixture's letter and the observed model convention: replace the unique-selector read with a prefix count — require exactly 2 headings whose text begins with `Stage Report: implementation` (level 2), each of whose sections contains `- DONE:` (the existing heading scanner in `internal/status/section_read.go` already yields spans; only the `len(matches) != 1` uniqueness demand must go). A demand for exactly 2 EXACT-text headings would be wrong the other way — it would flip both greens red, since real ensigns write the `(cycle 2)` suffix. The cleaner companion repair is fixture-side: have `shared_fixtures_test.go:139` instruct the distinct title `## Stage Report: implementation (cycle 2)` explicitly, which legitimizes the unique-selector reader for the cycle-1 section and matches what four of five ensigns did anyway.

Effect on the five observed grades: prerebase run-3 flips RED to GREEN under the corrected clauses (its spawns=1, completed=180 < validation=191 already passed; both duplicate sections would need their `- DONE:` items, which the grade never evaluated and the lost final entity cannot fully confirm — the stream's heading reads are consistent with it). Loop run-1 stays RED (its red is the gate-prepare assert, unaffected). Loop run-3 stays RED (spawns=2). Both greens stay GREEN. So the correction changes exactly one grade and masks nothing real.

What the surviving reds demand: run-1 demands FO adherence to SKILL step 5 (prepare and present the gate before stopping); loop run-3 demands adherence to the step-1 reuse routing for the fix target while the followup route is live. Separately, if reviewer independence is actually the contract's intent for Codex (the runner comment and SKILL step 4 say it is), the Codex branch needs a reviewer-topology assert it currently lacks — the greens show `assertImplementationWorkerLifecycle` alone certifies a self-validating single-worker chain.

Key paths: `/Users/clkao/git/spacedock-research/spacedock-v1/.worktrees/spacedock-ensign-repair-codex-rejection-round-recording/internal/ensigncycle/claude_runtime_helpers_test.go` (lines 161-164, 208-211), `.../internal/ensigncycle/shared_fixtures_test.go` (lines 136-149), `.../skills/feedback-rejection-flow/SKILL.md` (steps 1, 4, 5), `.../skills/first-officer/references/codex-first-officer-runtime.md` (lines 10, 21, 52-54), `/Users/clkao/git/spacedock-research/spacedock-v1/internal/status/section_read.go` (94-111), run artifacts under `/Users/clkao/.claude/jobs/4e49247e/tmp/repair-codex-rejection-round-recording-scratch/{loop,ci-config-runs-prerebase}/run-*/codex-shared-scenarios/rejection-flow/codex-exec.jsonl`.

---

## Side A determined-shape summary (NOTE: impl_spawns derivation corrected by synthesis — on codex the followup surface was live, so reuse/spawns=1 is determined; A's fresh-dispatch derivation governs the Claude `-p` leg)

{
  "impl_spawns": "Exactly 2 implementation-worker dispatches: (1) the normal round-1 dispatch out of backlog, which deliberately omits the fix marker; (2) one FRESH fix-agent dispatch for the rework round after the REJECTED validation. Deciding rule \u2014 skills/first-officer/references/claude-fo-dispatch.md:111-113: \"In bare mode, the feedback rejection flow is sequential: dispatch fix agent (wait for completion), then dispatch reviewer (wait for completion), then present at gate.\" This is determined, not a model choice, because every reuse escape hatch is closed: (a) single-entity `-p` forces bare mode (claude-fo-dispatch.md:9 \"In single-entity mode, skip background inter-agent communication and use bare-mode dispatch for all agent spawning\"), and bare dispatch omits `name` (\":25 omit if bare mode (field absent)\"), so \"A name-less dispatch forfeits reuse \u2014 `name` is the \u00abaddressable-worker\u00bb handle\" (:34); (b) the composed skill's step-1 routing (feedback-rejection-flow/SKILL.md:15) allows reuse \"only when it is addressable, reuse conditions pass, and \u00abcontext-budget\u00bb() reports it under budget; otherwise shut down and fresh-dispatch\" \u2014 and although the fixture workflow/host declares NO context-budget probe (claude-fo-dispatch.md:105 \"Codex declares none\"; codex-first-officer-runtime.md:28 \"\u00abcontext-budget\u00bb: ABSENT; its reuse condition is satisfied\"), so \"If no probe is declared, proceed to reuse\" removes the budget veto, reuse still requires addressability, which bare mode structurally lacks (codex adapter: \u00abaddressable-worker\u00bb \"ABSENT when the live surface exposes only spawn_agent and wait_agent\", codex-first-officer-runtime.md:10) \u2014 the routing therefore collapses to fresh-dispatch; (c) fewer than 2 is impossible: the fixture scripts implementation to run \"once per cycle\" across two distinct rounds (shared_fixtures_test.go:135-139), and the rework MUST be a new worker, not the reviewer or the FO (FO/ensign write-scope: ensigns write assigned code and reports); (d) more than 2 is forbidden: one fix agent per rejection cycle in the bare-mode rule, exactly one rejection cycle occurs (the second validation PASSES), and re-dispatch outside an observed dispatch failure is banned (claude-fo-dispatch.md:66, Dispatch Failure Retry ledger).",
  "report_sections": "Exactly 2 \"## Stage Report: implementation\" sections on the final entity. Deciding fixture text \u2014 shared_fixtures_test.go:136 (first round): \"Append a `## Stage Report: implementation` section with one `- DONE:` item noting the initial implementation does not yet carry the fix marker\"; and :139 (rework round): \"Then append a second `## Stage Report: implementation` section with one `- DONE:` item naming the fix.\" The fixture itself scripts the duplicate heading \u2014 a conforming entity ALWAYS carries two same-text sections.",
  "notes": "Reviewer dispatches: exactly 2 distinct validation workers \u2014 the cycle-1 reviewer (REJECTED) and a freshly-dispatched cycle-2 reviewer. Pinned by the runner comment at claude_live_runner_test.go:404-412 (\"the cycle-2 re-review is a DISTINCT freshly-dispatched validation worker (not a reuse of the bare cycle-1 reviewer, not the impl worker serving as its own validator)\"), by SKILL.md:24 (\"Ask that reviewer to re-review the updated entity state, never to validate its own fix work... fresh-dispatch a reviewer only when it is not [addressable]\"), and by codex-first-officer-runtime.md:54 (\"When \u00abaddressable-worker\u00bb is ABSENT, fresh-dispatch a separate validation reviewer for cycle 2\"). Total worker dispatches for the conforming run: 4 (2 implementation + 2 validation). Other pins: exactly ONE round publication \u2014 `gate record ... --round validation/1` invoked once for the whole cycle, \"there is no second publication after the reviewer re-run\" (SKILL.md:18); exactly one `- Cycle 1: REJECTED ...` line, copied verbatim from inputs/feedback-cycle.txt, appended by the FO BEFORE the round is recorded, and the passing re-validation adds no second line (rejectionReadme, shared_fixtures_test.go:147-150); final entity also carries exactly 2 \"## Stage Report: validation\" sections (REJECTED then PASSED, :143) and stays NONTERMINAL at a prepared-and-presented gate (prompt :175 \"leave the entity nonterminal\"; SKILL.md:28 \"exactly one fresh open gate has been prepared and presented, and the flow stops\" \u2014 graded by assertRejectionGatePrepared); briefing.review.jsonl ends as the 4-line rejectionCompleteLog (reviewer's 2 lines written by cycle-1 validation, worker's 2 lines appended by the rework round). Signal ordering pinned by bare-mode sequentiality plus the completion gate: impl-1 dispatch -> impl-1 Done -> status=validation set -> reviewer-1 dispatch -> reviewer-1 Done (REJECTED) -> fresh fix dispatch -> fix Done -> Cycle line + gate record + state commit -> fresh reviewer-2 dispatch -> reviewer-2 Done (PASSED) -> gate prepare/present -> stop. Line 209 enforces only the earliest link of that chain (completed < first status=validation). Escalation (cycle-3, no reviewer) is not reached: only one rejection cycle occurs. FindSectionSpans precision (verified in code, section_read.go:94-111): fence-safe exact-text heading match; for each selector, len(matches) != 1 (zero OR two-plus) returns nil spans and the error 'selector %q matches %d headings' \u2014 it never returns a first-match or partial span set on ambiguity; success returns one span per selector spanning the heading line through its level-owned section end."
}

## Side A clause verdicts (pre-synthesis; clause 1 verdict overturned by synthesis)

[
  {
    "clause": "spawns != 1",
    "matches_determined_shape": false,
    "why": "CONTRADICTS. The determined shape has exactly 2 implementation-worker spawns (round-1 buggy implementation + bare-mode fresh fix-agent dispatch for the rework). The counter (claude_runtime_helpers_test.go:161-163) increments on EVERY codex spawn_agent function_call whose Arguments contain 'implementation' \u2014 both rounds' dispatch names/prompts carry the stage name \u2014 so a fully conforming run yields spawns=2 and this clause marks conformance as failure. It encodes either a one-round shape or a reuse of the impl worker for the rework, which bare mode forbids ('A name-less dispatch forfeits reuse', claude-fo-dispatch.md:34) and the bare-mode flow rule overrides ('dispatch fix agent (wait for completion)', claude-fo-dispatch.md:113). Substring note: a validation-reviewer spawn whose arguments happen to mention the feedback-to 'implementation' stage would ALSO increment spawns, pushing conforming counts even further from 1."
  },
  {
    "clause": "completed < 0",
    "matches_determined_shape": true,
    "why": "MATCHES. The bare-mode flow waits for each fix agent's completion before proceeding (claude-fo-dispatch.md:113), and the completion gate requires the signal before any advance (fo-dispatch-core.md:44), so a conforming run always produces an observed implementation completion (codex 'Done:' agent_message from the tracked worker, helpers:172-174) and completed >= 0. Tracking detail: codexWorker follows the most recent spawn's call_id, but sequentiality means worker 1's Done lands while codexWorker still names worker 1, so completed is recorded."
  },
  {
    "clause": "validation < 0",
    "matches_determined_shape": true,
    "why": "MATCHES. Advancing rejection-task from implementation into the gated validation stage requires the FO's status mutation ('status --set {slug} status={next_stage} started', fo-dispatch-core.md:44/55), i.e. a command containing 'status=validation'; a conforming run necessarily emits it, so validation >= 0. (Detection channel \u2014 custom_tool_call at helpers:175-177 \u2014 is host-plumbing, not contract.)"
  },
  {
    "clause": "completed >= validation (fails when the impl completion is not strictly before the first status=validation)",
    "matches_determined_shape": true,
    "why": "MATCHES. The contract orders round-1 implementation completion strictly before the advance into validation: 'Enter \u00abgate.lifecycle\u00bb only after \u00abcompletion-signal\u00bb arrives and the stage report passes the completion gate' (fo-dispatch-core.md:44), and bare mode is blocking-sequential (claude-fo-dispatch.md:9,15). validation records the FIRST status=validation occurrence, which follows worker 1's Done in a conforming run, so completed < validation holds and the clause is consistent with the determined ordering."
  },
  {
    "clause": "reportErr != nil",
    "matches_determined_shape": false,
    "why": "CONTRADICTS. The fixture scripts TWO '## Stage Report: implementation' headings on the final entity (shared_fixtures_test.go:136 first round; :139 'append a second `## Stage Report: implementation` section'). FindSectionSpans (section_read.go:94-111) collects every fence-safe ATX heading whose exact text equals 'Stage Report: implementation'; with 2 matches it takes the len(matches) != 1 branch at :104-105 and returns (nil, error 'selector \"Stage Report: implementation\" matches 2 headings') \u2014 it errors on ambiguity exactly as on absence, and returns NO spans (nil, never a partial or first-match span). So on the conforming two-report entity reportErr is non-nil and this clause flags the scripted shape as failure."
  },
  {
    "clause": "len(spans) != 1",
    "matches_determined_shape": false,
    "why": "CONTRADICTS. On the conforming entity FindSectionSpans returns nil spans (len 0) alongside the ambiguity error, so this clause fires; and even under a duplicate-tolerant reader the determined section count is 2, not 1. The clause pins a single-report entity, which the fixture's own instructions forbid."
  },
  {
    "clause": "!strings.Contains(entity[spans[0].Start:spans[0].End], \"- DONE:\")",
    "matches_determined_shape": true,
    "why": "MATCHES on content, unreachable in practice. Both scripted implementation reports carry a '- DONE:' item (shared_fixtures_test.go:136, :139), so the content requirement agrees with the determined shape. But on the conforming entity this clause is never evaluated: Go's || short-circuits at reportErr != nil / len(spans) != 1 first (which also prevents a nil-slice panic on spans[0]); the check can only pass once the duplicate-section handling is repaired."
  }
]
