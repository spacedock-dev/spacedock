---
session-date: 2026-07-13
sequence: 1
first-commit: ca1f17e2
last-commit: 3c36d0fb
duration: unclear — boundary is approximate; this window overlaps a large unrelated parallel workstream (Codex/Zellij/Cowork runtime work), so first/last-commit bracket the dp-incident thread specifically, not the full session
---

# Session Debrief — 2026-07-13 #1

Focused debrief on the `fo-deferred-load-point-hunt-vs-skill-addressing` (dp) incident, per captain direction, rather than a full accounting of this window's commits. A prior Claude session spent an extended multi-round investigation (a standing advisory "science officer" pattern, repeated re-verification, adversarial second-guessing of its own findings) negotiating a revert of dp's Skill-promotion approach; the captain called that session's approach busywork, and the actual fix shipped afterward through different, faster means.

## Shipped

- **`fo-function-reference-invariant`** (88t) — merge:pr-merge:496. Replaced mutable step-number references (`Startup step 3`, `Dispatch step 2`, Event Loop step 0/3, Merge-and-Cleanup step 10, reuse-condition numbers) throughout the FO contract with named `«fn»` functions — an independent baseline of 51 fragile numeric references down to 0. Unrelated to the reference-loading bug itself, but landed in the same window and touches overlapping files.
- **The reference-loading fix itself** — confirmed live on `main`: `fo-merge-core.md` and `fo-write-core.md` are now `@`-referenced from `skills/first-officer/SKILL.md` (always loaded, reliable by construction, no separate callable Skill); `fo-smallest-sufficient-mechanism.md` was fully inlined into the shared core; `fo-dispatch-core.md` (the large ~2,386-word core) stayed a lazy, bare-path reference — the eager-vs-lazy tradeoff was resolved as "keep it lazy" for that one file. The entity that targeted exactly this mechanism, `fo-small-core-at-reference-imports` (m1y), is itself archived with `verdict: rejected` even though its target mechanism is what's live on main — the authoritative shipping commit is ambiguous between that entity's own work and 88t's PR; not fully untangled here, flagged rather than guessed.

## Rejected — the dp incident

- **`fo-deferred-load-point-hunt-vs-skill-addressing`** (dp) — rejected and archived, with no new stage report recorded beyond the one already seen mid-investigation (which recommended PASSED, "recommend the FO proceed to merge"). The Skill-promotion piece the captain rejected was never reverted *inside* dp — dp was simply closed once the clean replacement (`@`-reference imports, via m1y/88t) shipped through a fresh, narrowly-scoped entity instead.
- **`fo-contract-path-portability-sweep`** (fa) — also rejected, despite its own last validation stage report recommending PASSED ("Recommendation: PASSED. Validation independently reproduced the portability controls..."). No rejection reasoning is recorded in the entity body — reads as a bare captain-level call to close it out, not a worker-driven rejection.

## Non-PR commits (workflow-only)

Not enumerated individually. The state-checkout log in this window (~190 commits) is dominated by a large, unrelated parallel workstream (Codex resume/idle-monitoring, Safehouse/Zellij env passthrough, Cowork runtime detection, context-budget telemetry) that is out of scope for this focused debrief.

## Decisions

- Reject Skill-promotion (turning shared reference documents into separately-callable capabilities) as the fix for unreliable reference-file addressing. The side effect — making a file visible enough that the FO reaches for it at the wrong moment — was judged worse than the addressing bug it fixed.
- Use the established `@`-reference import pattern instead, applied where the eager-load cost is negligible (two small files).
- Accept `fo-dispatch-core.md` staying lazy-loaded (and therefore imperfectly reliable) rather than eager-loading a ~2,400-word file into every boot, including sessions that never dispatch.
- Close out `dp` and `fa` outright once a clean replacement existed, rather than continuing to rework their bloated multi-cycle histories.

## Issues — Workflow

None beyond what is already tracked in the 2026-07-11 debrief.

## Issues — Spacedock

None newly identified in this focused pass.

## Observations

- **The entity ballooned past the point of being worth rescuing.** Four implementation/validation rework cycles on dp, plus an extended same-session re-investigation (a standing advisory pattern issuing repeated re-verification requests, each individually reasonable), never changed the outcome — the entity still needed the exact revert-and-replace the captain had already decided on by instinct. The investigation validated a conclusion already reached, at real time cost, and converged more slowly than direct execution did afterward.
- **The actual fix, once it happened, was small and fast:** two files switched to an already-proven loading pattern, one file's tradeoff decided, done. It shipped through a *fresh* entity rather than by continuing to rework dp. This is the smallest-sufficient-mechanism principle already in this contract, applied to entity management itself, not just code — when a task's rework history has grown tangled and every new round just re-confirms the same direction, the cheaper move is closing it and starting the fix clean, not one more cycle on the original.
- **The specific failure mode to avoid next time:** treating "the captain disagrees with an approach" as a cue to dispatch more investigation (re-verify this claim, check that edge case, resolve this tradeoff) rather than as a decision to execute a bounded change and check it once. Each investigative round was individually well-reasoned; the sum was slow busywork relative to just making the change and verifying it directly.
- A captain-level rejection with no reasoning recorded in the entity body (both dp and fa) is a real gap for whoever reads this history later — there is no on-record account of *why*, only that it happened.

## What's Next

1. `fo-tier-delegation` (72) — still mid-rebuild from the prior session, now stale by ~35 commits and colliding with the very file that got restructured (the named-`«fn»` sweep and the `@`-reference consolidation both touched it). Needs a fresh design pass against the current shape, not a resume of the paused rebase.
2. `new-filing-exempt-from-write-scope-load` — untouched backlog.
3. `shallow-boot-before-greet-advance-contract-drift` — moved to implementation since the prior session left it in backlog; someone is already on it.
