---
session-date: 2026-06-03
sequence: 2
first-commit: 7dc3a9b9
last-commit: 7bb94bcf
duration: ~7h active (state-commit span) + a 6h idle gap during the live-CI wait
---

# Session Debrief — 2026-06-03 #2

The release-recovery + 0.19.5 test-themed-core session. Finished the v0.19.4 release (re-cut at the tip after root-causing a stale tag), took over the codex peer's lane, and shipped 5 entities to `next` under a 3-wide concurrent pipeline. The marquee finding: `yy`'s sonnet-flake fix passed offline + 3 detached-audit cycles but **failed its live AC-1** — the live-confirmation discipline caught a fix that changed the hang mode rather than closing it; filed `at` as the real follow-up.

## Shipped
- **f4** `codex-live-ci` — [#278](https://github.com/spacedock-dev/spacedock/pull/278). Enable Codex live CI by reusing shared runtime scenarios on top of r0 (inherited from the codex peer; merged on captain's "f4 is green, merge it").
- **kj** `split-root-onboarding-command` — [#279](https://github.com/spacedock-dev/spacedock/pull/279). One-command split-root onboarding `spacedock state new` (births the orphan state branch + linked worktree, inverse of `state init`).
- **0n** `reconcile-session-awareness` — [#280](https://github.com/spacedock-dev/spacedock/pull/280). `dispatch reconcile` auto-discovery was newest-mtime, poisoning roster classes from stale/parallel-session team configs; now matches `leadSessionId` against `$CLAUDE_CODE_SESSION_ID`, degrades to git-only.
- **8y** `real-shared-runtime-scenarios` — [#281](https://github.com/spacedock-dev/spacedock/pull/281). Extract f4's Codex scenarios into a host-neutral `sharedRuntimeScenarios()` table both Claude and Codex runners drive (inherited from codex peer).
- **yy** `sonnet-live-ci-flake` — [#282](https://github.com/spacedock-dev/spacedock/pull/282). FO terminal `TeamDelete`-no-retry hang fix (retry-to-success + settle + non-interactive cap-exhaustion). **Offline-complete but the live AC-1 FAILED — see `at`.**

Plus the **v0.19.4 GitHub Release** was published (re-cut the tag at `bce54524` after the session-10 tag sat at the pre-`RUNNER_TEMP`-fix `f676b955`; Homebrew cask + plugin manifests stamped).

## Filed (backlog)
- **0n** `reconcile-session-awareness` — shipped same session (#280).
- **yy** `sonnet-live-ci-flake` — shipped same session (#282); but live AC-1 failed → `at`.
- **ep** `ensign-contract-dev-leakage` — re-home dev-only discipline (TDD, code-only deliverables, "CODE only" worktree) out of the universal ensign shared core (ideation-done, queued).
- **fc** `launcher-binary-path-passthrough` — front-door launcher doesn't propagate its own `$0` to the launched claude/codex session; the in-session skill falls back to `$PATH`.
- **mt** `dispatch-build-json-ergonomics` — `dispatch build` needs a hand-built JSON blob; the contract's inline-`echo` example anchors the FO on shell heredocs instead of the Write tool (folded in `--print-schema`/`--validate-only`; StructuredOutput rejected — Workflow-only, returns to context not a file).
- **zd** `extract-team-orchestration-skill` — lift the generic ~10-step Claude-team lifecycle out of the FO contract into a reusable `using-claude-team` skill (it's a skill, NOT a binary command — team ops are Claude tools).
- **at** `non-interactive-teardown-exit` — the REAL sonnet-flake fix: non-interactive FO must hard-exit after a bounded teardown attempt; `yy`'s retry loops in `claude -p` and never exits. **Top priority next session.**

## Non-PR commits (workflow-only)
State transitions, feedback cycles, and scaffolding that didn't flow through a PR:
- `5f1fe6ab` + the 2→3 bump — `config: bump workflow stage concurrency default 1→2→3` (captain-directed; **direct README edits to `next`**, flagged as a scaffolding-guardrail judgment call).
- Feedback-cycle commits on `0n` (cycle 1), `8y` (cycle 1), `yy` (cycles 1 + 2) — detached-audit MATERIAL findings routed back to implementation; all closed.
- The v0.19.4 release was a tag re-cut (no new code commits) + the goreleaser-published Release.

All other session commits roll up into the shipped PRs above or routine state-machine churn.

## Decisions
- **Re-cut v0.19.4 at the tip, not the stale tag (FO + captain).** The session-10 tag was live at `f676b955` (pre-`RUNNER_TEMP` goreleaser fix), not deleted as the handoff believed — every run failed on the dirty-check. Re-cut at `bce54524` with the themed changelog; published.
- **Take over the codex peer's lane (captain: "take over from codex").** Absorbed `f4` (#278) + `8y` (#281); left `4n` (journey-cost-ledger). Persisted the peer's uncommitted `8y` entity.
- **Concurrency 1→2→3 (captain).** The README default was 1; raised to 2 then 3 to run a parallel pipeline. Surfaced that the throttle is only enforced in `status --next`, bypassable via manual `status --set`.
- **`yy` merge approach A — merge on offline+3×audit, confirm AC-1 live post-merge (captain chose A over B).** A retriggered n3/2a's live sonnet checks with yy's fix in the checkout; this is what caught the AC-1 failure. B (live-before-merge) was rejected due to the contract-source uncertainty.
- **A+C for the `at` follow-up (captain).** The real fix is the non-interactive hard-exit at the cap (contract) + the streamwatcher accepting best-effort-teardown-then-exit (test). `yy` kept as-is (its retry-to-success helps real sessions).

## Issues — Workflow
- **n3 #275 + 2a #277 remain blocked on the sonnet flake** — now waiting on `at` (the `yy` fix did not close it). Both are PR-pending (validation-PASSED, mod-block set), rebased onto current `next`.

## Issues — Spacedock
- **`yy`'s sonnet-flake fix is incomplete (→ `at`).** Offline + 3 audit cycles passed; the live cycle revealed the FO does the settle-then-retry loop (6 `TeamDelete` attempts) but reads the cap-exhaustion clause as "keep retrying" and never exits `claude -p`. Evidence: live runs `26891717026`/`26891718562`, artifact `runtime-live-e2e-claude-live-sonnet`. The audit's cycle-1 polish #3 predicted exactly this. Filed `at`. **Not a GitHub issue — tracked in-workflow.**
- **Reconcile session-awareness bug** — fixed + shipped (`0n` #280). Was actively hit this session (bare `reconcile` resolved a 2-day-old prior-session team).
- **FO/ensign ergonomics cluster** — `mt` (dispatch-build JSON), `fc` (launcher binary-path), `zd` (team-orchestration skill extraction). All captain-surfaced, filed.
- **Concurrency legibility** — a verified workflow change-list exists (status `--set` concurrency guard + `Stage.Concurrency()` accessor + per-stage occupancy line); NOT yet filed as an entity.
- **The detached audit found MATERIAL on EVERY high-stakes entity** (`0n`, `yy`×3 cycles, `8y`) — a vacuous assertion, unpinned exactness, oracle/prose drift, an isolating-negative-case gap. None are spacedock bugs; they're the audit lever working.

## Observations
- **The live oracle is non-negotiable for runtime-observable ACs — proven the hard way.** `yy` passed offline + 3 detached-audit cycles and STILL failed live. Offline proof, however exhaustive, cannot substitute for the live run on a producer-side FO fix.
- **The audit lever held at 100%** on high-stakes surfaces again — every validation PASSED, every audit found a real test-strength hole. Validation-green is necessary, never sufficient.
- **Supersede-then-redispatch with the same ensign name is fragile** — cooperative shutdown is slow, so a re-dispatched same-name validation ensign briefly coexists with the terminating one. It worked (distinct instances; routing/budget probe pick the newest), but a `-cycleN` suffix would be cleaner.
- **Recurring footguns:** (a) `git pull --rebase` before committing staged state changes → "please commit or stash" (commit FIRST); (b) `./spacedock` binary vanished mid-session once (cause unknown; rebuilt); (c) concurrent ensign state-pushes can spuriously fail the FO's `pull --rebase` ("Cannot rebase onto multiple branches") — retry/explicit-fetch fixes it; (d) the dispatch-build JSON heredoc trap (use Write).
- **`gh pr merge --delete-branch` always fails the local delete** when the branch is checked out in a worktree — harmless, but noisy; the worktree-checkout-before-delete guard (surfaced by the roadmap-readiness audit) would fix it.
- **The binary-simplification roadmap's verified high-value ranking** (workflow `wxdk8ctgt`): #1 `pr complete`, #2 `state commit-and-push`, #3 ensign-side report/signal, #4 `dispatch advance`; `gate verify` demoted (its AC-extractor doesn't exist in `validate.go` — blocked on `2a`); `reconcile --act` hard-blocked by the (now-fixed) `0n`.

## What's Next

**MUST resume (the real sonnet-flake fix):**
1. **`at` `non-interactive-teardown-exit`** — TOP priority. Spike the `claude -p` clean-exit mechanism FIRST (the exact thing `yy` got wrong), then the contract hard-exit-at-cap (A) + streamwatcher best-effort-teardown-then-exit (C). Unblocks n3/2a. `yy` stays as-is.
2. **Then retrigger n3 #275 + 2a #277 live sonnet** (approve CI-E2E env) to confirm `at`'s fix + merge both. They're rebased onto current `next`, validation-PASSED, mod-block set.

**0.19.5 queued (ideation-done, ready for implementation):**
- **ep** `ensign-contract-dev-leakage` — independent prose surface (skills/ensign/*.md + first-officer-shared-core.md "CODE only").
- **6b** `terminal-guard-rejected-consistency` — internal/status + first-officer-shared-core.md prose.
- **am** `ensigncycle-streamwatch-test-only` — the `streamwatch.go`→`*_test.go` rename; impl must also flip the `live_budget_test.go:28` filename literal.
- Sequencing: `ep`/`6b` both touch first-officer-shared-core.md (different sections, low conflict); `am` touches internal/ensigncycle. All clear to dispatch now that yy/8y/0n merged.

**Filed, awaiting prioritization:**
- **fc** launcher-binary-path-passthrough, **mt** dispatch-build-json-ergonomics, **zd** extract-team-orchestration-skill — the ergonomics cluster.
- Concurrency-legibility entity (not yet filed; verified change-list ready).

**Parked / peer:**
- **44** bundle-asset-distribution, **5w** notarize-macos-release, **ng** roborev-validation-hook — parked.
- **4n** journey-cost-ledger — codex peer's lane.
