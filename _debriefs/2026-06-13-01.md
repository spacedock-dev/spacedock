---
session-date: 2026-06-13
sequence: 1
first-commit: 45401127
last-commit: 541445b0
duration: ~multi-hour (sim-dated 2026-06-12 → 06-13)
---

# Session Debrief — 2026-06-13 #1

Shaping-FO session. Drove the **#343 docs-site reviewer-revision to merge** (captain-steered, three interactive ensign cohorts), then **shaped, staff-reviewed, gate-approved, and packaged two full sprints — 0.20.1 and 0.20.2** — under the roadmap sprint operating principle, and handed both to Commander cold-boot drives (a Commander began driving in parallel, visible in the shared state checkout: members in implementation/validation, PRs #347/#348 opened). Mid-session the captain corrected the FO seat onto **`main`** (post-flip Model B trunk); ran the whole session under the **SSH-push outage** (gh-HTTPS reroute).

## Shipped
- **pt0** `docs-site-feedback-revision` — [#343](https://github.com/spacedock-dev/spacedock/pull/343). Captain-steered interactive revision of the mkdocs docs site (simplify / dedupe / fix rendering per reviewer feedback); squash-merged to `main` (`55158746`), terminalized PASSED + archived. Three ensign cohorts (Phase-2, then `-c2`/`-c3` after a model-issue replacement); doc-diff cut published pages −893 lines / −64% words.

## Filed (backlog)
- **gja5…** `startup-sandbox-status` — show sandbox state on startup + `--version` + per-runtime detection (0.20.1 `gj`).
- **zrcm…** `non-sandboxed-launch-auto-mode` — non-sandboxed launch uses auto-mode (0.20.1 `zrc`).
- **8pwj…** `brew-cask-agentsview-safehouse-deps` — cask `depends_on agentsview` (0.20.1 `8p`).
- **ywjf…** `readme-and-docs-architecture-cleanup` — site canonical, remove `install-journey.md` + its prose-grep test, slim README (0.20.1 `yw`, sprint doc-owner).
- **ndpf…** `prefer-new-over-next-id` — `--next-id` hint + contract teaches `spacedock new` (0.20.2 `nd`).
- **tdpn…** `mdschema-conformance-validator` — full mdschema `--validate` (0.20.2 `td`).
- **9hbm…** `survey-value-prop-legibility` — live-run feedback (Tom/Heavybit); plain value, manual-not-mechanical, threads-to-pull. Subsumed into `5wv`.
- **5wvr…** `survey-output-redesign` — coalesces the six survey-feedback seeds into ONE "value & numbers first" skill rewrite (0.20.2 headline).
- **ci-pr-gating-on-main** — Commander-filed during the 0.20.1 drive (main-PR CI gating, #348).

## Non-PR commits (workflow-only)
Process docs on `main`/`next` (FO-owned roadmap), beyond the routine state machine:
- Sprint shaping: `roadmap(0201)` scope-lock + staff-review + package; `roadmap(0202)` shape → chosen output structure → coalesce → broaden → staff-review + package; `bring 0201/0202 docs onto main (trunk)` (`26290562`).
- `workflow(dev)`: ideation now proposes doc diffs for UX-affecting tasks (`53901ede`).
- Coalesce/carve: six survey seeds → `5wv` (deferred as requirement bands); cleanup group `nd`/`td`/`5ar`/`gf` into 0.20.2.

## Decisions
- **FO works on `main`, not `next`** (captain, post-flip Model B trunk): roadmap docs + sprint packages live on `main` so a Commander basing on `main` finds them. The `next`/`main` reconcile is **deferred until after the marketplace decouple `gp`** (it establishes the clean topology).
- **0.20.1 scope-lock = decouple-first**: `gp` (marketplace repo + pinned channels) leads; `ez` stamp-then-tag + `tw`/`qp`/`w6` deferred. Gates approved → Commander.
- **0.20.2 coalesced + broadened**: the six survey seeds rewrite ONE skill → one task `5wv` (anti-collision); broadened to "survey + cleanups" with `nd`/`td`/`5ar`/`gf`. Chosen survey output structure = **value & numbers first** (captain, with mocks). Gates approved → Commander.
- **`yw` owns the whole 0.20.1 doc surface** (same anti-collision logic): the five behavior members touch no docs; `yw` documents them on the canonical site + removes `install-journey.md` + kills its prose-grep test.
- **`readme-and-docs-architecture-cleanup` + `mdschema-conformance-validator`** filed off the captain's observation that the doc site (now shipped) makes `install-journey.md` redundant and `install_doc_test.go` a banned prose-grep.

## Issues — Workflow
- **Ideation ensigns linger in the roster** — `reconcile` doesn't flag them Class-A while their entity sits at `status=ideation` (gate-approved), so they need manual shutdown after gates/folds. Captain caught a batch of ~10.
- **Ideation spikes created stray worktrees** on `gj`/`zrc`/`8p` (`.worktrees/spacedock-ensign-{slug}` + a stray `worktree:` frontmatter field) though ideation is non-worktree; the `worktree=` clear is guard-blocked (reads as a terminal field) → the 0.20.1 package directs the Commander to `--force`-clear before fresh dispatch.

## Issues — Spacedock
- **The reconcile `E` class ("reset main→next") is wrong post-flip** and fires every boot — known noise; the `main`/`next` divergence is legitimate (`next` = roadmap docs, `main` = docs site, disjoint). Not filed (folds into the `gp`/topology work).
- **CI gates fire only on PRs to `next`, not `main`** — `main` PRs are ungated under the trunk model; the Commander filed `ci-pr-gating-on-main` (#348) to close it. Not separately filed.
- **`python3`/`sed` transiently dropped off PATH once** (sandbox hiccup; `uv` available as fallback). Environmental, not filed.

## Observations
- **The preflight staff review earned its keep 3×** — caught real Material every time, against real code, never the entity's self-report: `zrc` M1 (missing 7-oracle blast-radius co-edit → would ship RED believed-green), `yw` M1+M2 (retarget map cited site anchors that don't exist on the slim #343 page), `5wv` M1 (a lede figure that redefined itself to dodge a required new query, contradicting its own spike + the locked mock). Never skip it.
- **Coalesce members that rewrite ONE artifact** — `5wv` (6 survey seeds → one skill) and `yw` (the sprint's whole doc surface). N worktrees on one file is the collision the captain flagged twice; consolidating to a single owner is the fix.
- **The survey is the single-repo prototype of the 0.21.x decision-frontier.** Tom Drummond's (Heavybit) live run sharpened it: lead with plain value + concrete numbers, "manual" not "mechanical", and *"threads to pull, not threads you've lost — steady-state of what's there now, not how I got there; historic decisions you didn't choose pollute the context."* That last is a load-bearing **0.21.x decision-abstraction** input.
- **`spacedock new`** (atomic id-mint + write) is the blessed filing path now (dogfooded this session); seed `nd` makes the contract teach it + adds a `--next-id` hint.

## What's Next
- **Both sprints are driving (Commander, parallel session, off `main`):** 0.20.1 (`gp`/`te`/`gj`/`zrc`/`8p`/`yw`) — several members already implementation/validation, PRs #347 (brew) / #348 (ci-pr-gating) open; 0.20.2 (`5wv`/`nd`/`td`/`5ar`/`gf`) — implementation. **`5ar`#5 node-action bump has a ~2026-06-16 deadline** (package says first). The shaping FO handles escalations + the Close step (seed-next) when each cuts.
- **Deferred (post-`gp`):** `next`/`main` reconcile; README "full docs" URL (`yw`); the stray-worktree `--force`-clear (Commander).
- **Owed:** CL → Tom his redacted transcript (live survey-feedback run).
- **Next big shaping: 0.21.x decision-abstraction** (decision-event model + ready-room + `orient`) — grounded in `spacedock-ui-research/` + Tom's steady-state/threads-to-pull input; research says defer the rich UI (0.22.x mcp-app), start cheap.
- **Backlog (unsprinted):** `j9`/`ey`/`vc`/`44`/`bw`/`95`/`5wc`/`82`/`xp`/`6a`/`vv`/`e6`.

_Handoff prompt: `/tmp/spacedock-fo-handoff-2026-06-13.md`._
