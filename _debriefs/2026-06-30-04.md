---
session-date: 2026-06-30
sequence: 4
first-commit: cab0f47b
last-commit: 701f0bb5
duration: ~5h25m (carve → 6 cohort merges)
---

# Session Debrief — 2026-06-30 #4

The 0240-lean-contract sprint, driven end-to-end. Shaping FO this session: carve → ideate all 8 members → independent multi-lens preflight staff review → fold all 10 Material findings → package the Commander dispatch. A cold-booted Commander then drove the cohort from the package, landing 6 of 8 members (#447–#452); `k4` is in implementation and `z4` in ideation (Wave 3, after `k4`). Separately, the standalone usability mustfix `0qt` was driven to ship (#446). Two live-lane CI-harness flakes were diagnosed and filed. (Written to the split-root state checkout per convention; the `7d4` fix that makes this automatic landed THIS session as #449 but the running skill is the pre-fix one.) Parallel to #03's 0230-stable / v0.23.0 thread.

## Shipped
- **48g** `ac-scan-value-annotation-skip` — [#447](https://github.com/spacedock-dev/spacedock/pull/447). `status --ac-scan` now enumerates value-annotated AC headers (`**AC-1 (VALUE)**`), so the deterministic gate cross-check no longer silently drops the value AC.
- **f5** `journeymetrics-ensign-read-adoption` — [#448](https://github.com/spacedock-dev/spacedock/pull/448). Folds the dispatched-ensign sub-agent transcript into the journeymetrics `--read` adoption metric and normalizes launcher recognition; `82k`'s measurement predecessor.
- **7d4** `debrief-split-root-state-home` — [#449](https://github.com/spacedock-dev/spacedock/pull/449). Routes debriefs to the split-root state checkout (consumes `entity_dir` from `status --boot --json`, halts on an absent checkout) — fixes this very debrief-location bug.
- **82k** `read-guidance-redundant-with-grep` — [#450](https://github.com/spacedock-dev/spacedock/pull/450). Trims the redundant `status --read` section-read guidance and names grep the primary section-locator across the FO + ensign cores.
- **scr** `ensign-contract-dev-leakage` — [#451](https://github.com/spacedock-dev/spacedock/pull/451). Stage-neutralizes the universal ensign core (drops dev stage-name parentheticals) + adds a quarantine-legal regression guard; the seed's markers were already shipped by #290.
- **84** `entity-status` — [#452](https://github.com/spacedock-dev/spacedock/pull/452). Defers the Status Viewer + Issue Filing reference out of the boot-resident FO core into a lazily-loaded reference, content-carriage guarded.
- **0qt** `dispatch-build-team-name-advisory` — [#446](https://github.com/spacedock-dev/spacedock/pull/446). Warns when `--team-name` selects the legacy dispatch shape on host=claude and documents the shape flags in `dispatch build --help` (standalone mustfix, driven separately from the cohort; admin-override merge over unrelated flaky live lanes).

## Filed (backlog)
- **2wb** `claude-live-fo-wrong-root-env-leak` — the sonnet live-lane FO boots the WRONG root (`/home/user/spacedock-workflow`, `/tmp`) because a CI env leak lures it off its launch cwd; opus passes the identical suite, so it is sonnet-lane env/model-specific. Blocks the sonnet live lane for any PR.
- **tt4** `codex-live-rejection-flow-flake` — `codex-live` rejection-flow intermittently asserts the addressable-worker characterized ABSENT while the transcript shows a reuse `send_input`; the scenario itself succeeds. The handoff-named codex flake.
- _(Also re-stamped INTO 0240 this session, not newly filed: `f5` (from 0204) as `82k`'s predecessor; `48g` as the 8th member.)_

## Non-PR commits (workflow-only)
Roadmap/strategy docs on `main` (the Shaping-FO artifacts; not entity PRs):
- `cab0f47b` carve the 0240 sprint `index.md` (goal/DoD/sequencing).
- `28457bcc` `staff-review.md` — independent multi-lens preflight review (verdict SOUND-WITH-FIXES, 10 Material findings).
- `7a73ee1d` fold the 3 carve-level staff-review findings (AC-4 two-part re-scope; scr scope/#290 prior art; k4 boot-sweep command corrected).
- `2ef249e9` `dispatch-sprint-execution.md` — the Commander cold-boot package.

Ideation, fold, and validation stage reports live on the `spacedock-state/dev` branch (per-member `state:`/`ideation:`/`validation:` commits), not on `main`.

## Decisions
- **Ideate-first ordering (captain correction).** The roadmap lifecycle is ideate → preflight staff review → gates; the FO initially proposed a sprint-wide staff review *before* ideation. Corrected: each member ideated first; the independent review followed; gates locked after folding its findings.
- **Pulled `f5` into 0240 as `82k`'s measurement predecessor** — `82k`'s value-AC measures `--read` adoption, which is a vacuous `0==0` until `f5` folds the ensign transcript (and widens launcher recognition).
- **Pulled `48g` in as the 8th member** — the `--ac-scan` value-annotated-AC bug had blinded the FO's own gate cross-check this session.
- **`0qt` classified as a standalone usability mustfix**, driven separately from the cohort (its own review track).
- **`z4` #6 registry = four-entry** (Dispatch, Merge, Status-Viewer, Write-Scope), with all four load-points + greet-guards proven to survive.
- **Independent multi-lens staff review (3 lenses + synthesis, opus-4-8)** found 10 Material findings — all folded before gates locked. Headline catch: `k4`'s load-bearing sweep-independence spike cited the wrong boot-sweep command (`merge guard` vs the actual `status --set`/`--archive`); conclusion held, evidence re-pointed.
- **`0qt` implementation placement = option B** (success-path; the advisory fires only when the legacy envelope is emitted) — FO call; avoided weakening two existing `HasPrefix(stderr,"error:")` tests + minimized golden churn.
- **`0qt` merged via admin-override** — 3 live lanes failed on issues orthogonal to a claude-only, frozen-envelope change (opus + pi + deterministic green); captain authorized the bypass.

## Issues — Workflow
- None blocking; the Wave-1 trims (`82k`, `scr`) came back thinner than planned (`82k` a small trim with a measurement tail; `scr` largely delivered by #290) — consistent with "the contract is already lean."

## Issues — Spacedock
- `dispatch build` `--team-name`/`--bare-mode` foot-gun (silent legacy shape on claude; undocumented in `--help`) — **FIXED #446** (`0qt`).
- Debrief skill wrote to the definition dir, not the split-root state checkout — **FIXED #449** (`7d4`); the running skill is still the pre-fix one (hand-routed this debrief).
- `status --ac-scan` blind to value-annotated ACs — **FIXED #447** (`48g`).
- Sonnet live-lane FO boots the wrong root (CI env leak) — **filed** `claude-live-fo-wrong-root-env-leak`.
- Codex rejection-flow worker-characterization flake — **filed** `codex-live-rejection-flow-flake`.
- `merge guard` requires `--verdict {passed|rejected}` — not obvious from the contract prose (cost one round-trip).
- The pr-merge audit-link template uses the code-SHA + a state-relative path → 404s for split-root entities (the entity lives on the state branch); used the `spacedock-state/dev` branch link instead. Worth a mod fix.
- Transient `gh` `401` mid-session (self-recovered) — environmental.

## Observations
- Ideate-first earned its keep: the per-member spikes surfaced real dependencies (`f5`'s `0==0` vacuity, `scr`'s #290 prior-art, `k4`'s binary-self-authorizing sweep) that a pre-ideation review would have under-weighted.
- The independent staff review was high-value: 10 Material findings on a "ready" sprint, including a wrong-command citation in the load-bearing `k4` spike and an under-coded reachability oracle (`k4` later spiked the dangling-pointer guard RED-then-GREEN).
- Live-E2E lanes are flaky/env-fragile (sonnet boot-cwd env-leak, codex characterization). They blocked a verified low-risk change and forced an admin-override; the two filed issues track the harness fixes. The sonnet env-leak will block *any* PR's sonnet lane until fixed.
- The package handoff worked end-to-end: a cold-booted Commander drove 6 cohort members to merge from `dispatch-sprint-execution.md` while the Shaping FO handled `0qt` in parallel.

## What's Next
- **`k4`** `defer-write-scope-id-styles` — in **implementation**; drive → validation → merge. The last Wave-2 member; its new `references/fo-write-core.md` + the prose-pointer oracle (`TestDeferredReferenceProsePointersResolve`) unblock `z4`.
- **`z4`** `fn-binding-refinements` — in **ideation** (gate-approved); implements **last** (Wave 3, after `k4` — its four-entry registry `os.Stat`s the ref files `84`/`k4` create). Carry: AC-6 needs ONE live re-run; AC-4 is two-part (claude-fo-dispatch.md exempt); the four-entry registry.
- **0.24.0 cut** — after `k4` + `z4` land: `go test ./...` green, the pre-cut antipattern audit, then stamp→tag→publish→cask→`stable` per `docs/releasing.md`. **Watch the `q3` e2e-gate trap**: a re-run-to-green Live-E2E run is invisible to the gate's `--status success` filter — land a clean first-attempt-green run for the tagged SHA, or fix `q3` first.
- The two CI-harness flakes (`claude-live-fo-wrong-root-env-leak`, `codex-live-rejection-flow-flake`) — a future sprint; the sonnet env-leak is the more urgent (blocks the sonnet lane broadly).
- 0.25.0 candidates still held: `fo-opus-behavioral-robustness`, `minor-version-compat-coupling`.
