# Handoff 2026-08-16-01 — live coordination state (pre-compaction snapshot)

## PRE7 SHIPPED (2026-08-17 ~16:0x, supersedes everything below)

v0.27.0-pre7 TAGGED AND PUBLISHED: tag on 04e936a12 (stamp commit on merged main), e2e-gate satisfied by run 32040773989 attempt 2 — claude 17/17 on the release SHA (attempt 1), codex 17/17 (attempt 2 after ONE keep-moving-violation flake; unowned on codex at one occurrence, on the filing list; rerun-to-green per the release doc). release.yml run 32043792018 publishing (goreleaser + homebrew + edge reconcile). Release worktree/branch cleaned. REMAINING: session debrief, deferred local-refit, captain filing list, v0.27.0 stable after dogfood.

## MERGED (2026-08-17 ~15:1x, superseded above)

STACK #720 MERGED to main: merge commit 61dd8e435 (all 19 stack commits; full offline+race green on the merge tree). Registry note: dropped main's rejection-flow AUDIT block (recorded resolution) AND the auto-continue finding-3 block (described the codex-only wiring #723 removed; the tip run's true positive proved the host-neutral detector). zq gate attempt-4 captain-approved via binding review (digest artifact in _reviews/zq-validation-gate-review.md; attempts 2-3 withdrawn for presentation reshaping on captain orders — presentation lesson: lead with what the captain cares about, one page). ALL SIX ENTITIES done + archived via merge guard: lander #718 sentinel pr-merge:718; #719/#721/#722/#723/#724 closed as merged-via-stack with sentinel local-merge:61dd8e435 (each branch head verified ancestor of main first).

PRE7 CUT IN FLIGHT: release commit 04e936a12 (stamp on main), manifest-tag-gate green, Runtime Live E2E dispatched = run 32040773989 (lane auto-approve + completion watcher bbmg4iaq5). Changelog draft: ~/.claude/jobs/4e49247e/tmp/pre7-changelog-draft.md. ON GREEN: git tag -a v0.27.0-pre7 -F <changelog> 04e936a12; show captain; push tag (publishes: goreleaser, homebrew casks, stable ref stays untouched for pre tags, next reconciled + calendar bump). ON RED: attribute the failing lane from artifacts, then rerun-failed per the release doc's sanctioned re-run-to-green.

INCIDENT (repaired): post-merge worktree cleanup glob was overbroad — removed clean worktrees of 5 non-terminal entities (boot-identify…, define-fo-moving-target…, preserve-pi-terminal…, repair-codex-smallest…, shallow-boot…); dirty ones refused and survived; git branch -d deleted only merged branches. All 5 restored at their recorded paths on their exact prior heads (deleted branches recreated from logged SHAs). No committed work lost. Lesson: scope cleanup to the exact slugs being finalized, never `grep ensign`.

REMAINING ARC: session debrief (spacedock:debrief), deferred local-refit entity, captain filing list (unchanged), v0.27.0 stable after pre7 dogfood.

## CURRENT STATE (2026-08-17 ~05:1x, superseded above)

STACK #720, six layers, all OPEN, tip = PR #724 head 9b1c86a60 (fixture pin on 43f7537b0): main → #718 (skill rewrite) → #719 (hz repair + nested fix) → #721 (workflow-fit) → #722 (initial-gated-stage) → #723 (auto-continue) → #724 (team-mode rejection + final revise + Cycle-line target pin).

GATES: five entities approved-awaiting-merge by captain approve-all (18, hz incl. all ratified deviations, b8, ig hold-released at attempt-2, auto-continue). zq: validation-gate attempt-1 recorded REVISE (ratified the revise round, both tolerance rulings: self-correction ACCEPTED, heading STRICT+PIN); revise landed; PLUS second revise item 9b1c86a60 (captain "fold and rerun" 2026-08-17, recorded as entity Amendment 3, state commit 0f915a4f9). NEXT: its validator (addressable: spacedock-ensign-run-rejection-journey-in-team-mode-validation) delta re-verifies 52b94f9aa..9b1c86a60, flips REJECTED→PASSED, FO prepares gate attempt-2, presents via /r, captain approves+consumes → all six awaiting-merge.

CI HISTORY on 43f7537b0 (run 31991864922): attempt 1 claude FULLY GREEN (first ever), codex 16/17 (filing journey one-variant miss — greened on attempt 2, stands at one unowned occurrence, no filing). Attempt 2 codex red on REJECTION-FLOW: attributed from stream bytes — the codex FO drove the whole repaired journey correctly (reuse topology exact, round recorded entries=4, gate prepared/open) but appended the Cycle line to the workflow README.md; one write → both codes (rejection-cycle-line + rejection-round-missing via the README-bytes boundary check). Root cause: fixture never named the target file ("this section"); escalation sibling does. Fix: 9b1c86a60 pins `rejection-task/index.md` (fresh-dispatched correction worker — original implementer no longer addressable; offline+race green, gofmt clean, FO diff read-back exact).

CI RUN 31996696789 on tip 9b1c86a60 CONCLUDED RED (mixed): codex lane 17/17 FULLY GREEN — first ever, rejection-flow and filing both green on the pinned fixture. Claude lane 15/17 with two NEW reds, both attributed from stream bytes: (1) auto-continue `human-gate-bypassed` — TRUE POSITIVE, live sonnet FO ran `gate record --decision approve --actor person:captain --consume` with no conn grant, then SELF-CONFESSED in its final message and held; the #723 detector worked exactly as designed; model violation, not harness. (2) rejection-flow `rejection-round-missing` — REAL red, LYING LABEL: the FO DID invoke `gate record --round validation/1` (stream line 287, is_error=false) but EARLY — before routing the correction (rework spawn line 309) — so the immutable round room holds entries=2 not 4; the recognizer's success regex pins `entries=4`, making early-record indistinguishable from never-invoked. Grade correct, diagnostic dishonest. MATRIX FACT: no single run has landed all 34 journey-lane cells green; each of three runs flipped 1-2 different cells, all model-adherence variance honestly caught (codex filing a1; codex Cycle-line placement a2 — fixed by pin; claude bypass + claude early-record r3). Composite evidence: claude 17/17 on 43f7537b0 (delta to tip = the two-line fixture prose pin), codex 17/17 on 9b1c86a60. CAPTAIN DECIDES: reroll for a single all-green run vs accept composite as green-tip-equivalent vs harden further. FILING-LIST ADDITIONS: round-oracle README-immutability condition reports under `rejection-round-missing` (label nit); recognizer entries=4 conflation (early-record labeled never-invoked); sonnet auto-continue bypass mode needs a product-task owner if tolerated; claude rejection early-record mode likewise; boot-preamble find sweep seen again (audit finding 6). ON EVENTUAL GREEN/RULING: zq validator (range 52b94f9aa..9b1c86a60) → gate attempt-2 → captain → ATOMIC STACK MERGE; at merge, shared_live_runner_test.go CONFLICTS with main's AUDIT annotations (df0bd50d9): stack's version wins, drop ONLY the rejection-flow AUDIT block; then merge guard <slug> --verdict passed per entity. Never blind-rerun.

POST-MERGE ARC: v0.27.0 stable ritual per docs/releasing.md (changelog carries the gate/resolution story + dogfood numbers 392/282/319); deferred local-refit entity; session debrief. CAPTAIN FILING LIST (all recorded, none filed): CLAUDE_CONFIG_DIR spin-off (operators lose reuse/teammates/reconcile silently; three blockers in zq's entity), status --read misses worktree reports (auto-continue entity addendum), H4 substring asymmetry in feedbackCyclesSection (zq's last report; "declared level or deeper"), journey-metrics entity 3rdq29x1 (filed, undispatched), audit findings 4/5/6/9/13 (0.28 ledger), pi-dialect extractors (0.28).

MECHANICS THAT SAVE TIME: /r presentations need a briefing COPY with a context node + pinned bytes extracted via git show (gate prepare's briefing lacks 0.14.0's required context field; git-root:// unresolved — full recipe /tmp/subspace-r-friction.md); binding reviews launch via review-zellij with run_in_background and results read from the task output file; CI lane approvals via gh api POST .../runs/<id>/pending_deployments with environment ids; pushes via SSH git@github.com:spacedock-dev/spacedock.git; the state checkout is SHARED with live ensigns (path-scoped commits only, never stash, leave peers' dirty files); cd absolute paths every Bash call (cwd resets bite); zsh: no bare `===` echo args.

Written by the FO at the captain's compaction check. Durable state (gates, archives, README rules, releases) needs nothing; this file carries only what lived in conversation.

## In-flight workers and their couplings

- **hz (repair-codex-rejection-round-recording, implementation)**: code committed e870a2fd6 on spacedock-ensign/repair-codex-rejection-round-recording (base 0c6a2c32a). Running the targeted codex loop toward 5 consecutive greens; a green counts ONLY with the in-stream inline-commit line and zero pre-fix no-op strings. Surface overrun (7 files, +452, test-density) is FO-dispositioned keep-all-guards, rides as a declared deviation for validation. Next: stack PR onto main, stage report. TWO MORE DECLARED DEVIATIONS (FO-authorized at the review-finding checkpoint, captain ratifies at hz's validation gate): (a) recognizer quoting fix — codex emits a multi-character quote run (`'"'`) before artifact paths; `rejectionRoundArtifactArg` in internal/ensigncycle/shared_round_recording_test.go allows one optional quote and false-negatives a successfully recorded round as `rejection-round-missing` (FO verified from source + raw run-1 bytes; `directRoundLauncher` already uses the `['"]*` idiom). Authorized scope: `['"]?`→`['"]*` in the helper and the two sibling patterns in `commandRecordsRejectionRound`, plus a regression case with the verbatim run-1 command bytes. (b) loop scoring — reds where the FO never invokes `gate prepare` outside doc reads (both rounds recorded, no pre-fix strings) are out-of-scope: logged, neither count nor reset; contingency XFAIL applies only if in-scope reds persist. Streak restarts from the first run on the fixed tree; MAX_RUNS=12 stands. CONFOUND CORRECTION (ensign-discovered, FO-accepted): the early loop runs drove bare codex defaults — CI resolves codex via a PATH shim injecting the CI model + reasoning effort (runtime-live-e2e.yml ~355-388; runner uses LookPath). Those runs are discarded as gating evidence, preserved as a labeled dataset (bare-default-model-runs/); loop restarted under a verified shim replica and only shimmed runs count. ROOT CAUSE FOUND (ensign, FO-verified against cli.go:181 usage + live error + provenance diffs): the rejection-flow red is near-deterministic, not a flake — three independent defects on the journey path: (1) SKILL.md documents the round recorder WITHOUT the entity operand (`gate record --round ...` vs actual `gate record <entity> --round ...`; operand dropped by ea6723acb 2026-08-03), so a by-the-book FO gets a usage error on every attempt (4/4 live, both configs); (2) the skill's invoke-once/hold-on-failure language (d05730564 2026-08-09) converts that usage error into a terminal hold; (3) the dirty-tree dead end — hz's fix, CONFIRMED LIVE in CI-config run 1's stream (gate prepare exit 1 "selected source differs" → state commit exit 0 inline-commit line → gate prepare exit 0 state=open; the FO reached it by deviating from the skill and retrying with the operand). FO RULINGS (all declared deviations, captain ratifies at the respective gates): defects 1-2 routed to 18 as binding requirements (operand in the documented command; usage-error≠hold wording; provenance recorded; discovery attributed to hz's ensign) — 18's layer is now load-bearing for the journey AC; hz's contingency XFAIL is NOT to be applied (would be the ceremony the captain ruled against); hz's layer proof re-scoped to mechanism confirmation (run-1 chain cite + tests + quoting regression), with journey-level full-matrix green proven on the composed tree at the stack tip (atomic stack merge = shipping vehicle); LOOP CUT EARLY (captain-driven): the pre-rebase loop measured known routed defects on a never-ship base — stopped mid-collection; shimmed runs kept as a labeled dataset; the ONLY loop that matters is the composed-tree loop (task #15). CI approval policy: live lanes approved ONCE, on the stack tip (hz's PR), after the local composed loop is green — none spent on #718 alone. CAPTAIN PRE-AUTHORIZED (2026-08-16, "authorize tip ci when local is green"): the FO fires the tip's environment approvals (gh api pending_deployments POST on the queued Runtime Live E2E run) as soon as hz reports local composed green + PR open — no further ask. RESOLVED: the needs-preparation mechanism dispute closed - bullet tokens (DONE/SKIPPED + evidence + Summary), no review semantics; the 18 ideation's verdict-field claim is formally retracted (entity commit 00a86e53a); hz's AC-1 unaffected. Consequence for 18: FO-authorized one-clause addition binding step 5's gate preparation to step 4's re-review verdict (implementation-discovered-truth deviation class; captain ratifies at 18's validation gate).
- **18 (simplify-feedback-rejection-flow, implementation)**: IMPLEMENTATION COMPLETE — code 884b55af0+0e8dd3c49 on spacedock-ensign/simplify-feedback-rejection-flow (base 0c6a2c32a), state report d1d79dba2 pushed, go test plain+race green. The rewrite ships the ENTITY operand (defect 1) and the usage-error≠hold wording (defect 2); ensign exercised the operand defect itself (exit 2). Four FO dispositions issued: handoff-channel clauses confirmed authoritative (identical to direct relay); gate must state plainly the five-step shape alone would not have fixed the journey; step-5 retraction handling correct; overrun KEEP ruling (publication counter + five inverted cases stay — falsifiability outranks line estimate; final figures at ba91b333f: 4 files +285/-78 net +207 TOTAL, of which shared_round_recording_test.go is +261/-67 net +194 vs its ~45 estimate — earlier "+195" was the then-total misrecorded as per-file, 18-corrected; skill +105 words wholly the three FO-authorized clauses, the approved +107 hit exactly by the transcribed design). **CRITICAL COUPLING: live loop and stack PR HELD until hz's layer lands — the FO must signal it (task #20, blocked by #15).** Rebase guardrail issued: both layers changed shared_round_recording_test.go (hz c24eb0e89 quoting fix +40/-2 vs 18's reshape) — the rebase must preserve hz's `['"]*` tolerance and verbatim-bytes regression case, and run recognizer unit tests on the composed tree before the live loop. REBASE MECHANICS (18-recorded in task #20): 18's reshape extracted one of hz's two sibling patterns into var `rejectionRoundEntity` — hz's edit re-applies to the extracted var, not a textual merge; 18's own NEW round-value regex already uses `['"]*` with a table case (ba91b333f, FO-approved initiative), and 18 deliberately left both authorized siblings at `['"]?` to avoid colliding with hz's lines. PROVENANCE CITATION CORRECTED (18-verified by reading shipped text per commit): 00f8c203f carries no recorder command line — the prior-correct witness is c355fbe44 (2026-07-23) alone; relayed to hz for its stage report. Beware naive greps on that file: fixture examples DO name the entity; the step line is what an FO follows. Boundary (FO-confirmed): hz owns binary+grading, 18 owns skill+oracle-reshape, hz lands first.
- **comm-officer**: standing, idle.

## Board snapshot (2026-08-16 ~19:30, post-gate-sprint)

Stack #720: #718 (skill rewrite) → #719 (hz repair) → #721 (fo-workflow-fit, VALIDATION-APPROVED awaiting-merge) → #722 (initial-gated-stage, validation gate HELD by captain pending layer-2 fix + restack). Gate presentations run via /r (subspace) with presentation-copy briefings (context node + extracted pinned bytes — see /tmp/subspace-r-friction.md; gate prepare lacks the 0.14.0-required context field, git-root:// unresolved by plain loader).

IN FLIGHT: (a) auto-continue = layer 5: captain-approved ideation; implementation done offline; two findings — worktree gates-read misalignment (FIX authorized+proven, applying) and AC-4 validator-nondeterminism (ESCALATED to captain: pin fixture verdict vs named-risk — undecided); re-earning live loops both runtimes, then PR onto #722, link 718 719 721 722 <new>. (b) zq = layer 6: captain-approved attempt-2 (probe claim corrected: reuse works interactively, never in harness/CI — CLAUDE_CONFIG_DIR blindness + agentType mismatch + no teams config; spin-off entity RECOMMENDED, unfiled, captain word pending); implementation mid-live-loop. (c) hz nested inline no-op: verb STAGES then lies "nothing to commit" when workflow nested below git root; fixed locally 3826cdd63 (--relative; porcelain-empty regression), HOLDING push for the restack window (opens after layer 5 lands: hz pushes first with ls-remote lease, then FO rebases 721/722/L5 one pass, hz amends report with final SHA).

AUTO-CONTINUE REMEDY ARC: the first authorized gates-read fix (entityPath->reportEntity) was REFUTED by codex live runs — gate-record/report placement differs BY HOST (codex: report worktree, gates base; claude: gates worktree); corrected remedy authorized 2026-08-16: read gate state from EVERY distinct entity copy, fail closed; proven across three placements bidirectionally with symmetric falsification (each single path reds exactly one host's shape); additive commit on #723, both loops restart from zero streak. Lesson on record: the first proof's fixture encoded the hypothesis instead of varying placement. NEW PRODUCT FINDING (recorded, unfiled): `spacedock status --read` inspects the main entity copy and misses reports in the registered worktree (codex FO's structured reads failed on it live) — interacts with #722's surface; on the captain's filing list. RULED 2026-08-16 ("ok do it"): auto-continue finding-2 verdict pin AND pointer-anchor matcher both captain-approved; landing as additive commits on #723; BOTH runtimes re-loop from zero on the corrected candidate (codex re-earns on the shipping bytes); never-prepares recurrence -> quoted-instruction hold-and-report before hardening; NO new entity unless the mode survives hardening. Tip CI fires only after local ledgers close green and zq's final rebase lands. CAPTAIN RULING (2026-08-17, "2 & 3 need to go into existing ones"): NO new entities for stack greening — the ac-reanchor fixture identity fix AND the withdrawn-tolerance assert fix fold into zq's revise round at the tip (one commit window, no rebase ripple); the vanished-Cycle-line fix goes assert-side into the same window IF the root-cause is assert-side, or routes to hz's layer IF product-side (validator root-causing now; zq's implementer holds items 1+2 until the corrected validator report lands). zq's gate NOT presented until the validator report is corrected (its finding-3 "never recorded" was refuted by stream bytes: record succeeded entries=4, all writes committed, withdraw sanctioned). FINAL REVISE LANDED (3f6f3e935, fast-forward on 52b94f9aa): withdraw-recovery generalization (attempt-N, open-attempt selection, zero-open control, real-verbs regression proven falsifying), ac-reanchor fixture identity (persisted config + read-back guard + loud-fail commit; first non-test file, declared), heading STRICT+PIN (fixture names H3 explicitly; H2 byte-exact case reds by design; level-mismatch diagnostic polish). Final tip CI: run 31991220811, lanes auto-approved by watcher. NEW BACKLOG CANDIDATE (zq-found, deliberately deferred pre-CI): feedbackCyclesSection uses substring Index, so H4 "#### Feedback Cycles" contains the H3 needle and GREENS — "declared level or deeper", contradicting the strict ruling; shared with the escalation journey's assert; no observed trigger; file post-merge. CAPTAIN DECISIONS OPEN: (1) auto-continue finding 2 — pin fixture validator verdict (FO recommends) vs named-risk. (2) CLAUDE_CONFIG_DIR spin-off filing. (3) layer-4 hold release condition (FO reading: after hz fix + restack). (4) journey-metrics entity (3rdq…) still undispatched. MERGE-WINDOW NOTE (FO-owned): the stack root 0c6a2c32a predates main's df0bd50d9 registry annotations, so shared_live_runner_test.go will CONFLICT when zq's layer merges — resolution: stack's version wins, drop ONLY the rejection-flow AUDIT block (superseded by zq's layer), preserve every other journey's AUDIT block. LAYER ORDER SWAP (first-PR-ready): zq took layer 5 (AC-1 MET: codex 3/3, sonnet 3-consec in 5, budget 8, baseline 0; residual mode 1 hardened at a quoted instruction, no recurrence in 8 runs; three gate items: +174% surface overrun rides as declared, entity ordering correction FO-authorized, AUDIT block untouched); auto-continue is layer 6 on zq's branch. THEN: restack (hz push → rebase 721/722/L5/L6) → tip CI (pre-authorized on local green) → remaining validations (18, hz, L5, L6) → stack merge → 0.27 closing arc.

## Captain decisions pending

- b8 (fo-workflow-fit-gate) at backlog — filed from the captain's own draft; awaiting approve-into-ideation.
- Evidence pointer at risk: hz's pre-fix baseline stream (independent live trap reproduction) sits in the job tmp dir; hz was ordered to copy it into the entity as a durable artifact — verify in its stage report.

## Queued closing arc (order matters)

0. HZ IMPLEMENTATION COMPLETE (stage report filed in entity; state 8607023f4): PR #719 open on top of 18's branch, MERGEABLE; stack #720 formed (#718 bottom → #719 top; chain verified via GraphQL — RECIPE CORRECTION: `gh stack view --json` reads LOCAL gh-stack tracking state and false-negatives link-formed stacks, verify via GraphQL `pullRequest.stack` instead). Composed tree at 571017df3 (post-completion closures: true verbatim regression bytes; durable evidence/ dir in entity — 3 gzipped streams + ledgers + re-runnable repair-loop.sh + shim replica, with an extraction README that names every verb in codex's chained shell calls; measured-CI-prevalence deviation framing; shim discovery recorded incl. the sibling trap that spacedockBinary falls back to PATH spacedock when SPACEDOCK_BIN is unset — the stale-binary run cause; AC-1 verified from source). Full suite plain+race green at that head. COMPOSED LOOP: 4 CI-config runs, 2 green / 2 red. The lying label is dead (rejection-round-missing 0/4, inline-commit line 4/4). The two reds are single non-overlapping codes OUTSIDE both layers: (a) `rejection-gate-not-prepared` 1x — FO ended without preparing even under the fixed skill (residual model-adherence mode, now honestly labeled); (b) `implementation-worker-not-dispatched` — NEW DEFECT, SIZE CORRECTED (hz self-correction, FO-verified at claude_runtime_helpers_test.go:209): `assertWorkerLifecycle`'s composite condition requires spawns==1 AND exactly one "Stage Report: {stage}" section AND completion of only the LAST tracked spawn — a two-cycle rejection journey legitimately violates all of it (re-dispatch = spawns 2, correction = second report section). Observed 3 failures across 8 total runs in two variants (spawns=2/report=nil 2x, two-headings 1x). Not a selector tweak: assertion written for single-cycle journeys, shared with the default-headless-gate-stop scenario — fix needs a rejection-specific variant or parameterized expectations. Harness-only, PRE-EXISTING (not introduced by either stack layer). Named in hz's entity for separate filing, NOT yet filed (captain's word needed). Independent of the never-prepares residual (~1 in 4, honestly labeled `rejection-gate-not-prepared`) — fixing (b) alone leaves (a). TIP CI NOT FIRED — the pre-authorization condition "local is green" is unmet (2/4). CAPTAIN RULING (2026-08-16): do not invoke a journey in single-entity bare mode unless it is specifically testing bare-mode behavior. The rejection journey currently runs bare (`-p` single-entity; contract's bare-mode feedback flow = sequential fresh dispatch — the reuse mechanism under test cannot run at all; see comment claude_live_runner_test.go:404-408). Entity FILED to backlog: run-rejection-journey-in-team-mode (id zqb683j8jth0tyr2eme231e2, ff40fb714) — invoke in team mode both runtimes, align lifecycle assertion to the team-mode determined shape, strict. Awaiting captain's dispatch word (candidate layer 3 on stack #720). IN FLIGHT: (a) blind two-sided shape-attribution workflow (contract-derivation blind to streams vs stream-extraction blind to contract, then join) — explains the greens-passed puzzle and attributes each red; (b) live-harness-audit subagent (captain order): sweep all live scenarios for the same disease — fixtures not pinning mode/branch inputs, asserts accepting multiple paths where one is determined, or hardcoding one where none is. hz requested firing under its layer-bar reading — FO held: layer bar (mechanism) is met, journey bar is the captain's. QUOTING PREVALENCE NOW MEASURED: the three-char quote run appeared under the shimmed CI model too (pre-fix CI-config run 3: recorded round graded rejection-round-missing) — the quoting fix is provably CI-load-bearing; hz upgrading its deviation line and durably preserving exec-stream artifacts in the entity (job dir does not survive cleanup). NOTE: the session task list was found wiped after hz's completion — durable records live here and in the entities.
1. LAYER ORDER REVERSED (captain order 2026-08-16): DONE — 18's PR #718 OPEN as first layer (base main, head spacedock-ensign/simplify-feedback-rejection-flow, MERGEABLE, exactly 884b55af0+0e8dd3c49+ba91b333f, necessary-not-sufficient in body; pr=#718 FO-set on frontmatter 13c3cad11). STACK NOT YET FORMED and that is correct: a single-member stack cannot exist (`gh stack link 718` refuses, needs ≥2 args — and that refusal exits 0 through a pipeline; also `gh stack view` only answers from a branch IN the stack). The stack forms when hz's PR exists: `gh stack link 718 {repair-PR}` bottom-to-top, verify by read-back. hz next (task #15, unblocked): rebase onto ba91b333f (quoting fix re-applies to extracted var rejectionRoundEntity; recognizer unit tests on composed tree), targeted live loop on the composed tree — the journey-level proof, former out-of-scope reds become signal — then stack PR on top. Then validation → gates for both.
2. Durable-decisions sprint completed pre-cut audit (all members now terminal; rq parked as recorded decline, branch preserved unmerged at 579f12a00 for 0.28).
3. Deferred local refit (one entity): re-derive docs/dev/README.md template section + installed _mods/pr-merge.md from the SHIPPED versions 42c produced; fold in the AC-5 relocated clarification-routing exercise and the live-lane per-layer approval guidance (excluded from the shipped mod as CI-specific).
4. v0.27.0 stable ritual per docs/releasing.md: changelog carries the gate/resolution story with dogfood numbers (392 decisions, 282 consumed, 319 done — the captain's original user-facing-journey ask lands here).
5. Session debrief (spacedock:debrief) — incident-rich: scratchpad collision, stash swap, stale dispatch-pointer UUID, PR 712/715 duplicate (one-lander-per-stack lesson), gh stack link base-mutation/false-success, session-limit worker deaths and resume-by-message, git-author non-attribution, the rerun rebuke (ask before rerunning), equivalent-prior-green analysis.

## Session mechanics (re-learned facts that save time)

- Pushes: SSH remote git@github.com:spacedock-dev/spacedock.git (captain order; osxkeychain token lacks workflow scope). PR body edits: gh api PATCH, never gh pr edit. Stack-member base changes: only via gh stack link (REST refuses); link reorders bases by ARGUMENT ORDER and prints success on failure — verify by gh stack view/PR read-back. gh stack add leaves the root checkout ON the stack branch — check branch before committing.
- 0.28 revisit ledger: contract-3 retirement re-query (blocked by pre-#468 cache population, evidence in trim-version-output-decoration); opus repair holds bqy/66d (keep active as XFAIL insurance until N clean cadence runs); pi-dialect stream extractors (audit finding 11: rejection-flow p17swb + smallest-mechanism h30c9 pi XFAILs are structural claude-dialect blindness and can never XPASS — re-anchor those owners once extractors land); audit findings 3/4/5/6/9/13 (auto-continue gate blow-through worst) await captain filing decisions — registry annotated at df0bd50d9. OWNER-STANDARD RULING (captain 2026-08-16, "that's bs — a known failure should have a product task owner"): the active-owner lint only checks liveness, and the XFAIL owner entities were tracking stubs with no approach. CORRECTION: on current main only 060xp (sonnet keep-moving + break-glass) and four pi owners hold live bindings — the opus bindings (bqy/66d) were CLEARED during the stack (opus went green; bindings removed via PR 709-era work); bqy/66d are insurance holds for the cadence window only, archivable when it completes — violating the live-test-truth roadmap's own "owners own product repairs" line. All three upgraded with product-repair approach sections (060xp from its named mechanisms; the opus pair evidence-first since their mechanism was never named). Lint-hardening candidate for 0.28: require the owner entity to carry a non-empty Proposed approach section.
- Rejection-flow filings (captain-approved 2026-08-16): record-real-grades-in-journey-metrics (3rdq29x1…) backlog; own-rejection-flow-adherence-residuals folded into zq and archived (captain: "same thing").
- DISPATCHED TO IDEATION (all opus, all ride stack #720; implementation branches from stack tip at their dispatch time): run-rejection-journey-in-team-mode (zq — mission: local focused rejection test reliably green; finding-10 helper folded in); red-auto-continue-gate-bypass (7xe7hxt1… — audit finding 3 fast-track; local focused test first; ideation replays retained .live-artifacts-* against tightened grading to learn if past greens hid bypasses); fo-workflow-fit-gate (b8 — captain's draft seed + XFAIL-stub and contract-demands-ceremony evidence pointers); prepare-initial-gated-stage-from-seed (ra9qzfz9… — consumer FO field report: gated initial stage deadlocks, scheduler/contract demand a report the gate machinery's own fixture proves unneeded (prepare_test.go:1002); fix keyed on initial:true only, guard-preservation AC mandatory). FIXTURE-CORRUPTION incident: captain ruled IGNORE — no entity; rerun-to-green stands as the handling if it recurs.
