# Handoff 2026-08-16-01 — live coordination state (pre-compaction snapshot)

Written by the FO at the captain's compaction check. Durable state (gates, archives, README rules, releases) needs nothing; this file carries only what lived in conversation.

## In-flight workers and their couplings

- **hz (repair-codex-rejection-round-recording, implementation)**: code committed e870a2fd6 on spacedock-ensign/repair-codex-rejection-round-recording (base 0c6a2c32a). Running the targeted codex loop toward 5 consecutive greens; a green counts ONLY with the in-stream inline-commit line and zero pre-fix no-op strings. Surface overrun (7 files, +452, test-density) is FO-dispositioned keep-all-guards, rides as a declared deviation for validation. Next: stack PR onto main, stage report. TWO MORE DECLARED DEVIATIONS (FO-authorized at the review-finding checkpoint, captain ratifies at hz's validation gate): (a) recognizer quoting fix — codex emits a multi-character quote run (`'"'`) before artifact paths; `rejectionRoundArtifactArg` in internal/ensigncycle/shared_round_recording_test.go allows one optional quote and false-negatives a successfully recorded round as `rejection-round-missing` (FO verified from source + raw run-1 bytes; `directRoundLauncher` already uses the `['"]*` idiom). Authorized scope: `['"]?`→`['"]*` in the helper and the two sibling patterns in `commandRecordsRejectionRound`, plus a regression case with the verbatim run-1 command bytes. (b) loop scoring — reds where the FO never invokes `gate prepare` outside doc reads (both rounds recorded, no pre-fix strings) are out-of-scope: logged, neither count nor reset; contingency XFAIL applies only if in-scope reds persist. Streak restarts from the first run on the fixed tree; MAX_RUNS=12 stands. CONFOUND CORRECTION (ensign-discovered, FO-accepted): the early loop runs drove bare codex defaults — CI resolves codex via a PATH shim injecting the CI model + reasoning effort (runtime-live-e2e.yml ~355-388; runner uses LookPath). Those runs are discarded as gating evidence, preserved as a labeled dataset (bare-default-model-runs/); loop restarted under a verified shim replica and only shimmed runs count. ROOT CAUSE FOUND (ensign, FO-verified against cli.go:181 usage + live error + provenance diffs): the rejection-flow red is near-deterministic, not a flake — three independent defects on the journey path: (1) SKILL.md documents the round recorder WITHOUT the entity operand (`gate record --round ...` vs actual `gate record <entity> --round ...`; operand dropped by ea6723acb 2026-08-03), so a by-the-book FO gets a usage error on every attempt (4/4 live, both configs); (2) the skill's invoke-once/hold-on-failure language (d05730564 2026-08-09) converts that usage error into a terminal hold; (3) the dirty-tree dead end — hz's fix, CONFIRMED LIVE in CI-config run 1's stream (gate prepare exit 1 "selected source differs" → state commit exit 0 inline-commit line → gate prepare exit 0 state=open; the FO reached it by deviating from the skill and retrying with the operand). FO RULINGS (all declared deviations, captain ratifies at the respective gates): defects 1-2 routed to 18 as binding requirements (operand in the documented command; usage-error≠hold wording; provenance recorded; discovery attributed to hz's ensign) — 18's layer is now load-bearing for the journey AC; hz's contingency XFAIL is NOT to be applied (would be the ceremony the captain ruled against); hz's layer proof re-scoped to mechanism confirmation (run-1 chain cite + tests + quoting regression), with journey-level full-matrix green proven on the composed tree at the stack tip (atomic stack merge = shipping vehicle); LOOP CUT EARLY (captain-driven): the pre-rebase loop measured known routed defects on a never-ship base — stopped mid-collection; shimmed runs kept as a labeled dataset; the ONLY loop that matters is the composed-tree loop (task #15). CI approval policy: live lanes approved ONCE, on the stack tip (hz's PR), after the local composed loop is green — none spent on #718 alone. RESOLVED: the needs-preparation mechanism dispute closed - bullet tokens (DONE/SKIPPED + evidence + Summary), no review semantics; the 18 ideation's verdict-field claim is formally retracted (entity commit 00a86e53a); hz's AC-1 unaffected. Consequence for 18: FO-authorized one-clause addition binding step 5's gate preparation to step 4's re-review verdict (implementation-discovered-truth deviation class; captain ratifies at 18's validation gate).
- **18 (simplify-feedback-rejection-flow, implementation)**: IMPLEMENTATION COMPLETE — code 884b55af0+0e8dd3c49 on spacedock-ensign/simplify-feedback-rejection-flow (base 0c6a2c32a), state report d1d79dba2 pushed, go test plain+race green. The rewrite ships the ENTITY operand (defect 1) and the usage-error≠hold wording (defect 2); ensign exercised the operand defect itself (exit 2). Four FO dispositions issued: handoff-channel clauses confirmed authoritative (identical to direct relay); gate must state plainly the five-step shape alone would not have fixed the journey; step-5 retraction handling correct; overrun KEEP ruling (publication counter + five inverted cases stay — falsifiability outranks line estimate; final figures at ba91b333f: 4 files +285/-78 net +207 TOTAL, of which shared_round_recording_test.go is +261/-67 net +194 vs its ~45 estimate — earlier "+195" was the then-total misrecorded as per-file, 18-corrected; skill +105 words wholly the three FO-authorized clauses, the approved +107 hit exactly by the transcribed design). **CRITICAL COUPLING: live loop and stack PR HELD until hz's layer lands — the FO must signal it (task #20, blocked by #15).** Rebase guardrail issued: both layers changed shared_round_recording_test.go (hz c24eb0e89 quoting fix +40/-2 vs 18's reshape) — the rebase must preserve hz's `['"]*` tolerance and verbatim-bytes regression case, and run recognizer unit tests on the composed tree before the live loop. REBASE MECHANICS (18-recorded in task #20): 18's reshape extracted one of hz's two sibling patterns into var `rejectionRoundEntity` — hz's edit re-applies to the extracted var, not a textual merge; 18's own NEW round-value regex already uses `['"]*` with a table case (ba91b333f, FO-approved initiative), and 18 deliberately left both authorized siblings at `['"]?` to avoid colliding with hz's lines. PROVENANCE CITATION CORRECTED (18-verified by reading shipped text per commit): 00f8c203f carries no recorder command line — the prior-correct witness is c355fbe44 (2026-07-23) alone; relayed to hz for its stage report. Beware naive greps on that file: fixture examples DO name the entity; the step line is what an FO follows. Boundary (FO-confirmed): hz owns binary+grading, 18 owns skill+oracle-reshape, hz lands first.
- **comm-officer**: standing, idle.

## Captain decisions pending

- b8 (fo-workflow-fit-gate) at backlog — filed from the captain's own draft; awaiting approve-into-ideation.
- Evidence pointer at risk: hz's pre-fix baseline stream (independent live trap reproduction) sits in the job tmp dir; hz was ordered to copy it into the entity as a durable artifact — verify in its stage report.

## Queued closing arc (order matters)

1. LAYER ORDER REVERSED (captain order 2026-08-16): DONE — 18's PR #718 OPEN as first layer (base main, head spacedock-ensign/simplify-feedback-rejection-flow, MERGEABLE, exactly 884b55af0+0e8dd3c49+ba91b333f, necessary-not-sufficient in body; pr=#718 FO-set on frontmatter 13c3cad11). STACK NOT YET FORMED and that is correct: a single-member stack cannot exist (`gh stack link 718` refuses, needs ≥2 args — and that refusal exits 0 through a pipeline; also `gh stack view` only answers from a branch IN the stack). The stack forms when hz's PR exists: `gh stack link 718 {repair-PR}` bottom-to-top, verify by read-back. hz next (task #15, unblocked): rebase onto ba91b333f (quoting fix re-applies to extracted var rejectionRoundEntity; recognizer unit tests on composed tree), targeted live loop on the composed tree — the journey-level proof, former out-of-scope reds become signal — then stack PR on top. Then validation → gates for both.
2. Durable-decisions sprint completed pre-cut audit (all members now terminal; rq parked as recorded decline, branch preserved unmerged at 579f12a00 for 0.28).
3. Deferred local refit (one entity): re-derive docs/dev/README.md template section + installed _mods/pr-merge.md from the SHIPPED versions 42c produced; fold in the AC-5 relocated clarification-routing exercise and the live-lane per-layer approval guidance (excluded from the shipped mod as CI-specific).
4. v0.27.0 stable ritual per docs/releasing.md: changelog carries the gate/resolution story with dogfood numbers (392 decisions, 282 consumed, 319 done — the captain's original user-facing-journey ask lands here).
5. Session debrief (spacedock:debrief) — incident-rich: scratchpad collision, stash swap, stale dispatch-pointer UUID, PR 712/715 duplicate (one-lander-per-stack lesson), gh stack link base-mutation/false-success, session-limit worker deaths and resume-by-message, git-author non-attribution, the rerun rebuke (ask before rerunning), equivalent-prior-green analysis.

## Session mechanics (re-learned facts that save time)

- Pushes: SSH remote git@github.com:spacedock-dev/spacedock.git (captain order; osxkeychain token lacks workflow scope). PR body edits: gh api PATCH, never gh pr edit. Stack-member base changes: only via gh stack link (REST refuses); link reorders bases by ARGUMENT ORDER and prints success on failure — verify by gh stack view/PR read-back. gh stack add leaves the root checkout ON the stack branch — check branch before committing.
- 0.28 revisit ledger: contract-3 retirement re-query (blocked by pre-#468 cache population, evidence in trim-version-output-decoration); opus repair holds bqy/66d (keep active as XFAIL insurance until N clean cadence runs).
