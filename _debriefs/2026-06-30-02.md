---
session-date: 2026-06-30
sequence: 2
first-commit: 55039e74
last-commit: 2363f61b
duration: ~continuation (post-merge half)
---

# Session Debrief — 2026-06-30 #2

Continuation of the 06-29 repair-and-ship session, after bm (#441) + vv (#442) merged (see `2026-06-29-02`). This half was contract analysis + sprint planning: two deep reviews of the FO/ensign contract, then the assembly of the **0240-lean-contract** sprint. (Written to the split-root state checkout per the convention — the debrief-skill bug that puts these on `main` is now itself filed in this sprint.)

## Shipped
_(none new this half — bm #441 + vv #442 landed in the prior debrief; origin/main also advanced to #443 contract-version-bump via another session.)_

## Filed (backlog) — the 0240-lean-contract sprint (6 entities)
- **`entity-status`** (`84521x…`) — defer Status Viewer + Issue Filing (~675 tok) out of the boot-resident core.
- **`defer-write-scope-id-styles`** (`k408d2…`) — defer FO Write Scope + ID Styles (~1,027 tok) — sibling to entity-status.
- **`ensign-contract-dev-leakage`** (`scr2rx…`) — re-home dev-only discipline out of the per-dispatch ensign core (was an unfiled `_sprint-notes` item).
- **`debrief-split-root-state-home`** (`7d47cg…`) — the debrief skill writes to the definition dir instead of the split-root state checkout.
- **`fn-binding-refinements` (z4)** — EXTENDED 4 → 6 refinements: added `«halt.rebase-conflict»` (the one genuine cross-file fail-safe dedup) + the Dispatch/Merge pointer → `«fn»`-registry consolidation; AC-6 unblocked (bm landed).
- **`read-guidance-redundant-with-grep` (82k)** — moved into the sprint from `0204-structured-reads`.

## Decisions
- **0240-lean-contract = context occupancy + `«fn»` structure.** Hard **sequencing** constraint (they edit the same ~2–3 files; dispatch sequentially, NOT in parallel): 82k + ensign-leakage (trim cores) → entity-status + defer-write-scope (defer) → z4 (registry/`«fn»`).
- **Occupancy lever = DEFERRAL, not re-prosing.** ~32% of the ~7,170-token boot core is phase-specific reference (Status Viewer, FO Write Scope, ID Styles, Probe, Single-Entity, Mod Hook) needed at dispatch/status/write time, not at greet. Deferring it (the dispatch/merge-module pattern) is the real win; the `«fn»`/diagram reshaping is net-neutral legibility.
- **z4 ≠ the deferral.** Kept as separate entities by their value-AC identity: z4 = net-neutral `«fn»` legibility (AC-4 ≤ baseline); the deferrals = net-NEGATIVE occupancy. Mixing them blurs means/end (the gate-on-end-value lesson applied to scoping).
- **Relocated the 06-29 debriefs to the state checkout**; the old pushed 06-19/06-21 *main* debriefs are DISTINCT sessions sharing filenames (not dups) — left untouched.
- Top-scored non-theme items (`fo-opus-behavioral-robustness` 0.6, `minor-version-compat-coupling` 0.6) held for 0.25.0.

## Issues — Spacedock
- **Debrief skill is split-root-unaware** — writes to `{dir}/_debriefs/` on `main`, not the state checkout (filed: `debrief-split-root-state-home`). Same family as the two deferred tooling gaps (`status --validate` near-dup ids; `status --resolve` archived-scope).
- **Flaky PATH** (carried from #1): a Bash invocation lost `git`/`tail` from `$PATH`, breaking `merge guard` mid-finalize (recoverable; guard PATH).

## Observations
- **The contract is already lean.** The token roadmap is mostly shipped (fo-contract-token-cut, prose-function-restructure, trim-dispatch-adapter-prose, shared-contract-prose-sweep all done). The 41-agent token-efficiency review found only ~925 raw tokens of trims left — no order-of-magnitude move. The remaining prize is deferral (architecture), not prose-trimming.
- **The `«fn»` layer is disciplined.** 17 `«fn»`s = ~9 behavior contracts (real guard/block/done-when) + ~8 host-binding interfaces (the price of 3-runtime support, captain-confirmed real). No ceremony to demote; the scaffold is applied only where there's a real precondition/fail-safe/completion.
- **The structural-pass staff engineer falsified two hypotheses** (good adversarial value): a graphviz lifecycle diagram LOSES (the lifecycle is README *data*, not prose — a diagram is additive and over-specifies a stage-agnostic contract); and `«fn»` expansion is net-neutral on size (legibility, not occupancy).
- The earlier credential-rotation command looked exactly like an exfil injection (out-of-band, coercive footer, keychain-secret-to-file); the captain confirmed it was a legitimate rotation. Refuse-and-surface on that shape is still the right default; the cost was a false alarm.

## What's Next
- **Dispatch the 0240-lean-contract sprint** — staff-review the sprint design FIRST (deferral soundness, the AC-2 greet-independence guards, the sequencing), then the sequenced implementation (82k + ensign-leakage → deferrals → z4), then **0.24.0 packaging/release**. Handoff: `/tmp/spacedock-0240-handoff.md`.
- Remaining deferrable boot sections (Probe ~347, Single-Entity ~107, Mod Hook ~147) — a smaller third deferral sibling, after the first two land.
- 0.25.0 candidates: `fo-opus-behavioral-robustness`, `minor-version-compat-coupling`.
