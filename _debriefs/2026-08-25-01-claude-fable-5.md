---
session-date: 2026-08-25
sequence: 1
harness: claude
model: claude-fable-5
model-version-build: unknown
first-commit: cba523225
last-commit: b04b3effd
duration: ~15h
---

# Session Debrief — 2026-08-25 #1

From a fresh-VM install experience report to a stable release carrying its fixes: three tasks driven seed→PR→merge as a stacked train, the v0.27.0 cut executed with an audited waiver, the v0.28.0-pre0 line made coherent, and the release machinery's remaining patch-line gaps consolidated under one owner.

## Shipped
- **tdn** `install-sh-edge-prerelease-parity` — [#756](https://github.com/spacedock-dev/spacedock/pull/756). install.sh resolved `releases/latest` (excludes prereleases) and silently broke edge version parity; `SPACEDOCK_CHANNEL` now threads tag resolution, asset naming, and the dist glob, with the resolved tag disclosed before download.
- **p1t** `install-gate-channel-aware-hint` — [#757](https://github.com/spacedock-dev/spacedock/pull/757). The FO's binary-absent install hint was channel-blind; a three-arm classifier over the retained `{first_officer_base}` now hints the channel-correct command per OS, relocated into the renamed `references/fo-install.md` (four validation rounds; two captain PR-review correction rounds absorbed).
- **wn** `de-lecture-and-defer-fo-contract` — [#759](https://github.com/spacedock-dev/spacedock/pull/759). The FO contract shed its lecture (contract subtotal −3.1 KB) and deferred `## Completion and Gates`; boot-resident core 25,871 → 22,545 B (−13.6%) with the byte cap ratcheted so the win cannot refill. Folded superseded s6q; corrected its gate-first reachability grouping at ideation.
- **Released v0.27.0** (stable) with captain-approved notes; `v0.28.0-pre0` auto-cut fired exactly once (#729 idempotency observed); stable branch advanced to the tagged SHA; marketplace README repaired on both branches of the external repo (`403c46f`, `d390cad`).

## Filed (backlog)
- **765** `marketplace-readme-channel-model-repair` — marketplace README documented the retired channel model with a nonexistent install id; shipped same session, delivered direct to the external repo, closed done/PASSED.
- **rb** `plugin-install-https-keyless-machines` — keyless machines fail the SSH-default plugin clone; surface or automate `CLAUDE_CODE_PLUGIN_PREFER_HTTPS`.
- **trb** `approve-with-amendment-gate-decision` — record captain amendment bytes in the gate resolution so a minor amendment costs one captain touch instead of a withdraw/re-present cycle (motivated by this session's td amendment round).
- **8d** `fo-gate-lifecycle-cap-raise-for-gate-fns` — home the three boot-resident gate fns in fo-gate-lifecycle under a raised cap (~1,020 tok further greet reduction; follow-up adopted at the wn ideation gate).
- **d0** `patch-release-line-support` — release.yml cannot ship an old-line patch (the stamp-main step downgrades main; the stable-branch advance dies non-FF off a release branch) and does not automate the preversion bump; supersedes tw with the 2026-08-25 cut incidents as spec.
- **gr** `next-post-release-preversion-bump` — closed done/PASSED delivered-direct: the bump executed live (`b8346ffc9`) and the runbook step landed (`b04b3effd`); its next-branch mechanics predate the retired model.
- Superseded: **s6q** → wn; **tw** → d0 (both archived with verdict empty per convention).

## Non-PR commits (workflow-only)
State and process commits that do not belong to a PR:

- `f3754be28` (rebased to `f580eb45e`) refit: dev workflow scaffolding to spacedock@0.27.0-pre8 — canonical pr-merge mod with Stacked mode; README template adoptions (pr/mod-block fields, Risk evidence, Feedback Cycles, small-change fast path) plus contract-owned redundancy cuts.
- `17f5cd591` release: bump version to spacedock@0.27.0 (ritual step 3, manifest-tag-gate green).
- `b8346ffc9` release: bump version to spacedock@0.28.0-pre0 — manual post-release preversion bump under captain grant; closed the live edge mismatch loop.
- `b04b3effd` releasing.md: post-tag main preversion bump added as ritual step 9 (interim until d0 automates it).
- External (spacedock-dev/marketplace): README repair on main and edge; display-version tidy (stable → 0.27.0, edge date-key advanced).

All other session commits are rolled up in the shipped PRs above.

## Decisions
- Surface deviations on td (+359/7 vs +155±40/5) and p1t (+329/4 vs +150±40/4) accepted as estimate corrections — the overage was test battery in both cases, surfaced by the ensigns rather than silently traded.
- The sh-side env-var wording (`curl … | SPACEDOCK_CHANNEL=edge sh`) ratified over the ideation-approved prefix form, which provably never reaches the script — FO-authorized mid-stage under the disposition flow, captain-ratified.
- Lean-validation posture set for wn ("keep the validation lean, i'll review on github"): diff-bound suites at validation, full matrix at the stack tip.
- "Keep it lean" contract lens established at the #757 fo-install review (six inline comments) and generalized into the wn de-lecture; contract references carry rules and commands, rationale lives in entity bodies and tests.
- Tap addressing standardized on brew's short form `spacedock-dev/tap`; the repo is NOT renamed (brew requires the homebrew- prefix).
- Whole stack merged atomically (`gh stack merge 759 --yes`) after all three lanes greened at the tip; two CI reds diagnosed from run evidence (bf classifier, rc-family conduct) and cleared by targeted local runs plus rerun-to-green.
- v0.27.0 cut on the equivalent-prior-green waiver (stamp+docs-only delta, audit trail recorded, variable deleted after use).
- gr/tw reconciled: interim stamping in docs/releasing.md now; automation and patch-line support consolidated in d0.

## Issues — Workflow
- The terminal guard requires `--force` for delivered-direct closures (765, gr) — no PR exists by design; basis recorded in each archive commit.
- A stale `release/0.27.0` worktree+branch from an abandoned pre3-era attempt collided with the cut; audited and cleared. The 16-entry orphan worktree inventory from boot remains peer-owned, report-only.

## Issues — Spacedock
- Codex live grader false-red (`smallest-mechanism-violation` from untargeted wait records) — confirmed against this run's stream as tracked `bf`; targeted local run passed on the identical tree. Not re-filed.
- Claude live conn-citation paraphrase (real grant, non-verbatim quote in the recorded citation) — new sub-mode recorded as evidence for `rc`'s ideation; targeted local run passed. Not separately filed.
- The install/doctor version-mismatch remedy loops without escalation when the plugin source has no matching version yet (the 05:01Z race during the cut window). Not filed; candidate.
- Marketplace manifest display `version` fields re-stale by design (hand-tidied this session; automation candidate noted for d0's scope discussion).
- Canonical mod content can change without a version bump mid-dev, blinding refit's version-diff detection — captain ruled acceptable during dev (stamps become meaningful at release); refit byte-diffs as the workaround.

## Observations
- The review-finding disposition flow caught three real defects before merge in one session: the POSIX pipeline env-prefix bug (the approved design's own AC-1 falsifier), the contract⟷doc drift-lint pairing break from a "cosmetic" spelling change, and the next-id rule removal hiding inside a de-lecture strike. Each was surfaced-then-authorized, never silently fixed.
- The contract's test bindings make "just prose edits" expensive (an hour of verification tail for a byte-negative diff) and paid for themselves the same day; the captain's diff-bound-suites lever cut the validation tail by more than half without losing any of the catchers.
- Stacked-PR mechanics under `pull_request: branches: [main]`: the tip gets no automatic PR CI; lanes reach the tip via workflow_dispatch (or at retarget after bottom-up merges). The pr-merge mod's Stacked mode ceremony held up across create, link, restack, and an atomic three-layer merge.
- The captain's overbuilt/de-lecture rulings measurably compounded: fo-install.md 52 → 32 lines, the boot bullet 932-char cap → 232 actual, then the whole-contract sweep at −13.6% boot cost — all in one session, each round leaner than the last.

## Agent Testimonial
- Date: 2026-08-25
- Harness/runtime: Claude Code
- Model: Claude Fable 5
- Model version/build: unknown
- Session scale: 11 tasks touched; 8 workers dispatched; 3 PRs created and merged (6 more assessed in a peer stack)

Spacedock earned its overhead this session, and I want to be precise about where. The durable gate records made a genuinely messy sequence safe: td's design was amended mid-gate, p1t absorbed four post-approval correction rounds, and wn reversed an ideation-approved placement decision — in every case the briefing digests, recorded rulings, and stage reports meant nobody had to trust memory about what was approved when. The disposition flow is the standout: three real defects were caught by workers who had a sanctioned way to say "stop, this is wrong, authorize me" instead of quietly fixing or quietly shipping.

The friction is equally real. The withdraw/re-prepare cycle for a one-line captain amendment is heavy enough that we filed a protocol change (trb) after living it once. The stage-report completion guard pins an entity on a FAILED checklist item even after the captain has ruled — correct fail-closed behavior, but it costs a full round-trip to an ensign just to flip a line. Worktree cwd drift bit me twice in shell sequencing. And the contract itself was part of the problem this session: I was dispatching workers to edit prose whose test bindings neither of us could fully enumerate up front. The honest summary: Spacedock converts chaos into round-trips — every safety property it enforced cost at least one extra worker message, and every one of those round-trips was cheaper than the silent failure it prevented. Without it, this session's three-layer stack with seven captain rulings would have been unreconstructable by hour ten.

## What's Next
- **Recommended next session:** dispatch `rb` (keyless HTTPS plugin install) and `d0` (patch-release line support); decide shaping for `trb` (approve+amendment) and `8d` (cap raise); the prose-grep audit task and the remedy-loop copy fix await the captain's word.
- **Gate-blocked (peer-owned):** validation needs-preparation ×4 (93, 8v, vka, 0b); ideation approved-awaiting-advance ×3 (mn, 79, sh); open PRs #725/#747/#749/#750 from the pi line.
- **Deferred:** wn's AC-1 live A/B greet measurement — now cleanly measurable against released 0.26.0 vs 0.27.0 plugin bytes; the exploration-workflow commission (proposal at /tmp/exploration-workflow-proposal.md, naming and mechanics settled, four one-liner fixes pending).
