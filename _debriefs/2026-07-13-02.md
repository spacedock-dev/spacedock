---
session-date: 2026-07-13
sequence: 2
first-commit: 8efdbfe0
last-commit: 557f8df3
duration: ~47h18m
---

# Session Debrief — 2026-07-13 #2

Focused release/launcher-cycle debrief from `v0.25.0-pre1` through the blocked
`v0.25.0-pre2` stable-cut review. This deliberately excludes unrelated parallel
workflow activity.

## Shipped

- **Codex post-fence launch handling** — [#498](https://github.com/spacedock-dev/spacedock/pull/498), [#501](https://github.com/spacedock-dev/spacedock/pull/501), and [#502](https://github.com/spacedock-dev/spacedock/pull/502). The later repairs restored the model-only fresh-launch path, but the pre-cut audit found two remaining front-door regressions; this is shipped code, not a release-clear result.
- **Generic terminal-target environment forwarding** — [#499](https://github.com/spacedock-dev/spacedock/pull/499). Safehouse now composes terminal metadata generically, including the current Zellij variables without naming Zellij in the public mechanism.
- **Codex async idle monitoring** — [#500](https://github.com/spacedock-dev/spacedock/pull/500). The contract now names `wait_agent` as asynchronous idle monitoring, preserves captain interruption semantics, and uses a five-minute long-task default.
- **Prereleases** — `v0.25.0-pre1` and `v0.25.0-pre2` were published. The latter's exact-SHA Runtime Live E2E and release workflow were green.

## Filed (backlog)

- **z93** `claude-launch-cwd-authority` — ensure a Claude FO retains its launch CWD instead of treating the checkout-shaped launcher path as a workflow root; filed from a reproduced Opus fixture escape.
- **2h** `claude-live-installed-candidate-binary` — install the checked-out candidate into a neutral temporary bin for Claude Live E2E. Ideation is complete; it awaits the captain's implementation gate.

## Non-PR commits (workflow-only)

- `8efdbfe0`, `5b1b68aa` — prerelease version stamps for `0.25.0-pre1` and `0.25.0-pre2`.
- `134ea3d3` — recorded the 0250 prerelease in the roadmap.
- `557f8df3` — committed the independent pre-cut audit report with verdict **BLOCKED**.

Routine state transitions are suppressed. A mistakenly created pre-cut-audit entity was reverted rather than retained as workflow history.

## Decisions

- Hold stable `v0.25.0` until the pre-cut audit's two Codex front-door blockers are repaired and independently re-audited.
- Treat `wait_agent` as **async idle monitoring**, never foreground/synchronous waiting: it does not stop worker progress or captain interaction.
- A pre-cut antipattern audit is a Commander-owned independent-review checkpoint, not a workflow entity. Its report belongs under the sprint; only later non-blocking findings seed the next sprint.
- Keep the Claude wrong-root repairs visible as release-confidence work: `z93` anchors launch CWD; `2h` removes the checkout-shaped binary lure while preserving exact candidate provenance.
- #482's stable-to-edge release repair is implemented and test-covered, but has not yet been exercised by a real stable cut. The eventual `0.25.0` cut must be its live proof.

## Issues — Workflow

- The Commander incorrectly filed the pre-cut audit as an implementation task. The task and its state commits were reverted; the real report is `docs/roadmap/0250-fo-behavioral-discipline/post-sprint-audit.md` on `main`.
- One local duplicate audit branch remains after the report was cherry-picked. Safe deletion refused because its commit is not an ancestor; no force-delete was used.

## Issues — Spacedock

- **B1, stable-cut blocker:** a forwarded Codex positional receives the FO bootstrap as a second prompt and Codex exits 2. Existing launcher tests assert the invalid argv via a fake launcher instead of enforcing Codex's grammar.
- **B2, stable-cut blocker:** `--profile resume` is treated as the `resume` subcommand, suppressing default approval and the first-officer bootstrap on a fresh launch.
- `dispatch build` still has a JSON/flag selection mismatch: adding `--bare-mode` selects flag/file input despite help advertising stdin JSON. The existing deferred `jha` task covers the broader dispatch-build version-skew problem; this reproduction should be folded into it rather than spawning a duplicate.
- The stable-to-edge auto-pre0 path has only fixture/test evidence. `v0.24.0` predates #482, so it did not exercise the new step.

## Observations

- The independent audit was valuable because it ran the real Codex parser. It exposed two regressions that argv-shape unit tests accepted as correct.
- The Runtime Live E2E matrix greened the audited SHA but calls `codex exec` directly; it does not prove the complete `spacedock codex` front door.
- A narrow report plus direct repair work is the right release mechanism. Converting the audit itself into a staged task added ceremony without improving the review.

## What's Next

1. Scope and repair B1/B2 as narrowly bounded front-door work; prove model-only, forwarded positional/subcommand, `--profile resume`, and real `resume` behavior against a grammar-enforcing parser.
2. Present the `2h` implementation gate and start ideation for `z93` if both remain in the stable-release confidence scope.
3. After fixes, repeat the direct parser probes and independent pre-cut audit, then stamp → exact-SHA Runtime Live E2E → annotated stable tag. Verify the stable-to-edge auto-pre0 release path from that cut.
4. Delete the duplicate local audit branch only with explicit approval for the required forced deletion, or retain it as harmless local provenance.
