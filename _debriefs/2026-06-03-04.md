---
session-date: 2026-06-03
sequence: 4
first-commit: 7bb94bcf
last-commit: f98b7309
duration: ~6h active (state-commit span; long live-CI/audit waits)
---

# Session Debrief — 2026-06-03 #4 (Claude FO)

The `at` marquee session. Ran in parallel with the codex peer (debrief #3 covers `4n` #284 + `bx`/`pi-runtime-support`). Headline: **`at` (non-interactive-teardown-exit) reached PR #285 with live-CI running** — it REVERSES `yy`'s shipped #282 (retry-to-success → bounded best-effort + a `TERMINAL_TEARDOWN_BOUNDED` marker + launcher-owned exit), after the spike + the real failed CI artifact root-caused the hang to upstream Claude #38116/#57681 (an approved-shutdown member is never cleared from `members[]`). Also shipped the scenario-testing-principles doc and seeded the contract-simplification roadmap.

## Shipped
- **m8** `scenario-testing-principles` — local-merge `ffb829b8` to `docs/specs/scenario-testing-principles.md` (no-ceremony doc, archived PASSED). The semantic foundation: a scenario is a natural-language spec; a codified test and an LLM run are two **executors** of it; cost/coverage key by `(scenario, mode, runtime)`. Includes a machine-readable seed-scenarios block lockable to `sharedRuntimeScenarios()`.

## In flight (not merged)
- **atw** `non-interactive-teardown-exit` — **PR #285**, live-CI running (sonnet=AC-1, opus, codex envs approved). Mod-blocked until the live-sonnet AC-1 confirms. The FO-contract reversal of #282; unblocks n3/2a → am → 6b/ep on merge.

## Filed (backlog)
- **nz** `frontdoor-prompt-after-dash-guard` — the front door silently swallows a positional prompt placed after `--` (treated as host passthrough), so the bootstrap prompt never prepends; captain hit it launching this session.
- **cb** `review-report-conciseness` — **filed then WITHDRAWN** same session: the captain flagged it as mis-scoped (the generic gate-conciseness standard already exists in `first-officer-shared-core.md:172/185-188`; the real fix is FO discipline — distill reviewer findings, don't relay raw reports).

## Non-PR commits (workflow-only)
- `733d7d13` mod-block + `f98b7309` pr #285 pending — `at` PR boundary.
- `dd7ee3a8` + `92f6d4e0` (cycle 1) and `a0144ff4` (cycle 2) — detached-audit feedback on `at`; `05724c2e` validation PASSED.
- m8 local-merge + archive; nz file; cb file+withdraw — captured above.

## Decisions
- **`at` direction pivoted twice, each captain-approved, each forced by evidence.** (1) The session-11 A+C was **falsified by a spike** (5 real `claude -p` runs: the harness won't let the process self-exit while `members[]` is populated — proven on sonnet AND opus, EXIT=124). (2) The no-progress-reset reframe was **falsified by the real CI artifact** (the member approved shutdown + died at 14:37:30, but the FO still saw it "active" 55s later — the registry never clears it, so retry-to-success is structurally unreachable). Landed on **grade bounded best-effort teardown via a verbatim marker; the launcher owns the exit.**
- **Rejected the config-rewrite host-hack** (reaching into Claude's private team registry) — captain: keep it out of the production contract; this is a test-infra grading concern, not a contract registry edit.
- **`m8` collapsed from a dev entity to a no-ceremony doc** — captain caught that the workflow bans doc-only dev-queue tasks (README:80/130). Home: `docs/specs/`.
- **Approved `at` to PR + live-confirm-before-merge** — explicitly NOT merging on offline+audit alone (the `yy` mistake). Approved all 3 live envs.
- **Next wave starts off the `at` branch** (am/6b/ep) — parallelize while #285 is in CI; rebase onto `next` after `at` merges.

## Issues — Workflow
- None blocking. The "doc-ceremony" reflex (filing m8/cb as full entities) surfaced an ergonomic gap — see Observations.

## Issues — Spacedock
- **Upstream Claude bug #38116/#57681** (approved-shutdown member never cleared from `members[]`) is the ROOT cause of the sonnet live-CI hang — not a spacedock bug, but the thing `at` works around. Not filed (it's upstream Claude Code).
- No spacedock-framework issues filed this session.

## Observations
- **The detached adversarial audit caught 4 real test-strength holes on `at` across 2 cycles** (oracle near-synonym evasion ×2, ≤60s additive-composite blind spot, a parenthesized-MUL variant) — every one a test that passed but would also pass on a broken future edit. Validation-green was necessary, never sufficient. The `durationOf` guard is now a robust compositional AST evaluator; the prose-lints are at the sane ceiling (positive anchors + spelling-independent co-occurrence) with the reword-ceiling documented (AC-1 live is the real oracle).
- **Live verification is THE oracle for runtime-observable claims** — re-proven. `at` (like `yy`) cannot be confirmed by any offline/recorded check; only the live producer run decides. PR #285 confirms AC-1 on the live-sonnet check before merge, by design.
- **Scenario-testing model (m8):** treating "offline test" and "live run" as two *executors of one natural-language scenario* (not different kinds of thing) dissolves the `yy` failure mode. `p4` (citation gate) + the authoring primitive + `4n` (cost keyed by the tuple) build on it.
- **Contract-prose-cut roadmap** (captain-directed analysis): the high-value cuts are (1) **`zd`** — extract ~140 lines (≈45%) of generic Claude-team lifecycle out of the FO runtime adapter into a reusable `using-claude-team` skill (the captain's "10 steps, error-prone" footgun-killer; must follow `at`); then (2) the **binary-simplification roadmap** — `pr complete`, `state commit-and-push`, `dispatch advance`, now-unblocked `gate verify` — moving error-prone procedures from prose into commands.
- **Doc-ceremony ergonomic gap:** the FO reflex is "file an entity" (heavy lifecycle) even for docs/decisions/local-config. Fix is a pre-filing triage (code/test → entity; doc → write+land; decision → roadmap) + a first-class lightweight lane. Captain-surfaced via the m8/cb trips; not yet filed (intentionally, per the lesson).

## What's Next

**IMMEDIATE — `at` merge (next session resume):**
1. Check PR #285 live-CI: `gh pr checks 285`. If **sonnet (CI-E2E) AC-1 passes** → merge (clear mod-block standalone → terminalize PASSED → archive → cleanup). If it FAILS → the fix didn't hold live; route back to the `at` impl ensign (kept alive). This is the live-confirm-before-merge gate.
2. On `at` merge: **retrigger n3 #275 + 2a #277 live sonnet** (rebase onto next, approve CI-E2E env) → merge both (validation-PASSED, blocked only on the flake `at` fixes).

**THE NEXT WAVE — start off the `at` branch (captain-directed), parallel to #285's CI:**
- **am** `ensigncycle-streamwatch-test-only` — rename `streamwatch.go` → `*_test.go`. Premise VERIFIED to still hold after `at`: `streamwatch.go` stays test-only (zero non-test importers; `at`'s `gradeTerminalTeardown` is referenced only within it). Impl must also flip the `live_budget_test.go` filename literal. Start off the `at` branch.
- **6b** `terminal-guard-rejected-consistency` — `--set`/`--archive` verdict=rejected asymmetry + `first-officer-shared-core.md` prose (disjoint from `at`'s teardown section). Off the `at` branch.
- **ep** `ensign-contract-dev-leakage` (design-approved) — re-home dev policy out of the universal ensign core + split `2a`'s `externalTokenRe` into a `devExternalTokens` injection. **Doubly blocked:** AC-6 hard-deps on `2a` merging (`external_proof.go` not on next); shares `first-officer-shared-core.md` with `at`. Start its prose layer (AC-1..5) off the `at` branch; AC-6 after `2a`.
- All three: created as worktrees off `spacedock-ensign/non-interactive-teardown-exit`; rebase onto `next` once `at` lands.

**THEN (0.19.5+):**
- **p4** `live-verification-gate` (design-approved, slimmed to the citation gate) — implementation after `2a` merges (it extends `2a`'s `ClassifyEntityACs`).
- **zd** `extract-team-orchestration-skill` — the high-value prose cut; ideate after `at` merges (it extracts the teardown section `at` rewrote).
- Ergonomics cluster: `nz`, `fc`, `mt`; the binary-simplification roadmap.

**Codex peer's lane (coordinate, don't author):** `4n` #284 (cost-ledger — should adopt m8's `(scenario,mode,runtime)` key), `gn` #283, `bx`, `pi-runtime-support`.

**Roster at session end:** several idle agents (at impl/validation ensigns, p4 ensign, 2 reviewers) — tear down with the team at session end; the next session re-dispatches fresh off the `at` branch.
