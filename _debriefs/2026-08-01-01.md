---
session-date: 2026-08-01
sequence: 1
first-commit: 8501e61cb
last-commit: d1dee1d50
duration: ~15h07m
---

# Session Debrief — 2026-08-01 #1

First-officer session: finalized the z3 install-journey PR under a dying gh credential, then drove the resolution-consume semantic hole from backlog gate through a rejected over-engineered design, the captain-authored six-point minimum, necessity-first staff review, implementation, a validation rejection for an unapproved second authority writer, the captain-prescribed locked-writer rework, narrow corrections, and validation PASS. Session closed with a captain-directed state-history rewrite and a retroactive candidate-binary consume — the pending model demonstrated live (the "quick dogfood" testimonial below).

## Shipped

- **z3** `fo-boot-install-hint-linux-direct-sandbox` — [#586](https://github.com/spacedock-dev/spacedock/pull/586). First-officer boot install journey: OS line in `--version`, OS-aware install remedy (curl|sh on Linux, Homebrew on macOS), bounded direct-install offer, sandbox detection. Merged via local `--no-ff` + SSH push after the gh API token died (merge PUT 401); GitHub auto-detected the PR MERGED and closed #581. Merge guard finalized: done, verdict=passed, archived, worktree+branch cleaned.

## Filed (backlog)

- **zexb** `dev-stamp-in-tree-version-gate-compat` — in-tree builds must pass the FO version gate against in-tree skills, for dev usage and CI. Seed classes: gate-prose disambiguation for live-agent readers (codex-lane failure class) and an enforced in-tree-build ↔ in-tree-plugin compat invariant. Captain is shaping the ticket himself this weekend.

## Non-PR commits (workflow-only)

- `3522ee457` Staff-review principle: necessity before coherence ("should this mechanism exist at all") — new standing rule in docs/dev/README.md, lesson institutionalized from the 1w82 cycle-1 ratchet.
- `50c288a0a` Roadmap: add pre-stable gate necessity cut — captain-authored companion ruling.
- `c4b7dc188` **[rewritten out of history]** the old binary's premature consume for 1w62 (`consumed`+`done` before delivery) — dropped from state-branch history by captain directive, resolution retroactively applied with candidate binary `8750d608b` instead. Remote never carried it: `spacedock state commit`'s HTTPS push leg had silently failed for the final ~5 commits. Published fast-forward `3411238b2..d1dee1d50` via SSH. State history from 2026-07-31 21:12 → 2026-08-01 12:19 (+0800).

## Decisions

- **Merge z3 despite codex-live red ×2.** DIagnosis: the z3-rewritten version-gate prose is ambiguous to strict readers — codex's runtime misclassified the candidate's `0.27.0-pre2+dev` stamp as an integer-era dev build (claude/pi lanes green; main's lanes green 07-13/07-19 with old prose). Captain ruled merge anyway; the fix class is owned by the new `dev-stamp-in-tree-version-gate-compat` task, not a z3 amendment.
- **gh API token dead again** (keyring `default` invalid, ~2h after re-credentialing; reads work, writes 401/403). All session writes (main, state, branches) went over explicit SSH URLs.
- **1w62 ideation attempt-1 REJECTED by captain.** The staff-review rounds had ratcheted coherence (~2000 insertions, 16 surfaces: delivering/reversed states, reversal journal, scanner rewrite, flock/CAS regime, guards everywhere, legacy adoption violating durable-ruling :13) without challenging whether the mechanism should exist. Captain authored the six-point minimum (consume routes without spending; merge guard sole consumer; success = one atomic pending→consumed + terminal fields; rework = existing pending→superseded via declared feedback-to; retry leaves pending; nothing else unless the transaction proves necessity).
- **Staff review is now necessity-first** ("should this mechanism exist at all" before soundness) — landed in the workflow README + roadmap.
- **Pre-approval corrections (captain):** `gate consume` must not discover/arm merge hooks (merge-mechanism-agnostic; merge guard arms at delivery); `merge guard --failed` renamed `--rework` for the irreversible supersede-and-route.
- **Validation attempt-1 REJECTED by captain:** implementation had invented an unapproved second authority writer (`internal/status/envelope.go` seam, `updateFrontmatterNode` gate-subtree generalization, gate-authority exports into status). FO verified both harm proofs: (1) `bindingTerminalApproval` nil-collapse → terminalize-with-pending-authority (impl claimed fail-closed; it was fail-open); (2) authority writes riding CAS-less `runSet` outside `.gates.lock`. Captain prescribed: two narrow gates-owned locked ops over `io.go`'s variadic replacement machinery; pr sentinel retained through archive; `mod-block` cleared in its own step.
- **Validation attempt-2 revise (captain):** four narrow corrections — rework output must name the gated stage, not `elig.TargetStage`; digest-stale fixture must arm mod-block+sentinel before tamper; AC-2 evidence map re-pointed from a deleted test to the shared-finalize composition; Roborev 578 findings classified (trigger/harm/ownership) before any edit. Lock-removal regression test ruled disproportionate-now.
- **Validation attempt-4 APPROVED** (0.26-dev binary consumed into done at that point — old semantics, since the running host predates the fix).
- **Captain directed the state-history rewrite + retroactive application with the candidate binary** so the durable record reflects the doctrine before the merge.

## Issues — Workflow

- Version-gate prose ambiguity bites live agents on dev-stamped versions (4 distinct sessions hit the class this day; behaviors varied ask/abort/block). Owned by `dev-stamp-in-tree-version-gate-compat`.
- Stage-report completeness proof trips on an evidence-less checklist item (blank line between item and `### Summary`); one repair round needed. The `--set` advance-refusal message doesn't name which structural check failed — cost a debugging round.
- `spacedock state commit` push leg silently failed against the HTTPS remote for ~5 consecutive commits (local-only until plain-SSH push; outputs printed nothing). Watch for it while gh auth is dead.
- pr-merge ceremony for z3 ratified the very hole being fixed (done-before-delivery for z3 AND 1w62-attempt-4 under the old binary) — motivating context for the shipped fix.

## Issues — Spacedock

- `gate prepare` accepts an attempt on an UNGATED stage (implementation, gate:false) when the entity status is stale; the FO's own process had to close it as an error-hold. Candidate: prepare should refuse ungated current stages. Not filed (gh unreachable; candidate for the next session).
- Terminal-consume semantics gap itself — shipped by this session's PR-to-be (#TBD, branch `spacedock-ensign/resolution-consume-terminal-before-delivery`).

## Observations

- **The quick-dogfood (qd) testimonial.** Same binary command, two semantics, one day:
  - old binary `gate consume`: `application=advance/consumed ... consumed=true target-stage=done` → `status: done`, authority spent before delivery;
  - candidate `8750d608b` `gate consume` (retroactive, after history rewrite): `application=advance/pending ... consumed=false target-stage=done route=approved-awaiting-merge` → `status: validation`, zero entity mutations.
  The pending model ran for real. State is parked resumable: resolution closed (person:captain), application pending, nothing armed, nothing delivered.
- Resume semantics verified: a new-binary session resumes this pending ceremony exactly (`consume` idempotent re-route → merge guard arms → 0.12.5 mod). An old-binary session would resume the old model (works, but spends the approval at resume) — handoff must pin the launcher.
- The `approved-awaiting-merge` projection already shipped on main (`internal/gates/model.go:219`) — the design reused existing vocabulary, so parked pending entities display correctly today.
- Reviewer calibration: gpt-5.6-sol/xhigh asked before acting at friction points vs codex-lane runtime blocked silently — model variance under ambiguous prose; evidence into the dev-stamp task.
- Ensign round reports should state file counts from `git diff` output, not recollection (22-vs-23 defect required a second pass).

## What's Next

- **Next session (gh access confirmed):** complete the 1w62 merge ceremony — candidate-binary `merge guard --verdict passed` (arms at delivery), pr-merge 0.12.5 draft (presented and pre-shaped this session), push via SSH, `gh pr create`, CI watch (repo rule: this diff touches `internal/gates` + shipped FO contract → live lanes required-green, flakes re-run serial-isolated), captain merges, sentinel `pr=pr-merge:N`, `merge guard` finalize, worktree/branch cleanup. Full handoff: `/tmp/sd-handoff-1w62-pr-ceremony.txt`. Rebuild the candidate from branch tip `8750d608b`; do not trust `/tmp` builds across hosts.
- **Captain's desk:** `dev-stamp-in-tree-version-gate-compat` seed shaping (he's editing it); gh keyring re-credential; watch the Homebrew `node/libllhttp` env flake (healed mid-session; piruntime full-suite green now).
- **Parked (do not pull):** 3 entities at ideation approved-awaiting-advance (`gate-agent-ergonomics` sk, `respect-gate-application-authority` mn, `entity-session-claim-lease` 79); 26n lineage (captain-handled).
- **Deferred on record:** lock-removal regression test for the delivery ops (promote when concurrent-FO support lands); concurrency race class; `gate prepare`-while-pending shadowing (accepted residual, fails closed).
