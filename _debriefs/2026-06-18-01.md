---
session-date: 2026-06-18
sequence: 1
first-commit: 83379d95
last-commit: f2adbc69
duration: ~8h (2026-06-17 evening → 2026-06-18, incl. one crash + resume)
---

# Session Debrief — 2026-06-18 #1

Shaping-FO session driving the **0205-layered-fo** sprint toward Commander-dispatch-readiness. Landed the CI-version-pin keystone (#395), ideated + staff-reviewed the verb-core (gate-extract, merge-guard, state-verbs) and the tier-delegation member, carved the remaining 0205 roster, descoped the next-action track to 0.20.6, and drove m4 (the tmux team-mode harness) to a **local green** — but it's **RED in CI** on an interactive-auth/pty gap. Survived a mid-session crash (state intact, team agents lost). Several captain decisions are still pending (two verb gates, principles ratification, the m4-CI gap).

## Shipped
- **61** `pin-ci-claude-code-version` — [#395](https://github.com/spacedock-dev/spacedock/pull/395). Pins the live-e2e CI lane to Claude Code 2.1.177 (the last team-tool-capable release; 2.1.178+ dropped team tools) via a single `SPACEDOCK_PINNED_CLAUDE_VERSION` source-of-truth read by both the install fallback and an enforced `claude --version` assertion; detached adversarial audit refuted nothing material. Merged "no CI" (deterministic checks + audit; the live lane was broken pre-pin). **The keystone** — unblocks team-tool-capable CI for m4 + the 72 standing residency.

## Filed (backlog)
- **rgq** `state-verbs` — `spacedock state ready/commit <slug>` (+ `sweep` as a guillemet); the rebase-conflict HALT enforced by the verb's exit code. 0205, ideated, staff-approved, gate presented.
- **mz** `merge-finalize` — `spacedock merge guard <slug>`: atomic mod-block set→clear→terminalize ceremony envelope. 0205, ideated, staff-approved, gate presented.
- **czw** `prose-function-restructure` — express the FO cores as `«fn»` invocations (backtick verbs / guillemet recipes). 0205, stamped, needs ideation (sequenced after 6re+72 shared-core edits).
- **kt** `haiku-drive-validation` — the live Haiku-FO end-to-end drive (the sprint gate proof). 0205, stamped, **defer** (LAST; needs all members + m4's CI backend green).
- **vp** `dispatch-build-request-file` — single Markdown request-file input for `dispatch build` (schema shared with next-action). **Moved to 0206-dispatch-driver.**
- **dwd** `command-surface-gate` — a contractlint allowlist gate that enforces the command-surface anti-sprawl principles in the binary (not prose). 0205, ready.

## Non-PR commits (workflow-only)
State transitions and scaffolding that don't belong to a PR:
- `0e72c079` implementation(6re): all four `status --read` additions + step-4 rewrite + doc-diff + staff Polish (6re built; Commander drives validation per captain).
- `7c1d7934` **[reverted-then-restored]** revert(6re) back to gate-approved ideation (Shaping-FO role correction) — then `e3187f89` restore(6re) to implementation (captain: finish implementation, stop before validation).
- `02cb99d0` + `9d115791` m4 validation cycle-2: AC-4 reframe (accept clean-terminal OR marker) + stored-login seed → both live pty tests GREEN locally on 2.1.177.
- `5e3dfec6` move(m4) 0204→0205; `75e33c59` descope(vp) 0205→0206; `32774d88` carve(0205) the 4 stamps.
- Ideation cycles on 6re: `e8e7b762` (cycle 2), `44c49ce8` + `17b7fe63` (cycle 3 — consolidate into status --read + --fields).
- 61 ceremony: `857321e6` mod-block → `e57895f9` pr#395 → `a770809a` clear → `48702117` terminalize → `6d81cfef` archive.
- `eb88227a` file vp; `f231c897` ideation(state-verbs); `71f486f5` ideation(merge-finalize); `f2adbc69` file command-surface-gate.
- Code (main / branches): #395 (61) merged to `main` (origin at `ea03d094`); 6re branch `spacedock-ensign/gate-extract-verbs` (4 commits, built, unmerged); m4 branch force-pushed to `31d22d25` (PR #390 open).

## Decisions
- **Descope next-action + vp → 0.20.6** (`0206-dispatch-driver`, seeded). Tightens 0205; the w4 spike found `«next»` HOLDs (prose-operable), so the haiku-drive-validation doesn't depend on next-action being binarized.
- **m4 → 0.20.5** (0.20.4 closed; m4 was deferred into it).
- **6re consolidation:** dropped the proposed `spacedock gate` command; folded into `status --read` modes (`--stage X --checklist`/`--ac-scan`) + reused the existing `--fields` flag. Anti-sprawl.
- **Architecture (merge/state verbs):** **keep as CORE cobra subcommands**, not standalone frontdoor-routable binaries. Pluggability lives at the **workflow-definition layer** (README policy + registered mods); the verbs are backend-AWARE, not backend-routable. Standalone routing is net-new + YAGNI + weakens the layered-FO safety thesis (binary owns the non-optional invariant). Codified in `docs/dev/_proposals/command-surface-and-routing-principles.md` (PROPOSED, pending ratification) + the `command-surface-gate` enforcement task.
- **Role correction:** Shaping-FO stops at the ideation gate + writes the package; the **Commander** drives implementation→validation→done. (FO slipped once — dispatched 6re/61 implementations — caught + rolled back.) `61` and `m4` were **captain-greenlit FO-driven exceptions** (the keystone; a directed targeted fix).
- **6re:** captain directed "finish implementation, do not dispatch validation" → 6re built (handoff-ready), Commander validates.
- **m4 merge "no CI" / m4 to CI:** 61 merged without the (pre-pin-broken) live lane; m4 rebased + reframed + pushed + CI-E2E approved to probe the pinned-CI interactive auth.
- **state sweep:** recommended (ensign + reviewer) to **stay a guillemet** pointing at `dispatch reconcile` (read-only, no verb needed) — captain ratification pending.

## Issues — Workflow
- **m4 RED in CI (the live, correct run `27726848191` on `31d22d25`):** the pty team-mode step fails — the keychain stored-login seed is **macOS-only** (skipped on Linux CI per AC-6), so CI falls to the env credential and the interactive child writes **no transcript + an empty pane → 4-min timeout**. m4 is **green locally** (macOS keychain). The harness + AC-4 reframe are correct; the **CI interactive-auth/render path is the unsolved gap**. Cause not fully pinned (interactive `ANTHROPIC_API_KEY` auth failure vs CI-Linux pty no-paint — the empty pane points more at render than the local 401-banner). **This blocks `haiku-drive-validation`'s CI backend.** Needs a focused task (likely a CI-secret-seeded stored-login for the Linux runner, or pinning whether interactive CC auths from `ANTHROPIC_API_KEY` at all).
- **FO stale-CI-run misread:** the FO approved + diagnosed an OLD run (pre-fix commit `2b4beaa0`), produced a wrong "m4 RED" diagnosis, then corrected after the captain flagged it. Lesson: **verify the run's `headSha` matches the pushed commit before reading CI results.**
- **Crash mid-session:** killed the FO session + all team agents (the team config still lists 12 zombie members). State survived intact (all ensign commits pushed pre-crash). On resume: presume all prior agents dead; fresh `TeamCreate` on next dispatch.

## Issues — Spacedock
- **Interactive Claude Code 2.1.177 does not honor `CLAUDE_CODE_OAUTH_TOKEN`** the way headless `-p` does — it 401s interactively (banner "Claude API") and requires a stored-login `.credentials.json` in the config dir. (m4's seed works around it locally; CI Linux has no keychain.) Upstream CC behavior, not a spacedock bug, but the harness/CI must account for it. **Not filed** (upstream).
- **`status --set` merge-hook guard misfires on a BACKWARD/non-terminal transition:** setting `status=ideation` (or clearing `worktree=`) on an implementation-stage entity returned `Error: ... cannot advance to terminal — workflow has merge hook(s) ... that have not run` — a misleading message for a non-terminal backward set; `--force` was needed. Minor UX bug. **Not filed** (candidate).

## Observations
- **The layered-FO safety thesis is the dominant design lens:** the binary owns the mechanical invariant so a weak FO can't reorder/skip it. It decided the merge/state architecture (core, not routed) and motivates the command-surface gate.
- **Prose rules don't hold — make them code gates.** Two instances this session: the Shaping-FO/Commander role split (prose in the roadmap README → the FO slipped) and the command-surface principles (prose proposal → `command-surface-gate` task is the dog-food fix).
- **Local green ≠ CI green.** m4's macOS-keychain local pass did not translate to the Linux CI runner — the riskiest mechanism (CI team-mode auth) must be validated IN CI, which is exactly what the pin enabled us to discover now rather than at the final drive.
- **2y MERGED** (#385) this period — unblocked the entire 2y-dependent verb core.

## What's Next

**Captain decisions pending (carried forward):**
1. **Ratify** the command-surface principles + routing decision (keep-core) — and how the proposal doc lands.
2. **Approve the two verb gates** — `merge-finalize` (mz) + `state-verbs` (rgq), both staff-approved, hold as-is (+ `state sweep` = guillemet). Approving gets the verb-core gate-approved.
3. **m4 CI gap** — file a focused "CI interactive-auth/render for the pty lane" task; decide whether `haiku-drive-validation` relies on the CI backend or an alternative.
4. **`command-surface-gate`** (dwd) — keep (it's legit) or shelve (it was an FO misread of "binary gate").
5. **`vv` `launcher-resident-process`** — captain wants it (post-crash resilience: resident launcher so zellij can relaunch). Untagged backlog, seeded, ready to ideate — ideate now? which home (0205 / standalone resilience)?

**Shaping-FO remaining for 0205 Commander-readiness:**
- Close the two verb gates (above) → **ideate `prose-function-restructure`** (sequence: 6re step-4 + 72 step-1.5 land first; restructure re-expresses on top) → **write `dispatch-sprint-execution.md`** (the cold-boot Commander package). `haiku-drive-validation` stays deferred (LAST + m4-CI-backend gate).

**0205 roster:** `6re` built (handoff-ready) · `72` gate-approved · `mz`/`rgq` gate-presented-awaiting-vote · `czw` needs-ideation · `kt` defer · `m4` validation (local-green/CI-red) · `y2` defer.

**Cross-sprint:** `0206-dispatch-driver` seeded (next-action + vp). Orphan `27` agy-runtime-support still parked at implementation (long-stale, no sprint — needs a disposition call).

**Team:** all agents dead post-crash → fresh `TeamCreate` on next dispatch; re-dispatch from entity state.
