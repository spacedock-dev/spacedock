===== exit =====
0
===== stdout =====
{
  "schema_version": 2,
  "description": "Thing: validation",
  "fetch_commands": [
    "/opt/spacedock/bin/spacedock dispatch show-stage-def --workflow-dir <ROOT> --stage validation"
  ],
  "dispatch_file_path": "/tmp/spacedock-dispatch/spacedock-ensign-thing-validation-advance.md",
  "prompt": "Advancing to next stage: validation.\n\nRead /tmp/spacedock-dispatch/spacedock-ensign-thing-validation-advance.md and treat its content as your next-stage assignment.",
  "model": null
}

===== stderr =====

===== body =====
## Advancing to next stage: validation

You are continuing work on: Thing

Your working directory is <ROOT>/.worktrees/spacedock-ensign-thing
All file reads and writes MUST use paths under <ROOT>/.worktrees/spacedock-ensign-thing.
Your git branch is spacedock-ensign/thing. All commits MUST be on this branch. Do NOT switch branches or commit to main.

Continue working on the entity at <ROOT>/.worktrees/spacedock-ensign-thing/thing.md.

### Completion checklist

- a
- b

### Summary
{brief description of what was accomplished}

### Fetch commands

    /opt/spacedock/bin/spacedock dispatch show-stage-def --workflow-dir <ROOT> --stage validation


### Completion Signal

When you finish (after all commits and stage report writes are done), your last action MUST be:

    SendMessage(to="team-lead", message="Done: Thing completed validation. Report written to <ROOT>/.worktrees/spacedock-ensign-thing/thing.md.")

**If you are the first officer forwarding this prompt to Agent():** copy the entire block above into `Agent(prompt=...)` character-for-character. Do NOT paraphrase `SendMessage(to="team-lead", ...)` as "SendMessage with to='team-lead'" or any other English rewrite — the parenthesis-equals syntax is the literal call the ensign must emit, not a description of one.