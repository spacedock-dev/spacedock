---
id: ywjfm1fjmqbpqe7dwjcwt841
title: README slim + docs-architecture cleanup — site is canonical, remove install-journey.md + its prose-grep test
status: validation
source: "captain (2026-06-13) — now that the mkdocs doc site shipped (#343), the repo's standalone install/usage docs duplicate it. README should be a thin front door; install-journey.md is redundant; its test is a banned prose-grep."
started: 2026-06-13T05:03:08Z
completed:
verdict:
score:
worktree: .worktrees/spacedock-ensign-readme-and-docs-architecture-cleanup
issue:
sprint: 0201-post-flip-release-model
group: docs
sprint-readiness: ready
---

Now that the doc site (`docs/site/`, shipped via #343) is the canonical browsable documentation, the repo carries redundant docs. Establish the rule **site is canonical; the GitHub README is a thin front door**, remove the duplication, and — by captain decision — make this task the **sole owner of the whole 0.20.1 sprint's user-visible doc surface** so the five behavior members touch NO docs.

> **Branch grounding (read first — verified against `origin/main` HEAD `55158746`, the canonical target; local `main` is behind and must not be trusted).** This task targets **`origin/main`**, the post-flip trunk (roadmap 0201; the doc site shipped via #343). The site on `origin/main` is a **slim, restructured rewrite** (#343) — NOT a mirror of the old `install-journey.md`. The true per-branch state (`git cat-file -e` / `git show origin/main:…`):
>
> | file | on `origin/main` | on `next` |
> |---|---|---|
> | `docs/site/` (the canonical site) | **present, slim rewrite** (#343) | absent |
> | `docs/site/get-started/install.md` | present, **~50 lines, tabbed install + Launch/Skills/Sandboxing/Troubleshooting/Next** — NOT the 168-line journey | absent |
> | `docs/install-journey.md` | **absent** (the slim site replaced it) | present (168 lines) |
> | `internal/release/install_doc_test.go` | **present**, reads `docs/site/get-started/install.md`, still the banned prose-grep | present, reads `docs/install-journey.md` |
> | `README.md` | unslimmed, but Install already links the site | links `docs/install-journey.md`, has How-it-works/Usage |
>
> **What this changes for the task.** On `origin/main` the file-deletion is already done — `install-journey.md` is gone, and the site is a *slimmer* page, not a copy of the journey (the earlier ideation claim that the site "mirrors" the journey was read against local `main` and is WRONG). The **prose-grep test still exists**, repointed to read the slim `install.md`; repointing did not cure the tautology, so removing it is still the live deliverable. The slim rewrite also means **most of the five members' retarget anchors do not exist as-is** — several need a NEW section added to a real page (see the corrected map below, every anchor reverified against `origin/main`). The live deliverables on `origin/main`: (1) remove the prose-grep test, (2) slim the README, (3) own the five behaviors' site docs on REAL anchors. AC-1's `install-journey.md`-absence is already true on `origin/main` (the grep-clean is the live half there).

## Problem

- `internal/release/install_doc_test.go` is a **banned prose-grep**, and repointing it at the site page (as `main` did) did not cure it. Both tests `os.ReadFile` the install doc and assert substrings the author wrote — a `curl … install.sh | sh` line (`TestInstallJourneyDocumentsLinuxPath`) and the absence of "sandboxed on linux" phrasings (`TestInstallJourneyDoesNotOverclaimLinuxSandbox`). The expected values come from the file under test, not an independent source; a valid paraphrase fails them and an inverted clause passes them. This is the instruction-file-read / prose-grep the workflow quarantine forbids outside `internal/contractlint`. (The test names still say "InstallJourney" though `main` repointed the read to the site page — a stale name, but the removal makes that moot.)
- `README.md` carries "How it works" / "Usage" / "Quick start" prose that duplicates the site's `concepts/` + `running-workflows/` + `reference/command-reference.md`.
- `docs/install-journey.md` (169 lines on `next`) is a duplicate of `docs/site/get-started/install.md` (the canonical page). On `main` it is already gone; on `next` it is dead weight. The grep-clean (nothing references it in live docs/code) is the durable invariant.

## Anti-collision: yw owns the whole sprint's doc surface

By captain decision, this task is the **single writer** of every user-visible doc change in 0.20.1. The five behavior members (`gj`, `te`/`tes`, `gp`, `zrc`, `8p`) each authored a doc-diff in their ideation bodies; **those doc-diffs are SUPERSEDED by this task** and the members ship code-and-tests only, touching no `*.md`. The reason is concrete: every member's doc-diff edits `docs/install-journey.md` — the exact file this task **deletes**. If each member applied its own diff, they would collide on a file that no longer exists, and the README's launch/version lines would be edited by three hands. Consolidating the doc surface here removes the collision and lands each behavior's documentation on its **canonical site page** instead of the dead standalone doc.

### Canonical home per member (the retarget map)

For each behavior member, the table names where its now-superseded doc-diff content lands. The member's original diff (cited) is the intended wording; the canonical home is the site page or section this task edits instead. **Every "Canonical home" anchor below was reverified by reading the actual page on `origin/main` HEAD `55158746`.** Because #343 slimmed the site, several anchors the members' diffs assumed (a `--version` example, a "Confirm it" step, a "Keep things in sync" section, a `first-launch.md` page) **do not exist** — those rows name a NEW section yw ADDS to a real page, so AC-4 can cite an anchor that exists post-change.

| Member | User-visible change | Member's original diff target (SUPERSEDED) | **Canonical home (yw edits) — anchor reverified on `origin/main`** |
|---|---|---|---|
| **gj** (`gja5h…`, startup-sandbox-status) | `--version` gains a `Sandbox:` line + per-runtime install/enablement block; startup banner + `status --boot` gain sandbox state | `docs/install-journey.md` two `--version` spots; `README.md:139` | **ADD a new `## --version` section to `docs/site/reference/command-reference.md`.** Reverified: that page mentions `spacedock --version` only inline ("a top-level `spacedock --version` (the binary version and contract level)") with **no version-example block and no `--version` heading** — there is no existing anchor to amend. yw adds a `## --version` section carrying the example output (`spacedock 0.20.0 (contract 1)` + the `Sandbox:` line + the per-runtime block). The sandbox three-state phrasing can also extend the one-row table in `docs/site/reference/sandbox.md` (which exists, but today is a single `safehouse / macOS / trigger` row — a candidate secondary home, not required). |
| **te**/**tes** (`tes9t…`, install-refresh + upgrade hint) | doctor + front-door gate print an opt-in upgrade hint on a contract-compatible-but-behind plugin; `spacedock install --host codex` refreshes a present plugin | `docs/install-journey.md` "Keep things in sync" | **Amend the existing line in `docs/site/reference/command-reference.md` `## Setup`.** Reverified: there is no "Keep things in sync" section on the slim `install.md`; the doctor-refresh guidance lives in command-reference `## Setup` ("When `doctor` reports the plugin is out of date, refresh it with `spacedock install`"). yw extends that sentence to name the contract-compatible-but-behind upgrade-hint case (doctor + the front-door launch print an opt-in hint). |
| **gp** (`gpvg3…`, marketplace + pinned channels) | edge channel (`spacedock-edge`) is a first-class marketplace channel tracking `next`; stable serves a pinned tag | `docs/install-journey.md` "Build from source" edge note; `docs/releasing.md` (the manifest-in-plugin-branch lines) | **`docs/releasing.md`** (CORRECT — reverified: `docs/site/contributing/releasing.md` is a `120000` symlink → `../../releasing.md`, and that file carries the "keeps the marketplace entry serving the stable plugin from `main`" line + the "Cutting a Stable Release" steps gp's diff targets; yw makes the decouple-accurate edits there). PLUS the edge-channel **user** pointer goes in **`docs/site/contributing/build-from-source.md`** (reverified: the "Build from source" content MOVED off `install.md` to this new page; its closing paragraph already names the `next` dev channel — the natural spot for a `spacedock-edge` pointer). NOTE: the deep stamp-then-tag rewrite is `ezn`, **deferred** out of 0.20.1 — yw makes only the decouple-accurate edits gp's diff named. |
| **zrc** (`zrcmx…`, non-sandboxed auto-mode) | unsandboxed `spacedock claude` starts in `--permission-mode auto`; unsandboxed `spacedock codex` gets the captain-chosen equivalent (`--ask-for-approval on-request` under option A) | `README.md` launch lines; any `docs/` enumerating the inner host argv | **`docs/site/reference/command-reference.md` `## Launch`** (CORRECT primary — reverified it exists). The Launch section today does NOT enumerate the inner permission posture, so yw ADDS a sentence naming the unsandboxed-arm flag (`--permission-mode auto` for claude; the codex equivalent per the captain's pick) vs the sandboxed-arm skip-permissions. **`get-started/first-launch.md` is DROPPED — reverified it does NOT exist on `origin/main`** (get-started is `install` / `first-workflow` / `survey`). **Pending the captain's codex option A-vs-B choice** (zrc's open gate item). |
| **8p** (`8pwjd…`, brew agentsview dep) | `brew install spacedock` now also installs `agentsview`; safehouse stays a separate caveat | `docs/install-journey.md` Homebrew install step | **`docs/site/get-started/install.md`, the `=== "macOS (Homebrew)"` tab** (CORRECT — reverified: that tabbed Homebrew block exists). yw adds the one-sentence "also installs agentsview; safehouse is installed separately (see [Sandboxing](../reference/sandbox.md))" note under it. |

**Sequencing inside the Commander drive (recorded, not an AC):** yw's *structural* cleanup (remove the prose-grep test, slim the README — and delete `install-journey.md` only if work touches `next`, since on `origin/main` it is already gone) leads and can land immediately — it depends on nothing from the five members. yw's *behavior-doc* updates (the table above) follow each member **landing its code**, because the doc describes shipped behavior; a doc that describes `--version`'s sandbox block before `gj` ships is a lie. So the drive order is: (1) yw structural cleanup → (2) the five behaviors land code+tests → (3) yw's behavior-doc pass updates the canonical site pages. The whole surface targets `main` under the post-flip trunk model.

## Proposed approach

1. **Slim README** to the front-door essentials a repo visitor/contributor needs without leaving: one-line what-it-is + the "what's different" hook; the single install path (`brew install …` → `spacedock claude`) then a prominent **"full docs"** link to the published doc site; build-from-source + run-tests; License + CONTRIBUTING pointer. Move "How it works" / "Usage" prose to the site (it already lives on `docs/site/concepts/` + `docs/site/reference/command-reference.md`). See the README skeleton below for the exact target shape.
2. **Ensure `docs/install-journey.md` is absent and unreferenced.** On `main` it is already gone (its content is the canonical `docs/site/get-started/install.md`); the live work there is the grep-clean. If work lands on `next` first, the deletion is real there — no content fold needed (the site page already carries it, verified). The durable end-state is: file absent, nothing references it.
3. **Remove `internal/release/install_doc_test.go`** — the banned prose-grep, whichever doc it reads (`main` repointed it to the site page; that did not cure the tautology). The two real behaviors it gestured at are recorded as owed-REAL-tests below, NOT re-added as greps: (a) the Linux `install.sh` path actually works → a behavior/smoke test, not a doc-grep; (b) "spacedock ships no sandbox" honesty → already a code fact (`internal/safehouse` only detects a profile + wraps argv), guarded in code, not by scanning doc prose.
4. **Apply the five behaviors' doc content to the canonical homes** in the retarget map above, after each member's code lands.

### README target skeleton (AC-3's checkable shape)

The slimmed README keeps these sections and drops the rest:

- `# Spacedock` + the one-line what-it-is + "Why?" hook (kept — the front-door pitch).
- `## What's different` (kept — the differentiator, short).
- `## Install` — the `brew tap … && brew install spacedock` block + the one-line `spacedock claude "/spacedock:survey"` launch + the **full-docs link** (see decision below). **Dropped from here:** nothing; this stays the install front door.
- `## License`.
- A `CONTRIBUTING` pointer line (the site's `contributing/` is canonical; README links it).
- **Removed entirely:** `## Quick start` (the commission examples — now `docs/site/running-workflows/commission.md`), `## How it works` (now `docs/site/concepts/operating-model.md`), `## Usage` (the command grammar table — now `docs/site/reference/command-reference.md`).

The removed `## Quick start` / `## How it works` / `## Usage` sections and the presence of the full-docs link are AC-3's checkable change (a structural README diff).

### Open decision for the gate — the "full docs" link target

The assignment names a "prominent spacedock.md link," but no `spacedock.md` file exists anywhere in the repo or its history; it is shorthand for "the canonical doc-site link." Two readings:
- **(A) Link the published doc site URL** (`https://spacedock-dev.github.io/spacedock/`, the mkdocs `site_url`) — the browsable canonical home a README reader expects from "full docs." **Recommended.**
- **(B) Link the in-repo `docs/site/index.md`** path (what `main`'s README does today for install, pointing at `docs/site/get-started/install.md`).

**Recommendation: (A)** as the headline "full docs" link, keeping a secondary in-repo `docs/site/get-started/install.md` path link for readers browsing the repo on GitHub. AC-3 is written to accept either a published-site URL or an in-repo `docs/site/` link as the checkable target; the captain picks the headline at the gate.

## Out of scope

- The doc site's own *structural* redesign (nav, new pages). This task points the README at the site and lands the five behaviors' content on existing site pages; it does not restructure the site.
- The deep **stamp-then-tag** release-ritual rewrite of `docs/site/contributing/releasing.md` — that is `ezn`, deferred out of 0.20.1. yw makes only gp's decouple-accurate edits.
- The behavior **code/tests** themselves — those are the five members' deliverables. yw owns only their *docs*.

## Owed REAL tests (recorded, not re-added as greps)

The deleted `install_doc_test.go` gestured at two real behaviors. Neither is re-added as a doc-grep; each is recorded here for follow-up as a genuine behavioral check:

- **Linux `install.sh` path works.** A smoke test that runs `install.sh` (in a container or with a stubbed download) and asserts the `spacedock` binary lands on `PATH` and `spacedock --version` exits 0 — proves the install path, not that a doc mentions a URL. Follow-up; not blocking this task.
- **No-sandbox honesty.** Already a **code fact**, not a doc claim: `internal/safehouse` only detects a `.safehouse` profile and wraps argv when a `safehouse` binary resolves; it ships no sandbox. The honesty is guarded by the safehouse package's own behavior tests (a profile-present-but-binary-absent launch proceeds unsandboxed), not by scanning doc prose. No new test owed — the code already can't over-claim.

## Acceptance criteria

Each AC names a property of the finished entity, verified outside the task body.

**AC-1 — `docs/install-journey.md` is gone and nothing references it.** Verified by: the file is absent on the target branch (already true on `main`); `git grep -l install-journey -- '*.md' '*.go' '*.y*ml'` (excluding `_archive`/`.spacedock-state`/`.claude`/`docs/roadmap` historical debriefs) returns nothing in live docs/code — command output. (Roadmap debriefs under `docs/roadmap/019x*` are historical records of past sprints and are NOT rewritten; the grep scope excludes them. The grep MUST also come back clean of `install-journey` in `README.md` and in any live `*.go` — on `main` the only live `*.go` hit today is `install_doc_test.go`'s error strings, which go away with AC-2.)

**AC-2 — the prose-grep test is gone and the suite is green.** Verified by: `internal/release/install_doc_test.go` is absent (it is the banned prose-grep regardless of which doc it reads — `main` repointed it to `docs/site/get-started/install.md` without curing the tautology) and `go test ./...` is green from the repo root — exit code. Removing it is clean (verified during ideation): `readInstallJourney` lives only in `install_doc_test.go` (goes with the file), while `executableShellCommands` is defined in `workflow_exec_guard_test.go:441` and used by three other guard tests (`channel_agreement_guard_test.go`, `e2egate_workflow_test.go`, `workflow_exec_guard_test.go`) — it STAYS. So deleting the whole `install_doc_test.go` file leaves no dangling references; `go test ./internal/release/` green is the proof.

**AC-3 — the README is a thin front door linking the site.** Verified by: README contains the install one-liner + a canonical full-docs link (a published-site URL or an in-repo `docs/site/` link) + build/test + License/CONTRIBUTING pointer, and no longer carries the `## Quick start`, `## How it works`, or `## Usage` sections — observed in the rendered README diff (the link target and the three removed section headings are the checkable change). A structural check can assert the three section headings are absent and a `docs/site/` (or `spacedock-dev.github.io`) link is present.

**AC-4 — the five behaviors are documented on their named canonical site pages (anchors reverified on `origin/main`), and no member ships a `*.md` edit.** Verified by: after the behaviors land, each canonical home in the retarget map carries the behavior's wording, checkable by `git show` of the changed page —
- `docs/site/reference/command-reference.md` has a **new `## --version` section** whose example shows the `Sandbox:` line + per-runtime block (gj);
- `docs/site/reference/command-reference.md` `## Launch` names the unsandboxed `--permission-mode auto` / codex-equivalent posture (zrc);
- `docs/site/reference/command-reference.md` `## Setup` names the contract-compatible-but-behind upgrade hint (te);
- `docs/site/get-started/install.md` Homebrew tab names the agentsview brew dep (8p);
- `docs/releasing.md` carries the decouple-accurate edit (no longer claims the plugin-branch manifest serves stable) and `docs/site/contributing/build-from-source.md` names the `spacedock-edge` edge channel (gp).

Each is an anchor that EXISTS after the change (a heading yw adds, or an existing section it amends), so a `git show origin/main:<page>` post-change can confirm the wording landed — not a grep for a pre-assumed string that may not have a home. The proof that NO member edited docs is that each member's PR/commit touches no `*.md` (`git show --stat` over each member's change shows zero doc files). This AC is the anti-collision contract made checkable: the doc surface moved as one diff, authored once. (Note: AC-4 lands in drive step 3, after the behaviors; AC-1/AC-2/AC-3 land in step 1 and do not wait on the members.)

## Test plan

Mostly deletion + a README rewrite + a site-page doc pass. AC-1/AC-2 are command/exit-code checks (cheap, land first with the structural cleanup). AC-3 is a structural README diff (land first). AC-4 is a set of site-page diffs that land after the five behaviors ship their code — its proof is the diffs plus a per-member `git show --stat` showing zero `*.md` edits. Owed real behavior tests (Linux install path; no-sandbox honesty already code-guarded) are recorded above for follow-up rather than re-added as a grep.

**Sequencing:** this task is split into two waves inside the Commander drive — wave 1 (structural cleanup, AC-1/2/3) leads and depends on nothing; wave 2 (behavior-doc pass, AC-4) follows the five members landing their code. The whole surface targets `main` under the post-flip trunk model. No spike needed: the mechanism is file deletion + Markdown edits over an already-existing canonical site; no parser round-trip, runtime handoff, or on-disk format is at risk. The one fact the structural-cleanup half rests on — that removing `install-journey.md` and its prose-grep test loses no canonical content — holds because on `origin/main` the install-journey content was already replaced by the slim site (#343 restructured it across `get-started/install.md`, `reference/command-reference.md`, `reference/sandbox.md`, and `contributing/build-from-source.md`). The site is a *slimmer rewrite*, not a line-for-line copy; the install journey's how-to lives on those pages in condensed form. (Confirmed by reading the `origin/main` pages directly; a cross-branch `git diff` is not meaningful because the content was rewritten, not moved verbatim.)

## Stage Report: ideation

- DONE: Firm yw as the SOLE owner of the 0.20.1 sprint's doc surface — the structural cleanup AND the canonical site/README documentation for the five behavior members' user-visible changes, so those members touch NO docs. Read each member's ideated doc-diff and name the exact canonical home for each (gj, te, gp, zrc, 8p). The five members' own doc-diffs are SUPERSEDED by yw — record that.
  "Anti-collision" section declares yw the single doc writer and SUPERSEDES all five members' doc-diffs; the retarget map table names each member's canonical home (gj → `command-reference.md` `--version` + `install.md` Confirm-it; te → `install.md` Keep-things-in-sync; gp → `install.md` edge note + `contributing/releasing.md`; zrc → `command-reference.md` Launch + `first-launch.md` grammar; 8p → `install.md` Homebrew step), each citing the member's original (now-superseded) diff.
- DONE: Confirm `internal/release/install_doc_test.go` is removed (banned prose-grep); verify nothing else references install-journey.md (grep); record any owed REAL behavior test rather than re-adding a grep.
  AC-2 removes the test; verified the removal is clean (`readInstallJourney` is file-local, `executableShellCommands` is shared from `workflow_exec_guard_test.go:441` and stays). Exercised the AC-1 grep on both branches: `next` live refs = README + the test; `main` live refs = the test's stale error strings only. Owed-REAL-tests section records the Linux `install.sh` smoke (follow-up) and notes the no-sandbox honesty is already code-guarded in `internal/safehouse`, no new test owed.
- DONE: Produce the consolidated doc plan + ACs — external proof only. Note the implementation sequencing inside the Commander drive and the `main` post-flip target.
  Four ACs, all externally checkable: AC-1 (file absent + grep-empty, command output), AC-2 (test absent + `go test ./...` green, exit code), AC-3 (README structural diff — three section headings absent + a `docs/site/` or published-site link present), AC-4 (the five behaviors documented on named site pages + per-member `git show --stat` showing zero `*.md` edits). Two-wave sequencing recorded (wave 1 structural cleanup leads; wave 2 behavior-docs follow the members landing code); whole surface targets `main`.
- DONE: Riskiest-mechanism determination.
  No spike needed: the work is file deletion + Markdown edits over an already-existing canonical site — no parser round-trip, runtime handoff, or on-disk format at risk. The one fact the plan rests on (the site page already carries install-journey's content) was confirmed by a side-by-side read of both branch files.

### Summary
Reframed the task from a single-branch cleanup to the sprint-wide anti-collision doc-owner: yw is now the sole writer of every 0.20.1 user-visible doc change, and all five behavior members' doc-diffs are SUPERSEDED with their content retargeted to canonical `docs/site/` pages (mapped per member). Corrected the entity's branch grounding — `main` and `next` have diverged: `install-journey.md` is already gone on `main` (the site carries its content) and the prose-grep test was *repointed* to the site page on `main` (which did NOT cure the tautology, so its removal is still the live deliverable). Four externally-checkable ACs and a two-wave Commander-drive sequencing (structural cleanup leads, behavior-docs follow the members) recorded; the whole surface targets `main` under the post-flip trunk model. One open gate decision surfaced: the "full docs" link target (no `spacedock.md` exists — recommend the published doc-site URL).
> NOTE: this cycle-1 report's retarget anchors were read against LOCAL `main` (behind) and are partly wrong; the cycle-2 report below supersedes the map with anchors reverified on `origin/main`.

## Stage Report: ideation (cycle 2 — M1+M2 fold)

Preflight staff review found 2 Material defects, both from one wrong premise: I read the site pages from LOCAL `main` (behind), assuming `docs/site/get-started/install.md` mirrors the 168-line `install-journey.md`. On `origin/main` HEAD `55158746` (the canonical target) the site is a SLIM #343 rewrite, so several retarget anchors don't exist. Reverified every anchor by `git show origin/main:docs/site/…`.

- DONE: M1 — name the NEW section for each behavior whose content has no existing home; rewrite AC-4 to cite real, post-change-checkable anchors.
  Reverified on `origin/main`: command-reference.md mentions `--version` only inline (NO version-example, no `--version` heading) → gj now ADDS a `## --version` section there. The slim install.md has NO "Keep things in sync" section → te now amends command-reference.md `## Setup` (where the doctor-refresh line actually lives). gp's `docs/releasing.md` home is CORRECT (the `contributing/releasing.md` symlink → `../../releasing.md` carries the manifest-in-plugin-branch lines) — kept; the wrong install.md "Build from source" anchor is fixed to `contributing/build-from-source.md` (where #343 moved that content), the edge-channel pointer's real home. AC-4 rewritten to cite each anchor as one that EXISTS post-change (heading added or section amended), confirmable by `git show` of the changed page.
- DONE: M2 — drop zrc's non-existent `get-started/first-launch.md` secondary home; keep command-reference.md `## Launch`.
  Reverified `origin/main` get-started = `install` / `first-workflow` / `survey` — no `first-launch.md`. Dropped from the map; `## Launch` (which exists) is zrc's sole home, with a sentence ADDED naming the unsandboxed permission posture.
- DONE: Held clean (no change): install_doc_test.go removal (reverified it's still a banned prose-grep on `origin/main`, reading the slim install.md), AC-1/AC-2/AC-3, and the two-wave sequencing.
  Also corrected two stale cycle-1 claims that rested on the same wrong premise: the "site mirrors install-journey / is that content" line (the site is a slimmer REWRITE across four pages, not a copy) and the branch-grounding table (now grounded on `origin/main`, noting `install.md` is ~50 lines, not 168). Verified all README-skeleton link targets (operating-model, commission, command-reference, development-workflow, index) exist on `origin/main`.

### Summary
Folded both Material defects. Root cause: cycle-1 read the site from local `main` (behind origin) and wrongly assumed the site mirrors the old install-journey; on `origin/main` #343 slimmed the site, so the assumed anchors (a `--version` example, a "Confirm it" step, a "Keep things in sync" section, a `first-launch.md` page) don't exist. Every retarget anchor is now reverified against `origin/main` HEAD `55158746`: gj ADDS a `## --version` section to command-reference.md; te amends command-reference `## Setup`; gp keeps `docs/releasing.md` (via the verified symlink) and uses `contributing/build-from-source.md` for the edge pointer; zrc's `first-launch.md` secondary is dropped, leaving command-reference `## Launch`; 8p's install.md Homebrew tab is confirmed real. AC-4 now cites anchors that exist post-change, so it is externally checkable. install_doc_test.go removal and AC-1/2/3 held clean (reverified). No code or docs touched — ideation-body amendment only.

## Stage Report: implementation

Both waves landed in one pass (all 5 members merged; worktree branched off `origin/main` `cc5937c6`). Branch `spacedock-ensign/readme-and-docs-architecture-cleanup`; commits `31a7ae64` (wave 1), `816be5f7` (wave 2). The dispatch's 3 binding corrections (re-grounded against `cc5937c6`) were applied over the older-anchor entry body.

- DONE: WAVE 1.1 — remove `internal/release/install_doc_test.go` (banned prose-grep)
  `git rm`'d in `31a7ae64`. Verified clean first: `readInstallJourney` is file-local; `executableShellCommands` (def `workflow_exec_guard_test.go:441`, 9 other call sites) stays. `go test ./internal/release/` 76 passed; whole-repo `go test ./...` 1313 passed.
- DONE: WAVE 1.2 — slim README to the front-door skeleton
  KEPT title+Why hook, `## What's different`, `## Install`, `## License`, `## Contributing`; REMOVED `## Quick start` / `## How it works` / `## Usage` (verified absent). CORRECTION 1 applied: `## Install` now links `https://spacedock.md/docs/` (matches `mkdocs.yml site_url`, NOT the dead github.io) plus the secondary in-repo `docs/site/get-started/install.md`. IN-FLIGHT CHECK: re-read whole README — no orphaned cross-refs; only internal links are LICENSE / CONTRIBUTING.md / install.md.
- DONE: WAVE 1.3 — AC-1 grep-clean
  `install-journey.md` already absent on `origin/main`. `git grep -l install-journey` over live docs/code (excl `_archive`/`.spacedock-state`/`.claude`/`docs/roadmap`) returns nothing (the test's error strings vanished with item 1). Did NOT re-add the two gestured behaviors as greps.
- DONE: WAVE 2.4 — gj `## --version` section in `command-reference.md`
  Added section showing `spacedock 0.20.0 (contract 1)` + `Sandbox:` line + per-runtime block. Exact wording exercised by building and running `spacedock --version` (`/tmp/sd-yw`), not invented; three Sandbox states + four runtime states documented from `safehouse.State` / `runtimeLine`.
- DONE: WAVE 2.5 — te upgrade hint in `command-reference.md` `## Setup`
  Amended the doctor-refresh sentence to name the contract-compatible-but-behind opt-in hint (`run spacedock install --host <host> to refresh`; never blocks). Confirmed both `doctor` (`contract.RunDoctor` renders `res.Message` incl. hint) and the front-door (`gateHost`) print it.
- DONE: WAVE 2.6 — zrc unsandboxed posture in `command-reference.md` `## Launch`
  Added a sentence: claude `--permission-mode auto`, codex `--ask-for-approval on-request` unsandboxed vs `--dangerously-skip-permissions` / `--dangerously-bypass-approvals-and-sandbox` sandboxed. IN-FLIGHT CHECK resolved: read `pi.go` `runPi` — pi injects NO permission posture, so SCOPED the sentence to claude/codex only (no pi over-claim).
- DONE: WAVE 2.7 — gp decouple edits in `docs/releasing.md` + edge pointer in `build-from-source.md`
  CORRECTION 2 applied (REMOVE/REDIRECT, not reword): marketplace.json was deleted outright in #352. Rewrote the L18 "keeps the marketplace entry serving stable from main" line → standalone `spacedock-dev/marketplace` repo, plugin branches carry no manifest; removed `bump-calendar .claude-plugin/marketplace.json` + its commit path from the "Cutting a Stable Release" steps. Added `spacedock-edge` edge-channel user pointer to `build-from-source.md` near the `next` dev-channel mention.
- DONE: WAVE 2.8 — 8p brew note in `install.md` `=== "macOS (Homebrew)"` tab
  Added: brew also installs `agentsview` (powers `/spacedock:survey`); safehouse installed separately, linking `../reference/sandbox.md` (link resolves).
- DONE: AC proofs (external, not prose-greps of my own edits)
  AC-1 grep-clean (command output, exit 1). AC-2 test absent + `go test ./...` 1313 passed (exit 0). AC-3 three headings absent + `spacedock.md/docs` link present (structural README check). AC-4 each anchor carries wording (grep of each changed page) + each of the 5 member commits shows ZERO `.md` via `git show --stat` (`cc5937c6`/`c597c99a`/`e85fe52d`/`fca9d088`/`1579f8a2` all 0 md files).

### Summary
Removed the banned prose-grep test (`install_doc_test.go`) and slimmed README to a front door (dropped Quick start / How it works / Usage; added the `https://spacedock.md/docs/` full-docs link per CORRECTION 1), then landed all five 0.20.1 behaviors on their canonical site pages as the sole doc-owner. Applied all 3 binding corrections from the re-grounded dispatch and resolved both in-flight checks: pi has no unsandboxed permission posture so zrc's sentence is scoped to claude/codex; gp's marketplace.json edits REMOVE/REDIRECT rather than reword since #352 deleted the file. The only non-doc change is the test removal; `go test ./...` is green (1313 passed). Two items surfaced for follow-up (not blocking, not in my corrected scope): (1) README's `[Apache License 2.0](LICENSE)` link points at a non-existent `LICENSE` file — pre-existing, outside this task's enumerated edits; (2) `docs/releasing.md` "Dev-Only next Publishing" still mentions a `next-publish` workflow bumping a "marketplace calendar key" — that calendar mechanism lived in the deleted marketplace.json, so it is now stale, but rewriting it is part of the deferred `ezn` stamp-then-tag ritual, deliberately out of 0.20.1 scope.

## Stage Report: validation

FRESH independent validator (yw, last 0.20.1 member). Verified the worktree deliverable — branch `spacedock-ensign/readme-and-docs-architecture-cleanup` off `origin/main cc5937c6` (gp's #352, all 5 behavior members merged), two yw commits `31a7ae64`+`816be5f7`. Did NOT re-implement. Each documented behavior cross-checked against the MERGED code on `origin/main` (`git show`/built binary), not the plan.

- DONE: AC-1 — install-journey gone + grep-clean.
  `git ls-tree origin/main docs/install-journey.md` empty (absent); `git ls-files` empty (absent in worktree). `git grep install-journey` over live docs/code (excl roadmap/_archive/.spacedock-state/.claude) → exit 1, NOTHING. Zero hits in `*.go`, zero in README; remaining hits are exclusively `docs/roadmap/019x*` historical records (excluded). Command output, not prose.
- DONE: AC-2 — prose-grep test gone + suite green + no dangling symbol.
  `internal/release/install_doc_test.go` absent (`git ls-files` empty). `readInstallJourney` referenced only in roadmap historical docs (gone with the file from live `*.go`); `executableShellCommands` still defined `workflow_exec_guard_test.go:441` with 8 live callers — retained. `go test ./...` from worktree root: EXIT 0, 1313 passed in 16 packages, 0 FAIL lines.
- DONE: AC-3 — README is a thin front door.
  `## Quick start` / `## How it works` / `## Usage` ABSENT (grep exit 1; origin/main had all three, removed). Headings now: Spacedock / What's different / Install / License / Contributing. Full-docs link → `https://spacedock.md/docs/` (the LIVE site; zero `github.io` hits). Install one-liner retained. Whole README re-read: only internal links are LICENSE (pre-existing orphan — no LICENSE file exists anywhere in repo; identical link present on origin/main:144 before this task, NOT introduced here), CONTRIBUTING.md (tracked), install.md (tracked); no orphaned anchor from a removed section, flows clean. NOTE: no build/test section ever existed on origin/main README to "retain" (dispatch phrasing looser than binding AC-3, which does not require it).
- DONE: AC-4 — 5 behaviors on canonical pages, each MATCHES shipped merged code; no member ships `*.md`.
  Anti-collision: each member commit (`1579f8a2`/`fca9d088`/`e85fe52d`/`c597c99a`/`cc5937c6` = 8p#347/zrc#349/gj#350/te#351/gp#352) shows ZERO `.md` via `git show --stat`; combined range `41bd47ae..cc5937c6` ZERO `.md`. All single-parent (squash), so `--stat` is the full delta. Anchors `## --version`/`## Launch`/`## Setup` all EXIST post-change.
  - gj: built the merged binary, ran `spacedock --version` → output ordering (version+`contract 1` / `Sandbox:` / per-runtime block) matches `printVersion` (cli.go:507). `safehouse.State()` returns the exact 3 strings the doc names; `runtimeLine()` returns the exact 4 forms (`not installed`/`installed, spacedock enabled`/`installed, enablement unknown`/bare `installed`). Strings grounded, not invented.
  - te: hint string `"A newer plugin is available — run spacedock install --host %s to refresh."` (contract.go) fires only on `Compatible` + strictly-newer binary; `gateHost` prints `res.Hint` then proceeds (comment: "never blocks"); doctor folds it into Message. Doc's "doctor + front-door, opt-in, never blocks" matches.
  - zrc: frontdoor.go — claude unsandboxed `--permission-mode auto` (l327), sandboxed `--dangerously-skip-permissions` (l317); codex unsandboxed `--ask-for-approval on-request` (l509), sandboxed `--dangerously-bypass-approvals-and-sandbox` (l500); suppressed on resume/operator-mode. `pi.go` injects NO posture (grep exit 1) — doc correctly names ONLY claude/codex, does NOT over-claim pi.
  - gp: `marketplace.json` deleted on origin/main (confirmed). releasing.md: zero `marketplace.json` refs in the stable-release steps; stamp line no longer commits it; "stable from main" line redirected to standalone `spacedock-dev/marketplace` repo. `build-from-source.md` names `spacedock-edge` (matches `.goreleaser.yaml` cask id). yw did NOT touch the deferred `ezn` stamp-then-tag ritual (confirmed). The lone surviving line 96 (`next-publish` "marketplace calendar key") faithfully describes the still-shipped `.github/workflows/next-publish.yml` — that workflow's own breakage (operates on the deleted json) is gp/ezn code-decoupling, deferred out of 0.20.1; not a doc lie.
  - 8p: install.md Homebrew tab — `.goreleaser.yaml:125` `depends_on: - cask: agentsview` (auto-installed); safehouse is a separate caveat (lines 120-122, third-party tap, not a depends_on). Doc's "also pulls in agentsview / safehouse installed separately" matches the shipped cask. `../reference/sandbox.md` link resolves.
- DONE: Recommend PASSED / REJECTED.
  PASSED. All four ACs verified by external evidence (command output, exit codes, built-binary run, merged-code cross-check). The two surfaced follow-ups (LICENSE orphan; line-96 next-publish staleness) are both pre-existing/out-of-scope and correctly flagged by the implementer, not regressions of this task.

### Summary
Validation PASSED. The deliverable is exactly 5 doc files edited + `install_doc_test.go` removed, zero production code (the only `.go` change is the test deletion). All four ACs hold against external evidence: install-journey absent + grep-clean (exit 1); the banned prose-grep test gone with no dangling symbol and `go test ./...` green (exit 0, 1313 passed); README slimmed to a front door with the three sections removed and the live `spacedock.md/docs` link present; and all five behaviors documented on canonical pages with each doc string verified against the MERGED `origin/main` code (gj's `--version` example confirmed by building and running the binary, te's hint string and front-door/doctor wiring, zrc's claude/codex flags with pi correctly excluded, gp's marketplace decouple-accuracy, 8p's agentsview depends_on) — and every member commit ships ZERO `*.md`. Two non-blocking follow-ups noted: the pre-existing `[Apache License 2.0](LICENSE)` orphan (no LICENSE file in repo; unchanged from origin/main) and the stale `docs/releasing.md:96` next-publish calendar-key line (deferred to `ezn`; faithfully describes a still-shipped-but-broken workflow). Neither is a regression introduced by this task; both are correctly outside the corrected scope.

## Stage Report: implementation (cycle 2 — rebase + --version rewrite for #354)

Pre-merge fold requested by the FO. Two intervening PRs landed on `main` after the cycle-1 deliverable: #353 (marketplace bridge — adds the `LICENSE` file) and #354 (da — `--version` drops the enablement jargon for a version-forward per-runtime block). Rebased and rewrote the `## --version` doc to match the new shipped output. Branch now `24fd7f3b`+`18f5c913`+`26419af0` atop `origin/main 8e883d47`.

- DONE: REBASE onto current origin/main (`8e883d47`).
  `git fetch origin main` + `git rebase origin/main` → CLEAN (no conflicts; my doc files do not overlap #353/#354's code/test changes). The two prior commits replayed; the `--version` rewrite is the third.
- DONE: REWRITE `## --version` in `command-reference.md` to da's version-forward output (#354).
  Read `runtimeLine` (`internal/cli/host_runtime.go:55`) post-#354 and built+ran the binary. New per-runtime states documented in plain words: `<host>: spacedock <version>` (installed), ` (disabled)` appended only when the host reports disabled, `pi: spacedock ready` (skills, no versioned plugin), `<host>: spacedock not installed` (host present, no plugin), `<host>: not installed` (binary absent). Example first line uses `0.20.1` (the cut version). DROPPED every `enablement` / `installed, spacedock enabled` / `enablement unknown` phrasing (grep exit 1). This supersedes the earlier 0.20.0→0.20.1 first-line Polish (folded into the whole-section rewrite).
- DONE: Confirm README LICENSE link resolves post-#353.
  `git ls-tree HEAD LICENSE` finds the blob — #353 added it. The cycle-1 orphan finding is now moot; link resolves.
- DONE: docs-only fold, whole-repo green.
  `git diff --name-only origin/main...HEAD` = the 5 `*.md` + `install_doc_test.go` removal, ZERO production code. `go test ./...` from the worktree root: EXIT 0, 1318 passed in 16 packages (up from 1313 — #354 added tests).

### Summary (cycle 2)
Rebased the deliverable cleanly onto `origin/main 8e883d47` (now carrying #353 + #354) and rewrote the `## --version` section to match da's version-forward per-runtime output, dropping the obsolete enablement jargon — wording read from the merged `runtimeLine` and the example exercised by building and running `spacedock --version`. The earlier two cycle-1 follow-ups both close: the README `LICENSE` link now resolves (#353 added the file), and the `--version` doc no longer describes the removed enablement states. The fold stays docs-only (5 `*.md` + the test removal, zero production code) and `go test ./...` is green (1318 passed). Ready for the FO's re-verification of the `## --version` section against the live binary + merge.
