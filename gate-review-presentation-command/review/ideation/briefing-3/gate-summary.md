# Gate review: Gate presentation as an overridable channel (xb) — ideation, attempt 3

**What changed, per your question:** the binary command is gone. Presentation is now an overridable channel of the present-gate skill — DEFAULT is chat (what the skill already does), OVERRIDE is the hardened float script carrying the retention contract (caller-owned result path, room-resident from the first byte, probe ritual). The spacedock binary stays subspace-free, confirmed at baseline and made checkable by an acceptance criterion; the cross-repo coupling dissolves to the override script alone.

**Duties split honestly:** result validation and provider id-normalization move recorder-side (they guard what gets RECORDED, regardless of channel) — proposed to the recorder's spec via the change protocol with the owner-tag amendment. Atomic retention lives in the override script under the 12-fixture drive suite (blank-float EOF, leave-open, the destroyed approval, absent binary — all exercised).

**The one load-bearing condition, surfaced not hidden:** the override script must carry a committed, CI-run drive suite — otherwise it repeats the untested-launcher defect that destroyed results three times. Its home is the subspace repo, where the script lives; that ask rides the cross-repo channel, not this workflow.

**Kept:** the no-subspace fallback (now simply the default channel), the probe ritual, every red fixture.

**Surface:** ~zero Go — skill prose + one script + the recorder-verb ask; tolerance 2×.

**Recommend approve.**

**Decision:** approve = a pending advance to implementation. Revise = annotate. Hold = discuss.

---
*Companion artifacts: the full design (entity snapshot), the recorder contract.*
