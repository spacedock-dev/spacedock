---
title: "State/merge error-path remediation moves from resident contract prose into the binary's own failure output"
status: validation
group: tooling
source: "fable-token-trim-scout analysis 2026-07-02: the five state «fn» bodies (first-officer-shared-core.md:106-144, ~1,015 tok boot-resident) and much of fo-merge-core.md (~1,824 tok at terminal) re-describe shipped commands plus failure remedies the binary could emit at fire time — e.g. state commit exit 3 already means 'same-entity rebase conflict aborted' and its stderr can carry the halt/surface/never-force instructions; merge guard phases can name the FO's next step in their output. Est. ~600-700 off boot, ~700-900 off terminal. Fresh angle: instruction-space -> tool-output placement, not prose relocation."
id: s058nyrecqwtegn36rx6yew1
started: 2026-07-02T03:02:55Z
worktree: .worktrees/spacedock-ensign-error-path-guidance-in-binary-output
mod-block: merge:pr-merge
pr: "#465"
---

## Problem
Contract prose pays resident tokens to restate what shipped commands do and how to recover when they fail, even though that guidance is only needed at the moment the command actually fails or signals — when the binary is already talking to the FO through stdout/stderr. The five state «fn» bodies (`first-officer-shared-core.md:106-144`, 4,059 B ≈ 1,015 tok, boot-resident every session) and most of `fo-merge-core.md` (7,299 B ≈ 1,824 tok, loaded at the terminal boundary) re-describe what `state ready/commit/sweep` and `merge guard` already do and how to recover when they fail — remediation the binary can emit at fire time instead.

## Proposed approach

Two coupled halves, shipped together so no guidance is ever absent from both places:

1. **Binary output gains the FO's next action** at each failure/phase point (deltas D1–D5 below).
2. **Resident prose shrinks to compact bindings plus the pre-invocation prohibitions** that structurally cannot fire post-invocation (a warning printed after the command runs cannot stop the FO from running the wrong command first).

### Binary output deltas

**D1 — `state ready`/`state commit` output carries the halt remediation and the resume sequencing.** `haltOnConflict` (`internal/cli/state_sync.go:201`) today prints the HALT line, the conflicting paths, and the aborted/nothing-force-pushed line. It gains (a) the peer commit — one network-free `git rev-parse --short origin/{branch}` (the pull's fetch phase already updated the ref; spiked, see Spike record) — in stderr and as a `peer_commit` JSON field; and (b) the FO next-action lines the prose currently owns. Additionally (c): on the absent-checkout resume path, `state ready` appends `checkout resumed — re-run \`spacedock status --boot\` before the greet.` — carrying the re-boot-after-resume sequencing the current «state.ensure-ready» body ends with ("re-invoke `«state.boot»()` and proceed only after `entity_dir_present == true`"), which would otherwise be guidance silently lost.

    spacedock state commit: HALT — same-entity rebase conflict on {branch}.
    Conflicting path(s): {paths}
    Peer commit: {sha} (origin/{branch})
    The rebase was aborted (checkout left clean) and nothing was force-pushed; a peer's edit is preserved on origin.
    Next: HALT dispatch — do not dispatch against this state tree. Surface the conflicting path(s) and peer commit to the operator and stop.
    Never `git push --force`/`--force-with-lease`; never re-run with `-X ours`/`-X theirs`; never discard either side.

**D2 — `state sweep` distinguishes gh-unavailable from truly-empty.** Today `classC` swallows every gh error (`internal/dispatch/reconcile.go:638`), so a missing `gh` renders `0 entity(ies) merged but not yet terminalized.` — indistinguishable from a real empty sweep; the «state.sweep-merged» block clause is the only thing telling the FO to treat that as UNKNOWN. `Sweep` wraps the injected GhRunner to count probe errors (classC itself stays best-effort for the idle hook): when ≥1 PR-pending entity exists and every probe errored, the reason becomes `merge state UNKNOWN — gh unavailable; sweep skipped, not empty.` (JSON: `"gh": "unavailable"`). A non-empty sweep appends a next-step line that points at the mod, not a procedure: `next: advance each per the workflow's startup-hook advancement (_mods/{mod}.md)`, naming the registered startup-hook mod(s) from the same mods scan the boot MODS-REPORT uses (generic `_mods/` pointer when none). The binary must NOT prescribe sentinel+`merge guard` here: the shipped template `mods/pr-merge.md:15-17` advances directly (mod-block clear → terminalize → archive, two `--set` calls), while this repo's local `docs/dev/_mods/pr-merge.md` delegates to sentinel+`merge guard` — the mod file is the per-workflow authority on the advancement procedure, and hardcoding either variant in the binary would contradict the other. (Chosen over updating `mods/pr-merge.md` in the same PR, which would drag a shipped-template lifecycle change into this task's blast radius.)

**D3 — `merge guard` phase lines name the FO's next step.** The default prose signals (`internal/status/merge.go:449-493`) become:

- armed: `armed: mod-block set to merge:{hook} — invoke the {hook} merge hook ({workflow_dir}/_mods/{hook}.md; merge guard never invokes it), then re-run merge guard {slug}.`
- blocked: `blocked: PR {pr} is pending — mod-block left intact, never finalize on an open PR. When gh reports it MERGED, record the sentinel (pr=pr-merge:{number}) and re-run merge guard {slug}.`
- finalized: `finalized: {slug} -> {terminal} (verdict {verdict}), archived.` plus next-step clauses built from the pre-terminalize frontmatter: with a `worktree:` recorded — `Next: push; remove the worktree (git worktree remove {worktree}, no --force — if it refuses on untracked files, audit them with the operator before any --force); delete the local branch (git branch -d); keep the remote branch while a PR references it; tear down the entity's workers per your runtime adapter.` With no merge hook registered and no sentinel (the default-local-merge path) it also names: `no merge hook registered — merge the stage branch onto {trunk} with --no-ff if not already merged.` (trunk via the same resolution `dispatch trunk` uses; branch name best-effort via `git -C {worktree} rev-parse --abbrev-ref HEAD`, omitted on failure). The `--json`/`--quiet` envelopes keep their stable fields; guidance rides the default prose the FO reads.

**D4 — `--set` guard refusals warn instead of inviting `--force` (tail-only reword).** The mod-block and merge-hook refusal tails (`internal/status/handlers.go:167,208`) currently end "or use `--force`" — the binary's own text undermines the prose prohibition. Reword the tails to: `(--force bypasses this guard; a refusal usually means a ceremony step was skipped — re-run merge guard {slug} instead.)` The reword MUST stay tail-only: the merge-hook-guardrail live scenario pins the "cannot advance to terminal" HEAD of the refusal message, so the head is a live-asserted contract surface and does not change.

**D5 — a mid-flight mod-block naming a missing merge mod refuses, in the no-sentinel case only.** Code-read finding: with `mod-block=merge:X` where `_mods/X.md` no longer exists (`hookRegistered=false`), pr empty, and verdict not `rejected`, `merge guard` falls into the default case (`merge.go:150-157`) and silently finalizes — clearing the block without the hook ever running — where the prose demands captain escalation (`fo-merge-core.md:45`). New classifier case, scoped to exactly that silent-finalize state: when `mod-block` names a merge mod not among the registered hooks, the verdict is not `rejected`, AND no merge sentinel is recorded, exit 1 mutating nothing, with: `blocking mod {mod} is missing from {workflow_dir}/_mods/ — the entity is stuck. Restore the mod file, or have the operator clear the block with --force.` When `pr` carries a valid sentinel (`pr-merge:{n}` / `local-merge:{sha}`), finalize proceeds as today — the sentinel honestly records a landed merge, so a finalize with the mod file since deleted is legitimate and stays one. `verdict=rejected` also finalizes as today. This converts the escalation prose into a code gate (per shared-core Working Principles: prefer a code gate over a prose-only rule).

### Guidance-coverage map

For each prose passage proposed for removal or shrink: the binary output that replaces it, and what stays resident. Byte figures are the current spans.

| Passage (current bytes) | Replaced by | Stays resident |
|---|---|---|
| `«state.boot»` body, shared-core:108-112 (309 B) | nothing — kept as-is (already a minimal binding; no remediation prose to move) | whole body |
| `«state.ensure-ready»` body, shared-core:114-121 (1,164 B) | `spacedock state ready` already implements the halt-gate (absent checkout → auto-resume via the `state init` path, whose fetch-failure branch prints the manual `git fetch`+`worktree add` fallback on stderr, `state.go:65-67`) and the pull-on-boot; conflict → exit 3 + D1 stderr; the closing re-boot sequencing ("re-invoke `«state.boot»()`… proceed only after `entity_dir_present == true`") → D1(c)'s resume line `checkout resumed — re-run \`spacedock status --boot\` before the greet.` | compact body: guard (split-root only), `→ spacedock state ready` binding, one-pull-at-boot-not-per-read clause, `block: exit 3 → «halt.rebase-conflict»` |
| `«state.sweep-merged»` body, shared-core:123-129 (906 B) | D2 sweep output: gh-absent prints UNKNOWN explicitly; non-empty sweep points at the registered mod's own startup-hook advancement (`_mods/{mod}.md`) — the mod file, not the binary, owns the procedure (see D2's shipped-template-vs-local reconciliation) | compact body: guard, `→ spacedock state sweep` binding, boot-before-greet timing clause ("a greet-and-stop boot never enters the event loop, so a merged PR is advanced here or not at all" — sequencing the output cannot carry) |
| `«state.commit»` body, shared-core:131-136 (601 B) | command's own outcome lines (no-op / local-only / pushed) + D1 exit-3 stderr | compact body: `→ spacedock state commit <slug>` binding + `block: exit 3 → «halt.rebase-conflict»`; the line-106 routing rule "Every state write is one call: «state.commit»(slug)" stays verbatim (pre-invocation — it is what keeps the FO off raw git) |
| `«halt.rebase-conflict»` body, shared-core:138-144 (854 B) | for the binary-owned cases (state ready/commit exit 3) the D1 stderr carries abort-done/surface/never-force; prose shrinks to the FO-held-conflict case (a manual `pull --rebase` on a code worktree, `claude-fo-dispatch.md:162`, where the FO must run `git rebase --abort` itself) | the «fn» definition heading + the FO-held case + the contractlint-pinned anchor "do not force-push or auto-resolve" (verbatim, `prose_function_backstop_test.go:34`); by-name refs at shared-core:67,120,135 kept (fn-consolidation requires ≥3) |
| fo-merge-core `## Merge and Cleanup` prose, :5-12 (1,437 B) | phase mechanics re-description → D3 signal lines | launcher invariant paragraph (~590 B — a stale `$PATH` binary lacks `merge guard`; nothing at fire time can warn, the stale binary does not know); "«merge.guard» never invokes the hook and never local-merges" one-liner |
| `«merge.guard»` effect block + FO-owned steps 1-4, :13-26 (3,670 B) | classifier internals → the binary owns them (fail-closed `prIndicatesMerged` is code); step 1 (invoke hook, record sentinel) → armed/blocked lines; step 2 (default local merge) → finalized no-hook clause; step 3 (worktree/branch removal, remote-branch retention) → finalized next-step clauses; step 4 pointer → finalized "tear down workers per your runtime adapter" | compact «fn»: one-liner binding + invoke-once-per-phase + "--force is never part of the happy path — if the guard refuses, a step was skipped" prohibition; step 4's cohort-derivation stays with the runtime adapter (unchanged) |
| `### Worktree removal safety`, :27-38 (627 B) | the refusal itself is git's own dirty-worktree refusal (the code backstop); the audit next step rides the finalized line's no---force clause | one resident line: "`--force` is never default; audit untracked files with the operator first" |
| `## Mod-Block Guard`, :39-46 (1,297 B) | "guard refuses, does not auto-fix" mechanics → the existing runSet refusals with D4 reworded stderr; missing-mod escalation → D5 refusal | one resident resume line: "a non-empty `mod-block=merge:{mod}` on boot means a merge is mid-flight — check what the hook left and re-run `merge guard`" |

Nothing else in either file is touched. **Heading anchors survive the compaction:** contractlint `TestHostNeutralCoresResolveAndCarryCeremony` (`boot_resident_closure_test.go:34-37`) pins FOUR fo-merge-core heading anchors — `## Merge and Cleanup`, `## «merge.guard»`, `### Worktree removal safety`, `## Mod-Block Guard` — so every compacted section keeps its heading; only the bodies shrink. The compacted prose also keeps the phrase "`--no-ff` merge" (never the full `git merge --no-ff` command): `TestNoUnexpectedModHookOrPRMergeIntroduced` (`structural_checks_test.go:265`) bans that command string in shipped instruction markdown outside `mods/pr-merge.md`.

### Prose that must stay resident (pre-invocation prohibitions), with backstops

The riskiest mechanism in this design: post-invocation output cannot carry a pre-invocation prohibition — by the time the binary prints, the FO already chose which command to run. Per dropped passage, the binary-refusal backstop that holds the line anyway:

- **Never-force on state conflict:** the verbs themselves never emit a force-push (`state_commit_test.go:114` pins that a plain push stays rejected after HALT). Residual risk is the FO bypassing the verb with raw git — held by the resident routing rule (`«state.commit»` is the one write path) plus the resident "do not force-push or auto-resolve" anchor, and re-stated at fire time by D1.
- **Do-not-dispatch-on-unmerged-tree:** exit 3 is itself the enforcement (a caller gating on exit 0 cannot proceed); D1 stderr says HALT dispatch; the compact «halt.rebase-conflict» block clause stays resident.
- **Never-finalize-on-open-PR:** the blocked classifier + fail-closed sentinel validation (`prIndicatesMerged`, merge.go:170) is code, tested; the blocked line restates it.
- **Hook-skip terminalize:** the runSet mod-block/merge-hook guards refuse it (code, shipped); D4 makes their stderr carry the remediation instead of inviting `--force`.
- **`--force`-to-clear temptation:** cannot be a hard refusal (`--force` is the operator's legitimate escape hatch) — this is the one prohibition that genuinely cannot move; it stays resident as the compact "never part of the happy path" clause, with D4's reworded refusal as the fire-time echo.
- **Worktree `remove --force`:** git's own dirty-worktree refusal is the code backstop; the resident one-liner + the finalized-line audit clause carry the spacedock-specific ritual.
- **Missing blocking mod:** D5 turns the prose escalation into a refusal (code gate replaces prose outright).
- **Stale-binary fallback (launcher invariant):** no fire-time backstop is possible — a stale binary cannot warn about a subcommand it does not have. Stays resident verbatim.

## Acceptance criteria

- **AC-1 (VALUE — measured token delta).** Against the origin/main baselines (`git show origin/main:<file> | wc -c`): `skills/first-officer/references/first-officer-shared-core.md` shrinks by ≥2,000 bytes (≈500 tok at bytes/4; baseline 23,216 B) and `skills/first-officer/references/fo-merge-core.md` by ≥2,800 bytes (≈700 tok; baseline 7,299 B). Both deltas NEGATIVE; the guidance-coverage map above names the replacing output for every dropped passage, audited at validation so no guidance is silently lost. Tested by: `wc -c` comparison recorded in the implementation/validation stage reports.
- **AC-2.** `state commit` and `state ready` exit-3 stderr carries the remediation block: HALT-dispatch next-action line, the never-force/never-auto-resolve line, conflicting path(s), and the peer commit; `--json` carries `peer_commit`; and `state ready`'s absent-checkout resume path prints the `checkout resumed — re-run \`spacedock status --boot\` before the greet.` line. Tested by: extending the existing real-git conflict e2e in `internal/cli/state_commit_test.go` / `state_ready_test.go` (which already covers the resume path) to pin the new stderr lines and JSON field.
- **AC-3.** `merge guard` default prose for armed/blocked/finalized each names the FO's next step per D3 (armed: mod path + re-run; blocked: sentinel recording + never-finalize-open-PR; finalized: worktree removal with no---force audit clause, branch cleanup, remote-branch retention, worker teardown; no-hook path: `--no-ff` merge onto trunk). Tested by: `internal/status/merge_guard_test.go` cases pinning each phase's line content, including the finalized line with and without a recorded `worktree:`.
- **AC-4.** With ≥1 PR-pending entity and `gh` probes all failing, `state sweep` output (prose + JSON) declares merge state UNKNOWN/gh unavailable rather than `0 entity(ies)`; a non-empty sweep names the mod-pointer next step (D2). Tested by: injected GhRunner stub in `internal/dispatch/sweep_test.go` — the injection seam lives at `dispatch.Sweep`; the cli-level `run()` hardcodes `GhRunnerExec`, so `internal/cli/state_sweep_test.go` cannot inject.
- **AC-5.** `merge guard` on `mod-block=merge:X` with `X` unregistered: (a) verdict `passed` + no sentinel (pr empty or bare ref) → exits 1 with the missing-mod escalation text and mutates nothing; (b) verdict `passed` + `pr=pr-merge:{n}` (or `local-merge:{sha}`) → finalizes as today (a recorded sentinel is a legitimate finalize even with the mod file deleted); (c) verdict `rejected` → finalizes as today. Tested by: new `merge_guard_test.go` cases covering all three rows — TDD, the failing test first pins today's silent finalize in row (a).
- **AC-6.** The mod-block and merge-hook `--set` refusal stderr ends with the D4 warning wording, not a bare "or use `--force`". Tested by: the existing guard-refusal unit tests updated to pin the new tails.
- **AC-7.** Pre-invocation prohibitions stay resident and contractlint stays green: `prose_function_backstop_test.go` ("do not force-push or auto-resolve" anchor), `fn_consolidation_structure_test.go` (exactly 1 `«halt.rebase-conflict»` definition, ≥3 by-name refs, `git rebase --abort` exactly once — the trimmed bodies keep their by-name block clauses), `boot_resident_closure_test.go` (the deferred load point resolves AND `TestHostNeutralCoresResolveAndCarryCeremony` pins the four fo-merge-core heading anchors named in the coverage map — all four headings survive). Tested by: `go test ./internal/contractlint/`.
- **AC-8.** Live behavior preserved: the claude-live lane green on the PR (see Test plan — the diff touches `skills/**/references/**`, so the lane is REQUIRED, not optional).

## Test plan

- **Go unit/e2e (cheap, existing harnesses):** AC-2 via the real-git two-writer conflict repos already built in `state_commit_test.go`/`state_ready_test.go`; AC-3/AC-5 via the merge-guard fixture workflows in `internal/status/merge_guard_test.go`; AC-4 via the injected `GhRunner` seam at `internal/dispatch/sweep_test.go`; AC-6 via the runSet guard tests. No golden fixtures pin any of this output today (checked `fixtures/`), so no fixture churn.
- **contractlint:** full `./internal/contractlint/` suite; the trims are designed to keep the pinned counts satisfied without test edits (the abort verb stays exactly once in the «halt.rebase-conflict» body — the FO-held code-worktree case still needs it; the by-name refs at shared-core:67,120,135 survive the compaction). If a count legitimately changes, the test updates ship in the same PR with the rationale in the commit.
- **Live merge gate (recorded per the ideation checklist):** the diff touches `skills/first-officer/references/**`, which the claude-live lane loads — per `docs/dev/README.md:77` the path→lane mapping makes `claude-live` REQUIRED green before merge; a flake is re-run to green, never skipped. Scope honesty: the `merge-hook-guardrail` live scenario drives only a `--set` refusal — it never exercises `merge guard` phases — so behavior preservation for the ceremony trims rides on the Go tests plus a boot smoke confirming the greet still composes from the compacted «fn» bodies. The constraint that scenario DOES impose: its live assertion pins the "cannot advance to terminal" HEAD of the refusal message, which is why D4's reword is tail-only.
- **Estimated cost:** implementation is localized (one stderr block, three signal lines, one sweep wrapper, one classifier case, two refusal tails, two prose files); Go tests run in the offline lane; one claude-live approval cycle.

## Spike record

- **Peer-commit capture (riskiest unverified mechanism, exercised first):** PASSED 2026-07-02. Two-clone/shared-bare-origin repro in scratchpad: after writer B's `pull --rebase` conflicts and after `git rebase --abort`, `git rev-parse --short origin/<branch>` still resolves to writer A's pushed commit (verified equal to A's HEAD SHA). The pull's fetch phase updates the remote ref before the rebase fails, and abort does not touch it — so D1's peer-commit line costs one network-free git call.
- **Silent-finalize gap (D5):** established by code read (`merge.go:150-157` default case reached when `mod-block=merge:X`, X unregistered, pr empty, verdict passed); the implementation's first failing test reproduces it before the fix, per TDD.
- **No spike needed for:** multi-line stderr from these verbs (shipped `haltOnConflict` already emits three pinned lines), frontmatter `worktree:` availability inside `MergeGuard` pre-terminalize (`ParseFrontmatter` already called at merge.go:107), the injected GhRunner test seam (exported for exactly this purpose, reconcile.go:79-85).

## Docs impact

Checked the docs site: `docs/site/reference/command-reference.md` covers `spacedock state` as a one-line table row and documents no output text; `docs/site/advanced/split-root-state.md:9` says same-entity conflicts "halt for your call rather than auto-resolving" — which remains true and is strengthened by D1. No doc file describes the changed failure/phase output lines, so no doc diff is required; this determination is the ideation-stage record.

## Stage Report: ideation

- DONE: The guidance-coverage map is explicit: for each prose passage proposed for removal (the five state «fn» bodies, fo-merge-core sections), name the binary output (exit code + stderr/stdout text) that replaces it, and mark what must STAY resident (pre-invocation prohibitions like never-force) — no guidance silently lost.
  `### Guidance-coverage map` table: 9 passages with current byte spans, each mapped to a D1-D5 output (exit code + concrete text) and a stays-resident column; «state.boot» explicitly kept as-is.
- DONE: ACs include a VALUE measure (boot-resident and terminal token deltas against the current byte counts) plus behavioral tests pinning the new failure/phase output (exit-3 stderr remediation text, merge guard armed/blocked/finalized next-step lines).
  AC-1 measures ≥2,000 B off shared-core (baseline 23,216 B) and ≥2,800 B off fo-merge-core (baseline 7,299 B) vs `git show origin/main`; AC-2/AC-3 pin exit-3 stderr and the three phase lines via existing real-git and merge-guard test harnesses.
- DONE: The riskiest mechanism — post-invocation guidance cannot carry pre-invocation prohibitions — is addressed in the design with the binary-refusal backstop named per dropped passage, and the claude-live merge-gate consequence of touching skills/** is recorded in the test plan.
  `### Prose that must stay resident` names the backstop for all 8 prohibition classes (incl. the two that cannot move: --force-to-clear, stale-binary launcher invariant); test plan cites docs/dev/README.md:77 making claude-live REQUIRED.

### Summary

Fleshed out the instruction-space→tool-output design: five binary output deltas (D1 exit-3 halt remediation + peer commit, D2 sweep gh-absent UNKNOWN, D3 merge-guard phase next-steps, D4 refusal rewording, D5 missing-mod refusal) paired with a per-passage coverage map trimming the five state «fn» bodies and fo-merge-core. Spiked the riskiest mechanism first (peer-commit ref survives rebase abort — PASSED) and found a real bug en route: a mod-block naming a deleted merge mod silently finalizes today (D5 converts the prose escalation into a code gate). Docs impact checked: none required.

## Stage Report: ideation (cycle 2)

- DONE: M1 — reconcile D2's sweep next-step with the shipped pr-merge mod
  D2 now emits a mod-pointer (`_mods/{mod}.md`), never a procedure; rationale recorded: shipped `mods/pr-merge.md:15-17` advances directly while local `docs/dev/_mods/pr-merge.md` uses sentinel+merge-guard — the mod file owns the procedure. Chose pointer over shipped-template edit to keep the blast radius fenced.
- DONE: M2 — cover the dropped re-boot-after-resume sequencing
  D1(c): `state ready`'s resume path prints `checkout resumed — re-run spacedock status --boot before the greet.`; coverage-map ensure-ready row and AC-2 updated to pin it.
- DONE: M3 — record the four fo-merge-core heading anchors
  Coverage map notes TestHostNeutralCoresResolveAndCarryCeremony (boot_resident_closure_test.go:34-37) pins all four headings, which survive; AC-7's description corrected.
- DONE: M4 — narrow D5 to the no-sentinel silent-finalize case
  D5 refuses only when no merge sentinel is recorded; sentinel+missing-mod finalizes as today. AC-5 is now a three-row matrix (no-sentinel refusal / sentinel finalize / rejected finalize).
- DONE: M5 — downgrade the merge-hook-guardrail live claim and record its real constraint
  Test plan states the scenario drives only a --set refusal (Go tests + boot smoke are the ceremony oracle) and that its live assertion pins the refusal-message HEAD — recorded in D4 as the tail-only constraint.
- DONE: polish — AC-4 harness corrected to internal/dispatch/sweep_test.go (cli run() hardcodes GhRunnerExec); "--no-ff merge" phrasing constraint recorded (structural_checks_test.go:265); state.go:65-67 fallback softened to the fetch-failure branch.

### Summary

Applied the staff review's five material findings as task-body reconciliations — no architectural change. The one directional choice (M1) picks the workflow-agnostic mod-pointer output over editing the shipped pr-merge template, keeping this task's blast radius at the two contract files plus the named Go surfaces.

## Stage Report: implementation

- DONE: D1 — state commit/ready HALT stderr carries the peer commit + FO remediation; ready's absent-checkout resume prints the re-boot line
  `internal/cli/state_sync.go` `haltOnConflict`/`peerCommitSHA`/`runStateReady`; TDD red confirmed then green: `TestStateCommitHaltStderrCarriesRemediationAndPeerCommit`, `TestStateCommitHaltJSONCarriesPeerCommit`, `TestStateReadyHaltStderrCarriesRemediationAndPeerCommit`, `TestStateReadyResumesAbsentCheckout` (resume-line assertion added).
- DONE: D2 — state sweep distinguishes gh-unavailable UNKNOWN from a truly empty sweep; non-empty sweep names the startup-hook mod-pointer next step
  `internal/dispatch/reconcile.go` `Sweep`/`sweepNextStep`, `internal/status/mutate.go` `ScanMods` export; `internal/dispatch/sweep_test.go` `TestSweepGhUnavailableReportsUnknown`, `TestSweepGhPartiallyAvailableStillReportsNormally`, `TestSweepNonEmptyNamesRegisteredStartupModNextStep`, `TestSweepNonEmptyNamesGenericModPointerWhenNoneRegistered`.
- DONE: D3 — merge guard armed/blocked/finalized default prose names the FO's next step (hook path, sentinel format, worktree/branch/worker cleanup, no-hook manual `--no-ff` merge)
  `internal/status/merge.go` `signalArmed`/`signalBlocked`/`signalFinalized`/`resolveMergeTrunk`/`abbrevRefHead`; new fixture `merge-no-hook-workflow` + `merge-pr-workflow/150-worktree-finalize.md`; 6 new tests in `merge_guard_test.go` pinning exact phase-line content, including finalized with/without `worktree:` and with/without a registered hook.
- DONE: D4 — mod-block/merge-hook `--set` refusal tails reworded, tail-only (HEAD "cannot advance to terminal" unchanged, live-asserted)
  `internal/status/handlers.go:167,208`; golden fixtures regenerated (`-update`); `TestMergePrDefaultNoSentinelStillRefuses` pins the new tail and the absence of "or use --force to bypass".
- DONE: D5 — merge guard refuses (exit 1, mutates nothing) a mod-block naming a merge mod missing from `_mods/`, in the no-sentinel case only; TDD failing test landed first, pinning today's silent finalize before the fix
  `internal/status/merge.go` `modBlockNamesMissingMergeMod`/`refuseMissingMergeMod`, new switch case ahead of both the blocked and default cases; fixtures `120/130/140-*.md` in `merge-pr-workflow`; `TestMergeGuardRefusesMissingMergeModNoSentinel` (row a), `TestMergeGuardFinalizesMissingMergeModWithSentinel` (row b), `TestMergeGuardFinalizesMissingMergeModRejected` (row c).
- DONE: Two contract files shrink per the coverage map with every guard intact
  `first-officer-shared-core.md` 23216 B → 21215 B (delta 2001 B, ≥2000 required); `fo-merge-core.md` 7299 B → 3226 B (delta 4073 B, ≥2800 required), both measured via `git show origin/main:<file> | wc -c` vs current. `go test ./internal/contractlint/...` green: all 4 fo-merge-core heading anchors survive, `«halt.rebase-conflict»` has exactly 1 definition + 3 by-name refs, `git rebase --abort` appears exactly once, `do not force-push or auto-resolve` survives verbatim, `--no-ff merge` phrase present (never the full `git merge --no-ff` command).
- DONE: Full suites green
  `go build ./...`, `go vet ./...`, `gofmt -l` clean on every file I touched (one pre-existing unrelated gofmt finding in `internal/cli/pi_frontdoor_test.go`, untouched by this task), `go test ./...` all packages green including `internal/cli`, `internal/dispatch`, `internal/status`, `internal/contractlint`.
- SKIPPED: AC-8 claude-live lane
  Requires a live PR/CI run, out of reach for the implementation stage in this environment; the diff touches `skills/first-officer/references/**` so the lane is REQUIRED at the PR boundary per `docs/dev/README.md:77` — flagged for validation/merge to drive.

### Summary

Shipped all five binary output deltas (D1 state-sync HALT remediation + peer commit, D2 sweep gh-unavailable UNKNOWN + mod-pointer, D3 merge-guard phase next-steps, D4 tail-only refusal reword, D5 missing-mod refusal) with TDD red-then-green for each, then shrank the two contract prose files per the guidance-coverage map — shared-core by 2001 B and fo-merge-core by 4073 B, both clearing their AC-1 minimums with contractlint's pinned anchors (heading survival, by-name ref counts, the verbatim `do not force-push or auto-resolve` clause, the `--no-ff merge` phrasing constraint) all green. D3's phase-line work needed a new no-merge-hook fixture and a worktree-recorded fixture to pin the four independent output shapes (with/without worktree × with/without a registered hook); D5's fix is scoped to fire before any `--set` mutation runs, so the refusal case is provably zero-mutation. The full `go test ./...` suite is green; the one open item is the claude-live PR gate (AC-8), which this stage cannot drive locally and is flagged for validation.

## Stage Report: validation

- DONE: D1-D5 reproduced, not trusted: run the named harnesses yourself (state_commit/state_ready real-git conflict e2e, merge_guard phase-line cases, the three-row D5 matrix, sweep GhRunner stub, refusal-tail tests) AND drive the built binary — force a real two-writer conflict and read the exit-3 stderr, run merge guard through armed/blocked/finalized on a fixture, reproduce D5's refusal and its sentinel carve-out; confirm the D5 test genuinely fails on pre-fix code (revert the classifier case locally, watch it red, revert back)
  All named harnesses re-run green (6 state e2e, 28 merge-guard, 6 sweep). Binary drives: real two-clone/bare-origin conflict → exit 3 with the full D1 stderr block, peer commit = writer A's HEAD, `--json` carries `peer_commit`, checkout clean; `state ready` fresh clone prints the D1(c) resume line; armed/blocked/finalized(+worktree)/finalized-no-hook lines all observed verbatim; D5 rows a/b/c driven (refusal exits 1, entity file byte-identical, not archived; sentinel and rejected rows finalize). Red-check: classifier case neutralized → `TestMergeGuardRefusesMissingMergeModNoSentinel` FAILS, restored → PASS, tree clean. Additionally rebuilt a pre-fix binary and reproduced the true silent finalize (no-hook workflow + ghost mod-block → archived, exit 0) that the fixed binary now refuses. D2 also driven live: gh stripped from PATH + PR-pending entity → `merge state UNKNOWN — gh unavailable; sweep skipped, not empty.`; fake gh MERGED → non-empty sweep with the `_mods/` pointer line. D4 mod-block tail driven live via a `--set` refusal.
- DONE: The coverage map audit: for every dropped prose passage, confirm the named replacing binary output actually ships (no guidance silently lost), all four fo-merge-core heading anchors survive, the resident prohibitions are intact (never-force anchor verbatim, halt-fn 1 definition + exactly 3 refs + abort-once, "--no-ff merge" phrasing), and AC-1's byte/token deltas are re-measured against origin/main and match the recorded floors
  All 9 coverage-map rows checked: each replacing output observed live (above) and each stays-resident clause present in the compacted files (routing rule verbatim at shared-core:106, FO-held conflict case, launcher invariant, Step 10 teardown paragraph — which keeps worker-teardown guidance for the no-worktree finalize where the binary prints no teardown clause). Direct grep: 1 halt-fn definition + 3 by-name refs (shared-core:67,117,126), `git rebase --abort` exactly once, never-force anchor verbatim (shared-core:130), all four fo-merge-core headings at :5,:11,:20,:24, "--no-ff merge" phrase present with zero `git merge --no-ff` occurrences. AC-1 re-measured against current origin/main (1af45578; both files unchanged there since merge-base): shared-core 23216→21215 = −2001 B (floor 2000), fo-merge-core 7299→3226 = −4073 B (floor 2800).
- DONE: Full suites green including contractlint (unmodified or with rationalized edits shipped in the same commit); AC-8 (claude-live) stays pending the merge gate — record it as such, not SKIPPED silently; close with PASSED/REJECTED per AC
  `go build ./...`, `go vet ./...`, `go test ./...` all green; `go test -count=1 ./internal/contractlint/` green with zero contractlint file edits on the branch; gofmt clean on all touched Go files. AC-1 PASSED, AC-2 PASSED, AC-3 PASSED, AC-4 PASSED, AC-5 PASSED, AC-6 PASSED, AC-7 PASSED, AC-8 PENDING (claude-live lane runs at the PR merge gate; diff touches `skills/first-officer/references/**` so the lane is REQUIRED per docs/dev/README.md:77).

### Summary

Recommendation: PASSED (AC-1..AC-7; AC-8 pending the merge gate's claude-live lane). Every delta was reproduced live against the built binary, not just via the harnesses, including a pre-fix-binary comparison proving D5's silent-finalize bug was real and is now a refusal. Two notes for the FO: (1) the branch (0641c829) is not rebased on current origin/main — origin/main advanced with unrelated internal/dispatch+claudeteam+ensigncycle changes, no file overlap with this diff, so a routine rebase/merge is expected clean; (2) minor test-coverage observation, non-blocking: the D5 unit matrix exercises the ghost-mod shape with another hook registered (pre-fix: confusing refusal), while the no-hook-registered shape (pre-fix: the actual silent finalize) is covered by the same classifier and was verified live here but has no dedicated unit test.

### Feedback Cycles

**Cycle 1 (2026-07-02, detached adversarial audit, pre-gate).** Audit `audit-error-path-guidance` on a throwaway clone: no material refutation of the code or shipped guidance — D5 both-directions, D1 line-by-line, D3/D4 exact-string pins all went RED under claim-breaking mutants; D4's refusal heads proved pinned OFFLINE (4 tests each), better than the protocol assumed. Two test-strength items routed to implementation: (T1) the no-hook D5 shape — deleted mod was the workflow's ONLY merge hook, the likeliest real-world trigger and the actual pre-fix silent finalize — has no dedicated unit test; a mutant scoping the classifier with `if len(mergeHooks)==0 { return false }` passes the entire 4-package suite (validator's note, now empirical). (T2) the D5 remediation tail ("Restore the mod file, or …") is unpinned — dropping it stays green. Protocol corrections recorded, no repo action: the audit's own suite list omitted ./internal/dispatch (CI's offline lane runs full ./...), and fn_consolidation pins ≥3 halt-fn refs (a floor), not exactly-3. Noted, not routed: the hardcoded claude-fo-dispatch.md:162 line citation in the new halt prose is fragile under future edits (41 churns that file next).

**Cycle 2 (2026-07-02, PR #465 codex-live red, real regression — not a flake).** The shallow-boot scenario failed: the FO ran `state sweep` in a fixture whose stub `gh` (reporting MERGED) IS on the subprocess PATH (codex_live_runner_test.go:262-265, withPATHPrefix), yet D2's output was `merge state UNKNOWN — gh unavailable; sweep skipped, not empty.` — the probe-error wrapper misclassified a working gh, the sweep skipped the merged-PR advancement, and the FO's manual fallback set status=done but never archived (assertion: entity not archived before greet). Artifact: run 28570801721 codex shallow-boot codex-exec.jsonl. Routed to implementation: reproduce with the fixture's stub gh on PATH, root-cause the misclassification (wrapper error-count vs GhRunnerExec env/exec path), fix, and add the regression test the audit shape implies — sweep with a working stub gh and a PR-pending entity must advance (or at minimum not report UNKNOWN). The second #465 codex failure (rejection-flow) is the known narration-heuristic false positive (task mq), not this diff.

**Cycle 1 implementation amendment (2026-07-02).** Both routed items landed, test-only, on `spacedock-ensign/error-path-guidance-in-binary-output` (commit `c6bfe2a3`, pushed — re-triggers PR #465 CI):

- T1: new `TestMergeGuardRefusesMissingMergeModWhenNoHookRegisteredAtAll` against a new fixture entity (`merge-no-hook-workflow/030-missing-mod-no-hooks-registered.md`, `mod-block: merge:ghost-merge`, zero `_mods/` merge hooks registered — distinct from the existing `120-missing-mod-no-sentinel.md` fixture, whose workflow still registers `local-merge`). RED proof: applied `if len(mergeHooks) == 0 { return false }` to `modBlockNamesMissingMergeMod` — the test failed with the entity silently finalized (`"finalized: 030-... archived."`, exit 0), reproducing exactly the audit's predicted mutant escape. Reverted (`internal/status/merge.go` byte-identical to pre-mutant, confirmed via `git diff`), re-ran GREEN.
- T2: extended `TestMergeGuardRefusesMissingMergeModNoSentinel` to assert the full tail `"Restore the mod file, or have the operator clear the block with --force."`, not just the mod-name + "is missing" fragment. RED proof: dropped the tail from `refuseMissingMergeMod`'s format string — the test failed (`stderr should carry the full remediation tail`). Reverted, re-ran GREEN.
- Full suite re-run after both reverts: `go build ./...`, `go vet ./...`, `go test ./...` all green (including `internal/contractlint`); `gofmt -l` clean on both touched files (test file + new fixture). No production code changed — `internal/status/merge.go` is unmodified from the implementation-stage commit.

### Cycle 1 re-verification (validation)

Re-verified the cycle-1 test-only amendment on the rebased branch (HEAD c6bfe2a3, pulled fresh): the full `go test ./...` suite is green as shipped, and the cycle-1 commit touches only `internal/status/merge_guard_test.go` plus the new fixture `merge-no-hook-workflow/030-missing-mod-no-hooks-registered.md` — no product code. Both audit mutants reproduced RED by me and reverted: T1 (scoping `modBlockNamesMissingMergeMod` with `if len(mergeHooks)==0 { return false }`) fails only the new `TestMergeGuardRefusesMissingMergeModWhenNoHookRegisteredAtAll` — its failure output shows the exact silent finalize ("finalized ... archived") while every other D5 test stays green under the mutant, proving the new pin is the sole catch; T2 (dropping the "Restore the mod file, or have the operator clear the block with --force." tail) fails the extended assertion in `TestMergeGuardRefusesMissingMergeModNoSentinel`. After each revert the status package re-ran green (uncached) with a clean tree. This closes the non-blocking no-hook coverage observation from my original report; recommendation stays PASSED (AC-1..AC-7; AC-8 pending the merge gate).

**Cycle 2 implementation amendment (2026-07-02).** A real regression, caught by PR #465's codex-live shallow-boot scenario (artifact 28570801721): D2's `state sweep` reported `merge state UNKNOWN — gh unavailable` against the shallow-boot fixture's stub `gh` (which reports MERGED and IS on the FO subprocess PATH), so the boot sweep skipped the merged-PR advancement and the FO hand-advanced without archiving.

- **Root cause** (reproduced offline first, per instruction): `ghRunnerExec` (`internal/dispatch/reconcile.go`) called `gh pr view PR --json state` and `json.Unmarshal`'d the result, expecting a `{"state":"MERGED"}` envelope. `boot.go`'s already-proven PR_STATE probe (`internal/status/boot.go:114`) instead calls `gh pr view PR --json state --jq .state`, which extracts the field as bare text via `jq` itself. The shared ensigncycle stub `gh` (`writeStubMergedGh`, `shallow_boot_fixture_live_test.go`) matches boot.go's shape — it unconditionally echoes bare `MERGED` for any `pr view` invocation, ignoring flags — so `ghRunnerExec`'s JSON unmarshal failed (`invalid character 'M' looking for beginning of value`), and D2's new probe-error counting correctly reported that failure as `gh unavailable` UNKNOWN. Pre-D2, this same failure was silently swallowed by classC's best-effort `continue`, so it was invisible (the real advancement flowed through the boot PR_STATE probe + pr-merge hook, not through `state sweep`) — D2 made the pre-existing probe bug visible for the first time, and my new prose told the FO to trust `state sweep`'s output.
  Reproduced offline via the real binary against a hand-built stub matching the shared fixture's exact script before touching any code: `state sweep --json` returned `"reason": "merge state UNKNOWN — gh unavailable..."` against a working stub gh.
- **Fix:** `ghRunnerExec` now calls `gh pr view PR --json state --jq .state` and returns the trimmed, uppercased bare text — the same invocation shape, same leading-`#` trim, and same extraction boot.go already proves works, so the two probes agree on what "gh can answer" means. `internal/dispatch/reconcile.go`.
  Verified fixed via the same offline binary repro: `state sweep --json` now returns the swept entity, not UNKNOWN.
- **Regression tests** (both RED under the pre-fix `ghRunnerExec`, reverted, GREEN restored): `TestGhRunnerExecParsesJqExtractedState` pins the production probe directly against the shared stub's exact script shape; `TestSweepWithRealGhStubDoesNotReportUnknown` drives `Sweep` end-to-end with the real `GhRunnerExec` (not an injected fake) and asserts the merged entity is swept, not UNKNOWN. `internal/dispatch/sweep_test.go`.
  RED proof: manually restored the pre-fix `ghRunnerExec` body, both new tests failed (`invalid character 'M' looking for beginning of value`; `reason="merge state UNKNOWN..."`), reverted to the fix, both green.
- Full suite green: `go build ./...`, `go vet ./...`, `go test ./...` (including `internal/contractlint`), `gofmt -l` clean on both touched files. Pushed as commit `868fd10f` — re-triggers PR #465 CI.

### Cycle 2 re-verification (validation)

Re-verified the cycle-2 fix on the pulled branch (HEAD 868fd10f): full `go test ./...` green as shipped (exit 0, all 15 packages), and the cycle-2 commit touches only `internal/dispatch/reconcile.go` plus `internal/dispatch/sweep_test.go`. Regression reproduced by me end-to-end: with a stub `gh` matching the shared fixture's exact shape (dispatches on `$1 $2`, unconditionally echoes bare `MERGED` — no JSON envelope), the fixed binary's `state sweep --json` detects the merged PR-pending entity (`swept` carries it, no `"gh": "unavailable"`, no UNKNOWN); with `reconcile.go` locally reverted to the pre-fix c6bfe2a3 version, both new regression tests (`TestGhRunnerExecParsesJqExtractedState`, `TestSweepWithRealGhStubDoesNotReportUnknown`) go RED and a pre-fix binary live-drives to the wrong `merge state UNKNOWN — gh unavailable` against that working stub. Reverted back; dispatch package re-green uncached; tree clean; the cycle-1 T1 no-hook pin still passes. Honest validation note: my original D2 live drive faked `gh` with a `{"state":"MERGED"}` JSON envelope — the shape the old parser happened to expect — so it could not catch this divergence; the codex-live scenario's fixture-shape stub did. Recommendation stays PASSED (AC-1..AC-7; AC-8 pending the merge gate).
