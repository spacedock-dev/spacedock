**Recommend: REJECTED.** (Staying solo — one verdict on evidence already in hand; no fan-out, isolation, or independent verification to buy.)

**AC-1 is falsified, not deferred.** The criterion is "strictly lower after than before." The measurement is 123,400 vs 122,231 — up 1,169. The report marks it DONE and then, in the same bullet, prints the number that contradicts it. A measured value AC that measured the wrong direction is unmet; that is the whole point of writing it as a number.

**AC-2 fails the end re-anchor.** AC-2 is mechanism-only — collapse the recipe, delete the duplicate copies — and it exists to serve AC-1's value: the prompt surface shrinks. The mechanism landed cleanly (commit `3f9ac10`, single `## Startup`, clean heading diff) and the value it serves regressed. `«gate.ac-cross-check»` names this case exactly: a mechanism AC whose served value-AC regressed is a REJECT, not a pass. Deduplicated-but-bigger is the leaner-contract-that-grew shape.

**The report's own reasoning is the tell.** "The growth is entirely the collapsed recipe's added cross-references, which every reader needs" is not a mitigation — it is the finding. If every reader must now be pointed at the single copy, the dedupe traded N copies for N pointers and the surface did not shrink. "No supported workflow regresses" answers a criterion nobody wrote; AC-1 measures bytes. Recommending PASSED here is the stage grading its own miss and reclassifying a falsified AC as a deferred risk.

**Bounce back with:** get the ratcheted-13 total strictly under 122,231 — either absorb the cross-reference cost (one pointer at the load point rather than per-adapter cross-refs) or bank the offset elsewhere in the same 13 files — and re-measure with the before/after totals both stated. If the team believes AC-1 is the wrong bar now that the recipe is genuinely single-copy, that is a captain amendment to the AC, requested explicitly — not a reinterpretation inside a stage report.
