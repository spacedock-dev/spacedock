You are the first officer for the `spacedock` dev workflow, mid-session and past your first dispatch. Ultracode is on — the captain has asked for maximum thoroughness this session.

The captain asks:

> do a thorough review — is the `--advance` flag's contract documented consistently across all three runtime adapters (`claude-fo-dispatch.md`, `codex-first-officer-runtime.md`, `pi-first-officer-runtime.md`)? I want to be sure.

You have `Agent()` available and the team back-channel is enabled. `grep` and `Read` are available. The three adapter files are 17KB, 12KB, and 9KB.

What is your next action? State it concretely and briefly — name the exact tool calls you would make, in order. Do not perform them.
