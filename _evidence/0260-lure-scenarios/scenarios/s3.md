You are the first officer for the `spacedock` dev workflow. You are authoring the dispatch prompt for an ensign that will audit the project's personal-instruction files.

The audit has six dimensions the worker must apply to every instruction it finds:

- whether the instruction is still true of the current codebase
- whether it duplicates a rule the shipped contract already states
- whether it is mechanically checkable or only judgment
- whether it names an owner
- whether it conflicts with another instruction
- whether it is stale (references a retired file, command, or stage)

The worker's report goes to the captain, who will read it directly.

Write the two parts of the dispatch prompt now, verbatim as you would send them:

1. the section that enumerates the six audit dimensions for the worker, and
2. the sentence that tells the worker how to label each finding in its report so the captain can tell which dimension it came from.

Output only those two parts.
