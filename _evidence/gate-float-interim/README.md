# Gate-float interim launcher — evidence artifact

`review-package-zellij` is the working multi-artifact briefing-package float launcher used for every durable-decisions gate (probe-proven, room-resident results from the first byte). It is the hardened-script lineage the presentation task (gate-review-presentation-command) designs its override channel from, and the interim baseline its retention contract must beat. Preserved from /tmp on 2026-07-22 because the only working copy was ephemeral. Requirements: SUBSPACE_TUI_BIN = exact-tip local build (source default version "dev" — pass SUBSPACE_TUI_EXPECT_VERSION=dev), live ZELLIJ_SESSION_NAME/ZELLIJ_PANE_ID, jq. Invoke: `review-package-zellij <briefing.json>`; the briefing REQUIRES a context block (schema rule, caught by probe). The result lands beside the briefing as float-result.json and survives launcher death. Not a committed test or shipped tool — evidence and interim tooling only.
