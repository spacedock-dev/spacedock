---
title: Roborev review type for sprint-level reviews with a fable + gpt-5.6-sol panel
status: backlog
source: "0260 shaping close — captain direction, 2026-07-20."
id: 7wv0gfmavp8exe2xc3zgtys7
---

Sprint-level reviews (the preflight design review and the pre-cut antipattern audit) live as checklist prose with no owned mechanism, so they are skippable by rationalization — observed 2026-07-20 when the 0260 preflight was bypassed via a stale first-pass reference and the Commander package was assembled before any reviewer refuted the final state. Define a roborev review type for them, with a two-seat cross-model panel: fable + gpt-5.6-sol, models CROSSED at verification (fable findings verified by sol and vice versa) so same-model blind spots cannot self-confirm. Findings emit in the committed materiality classes and are consumed under the finding-triage rule (recorded declines legal).

Shape notes for ideation: the pre-cut audit is natively roborev-shaped (unit = the sprint's merged delta vs the last release tag); the preflight design review is NOT diff-shaped (unit = a document assembly: member designs + index + Commander package) and needs a manifest-style input. Panel scope is the two sprint-level gates only — per-member validation stays single-reviewer + triage, per the proportionality thesis. Evidence the panel earns its place: the adopted codex cross-review was 0260's most consequential review (second re-lock: four merges, two reductions); z7's lure-catalog recipe already names gpt-5.6-sol for cross-model coverage; the 0260 preflight lens set + two-verifier pattern is banked as seed material in the shaping session's staff-review workflow script. Sequencing constraint: land AFTER the finding-triage and cycle-record members are implemented, so panel findings arrive into a world that can legally decline them. This is a new review process under the captain's consent ruling — filed on the captain's direction; not part of 0260.
