# Gate review — run-rejection-journey-in-team-mode, validation

**Gate:** validation, terminal target `done`. Approve routes to approved-awaiting-merge — the LAST of the six stack gates before the atomic merge of stack #720 and the 0.27.0-pre7 cut. Reject returns findings to implementation.

**Validator recommendation: PASSED** — delta re-verification of 52b94f9aa..9b1c86a60, recorded as entity Amendment 4 (state commit 0b5ae96a8). Attempt-1 was resolved REVISE by the captain; this attempt presents the landed revise.

## What this layer ships

1. The rejection journey runs in TEAM MODE on both runtimes, so the reuse routing it exists to prove can actually run; lifecycle grading is aligned to the determined worker topology, strict, branch-keyed per host.
2. Withdraw-recovery generalization: the pre-existing main-side gate-boundary check pinned attempt-1 three ways and accused sanctioned recoveries; it now selects the OPEN attempt, tolerates withdrawn attempts, and keeps decided-attempt and zero-open controls, with a real-verbs regression.
3. ac-reanchor fixture identity: persisted git config plus a read-back guard the old per-invocation `-c` form cannot pass.
4. Heading STRICT+PIN per the captain's attempt-1 ruling: the fixture names H3 explicitly, an H2 byte-exact line reds by design, and the drift diagnostic says what actually happened.
5. Cycle-line target pin per the captain's fold-and-rerun order (9b1c86a60): the fixture names `rejection-task/index.md` as the projection target, closing the "this section" ambiguity that let a codex FO write the line into the workflow README.

## Validator's delta verification (Amendment 4)

1. Finding 5's fix matches the filed shape exactly — open-attempt selection, withdrawn tolerated, decided-attempt and zero-open controls, attempt/briefing agreement. Falsifying probe: restoring the attempt-1 pin reds the regression.
2. Finding 6 closed per the STRICT ruling; the honest drift diagnostic is probe-verified falsifiable.
3. The ac-reanchor read-back guard verified against the old form.
4. The CI attribution was verified independently from run artifacts, not taken from the FO: the tip claude rejection red is the early-record adherence mode (round recorded with entries=2 before the rework spawn), not a defect of this branch's asserts or fixtures.

## Green evidence (composite, captain ruling 2026-08-17)

1. Codex lane 17/17 on tip 9b1c86a60 (run 31996696789) — the first fully green codex lane, rejection-flow included, proving the pin live.
2. Claude lane 17/17 on parent 43f7537b0 (run 31991864922 attempt 1); the tip delta is the 2-line fixture prose pin.
3. The tip claude reds are owned residual modes, filed to backlog with approach sections: own-sonnet-gate-conn-bypass (auto-continue true positive), own-claude-early-rejection-round-record (early record plus its lying label), own-codex-filing-variant-miss.

## Riding this gate for ratification

1. The Cycle-line pin was executed by a fresh-dispatched correction worker — the original implementer was no longer addressable, so the contract's fresh-dispatch path applied; the FO read the pushed diff back against the authorized shape, exact.
2. This gate's own attempt-2 was withdrawn and re-prepared to carry this digest artifact per the captain's presentation order — the same sanctioned withdraw-and-re-prepare recovery this layer's fix tolerates.

**Reference:** the full entity with all four amendments is pinned alongside this review.
