---
title: FO should auto-continue from completed non-gated stages to next-stage dispatch
status: validation
source: captain (2026-06-04) — FO stopped after implementation reporting instead of immediately advancing to validation; AI-engineer review found the current contract implies but does not enforce this lifecycle invariant
score: "0.32"
started: 2026-06-04T15:05:37Z
completed: 2026-06-05T06:19:51Z
verdict: PASSED
worktree:
issue:
id: wmn2x3k7j0fjshvdz126ray3
mod-block:
pr: "#303"
---

The first officer should not stop after an implementation worker completes and the implementation stage report is filed. For a non-gated, non-terminal stage, the FO should continue the workflow lifecycle: verify the stage report, advance to the next stage, and dispatch the next worker — all before ending its turn. In the dev workflow, that means implementation completion immediately advances to validation and dispatches an independent fresh validator unless a gate, terminal merge ceremony, blocker, or captain decision interrupts.

## Problem

The existing contract *implies* this behavior but never forbids the failure case, so a confused FO can stop early without violating any explicit clause. The two relevant spans both assume continuation rather than mandating it:

- `first-officer-shared-core.md` `## Completion and Gates` describes what happens when a worker completes (verify report, check gate, reuse-or-fresh) and says "**If fresh dispatch:** … Run `status --next` and dispatch the next stage." But it never says the FO MUST do this *before ending the turn*, and never names a completion-only captain-facing stop as forbidden. The non-gated branch (`If not gated: terminal → merge; else decide reuse-or-fresh.`) routes the FO into dispatch logic but does not close the door on stopping.
- `claude-first-officer-runtime.md` `## Event Loop` says "Repeat from step 1 after each agent completion until the captain ends the session." "Repeat from step 1" reads as *next iteration*, which a model can interpret as *next turn* — file the report, surface a status to the captain, and end the turn, expecting to resume later. Nothing pins the advance-and-dispatch as same-turn-mandatory.

Concretely, this let the FO report implementation completion for the Pi entity `pi-stage-dispatch-uses-build-artifact` and stop, instead of immediately advancing to validation and dispatching a fresh validator. The failure surfaced under Pi (`pi-subagents`), where the subagent result returns to the parent and the parent must itself drive the next lifecycle step — there is no separate event-loop turn to "resume" into.

This violates the workflow's own Working Principle ("prefer a code gate over a prose-only rule"): the lifecycle invariant lives only in implied prose, so its ceiling is wording-is-present, and there is no failable proof that an FO actually advances.

## Out of scope

- A binary-level state-machine command that emits the next mandatory action (e.g. a `dispatch next-action` helper). The captain's seed and the AI-engineer note floated this; it is a larger mechanism than the gap needs and would itself need its own ideation. This entity's deliverable is contract wording on three surfaces + a failable live regression scenario. If a helper is wanted later, it is a separate task.
- Changing the gated-stage behavior. Gates, terminal merge ceremony, blockers, and captain-decision interrupts are unchanged — they remain the explicit exceptions that DO stop the auto-continuation.
- The `claude-first-officer-runtime.md` `## Event Loop` and `codex-first-officer-runtime.md` are NOT amended here (the assignment scopes the three surfaces below). The host-neutral clause lands in the shared core; the Claude/Codex event loops already inherit it by reference. Only Pi gets a runtime-specific clause because its `pi-subagents` lifecycle is where the gap concretely bit.

## Proposed approach

Make the lifecycle invariant explicit on three surfaces, each backed by a failable check, plus one failable live regression scenario. The shared-core wording is host-neutral (it must not name a Claude-only event-loop step or helper — `prose_neutrality_test.go` polices the generic core).

### Surface 1 — `skills/first-officer/references/first-officer-shared-core.md`, `## Completion and Gates`

Insert a new clause immediately after the non-gated routing line. Current text (line 129):

> If not gated: terminal → merge; else decide reuse-or-fresh.

Add, on the line directly below it:

> **A completed non-gated, non-terminal stage is not a stopping point.** After verifying the report, the FO MUST advance the entity to the next stage and dispatch it (reuse-or-fresh per below) BEFORE ending its turn. It does not file a completion-only status and stop, waiting for the captain or a later turn to resume — advancing is the FO's own next action, not the captain's. The only spans that legitimately halt the turn here are: the next stage is `gate: true` (present the gate and wait), the entity is terminal (run the merge/cleanup ceremony), an explicit blocker (a rebase-conflict halt, an unmet clarification), or a captain decision the contract requires. Absent one of those, stopping after a completion-only report is a contract violation.

This sits inside `## Completion and Gates` alongside t3's and a9's edits — see the serialization note below.

### Surface 2 — `docs/dev/README.md`, `### implementation` stage definition

The dev workflow README must state that filing the implementation report is not a stopping point. Current `### implementation` `Outputs:` bullet (line 97):

> - **Outputs:** The deliverable committed to the relevant repo or state checkout, with a summary of what was produced and where

Add a following bullet under the same `### implementation` Outputs list:

> - Implementation completion is not a stopping point: once the deliverable is committed and the stage report filed, the entity routes immediately to independent `validation` dispatch — a fresh validator, since `validation` is `fresh: true` — unless a gate, blocker, terminal ceremony, or captain decision intervenes. The FO does not park a completed implementation and wait.

This is dev-workflow-specific (it names `validation` and `fresh: true` by name), which the shared-core clause cannot do (the shared core is workflow-agnostic).

### Surface 3 — `skills/first-officer/references/pi-first-officer-runtime.md`, `## Awaiting Completion`

Pi's `pi-subagents` lifecycle is where the gap bit. After the parent observes the subagent result and verifies the stage report, it must continue the shared lifecycle in the same turn — there is no separate event-loop iteration to defer into. Current `## Awaiting Completion` text (after the line about reading the entity file and verifying the stage report):

> For `pi-subagents`, the completion signal is the subagent result returned to the parent. After the result arrives, read the entity file and verify the stage report exactly as the shared core requires. Do not advance state based only on a cheerful worker summary.

Add a following sentence/paragraph in that subsection:

> Verifying the stage report is not the end of the parent's turn. Once the report is verified for a non-gated, non-terminal stage, the parent MUST continue the shared `## Completion and Gates` lifecycle in the same turn — advance the entity and dispatch the next stage (a fresh subagent when the next stage is `fresh: true`) — and only then return its final response. It does not return a completion-only result for the captain to resume from, unless the shared core's halt spans apply (next stage gated, terminal, blocked, or awaiting a captain decision).

### Serialization with t3 and a9 (same section, must not land concurrently)

All three of these in-flight entities edit `first-officer-shared-core.md` `## Completion and Gates`:

- **t3 — `gate-presentation-skill-extraction`** (id `t3w3`, implementation): extracts `## Gate Presentation` and rewrites the gated branch's "present the stage report per `## Gate Presentation` below" reference (around line 152 of the section).
- **a9 — `feedback-rejection-flow-skill-extraction`** (id `a9nt`, ideation): extracts the Feedback Rejection Flow that the gated branch's auto-bounce lines reference (around lines 154-155).
- **this entity — `wmn2`**: adds the non-gated-branch auto-continuation clause (around line 129-130).

The three edits touch different lines of the same section, so a concurrent worktree merge will conflict (the section is one contiguous block in one file). The implementation stage for this entity MUST serialize behind t3 (already in implementation) and a9: this entity's worker rebases onto whichever of t3/a9 has landed and re-anchors its insertion against the then-current `## Completion and Gates` text, rather than landing in parallel. The FO should not dispatch this entity's implementation worktree while t3's or a9's implementation worktree is open against the same file. (The shared-core split-root contract makes worktree edits land via merge; disjoint *lines* still conflict because they are the same `## Completion and Gates` block.)

### Riskiest unknown — the failable regression scenario (resolved at ideation, see Spike)

The design's soundness rests on one unverified question: *can a failable test catch an FO that stops after implementation, and what is the right substrate?* The Spike section below resolves the substrate choice and records the smallest probe that would invalidate it. The contract wording itself is real authoring, not a tested AC — per the proof policy (`f8b257cf`), a string-match over an instruction file the model ingests is not behavioral proof. The single acceptance criterion is the behavioral live regression.

## Spike (riskiest unknown — RESOLVED at ideation)

**The genuinely-novel question is the regression substrate, not the contract wording.** The contract clauses are the authored fix; their presence is not the proof (a presence oracle over an LLM-ingested instruction file is a banned tautology under `f8b257cf`). The unexercised mechanism is the behavioral AC: a test that starts from an implementation-ready dev entity, runs a real FO, and FAILS if the FO stops after the implementation report instead of advancing to validation and dispatching a fresh validator.

### Candidate substrates considered

| Substrate | What it would take | Verdict |
|---|---|---|
| **`internal/livescenario` primitive** (p4, `live-verification-gate`) — `Scenario{Runbook, Setup, Assert}` + `Runner` seam, graded on durable entity state before→after + observed output | author ONE scenario standalone, importable, run against the existing Claude live adapter (`claudeRunnerAdapter` in `livescenario_adapter_live_test.go`); offline negative case is a pure `Assert` over a hand-built broken end-state (no model spend) | **CHOSEN** |
| **Shared-scenario table** (`sharedRuntimeScenarios()` in `internal/ensigncycle`) — the gate-guardrail / rejection-flow / merge-hook-guardrail journeys | add a 4th entry to the host-neutral table + BOTH a Claude and a Codex runner | **REJECTED — too heavy** |
| **Fixture/golden or runtime assertion** (no real agent) | a frontmatter/state fixture asserting post-conditions | **REJECTED — cannot decide the claim** |

### Why livescenario, not the shared table

The shared-scenario table is parity-locked: `TestSharedRuntimeScenarioDefinitions` pins the EXACT name set `{gate-guardrail, rejection-flow, merge-hook-guardrail}` and reflects the type, so a 4th entry reds it until both the table AND both per-host runner maps are updated; `TestSharedScenarioRunnerCoverage` then requires a Claude AND a Codex runner for the new ID. That is the right machinery for a regression every host must independently exercise — but this entity's regression is the *generic* implementation→validation auto-continuation, and the p4 primitive was promoted EXACTLY so a scenario can be authored standalone (outside the ensigncycle parity machinery) and run against the existing Claude adapter. Using livescenario keeps the change to one new file plus the contract edits, with no edit to the pinned shared table. (A future task can promote it INTO the shared table if Codex parity is wanted; that is a separate, additive step, not a blocker here.)

### Why not a fixture/runtime assertion

A pure fixture can assert "given this end-state, the post-conditions hold" but cannot decide whether a *real FO actually advances* — the whole point of the regression is the live producer's behavior, which only a real agent run reveals (the same lesson `live-verification-gate` encoded: a recording proves the watcher, not the producer; a contract-text check proves the words, not the behavior). So the behavioral AC is a runtime-observable claim and its proof is a live run, gradable by the livescenario `Assert`.

### The smallest probe that would invalidate the choice (run at ideation)

The choice rests on two already-proven mechanisms; I ran the smallest exercise of each rather than assume:

1. **The livescenario primitive is live and green** in this tree (the offline grade path + the Claude adapter compile under `-tags live`). Ran `go test ./internal/livescenario/ -v` → `3 passed` (the `Scenario/Run/Runner` triple + `TestScenarioGradesBrokenOutcomeNegative` — a broken durable outcome reds the grade). This is the offline-negative-case mechanism the behavioral AC rides.
2. **A real FO dispatches a follow-up worker from a mid-lifecycle non-gated state, and the durable second stage report appears** — the structural twin of the behavioral AC. The `rejection-flow` shared scenario already proves this for the symmetric direction (validation→implementation): the FO, given a mid-lifecycle report, dispatches the next worker and a SECOND `## Stage Report: implementation` plus a routed `status:` appears in durable state. Ran the offline negative guard for it: `go test ./internal/ensigncycle/ -run TestRejectionFlowNegativeMissingRoute` → PASS (the un-routed / partial-route end-states red the assertion). The behavioral AC is the mirror image: start with an implementation report, assert a validation stage report + `status: validation` (+ a fresh worktree) appears.

**Result:** no new mechanism. The behavioral AC composes (a) p4's livescenario primitive {runbook, setup, durable-outcome grade} run against (b) the existing Claude live adapter, with (c) the rejection-flow-style durable-state assertion. The probe confirmed both substrates are green in this tree, so the design only composes already-proven behavior. The one design decision the probe settled: grade on DURABLE STATE (a `## Stage Report: validation` appears AND `status:` advanced past implementation AND a fresh validation worktree exists), not on transcript phrasing — exactly the durable-vs-transcript discipline p4 and the shared assertions already use, so the grade greens only when the FO truly advanced and reds on a completion-only stop even if the transcript narrates intent to continue.

## Acceptance criteria

The entity's only acceptance criterion is behavioral (AC-1). The three contract clauses are the deliverable's real authoring, not acceptance criteria in their own right — per the proof policy (`f8b257cf`), a string-match over an instruction file the model itself ingests NEVER satisfies a behavioral AC, so the clauses' mere presence is not a "Verified by" gate. They are listed below as the authored fix the behavioral AC exercises.

**The three contract clauses (authored fix, NOT an acceptance criterion):**
- `skills/first-officer/references/first-officer-shared-core.md` `## Completion and Gates` carries the not-a-stopping-point clause: a completed non-gated, non-terminal stage requires advancing/dispatching the next stage before ending the turn, with the named halt exceptions (gate / terminal / blocker / captain decision). Host-neutral (`internal/hostneutrality` green — no Claude-only term leaks into the generic core).
- `docs/dev/README.md` `### implementation` Outputs carries the routes-immediately-to-fresh-`validation`-dispatch clause.
- `skills/first-officer/references/pi-first-officer-runtime.md` `## Awaiting Completion` carries the same-turn-continuation clause after a `pi-subagents` completion.

These are real edits to the shipped contract, present and grep-confirmed. Their presence is necessary authoring; it is not the proof. The proof is AC-1.

**AC-1 — A real FO, given an implementation-ready dev entity under a neutral runbook, advances to validation and dispatches a fresh validator instead of stopping after the implementation report.**
Verified by: a live regression authored via the `internal/livescenario` primitive {runbook, setup, durable-outcome `Assert`}, run by a real agent through the existing Claude live adapter, that starts from the implementation-ready fixture (a dev-shaped workflow backlog→implementation→validation(`worktree: true, fresh: true, gate: true`)→done parked at `status: implementation` with a filed `## Stage Report: implementation`, neutral `Use $spacedock:first-officer` runbook with no "drive to done" coaching) and grades the DURABLE end-state — `status:` advanced past implementation AND a `## Stage Report: validation` appears (the fresh validator ran) AND the implementation report survives — not the transcript. Proven on Claude: live session `e59d1f3b` PASSED (the FO auto-advanced, dispatched a fresh validator, the validation report appeared, and it archived to terminal). The Pi origin-leg (`internal/ensigncycle/auto_continue_pi_live_test.go`, the `pi-subagents` runtime where the bug bit) is authored, compiles (`go vet -tags live` clean), and is graded by the same durable-state `assertAutoContinue`, but is ENV-BLOCKED in this environment — the pinned `pi-subagents@0.28.0` peer dep `@earendil-works/pi-coding-agent` is not installed (the existing proven `TestLivePiSubagentEnsignSmoke` fails identically), so it is owed to a Pi-provisioning follow-up, not faked here. Backed by an OFFLINE negative case (a pure `Assert` over a hand-built end-state still at `status: implementation` with no `## Stage Report: validation`) that reds the grade with no model spend — proven state-oriented, not transcript-shaped, because weakening `assertAutoContinue` to trust an "advancing to validation" transcript reds the negative test. The live half carries a `live <session:>` citation (session `e59d1f3b`); the offline negative + the rest of the suite run in the secret-free `go test ./...` lane.

## Test plan

- **Spike DONE at ideation** (see Spike): both substrate mechanisms exercised green in this tree (`go test ./internal/livescenario/` 3/3; `go test ./internal/ensigncycle/ -run TestRejectionFlowNegativeMissingRoute` PASS). No new mechanism — the behavioral AC composes p4's livescenario primitive + the Claude live adapter + the rejection-flow-style durable-state assertion.
- **The three contract clauses are real authoring, not a tested AC.** Per the proof policy (`f8b257cf`), a string-match over an instruction file the model ingests is not behavioral proof, so there is no presence oracle over the clauses. The earlier `skills/integration/auto_continue_contract_test.go` presence oracles were deleted as banned tautologies. Host-neutrality of the shared-core clause is still guarded — `go test ./internal/hostneutrality/` reds if a Claude-only event-loop term leaks into the generic core (a code-side property, not a clause string-match).
- **AC-1 behavioral live regression + offline negative:** one scenario via the promoted `internal/livescenario` primitive: a `Setup` that stages the implementation-ready dev fixture (backlog→implementation→validation(worktree, fresh, gate)→done parked at `status: implementation` with a filed implementation report), a `Runbook` that is the neutral `Use $spacedock:first-officer` prompt, and an `Assert` grading the durable end-state (`status:` advanced past implementation AND a `## Stage Report: validation` appears AND the implementation report survives; archival-aware read). Run against a real agent through the Claude adapter (`//go:build live`, like p4's `TestLivePrimitiveRunsAgainstClaudeAdapter`) — Claude leg PASSED, session `e59d1f3b`. The Pi origin-leg (`auto_continue_pi_live_test.go`) is authored and graded by the same `assertAutoContinue` but env-blocked (peer dep `@earendil-works/pi-coding-agent` absent; the proven Pi smoke fails identically) — owed to a provisioning follow-up. Plus an OFFLINE negative `Assert` case over a hand-built `status: implementation` end-state with no validation report → reds the grade with no model spend, proven state-oriented (weakening the assertion to trust an "advancing to validation" transcript reds the negative test). Cost: live half needs a credential + minutes of wallclock (live-gated); the offline negative + the rest of the suite run in the secret-free `go test ./...` lane.
- **High-stakes → detached adversarial audit BEFORE merge.** Trigger: this is the FO's OWN operating contract on the shipped scaffolding surface (the dev template names shipped contract/scaffolding as high-stakes). The audit runs read-only on a detached checkout of the merge result and tries to REFUTE the behavioral proof: confirm the AC-1 offline negative reds on a transcript that narrates "advancing to validation" while the durable state stayed at `status: implementation` (catching a grade that trusts the transcript over the state).
- **Serialization gate (process, not a test):** the implementation worker for this entity rebases onto and re-anchors against the then-current `## Completion and Gates` after t3 and a9 land; the FO does not open this entity's implementation worktree concurrently with t3's or a9's against `first-officer-shared-core.md`.

## Notes

An AI-engineer review found this should not be docs-only. The solution is contract wording on three surfaces (the authored fix) PLUS a failable live regression (the single behavioral AC) that proves a real FO actually advances. Per the captain's proof policy (`f8b257cf`), the clauses' presence is NOT itself an acceptance criterion — a string-match over an instruction file the model ingests is a banned tautology, not behavioral proof; the only proof is the live regression graded on durable state. The binary-helper "next-action command" the seed floated is deliberately out of scope (a larger, separate mechanism). The serialization overlap with t3 (`gate-presentation-skill-extraction`) and a9 (`feedback-rejection-flow-skill-extraction`) on `## Completion and Gates` is named so implementation does not land concurrently against the same section.

## Stage Report: ideation

- DONE: Pin the exact contract wording and insertion points, grounded against the live files, across all three surfaces and name the t3/a9 serialization overlap
  `## Proposed approach` pins before/after wording + insertion anchors for `first-officer-shared-core.md` `## Completion and Gates` (after line 129's non-gated routing line), `docs/dev/README.md` `### implementation` Outputs (after line 97), and `pi-first-officer-runtime.md` `## Awaiting Completion` (after the verify-the-report sentence). The serialization note names t3 (`gate-presentation-skill-extraction`, id t3w3, implementation, ~line 152) and a9 (`feedback-rejection-flow-skill-extraction`, id a9nt, ideation, ~lines 154-155) editing the same `## Completion and Gates` block; verified both entities' status/ids from the live state checkout.
- DONE: Resolve the riskiest unknown — AC-5's failable regression substrate — and run the smallest probe that would invalidate the choice
  `## Spike` chooses the `internal/livescenario` primitive over the parity-locked shared-scenario table (which `TestSharedRuntimeScenarioDefinitions` + `TestSharedScenarioRunnerCoverage` would force a 4th table entry + both host runners) and over a non-live fixture (cannot decide the producer's behavior). Probe: `go test ./internal/livescenario/` 3/3 green (offline grade + negative case) and `go test ./internal/ensigncycle/ -run TestRejectionFlowNegativeMissingRoute` PASS (the symmetric mid-lifecycle dispatch + durable-state assertion). Recorded: no new mechanism — AC-5 composes proven parts; grade on durable state (validation report + advanced status + fresh worktree), not transcript.
- DONE: Write entity-level ACs each backed by a code/test gate, every "Verified by" naming something outside the entity body that can fail
  AC-1/AC-2/AC-3 are `sectionAfter`-scoped presence oracles (the proven `skill_text_test.go` pattern) over the three contract surfaces — legitimate text-claim proof at the claim's own level. AC-4 is the concrete AC-5 scenario definition (fixture shape, FO prompt, durable end-state, negative case). AC-5 is the failable live regression via the `internal/livescenario` primitive run through the Claude live adapter, plus an offline state-oriented negative case, with a `live <ci-run:|session:>` citation subject to p4's live-run gate. Added an `## Out of scope` (the binary next-action helper is deferred) and a detached-audit trigger (this edits the FO's own shipped contract).

### Summary

Fleshed the ideation for the FO's own operating contract — a HIGH-STAKES edit. The gap is that `## Completion and Gates` and the Claude `## Event Loop` both ASSUME continuation without forbidding a completion-only stop; the fix lands an explicit, host-neutral not-a-stopping-point clause on the shared core, a dev-specific routes-to-fresh-validation clause on `docs/dev/README.md`, and a same-turn-continuation clause on the Pi `## Awaiting Completion` (where the regression bit). Each contract surface is backed by a section-scoped presence oracle; the behavioral half is a failable live regression (AC-5) authored via the already-promoted `internal/livescenario` primitive and graded on durable state, not transcript. The riskiest unknown (AC-5's substrate) is resolved with a probe confirming both proven mechanisms green in this tree — no new mechanism. Named the t3/a9 serialization overlap so implementation does not land concurrently against the same `## Completion and Gates` block.

## Stage Report: implementation

- DONE: All three contract clauses land verbatim at their pinned anchors — shared-core `## Completion and Gates` (not-a-stopping-point + advance/dispatch BEFORE ending the turn + named halt exceptions: gate/terminal/blocker/captain-decision), dev README `### implementation` Outputs (routes immediately to fresh validation dispatch), pi-runtime `## Awaiting Completion` (same-turn continuation after a pi-subagents completion) — each backed by a section-scoped presence oracle that reds when its clause is gutted (AC-1/AC-2/AC-3).
  Commit c1830f42. Branched off origin/next (already carries t3 #296 + a9 #297); re-anchored the shared-core clause directly after the current non-gated routing line (`If not gated: terminal → merge; ...`), the post-t3/a9 text — the ~129/152/154 line numbers in the ideation no longer matched. Oracles in `skills/integration/auto_continue_contract_test.go`: `TestSharedCoreForbidsCompletionOnlyStop` (sectionAfter over `## Completion and Gates`), `TestDevReadmeImplementationRoutesToValidation` (subsectionAfter over `### `implementation``), `TestPiRuntimePreservesSameTurnLifecycle` (sectionAfter over `## Awaiting Completion`). Adversarial guard run: gutted each clause with a meaning-inverting near-synonym paraphrase and confirmed its oracle reds, then restored. `go test ./internal/hostneutrality/` 37/37 green confirms the shared-core clause stays host-neutral (no Claude-only event-loop/helper leak).
- DONE: AC-5 failable live regression authored on the `internal/livescenario` primitive {runbook, setup, durable-state Assert}, PLUS an OFFLINE negative case that reds on a hand-built `status: implementation` end-state with no validation stage report (state-oriented, not transcript-shaped); the offline halves (the 3 presence oracles + the negative case) run green in the secret-free `go test ./...` lane.
  `internal/ensigncycle/auto_continue_live_test.go` (`//go:build live`) authors `livescenario.Scenario{Setup: writeAutoContinueWorkflowNoGit, Runbook: autoContinuePrompt (neutral `Use $spacedock:first-officer`), Assert: assertAutoContinue}` run through the existing `claudeRunnerAdapter`; fixture is the dev-shaped backlog→implementation→validation(worktree,fresh,gate)→done parked at `status: implementation` with a filed implementation report (`auto_continue_fixtures_test.go`, default tag). `assertAutoContinue` grades durable state: status advanced past implementation AND a `## Stage Report: validation` appears (validator ran) AND the implementation report survives. `auto_continue_negative_test.go` reds on the stop end-state even when the transcript narrates "Advancing to validation," plus the two partial-move cases (status bumped no report / report no status bump). Offline lane `go test ./...` 1113/1113 green; `go vet -tags live ./internal/ensigncycle/` clean (live half compiles, not run — needs a credential).

### Summary

Made the implementation→next-stage auto-continuation invariant explicit on three FO contract surfaces and backed each with a failable check. The three contract edits are verbatim from the ideation's `## Proposed approach`; only the shared-core insertion POSITION re-anchored against the current post-t3/a9 `## Completion and Gates` text. AC-1/2/3 are section-scoped presence oracles proven to red on a meaning-inverting paraphrase (text-claim proof at the claim's own level). AC-5 composes the promoted `internal/livescenario` primitive + the existing Claude live adapter + a shared `assertAutoContinue` durable-state assertion; the offline negative + the 3 oracles run with no model spend, and the `//go:build live` half (the behavioral proof a real FO advances) carries its `Verified by: live …` citation for the live-run gate at validation. The high-stakes detached adversarial audit before merge is owed at validation per the dispatch note.

## Stage Report: validation

- DONE: Reproduce every AC-N's cited OUTSIDE-body evidence — re-run the AC-1/AC-2/AC-3 section-scoped presence oracles AND repeat the gut-and-restore adversarial guard (gut each clause with a meaning-inverting paraphrase → confirm its oracle reds → restore); confirm AC-4's scenario definition is present; run the AC-5 OFFLINE negative and confirm it reds on the stop-end-state narrating "advancing to validation" while state stayed at `status: implementation`; full offline `go test ./...` green. Flag any AC whose only proof is self-referential prose.
  All three oracles in `skills/integration/auto_continue_contract_test.go` PASS (`TestSharedCoreForbidsCompletionOnlyStop`, `TestDevReadmeImplementationRoutesToValidation`, `TestPiRuntimePreservesSameTurnLifecycle`). Gut-and-restore reproduced per surface: each clause replaced with a meaning-inverting paraphrase (prohibition → permission to stop) → its oracle RED → restored; `git status` clean after restore confirms byte-exact restore, oracles GREEN again. AC-4 definition present and encoded in runnable code (fixture shape backlog→implementation→validation(worktree,fresh,gate)→done in `auto_continue_fixtures_test.go:55-84`, neutral prompt `autoContinuePrompt()`, durable end-state `assertAutoContinue`, negative case `auto_continue_negative_test.go`) — its proof is AC-5 existing, not self-referential prose. AC-5 offline negative `TestAutoContinueNegativeStoppedAfterImplementation` PASS; independently refuted by weakening `assertAutoContinue` to trust the "Advancing to validation" transcript → the negative test RED at `auto_continue_negative_test.go:37`, proving the grade is state-oriented not transcript-shaped; restored. Full offline `go test ./...` GREEN (14/14 test packages ok, 0 FAIL, exit 0). No AC rests on self-referential prose.
- DONE: Confirm the deliverable reflects the cleared serialization (shared-core clause re-anchored against post-t3/a9 `## Completion and Gates`, no stale line-number assumptions) and stays host-neutral (`go test ./internal/hostneutrality/` green — no Claude-only term leaked into the generic core).
  Branch built on origin/next (carries t3 #296 + a9 #297, commits bdeb5f33/d3580eb6); the shared-core clause lands directly after the current non-gated routing line (`If not gated: terminal → merge; else decide reuse-or-fresh.`, line 129), not at the stale ideation line numbers. `go test ./internal/hostneutrality/` GREEN — the host-neutral clause names no Claude-only event-loop step or helper.
- DONE: AC-5 live half — assess the `Verified by: live <ci-run:|session:>` citation; recommend drive-now vs close-post-merge per the live-AC A/C policy; deliver a PASSED/REJECTED recommendation.
  The dev workflow README declares NO `require-external-proof: true`, so the live-run terminalize guard (`internal/status/live_proof.go` + `handlers.go:240`) is DORMANT for this workflow — a terminal `--set` is not refused on a missing live citation. The live test `auto_continue_live_test.go` compiles clean (`go vet -tags live ./internal/ensigncycle/` 0 issues) and correctly `t.Skip`s with a clear message when no credential is present (offline lane stays green). Recommendation below: PASSED with the live half driven post-merge.

### Summary

Reproduced every AC's outside-body evidence by exercising, not re-reading: the three AC-1/2/3 presence oracles pass and red under meaning-inverting paraphrases (gut-and-restore, byte-exact restore confirmed by a clean `git status`); the AC-5 offline negative passes and independently reds when I weakened `assertAutoContinue` to trust the transcript — proving the grade is durable-state-oriented, the exact test-strength hole the detached audit hunts. AC-4's scenario definition is present and encoded in runnable code (not self-referential prose). Full offline `go test ./...` is green (14/14 packages, 0 FAIL); host-neutrality green; the shared-core clause is re-anchored against the post-t3/a9 section text. The only open item is AC-5's live half: its `//go:build live` test compiles and skips cleanly but has not been driven against a real credential, and this workflow does not opt into the live-run guard.

### Validation recommendation: PASSED — with the AC-5 live half driven post-merge

All offline proof is green and adversarially sound. AC-1/2/3 (contract text), AC-4 (scenario definition), and AC-5's offline negative are fully satisfied and survive refutation. AC-5's live half is the one residual: the behavioral proof that a real FO actually advances requires a credentialed run (`go test -tags live -run TestLiveAutoContinueAfterImplementation`) producing the `live <ci-run:|session:>` citation. Because the dev workflow does NOT opt into the live-run terminalize guard, this citation is advisory here rather than enforced, and the live run spends an external credential plus minutes of wallclock. Per the live-AC A/C policy (a runtime-observable claim's live half may close post-merge when the guard is not opt-in), I recommend driving the live half post-merge and recording the `live <…>` citation on AC-5 then, rather than blocking this gate on a credential spend. If the captain wants the live half driven before merge, that is the conservative alternative and the infrastructure is ready (test compiles, skips cleanly without a credential). Detached adversarial audit: the two adversarial edits this entity's Test plan names as owed (a meaning-inverting paraphrase that keeps a near-synonym; a transcript that narrates advancement over an unchanged state) were both reproduced here and both caught by the deliverable's own tests — refuted nothing material. The team-lead should fold in the parallel detached auditor's findings before presenting the gate.

### Feedback Cycles

**Cycle 1 — validation REJECTED: re-scope to behavioral-only proof, reproduce on Pi (proof-policy `f8b257cf` + captain).** The detached audit showed AC-1/2/3's presence oracles are negation-defeatable, and the captain has since ruled and shipped (`f8b257cf`) that a string-match over an instruction file the model ingests NEVER satisfies a behavioral AC — so AC-1/2/3 are not valid proof. Routed to `implementation` (reused impl ensign, `reuse_ok` 13.2%, same worktree). New scope:

- **Drop AC-1/2/3 presence oracles entirely** (banned tautologies). KEEP the three contract clauses themselves (the fix is real authoring, not an AC). Finding 2 (the dropped-`terminal` halt-exception hole) is moot once the oracle is gone.
- **Rebase onto current origin/next (`f8b257cf`)** — it now carries the proof-policy edit to the dev-README's ideation/validation/done sections; your `### implementation` Outputs clause is a disjoint section and coexists.
- **Reproduce the auto-continue behavior with a LIVE drive — on Pi (the origin runtime where the bug bit), via `internal/ensigncycle/pi_live_runner_test.go`** — plus the existing Claude AC-5 drive. RUN both (benchmark-token is rotated); do NOT defer. The bug was a `pi-subagents` failure; proving the fix on Claude alone never touches the origin. If Pi live infra is genuinely unavailable in this env, that is a finding — report it, drive the Claude leg at minimum, and flag the Pi gap; do not fake it. Keep the offline state-grader negative (it grades produced state — legitimate).
- The entity's real proof is now: the behavior reproduced live (Pi + Claude) + the offline state-grader. No presence oracles.

Re-validate after; the validator must reject any presence-oracle-only AC per the new policy.

**Cycle 2 — validation REJECTED: narrow body-only AC reconciliation (no code change).** Substance is PASS-grade — the validator independently confirmed the Claude live proof (session e59d1f3b), the durable-state grader, the honest Pi env-gap, offline green, and a clean merge-tree; the detached audit refuted nothing material. The ONE block: the entity's `## Acceptance criteria` (AC-1/2/3/4) + `## Test plan` STILL cite the now-DELETED presence oracles ("Verified by: a Go presence oracle…") — a presence-oracle behavioral AC surviving in the contract body, banned by `f8b257cf` (the policy catching itself). Routed to implementation via **FRESH dispatch** (the prior impl ensign is over budget, `reuse_ok` false). Fix (body-only, NO code change — the `auto_continue_*` tests + the three clauses are correct):
- Rewrite AC-1/2/3 → the three contract clauses are present = real authoring, NOT an AC.
- Collapse AC-4/AC-5 into the single behavioral AC: the Claude live regression (session e59d1f3b) + the offline state-grader; the Pi origin-leg cited as env-blocked, owed to a provisioning follow-up.
- Update `## Test plan` to drop the presence-oracle references.
Note (audit polish, non-blocking): the Claude runbook carries a generic `antiShutdownOverride` adjacent to the behavior (the durable-state grade gates regardless); the pi-runtime clause's only behavioral proof is the env-blocked Pi leg. This is cycle 2 — a 3rd rejection escalates to the human instead of a 4th bounce.

## Stage Report: implementation (cycle 2)

- DONE: Drop the AC-1/2/3 presence oracles entirely (banned tautologies per proof-policy f8b257cf); keep the three contract clauses themselves.
  Commit bda78e0e removed `skills/integration/auto_continue_contract_test.go` in full (its two helpers were used only inside it). The three contract clauses survive untouched (shared-core `## Completion and Gates`, dev README `### implementation` Outputs, pi-runtime `## Awaiting Completion` — all grep-confirmed present after the rebase). Finding 2 (the dropped-`terminal` halt-exception hole) is moot — the oracle is gone.
- DONE: Rebase onto current origin/next (f8b257cf, the proof-policy edit); resolve the trivial rebase.
  `git rebase origin/next` replayed both cycle commits cleanly with NO conflicts (commits 16cd8483 + bda78e0e now sit on f8b257cf). The proof-policy edit touched the dev README's ideation/validation/done sections; my `### implementation` Outputs clause is a disjoint section and coexists (both present, grep-confirmed). `merge-base --is-ancestor f8b257cf HEAD` = YES.
- DONE: Reproduce the auto-continue behavior with a LIVE Claude drive (the existing AC-5 leg), graded on durable state.
  `go test -tags live -run TestLiveAutoContinueAfterImplementation` PASSED against a real OAuth credential (benchmark-token). live session: `9e68446a` (initial 4m budget — FO advanced + dispatched the validator, cut off mid-validator) and `e59d1f3b` (PASS). Durable evidence in the stream: `dispatch: auto-continue-task entering validation` (status advanced), a fresh `spacedock:ensign` subagent `task_description: "Auto Continue Task: validation"` (the validator), a produced `## Stage Report: validation`, and terminal archival to `_archive/auto-continue-task.md`. In single-entity (`claude -p`) mode the FO drives to terminal `done` and archives, so the grade (shared `assertAutoContinue`) reads the entity from the original path OR `_archive/` (commit 1b11d41d, `resolveAutoContinueEndState`) and accepts status validation|done — the over-run more than proves the FO did not stop after implementation. Artifacts: `/tmp/spacedock-live-artifacts/claude-leg-pass/`.
- FAILED: Reproduce the auto-continue behavior with a LIVE Pi drive (origin runtime, `pi-subagents`) — environment packaging gap, reported as a FINDING per the rework instructions.
  Authored `internal/ensigncycle/auto_continue_pi_live_test.go` (`TestLivePiAutoContinueAfterImplementation`, split-root dev-shaped fixture, neutral runbook, shared `assertAutoContinue` graded on durable state, archival-aware read); `go vet -tags live` clean. The RUN fails at launch: `Failed to load extension ".../pi-subagents/src/extension/index.ts": Cannot find module '@earendil-works/pi-coding-agent'`. Root cause: `pi-subagents@0.28.0` requires the peer dep `@earendil-works/pi-coding-agent`, which is NOT installed; the local `pi` CLI (0.68.0) ships that module under the OLD scope `@mariozechner/pi-coding-agent` (a scope rename the pinned pi-subagents predates). The EXISTING, proven `TestLivePiSubagentEnsignSmoke` fails IDENTICALLY in this env → this is an environment packaging gap, not a defect in the auto-continue test. The Pi test is committed and ready to run once the peer dep is provisioned. This is the "Pi live infra genuinely unavailable" finding the rework named — reported, not faked; Claude leg driven at minimum.
- DONE: Keep the offline state-grader negative (it grades produced durable state — legitimate, not a presence oracle).
  `TestAutoContinueNegativeStoppedAfterImplementation` unchanged and green; `assertAutoContinue` unchanged. Added `TestScenarioVanishedEntityReachesAssert` covering the new tolerant post-run read in the `livescenario` primitive (a vanished/archived entity reaches the Assert as an empty after-body rather than failing Run — a workflow-neutral robustness improvement; `_archive/` knowledge stays in the Spacedock-aware Assert, not the primitive).
- DONE: Keep offline `go test ./...` green.
  Full offline lane GREEN: `go build ./...` ok, `go vet ./...` clean, `go test ./...` 1111 passed across 15 packages (was 1113 in cycle 1; −3 dropped oracles, +1 new primitive test). Both live files are `//go:build live`, excluded from the offline lane.

### Summary

Cycle-2 rework per the captain proof-policy ruling (f8b257cf): a string-match over an LLM-ingested instruction file never satisfies a behavioral AC. Dropped the AC-1/2/3 presence oracles entirely (kept the three contract clauses as real authoring), rebased cleanly onto f8b257cf, and made the entity's proof behavioral + state-oriented. The Claude live leg PASSES against a real credential (session e59d1f3b): a real FO, given an implementation-ready dev entity under a neutral runbook, advances to validation and dispatches a fresh validator, with the durable `## Stage Report: validation` as the footprint — graded by the shared `assertAutoContinue`, archival-aware. The Pi leg (origin runtime) is authored, compiles, and is ready, but cannot run in this env: the pinned `pi-subagents@0.28.0` peer dep `@earendil-works/pi-coding-agent` is not installed (pi CLI 0.68.0 ships it under the old `@mariozechner` scope) — the existing proven Pi smoke fails identically, so it is an environment packaging gap, reported as a finding rather than faked. The offline state-grader negative is kept; the `livescenario` primitive gained a workflow-neutral tolerant post-run read (covered by a new test). Offline `go test ./...` green (1111). Open item for the captain/validator: provision the Pi peer dep to drive the origin-runtime leg.

## Stage Report: validation (cycle 2)

- DONE: Verify the proof model is policy-compliant — the AC-1/2/3 presence oracle file is removed (banned tautology per f8b257cf), the three contract clauses remain, and no presence-oracle-only behavioral AC test survives.
  `skills/integration/auto_continue_contract_test.go` is deleted (commit bda78e0e; absent from disk — confirmed). All three contract clauses present (grep-confirmed): shared-core `## Completion and Gates` not-a-stopping-point, dev-README `### implementation` Outputs, pi-runtime `## Awaiting Completion`. Swept every `.go` referencing these surfaces — only the four behavioral `auto_continue_*` test files plus unrelated feature tests (codex idle / feedback-rejection / present-gate / terminal-teardown) reference the section for their own purposes; none asserts the auto-continue clause text. No presence-oracle-only behavioral AC test survives.
- DONE: Verify the BEHAVIORAL proof (now the sole proof) — the Claude live leg, graded on durable state, and the offline state-grader negative is durable-state-oriented not transcript-shaped.
  Claude live leg verified from independent artifacts (`/tmp/spacedock-live-artifacts/claude-leg-pass/claude-stream.jsonl`, session `e59d1f3b-15dc-4404-96ab-4cec4adb4f85` — matches the cited session): the stream shows `status: implementation -> validation` (FO advanced), `dispatch: auto-continue` + `spacedock:ensign` (fresh validator dispatched), a produced `## Stage Report: validation`, and terminal archival to `_archive/auto-continue-task.md`; final message confirms "Stage not gated → auto-advanced immediately" then validation dispatched fresh. The over-run to terminal `done` more than proves the FO did not stop after implementation. Offline negative `TestAutoContinueNegativeStoppedAfterImplementation` PASS; independently refuted — weakened `assertAutoContinue` to accept an "advancing to validation" transcript → the negative RED at `auto_continue_negative_test.go:37` (the grade catches a transcript-trusting assertion), then restored (git status clean, green again). `TestScenarioVanishedEntityReachesAssert` PASS (the tolerant post-run read the archival-aware grade depends on; the primitive returns an empty after-body for a vanished entity and the workflow-aware Assert resolves the `_archive/` copy — never fabricates state).
- DONE: Verify the Pi origin-leg is a genuine ENVIRONMENT packaging gap, not a wm test defect.
  Independently reproduced. `pi` 0.68.0 on PATH; `~/.pi/agent/npm/node_modules/pi-subagents@0.28.0` declares peer dep `@earendil-works/pi-coding-agent`, but only the OLD `@mariozechner/pi-coding-agent` scope is installed (homebrew, `/opt/homebrew/lib/node_modules`) — the new `@earendil-works` scope has only `pi-tui`, not `pi-coding-agent`. Running `pi --extension .../pi-subagents/src/extension/index.ts` directly yields `Failed to load extension: Cannot find module '@earendil-works/pi-coding-agent'`, with npm deprecation warnings confirming the `@mariozechner`→`@earendil-works` scope rename the pinned pi-subagents predates. Both `TestLivePiSubagentEnsignSmoke` (existing, proven) AND `TestLivePiAutoContinueAfterImplementation` (new) FAIL IDENTICALLY at the launch step (`auto_continue_pi_live_test.go:52`, before any grading) with this same error. The Pi test is authored, `go vet -tags live` clean, and reaches launch before the env blocks it — an environment packaging gap, not a test defect.
- DONE: Confirm offline `go test ./...` green and the deliverable merges cleanly onto current origin/next.
  `go build ./...` ok, `go vet ./...` clean, `go test ./...` GREEN (14 test packages ok, 0 FAIL, exit 0). `go vet -tags live ./internal/ensigncycle/` clean (both live files compile). `go test ./internal/hostneutrality/` GREEN (shared-core clause stays host-neutral). `merge-base --is-ancestor f8b257cf HEAD` = YES (rebased onto the proof-policy commit). origin/next has since advanced 5 commits (a60b3b58/ac549d34 touch the FO files too); `git merge-tree --write-tree HEAD origin/next` exits 0 with NO conflicts — the deliverable merges cleanly despite the overlap.
- FAILED: The entity's `## Acceptance criteria` (AC-1/2/3/4) and `## Test plan` still describe the dropped presence-oracle proof model — stale, not reconciled with the cycle-2 behavioral-only proof.
  AC-1/AC-2/AC-3 still read "Verified by: a Go presence oracle … the proven `skill_text_test.go` pattern" — but that oracle was deliberately DELETED as a banned tautology, so their cited evidence no longer exists. AC-4 cites the Spike/Test-plan prose, and the Test plan (line 136) + Spike (lines 83, 87) still call AC-1/2/3 "legitimate text-claim presence checks." Per the validation stage def ("reproduce the evidence cited in each Verified-by; flag any AC without evidence") and the re-review's "REJECT if any presence-oracle-only behavioral AC survives," an AC whose definition still claims a presence oracle as its proof is a banned proof model surviving in the entity's contract. The Feedback Cycles entry documents the re-scope, but the formal AC list and Test plan were never rewritten to match. This is the one blocking reconciliation.

### Summary

Re-verified the cycle-2 deliverable end-to-end by exercising, not trusting the report. The substance is sound and policy-compliant: the presence-oracle test is gone, the three contract clauses (the real authoring) remain, and the proof is now genuinely behavioral — the Claude live leg is confirmed from independent stream artifacts (session e59d1f3b: FO auto-advances, dispatches a fresh validator, files the validation report, archives to terminal), and the offline state-grader negative is proven durable-state-oriented (it reds when I weakened the assertion to trust the transcript). The Pi origin-leg's failure is a genuine, independently reproduced environment packaging gap (`@earendil-works/pi-coding-agent` peer dep absent; the proven Pi smoke fails identically) — authored honestly as a finding, not faked. Offline `go test ./...` is green and the branch merges cleanly onto current origin/next. The one blocking miss: the entity's `## Acceptance criteria` (AC-1/2/3/4) and `## Test plan` still describe the dropped presence-oracle proof, contradicting the shipped behavioral-only model and the captain's proof policy.

### Validation recommendation: REJECTED — narrow, body-only reconciliation; substance is PASS-grade

The behavioral fix and its proof PASS. The block is documentation consistency, not proof: the entity's formal `## Acceptance criteria` (AC-1/2/3/4) and `## Test plan` still cite the deleted presence oracle as their proof, which the captain's f8b257cf policy bans and the re-review explicitly says to reject. Rewrite the AC section and Test plan to match what shipped — AC-1/2/3 become "the three contract clauses are present (real authoring, NOT an AC on their own)" with the behavioral AC restated as the live auto-continue regression; AC-4/AC-5 collapse into the single behavioral AC ("a real FO advances to validation + dispatches a fresh validator, graded on durable state, proven by the Claude live leg session e59d1f3b + the offline state-grader negative; Pi origin-leg authored, env-blocked"). No code change is needed — `auto_continue_*` tests and the contract clauses are correct as-is.

Recommendation on the Pi env-gap (independent of the reconciliation): SHIP with the Claude behavioral proof + a documented Pi-provisioning follow-up. It does NOT block merge. The fix is host-neutral contract wording (shared-core clause, `internal/hostneutrality` green); the Claude live leg proves the lifecycle behavior the clause mandates; the Pi leg is authored and will run once `@earendil-works/pi-coding-agent` is provisioned. The bug's origin runtime was Pi, but the proof that the contract change drives the behavior does not require re-running on every host to be valid — and the env gap is a fleet-wide pi-subagents packaging issue (it breaks the existing proven smoke too), not specific to this entity. Open a follow-up task: provision the pi-subagents peer dep and run `TestLivePiAutoContinueAfterImplementation` to close the origin-leg citation.

Detached adversarial audit: I reproduced the audit-owed refutation (weaken the state-grader to trust the transcript → the offline negative reds) — refuted nothing material in the behavioral proof. The team-lead should fold in the parallel detached auditor's findings before presenting the gate.

## Stage Report: implementation (cycle 3)

- DONE: Reconcile the entity's `## Acceptance criteria` + `## Test plan` to the SHIPPED behavioral-only proof model — BODY-ONLY, NO code change (the `auto_continue_*` tests and the three contract clauses are correct as-is; not touched).
  Worktree code confirmed clean (`git status --short` empty, exit 0) — the cycle-2 deliverable was already committed and the validator verified it; no code WIP to recover. The presence-oracle file `skills/integration/auto_continue_contract_test.go` is absent and all three contract clauses are grep-confirmed present. Only the state-checkout entity body changed (45 lines, +20/−25). No `.go`, skill, or README file touched.
- DONE: Rewrite AC-1/AC-2/AC-3 so "the three contract clauses are present = real authoring, NOT an acceptance criterion" (per the proof-policy's text-half-is-not-an-AC rule).
  `## Acceptance criteria` now opens with the rule and lists the three clauses under a "**The three contract clauses (authored fix, NOT an acceptance criterion):**" header naming each surface and its host-neutrality guard — explicitly "their presence is necessary authoring; it is not the proof."
- DONE: Collapse AC-4/AC-5 into the single behavioral AC — the Claude live regression (session e59d1f3b) + the offline state-grader negative as the proof, with the Pi origin-leg cited as env-blocked (owed to a provisioning follow-up).
  The old AC-1..AC-5 are replaced by a single **AC-1** behavioral criterion: the live regression via `internal/livescenario`, Claude leg PASSED (session `e59d1f3b`), graded on durable state; the offline negative (proven state-oriented, not transcript-shaped); the Pi origin-leg authored + `go vet -tags live` clean but ENV-BLOCKED (`@earendil-works/pi-coding-agent` absent), owed to a provisioning follow-up.
- DONE: Update `## Test plan` to drop every reference to the now-deleted presence oracles.
  The AC-1/2/3 presence-oracle bullet is replaced by "The three contract clauses are real authoring, not a tested AC" (notes the deleted `auto_continue_contract_test.go`, keeps the code-side `internal/hostneutrality` guard); the AC-5 bullet is renamed to the AC-1 behavioral regression + offline negative; the detached-audit bullet now only refutes the behavioral proof. Also reconciled the forward-looking `## Spike` and `## Notes` to drop the dead AC-5 numbering and the "presence oracles are legitimate proof" claims (the historical Stage Reports + Feedback Cycles are left as the immutable audit trail).

### Summary

Cycle-3 rework is the narrow body-only AC reconciliation the cycle-2 validator named as the single block: the entity's formal `## Acceptance criteria` and `## Test plan` still cited the deleted presence oracles, contradicting the shipped behavioral-only proof model (captain policy `f8b257cf`). No code change — the worktree is clean and the `auto_continue_*` tests plus the three contract clauses are correct as-is. AC-1/2/3 are recast as the authored fix, not acceptance criteria; AC-4/AC-5 collapse into one behavioral AC proven by the Claude live regression (session e59d1f3b) graded on durable state plus the offline state-grader negative, with the Pi origin-leg honestly cited as env-blocked and owed to a provisioning follow-up. The forward-looking Spike and Notes were reconciled too so the document is internally consistent; the historical stage reports and Feedback Cycles are preserved unchanged.

## Stage Report: validation (cycle 3)

- DONE: Verify the AC reconciliation — the `## Acceptance criteria` (and forward-looking `## Spike`/`## Test plan`/`## Notes`) no longer cite any deleted presence oracle as proof; AC-1/2/3 are recast as "the three contract clauses are present = real authoring, NOT an AC"; the single behavioral AC cites the Claude live regression + the offline state-grader; the Pi origin-leg is cited env-blocked → provisioning follow-up.
  Grepped every "presence oracle" mention: in the live/normative sections (`## Acceptance criteria`, `## Spike` L87, `## Test plan` L131) the ONLY mentions correctly state the oracle is NOT proof / was deleted as a banned tautology — zero cite it as a "Verified by" gate. `## Acceptance criteria` (L114-126) opens with the text-half-is-not-an-AC rule, lists the three clauses under "**The three contract clauses (authored fix, NOT an acceptance criterion):**", and carries one behavioral **AC-1** ("a real FO … advances to validation and dispatches a fresh validator instead of stopping") whose Verified-by is the `internal/livescenario` live regression graded on durable state — Claude leg PASSED session `e59d1f3b`, Pi origin-leg authored + env-blocked (peer dep `@earendil-works/pi-coding-agent` absent) owed to a follow-up, plus the offline state-grader negative. The remaining "presence oracle" mentions are all in the IMMUTABLE historical Stage Reports + Feedback Cycles (L140+), correctly preserved as the audit trail.
- DONE: Confirm the live/normative sections are internally consistent — no dangling AC-2..AC-5 under the single-AC model.
  The only AC-N references in L1-138 are to **AC-1** (L116/123/125/132/133); the Spike refers to "the behavioral AC" generically, not a stale number. No dangling AC-2..AC-5 in any forward-looking section. The document is coherent under the single behavioral AC.
- DONE: Confirm NO new code change (same three commits) and offline `go test ./...` still green.
  Worktree HEAD is `1b11d41d` / `bda78e0e` / `16cd8483` on `f8b257cf` — unchanged from cycle 2 (the reconciliation was state-checkout body-only). `git status --short` empty. `go clean -testcache && go test ./...` GREEN: 14 test packages ok, 0 FAIL, exit 0 (one package no-test-files); no FAIL/panic lines.

### Summary

Cycle-3 is the AC-prose reconciliation only, and it lands clean. The entity's `## Acceptance criteria`, `## Spike`, `## Test plan`, and `## Notes` no longer cite the deleted presence oracles as proof — AC-1/2/3 are recast as the authored fix (not an AC), and a single behavioral **AC-1** carries the proof: the `internal/livescenario` live regression graded on durable state (Claude leg PASSED, session e59d1f3b), the offline state-grader negative, and the honestly-cited env-blocked Pi origin-leg owed to a provisioning follow-up. The live/normative sections are internally consistent (no dangling AC-2..5); the historical stage reports and Feedback Cycles are correctly preserved as the immutable audit trail. No code changed (same three commits), and offline `go test ./...` is green. Per the re-review scope I did not re-audit the code — cycle 2 already verified the behavioral proof end-to-end (independently confirmed session e59d1f3b, the durable-state grader, the honest Pi env-gap, a clean merge-tree) and the detached audit refuted nothing material.

### Validation recommendation: PASSED

The single cycle-2 block — the entity's formal acceptance criteria and test plan still citing the deleted presence oracles — is reconciled. The shipped proof model is now stated consistently across the entity body: the three contract clauses are real authoring (not an AC), and the one behavioral AC is proven by the Claude live regression (session e59d1f3b) graded on durable state plus the offline state-grader negative. No code change; offline suite green. This brings wm to its gate.

Two non-blocking notes for the gate (neither affects PASSED): (1) the Pi origin-leg remains env-blocked (`@earendil-works/pi-coding-agent` peer dep absent — a fleet-wide pi-subagents packaging gap that breaks the existing proven Pi smoke too, not entity-specific); ship with the Claude behavioral proof and open a Pi-provisioning follow-up to run `TestLivePiAutoContinueAfterImplementation` and close the origin-leg citation. (2) AC-1's clause reads "Verified by: a live regression …" rather than leading "Verified by: live <session:e59d1f3b>"; the `live`-lead live-run classifier would not flag it — moot here because this workflow does not opt into `require-external-proof` (the terminalize guard is dormant), but a future opt-in would want the explicit lead form. The team-lead should fold in the parallel detached auditor's findings before presenting the gate.
