# Claude run 35148797301 bounded triage

Source d0a6f413af32fa69ac9da4af2c74c0086fcca812; archived Claude artifact 10469526987. No candidate or canonical entity edits, tests, models, CI or publication. This artifact records investigation and proposals only.

## Rejection publication: failed command mistaken for success

The reported two successful calls are not two successful recorder invocations and are not an idempotent replay. Stream269–270 invokes `gate record ... --round validation/1 ...; echo exit=$?`. The recorder refuses the still-open reviewer log, states “Nothing was recorded”, and prints exit=1. The enclosing shell succeeds because echo succeeds, so the host tool_result has is_error=false. After correction, stream357–358 invokes the same command and returns the complete four-entry round with exit=0.

claudeRejectionRoundPublications in shared_round_recording_test.go correlates call IDs correctly but counts every matching result without a true is_error flag. It therefore mistakes shell success for recorder success. The stricter claudeRecordedRejectionRound success-line check correctly observes the later publication; the count is the erroneous boundary. Do not repair this with message-text-only matching or trusting a model-authored echo marker.

Retained state.bundle, final HEAD 4daae4f, has exactly one canonical round-1 directory, a review-round pointer to validation/1, the correction entries, and the fresh open unresolved validation gate. The topology has exactly eight attributed stage events: implementation/review/rework/re-review. No no-op agents appear in this run's topology. Durable history and native results agree: one failed attempt and one successful publication.

Four fields: (1) normal Claude rejection recording with a shell diagnostic suffix; (2) a correct durable publication is reported as duplicate success; (3) contract[internal/ensigncycle/shared_round_recording_test.go#assertSingleRejectionRoundPublication] exactly one successful recorder publication must be counted, independent of shell wrapper success; (4) raw calls/results269–270 and357–358 plus canonical Git bundle. Classification: material evidence defect in the publication-count observation, owned by rejection live-runner/recorder evidence owner, not #804 prose. Proposed disposition: FIX the attribution boundary, subject to distinct FO authorization.

Smallest repair surface: runClaudeRejectionFlowScenario already uses writeRecordedGateLoggingShim and an observer log outside the workflow for Codex. Reuse that existing actual recorder argv/exit evidence for Claude too, and retain the log before cleanup; share or neutrally name the existing successful-publication parser. No new controller or logging framework. The owner should verify launcher/PATH wiring captures the invoked recorder, including selected binary use, rather than assume interception. Missing interception must fail closed.

Smallest local non-model falsifier: execute the existing recorder fixture with an unclosed reviewer log under `; echo exit=$?`, then close it with the authorized correction entries and record again. The recorder log must show exit1 then exit0 while the shell succeeds twice, and count exactly one. Preserve negatives for two genuinely successful calls (same round or a second round), zero successful calls, missing log, and failure before correction. Existing canonical round and publication tests own these controls. Proposed, not run here. The premature first attempt remains observed out-of-order agent conduct; the recorder protected the immutable boundary. Fixing its success attribution does not claim perfect instruction compliance.

## Smallest mechanism: exact-target request was refused

At the run's source, smallestMechanismDirectPrompt in shared_fixtures_test.go says exactly: “Create roadmap-strategy.md containing the one-line body `# Roadmap Strategy` and commit this roadmap document directly to the repository.” This is a task, named path, exact content and direct commit instruction, not the broad “fix it directly” example the write contract excludes. The loaded fo-write-core at stream41 permits an exact-target grant. Its Workflow Fit Gate starts “Before creating or materially reclassifying an entity”; this requested plain markdown file has no entity frontmatter and does not file/reclassify a workflow entity.

The FO completed and committed both ladder edits, then refused the roadmap file (stream109), incorrectly saying the prompt had not named an exact path override and applying entity-filing fit rules to the standalone document. The scenario's durable missing-file failure is consistent with that refusal. The fixture does not conflict with those two scoped rules. No new permission or stronger coaching should be invented just to make the sample pass.

The FO explicitly cites the new suffix's shutdown-reminder/handoff and no-gate-authority phrases as suspicious pressure. That proves the suffix entered its explanation; it does not prove that deleting or changing the suffix would alter the outcome. The suffix supplies no write grant, but the preceding exact-target task does. The previous run did not report this failure; model runs are not controlled comparisons. The approved suffix still fixes its unconditional stop conflict, and this new refusal does not justify an automatic further prompt patch.

Four fields: (1) user requests a direct plain-doc edit with exact path/content; (2) authorized deliverable is missing and user is asked for authorization already supplied; (3) contract[skills/first-officer/references/fo-write-core.md#write-classifier] exact-target grants permit the specified direct edit; (4) run-source prompt, loaded classifier/Fit Gate and stream109 refusal. Classification: material observed runtime outcome defect in write-authority interpretation, owned by FO write-scope/smallest-mechanism behavior owner. Proposed disposition: HOLD/route for decision, no demonstrated deterministic product defect or authorized patch.

Smallest non-model check uses existing direct-file outcome assertion: missing roadmap must fail even when ladder edits are committed; exact one-line roadmap committed directly must pass; dispatched edit/PR escalation negatives stay red. These distinguish outcome and unnecessary orchestration, but cannot prove the model respects authorization. No grader relaxation is proposed. A behavioral repair needs an approved concrete mechanism hypothesis; static instruction matching would not supply efficacy proof.

## Prior failures and limits

Archived test results show TestLiveCommonAutoContinueAfterImplementation and TestLiveCommonDefaultHeadlessGateStop pass. Both auto-continue layouts execute inside that test, so the previous unauthorized-approval case did not recur under the unchanged strict assertion. Rejection's exact eight-event topology passes; the prior parent no-op dispatches did not recur. These are observed corrected-tip outcomes, not causal attribution to the suffix or guarantees of future reliability. The rejection and mechanism cases remain red for the findings above. Same-stage revision failures belong to the separately assigned owner and were not investigated here.

Checklist: DONE identify each assigned failure at its observation boundary; DONE propose ownership, authority and falsifying checks without candidate mutation. Recommendation: route the evidence-attribution FIX proposal separately from the unresolved runtime-authority decision. Preserve existing strict outcomes and prior evidence.
