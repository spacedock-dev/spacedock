# V1 correction proof index

Candidate `d46f74245c1c18c8a376e9a6d2a508c774e32759`; correction commit adds16net lines in two existing owners. Final task delta against `2a7b8719843e40b79545f0bb4def6609cdd9ebbf`: **+252 net / five files**,269 insertions and17 deletions. Prior candidate `5e04442a9c89ef644135721522dbf9bfcc8b7de0` and all original evidence remain unchanged.

Captain approval `resolution:binding-1789517330520748000`, artifact SHA-256 `32821e2071d9c315c7a6dfc09ba4c80c14e034bb07bb75958c136abd3a35bf30`, plus distinct FO FIX authorized this bounded retention repair and eight unique local Codex drives. Conventional bundle retention received a separate FO FIX; removing a redundant test alias funded its one existing git-bundle call without weakening assertions.

## Native replacement inventory

| Run folder | Result | Test seconds | Native evidence and durable state |
|---|---|---:|---|
| before | Expected FAIL |146.84|One completed correction worker; corrected committed plan, one rejected attempt; old skill stalls on absent canonical round.|
| after | PASS |164.58|One completed correction worker; corrected selected revision and exactly one fresh open gate, no round/reviewer.|
| review-required | PASS |140.47|Completed same-stage correction; missing review source holds gate; no invented round.|
| separate-review-required | PASS |215.41|Distinct completed implementation/validation workers; missing source holds gate.|
| round-required | PASS |214.97|One completed correction worker; exactly one four-entry canonical round without projection; corrected selected revision and fresh open gate.|
| round-missing | PASS |489.71|One completed worker; actual missing-log refusal, zero canonical rooms and no fresh gate.|
| cycle-limit | PASS |169.07|One completed worker; durable escalation, three unchanged attempts and no fourth, no invented round.|
| conventional | PASS |301.12|Eight exact producer/reviewer spawn/reuse/completion rows, distinct reviewer, one four-entry round/Cycle and open gate.|

Every folder retains `codex-native-lifecycle.jsonl`, public `codex-exec.jsonl`, `rejection-topology.tsv`, `state.bundle`, stderr, final/process result, test log and launcher/skill metadata. The native artifact is exactly the public stream, one newline, then the correlated parent rollout: the same bytes supplied to the live topology grader. It excludes auth homes and credentials.

All drives use `/opt/homebrew/Caskroom/spacedock@next/0.28.0-pre3/spacedock`, isolated local Codex OAuth and this worktree's skill source. Baseline skill SHA-256 is `9896b43ff641559bcd0965bdb6715030aff91c9285a0a708914954db984eb934`; candidate skill SHA-256 is `cf43979ac026c3a3964956853f122d93b5dbc18aa3d0b478f094024a97cdd75d`. Causal-pair fixtures, launcher and retention harness were identical; only skill bytes changed. After the pair, the separately authorized conventional bundle call and semantically identical test-alias removal produced the final harness. No fixture, skill or native parser change occurred.

## Exact producer replay

`native-state-regrade.json` records all eight public thread IDs, raw/public/bundle SHA-256 digests, bundle HEADs, complete source-indexed routes, gate attempt counts and canonical room counts. Raw prefix equality and parent session identity were verified. Spawn call IDs pair with returned task names; followups reuse the returned handle; each completion is author-attributed FINAL_ANSWER/Done and clears exactly one pending dispatch. Recomputed TSV bytes match every retained topology exactly, with no pending completion debt. The independent validator can replay directly from the retained source bytes.

All seven same-stage bundle regrades reproduce exact committed `KEEP message A; DELETE message B\n` plan/frozen input. Fresh-gate sources resolve the intended plan's exact pinned state revision/digest. Required-round contains exactly four entries; missing-round/review controls contain none. Conventional bundle contains the committed correction marker, four canonical entries, declared Cycle line and unresolved/unapplied final gate. The first inspection accidentally selected a canonical round room instead of the final gate briefing; filtering the briefing-room reference corrected that read-only inspection error. No candidate or evidence bytes changed.

## Focused proof and retained checks

`retention-negative-before.log` records the expected pre-fix failure: a failed persistence path incorrectly returned success. `retention-focused.log` and `retention-final-focused.log` pass actual persistence, fail-closed write, exact byte replay after source-home deletion, missing/ambiguous parent lookup, and existing completion/identity negatives. `retention-discovery.log` proves the live entry compiles. Existing registry, selected-revision, canonical Roborev/round controls and final normal/race logs remain reused as authorized; no broad suite/all-runtime reruns occurred. Prior full normal/race each exit1 only for the FO-declined installed-host resolver baseline; no green-suite claim is made.

## Count and timing

Exactly eight unique drives ran, with no retries or duplicate native drives. The initial `review-required$` subtest selector also matched `separate-review-required`; both passed in one Go invocation. The queue wrapper was paused, then terminated only after its Go child exited, preventing its later duplicate. Remaining selectors were anchored. The wrapper's intentional137 termination is not a native test failure.

`timing.json` derives all figures solely from retained timestamps and existing outputs. Summed case runtime:1842.17s (30m42.17s); package runtime:1844.951s. Total wall span:4556.9645s (75m56.96s). Inter-command gaps total163.129s; round-missing has2541.502s wall-minus-package discrepancy, while other within-command overhead totals7.383s. Its retained native stream has a2799.295s timestamp gap. Cause is not established; these figures must not be labeled wholly model time or tooling work. FO confirmed40–50min was an estimate and authorized finishing the existing eight within their20min per-command timeouts; none exceeded its timeout.

## Canonical correction round

Sources: `briefing.json` and `briefing.review.jsonl` here, with pinned reviewer V1 evidence, advisory producer proposal, distinct FO authorizations, producer completion and closing advisory Resolution. FO owns Feedback Cycles projection and neutral `gate record --round implementation/1` publication; producer has not invoked it. The retained independent validator owns the next recommendation. Existing9g backlog shares this retention seam; this correction makes no claim to have diagnosed or completed its separate conduct question.
