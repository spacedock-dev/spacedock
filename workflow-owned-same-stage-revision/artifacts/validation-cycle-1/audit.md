# V1 independent correction validation

Candidate d46f74245c1c18c8a376e9a6d2a508c774e32759; +252 net/five files under captain resolution:binding-1789517330520748000 and distinct FO FIX. Detached throwaway checkout at candidate HEAD; candidate unchanged. Previous detached authority/selection/round audit remains applicable because the correction changes only evidence retention and conventional bundle capture.

## Exact replay

Command: `go test -tags live ./internal/ensigncycle -run '^TestDetached' -count=1 -v`. This runs only local retained-artifact tests, never a native drive. All tests pass in 1.124s. Source files and full log are retained alongside this report. Initial audit compilation without `-tags live` failed because topology digest helpers are live-tagged; corrected invocation includes that tag without selecting any live test. No candidate failure was involved.

Eight retained public/native pairs replay: before, after, review-required, separate-review-required, round-required, round-missing, cycle-limit, conventional. Each raw file starts with exact saved public bytes plus newline; the remaining parent's session_meta ID matches the public thread.started ID. Eight IDs are distinct. Spawn call/response identity, author-attributed completion, and followup reuse reproduce every TSV row including source index, stage, owner, event and order. Conventional replays eight events including both correction/reviewer reuse rounds. Removing attributed FINAL_ANSWER/Done completion records makes every round sequence invalid.

Each corresponding state.bundle was cloned read-only into the throwaway checkout. Seven self-feedback states pass exact committed/frozen plan bytes, expected attempt/round cardinality and authority, selected-plan Git object/digest resolution, and canonical four-entry round validation. Conventional matching bundle passes canonical round, Cycle projection, and fresh gate controls. Blocked-state correction commits are referenced in raw native evidence; new gate digests in after, round-required and conventional correlate their observed native gate output to retained state. Evidence is now replayable after isolated-home cleanup; durable reports are not being substituted for native identity.

The prior deterministic selection negatives also pass on replacement after evidence: wrong identity/revision/type, zero-width Unicode suffix, empty selection and truncated JSON. Worker ordering/ownership negatives pass. Prior canonical Roborev five-entry/status-preserving and incomplete-round rejection controls remain reused, without rerunning owned green checks.

## Retention seam and scope

The existing codexNativeLifecycleStream reads the uniquely correlated parent rollout, persists public+newline+native bytes to artifactDir at mode0600, and returns a write error instead of success. The retained focused test first passes a nonexistent output directory and requires failure, then deletes the isolated source home, rereads saved bytes and requires exact equality. Its completion/handle negatives remain intact; retained pre-fix red establishes that omitted write-failure handling was detected. No new lifecycle controller, workflow policy, CLI or state schema was introduced.

The recorded correction round remains validation/1, ten entries, owned and published by FO. This validation does not append a second round or alter canonical room bytes. V1 is satisfied; no new material, deferred-risk or polish finding was found.

## Limits

No new native/full-suite runs, candidate edits, code push or CI occurred. Prior full normal/race both exit1 solely on TestCodexResolveManifestAgainstInstalledHost, previously declined by FO as out of scope; this is not a wholly green-suite claim. Replacement local Codex evidence is targeted task proof, not all-runtime/CI/release parity.

Timing arithmetic independently matches retained output: eight unique drives, 1842.17s summed test cases, 1844.951s package time, 4556.9645s total wall span. The review-required command selected both review variants once; duplicated log copies do not imply duplicated drives. The 2541.502s wall/package discrepancy is concentrated in round-missing and remains unexplained. It is neither attributed to model/tool overhead nor reported as a measured regression; timing was an estimate, not an acceptance threshold.
