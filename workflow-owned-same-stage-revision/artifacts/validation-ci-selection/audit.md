# V2 independent recheck

Candidate 342edfa8903e0e822ec545d2ee45401352eb6c60; detached checkout. Replayed the unchanged prior mutation driver against the new candidate. No additional attack matrix, candidate edit, native/model drive, duplicate broad/package suite, CI, or code push.

The actual TestRuntimeLiveRegistryReconciliation command now returns baseline=0 and every retained negative=1: missing selector, removed scheduler row, Codex-only runtime omission, commented Claude command, targeted/unclassified entry and missing exemption reason. V2a specifically reports TestLiveCommonSameStageRevision selected0 in codex-live; V2b reports Claude entries selected0. Exact logs and driver are retained.

The new observer copies exact scheduled_live_test.go and live_schedule_test.go into a temporary Go test package. Stubs are generated from discovered callable declarations, not registry desired membership or row labels. Each stub logs its own function identity only when the unchanged slot loop invokes it. Claude/Codex subprocess output counts feed the existing lane checker. This observes the previously missed continue filter; it does not interpret arbitrary Go or launch runtime drivers. Each subprocess has a one-minute timeout. The permanent mutation test retains the precise filter and comment controls.

Workflow coverage parses jobs.steps.run through existing yaml.v3 and ignores shell-comment lines. This closes the demonstrated comment hole, without claiming general shell control-flow analysis. Original missing/duplicate selection, classification, explicit exemption and missing-reason fixtures remain in the same checker test.

Only the checker file changed: +44net. Six-variant grader preservation, conventional journey byte equivalence, unchanged three exemptions and original independent native/state evidence carry forward. Current common entry is selected once per required lane without targeted-only policy or added XFAIL.

Reused actual changed-package normal/race logs pass in2.632s/8.696s. Prior full suites belong to bf64ebeca and retain only the known FO-declined installed-manifest resolver; they are not full checks of this head. Final combined-tip full suites and new tip host CI remain FO-owned pending proof. Previous CI cannot prove the promoted journey executed. Local recheck PASSED; V2a/V2b satisfied; no fresh finding.
