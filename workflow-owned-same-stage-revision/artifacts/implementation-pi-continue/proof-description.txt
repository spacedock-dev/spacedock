One-off standard-library Go test (not a committed harness): TestVariants loops serially over the six same-stage names using t.Run; each child logs execution; plain calls t.Error. go test -json -count=1 [before: -failfast; after: no flag] <temporary variants_test.go>. Both exit1; before runs plain only, after runs all six. Temporary source removed after both runs. No model process or product-source mutation.
