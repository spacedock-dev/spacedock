---
title: Build a terminal/pty live harness for team-mode e2e (residency + teardown)
status: implementation
source: "FO + captain (2026-06-16, during 2yf): headless `claude -p` cannot sustain team mode — anthropics/claude-code 2.1.178 dropped the native TeamCreate/TeamDelete tools from headless sessions (anthropics/claude-code#68721), and even with tools present the SDK/headless session lifecycle races to end_turn before teammates finish (anthropics/claude-code-action#1124). Per 7e's recorded steer (headless `-p` goes bare), the two forced-team `-p` live tests (TestLiveEnsignCycleTeamTeardown, TestLiveStandingResidencyInjectsCommOfficer) were RETIRED in 2yf because they cannot work headless. Team-mode MECHANISMS stay covered offline (internal/dispatch/spawn_standing_all_test.go for comm-officer injection; internal/ensigncycle/teardown_grade_watcher_test.go + testdata/sonnet_teamdelete_*.jsonl for the bounded-teardown marker grading). The GAP this task closes: live end-to-end team-mode coverage (FO creates a real team, injects the comm-officer standing teammate into the roster, dispatches through the team, terminalizes, and runs the bounded teardown emitting TERMINAL_TEARDOWN_BOUNDED). That requires an INTERACTIVE (pseudo-terminal) harness where team tools are present and the session stays alive — the current live suite is entirely `claude -p`."
started: 2026-06-16T15:15:39Z
completed:
verdict:
score:
worktree: .worktrees/spacedock-ensign-live-team-mode-terminal-harness
issue:
id: m40mphxan8phr3t3tp03gk89
sprint: 0205-layered-fo
mod-block: merge:pr-merge
pr: "#390"
sprint-readiness: in-progress
---

## Problem

Team mode is interactive-only: it needs a live tty session where the native team tools (TeamCreate/TeamDelete) are exposed and the agent loop stays alive while teammates work. The entire live-e2e suite drives `claude -p` (headless), which cannot do either reliably. So the live end-to-end team-mode path — comm-officer roster injection and the bounded terminal teardown — has no working harness; it has only offline mechanism coverage.

## What's needed

A live harness that drives a real INTERACTIVE Claude session via a pseudo-terminal (pty), boots the FO, and exercises:
- standing-teammate (comm-officer) injection landing in the team `config.json` roster;
- the bounded terminal teardown emitting the `TERMINAL_TEARDOWN_BOUNDED` marker after a real TeamCreate→dispatch→terminalize→teardown cycle.

## Notes / prior art

- Retirement of the two forced-team `-p` tests + the go-bare alignment happened in 2yf (shared-merge-dispatch-contract). 7e (headless-dispatch-mode-intent, #381) determined headless `-p` goes bare. #271 (headless-fo-drive-flake) first surfaced the headless silent-await stall and deferred the runtime-await question.
- Mechanism coverage that already exists (do not duplicate): `internal/dispatch/spawn_standing_all_test.go`, `standing_parity_test.go`; `internal/ensigncycle/teardown_grade_watcher_test.go`, `teardown_grade_test.go`, `streamwatch_test.go` + `testdata/sonnet_teamdelete_*.jsonl`.
- Upstream constraints to watch: anthropics/claude-code#68721 (2.1.178 headless team-tool regression — may resolve and restore headless tools), anthropics/claude-code-action#1124 (SDK/headless team lifecycle).

## Spike: riskiest mechanism (DEMONSTRATED, not asserted)

The riskiest claim — the one that invalidates the whole task if false — is: *can a pty/tmux drive of a REAL interactive `spacedock claude` session expose the native TeamCreate/TeamDelete tools AND stay resident while teammates work, where headless `claude -p` cannot?* I drove the smallest end-to-end exercise of that path with tmux on this machine (tmux 3.6a, claude 2.1.177, Go 1.26.1). It PASSED.

**Probe transport** (matches spacedock-gym's `internal/driver` approach): `tmux new-session -d -s <name> -c <cwd> "<launch>"`, then `tmux send-keys -t <pane> -l <text>` + `Enter` to drive, `tmux capture-pane -p -t <pane>` to observe. Launch line is the live runner's exact front door minus `-p`: `spacedock claude --plugin-dir <staged> --skip-contract-check -- --permission-mode bypassPermissions --model sonnet`.

**Probe 1 — team tools present + resident.** Booted the interactive session; the TUI banner showed `Claude Team · Recce` and `@spacedock:first-officer` loaded (team mode, FO contract live). Sent a prompt asking it to call TeamCreate. A real team `config.json` landed at `~/.claude/teams/ptyspike-<ts>/config.json` with `team-lead` in `members[]`, and `tmux has-session` reported the session still ALIVE after the tool call. This is exactly what headless `-p` cannot do (no team tools post-2.1.178; SDK races to end_turn).

**Probe 2 — full teardown branch.** Drove TeamCreate → `Agent(run_in_background)` spawn of a teammate `slowpoke` → `TeamDelete` while slowpoke active. The FO's own result table (captured from the FO pane):
- `TeamCreate("ptyteardown2-probe")` → `✓ team created`
- `Agent(name=slowpoke, run_in_background=true)` → `✓ member active` (on-disk roster became `members: ['team-lead', 'slowpoke']`; slowpoke launched into a SECOND tmux pane via the harness's own `claude --agent-id slowpoke@… --team-name … --agent-type general-purpose`)
- `TeamDelete() while slowpoke alive` → `✗ Cannot cleanup team with 1 active member(s): slowpoke` (the exact bounded condition the marker grades)
- marker → `✓ TERMINAL_TEARDOWN_BOUNDED: …` emitted by the FO as its own assistant text

Session stayed resident throughout (`Idle · teammates running · 1 teammate`). The marker was FO-authored, not a prompt echo (the marker/refusal strings were deliberately absent from the injected prompt; an earlier probe revision that put the marker string IN the prompt produced a false positive — recorded below as a test-design hazard).

**Spike findings that shape the design (these would have cost an implementation cycle to discover):**
1. **No pipe on launch.** Piping the launch through `| tee` makes claude's stdout a non-tty, so the interactive TUI never renders (the session ran but stayed at the wrapper banner). Claude MUST own the pane tty; observe via `capture-pane`, never a pipe. (spacedock-gym already does this.)
2. **Teammate spawn opens a new pane → capture the FO pane explicitly.** A `run_in_background` Agent spawn creates a second tmux pane and the active pane switches to the teammate. `capture-pane -t <session>` then reads the teammate, not the FO — the FO's TeamDelete/marker live in pane 0 (`#{pane_title}` = `spacedock:first-officer`). The driver MUST resolve and read the FO pane by title, exactly spacedock-gym's `firstOfficerPaneIndex` (`internal/driver/foreground.go`). This is the single most important borrowed mechanism.
3. **Send only when genuinely idle.** Keystrokes sent while the FO is mid-boot QUEUE behind the boot turn (TUI shows "Press up to edit queued messages") and the probe stalls. Gate the send on a stable-idle signal (status line present AND no spinner word / "esc to interrupt") for N consecutive polls.
4. **Auth, and the benchmark-token rotation hazard.** The repo's `~/.claude/benchmark-token` was STALE (401) on this machine; the live headless suite shares this exact dependency. The spike ran on the operator's logged-in auth (`~/.claude.json`). Note: operator auth lives in the config-dir-level `.claude.json`, so isolating `CLAUDE_CONFIG_DIR` away from it yields "Not logged in". The pty harness inherits the headless suite's auth decision tree (`isolatedClaudeEnv`: OAuth benchmark-token / `ANTHROPIC_API_KEY`, else skip) — and the same token-expiry operational risk.

## Approach

Build the pty/tmux harness as a NEW DRIVER (transport) over the EXISTING shared scenario+fixture set, not a new scenario set. The current live suite already separates a host-neutral scenario table (`sharedRuntimeScenarios()`) from a host adapter that launches the model and feeds shared assertions. Today there is exactly ONE transport baked into that adapter — `exec.Command(spacedock, "claude", … "-p", …)` inside `claudeLiveRunner.run` (`internal/ensigncycle/claude_live_runner_test.go:320-413`). The pty harness is a SECOND transport that runs the SAME scenarios.

The minimal refactor: extract the transport behind a small interface so the per-scenario orchestration (set up the workflow fixture, run the scenario, feed the shared assertions) is driver-agnostic, then add the pty driver as a second implementation alongside the current `-p` driver.

### The driver seam (the minimal refactor)

Today's coupling: each `runClaude<Scenario>Scenario(t, runner, scenario)` function calls `runner.run(t, scenario, workflowRoot, prompt) → claudeScenarioResult{finalMessage, stream, …}`. The orchestration (workflow setup + assertions) is already separate from the launch; only the launch mechanism is hard-bound to `-p`. Extract:

```go
// liveDriver is the transport seam: it turns a prompt + workflow root into the
// observed (finalMessage, stream) the shared assertions consume. The headless
// `-p` runner and the pty/tmux runner are two implementations; the scenario
// orchestration and assertions do not know which transport ran.
type liveDriver interface {
    run(t *testing.T, scenario sharedRuntimeScenario, workflowRoot, prompt string) liveResult
}

type liveResult struct {
    finalMessage string // headless: stream result event; pty: FO-pane final text / sentinel
    stream       string // headless: stream-json transcript; pty: session jsonl under CLAUDE_CONFIG_DIR
    artifactDir  string
    duration     time.Duration
}
```

`claudeLiveRunner` already satisfies this shape (`run(t, scenario, workflowRoot, prompt) claudeScenarioResult`) — the change is renaming `claudeScenarioResult`→`liveResult` (or aliasing) and declaring it implements `liveDriver`. The per-scenario functions take `liveDriver` instead of `claudeLiveRunner`. NO scenario, prompt, fixture, or assertion is rewritten — they are reused verbatim.

The pty driver `ptyLiveDriver` implements the same `run`:
- launches `spacedock claude --plugin-dir … --skip-contract-check -- --permission-mode bypassPermissions --model …` (NO `-p`) in a detached tmux pane (claude owns the pane tty);
- waits for stable-idle, then `send-keys -l <prompt>` + `Enter`;
- resolves the FO pane by `#{pane_title}` and reads it via `capture-pane` (borrowed `firstOfficerPaneIndex`);
- sources `stream` from the session jsonl written under the pinned `CLAUDE_CONFIG_DIR` (the same transcript shape the existing assertions already grade), so the shared marker/roster assertions run UNCHANGED;
- on exit, `tmux kill-session` (deferred teardown), reaps panes.

`liveResult.stream` being the session jsonl (not the `-p` stream-json) is the key compatibility point: the existing `streamWatcher`/grading code parses the stream-json dialect, and the interactive session writes the SAME dialect to its session jsonl. The pty driver tails that file for liveness instead of a stdout pipe.

### What the pty driver runs (shared) vs adds (team-mode only)

**Reused verbatim (NO duplication):**
- Scenario table: `sharedRuntimeScenarios()` (`internal/ensigncycle/shared_scenarios_test.go`) — `gate-guardrail`, `rejection-flow`, `feedback-3-cycle-escalation`, `merge-hook-guardrail`, `filing`, `shallow-boot`.
- Per-scenario fixtures + assertions: `writeGateWorkflow`/`assertGateHeld`, `writeRejectionWorkflow`/`assertRejectionFlow`, `assertThirdCycleEscalation`, `writeMergeHookGuardWorkflow`/`assertMergeHookGuardHeld`, `assertClaudeFilingViaNew`, `assertShallowBoot` + `assertNoTeamCreateBeforeGreet` + `assertShallowBootMeasured`.
- Liveness: `streamWatcher` (the shared FOStreamWatcher port) — one mechanism.
- The auth/HOME isolation decision tree: `isolatedClaudeEnv` / `decideClaudeEnv`.

**Added by the pty driver, ON TOP of the shared set (team-mode-only, the gap this task closes):**
- **comm-officer roster injection (live):** after a team-mode boot+dispatch, assert the comm-officer standing teammate actually lands in the team `config.json` `members[]` under the run's isolated team root. (Offline `spawn_standing_all_test.go` proves the SPEC is emitted; the live assertion proves the member JOINS the roster — the spike already showed an Agent-spawned teammate joining `members[]`.)
- **bounded-teardown marker (live):** after a real TeamCreate→dispatch→terminalize→teardown, assert the FO emits `TERMINAL_TEARDOWN_BOUNDED` (grade with the EXISTING `gradeTerminalTeardown` / `markerEmittedByAssistant` over the captured stream — same grader the offline fixture suite uses; the spike showed the live FO emitting it on the active-member refusal).

These two assertions are the resurrected content of the retired `TestLiveStandingResidencyInjectsCommOfficer` and `TestLiveEnsignCycleTeamTeardown`, now driven by the pty driver instead of `-p`.

### Coverage boundary (what m4 closes; what it must NOT duplicate)

- **m4 closes:** the LIVE team-mode end-to-end gap — real team → comm-officer in roster → dispatch → terminalize → bounded teardown — through the pty driver, resurrecting the two retired forced-team tests. This is the live home `live_test.go:76-82` explicitly defers to ("live team end-to-end is deferred to the terminal/pty harness task m40mphxan8phr3t3tp03gk89").
- **m4 does NOT duplicate** the OFFLINE mechanism coverage, which stays as-is: comm-officer SPEC emission/dedup (`internal/dispatch/spawn_standing_all_test.go`, `standing_parity_test.go`) and bounded-teardown marker GRADING over fixtures (`internal/ensigncycle/teardown_grade_watcher_test.go`, `teardown_grade_test.go`, `streamwatch_test.go` + `testdata/sonnet_teamdelete_*.stream.jsonl`). The pty test REUSES the grader (`gradeTerminalTeardown`) against a LIVE stream; it does not reimplement grading.
- **Relation to siblings:** unblocks `team-mode-verdict-omission` (reeppr990pyzzaejmbnyrvt7) — that task needs a stable team-mode live drive to test verdict presence, which only this pty harness provides — and complements `bare-mode-coverage-baseline` (e3z): the default `TestLiveEnsignCycle` stays team-AGNOSTIC/bare, this harness owns team-mode. The current `TestLiveEnsignCycle` flake gating v0.20.4 is a separate budget/await issue, but team-mode coverage having no live home is why the marker gate was dropped from it; m4 gives that gate a real home.

### spacedock-gym decision: reference-only (port the mechanism, not the module)

spacedock-gym (`github.com/spacedock-research/spacedock-gym`, go 1.26) has a working tmux `Driver` (`internal/driver/driver.go`: `Launch/Send/WaitForTurn/WaitForGate/Snapshot/Kill/Alive`, pane resolution in `foreground.go`, picker drive in `picker.go`, pane reads via `capture-pane`). It is a SEPARATE Go module with a different module path and Go version (1.26 vs spacedock-v1's 1.22), and it is a transcript/session test harness, not a spacedock runtime component. Decision: **reference-only** — build a minimal spacedock-v1 `ptyLiveDriver` (under `internal/ensigncycle`, `//go:build live`) that borrows gym's proven mechanisms (no-pipe launch, `firstOfficerPaneIndex` pane-by-title resolution, capture-pane polling, send-keys keystroke drive) without importing the module. Rationale: (a) cross-module import would force a Go-version bump and a dependency on a sibling research repo for a test-only path; (b) the spacedock-v1 driver only needs the narrow `liveResult` surface, far less than gym's full picker/cast machinery; (c) the shared FIXTURES already exist in BOTH repos (`testdata/sonnet_teamdelete_*`), so there is no fixture to port — only the transport technique. Extracting a shared lib is rejected by YAGNI: one consumer (the pty test) does not justify a third module. If a future second consumer appears, revisit.

## Acceptance criteria

AC-1. A `liveDriver` interface exists in `internal/ensigncycle` and BOTH the headless `-p` runner (`claudeLiveRunner`) and the new `ptyLiveDriver` implement it; the per-scenario orchestration functions accept `liveDriver`, not a concrete runner. *Test:* a compile-time assertion `var _ liveDriver = …` for each impl, and `TestLiveClaudeSharedScenarios` still green driving through the interface (the refactor is behavior-preserving for the existing `-p` path).
AC-2. The pty driver runs the EXISTING `sharedRuntimeScenarios()` with the EXISTING fixtures and assertions — no scenario, fixture, or assertion is duplicated or forked. *Test:* a parity meta-test (mirroring `claudeScenarioRunners` coverage guard) fails if the pty driver lacks a runner for any shared scenario; `grep` guard / review confirms the pty path imports the shared assertions rather than redefining them.
AC-3. A live pty team-mode test drives a real TeamCreate→dispatch and the comm-officer standing teammate lands in the team `config.json` `members[]` under the run's isolated team root. *Test:* `//go:build live` test asserting the on-disk roster contains `comm-officer` after the drive (resurrects `TestLiveStandingResidencyInjectsCommOfficer` via pty). Spike-proven mechanism: an Agent-spawned teammate joined `members[]`. **(LEGACY LANE — 0.20.5 re-scope:** this describes the legacy INTERACTIVE pty harness, now best-effort and SKIPPED on a merged host where native `TeamCreate` is gone via the `claude --version` capability gate (AC-8); it is NOT expected to RUN on the unpinned/merged CI floor and is not deleted — the legacy lane still exists for a team-tool-capable host. The merged-host coverage of the same dispatch + member-registration behavior is AC-7.)
AC-4. A live pty team-mode test drives a real TeamCreate→dispatch→terminalize and proves the dispatch DROVE THE ENTITY TO A TERMINAL STATE, accepting EITHER valid terminus: (a) the clean dispatch-to-terminal happy path — the entity reached its terminal/archived state (mode-invariant facts: `status: done` + a stage-report + a path-scoped commit) — OR (b) the cap-exhaustion path — the FO authored `TERMINAL_TEARDOWN_BOUNDED`, re-graded by the EXISTING `gradeTerminalTeardown`/`markerEmittedByAssistant` over the captured live stream so the marker stays a fully valid (but not the only) terminus. Rationale: in the interactive/tmux world the launcher just kills the session, so a clean terminalize is a valid terminus; the marker is a headless-`-p` cap-exhaustion fallback (#275/#282), whose shape is graded deterministically OFFLINE (this live oracle does not duplicate or weaken that). The `verdict:` field is NOT asserted — it is non-deterministic in team-mode finalize and owned by the spun-off verdict-omission task. *Test:* `//go:build live` test (resurrects `TestLiveEnsignCycleTeamTeardown` via pty) waiting via `expectCondition` for either terminus, then asserting the entity's mode-invariant terminal end-state on disk; reuses the offline grader (unchanged) for the marker path. Spike-proven mechanism: the live FO emitted the marker on an active-member TeamDelete refusal, and (assessment-proven) cleanly terminalizes+archives on the happy path. **(LEGACY LANE — 0.20.5 re-scope:** this describes the legacy INTERACTIVE pty harness, now best-effort and SKIPPED on a merged host (AC-8); it is NOT expected to RUN on the unpinned/merged CI floor and is not deleted. The merged-host terminal-state terminus is covered by AC-7's entity-reached-terminal check; the per-name `shutdown_request` teardown is interactive-only and has no headless analog.)
AC-5. The pty driver reads the FO pane by `#{pane_title}` (not the active pane), launches without a stdout pipe, and gates its first send on stable-idle — the three spike-surfaced hazards are encoded, not left to rediscovery. *Test:* the driver resolves the pane by title (unit-level seam test over a synthetic `list-panes` output, mirroring gym's `firstOfficerPaneIndex` test); the live tests passing is the end-to-end proof.
AC-6. The pty live tests are gated `//go:build live` and skip (never fatal) when no auth is available, reusing `isolatedClaudeEnv`. *Test:* default `go test ./...` excludes them (build tag); `go test -tags live` with no credential skips with the existing auth-missing message.

AC-7 (the 0.20.5 MERGED-lane deliverable). On a merged host (claude ≥2.1.178, native `TeamCreate` gone), a HEADLESS `claude -p` FO (no tmux) drives the workflow as a named BACKGROUND ensign dispatch — the in-process team shape #396 committed — proven by ALL of: (1) NO `TeamCreate`/`TeamDelete` — absent from the `system/init` event tool surface (the static analog of the contract's `ToolSearch(select:TeamCreate)`-empty discriminator) AND zero `TeamCreate`/`TeamDelete` tool_use in the FO stream (the FO must NOT fall to bare/sequential); (2) at least one `Agent(name set, subagent_type="spacedock:ensign", run_in_background=true, NO team_name)` dispatch; (3) the on-disk in-process member record `projects/<encoded-cwd>/<session-id>/subagents/agent-*.meta.json` carries `agentType:"spacedock:ensign"` + a name + NO `team_name`; (4) `spacedock dispatch reconcile` (NO `--team-name`, run under the FO's `$CLAUDE_CODE_SESSION_ID` + the run's isolated HOME/`CLAUDE_CONFIG_DIR`) exits 0; (5) the merged dispatch file pins the completion target `SendMessage(to="team-lead")` (per #396's committed `build.go`, NOT "main"); (6) the entity reached its terminal/archived end-state (mode-invariant facts: `status: done` + a stage-report + a path-scoped commit). **SCOPE (verified live, team-lead-approved):** a headless `-p` merged host does NOT write a `~/.claude/teams/session-<id>/config.json` registry (that is the INTERACTIVE/TeamCreate-era artifact 9243 observed); the member record is the `subagents` meta read in (3). Consequently (4) asserts reconcile EXIT 0 ONLY — it degrades to git-only since no on-disk roster lands headless, so a non-empty resolved roster is NOT asserted; and the per-name `shutdown_request` teardown / `members[]`-prune is interactive-only and is NOT asserted (the (6) terminal-state check is the kept terminus). *Test:* `TestLiveMergedTeamModeDispatch` (`//go:build live`, GREEN live on local 2.1.181, PASS ~338s) + offline `TestMergedEnsignDispatchShape` proving the stream/meta parsers; skip-not-fatal without auth via `isolatedClaudeEnv` (AC-6's gate), and SKIPPED on a legacy host (AC-8).

AC-8. Exactly ONE team lane runs per claude version, gated by a `claude --version` capability probe (the harness-observable analog of `ToolSearch(select:TeamCreate)`): the legacy interactive pty lane (AC-3/AC-4) SKIPs on a merged host (≥2.1.178, native team tools gone) and the merged lane (AC-7) SKIPs on a legacy host (<2.1.178, native `TeamCreate` present) — so whichever claude CI pins/unpins to, one team lane runs and the other SKIPs cleanly (never RED). This is what makes the CI unpin land green. *Test:* offline `TestTeamCreateCapable` (12 cases incl. the 2.1.178 floor, future majors, unparseable→skip-not-fatal); live-observed both directions (legacy SKIP + merged PASS on 2.1.181; merged SKIP on a stubbed 2.1.177). The two live gates are `skipUnlessTeamCreateCapable` (legacy lane) and `skipUnlessMergedHost` (merged lane).

## Test plan

- **Cost/complexity:** the refactor (AC-1/AC-2) is cheap and offline-verifiable (compile + the existing `-p` suite stays green). The live pty tests (AC-3/AC-4) are multi-minute, live-credentialed, `-tags live` only — same cost class as the current live suite, run behind the CI-E2E gate. AC-5's pane-resolution seam is a fast offline unit test.
- **Fixture tests:** the pane-by-title resolver gets a table test over synthetic `tmux list-panes` output (no tmux needed), borrowed from gym's `foreground` test shape.
- **CLI/behavior:** none new — the harness is test-only; it drives the EXISTING `spacedock claude` front door.
- **Live workflow tests:** AC-3/AC-4 are the live claims; they are the resurrected forced-team tests, now over the pty transport. They reuse the shared fixtures and the offline grader, so the live test adds only the team-mode assertions (roster membership, marker emission), not a parallel grading stack.
- **Risk already retired by the spike:** the load-bearing unknown (interactive pty exposes team tools + stays resident through TeamCreate/Agent/TeamDelete) is DEMONSTRATED above; implementation starts from a proven mechanism, and the spike's three hazards (no-pipe, FO-pane capture, idle-gated send) seed the driver's first tests.

## Docs impact

None user-visible: the harness is a `//go:build live` test-only addition. No CLI surface, startup banner, or docs-site behavior changes, so no doc diff is required at the ideation gate.

## Stage Report: ideation

- DONE: Spike the RISKIEST mechanism FIRST and record the evidence in the entity body: prove a pty/tmux harness can drive a REAL interactive Claude session (not `claude -p`) in which the native TeamCreate/TeamDelete tools are PRESENT and the session stays alive while teammates work — captured end-to-end by (a) the comm-officer landing in the team config.json roster and (b) a real TeamCreate→dispatch→terminalize→teardown cycle emitting TERMINAL_TEARDOWN_BOUNDED. Reuse spacedock-gym's internal/driver tmux pane driver as prior art to de-risk this cheaply; the unknown that invalidates everything is whether the pty drive actually exposes team tools + stays resident where headless `-p` cannot.
  Two live tmux probes PASSED on this machine (tmux 3.6a, claude 2.1.177): probe 1 — interactive `spacedock claude` boots team mode (`Claude Team`) and TeamCreate lands a real `~/.claude/teams/.../config.json` with the session resident; probe 2 — TeamCreate→Agent-spawn (teammate joined `members[]` in a 2nd pane)→TeamDelete refused with active member→FO emits `TERMINAL_TEARDOWN_BOUNDED`, session resident throughout. See `## Spike` section.
- DONE: Design the scenario/driver SPLIT so the new pty/tmux driver runs the EXISTING shared scenarios and reuses the EXISTING fixtures — NOT a duplicated scenario set, test codepath, or fixtures. [pinned `liveDriver` interface, named reused scenarios+fixtures+grader, named the two team-mode assertions added on top]
  See `## The driver seam` + `## What the pty driver runs (shared) vs adds`: `liveDriver` interface (both `-p` and pty implement), reuses `sharedRuntimeScenarios()` + per-scenario fixtures/assertions + `streamWatcher`, adds only live comm-officer roster + live `TERMINAL_TEARDOWN_BOUNDED` graded by the EXISTING `gradeTerminalTeardown`.
- DONE: Pin the coverage boundary: m4 closes the LIVE team-mode end-to-end gap, resurrecting the two retired forced-team `-p` tests through the pty driver — WITHOUT duplicating the existing OFFLINE mechanism coverage. State the decision on spacedock-gym's driver.
  See `## Coverage boundary` (resurrects `TestLiveStandingResidencyInjectsCommOfficer` + `TestLiveEnsignCycleTeamTeardown` via pty; offline spec-emit + fixture-grading coverage untouched; relates to verdict-omission + bare-baseline siblings) and `## spacedock-gym decision`: reference-only (borrow mechanism, no cross-module import / no shared lib, YAGNI).

### Summary

Designed the pty/tmux harness as a SECOND transport (`ptyLiveDriver`) over the one existing shared scenario+fixture set, behind a minimal extracted `liveDriver` interface that the current `claudeLiveRunner` already shape-satisfies — so no scenario, fixture, assertion, or grader is duplicated; the pty driver only ADDS the two team-mode-only live assertions (comm-officer roster membership, bounded-teardown marker). The riskiest unknown — whether an interactive pty drive exposes native team tools and stays resident where headless `-p` cannot — was DEMONSTRATED with two live tmux probes, which also surfaced three implementation hazards now encoded as ACs (no stdout pipe on launch, capture the FO pane by title not the active pane, gate the first send on stable-idle) and one operational hazard (the benchmark-token was stale/401; the harness inherits the headless suite's auth dependency). spacedock-gym is reference-only: borrow its proven tmux mechanisms (pane-by-title resolution, capture-pane polling, send-keys drive) without importing the separate module.

## Stage Report: implementation

- DONE: Extract the `liveDriver` seam reusing every existing scenario/fixture/assertion verbatim — no fork, no duplication — so the existing `-p` suite (`TestLiveClaudeSharedScenarios`) stays green driving through the interface (AC-1/AC-2).
  Renamed `claudeScenarioResult`→`liveResult`, added `liveDriver` (run/model/home/withStubPATH) with `var _ liveDriver = claudeLiveRunner{}`; the six `runClaude*Scenario` runners + `claudeScenarioRunners()` map now take `liveDriver`. Offline-verified: `go test ./...` green (0 fails), `TestSharedScenarioRunnerCoverage` green under `-tags live` (commit 6ee167cd).
- DONE: `ptyLiveDriver` encodes all three spike hazards as code — no stdout pipe on launch, resolve the FO pane by `#{pane_title}` not the active pane, gate the first send on stable-idle — with the pane-resolver covered by an offline table test (AC-5).
  `pty_live_driver_test.go` (`//go:build live`) implements `liveDriver` via tmux new-session (no pipe), `waitStableIdle`, `firstOfficerPaneIndex` (ported from gym, NOT imported); `TestFirstOfficerPaneIndex` runs offline (green). Two hazards the spike MISSED, found + fixed by tmux probe: (a) `tmux new-session` against a pre-existing server drops the command env → child env now rides per-session `-e KEY=VAL` (else HOME/CLAUDE_CONFIG_DIR/OAuth-token leak to operator real values); (b) a fresh isolated HOME stalls at Claude Code's theme/login/trust dialogs before the FO loads → `seedInteractiveClaudeConfig` pre-clears them (commit f643d278).
- DONE: The two added team-mode live assertions (comm-officer lands in team `config.json` `members[]`; live `TERMINAL_TEARDOWN_BOUNDED` graded by the EXISTING `gradeTerminalTeardown`/`markerEmittedByAssistant`) are `//go:build live`, reuse the offline grader with no parallel grading stack, and skip-not-fatal without auth (AC-3/AC-4/AC-6).
  `pty_team_mode_live_test.go` resurrects the two retired forced-team tests over the pty transport, reusing `isolatedClaudeEnv` (skip-not-fatal, AC-6), `gradeTerminalTeardown`/`expectTerminalTeardownGrade` (no new grader), and the shared fixtures (`readmeRealisticLifecycle`/`entityFixture`/`locateEntity`/...). Wired into `runtime-live-e2e.yml` (tmux install + dedicated step) behind the CI-E2E gate (commit e5aaef92).

### Summary

Built `ptyLiveDriver` as a second `liveDriver` transport over the one shared scenario+fixture set behind a minimal extracted interface the existing `claudeLiveRunner` already shape-satisfied — no scenario/fixture/assertion/grader forked. AC-1/AC-2/AC-5 are proven OFFLINE (build clean, `go test ./...` 0 fails, parity meta-test + pane-resolver table test green); the two `//go:build live` team-mode tests (AC-3 roster injection, AC-4 bounded-teardown marker) reuse the offline grader and skip-not-fatal without auth (AC-6), wired into the CI-E2E gate with tmux installed. BLOCKER for live AC-3/AC-4 on THIS machine: the repo `~/.claude/benchmark-token` is STALE (401) — a tmux probe confirmed the FO boots to idle, the FO pane resolves by title, the send lands, and the seed clears all onboarding dialogs, but every model call 401s so no real TeamCreate/teardown runs and (separately) a 401 session writes no transcript jsonl. The live tests need a fresh `~/.claude/benchmark-token` (or CI's `ANTHROPIC_API_KEY`) to green end-to-end; the harness plumbing is validated up to the credentialed model call.

### Feedback Cycles

**Detached adversarial audit (validation, cycle 1) — REFUTED NOTHING MATERIAL.** Ran on a THROWAWAY detached worktree (`git worktree add --detach` at `e5aaef92`, torn down after), never the implementation worktree. Four refutation attempts:

1. **AC-2 parity guard — genuine.** Removed the `"filing"` runner from `claudeScenarioRunners()`; `TestSharedScenarioRunnerCoverage` REDs with `shared scenario "filing" has no Claude runner`. The guard is a real discriminator. Structural note: the pty driver does NOT have a separate runner map — it SHARES `claudeScenarioRunners()` with the `-p` runner through the `liveDriver` interface, so AC-2's "pty driver lacks a runner" condition is structurally impossible by construction (the pty driver reuses the same `runClaude*Scenario` functions verbatim). The parity guard correctly covers the one shared map.
2. **Pane resolver — does not mis-resolve.** Adversarial `list-panes` inputs (FO NOT at index 0 with an active ensign at 0; FO last among three; a teammate title embedding "first-officer" prose without the `spacedock:first-officer` marker) all resolved the FO pane correctly. Resolves by marker substring and returns the real index, not "0".
3. **Fork check — no fork.** Every "shared" fixture/assertion the pty test uses (`readmeRealisticLifecycle`, `entityFixture`, `locateEntity`, `someCommitNamesOnly`, `liveStageReportHeading`, `frontmatterField`, `isTeamCreate`, `isEnsignDispatch`, `detectWrongRootBoot`, `gradeTerminalTeardown`/`markerEmittedByAssistant`/`expectTerminalTeardownGrade`) is defined OUTSIDE the pty files and imported same-package. The `pty*`-prefixed helpers are genuinely new team-mode-only code (no pre-existing equivalent forked). `ptyAnyTeamHasMember` parses the IDENTICAL `members[].name` shape `claudeteam.MemberExists`/`teamConfig` reads — an independent on-disk oracle, NOT self-referential.
4. **Spike-hazard re-introduction — a HOLE EXISTS, but it is AC-5's documented live-only altitude, NOT a strength gap.** Re-introduced each of the three hazards in the audit checkout (active-pane read in `captureFOPane`; removed the `waitStableIdle` gate from `launchAndSend`; piped the launch through `| tee`). Each left the ENTIRE offline suite green AND `go vet -tags live` clean. So no OFFLINE test guards the driver-level wiring of the three hazard fixes. This is acknowledged by AC-5 itself: its offline proof is scoped to the `firstOfficerPaneIndex` SEAM, and it states "the live tests passing is the end-to-end proof" for the integration. The hazard fixes are real code and the live AC-3/AC-4 tests exercise them; the hole is the inherent live-only e2e altitude, the same boundary every `//go:build live` test carries — not a claimed-but-missing offline guard. Recorded, not blocking.

Minor non-material notes (no action required): `paneIsStableIdle` is a pure, offline-testable function that correctly rejects empty/whitespace/busy panes but has no dedicated offline test (only the live `waitStableIdle` exercises it); `ptyAnyTeamHasMember` uses case-insensitive `EqualFold` vs the binary's exact `==` (strictly more permissive, does not weaken the AC-3 proof).

## Stage Report: validation

- DONE: Verify AC-1/AC-2/AC-5/AC-6 OFFLINE in the worktree: `go test ./...` green (the `liveDriver` refactor is behavior-preserving), the parity meta-test (`TestSharedScenarioRunnerCoverage`, `-tags live`) fails if a shared scenario lacks a pty runner, `TestFirstOfficerPaneIndex` resolves the FO pane by title, and the two `//go:build live` tests SKIP-not-fatal without auth (`go test ./...` excludes them; `-tags live` with no credential skips).
  AC-1: `go test -count=1 ./internal/ensigncycle/` green (6.4s) + full `go test ./...` all `ok` (behavior-preserving refactor; both `claudeLiveRunner` and `ptyLiveDriver` carry `var _ liveDriver = …`). AC-2: `TestSharedScenarioRunnerCoverage` green under `-tags live`; refuted by removing a runner → REDs (see Feedback Cycles #1). AC-5: `TestFirstOfficerPaneIndex` 3/3 green under DEFAULT tags (two-pane case resolves FO=0 not active ensign=1); `launchAndSend` has no launch pipe and gates the send on `waitStableIdle` (code-read + hazard re-intro audit). AC-6: with an empty no-token HOME and no `ANTHROPIC_API_KEY`, both live tests SKIP (not fatal) with the `isolatedClaudeEnv` auth-missing message, package PASS; default `go test ./...` excludes them (build tag).
- DONE: Run the DETACHED adversarial audit on a THROWAWAY checkout (never the implementation worktree): refute the offline ACs. Record findings as a `### Feedback Cycles` entry naming the adversarial edit; "refuted nothing material" is a valid recorded outcome.
  See `### Feedback Cycles` above — four adversarial edits on a throwaway detached worktree at `e5aaef92` (torn down clean). Refuted nothing material: parity guard genuine, pane resolver does not mis-resolve, no fork, oracle independent. One documented hole (the three AC-5 hazard fixes have no offline guard) is AC-5's own declared live-only altitude, not a strength deficiency.
- DONE: Assess AC-3/AC-4 (live, CI-gated): confirm the two team-mode tests are correctly wired into `runtime-live-e2e.yml` (tmux installed, correct test selectors, behind the CI-E2E gate) and would actually assert comm-officer roster membership + `TERMINAL_TEARDOWN_BOUNDED` via the EXISTING `gradeTerminalTeardown`. Recommend PASSED-pending-CI-E2E. Do NOT mark FAILED for the local auth gap.
  Wiring confirmed: `runtime-live-e2e.yml` installs tmux (step "Install tmux", `tmux -V`) before the pty step; the pty step `-run 'TestLivePtyStandingResidencyInjectsCommOfficer|TestLivePtyEnsignCycleTeamTeardown'` selectors exactly match the two `func TestLivePty…` names (`go test -tags live -list` confirms exactly those two); behind the `CI-E2E`/`CI-E2E-OPUS` environment matrix (required-reviewer gate) with `ANTHROPIC_API_KEY` and `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` (the flag that exposes native team tools). AC-3 oracle is the on-disk team `config.json` `members[]` (independent of the test). AC-4 reuses the EXISTING `gradeTerminalTeardown`/`markerEmittedByAssistant` (defined in `streamwatch_test.go`/`teardown_grade*.go`, NOT reimplemented) — verified an honest discriminator: offline grader tests prove it PASSES on FO-authored marker emission and FAILS on silent give-up AND on a contract-Read of the marker string (the false-positive guard). Cannot green locally (stale benchmark-token 401); NOT marked FAILED.

### Summary

Validated the m4 terminal/pty live harness. AC-1/AC-2/AC-5/AC-6 are PROVEN OFFLINE by running the behavior (full suite green, parity meta-test green and refute-tested, pane-resolver table test green under default tags, live tests SKIP-not-fatal with no auth and are build-tag-excluded by default). The detached adversarial audit on a throwaway checkout refuted nothing material: the parity guard is a genuine discriminator, the pane resolver does not mis-resolve under adversarial `list-panes` inputs, no shared scenario/fixture/assertion/grader is forked (all imported same-package, the team-roster oracle parses the binary's own `members[]` shape), and the one hole found — no offline guard on the three AC-5 hazard fixes inside `launchAndSend`/`captureFOPane` — is AC-5's explicitly documented live-only altitude, not a strength gap. AC-3/AC-4 are live/CI-gated: the two team-mode tests are correctly wired into `runtime-live-e2e.yml` (tmux installed, exact `-run` selectors, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, behind the CI-E2E approval gate) and reuse the EXISTING honest teardown grader; they cannot green locally only because the operator benchmark-token is stale (401), so they are PASSED-pending-CI-E2E (NOT failed). Verdict: PASSED — AC-1/2/5/6 verified offline now; AC-3/AC-4 must show, in the CI-E2E gate run, the live team `config.json` carrying `comm-officer` in `members[]` (AC-3) and an FO-authored `TERMINAL_TEARDOWN_BOUNDED` that `gradeTerminalTeardown` greens over the captured session jsonl (AC-4).

### Stage Report addendum: macOS symlink fix + F30 transcript-pinning fix (post-validation)

A local live re-run (fresh credential) surfaced TWO issues in the pty team-mode tests, both now fixed:

1. **macOS symlink false-positive (the reported issue, commit cf6f2eba):** `detectWrongRootBoot` compared the boot cwd against the fixture root as raw strings; macOS `t.TempDir()` returns `/var/folders/...` while the FO boots the EvalSymlinks-resolved `/private/var/folders/...` (same dir), false-flagging a wrong-root wander. Resolved the fixture root with `filepath.EvalSymlinks` before the check in both tests (the existing pattern). No-op on the CI Linux runner; the wrong-root guard still catches a genuinely wrong root.

2. **F30 transcript-pinning bug the symlink message was masking (commit f1d9f120):** with the symlink message gone, the REAL failure surfaced — `expect(isTeamCreate)` timed out at 4m even though the FO created the team and dispatched (visible on disk). Root cause: once the FO creates a team and dispatches the comm-officer, that teammate writes its OWN transcript into the same projects dir (newer, assistant-bearing), and the tail's newest-file resolver FLIPPED to the comm-officer transcript — which carries no TeamCreate. The 42 lines the tail read were all `agentName=comm-officer`; the FO's TeamCreate/TeamDelete were in the team-ROOT transcript (the file with NO `agentName`, confirmed against every TeamCreate-bearing transcript on disk) it never read. Fix: capture the FO's own session id at boot from the root transcript and resolve the tail file BY THAT ID each drain (`sessionFileByID`), so a teammate transcript never lures the tail off the FO. Added offline regression `TestFOSessionPinning` (the tail reads the FO's TeamCreate, not the comm-officer transcript) — caught offline, no live auth.

Offline: `go test ./...` green (0 fails), `TestFOSessionPinning` + `TestFirstOfficerPaneIndex` green, `go vet -tags live` clean. Live re-confirmation BLOCKED by OAuth quota exhaustion ("session limit, resets 4:30pm PDT") on the local credential; the F30 fix is proven by the offline regression against the exact failing transcript shape. CI's `ANTHROPIC_API_KEY` lane is the authoritative live proof.

### Stage Report addendum: CI route to the live green (captain's call)

Per captain's decision, the live AC-3/4 green comes via CI (ANTHROPIC_API_KEY — no OAuth quota), not a local re-run. Three steps done on the worktree branch:

- **Decoupled the live steps from an earlier step's failure:** added `if: ${{ !cancelled() }}` to both the "Run live Claude shared scenarios" and "Run live pty team-mode harness" steps in `runtime-live-e2e.yml`, so a failure in the earlier "Run live ensign cycle" step (e.g. the current `TestLiveZeroDiscover` flake) no longer fail-fast SKIPS them. Each step now reports its own pass/fail; the pty AC-3/4 proof always runs.
- **Rebased onto current `origin/main`** (ezf #391 + hf #392 + the README proof-policy commit) — clean rebase, no conflicts (my files don't overlap). Build, `go vet -tags live`, and `go test ./...` green on the rebased branch (one transient `TestSonnetTeamDeleteHangReplay` flake under concurrent full-suite load — a pre-existing test in streamwatch_regression_test.go I never touched; passes in isolation and on package re-runs).
- **Force-pushed** (`--force-with-lease`) — PR #390 updated, CI re-triggered.

The pty step greening in the CI-E2E run is m4's AC-3/4 live proof.

### Stage Report addendum: LOAD-BEARING FINDING — the interactive FO writes NO on-disk transcript (CC 2.1.177); m4's transcript-observation premise is invalid

While fixing the idle gate (CI failed because the pane-text gate is render-fragile on headless runners), a controlled probe surfaced a finding that invalidates m4's core observation mechanism and is being escalated to the captain for a possible re-scope. STOPPED here rather than force a green.

**Finding:** In Claude Code 2.1.177 (interactive launch, isolated `CLAUDE_CONFIG_DIR`), the team-LEAD / FO NEVER persists its own conversation transcript to disk. Only SPAWNED teammates write a `projects/<cwd>/*.jsonl`. The FO's own TeamCreate / TeamDelete / `TERMINAL_TEARDOWN_BOUNDED` marker render ONLY in the live tmux pane, never on disk.

**Reproducible probe (a detached tmux `spacedock claude` with HOME/CLAUDE_CONFIG_DIR/OAuth via `-e`, onboarding pre-seeded):**
1. FO boots, auto-engages, runs `--version` / `git rev-parse` / `status --discover`, greets → NO `.jsonl` anywhere (config dir, isolated HOME, real HOME all checked; only an empty `session-env/<uuid>/`).
2. FO creates a team (`teams/<name>/config.json` lands on disk) → STILL no FO transcript, no `projects/` dir.
3. FO spawns a teammate via `Agent(run_in_background)` → ONE `projects/*.jsonl` appears, and it is the TEAMMATE's (every conversation entry `agentName=<teammate>`; 0 TeamCreate in it). The FO's TeamCreate is in no transcript.

**Implications for the ACs:**
- **AC-3 (comm-officer roster) — still satisfiable on disk:** the roster check reads the real `teams/<name>/config.json` directly, which IS written.
- **AC-4 (FO-authored `TERMINAL_TEARDOWN_BOUNDED` graded over the captured FO transcript) — NOT satisfiable as specified:** there is no FO transcript to grade; the marker is pane-only.
- **The idle gate, `expect(isTeamCreate)`, and the liveness drain** all assumed an FO transcript that does not exist. (This also explains the earlier residency run: it "saw" `teamName` entries only because it tailed the comm-officer TEAMMATE transcript via the F30-flipped resolver; its `expect(isTeamCreate)` actually timed out too — consistent.)

**Status:** committed offline work stays valid (the idle-gate `transcriptReachedIdle` + `TestTranscriptReachedIdle`, the F30 FO-session pin + `TestFOSessionPinning`, the macOS symlink fix, the CI `if: !cancelled()` decoupling). The captain-nudge code is uncommitted, held pending the re-scope decision. No fix attempted around the finding — it is an architecture mismatch between m4's transcript-observation design and CC 2.1.177's interactive persistence behavior, escalated for the captain's call on m4's scope (and its place in the v0.20.4 path).

### Feedback Cycles

**Validation → implementation bounce (captain-adjudicated, 2026-06-16 session #4).** Captain's call: FINISH m4 into v0.20.4 — and the LOAD-BEARING FINDING above is **FALSE**. The interactive team-lead FO **does** persist its own session transcript to disk; the probe looked in the wrong place. FO-side de-risk (gathered with zero live-auth spend, against a live interactive FO session — the Commander's own):

- The FO transcript is the **flat** `{CONFIG_DIR}/projects/{encode(cwd)}/{FO-session-id}.jsonl` (one entry-per-line stream; first entry keys `[agentSetting, sessionId, type]`, **no** `agentName`).
- The sibling `{...}/{FO-session-id}/` **directory** (no `.jsonl`) holds `subagents/` + `workflows/` — i.e. the **teammate/subagent** transcripts. `session-env/` is env staging, unrelated to transcripts.
- The committed probe read `session-env/<uuid>/` and/or the `<id>/` subdir (teammate transcripts) and missed the sibling flat file one level up — the **same class as F30** (the resolver flipping to the teammate transcript).

**Routed implementation scope:** point the transcript resolver at the flat FO `<session-id>.jsonl` (confirm the path holds under the harness's isolated `CLAUDE_CONFIG_DIR`, polling for first-turn flush — do not single-shot check); correct the FALSE addendum; confirm `transcriptReachedIdle` and AC-4 resolve over the located FO transcript; commit the bounded captain-nudge; re-green locally. The FO (not the ensign) owns the re-push + CI-lane approval.

## Stage Report: implementation (cycle 2)

- DONE: The transcript resolver locates the interactive FO's OWN session jsonl (the flat `projects/{encode(cwd)}/{FO-session-id}.jsonl`, NOT the `{id}/` subdir or session-env), proven by a regression test.
  Fixed the real F30-class resolver bug: `foRootSessionID` skipped any flat `.jsonl` containing `"agentName":` anywhere in the body, which WRONGLY skips a real FO root transcript that carries a later `{"type":"agent-name","agentName":"spacedock:first-officer"}` entry (the FO self-identifying). Switched to the captain's first-entry oracle — the FO root is the flat file whose FIRST sessionId-bearing entry has no top-level `agentName` (new `foRootSessionFlatFile`). Confirmed against on-disk data: every flat TeamCreate-bearing transcript's first entry has no top-level agentName, and 0/2395 flat transcripts carry a first-line agentName (teammate/subagent transcripts live in the sibling `{id}/subagents/` subdir). Regression `TestFORootResolvesWhenSelfIdentifying` REDs on the old resolver, GREENs on the fix; `TestFOSessionPinning` + `TestTranscriptReachedIdle` still green (commit c939aea8 post-rebase). The resolver correctly resolves the flat FO file WHEN one exists on disk.
- DONE: The bounded captain-nudge is committed.
  `nudgePastGreet` (≤3 nudges keyed on the on-disk transcript carrying TeamCreate / ensign-dispatch) + `linesHaveTeamCreate`/`linesHaveEnsignDispatch` wired into both team-mode tests, committed in c939aea8 alongside the resolver fix.
- FAILED: Correct the FALSE "FO writes no transcript" addendum + confirm `transcriptReachedIdle` and AC-4 resolve over the located FO transcript.
  Could not confirm, because a LIVE probe under the harness's exact launch shows the FO transcript is NOT written. ESCALATED to the captain — see the addendum below. The addendum's CONCLUSION ("the harness's interactive FO writes no on-disk conversation transcript") reproduced robustly; what was wrong in it was the MECHANISM claim, now corrected. The resolver is fixed regardless (it was a genuine bug), but it has no flat FO transcript to resolve under the tmux-subprocess launch, so AC-4's grade-over-the-FO-transcript cannot green.

### Stage Report addendum (cycle 2): LIVE PROBE — the harness's tmux-spawned FO writes NO conversation transcript (reproduced 6×); AC-3 on-disk oracle works, AC-4 transcript-grade is blocked

I closed the captain's three live caveats with a fresh-credential live probe under the harness's exact isolated env. Caveat #1 ("confirm the flat file lands under the isolated `CLAUDE_CONFIG_DIR`") closed with a NEGATIVE result, which is exactly the verification the captain asked for rather than assume.

**What I ran (refreshed `~/.claude/benchmark-token` from Keychain first; validated it with a headless `claude -p` → `OK`):**
- The harness's own `launchAndSend` (full `spacedock claude` FO boot) under the isolated `CLAUDE_CONFIG_DIR`: the FO booted, ran `--version`/`status --discover`/`status --boot`, presented its full greet, and PARKED idle — a complete multi-turn boot — yet over a 4-minute poll the `projects/` dir was NEVER created. Only an empty `session-env/<uuid>/` appeared (the same artifact the original addendum's probe found).
- Six controlled tmux probes isolating each variable (custom vs default `CLAUDE_CONFIG_DIR`; env `CLAUDE_CODE_OAUTH_TOKEN` vs stored-login `.credentials.json`+`oauthAccount`; warm real `~/.claude` vs fresh isolated HOME; explicit `--session-id`; multi-turn). In EVERY case a turn completed with real token spend (status line `📊 12% · 💰 $0.05`) and NO `projects/*.jsonl` was written — not under the isolated config, not under the warm real `~/.claude`. `tmux -e HOME`/`CLAUDE_CONFIG_DIR` were verified to apply to the child.

**Reconciliation with the captain's de-risk (NOT a contradiction of the on-disk facts):** real FO transcripts with first entry `[agentSetting, sessionId, type]` DO exist on disk (e.g. this very Commander session's `76fbbaa9-…jsonl`) — the captain's path STRUCTURE is correct, and my resolver fix targets exactly that shape. The gap is the LAUNCH CONTEXT: a `claude` session spawned as a **detached tmux subprocess** (the harness's transport) does not persist a conversation transcript, whereas the **top-level** CC session the captain de-risked against (the Commander's own) does. The captain's de-risk was against a top-level session and did not control for the tmux-subprocess launch.

**Clean AC split (decisive probe — tmux FO, isolated config, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, prompted to call TeamCreate):**
- **AC-3 (comm-officer roster) — SATISFIABLE:** the FO created the team and wrote `teams/probecheck/config.json` on disk under the isolated config dir. The team tools are exposed and the on-disk roster oracle is real and independent of any transcript.
- **AC-4 (`TERMINAL_TEARDOWN_BOUNDED` graded over the FO transcript) — BLOCKED as specified:** the marker rendered in the pane (`⏺ Team "probecheck" created`) but NO conversation transcript was written, so there is nothing for `gradeTerminalTeardown`/`markerEmittedByAssistant` to grade. The idle gate (`transcriptReachedIdle`) and the `expect(isTeamCreate)` drain assume an FO transcript that the tmux launch does not produce.

**Why I escalated rather than re-scoped or forced a green:** the captain's adjudication explicitly said "do NOT re-scope around the false premise — fix the resolver," and forbade re-asserting the addendum. But the live evidence shows AC-4's transcript-grade genuinely cannot run under the current tmux transport, and forcing a green over a non-existent transcript would be the false-positive trap the spike warned about. This needs a captain/FO call on AC-4's observation surface (options that surface from the evidence, FO/captain to choose): (a) source AC-4's marker from the captured FO PANE text instead of the transcript (pane is render-fragile on headless CI — the very lesson that retired the pane-gate); (b) drive the FO with `--session-id` AND find/force a transcript-persistence path that survives the subprocess launch; (c) narrow m4 to AC-3's on-disk roster proof (which works live) and route AC-4's marker coverage to the existing OFFLINE fixture grade (`teardown_grade_watcher_test.go` + `testdata/sonnet_teamdelete_*`), since the live transcript surface is unavailable. The resolver fix + captain-nudge ship regardless.

### Summary

Fixed the genuine F30-class resolver bug (the whole-file `"agentName":` skip wrongly dropped an FO transcript that self-identifies as `spacedock:first-officer`) with the captain's first-entry oracle and a RED→GREEN regression; committed the bounded captain-nudge; rebased clean onto `origin/main`; offline GREEN recorded (`go build ./...`, `go vet -tags live ./...`, `go test ./...` 15/15 ok, the three offline regressions pass). A live probe on a fresh benchmark-token then CLOSED the captain's caveat #1 with a negative, reproduced-6× result: the harness's **tmux-spawned** interactive FO writes NO conversation transcript (under any config dir / auth / session-id), even though it writes the team `config.json` and spends tokens. The captain's path structure is correct for top-level CC sessions (real FO transcripts exist on disk and my fix targets them), but the harness's detached-subprocess launch does not persist one — a distinction the top-level de-risk didn't control for. Net: AC-3's on-disk roster oracle is live-satisfiable; AC-4's grade-over-the-FO-transcript is blocked by the missing transcript and needs a captain/FO call on its observation surface. Did NOT push (FO owns the re-push). Did NOT force a false green.

### Stage Report addendum (cycle 2, CORRECTION): the "no transcript" conclusion was a LOCAL ENV ARTIFACT (inherited nested-session markers); env-scrub fix RESTORES the transcript live; the remaining block is an UPSTREAM team-tool regression in claude 2.1.178/179

The team-lead caught the confound in my "no transcript" conclusion, and a follow-up live probe confirmed it. The conclusion was WRONG; the mechanism is now nailed.

**ROOT CAUSE of the no-transcript probes (team-lead's diagnosis, confirmed):** All my "no transcript" reproductions ran the tmux child from inside THIS Claude session, whose env exports the nested-session markers `CLAUDECODE=1`, `CLAUDE_CODE_CHILD_SESSION=1`, `CLAUDE_CODE_SESSION_ID`, `CLAUDE_CODE_AGENT`, `CLAUDE_CODE_ENTRYPOINT`, `CLAUDE_CODE_EXECPATH`. Since CC 2.1.170 ("sessions launched from a shell that inherited Claude Code env vars don't save transcripts"), a child that inherits those markers does NOT persist its conversation transcript. My first scrub attempt failed to actually scrub — `tmux -e KEY=VAL` ADDS per-session vars but cannot REMOVE a var the tmux server inherited, so the markers leaked through. A REAL scrub (`env -u <markers> claude …`) flips it: the flat FO transcript `projects/{encode(cwd)}/{id}.jsonl` lands under the isolated `CLAUDE_CONFIG_DIR`, and the resolver chain resolves over it live (foRootSessionID → flat id, sessionFileByID → flat file, foRootSessionFlatFile oracle confirms FO root, transcriptReachedIdle reads idle on the end_turn, FO-pinned tail reads TeamCreate). So the captain's premise was RIGHT all along — the FO does persist a transcript; my harness was suppressing it by leaking the parent's nested-session env.

**THE FIX (committed 2b4beaa0, offline-green, proven live):** `nestedSessionMarkers` + `unsetNestedSessionArgs` — the pty launch is now `env -u CLAUDECODE -u CLAUDE_CODE_CHILD_SESSION -u CLAUDE_CODE_SESSION_ID -u CLAUDE_CODE_AGENT -u CLAUDE_CODE_ENTRYPOINT -u CLAUDE_CODE_EXECPATH spacedock claude …`. The team flag (`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`) and the auth/config vars are NOT unset. Robust both locally-from-claude AND in CI (where none of the markers are set, the unset is a no-op). Offline regression `TestUnsetNestedSessionArgs` (every marker unset, team flag preserved, command intact).

**LIVE AC-4 run with the scrub applied (fresh benchmark-token, 332s):** the transcript NOW persists and the harness reads it end-to-end (the resolver pinned FO session id `7916afcd-…`, the drain streamed full FO assistant turns). The transcript half of m4 is PROVEN live. But the run FAILED the teardown grade for a DIFFERENT, upstream reason:

**REMAINING BLOCK — UPSTREAM (anthropics/claude-code#68721 blast radius reaching interactive sessions):** claude auto-updated 2.1.177 → 2.1.179 mid-session (the isolated test HOME lacks the real settings' `DISABLE_AUTOUPDATER`). On 2.1.179, the native team tools are NOT exposed even to an interactive session with `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` CONFIRMED present in the child env — a direct live probe asking the FO to list Team* tools returns `NO_TEAM_TOOLS`, and the AC-4 FO reported "TeamCreate is not available in this session environment … CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 [present]" and correctly fell back to bare mode. (My earlier manual probes that DID create `teams/scrubprobe3/config.json` ran on 2.1.177, BEFORE the auto-update — that version still exposed the tools.) Bare mode has no team to tear down, so the FO never authors `TERMINAL_TEARDOWN_BOUNDED` and the grade correctly REDs. This is the exact upstream regression the entity flagged as a watch item, now confirmed to extend to interactive sessions.

**Corrected AC status:**
- **AC-1/AC-2/AC-5/AC-6** — GREEN offline (unchanged).
- **Transcript mechanism (resolver + idle gate + drain + env-scrub)** — PROVEN LIVE.
- **AC-3 (comm-officer roster) / AC-4 (bounded teardown marker)** — BLOCKED by the 2.1.178/179 team-tool regression, NOT by the harness. They are ACHIEVABLE the moment a team-tool-capable claude is used (the env-scrub + resolver make the transcript surface available). To green them now requires either (a) pinning/using a claude version that still exposes native team tools (2.1.177 did), with `DISABLE_AUTOUPDATER` in the isolated test config so CI doesn't auto-update past it, or (b) waiting for upstream to restore the tools (#68721). This is a captain/FO call on version pinning vs deferral — outside the ensign's authority.

**Net:** the captain's resolver + transcript premise is vindicated and fixed (resolver oracle + env-scrub, both committed, transcript proven live). The live team drive's remaining red is upstream team-tool unavailability in 2.1.179, independently confirmed. Offline suite green (`go test ./...` 15/15 packages ok; new offline regression `TestUnsetNestedSessionArgs` + `TestFORootResolvesWhenSelfIdentifying` pass; `go vet -tags live ./...` clean). Did NOT push. Did NOT force a false green. The earlier cycle-2 "tmux transport does not persist a transcript" conclusion is SUPERSEDED — it was the inherited-env artifact, now fixed.

### Stage Report addendum (cycle 2, DEFERRED-HOP DISAMBIGUATION): the 2.1.179 team-tool absence is a REAL regression, not a deferred-hop false-negative

The team-lead flagged that TeamCreate/TeamDelete are DEFERRED tools in the spacedock contract — absent from a session's `init` tool list by design, callable only after a `ToolSearch(query="select:TeamCreate")` hop. My first `NO_TEAM_TOOLS`/`NO_TEAMCREATE` verdicts came from prompts ("list your Team tools", "call TeamCreate if available") that let the FO eyeball its visible/init list rather than run the hop — a confounded false-negative. Two re-probes on the auto-updated 2.1.179 that FORCE the explicit hop, reported verbatim:

1. **env-token path (banner `Sonnet 4.6 · Claude API`):** FO ran `ToolSearch(query="select:TeamCreate")` → "no match"; TeamCreate skipped (schema unresolved).
2. **stored-login path (banner `Sonnet 4.6 · Claude Team · Recce` — team mode CONFIRMED engaged, the same banner that worked on 2.1.177):** FO ran the actual hop → "no match. The tool returned: No matching deferred tools found. TeamCreate does not exist in the available deferred tool registry."

Probe #2 is decisive: the FO did the REAL ToolSearch hop (not an init eyeball), and `select:TeamCreate` returns no-match on 2.1.179. Contrast: on 2.1.177 (before the mid-session auto-update) my natural-language drives successfully created `teams/scrubprobe3/config.json`, and the FO independently confirms its live 2.1.177 session resolves TeamCreate via the hop. So the deferred-hop false-negative class is RULED OUT — the 2.1.179 team-tool absence is a genuine regression, not a measurement artifact. The `teams/session-<id>/config.json` files that appear are session-team auto-scaffolding, not TeamCreate results.

### Stage Report addendum (cycle 2, GROUND-TRUTH BYTES): the "Claude Team" banner is the SUBSCRIPTION org name, NOT engaged team mode; 2.1.179 interactive genuinely lacks the team tools (all three confound classes ruled out)

The team-lead flagged an internal contradiction in the prior addendum: "banner team mode engaged" + "TeamCreate no-match" cannot both hold. Resolved at ground truth — the banner claim was WRONG. A bounded 2.1.179 interactive probe (stored-login, env-scrubbed, flag confirmed) captured the literal artifacts:

- **Flag (read back inside the pane):** `echo "FLAG=$CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS"` → `FLAG=1`.
- **ToolSearch tool_result blocks, lifted verbatim from the persisting transcript jsonl (not the FO's self-report):** `select:TeamCreate` → `No matching deferred tools found`; `select:SendMessage` → `[{"type":"tool_reference","tool_name":"SendMessage"}]` (search mechanism WORKS); keyword `team` → `SendMessage`, `TaskList`, two HubSpot tools (NO team-management tool under any name); `select:TeamCreate,TeamDelete` → `No matching deferred tools found`.
- **Direct-call surface:** the interactive transcript carries NO `tools[]` array entry (unlike `-p` stream-json), so a direct TeamCreate call was attempted — FO verbatim: `TEAMCREATE_UNAVAILABLE. There is no TeamCreate tool available to me by any means — it does not appear in my built-in tool list, was not returned by ToolSearch in the deferred registry, and I have no mechanism to call it directly.` No `directprobe` team on disk.
- **Banner reconciliation:** `Sonnet 4.6 · Claude Team · Recce` is the SUBSCRIPTION/org label, NOT an engaged agent team. The isolated `.claude.json` `oauthAccount` (on disk) carries `"organizationName":"Recce","organizationType":"claude_team","seatTier":"team_tier_1"`. There is NO `teams/Recce/` dir — the only team dir is `teams/session-<id>/config.json` (auto-scaffolding: one `team-lead` member, `backendType:"in-process"`, `leadSessionId` = the session id). So the banner shows the Claude Team PLAN; it never implied TeamCreate availability.
- **Not a plugin-registration miss:** TeamCreate is a NATIVE CC deferred tool (the spacedock skills only describe the ToolSearch hop; `grep` of `plugin/` shows no plugin-side team-tool registration), so the bare probes' missing `--plugin-dir` is irrelevant.

All three of the team-lead's confound classes are ruled out: (1) NOT the wrong-search-surface — `select:SendMessage` resolves; (2) NOT a renamed tool — keyword `team` finds none; (3) NOT a direct/non-deferred tool — direct call is `TEAMCREATE_UNAVAILABLE`. The 2.1.179 interactive session genuinely lacks the native team tools with the flag set. Since 2.1.177 (the entity's premise era, where my bare probes created teams) DID have them, this is a 2.1.178/179 regression that spread BEYOND #68721's documented HEADLESS scope into interactive sessions. The captain is correct that #68721 is headless-scoped; the interactive loss is a newer/broader regression. The (pin-2.1.177 + DISABLE_AUTOUPDATER) vs (defer) call stands for the captain.

### Feedback Cycles

**Captain decision (2026-06-16 session #4): DEFER m4 from v0.20.4 → carry to v0.20.5.** The read-cost DoD (the v0.20.4 cut criterion) is MET independently of m4; m4 is team-mode coverage, not part of it. Given the byte-verified upstream block (interactive 2.1.179 lacks the native team tools), the captain chose NOT to pin an old claude to green a test of a capability current users can't use, and NOT to merge a live-deferred AC into the cut. v0.20.4 cuts on the read-cost DoD alone; m4 carries forward.

**State on deferral:**
- **Harness is DONE and PROVEN** — the resolver first-entry-oracle fix (`c939aea8`, real F30-class bug, RED→GREEN `TestFORootResolvesWhenSelfIdentifying`) and the env-scrub fix (`2b4beaa0`, `unsetNestedSessionArgs` + `TestUnsetNestedSessionArgs`) are committed, offline-green (`go test ./...` 15/15, `go vet -tags live` clean), rebased clean on `origin/main`, and **force-pushed to #390** to preserve the work for v0.20.5.
- **AC-1/2/5/6:** offline-green. **AC-3 (roster) + AC-4 (bounded-teardown marker over the FO transcript):** live-OBSERVED on 2.1.177 (real teams created; the resolver→idle-gate→teardown-grade chain reads the now-persisting FO transcript end-to-end). The env-scrub fix is what makes the FO transcript persist (it was the inherited-nested-session-marker artifact, not a transport property).
- **The sole remaining block is the upstream platform:** native TeamCreate/TeamDelete are absent from interactive sessions on claude 2.1.178/179 (broader than #68721's headless scope). The live AC-3/AC-4 CI green is deferred until upstream restores the tools (then the team-mode assertions activate on the version users actually run) — or, at v0.20.5 close, via a capability-aware skip-not-fatal mirroring the AC-6 auth-skip.

**v0.20.5 pickup:** re-validate when team tools return (or add the capability-skip); then merge #390. CI stays on latest as the regression sensor that caught both this and the zero-discover flake. Sprint re-tag (0204 → the v0.20.5 sprint) is a coordination decision deferred to when that sprint forms.

**Production note (captain's call: note-only):** team mode (concurrent dispatch, comm-officer, standing teammates) is upstream-broken for any spacedock user on claude 2.1.178+ — they silently fall back to bare mode. Recorded here and for the session debrief; no separate task / GH issue filed per the captain.

## Stage Report: validation (cycle 2 — captain-directed AC-4 reframe + local-live attempt)

- DONE: Reframe the LIVE AC-4 oracle (`TestLivePtyEnsignCycleTeamTeardown`) to accept EITHER valid terminus — (a) clean dispatch-to-terminal (entity reached terminal/archived state) OR (b) the FO emitted `TERMINAL_TEARDOWN_BOUNDED` — and update the AC-4 text + the test comments to match. Keep the OFFLINE marker grade (`gradeTerminalTeardown` + `teardown_grade_test.go` + `teardown_grade_watcher_test.go`) INTACT and UNCHANGED.
  Reframed via `expectCondition(reachedTerminus, ...)` waiting for EITHER the on-disk entity reaching `status: done` + a stage-report OR an FO-authored marker; the marker path is still re-graded by the unchanged `gradeTerminalTeardown` so it stays a fully valid (FO-authored, not contract-Read) terminus; then the mode-invariant terminal end-state is asserted on disk (`verdict:` deliberately NOT asserted — non-deterministic in team finalize, owned by the verdict-omission task). Worktree commit `f8cfc57e` (one file, +77/-35). `git diff --name-only` confirms the three offline grader files are UNCHANGED. AC-4 entity text updated above.
- DONE: Confirm offline `go test ./...` green and `go vet -tags live ./internal/ensigncycle/` clean after the change.
  `go test ./...` exit 0 — every package `ok` (incl. the offline grade tests `TestTerminalTeardownGrade*`, the parity meta-test `TestSharedScenarioRunnerCoverage` under `-tags live`, and the harness unit tests `TestFirstOfficerPaneIndex`/`TestFORootResolves`/`TestFOSessionPinning`/`TestUnsetNestedSessionArgs`/`TestTranscriptReachedIdle`). `go vet -tags live ./internal/ensigncycle/` exit 0.
- FAILED: Verify BOTH live pty tests locally on this 2.1.177 box (AC-3 must PASS; the reframed AC-4 must PASS on the clean terminus).
  BLOCKED by an interactive-auth divergence, NOT by the reframe. Headless `claude -p` PONG pre-flight with the benchmark-token PASSED (returned `PONG`, no 401) — so the dispatch's pre-flight gate was satisfied. But the live test's INTERACTIVE tmux child 401s on every turn (`Please run /login · API Error: 401 Invalid authentication credentials`), so `TestLivePtyStandingResidencyInjectsCommOfficer` FAILED at 23.9s before any TeamCreate. Root cause proven by a decisive A/B probe under the harness's exact env (`env -u` nested markers, isolated `CLAUDE_CONFIG_DIR`, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, claude 2.1.177): with `CLAUDE_CODE_OAUTH_TOKEN`=benchmark-token the banner is `Sonnet 4.6 · Claude API` and the turn 401s (no transcript written); with a `.credentials.json` seeded from the macOS keychain (`Claude Code-credentials`, stored-login, `subscriptionType: max`) and NO env token the banner is `Sonnet 4.6 · Claude Max` and the turn AUTHENTICATES + persists a transcript. So interactive CC 2.1.177 does NOT honor `CLAUDE_CODE_OAUTH_TOKEN` the way headless `-p` does — it wants the stored-login credential. The harness's `isolatedClaudeEnv` supplies only the env token (the OAuth path), so the interactive child 401s. AC-4 could not be exercised locally for the same reason. NOT a reframe defect: the AC-4 logic is offline-proven and the live drive never reached a model turn. ESCALATED to the FO/captain for the auth-path decision — see addendum.

### Stage Report addendum (validation cycle 2): interactive auth uses stored-login, not `CLAUDE_CODE_OAUTH_TOKEN` — the local-live blocker (NOT the reframe)

The reframe is done and offline-green; the sole remaining block to the local-live green is an auth-path divergence the harness inherits from the headless suite (the entity's spike finding #4 + the cycle-2 "env-token path vs stored-login path" note, now isolated cleanly):

- **Headless `-p` honors `CLAUDE_CODE_OAUTH_TOKEN`** (the benchmark-token) — my PONG pre-flight proved the token VALUE is valid (no 401).
- **Interactive CC 2.1.177 does NOT** — the same token in the same isolated env 401s on every interactive turn; the banner says `Claude API` and no transcript is written. The interactive TUI authenticates from the stored-login credential (`.credentials.json` / keychain `claudeAiOauth`), which `isolatedClaudeEnv` does not provide.
- **Decisive A/B:** seeding the keychain stored-login as `.credentials.json` into the isolated config dir (and dropping the env token) flips the banner to `Claude Max` and the turn authenticates + persists a transcript.

This is a real machine-auth surface, not a hidden machine dependency I can paper over: the credential lives in CL's keychain. Options surfaced for the FO/captain (I did NOT pick one — it touches AC-6's shared auth tree and is outside this AC-4-only fix):
- (a) **Local green:** seed the interactive child's config dir with the stored-login credential on the OAuth path (a small `isolatedClaudeEnv`/seed change). This is the only path to a LOCAL live green on this box; it touches the AC-6 auth contract, so it needs the FO/captain's go.
- (b) **CI green:** the FO's planned `ANTHROPIC_API_KEY` CI lane — but whether interactive CC authenticates from `ANTHROPIC_API_KEY` (the third auth path) is UNVERIFIED; I have no API key locally to probe it. If interactive ignores `ANTHROPIC_API_KEY` too (as it ignores `CLAUDE_CODE_OAUTH_TOKEN`), the CI pty step would 401 the same way and the reframe's live green would not land there either. This is the load-bearing open question for the FO before counting on the CI lane.

### Summary

Executed the captain-directed targeted fix: reframed the LIVE AC-4 oracle (`TestLivePtyEnsignCycleTeamTeardown`) to accept EITHER valid terminus — a clean dispatch-to-terminal (entity at `status: done` + stage-report, the happy path) OR an FO-authored `TERMINAL_TEARDOWN_BOUNDED` marker (re-graded by the EXISTING unchanged `gradeTerminalTeardown`) — proving the load-bearing behavior (the dispatch DRIVES THE ENTITY TO A TERMINAL STATE), not the marker edge alone. Updated the AC-4 entity text + the test comments to match. The OFFLINE marker-specific grade is INTACT and UNCHANGED (verified by `git diff --name-only`); offline `go test ./...` is exit-0 green and `go vet -tags live` clean (worktree commit `f8cfc57e`, one file). The two LIVE pty tests could NOT be greened locally: the headless `-p` PONG pre-flight with the benchmark-token PASSED, but the INTERACTIVE tmux child 401s because CC 2.1.177 interactive auth uses the stored-login credential, not `CLAUDE_CODE_OAUTH_TOKEN` (proven by a clean A/B probe). This is an auth-path block, NOT a reframe defect, and it touches AC-6's shared auth tree — escalated to the FO/captain with the surgical local-green option (seed the stored-login credential) and the unverified-but-load-bearing CI question (does interactive auth from `ANTHROPIC_API_KEY`?). Did NOT modify the auth tree, did NOT push, did NOT terminalize/merge.

### Stage Report addendum (validation cycle 2, RESOLVED): stored-login seed applied (captain-authorized) — BOTH live pty tests GREEN on 2.1.177; reframed AC-4 passed on the clean dispatch-to-terminal terminus

The captain authorized option (a). Applied the stored-login seed and verified per the FO's order of operations (cheap auth check first, then the long tests). Both live oracles are now GREEN.

**The fix (worktree commit `31d22d25`, 2 files, +78):** `seedStoredLoginCredential(configDir)` in the pty driver's `launchAndSend` writes the operator's stored-login OAuth credential (macOS keychain `Claude Code-credentials`, whose `-w` output is already the `{"claudeAiOauth":{...}}` shape claude reads) to the per-run isolated config dir's `.credentials.json`. The non-obvious second half: with BOTH the seeded credential AND `CLAUDE_CODE_OAUTH_TOKEN` present, interactive claude PREFERS the env token (banner `Claude API`) and 401s — proven by a controlled probe — so on the seeded path the launch now also DROPS `CLAUDE_CODE_OAUTH_TOKEN` (`withoutEnvKey`) so the stored login is authoritative (banner flips to `Claude Max`, a real authenticated turn). Best-effort + operator-local: a host with no reachable stored login (CI Linux, no `security`/keychain) is a no-op, the env-token / `ANTHROPIC_API_KEY` path is unchanged, and AC-6's skip-not-fatal-without-auth (decided upstream in `isolatedClaudeEnv`) is intact. NB: this resolves the LOCAL block; the **CI** `ANTHROPIC_API_KEY` interactive-auth question (does interactive CC honor an API key?) is still UNVERIFIED — the CI lane should be watched on the next run, since the keychain seed is a no-op on the Linux runner.

**Cheap auth check (FIRST, per the FO):** under the harness's real `launchAndSend` the FO booted and the harness PINNED the FO session id — i.e. the FO conversation transcript IS written (only happens when auth succeeds), with ZERO `Invalid authentication`/`/login` in the stream. The env-token run died at 23.9s with a 401 and never pinned an FO id; the seeded run booted clean.

**Live AC oracles (claude 2.1.177, `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`):**
- **AC-3 — PASS (141.07s).** `comm-officer present in the live team roster across 1 team config(s)` (test log line). The oracle is the on-disk team `config.json` `members[]` parsed with the binary's own membership shape — independent of any transcript. The transcript's `deferred_tools_delta` confirms `TeamCreate`/`TeamDelete` are exposed (2.1.177 still has the native team tools; this is why the CI pin to 2.1.177 was the right call).
- **AC-4 (reframed) — PASS (233.03s) on the CLEAN dispatch-to-terminal terminus.** The FO ran TeamCreate (1 tool_use) → dispatched → terminalized; it did NOT emit the marker (FO-authored marker emission: false; TeamDelete: 0). The reframed `expectCondition` accepted the clean terminus and the on-disk assertions passed (entity located, `status: done` + stage-report + path-scoped commit; verdict NOT asserted). This is the decisive proof of the reframe: the OLD marker-only oracle would have TIMED OUT on this exact run (no marker), exactly the over-strict failure the captain's reframe fixes; the new oracle greens.

**Offline after the seed change:** `go test ./...` exit 0 (all packages ok), `go vet -tags live ./internal/ensigncycle/` clean, the live test binary compiles. The offline marker grade remains intact and unchanged.

**Net:** m4's full AC set is now demonstrated — AC-1/2/5/6 offline-green; AC-3 + reframed AC-4 live-GREEN on the team-tool-capable 2.1.177. Worktree commits: `f8cfc57e` (AC-4 reframe) + `31d22d25` (stored-login seed). Did NOT push the code branch and did NOT terminalize/merge — the FO coordinates the push + CI-deployment approval.

## Stage Report: implementation (cycle 3 — the 0.20.5 MERGED lane + legacy skip-gate)

- DONE: Merged-mode live lane authored: a headless `claude -p` FO (no tmux) on the local current Claude drives an ensign as a named background Agent with NO TeamCreate, and the test asserts the on-disk auto-team config.json member carries agentType "spacedock:ensign", reconcile (no --team-name) resolves the roster by leadSessionId, and teardown is per-name shutdown_request (no TeamDelete); completion-signal target is "team-lead". Run it live locally and confirm green.
  `TestLiveMergedTeamModeDispatch` (`merged_team_mode_live_test.go`, `//go:build live`) is GREEN live on local 2.1.181 (PASS 338s, commits `c593a536`+`e81c1628`). It reuses the headless `-p` launch wiring, `isolatedClaudeEnv`, the `streamWatcher`, `detectWrongRootBoot`, `locateEntity`, and the nested-marker scrub verbatim; offline `TestMergedEnsignDispatchShape` proves the parsers. **The auto-team `config.json` registry premise in the spec did NOT hold on a headless `-p` host** (escalated + adjudicated below) — re-pointed to the on-disk shape the merged host actually writes. Live-asserted GREEN: (#1) no TeamCreate — absent from the init-event tool surface AND zero TeamCreate/TeamDelete tool_use; (#2) `Agent(name set, run_in_background=true, subagent_type="spacedock:ensign", no team_name)`; (#3) the on-disk `projects/<cwd>/<session-id>/subagents/agent-*.meta.json` member carries `agentType:"spacedock:ensign"` + name + no team_name; (#4) `spacedock dispatch reconcile` (no --team-name, FO session id + isolated HOME/CONFIG_DIR) exits 0; (#6) the merged dispatch file pins `SendMessage(to="team-lead")` (not "main"). Kept terminus: the entity reached its archived terminal state.
- DONE: Legacy interactive pty tests SKIP (not FAIL) on a merged host where TeamCreate is absent — gate them on the TeamCreate capability probe so unpinning CI makes them skip cleanly rather than RED.
  `skipUnlessTeamCreateCapable` (`team_capability_live_test.go`) probes `claude --version` and applies `teamCreateCapable` (the harness-observable analog of the contract's `ToolSearch(select:TeamCreate)`-empty discriminator: native team tools present <2.1.178, gone ≥). Wired as the first action in both `TestLivePtyStandingResidencyInjectsCommOfficer` + `TestLivePtyEnsignCycleTeamTeardown`; verified LIVE on 2.1.181 → both SKIP (not RED), naming the version. Offline `TestTeamCreateCapable` proves the discriminator (12 cases incl. the floor 2.1.178, future majors, unparseable). Added the symmetric `skipUnlessMergedHost` so the MERGED lane SKIPs on a legacy host (<2.1.178) — exactly one team lane runs per pinned/unpinned claude (verified both directions: merged PASS on 2.1.181 + SKIP on a stub 2.1.177; legacy SKIP on 2.1.181). Commit `fa9aaa5a`.

### Summary

Authored the 0.20.5 MERGED-semantics live lane (`TestLiveMergedTeamModeDispatch`) — GREEN live on local 2.1.181 — and skip-gated the legacy interactive pty lane so CI unpinning makes it SKIP-not-RED. Per the "validate the riskiest mechanism FIRST" discipline, I drove a live headless `-p` merged FO probe BEFORE writing the test and surfaced a load-bearing contradiction with the m4-readiness merged-lane SPEC: a headless `-p` merged host does NOT write a `teams/session-<id>/config.json` auto-team registry (that is an INTERACTIVE-session artifact). Escalated to the team-lead with evidence + a recommendation, then (heads-up sent, non-blocking) implemented the recommended honest re-pointing: the on-disk member oracle reads the `subagents/agent-*.meta.json` Claude Code DOES write headless (same agentType/name/no-team_name shape); reconcile is asserted to EXIT 0 (it degrades to git-only since no on-disk roster lands — a non-empty roster is NOT asserted); the per-name `shutdown_request` members[]-prune is interactive-only and not asserted (the entity-reached-terminal terminus is the kept proof). Two symmetric `claude --version` capability gates ensure exactly one team lane runs per claude version. Offline `go test ./...` green (15 pkgs, 0 fail), `go vet -tags live` clean, the two offline discriminator tests green. CI: a dedicated headless `Run live merged team-mode lane` step added to `runtime-live-e2e.yml` (no tmux, `if: !cancelled()`), detail jsonl uploaded.

### Scoping decision (escalated → team-lead APPROVED 2026-06-18, corroborated by their own headless `-p` trials):
The spec's auto-team `config.json` registry (assertions #3/#4/#5 roster-on-disk) does NOT exist on a headless `-p` host — the merged member record is the `subagents/agent-*.meta.json`. The team-lead approved the honest re-pointing exactly as built: #1/#2/#6 as specified; #3 reads the subagents meta (agentType=spacedock:ensign + name + no team_name); #4 asserts reconcile exit 0 ONLY (no non-empty roster); #5 (teardown/members[]-prune) DROPPED for the headless lane. If the captain later wants the roster-on-disk proof + the per-name shutdown_request teardown, it belongs on an INTERACTIVE pty merged lane, not the headless `-p` lane — a separate follow-up (the legacy interactive lane currently can't auth on 2.1.181+, the deferred legacy-auth bug). The merged headless lane proves the dispatch SHAPE + member registration + reconcile contract + completion target — the 0.20.5 deliverable's load-bearing coverage.

### FLAGGED FINDING (NOT fixed here — out of m4 scope; team-lead tracks the contract correction separately):
`skills/first-officer/references/claude-fo-dispatch.md` (~line 7) claims: *"Claude Code writes the on-disk roster `~/.claude/teams/session-<id>/config.json` automatically, keyed by session id, recording each member's `agentType`."* On a headless `claude -p` merged host (verified 3× on local 2.1.181, with AND without `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`) **this is FALSE**: no `teams/` dir is created at all; the member record lands at `projects/<cwd>/<session-id>/subagents/agent-*.meta.json` instead. That `teams/session-<id>/config.json` is the INTERACTIVE / TeamCreate-era artifact (the shape 9243 + the legacy spike observed), not the headless reality. **Consequence:** the FO's event-loop reconcile sweep (claude-fo-dispatch.md ~line 161: `spacedock dispatch reconcile` with leadSessionId auto-discovery) is NON-FUNCTIONAL on a headless `-p` merged host — `LoadReconcileTeam` globs `~/.claude/teams/*/config.json`, finds nothing, and degrades to git-only (the lingering/superseded/un-advanced-pr roster classes are silently suppressed) on every headless merged FO session. The reconcile command itself is correct (exits 0, degrades safely); the contract prose's premise about where Claude Code persists the roster is what's wrong. This is a contract/skill-file correction (NOT code), left for the team-lead's tracked follow-up.

### Push / branch note:
The branch `spacedock-ensign/live-team-mode-terminal-harness` (commits `fa9aaa5a` legacy-skip, `c593a536` merged-lane, `e81c1628` CI+gate, `e3f137a9` scope-comment atop `96b09741`) needs a **force-push to update PR #390** — the FO/captain handles the push + PR (the branch diverged from origin: 15 local vs 11 remote, as expected after the re-rebase onto main #396).
