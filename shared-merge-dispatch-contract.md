---
title: Extract host-neutral merge + dispatch contract — codex/pi name no merge reference and break at terminalization
status: validation
source: 'captain (2026-06-15, this session) — the shared first-officer merge/dispatch ceremony is siloed in claude-named refs, so codex/pi FOs are MISSING required terminal contract and will likely break at terminalization. Verified — first-officer-shared-core.md:128-130 ONLY defers (the ceremony lives in the runtime merge reference named by the runtime adapter) with no host-neutral fallback, while codex-first-officer-runtime.md and pi-first-officer-runtime.md name NO merge reference and carry no mod-block / ship-local / archive / worktree-removal contract (grep clean). Correction to an earlier FO analysis — codex (send_input + mailbox) and pi (message_dm via pi-agent-teams) DO message, so the reuse/feedback/await machinery is shared too, not Claude-only. This is a correctness gap, not a token cleanup. Captain routed it to 0.20.3 (0203-fo-efficiency; v0.20.2 is latest), otherwise codex breaks without the contract.'
started: 2026-06-15T15:06:22Z
completed:
verdict:
score: 0.55
worktree: .worktrees/spacedock-ensign-shared-merge-dispatch-contract
issue:
id: 2yfsf01jf15fmts7xt7w71m2
sprint: 0203-fo-efficiency
mod-block: merge:pr-merge
pr: "#385"
---

The shared first-officer terminal-merge and dispatch ceremony is generic (pure `spacedock status` + `git`) but lives only in `claude-fo-merge.md` / `claude-fo-dispatch.md`. The boot-resident core defers to "the runtime's merge reference," but only Claude provides one. A codex or pi FO that reaches a terminal stage has no mod-block set->invoke->clear, no Ship-Local ceremony, no worktree-removal safety, and no archive sequence to follow. It improvises or breaks. Extract the host-neutral ceremony so every runtime has it; leave only the genuine host seam per file.

## Problem

`first-officer-shared-core.md:128-130` (`## Merge and Cleanup (deferred module)`) defers the entire terminal ceremony to a per-host merge reference and provides no host-neutral fallback. Only `claude-fo-merge.md` exists; `codex-first-officer-runtime.md` and `pi-first-officer-runtime.md` name no merge reference and contain no merge/mod-block/ship-local/archive/worktree-removal prose (verified by grep this session). So:
- A codex/pi FO reaching terminalization follows the shared-core pointer to a runtime merge reference that does not exist, and has no contract for the mod-block guard, the local-merge sequence, archival, or worktree-removal safety. This is the break.
- The same silo wastes tokens (the generic ceremony cannot be shared across hosts; the token-cleanup audit independently kept `claude-first-officer-runtime.md` as load-bearing precisely because it is the SOLE namer of `claude-fo-merge.md`), but the token cost is secondary to the correctness gap.

Dispatch has the same shape: the dispatch procedure, the event loop (PR/mod-block/next), worktree ownership, and the reuse principle are generic and duplicated in prose across the three runtime files. Codex and pi DO message (`send_input`/mailbox; `message_dm`), so the reuse/feedback-routing/await-completion contract is shared, not Claude-specific. The genuinely Claude-only residue is narrow: the Claude Code registry-desync recovery (#36806), the async teardown race + `TERMINAL_TEARDOWN_BOUNDED` marker, the reconcile sweep, and the context-budget probe.

## Spike result (gap-before, on the record)

RISKIEST claim — "codex AND pi actually lack a terminal-merge contract today" — confirmed by a structural reference-closure trace this session (account under sustained 429; a live codex drive is a noted follow-up, not the gate proof — a faithful trace is sufficient per the Proof policy). Evidence:

1. **The boot-resident core only defers, with no host-neutral fallback.** `first-officer-shared-core.md:128-130` (`## Merge and Cleanup (deferred module)`) and `:97-99` (`## Dispatch (deferred module)`) both say the machinery "lives in the runtime's merge/dispatch reference … The runtime adapter names the [load point/reference]." Neither carries the ceremony nor a fallback — they are pure pointers.
2. **Only the Claude adapter names a merge/dispatch reference.** `grep -rln claude-fo-merge .` and `claude-fo-dispatch` both return exactly ONE file: `claude-first-officer-runtime.md` (its `## Merge reference (load at terminalization)` :11-13 and `## Dispatch reference (load at first dispatch)` :5-9). No other file names either reference.
3. **codex and pi name NO merge reference and carry no ceremony prose.** `grep -niE "merge|mod-block|ship-local|archive|worktree remov|teardown"` over `codex-first-officer-runtime.md` + `pi-first-officer-runtime.md` returns only two codex hits, both unrelated to terminal merge: line 68 ("a `wait_agent` timeout … is not a teardown trigger") and line 108 (the Backstop: "a missed terminal teardown … stays missed" — it *names* the boundary step but carries zero ceremony). ZERO hits for `mod-block` / `ship-local` / `archive` / `worktree remov` in either file. pi has zero hits entirely.

So a codex/pi FO reaching its terminal stage follows the shared-core pointer to a host merge reference **that does not exist for its host**, and has no contract for the mod-block set→invoke→clear guard, the local-merge sequence, the post-merge sentinel, archival, or worktree-removal safety. The gap is real, not inferred. (A live codex terminal drive proving the break end-to-end is recorded as a follow-up for when the 429 clears; it is not required to gate this design.)

**Seam-boundary spike (where the genuine host residue sits).** Confirmed the Claude-only residue is narrow and already lives outside the moved ceremony, so the cut is clean:
- The terminal-teardown marker `TERMINAL_TEARDOWN_BOUNDED: …` and the `TeamDelete`-race / settle / attempt-cap machinery already live in `using-claude-team` SKILL.md `## Terminal Team Teardown` (:98-109). `claude-fo-merge.md:24` (step 10) only *narrates a pointer* into it. So the marker text never needs to enter a host-neutral file.
- `reconcile` and `context-budget` live under `internal/claudeteam/` (they read `~/.claude/teams/*/config.json`) — host-gated at the binary, genuinely Claude-only.
- `status --set` / `status --archive` / `dispatch trunk` / `dispatch build` are NOT host-gated — host-neutral CLI surfaces the ceremony already calls identically on every host.

## Proposed approach

Extract TWO host-neutral deferred cores and re-home the genuine host residue. Two cores, not one: merge loads at terminalization, dispatch loads at first dispatch — different lazy load points, so keeping them separate preserves the existing lazy-load economics (a boot that greets and stops reads neither; a session that dispatches but never terminalizes reads only the dispatch core). Collapsing them into one file would force the merge ceremony to load at first dispatch.

**New files (product scaffolding, `skills/first-officer/references/`):**
- `fo-merge-core.md` — the host-neutral terminal merge-and-cleanup ceremony.
- `fo-dispatch-core.md` — the host-neutral dispatch procedure, reuse contract, worktree ownership, and event-loop skeleton.

**Composition — the boot-resident core names the host-neutral cores directly; each adapter names only its seam reference.** The shared-core deferred-module pointers stop saying "lives in the runtime's reference" and instead say "the host-neutral ceremony lives in `fo-merge-core.md` / `fo-dispatch-core.md`; the runtime adapter names its host seam." Because the cores are host-agnostic, the boot-resident core can name them directly (they are loaded lazily at the same load points as today — naming ≠ loading). Each runtime adapter keeps a thin `## Merge seam` / `## Dispatch seam` section naming only its host residue. Net load order at terminalization: read `fo-merge-core.md` (the ceremony) + the adapter's merge-seam pointer (Claude → `using-claude-team` `## Terminal Team Teardown`; codex/pi → their teardown analogue).

### Merge: what moves to `fo-merge-core.md` (host-neutral) vs stays per-host (seam)

HOST-NEUTRAL (moves to `fo-merge-core.md`, verbatim — same `spacedock`/`git` steps):
- `## Merge and Cleanup` steps **1-9** (mod-block set-before-invoke, run hooks, detect completion via state delta, blocked-leaves-mod-block, standalone mod-block clear, default local merge, terminalize frontmatter, archive, worktree+local-branch removal).
- `### Ship-Local Ceremony` (all 7 steps — `dispatch trunk`, the post-merge `pr=local-merge:{sha}` sentinel, the set→invoke→clear mandate).
- `### Worktree removal safety` (the no-`--force` default, the audit-then-force ladder).
- `## Mod-Block Enforcement` (the whole section — the mechanism-level invariant in `status --set`/`--archive`, the empty-pr/empty-mod-block recovery, session-resume scan).

PER-HOST SEAM (stays in / moves to each adapter):
- **Merge-and-Cleanup step 10 (terminal agent teardown).** The host-neutral core states the boundary obligation generically — "at the terminal boundary, derive the entity's worker cohort and run the host's terminal-teardown ceremony (cooperative shutdown of the cohort, then the host's bounded team/worker teardown); teardown is mandatory whether merge ran locally or via a PR host." It names NO marker, NO settle interval, NO cap. Each adapter's merge-seam names the concrete teardown:
  - **Claude:** `using-claude-team` `## Terminal Team Teardown` (the `TeamDelete`-race, the ~2s settle, the ≈5 cap, the `TERMINAL_TEARDOWN_BOUNDED` marker). UNCHANGED — already there.
  - **codex:** no team registry → cohort cooperative-shutdown via the mailbox surface, no `TeamDelete`, no marker. **This is NEW authored prose, not a repoint** — codex has NO existing positive terminal-shutdown contract: its only teardown mention is the Backstop (`codex-first-officer-runtime.md:108`, "None… a missed terminal teardown stays missed until session end"), which states the *absence* of enforcement, not the obligation. The implementer must AUTHOR codex's merge-seam to state the cohort-cooperative-shutdown obligation positively (derive the entity's worker cohort, issue the mailbox shutdown, drop from session memory), then note the Backstop's "stays missed if skipped" as the residual risk. Do not present it as pointing at existing codex prose.
  - **pi:** `pi-subagents` → a completed child needs no shutdown (mark closed in FO memory); `pi-agent-teams` → the adapter's `member_shutdown`/`team_done` mapping. Unlike codex, pi DOES have existing prose to point at — `pi-first-officer-runtime.md` `## Shutdown` covers both substrates — so pi's seam is a repoint: it becomes the terminal-boundary entry point for that existing `## Shutdown` section.

### Dispatch: what moves to `fo-dispatch-core.md` (host-neutral) vs stays per-host (seam)

HOST-NEUTRAL (moves to `fo-dispatch-core.md`):
- The per-entity `## Dispatch` procedure (steps 1-9: read entity+stage def, build the ≤3 linchpin checklist, conflict check, resolve `dispatch_agent_id`, the dispatch frontmatter `--set`, the state-transition commit, worktree creation on first worktree-stage dispatch, dispatch via the runtime adapter, wait-for-result).
- `## Worker Resolution` (the `dispatch_agent_id` / `worker_key` split, `:`→`-`, worktree/branch naming) — pure string mechanics, identical on every host.
- `## Worktree Ownership` + `### Split-Root Worktree Contract` — about where entity state lives, not how the host messages.
- The `## Reuse and Fresh Dispatch` **contract** — the reuse *conditions* 1-4 (not-bare, no `fresh: true`, worktree-routing match, model-match) and the reuse/fresh decision, stated host-neutrally with condition 0 (budget probe) and the messaging mechanism (`SendMessage` advance vs `send_input` vs `subagent` re-dispatch) delegated to the adapter seam. The captain's correction is load-bearing here: codex (`send_input`) and pi (`message_dm`/`subagent`) DO message, so the reuse/feedback-routing/await contract is host-neutral; only the concrete handle call is the seam.
- `## Dispatch Adapter` — the `spacedock dispatch build`-mandatory rule, the write-inputs-to-files discipline, the parse-stdout-call-`Agent()`-verbatim flow, and the Break-Glass fallback are host-neutral (codex/pi runtimes ALREADY restate `dispatch build` — that restatement is the duplication this collapses). The host substitutes its spawn call (`Agent()` / `spawn_agent` / `subagent`) at one named point.
- The `## Event Loop` **skeleton** steps 1-4 (PR-pending check → mod-blocked check → `status --next` → idle), which are pure `spacedock status --where`/`--next` reads.

PER-HOST SEAM (stays in / moves to each adapter):
- **Claude:** `## Team Creation` (`using-claude-team`, TeamCreate-first sequencing), the no-pre-dispatch-probe registry-desync rule (#36806), `## Degraded Mode (spacedock seams)`, `## Context Budget and Dead Ensign Handling` (the probe that realizes reuse-condition-0), Event-Loop **step 0 reconcile sweep** (Class A-E) and `### Backstop (Claude)`, the spawn-call (`Agent()` + `spacedock dispatch build`), the `SendMessage` reuse-advance handle.
- **codex:** `spawn_agent`/`send_input`/`wait_agent`, the mailbox completion signal, "declares no budget probe" (reuse-condition-0 satisfied), no reconcile (`### Backstop (Codex)` already says so).
- **pi:** `subagent(...)` / `pi-agent-teams`, `context: "fresh"` default, the epoch-based stale-reuse guard, the live-harness isolation. (All already in `pi-first-officer-runtime.md`.)

**Net:** codex/pi GAIN the terminal-merge contract and the dispatch procedure they currently lack (by naming the new host-neutral cores); the generic ceremony is stated ONCE; the Claude-only residue (registry-desync, teardown-race+marker, reconcile, budget probe) becomes a visible, auditable per-host seam instead of being buried inside generic prose only Claude can reach.

## Out of scope

- **The Claude-only residue is NOT genericized.** The `TERMINAL_TEARDOWN_BOUNDED` marker, the `TeamDelete`-race/settle/cap, registry-desync #36806, the reconcile sweep, and the context-budget probe stay in Claude files (`using-claude-team`, the Claude adapter, `internal/claudeteam/`). This task re-homes the host-neutral ceremony; it does not touch the host residue's behavior.
- **No behavioral change to the ceremony itself.** Same `spacedock status --set`/`--archive`, `dispatch trunk`, `dispatch build`, `git worktree` steps, same order, same MUSTs. This is a re-home (move text + repoint references), not a redesign.
- **The per-line token cuts** belong to the parallel proposal `docs/dev/_proposals/fo-contract-token-cleanup-2026-06-15.md` (the trim). This task is the structural re-home; word-level reduction is that proposal's concern. (The re-home will *enable* dedup savings, but measuring/claiming them is out of scope here.)
- **No codex/pi teardown *mechanism* is built.** codex/pi gain the merge *contract* (they now name an existing ceremony). Their teardown seams describe the obligation against their existing shutdown surfaces (codex mailbox / pi `member_shutdown`); no new binary support or marker is added for them.

## Documentation impact

No user-visible behavior changes — no CLI output, banner, command surface, or docs-site content changes. The edits are entirely to internal `skills/first-officer/references/` scaffolding the FO loads at runtime; the captain-facing behavior (the same merge ceremony runs, the same gate flow) is byte-for-byte preserved on Claude and newly-available (not changed) on codex/pi. No doc diff against the docs site is required. (Per the ideation stage def's doc-diff rule: it applies when a task changes user-visible behavior; this one does not.)

## Acceptance criteria

**AC-1 — Reachability: the host-neutral merge and dispatch cores are reachable from every host (codex and pi, not only Claude) and carry the ceremony.**
Verified by: `internal/contractlint/boot_resident_closure_test.go::TestHostNeutralCoresResolveAndCarryCeremony` plus the existing `TestBootResidentDeferredLoadPointsResolve` closure walk. Reachability rides on the SINGLE core-naming in `first-officer-shared-core.md`'s deferred-module pointers — every host loads the boot-resident shared core, so a core it names there is reachable on every host; no per-adapter re-naming is required (the per-adapter restatement that the first cut added only created redundancy and was removed in the lean rework). The closure half: `TestBootResidentDeferredLoadPointsResolve` walks `bootResidentBodies` (now including the shared core + all three adapters) and `os.Stat`s every named `references/*.md` load-point, with the dangling-target control (`TestBootResidentDeferredLoadPointGuardFailsOnDanglingTarget`) proving the oracle can fail. The content half: `TestHostNeutralCoresResolveAndCarryCeremony` asserts the shared core NAMES each core at a load point AND each core resolves on disk AND carries its four ceremony anchors (so "reachable" means "reaches a real ceremony," not an empty file). The independent source is the reference graph + the filesystem + the anchor set. The **gap-before** half is on the record (Spike result: codex/pi reference walks dead-ended pre-extraction). The test lives in `internal/contractlint/`. Live codex terminal drive: noted follow-up (account 429), not required for this AC.

**AC-2 — REMOVED.** The first cut's single-source content-fingerprint test (`fo_core_single_source_test.go`) was a transient migration assurance — it verified THIS move happened (each moved block in a core XOR in an adapter), but guarded no durable invariant and was brittle to fingerprint drift. Per the lean rework (captain decision), it is dropped and the test file deleted. Single-source is now upheld structurally by the design itself: the shared core is the sole core-namer and the lean rework left no restated ceremony in any adapter (verified once at rework time via the AC-3b fragment audit, not as a standing gate).

**AC-3 — Behavior-preserving on Claude: the marker-in-file guard and the live-e2e stay green, and no MUST / qualifier is dropped from the moved text.**
Verified by: (a) three existing gates stay GREEN —
  - `internal/ensigncycle/teardown_marker_consistency_test.go::TestGradeMarkerMatchesContract` — the **path-level** guard: it hard-asserts the verbatim `TERMINAL_TEARDOWN_BOUNDED` marker is present in `claude-fo-merge.md` AND `using-claude-team/SKILL.md` by explicit path. THIS is the test that breaks if the step-10 marker is wrongly moved into a host-neutral core — it pins the marker to the FILE, where the watcher tests only grade a live stream. It is the single most direct guard on the riskiest regression (the marker must STAY in the Claude file), so it is named first.
  - the `internal/ensigncycle` teardown-grade watcher tests (`teardown_grade_watcher_test.go:13` / `streamwatch_test.go:57`) — assert the marker byte-exact in a live FO stream, catching drift in the moved step-10 narration's runtime behavior.
  - the existing `internal/ensigncycle` live-e2e cycle (`TestLiveEnsignCycle`) stays GREEN on a real Claude drive — the load-bearing end-to-end behavior gate.
(b) a **word-level no-MUST-dropped audit** of the moved blocks: diff the relocated text against the pre-move `claude-fo-merge.md` / `claude-fo-dispatch.md` and assert zero MUST / refusal / qualifier deletions (same audit method 7e used at `headless-dispatch-mode-intent.md:260` — "contract diff vs main drops NO MUST/qualifier"). User-visible behavior: NONE changes → no doc-diff required (see Documentation impact).

**Token budget — accepted intrinsic overage.** The lean rework targeted the pre-2y Claude lifecycle baseline (9779 words = shared-core 3899 + claude-runtime 795 + claude-fo-dispatch 3647 + claude-fo-merge 1438). The final Claude lifecycle load is 10403 (shared-core 4037 + claude-runtime 785 + fo-dispatch-core 2263 + claude-fo-dispatch 1734 + fo-merge-core 1138 + claude-fo-merge 446) — **+624 over baseline**, accepted by captain decision as INTRINSIC core/adapter split-overhead, NOT duplication. (Of the overage, +125 is the headless-gate-stop MUST added in this cycle — a deliberate boot-resident contract strengthening, see the headless stage report below; the structural-split residual is +499.) Proven section-by-section: the moved host-neutral sections are faithful (Dispatch 500→503, Reuse 473→492, Worktree-Ownership 146=146) and the Claude residue is faithful to pre-2y (Team-Creation 260=260, Context-Budget 334→323). The residual is the one-time cost of splitting one file into core+adapter: two new file headers, the connective core↔adapter pointer sentences, and the Dispatch-Adapter contract necessarily stated as an abstract MUST-list in the core AND a concrete `Agent()`/break-glass block in the Claude adapter — and it sits mostly in DEFERRED loads (the cores load at first-dispatch / terminalization, not the boot-resident path). On inspection, the Claude `id_style`/boot-json paragraph is NOT pure duplication — its `--boot --json` flat-key list (`command`/`next_id`/`min_prefix`/…, every value a string) is faithful Claude-specific prose the shared core does not fully restate — so per the captain's "leave faithful residue" directive it stays. The broad Claude-verbosity trim (Context-Budget / reconcile prose) is deferred to the parallel token-cleanup proposal `docs/dev/_proposals/fo-contract-token-cleanup-2026-06-15.md`; 2y stays the structural extraction. codex/pi GAIN the contract (codex 886, pi 936) — that growth is the correctness fix.

## Test plan

**Cost/complexity:** moderate-high — this is a boot-resident contract touching all three runtime adapters plus two new deferred cores. The risk is behavior drift in the moved Claude text (caught by AC-3a's existing live-e2e). A restated copy left in an adapter is no longer structurally tested — AC-2 was dropped as transient migration-theater (captain decision); a restatement is a redundancy, not a behavior break, so it is left to the AC-3b word-audit + review rather than a dedicated guard. The gap-proof spike (AC-1 before-half) is already done structurally this session.

1. **AC-1 reachability (`internal/contractlint/boot_resident_closure_test.go`, fixture-level — no live drive).** `TestHostNeutralCoresResolveAndCarryCeremony` asserts the shared core names each core + each core resolves on disk + carries its four ceremony anchors; `TestBootResidentDeferredLoadPointsResolve` walks `bootResidentBodies` (shared core + all three adapters) and `os.Stat`s every named reference, with the dangling-target control. Reachability for codex/pi rides on the shared core's single core-naming, not a per-adapter restatement.
2. **AC-2 — REMOVED.** The transient single-source fingerprint test was dropped in the lean rework (captain decision); single-source is upheld by the sole-core-namer design, verified once at rework time via the AC-3b fragment audit.
3. **AC-3a marker-in-file + watcher + live-e2e (all existing, re-run).** First `internal/ensigncycle/teardown_marker_consistency_test.go::TestGradeMarkerMatchesContract` (path-level: marker present in `claude-fo-merge.md` + `using-claude-team/SKILL.md` — the guard that fails if the marker is wrongly moved); then the teardown-grade watcher tests; then `TestLiveEnsignCycle` on a real Claude drive. The marker-in-file test is fixture-level (fast, runs in CI without a live drive) and is the cheapest direct guard on the riskiest regression; the live-e2e is the one real-drive cost and it already exists.
4. **AC-3b word-level audit (manual, recorded in the implementation stage report).** `git worktree add --detach` the pre-extraction tree, diff moved blocks, assert no MUST/qualifier dropped; record the net word delta (expected: a reduction from dedup, but the trim is the parallel proposal's claim — here we only assert *no loss*).

**Fixture vs CLI vs live:** AC-1, AC-2, and AC-3a's marker-in-file/watcher tests are fixture-level Go tests in `internal/contractlint/` (AC-1/AC-2) and `internal/ensigncycle/` (the marker tests) — fast, deterministic, the structural + path-level gates. AC-3a's `TestLiveEnsignCycle` is the single existing live test (real Claude, no new live infra). No new CLI surface, so no golden-fixture CLI test. A live codex terminal drive proving the end-to-end merge under the new ref is a **noted follow-up** (account 429) — not in this task's gate.

**Rebase ordering (coordination).** 7e (`headless-dispatch-mode-intent`, id `7ea4…`) merges to main FIRST — it is at `validation` and validated-clean. 7e touched `first-officer-shared-core.md`'s Single-Entity Scope + step 9 and the Claude-adapter exception; it did NOT touch `claude-fo-merge.md`/`claude-fo-dispatch.md`, the deferred-module pointers (:97-99, :128-130), or the adapter load-point sections — confirmed disjoint from 2y's edit set (grep over `headless-dispatch-mode-intent.md` returns no `claude-fo-merge`/`claude-fo-dispatch`/`deferred module`/`Merge and Cleanup` hit). So 2y branches off post-7e main; no conflict expected. Note: 7e's validation also confirmed `claude-fo-merge.md:24`'s `TERMINAL_TEARDOWN_BOUNDED` marker is byte-identical to `teardown_grade_watcher_test.go:13` — the extraction MUST keep that line byte-identical (it is the step-10 teardown seam that STAYS in the Claude file, not moved). The path-level guard for this is `internal/ensigncycle/teardown_marker_consistency_test.go::TestGradeMarkerMatchesContract`, which hard-asserts the verbatim marker in `claude-fo-merge.md` AND `using-claude-team/SKILL.md` by explicit path — it RED-fails the instant the marker is moved into a host-neutral core, so it is the citable guard the implementer runs to prove the marker stayed put.

**Complexity assessment for staff review.** This is complex by the ideation stage definition's own examples (skill integration + a boot-resident contract spanning all three runtimes). Recommend an independent staff review before the gate: the review should check (a) the one-core-vs-two call and the boot-resident-names-the-core composition, (b) that the seam boundary correctly leaves the genuine Claude residue per-host (especially that step-10's marker stays in the Claude file), (c) AC-1/AC-2 bind to independent sources and are not tautological prose-greps, and (d) the AC-3a live-e2e is the right behavior gate. The riskiest unverified mechanism (does codex/pi actually lack the contract) was exercised structurally first and recorded in the Spike result above.

## Stage Report: ideation

- DONE: RISKIEST SPIKE FIRST (AC-1 seed): confirm codex AND pi ACTUALLY lack a terminal-merge contract today — structurally trace that shared-core defers with no host-neutral fallback, codex/pi name no merge reference and carry no ceremony prose, and the ceremony exists only in claude-fo-merge.md/claude-fo-dispatch.md.
  Reference-closure trace recorded in `## Spike result`: `grep -rln claude-fo-merge/dispatch` → only `claude-first-officer-runtime.md`; `grep -niE "merge|mod-block|ship-local|archive|worktree remov|teardown"` over codex/pi runtimes → 2 codex hits both unrelated to terminal merge, 0 in pi; shared-core :97-99/:128-130 are pure pointers. Gap confirmed on the record. Live codex drive deferred (account 429) as a noted follow-up.
- DONE: Pin the extraction design with before/after structure: which sections are HOST-NEUTRAL vs the genuine CLAUDE-ONLY seams; whether dispatch+merge are ONE core or TWO; and exactly how shared-core / each runtime adapter names + composes host-neutral-core + per-host seam.
  `## Proposed approach`: TWO cores (`fo-merge-core.md` + `fo-dispatch-core.md` — distinct lazy load points). Boot-resident core names the cores directly; each adapter keeps a thin merge/dispatch SEAM. Per-section move/stay tables for both merge (steps 1-9 + Ship-Local + worktree-safety + Mod-Block Enforcement move; step-10 teardown marker stays per-host) and dispatch (procedure/reuse-contract/worker-resolution/event-loop-skeleton move; team-lifecycle/reconcile/budget-probe/registry-desync stay Claude). Seam-boundary spike confirmed the marker + reconcile + context-budget already sit outside the moved ceremony.
- DONE: Behavioral ACs (NO prose-grep): AC-1 codex/pi gain a NAMED existing terminal-merge contract (reference-closure); AC-2 generic ceremony stated ONCE with no restated per-host copy (single-source structural check); AC-3 behavior-preserving for claude (live-e2e green + word-level no-MUST-dropped audit). State whether user-visible behavior changes.
  `## Acceptance criteria` AC-1 = reference-graph closure walk (Go test, binds to file existence + graph); AC-2 = content-fingerprint single-source (block in core XOR adapter, not a tautological grep); AC-3 = existing `internal/ensigncycle` live-e2e + byte-exact `TERMINAL_TEARDOWN_BOUNDED` watcher assertion + word-level MUST-audit. `## Documentation impact`: NO user-visible change → no doc-diff required.

### Summary

Designed the extraction of two host-neutral deferred cores (`fo-merge-core.md`, `fo-dispatch-core.md`) named directly by the boot-resident core, with each runtime adapter reduced to a thin host seam. Confirmed the gap structurally first (codex/pi merge-reference walks dead-end today; on the record) and pinned the seam boundary precisely — the genuine Claude-only residue (TERMINAL_TEARDOWN_BOUNDED marker, reconcile, context-budget, registry-desync #36806) already lives outside the moved ceremony, so the cut is clean and the step-10 marker line stays byte-identical to its Go-test assertion. ACs bind to independent sources (reference graph, content-fingerprint, existing live-e2e) — no prose-grep. Assessed COMPLEX (boot-resident contract across all three runtimes); recommended an independent staff review before the gate, with the four review focal points named in the test plan.

## Stage Report: ideation (cycle 2)

Staff review returned NEEDS-REVISION-BEFORE-GATE: core design SOUND (two cores, marker-stays-per-host, independent-source ACs, gap re-confirmed) — six fixes, no redesign. All six applied; each cited test/infra independently verified to exist with the named behavior before citing.

- DONE: M1 — name the path-level marker guard in AC-3a + the rebase note.
  `internal/ensigncycle/teardown_marker_consistency_test.go::TestGradeMarkerMatchesContract` (verified: asserts the verbatim marker in `claude-fo-merge.md`:34 AND `using-claude-team/SKILL.md`:35 by explicit path) added as AC-3a's first-named gate and to the rebase-ordering note — it is the test that RED-fails if step-10's marker moves to a host-neutral core.
- DONE: M2 — pin AC-1/AC-2 Go tests to `internal/contractlint/`.
  Verified `boundary_guard_test.go::TestNoInstructionReadsOutsideQuarantine` bans instruction-file reads outside `quarantinePkg = "internal/contractlint"`; both ACs now state the placement requirement with the boundary-guard rationale.
- DONE: M3 — AC-1 EXTENDS existing infra, not a net-new parser.
  Verified `boot_resident_closure_test.go` already walks `bootResidentBodies` (currently `first-officer-shared-core.md` + `claude-first-officer-runtime.md` only), extracts `references/*.md` via `bodyReferenceRe`, `os.Stat`s them, with a dangling-target control. AC-1 + test-plan step 1 rewritten to "add codex/pi adapters to the walked set," not "new graph parser."
- DONE: P1 — delete the duplicate `## Out of scope` template stub.
  Removed the `{Ideation pins…}` remnant; exactly one `## Out of scope` remains (grep-confirmed count = 1).
- DONE: P2 — frame AC-1's anchor check as structural-existence-of-the-resolved-file.
  AC-1 now owns "does the host's named merge reference `os.Stat` clean"; the four-anchor content work is explicitly delegated to AC-2's block-in-core-XOR-adapter, keeping AC-1 on the file/graph side of the quarantine line (no present-here heading grep).
- DONE: P3 — codex teardown seam is NEW authored prose, not a repoint.
  Move/stay table now flags codex's merge-seam as authored prose (its only teardown mention is the Backstop's "stays missed", which states absence, not obligation); pi remains a repoint at its existing `## Shutdown`.

### Summary

Landed all six staff-review fixes against the entity body, leaving the reviewed-sound core (two cores, per-host seams, marker-stays-Claude) untouched. The three material fixes re-anchored the ACs to existing test infrastructure: AC-3a now names the path-level marker guard `TestGradeMarkerMatchesContract`, AC-1/AC-2 are pinned to `internal/contractlint/` (the only package the boundary guard permits to read instruction files), and AC-1 is framed as extending `boot_resident_closure_test.go`'s existing closure walk to codex/pi rather than reinventing it. Verified each cited test exists with the claimed behavior before citing it. Ready for the gate.

## Stage Report: implementation

- DONE: Extract the host-neutral ceremony/procedure into NEW fo-merge-core.md + fo-dispatch-core.md VERBATIM (no MUST/qualifier/refusal dropped); reduce each runtime adapter (claude/codex/pi) to a thin seam naming only its host residue; the boot-resident core's deferred-module pointers name the two cores.
  `skills/first-officer/references/fo-merge-core.md` (Merge-and-Cleanup 1-9, Ship-Local, worktree-removal safety, Mod-Block Enforcement) + `fo-dispatch-core.md` (dispatch procedure, reuse contract, worker resolution, worktree ownership, dispatch adapter, event-loop skeleton); shared-core `## Dispatch`/`## Merge and Cleanup` pointers now name the cores; claude/codex/pi adapters each carry a thin merge+dispatch seam. Commit `ff34a8b1`.
- DONE: CRITICAL: the TERMINAL_TEARDOWN_BOUNDED marker line STAYS byte-identical in claude-fo-merge.md ... do NOT move it into a core.
  Step-10 teardown line kept verbatim in `claude-fo-merge.md` — `shasum` of the line = `a59f79db559701e99160417aa0e98748c8859c69`, identical to the pre-move line 24. `TestGradeMarkerMatchesContract` GREEN (asserts the marker in claude-fo-merge.md + using-claude-team/SKILL.md by path); the ensigncycle teardown-grade watchers GREEN.
- DONE: codex needs a NEW authored teardown seam (it has none today); pi repoints to its existing ## Shutdown.
  codex `### Terminal Teardown (Codex — Merge-and-Cleanup step 10)` authored positively (derive cohort, mailbox cooperative-shutdown, drop from memory; no TeamDelete/marker; Backstop "stays missed" noted as residual risk). pi `## Merge seam` points step 10 at its existing `## Shutdown`.
- DONE: Write the AC-1 + AC-2 tests in internal/contractlint/ ONLY. AC-1: EXTEND the existing boot_resident_closure_test.go walk to codex + pi and assert the resolved merge/dispatch reference EXISTS. AC-2: single-source content-fingerprint — each moved ceremony block lives in a core XOR in any adapter. TDD: write AC-1 first, watch codex/pi dead-end RED, do the extraction, watch GREEN.
  AC-1 `TestEachHostNamesResolvingMergeAndDispatchReference` extends `bootResidentBodies` to codex/pi + asserts each host names a resolving merge+dispatch ref: RED for codex/pi before extraction (4 dead-end failures captured), GREEN after. AC-2 `TestMovedCeremonyBlocksAreSingleSourced` (new `fo_core_single_source_test.go`) fingerprints 7 moved blocks (4 merge, 3 dispatch), each present-in-core + absent-from-5-adapters; proven RED on a planted restatement in codex, GREEN after revert. Both in `internal/contractlint/` (boundary-guard quarantine); each ships a control test.
- DONE: Behavior-preserving: go test ./... + go vet -tags live GREEN, including teardown_marker_consistency_test, boot_resident_closure_test (now extended), and boundary_guard.
  `go test ./...` GREEN (all 14 packages); `go vet -tags live ./...` exit 0. `TestNoUnexpectedModHookOrPRMergeIntroduced` needed `fo-merge-core.md` added to its `## Hook:` allowlist (the moved Mod-Block Enforcement prose names `## Hook: merge` — same allowlist update the prior contract-split made for claude-fo-merge.md). `TestGradeMarkerMatchesContract`, the teardown-grade watchers, `TestNoInstructionReadsOutsideQuarantine`, and the extended `boot_resident_closure_test` all GREEN.
- DONE: AC-3b: word-level no-MUST/qualifier-dropped audit of the moved blocks recorded in the stage report.
  Throwaway `git worktree add --detach` at branch base `7999672c`; diffed moved blocks. Token-count audit: zero categories dropped (MUST 11->12, MUST NOT 2->2, NEVER 3->3, Do NOT 8->9, refuse 7->7, REQUIRED 5->5, mandatory/Mandatory preserved). Fragment-level audit: 35 distinctive MUST/refusal/qualifier fragments checked, **0 dropped** from the post-move set. Net word delta across the 8-file FO contract surface: **+1475** (11435 -> 12910) — a positive delta, expected: this is a structural re-home that ADDS six seam-pointer sections + the codex authored teardown seam; the per-line trim is the OUT-of-scope parallel proposal. AC-3b gates on no-MUST-loss (satisfied), not on a reduction.
- SKIPPED: The AC-3a live-e2e gates at CI (account 429 locally) — do not block on a local live run.
  Account under sustained 429; no local live drive attempted per the dispatch. AC-3a (`TestLiveEnsignCycle` + the byte-exact marker watcher) gates at CI on 2y's PR (both-model + codex + pi lanes). The marker-byte-identical + watcher fixture tests (the cheap direct guards on the riskiest regression) ARE green locally.

### Summary

Extracted the generic FO terminal-merge and dispatch ceremony into two host-neutral deferred cores (`fo-merge-core.md`, `fo-dispatch-core.md`) named directly by the boot-resident core, reducing each runtime adapter to a thin host seam. codex/pi now name a resolving merge+dispatch reference (AC-1 RED->GREEN), the ceremony is single-sourced (AC-2, proven non-vacuous), and the step-10 `TERMINAL_TEARDOWN_BOUNDED` marker stayed byte-identical in claude-fo-merge.md. The AC-3b word-level audit found zero MUST/qualifier/refusal deletions. Offline `go test ./...` + `go vet -tags live` are green; AC-3a's live-e2e gates at CI (account 429). NOTE for the FO: origin/main has advanced past my base to `69bcd93f` (#382 renames reconcile drift classes A-E -> descriptive names in claude-fo-dispatch.md) — that file is in my edit set, so the pre-PR rebase onto post-7e/post-#382 main will NOT be conflict-free; my Claude reconcile seam still carries the old A/B/C/D/E class letters and must be reconciled with #382 during rebase.

## Stage Report: implementation (rebase addendum)

- DONE: Rebase onto current origin/main (f2b6f3dc — includes 7e #381 AND #382 69bcd93f) and reconcile the #382 collision.
  Rebased; new branch HEAD `84d5c5da`, merge-base now `f2b6f3dc`. ONE expected conflict in `claude-fo-dispatch.md` (the Claude reconcile SEAM I kept); the two other touched files (`claude-first-officer-runtime.md`, `first-officer-shared-core.md`) auto-merged clean — 7e's edits (the headless-vs-interactive dispatch-mode step, the launcher-invariant) are disjoint from mine and both sides coexist (verified: my core-naming pointers + 7e's step 9 both present).
- DONE: #382 reconcile-rename resolution (how I merged the rename into my seam).
  #382 is a pure class-name rename, NO logic change (confirmed by diff). Git auto-took #382's renamed bullets (lingering/superseded/un-advanced-pr/stale-branch/local-main-drift) + the renamed Backstop; the conflict region was only the summary line + event-loop steps 1-4. Resolved by KEEPING BOTH sides: adopted #382's descriptive-named summary line (`reconcile: {N} entries: lingering={N} superseded={N} un-advanced-pr={N} stale-branch={N} local-main-drift={N} — acting`) AND preserved my seam restructure (steps 1-4 stay MOVED to `fo-dispatch-core.md`, not restated in the Claude seam). Verified: zero `Class A`/`A={N_A}`/`class":"A|B` residue in claude-fo-dispatch.md; steps 1-4 absent from the seam and present (×4) in the core.
- DONE: Re-verify GREEN post-rebase + marker byte-identical.
  `go test ./...` GREEN (all packages); `go vet -tags live ./...` exit 0. AC gates GREEN: AC-1 `TestEachHostNamesResolvingMergeAndDispatchReference` + extended `boot_resident_closure`, AC-2 `TestMovedCeremonyBlocksAreSingleSourced` + control, `boundary_guard`, `TestNoUnexpectedModHookOrPRMergeIntroduced`, `TestGradeMarkerMatchesContract`, all 5 teardown-grade watchers. TERMINAL_TEARDOWN_BOUNDED step-10 line still byte-identical — `shasum` = `a59f79db559701e99160417aa0e98748c8859c69` (unchanged; the conflict never touched claude-fo-merge.md).
- DONE: Push the rebased branch.
  `git push -u origin spacedock-ensign/shared-merge-dispatch-contract` (branch was not previously on origin — only the state branch was; no force needed). HEAD `84d5c5da` on origin.

### Summary

Rebased onto post-7e/post-#382 main (f2b6f3dc); resolved the single expected `claude-fo-dispatch.md` conflict by adopting #382's descriptive reconcile-class rename into the Claude reconcile seam while preserving the seam restructure — both sides kept, neither dropped, and #382 confirmed a pure rename (no logic change). Offline `go test ./...` + `go vet -tags live` + all AC/marker gates GREEN post-rebase; the TERMINAL_TEARDOWN_BOUNDED marker stayed byte-identical (a59f79db). Branch pushed at HEAD 84d5c5da, merge-ready for validation.

## Stage Report: validation

- DONE: Independent AC-1 + AC-2 verification (reproduce, don't trust) — contractlint suite green on rebased branch (84d5c5da, merge-base f2b6f3dc = current origin/main).
  AC-1 `TestEachHostNamesResolvingMergeAndDispatchReference` + extended `boot_resident_closure` (`bootResidentBodies`/`hostRuntimeAdapters` now include codex+pi) GREEN; AC-2 `TestMovedCeremonyBlocksAreSingleSourced` + control GREEN; `boundary_guard` `TestNoInstructionReadsOutsideQuarantine`, `TestNoUnexpectedModHookOrPRMergeIntroduced`, `TestGradeMarkerMatchesContract`, all 5 ensigncycle teardown-grade watchers GREEN.
- DONE: AC-1 RED-before is REAL (not asserted) — codex/pi merge+dispatch walks genuinely dead-ended pre-extraction.
  Detached worktree at f2b6f3dc (cores absent), copied ONLY the two new test files onto it: AC-1 produced 4 dead-end failures (codex names no merge ref, no dispatch ref; pi same); AC-2 RED ("read fo-merge-core.md: no such file"). GREEN after extraction on the branch. Dangling-target control `TestBootResidentDeferredLoadPointGuardFailsOnDanglingTarget` passes → oracle can fail.
- DONE: Detached adversarial audit on a THROWAWAY checkout (never the worktree) — three refute-edits, each guard REDs as designed.
  (1) Moved the marker out of `claude-fo-merge.md` into `fo-merge-core.md` → `TestGradeMarkerMatchesContract` RED ("does not carry the marker verbatim"). (2) Restated AC-2 fingerprint[0] in `codex-first-officer-runtime.md` → AC-2 RED ("restated in host adapter codex-…"). (3) Repointed codex merge seam to `fo-merge-core-GONE.md` → both `TestBootResidentDeferredLoadPointsResolve` and AC-1 RED. All throwaways removed (`git worktree remove --force`).
- DONE: AC-3a marker byte-identity + step-10 stays per-host (NOT moved into a core).
  `claude-fo-merge.md` step-10 line shasum = `a59f79db559701e99160417aa0e98748c8859c69` (matches the recorded value, unchanged). `fo-merge-core.md` step 10 states the boundary obligation generically and explicitly disclaims the marker/settle/cap ("This core names none of them"); the literal `TERMINAL_TEARDOWN_BOUNDED` token is absent from both cores. codex seam = NEW authored positive obligation (cohort→mailbox shutdown→drop, Backstop "stays missed" as residual risk); pi seam = repoint to its existing `## Shutdown`.
- DONE: AC-3b word-level no-MUST/qualifier/refusal-dropped audit (manual, recorded here).
  Per-category counts (pre-move ceremony at f2b6f3dc vs the full 8-file post-move FO surface): ZERO categories dropped (every post count ≥ pre). Fragment-level: 69 distinctive obligation clauses checked; the 5 verbatim mismatches are ALL the intended `Agent()`→host-neutral "spawn call" abstraction — each obligation survives in `fo-dispatch-core.md` ("Do NOT assemble worker prompts manually", "forward its output to the spawn call verbatim", "`description` … REQUIRED — do not omit it") with the Claude `Agent()` form retained in `claude-fo-dispatch.md`. No MUST/refusal lost.
- DONE: #382 reconcile reconciliation — descriptive class names adopted without dropping the rename or the seam restructure.
  Zero `Class A`–`Class E` / `N_A`–`N_E` residue in `claude-fo-dispatch.md`; all five descriptive classes present (lingering/superseded/un-advanced-pr/stale-branch/local-main-drift), incl. the #382 summary line. Event-loop steps 1-4 MOVED to `fo-dispatch-core.md` `## Event Loop`; the Claude seam carries ONLY step-0 reconcile and points 1-4 at the core. Both #382's rename and 2y's restructure coexist.
- DONE: AC cross-check + offline go test/vet green.
  `go test ./...` GREEN (15 packages, incl. non-cached dispatch/status/release); `go vet -tags live ./...` exit 0. shared-core deferred pointers (`:94`, `:125`) name `fo-dispatch-core.md` / `fo-merge-core.md` directly; `claude-first-officer-runtime.md` remains the sole namer of the thin claude-fo-* seams. The `structural_checks_test.go` allowlist add (fo-merge-core.md → `## Hook:` allowlist) is the faithful move-consequence the prior contract-split already applied to claude-fo-merge.md, not a guard weakening.
- SKIPPED: AC-3a live behavior gate (`TestLiveEnsignCycle` + byte-exact marker watcher) — gated at CI, not run locally.
  Account under sustained 429 per the dispatch; no local live drive attempted. AC-3a gates at the 2y PR's runtime-live-e2e CI (both-model + codex + pi lanes); the rebased base carries 7e's `-timeout 40m` fix. The cheap direct guards on the riskiest regression (marker-byte-identity + the fixture watcher tests) ARE green locally.

### Summary

PASSED. Every AC reproduces independently on the rebased branch (84d5c5da): AC-1's extended closure walk is GREEN and its RED-before is genuine (codex+pi dead-ended 4× on a detached pre-extraction tree, not merely asserted); AC-2 single-source is GREEN and proven non-vacuous (a planted restatement REDs it); AC-3a's marker stayed byte-identical (a59f79db) in `claude-fo-merge.md` with the host residue correctly kept out of the cores, and AC-3b's word-level audit found zero MUST/qualifier/refusal deletions (the 5 verbatim mismatches are the intended `Agent()`→spawn-call abstraction, obligation-preserving). The detached adversarial audit on a throwaway checkout confirmed all three guards RED on claim-breaking edits (marker-into-core, ceremony-restated-in-adapter, seam-points-at-missing-ref). The #382 reconcile rename was adopted without dropping the rename or the seam restructure. Offline `go test ./...` (15 pkgs) + `go vet -tags live` are green. MERGE GATES on the 2y PR's runtime-live-e2e CI being green — AC-3a's `TestLiveEnsignCycle` is the authoritative behavior gate and was not runnable locally (account 429); the structural guards reproduce locally, the behavior gate is wired to the PR CI, so this is PASSED conditional on that CI lane going green before merge.

## Stage Report: implementation (rework)

Captain sent 2y back for a lean rework: the first cut PASSED its guards but GREW the contract (+1475 total, +~965 on the Claude FO) — the opposite of "condense." Root causes: (1) a duplicated core-naming preamble restated in all 3 adapters × 2 modules, (2) AC-2 as a transient migration assurance. The extraction itself was judged correct.

- DONE: Single core-naming; adapters carry ONLY host-specific parts; drop "seam"/"host-neutral version" jargon.
  Shared-core deferred-module pointers are the sole core-namer. Stripped the "host-neutral X lives in fo-X-core.md, read it at [load point], this file supplies the [host] specifics" preamble from claude/codex/pi (both modules); each adapter keeps only its host part (Agent()/SendMessage/teardown; spawn_agent/send_input; subagent/Shutdown). Replaced "seam"/"host-neutral version" with the contract's existing vocabulary (deferred module/reference, load point, runtime adapter). Commits aa8599c5, 617a674a.
- DONE: Reframe AC-1 to reachability-via-shared-core; DELETE AC-2.
  AC-1: new `TestHostNeutralCoresResolveAndCarryCeremony` — shared-core names each core + each core resolves on disk + carries its 4 ceremony anchors; reachability for codex/pi rides on the single shared-core naming, no per-adapter re-naming (removed `TestEachHostNamesResolvingMergeAndDispatchReference` + `mergeCoreRefRe`/`dispatchCoreRefRe`). AC-2 deleted (`fo_core_single_source_test.go` removed) — transient migration assurance. Entity-body `## Acceptance criteria` + test plan updated to match. Commit a3e7577c.
- DONE: CONTROL THE CLAUDE RUNTIME DELTA (≤9779 target) + report the breakdown.
  FINAL Claude lifecycle load = **10278** (shared-core 3912 + claude-runtime 785 + fo-dispatch-core 2263 + claude-fo-dispatch 1734 + fo-merge-core 1138 + claude-fo-merge 446). **+499 over the 9779 baseline** — captain-accepted (Option B) as INTRINSIC core/adapter split-overhead, NOT duplication (proven section-by-section: moved sections faithful — Dispatch 500→503, Reuse 473→492, Worktree-Ownership 146=146; Claude residue faithful — Team-Creation 260=260, Context-Budget 334→323). The residual is 2 file headers + connective core↔adapter pointers + the Dispatch-Adapter contract necessarily abstract-in-core AND concrete-in-Claude, mostly in DEFERRED loads (cores load at first-dispatch/terminalization, not boot). On the captain's "cut only genuine duplication" nuance: I inspected the Claude id_style/boot-json paragraph, found its `--boot --json` flat-key list is faithful Claude-specific prose the shared core does NOT fully restate, and per the "leave faithful residue" directive REVERTED my earlier dedup of it (commit 946b5569 reverts 617a674a) — so it stays. Broad Claude-verbosity trim deferred to the parallel token-cleanup proposal. codex 886, pi 936 (gain the contract — the correctness fix).
- DONE: Behavior-preserving — green + marker byte-identical + AC-3b no-MUST audit.
  `go test ./...` GREEN (all packages); `go vet -tags live ./...` exit 0; `TestHostNeutralCoresResolveAndCarryCeremony`, the extended `boot_resident_closure`, `boundary_guard`, `TestGradeMarkerMatchesContract`, and all 5 teardown-grade watchers PASS. TERMINAL_TEARDOWN_BOUNDED step-10 line byte-identical — shasum `a59f79db559701e99160417aa0e98748c8859c69`. AC-3b fragment audit on the reworked+deduped set: 0 of 35 MUST/refusal/qualifier fragments dropped; no token category regressed.

### Summary

Reworked the extraction lean: the shared-core deferred-module pointers are now the sole core-namer, every runtime adapter carries only its host-specific part, and the invented "seam" jargon is gone in favor of the contract's existing vocabulary. AC-1 reframed to reachability-via-shared-core and AC-2 (a transient migration assurance) deleted. The final Claude lifecycle load is 10278 — +499 over the 9779 baseline, captain-accepted (Option B) as intrinsic core/adapter split-overhead (proven section-by-section, mostly in deferred loads); I did NOT trim faithful Claude residue prose to chase a number inflated by the approved extraction, and after inspection reverted my earlier id_style dedup because that paragraph's `--boot --json` key list is faithful Claude-specific prose, not a verbatim restatement of the shared core. Behavior preserved: offline tests + vet green, the marker byte-identical (a59f79db), and the AC-3b audit confirms 0 MUSTs dropped. Branch HEAD 946b5569, merge-ready for fresh validation.

## Stage Report: validation (cycle 2)

- DONE: Independent AC-1 reachability + structure verification on the lean rework (branch HEAD 946b5569): reproduce the contractlint suite GREEN.
  `go test -count=1 ./internal/contractlint/` GREEN: `TestHostNeutralCoresResolveAndCarryCeremony` (shared core NAMES each core + each resolves + carries its 4 ceremony anchors), `TestBootResidentDeferredLoadPointsResolve` (walks shared core + all THREE adapters, `os.Stat`s every `references/*.md`), `TestBootResidentDeferredLoadPointGuardFailsOnDanglingTarget` (dangling control), `boundary_guard`, `TestNoUnexpectedModHookOrPRMergeIntroduced`. Read the test bodies — `bootResidentBodies` includes codex+pi (lines 23-24); reachability rides on the single shared-core naming, not per-adapter restatement.
- DONE: Confirm AC-2's test (`fo_core_single_source_test.go`) is REMOVED and no stale AC-2 reference in `## Acceptance criteria` / `## Test plan`.
  File absent on disk; `grep` for `fo_core_single_source` / `TestMovedCeremonyBlocksAreSingleSourced` / `mergeCoreRefRe` over `*.go` returns nothing. Live `## Acceptance criteria`:99 and `## Test plan`:115 both mark AC-2 **REMOVED**; the only fingerprint-test mentions are in prior (append-only) stage reports, correctly historical.
- DONE: Confirm the structure is genuinely LEAN: SINGLE core-naming in shared-core; NO duplicated "host-neutral X lives in fo-X-core.md, read at [load point]" preamble in any adapter.
  shared-core:94/:125 are the sole place naming `fo-dispatch-core.md`/`fo-merge-core.md` with load points. Each adapter's core mention is a thin host cross-reference ("this file is the Codex parts those defer to"; "the spawn call fo-dispatch-core.md defers to is `spawn_agent`"), not the removed preamble. One residual "seam" — `claude-fo-dispatch.md:53` `## Degraded Mode (spacedock seams)` — is plain-English (spacedock CLI integration points), not structural core/adapter jargon. Judged legitimate.
- DONE: Detached adversarial audit on a THROWAWAY checkout (never the worktree) — AC-3b word-audit + three refute edits.
  Detached worktrees at base f2b6f3dc (pre-extraction; claude-fo-merge.md=1438w, no cores) and head 946b5569. AC-3b: 0 of 9 categories dropped (every post count ≥ pre); 40 distinctive obligation clauses checked, the 8 non-verbatim are ALL split-artifacts or the intended `Agent()`→"spawn call" abstraction (obligation survives in `fo-dispatch-core.md`, concrete `Agent()` retained in `claude-fo-dispatch.md`) — 0 MUST/refusal/qualifier lost. Three refutes each RED as designed: (1) marker→core ⇒ `TestGradeMarkerMatchesContract` RED; (2) shared-core core-ref→missing file ⇒ `TestBootResidentDeferredLoadPointsResolve` AND `TestHostNeutralCoresResolveAndCarryCeremony` RED; (3) emptied a core anchor ⇒ `TestHostNeutralCoresResolveAndCarryCeremony` RED ("reaches an empty file"). All throwaways `git worktree remove --force`'d.
- DONE: AC-3a marker byte-identity + step-10 stays per-host (NOT moved into a core).
  `claude-fo-merge.md` step-10 line shasum = `a59f79db559701e99160417aa0e98748c8859c69` (matches recorded). `TERMINAL_TEARDOWN_BOUNDED` token absent from both cores; fo-merge-core.md:24 states the boundary obligation generically and disclaims the marker/settle/cap. `TestGradeMarkerMatchesContract` GREEN; all teardown-grade watchers GREEN.
- DONE: The +499 Claude-lifecycle total independently re-measured + confirmed intrinsic.
  Re-measured: 10278 (shared-core 3912 + claude-runtime 785 + fo-dispatch-core 2263 + claude-fo-dispatch 1734 + fo-merge-core 1138 + claude-fo-merge 446) = +499 over 9779 — matches the entity exactly. Section faithfulness reproduced (Dispatch 500→503, Reuse 468→492, Worktree-Ownership 146=146, Mod-Block 330=330, Context-Budget 334→323). Merge module split 1438→1138+446=1584 (+146 = 2 headers + connective pointers); the Merge-and-Cleanup section's −299 is step-10's teardown prose correctly STAYING in claude-fo-merge.md, not lost. Residual is intrinsic split-overhead, not hidden bloat. codex 886, pi 936 (the gained contract).
- DONE: AC cross-check + AC-3a CI gate + offline green.
  `go test ./...` GREEN (16 pkgs); `go vet -tags live ./...` exit 0 (no output). AC-3a wired: `.github/workflows/runtime-live-e2e.yml` triggers on `pull_request`, runs `TestLiveEnsignCycle`(+TeamTeardown) + `TestLiveClaudeSharedScenarios` (claude-live matrix sonnet+opus), `TestLiveCodexSharedScenarios` (codex-live), `TestLivePiFrontDoorSmoke`+coverage (pi-live), all `-tags live -timeout 40m` (7e's fix present); the byte-exact teardown-marker watcher rides in the live ensigncycle suite.
- SKIPPED: AC-3a live behavior gate run locally — account under sustained 429.
  No local live drive attempted per the dispatch. The cheap direct guards on the riskiest regression (marker byte-identity + the fixture watcher tests) ARE green locally; the live-e2e gates at the 2y PR's runtime-live-e2e CI.

### Summary

PASSED. The lean rework holds and nothing regressed. Independently reproduced on branch HEAD 946b5569 (merge-base f2b6f3dc = current origin/main, no rebase): the contractlint suite is GREEN with AC-1 reframed to reachability-via-single-shared-core-naming (verified in the test bodies, not just by a passing run), AC-2's test file is removed with no stale standing-gate reference in the live criteria/test plan, and the structure is genuinely lean — single core-naming, no duplicated adapter preamble, the one residual "seam" is plain-English CLI vocabulary. The detached adversarial audit on a throwaway checkout reproduced AC-3b (0 of 35-class obligations dropped; the 8 non-verbatim clauses are the intended Agent()→spawn-call abstraction, obligation-preserving) and proved all three guards RED on claim-breaking edits (marker-into-core, core-ref-to-missing-file, emptied-anchor). The step-10 marker is byte-identical (a59f79db) and absent from the cores; the +499 Claude-lifecycle overage re-measured to 10278 exactly and is genuinely intrinsic split-overhead (section-by-section faithful, the merge-section −299 is teardown prose correctly staying per-host). Offline `go test ./...` + `go vet -tags live` are green. MERGE GATES on the 2y PR's runtime-live-e2e CI being green — AC-3a's `TestLiveEnsignCycle` + the byte-exact marker watcher (both-model + codex + pi lanes) is the authoritative behavior gate and was not runnable locally (account 429); the structural guards reproduce locally, the behavior gate is wired to the PR CI.

## Stage Report: implementation (headless gate-stop strengthening)

2y's CI re-run surfaced a 7e-era contract gap (NOT a regression in the lean rework). Captain folded the fix into 2y.

- DONE: Strengthen Startup step-9 headless bullet to an unambiguous MUST.
  Root cause (from the `TestLiveDefaultHeadlessStopsAtGate` transcript): the headless `-p` FO correctly drove to the `review` (gate:true) stage and reported a stop reason, but step 9 only said "EXIT reporting each entity's stop reason" — so model variance authored the full present-gate review in 7e (passed) and a terser summary here (failed both models). `first-officer-shared-core.md` step-9 headless bullet now MUSTs: at each gate:true stop the FO authors the FULL present-gate review (`Skill(present-gate)`'s `Gate review:`/chosen-direction/checklist/`Decision:` template, per `## Completion and Gates`) BEFORE exiting; a terse stop-reason line is NOT sufficient. The FO still does NOT resolve the gate headless (only "given the conn" does). Rationale baked in: the human picks up the headless transcript and decides from the authored gate content. Commit a6d1f4f2.
- DONE: Confirm the gate is the test asserting the present-gate format.
  `internal/ensigncycle/live_gate_stop_test.go::foAuthoredGateReview` (`//go:build live`) scans the stream for an FO-authored assistant block carrying both `gate review:` AND `decision:` (case-insensitive) — exactly the present-gate format the strengthened MUST now requires. Left as the AC-a gate; it correctly enforces the strengthened contract.
- DONE: Keep the entire lean rework intact + re-measure.
  Cores, single core-naming, AC-1/AC-2 state, and the marker all unchanged. Claude lifecycle 10278 → 10403 (+125 for the MUST; +624 vs the 9779 baseline). TERMINAL_TEARDOWN_BOUNDED marker byte-identical — shasum a59f79db.
- DONE: go test ./... + go vet -tags live GREEN.
  Offline `go test ./...` all packages green (no FAIL); `go vet -tags live ./...` exit 0 (compiles the live-tagged gate-stop test clean). `TestStartupGateGuidanceHasSingleSource`, contractlint closure/marker tests all green. The LIVE behavior (FO reliably authors the review at a headless gate-stop on both models) re-runs at the PR CI — not runnable locally (account 429); the strong, unambiguous MUST is the reliability lever, the CI re-run is the proof.

### Summary

Strengthened the boot-resident contract so a headless `-p` FO stopping at a gate:true stage MUST author the full present-gate review (`Gate review:` … `Decision:`) before exiting, not just a terse stop-reason line — closing the 7e-era under-specification that let model variance flake `TestLiveDefaultHeadlessStopsAtGate`. The FO still does not resolve the gate headless. The whole lean extraction (cores, single core-naming, AC-1/AC-2 state, marker a59f79db) is intact; Claude lifecycle is 10403 (+125 for the MUST, +624 vs baseline). Offline tests + vet green; the live gate-stop behavior re-runs at PR CI. Branch HEAD a6d1f4f2.

## Stage Report: validation (cycle 3 — focused re-validation of the headless gate-stop MUST)

Focused re-validation: the ONLY delta this cycle is the single additive headless gate-stop MUST (a6d1f4f2). The prior lean rework (946b5569) PASSED a full validation last cycle incl. a clean detached adversarial audit, so the unchanged extraction was confirmed not-moved rather than re-audited from scratch.

- DONE: Confirm the headless gate-stop MUST is sound (the ONE change this cycle): step-9 headless bullet now MUSTs the FULL present-gate review per gate, terse line NOT sufficient, no headless resolve. Unambiguous + self-contained; AC-a test scans for that exact format.
  `first-officer-shared-core.md:32` headless bullet: appended MUST tells the FO, at each `gate: true` stop, for EACH gate, BEFORE exit, to `Skill(skill="spacedock:present-gate")` and render the complete template (`Gate review:` heading, chosen-direction prose, checklist roll-up, `Decision:` prompt) per `## Completion and Gates`, "exactly as the interactive path does"; "A terse stop-reason line is NOT sufficient"; "The FO still does NOT resolve the gate headless (no verdict, no terminalize)". Self-contained: the four template tokens it names exist verbatim in `skills/present-gate/SKILL.md` (`Gate review:`:16, `Chosen direction:`/`Checklist:`:45, `Decision:`:29). Additive/non-contradictory: appended to the existing bullet, the "Stop AT gates … do not resolve them" clause and the "given the conn" resolve path (`:33`) are unchanged; reinforces, drops nothing. AC-a match: `internal/ensigncycle/live_gate_stop_test.go::foAuthoredGateReview` scans the LIVE FO-authored assistant stream (parses JSONL `type=="assistant"` blocks — not a static file grep) for a block containing both `gate review:` AND `decision:` (case-insensitive) — precisely the MUST's two structural tokens. The MUST demands exactly what the live test grades.
- DONE: Confirm the delta-since-prior-validation is ONLY this MUST: diff 946b5569..a6d1f4f2 is a SINGLE one-line hunk in first-officer-shared-core.md; the whole lean extraction is BYTE-UNCHANGED. Re-confirm TERMINAL_TEARDOWN_BOUNDED marker byte-identical.
  `git diff --stat 946b5569 a6d1f4f2` = `first-officer-shared-core.md | 2 +-, 1 file changed, 1 insertion(+), 1 deletion(-)`; `git diff --name-only` returns ONLY that file; the full diff is the one headless-bullet line. The other five Claude-lifecycle files (claude-first-officer-runtime, fo-dispatch-core, claude-fo-dispatch, fo-merge-core, claude-fo-merge) are shasum-IDENTICAL between 946b5569 and a6d1f4f2 — so the two cores, single core-naming, reduced adapters, AC-1 reachability, AC-2-removed state, and the +499 structural overage did NOT move; re-confirmed not-moved, not re-audited. Marker: `claude-fo-merge.md` step-10 line shasum = `a59f79db559701e99160417aa0e98748c8859c69` (matches recorded); `TERMINAL_TEARDOWN_BOUNDED` token absent from both cores (0 hits each) — marker correctly stays per-host. Note: marker is now at `claude-fo-merge.md:7` (step 10) post lean-rework restructure, not the older :24/:34 line number — same byte-identical content, `TestGradeMarkerMatchesContract` pins it by path+substring not line.
- DONE: Offline green + AC-a CI gate compiles + Claude total = 10403.
  `go test -count=1 ./...` GREEN — all 16 test packages `ok` (incl. contractlint, ensigncycle, dispatch, status, cli; the only stderr line is a Go-telemetry sandbox `upload.token` permission notice, not a test result). `go vet -tags live ./...` exit 0, no output — confirms the live-tagged `live_gate_stop_test.go` (the AC-a gate) COMPILES clean under the `live` tag. `TestGradeMarkerMatchesContract` + all 5 teardown-grade watchers + the contractlint closure/single-source/boundary guards GREEN. Claude lifecycle re-measured word-by-word = **10403** (shared-core 4037 + claude-runtime 785 + fo-dispatch-core 2263 + claude-fo-dispatch 1734 + fo-merge-core 1138 + claude-fo-merge 446) — matches the entity exactly. The +125 vs the prior 10278 is precisely the MUST: shared-core grew 3912→4037 (+125) and the other 5 files are byte-identical. +624 vs the 9779 baseline = +499 accepted structural split + +125 this behavioral MUST.
- SKIPPED: AC-3a / TestLiveDefaultHeadlessStopsAtGate live behavior gate run locally — account under sustained 429.
  No local live drive attempted per the dispatch. The MUST is a prose reliability lever on model behavior; whether the FO now RELIABLY authors the review is proven by `TestLiveDefaultHeadlessStopsAtGate` at the 2y PR's runtime-live-e2e CI on BOTH claude legs (sonnet+opus), not locally. The cheap direct guards (marker byte-identity, the fixture marker/watcher tests, the live-tagged compile) ARE green locally.

### Summary

PASSED. The sole change this cycle is the single additive headless gate-stop MUST (`first-officer-shared-core.md:32`, commit a6d1f4f2): `git diff 946b5569..a6d1f4f2` is exactly ONE one-line hunk in that file and the other five Claude-lifecycle files are shasum-identical to the prior-PASSED lean rework (946b5569, which cleared a full validation incl. a clean detached adversarial audit) — so the entire extraction is byte-unchanged and re-confirmed not-moved, not re-audited. The MUST is sound: unambiguous, additive (it reinforces "stop at gates, do not resolve them" and leaves the "given the conn" path untouched, dropping no obligation), and self-contained (the four present-gate template tokens it names — `Gate review:`, chosen-direction, checklist, `Decision:` — exist verbatim in `skills/present-gate/SKILL.md`). It is aligned exactly with the AC-a gate `live_gate_stop_test.go::foAuthoredGateReview`, which scans the LIVE FO-authored stream for a block carrying both `gate review:` AND `decision:` (a runtime-behavior check on real output, not a static file grep). The `TERMINAL_TEARDOWN_BOUNDED` marker stayed byte-identical (a59f79db) in `claude-fo-merge.md` and is absent from both cores. Offline `go test ./...` (16 pkgs) + `go vet -tags live` are green, the live-tagged gate-stop test compiles, and the Claude lifecycle re-measured to 10403 exactly (+125 = the MUST; +624 vs the 9779 baseline = +499 accepted structural split + +125 the MUST). MERGE GATES on the 2y PR's runtime-live-e2e CI being green — specifically `TestLiveDefaultHeadlessStopsAtGate` passing on BOTH claude legs (sonnet + opus): the strengthened MUST is the reliability lever, and the gate-stop test going green on both legs is the proof it worked. The MUST is a prose lever on model behavior, not runnable locally (account 429); the structural delta-is-only-the-MUST + marker + offline gates reproduce locally, the behavior proof is wired to that PR CI lane.

## Stage Report: implementation (live-suite reliability fixes)

2y's CI re-run surfaced TWO pre-existing live-harness reliability bugs (NOT contract work; the extraction + gate-stop MUST are validated). Captain folded the fixes into 2y so its CI can go green. Both are in `internal/ensigncycle` (the live test harness).

- DONE: BUG 1 (PRIMARY — wrong-root boot). Isolate the live `--plugin-dir` from the real `docs/dev`.
  Root cause (from the opus `TestLiveEnsignCycleTeamTeardown` transcript): every claude-live launch passed `--plugin-dir = repoRoot` (the real repo), whose `docs/dev` sibling carries live entities (0q/48). An FO that anchored `git rev-parse --show-toplevel` + `status --discover` on the plugin path — not its isolated cwd fixture — discovered + drove the REAL workflow for 27 min. FIX: new `livePluginDir(t)` (`live_test.go`) stages a copy of the plugin scaffolding (`.claude-plugin/`, `skills/`, `agents/`) into a temp dir with NO `docs/dev` sibling, then `git init`s it so a `rev-parse` from the plugin path resolves to a workflow-free root. Repointed all FIVE claude-live `--plugin-dir` sites (shared runner, full cycle, gate-stop, residency, zero-discover) to it — one helper, hardens every scenario (and fixes a latent wrong-root in zero-discover). Verified offline by simulation: the staged dir carries the scaffolding, has no `docs/dev`, `rev-parse` resolves to itself, and `status --discover` there yields ZERO workflows → the FO falls back to the fixture. Commit b9c2c243.
- DONE: BUG 2 (residency/teardown force-team coin). Make force-team reliable.
  `TestLiveStandingResidencyInjectsCommOfficer` (and `TestLiveEnsignCycleTeamTeardown`) used a SOFT cue ("run in team mode (create a team for concurrent dispatch)") that 7e's headless `-p` bare-mode determination could decline, so the FO drove BARE (no team, no roster) and red the team-only oracles on correct behavior. FIX: new shared `forceTeamModeCue` (`claude_live_runner_test.go`) — a strong unambiguous MUST (TeamCreate before first dispatch, dispatch through the team, bare mode NOT acceptable, override the `-p` bare default). Same prose-lever pattern as the gate-stop MUST. Used in both residency + teardown.
- DONE: Keep the 2y extraction + gate-stop MUST + cores/marker intact; green offline.
  No contract/scaffolding change — only the live test harness. `go test ./...` all packages green (no FAIL); `go vet -tags live ./...` exit 0 (compiles the live-tagged harness clean). TERMINAL_TEARDOWN_BOUNDED marker unchanged (a59f79db). The LIVE behavior (no wrong-root boot; residency/teardown reliably get a team) re-runs at PR CI — account 429 locally, so BUG-1's fix is deterministic code (the isolation is verified by simulation) and BUG-2's is the strong lever.

### Summary

Fixed two pre-existing live-suite reliability bugs the CI surfaced, both in the `internal/ensigncycle` live harness. BUG 1: a new `livePluginDir(t)` stages an isolated plugin copy with no discoverable `docs/dev`, so no claude-live FO can wrong-root-boot into the real workflow — repointed all five claude-live `--plugin-dir` sites; verified by simulation that the staged root discovers zero workflows. BUG 2: a new shared `forceTeamModeCue` strong-MUST reliably forces team mode for the residency + teardown tests whose oracles require a real team, dissolving the headless bare-mode coin. The 2y extraction, the gate-stop MUST, and the cores/marker are untouched; offline tests + vet are green; the live behavior re-runs at PR CI. Branch HEAD b9c2c243.
