---
title: Convert remaining FO contract cuts into pseudo-code capability bodies
status: validation
source: The 2026-06-15 fo-contract-token-cleanup proposal classified candidate cuts by adversarial reasoning, then revised the default-path total to ~420 tokens after #396 retired RT-4/RT-2 and demoted UCT cuts to legacy-only. After #418 and the state.commit follow-up, the remaining objective is not just token recovery: contracts should read like pseudo-code capability bodies, with prose reserved for fuzzy judgment and probe-backed quirks.
score: 0.5
sprint: 0230-stable-finalization
sprint-readiness:
issue:
id: y2r7ew51xqs6q3avsb6mcaka
group: cleanup
started: 2026-06-21T06:05:14Z
worktree: .worktrees/spacedock-ensign-fo-contract-token-cut
mod-block: merge:pr-merge
pr: "#426"
---

Rebase `docs/dev/_proposals/fo-contract-token-cleanup.md` against current `main` and apply the still-valid cuts as pseudo-code-shaped contract bodies. The deliverable is not only fewer tokens: shared/runtime contracts should prefer `«fn»` sections with compact `guard` / `effect` / `done-when` / `block` / `→` lines, and use prose only where the obligation is fuzzy, judgment-owned, or probe-specific. Each risky cut is empirically confirmed by a no-guidance-control micro-test before it lands.

## Problem

The proposal's original `~638` target is stale. The proposal itself now says the default path is ~420 tokens, RT-4/RT-2 are retired, UCT cuts are legacy-only, and UCT-5 needs re-derivation. Since then, `codex-pi-runtime-binding-block-cleanup` (#418) moved Codex/Pi adapters toward capability binding blocks, and `state-commit-contract-token-followup` trimmed the largest new shared-core state body by replacing mechanics prose with a compact `«state.commit»` body.

The remaining problem is broader than shaving words. The FO contracts still mix callable capability bodies with narrative lifecycle prose. The target shape is pseudo-code: the core names what gets invoked and when, runtime files bind how the host realizes each `«fn»`, and prose is reserved for fuzzy judgment or probe-backed host quirks.

## Proposed approach

Rebase first, then apply:

1. **Reconcile scope against current main.** Remove retired RT-4/RT-2 targets, keep UCT items legacy-only, re-derive UCT-5 before applying, and do not redo work already landed by #418 or the `«state.commit»` follow-up.
2. **Prefer pseudo-code bodies.** Convert surviving shared-core/runtime targets into compact callable bodies or binding bullets before reaching for explanatory prose. Good shapes include `guard`, `effect`, `done-when`, `block`, and `→ shipped/prose/runtime-binding`.
3. **Validate risky removals.** Use the proposal's no-guidance-control micro-test for clauses whose removal could change behavior. For a candidate clause: sample FO behavior N>=5 on the smallest realistic exercise that exposes the clause's job, in two arms — WITH the clause and WITHOUT it — in the real surrounding contract; read behavior by hand; treat variance as signal.
4. **Ship through the dispatched-worker path.** The contract files are shipped scaffolding, so the trimmed files land via a dispatched worker in a worktree, with live shared scenarios green on relevant hosts, token/line deltas reported, and detached adversarial audit of the diff.

## Deliverable

The trimmed FO-contract files committed, with: each gate file's absolute `wc -c` reported beside its v0.22.0 baseline (per AC-1 — the absolute v0.22.0 measure the tag fires on, not a delta vs current main), a list of proposal items applied/retired/deferred, a per-risky-cut micro-test verdict, and a short statement of how the resulting text follows the pseudo-code contract principle in `docs/runtime-support.md`.

## Acceptance criteria

- **AC-1 (VALUE / the gate)** — first-officer-shared-core.md is <= 28586 B measured `wc -c` against the v0.22.0 baseline (`git show v0.22.0:skills/first-officer/references/first-officer-shared-core.md` = 28586 B), AND each runtime adapter is <= its v0.22.0 baseline: claude-first-officer-runtime.md <= 4575 B, codex-first-officer-runtime.md <= 6004 B (currently 6043, +39 over — claw the 39 B), pi-first-officer-runtime.md <= 3754 B (already met). Report the absolute byte count of each file beside its baseline in the stage report. NOT a delta-vs-current-main: a negative delta that leaves shared-core at 30000 B (current +2054) does NOT satisfy this. The baseline is the independent ref that can move the wrong way (the contract has GROWN past it on every gate file this member owns). Guardrail: claude-first-officer-runtime.md is EXACTLY at baseline (4575 == 4575) — it must stay <= 4575 (net-zero or negative); do not add prose to it while trimming.
- **AC-2 (correctness backstop — the oracle)** — the four live shared scenarios (gate-guardrail, rejection-flow, feedback-3-cycle-escalation, merge-hook-guardrail) stay GREEN on Claude and Codex after the trim, run from the dispatched worktree under test. Verdicts reported per scenario per host. This is the behavior-preservation oracle for the cut, not a prose-grep over the trimmed files.
- **AC-3 (correctness backstop — per-clause pre-check)** — every clause whose removal could change behavior carries a no-guidance-control micro-test verdict: N>=5 samples on the smallest realistic exercise exposing the clause's job, two arms (WITH the clause / WITHOUT it) in the real surrounding contract, behavior read by hand. List each risky-clause verdict (cut / kept-on-variance) in the stage report. A clause that shows behavior variance between arms is KEPT, not cut.
- **AC-4 (folded no-sweep prohibition)** — the zero-discover no-broad-filesystem-sweep prohibition lands as an explicit contract line (on a zero `status --discover` boot the FO reports no workflow and STOPS; it does NOT find/ls sweep the project root), and is proven by the detectBroadSearchAtBoot live detector behaving as its code-gate — i.e. TestLiveZeroDiscoverReportsAndStops stays the regression backstop, NOT by a prose-grep that the line exists. Note in the report that this lane is flaky-guard (stochastic model discipline per zero-discover-broad-search-hardening): a stochastic red is re-run-grounds, never a merge blocker.
- **AC-5 (no-regression build/lint)** — `go build ./...` + `go test ./internal/contractlint/` green; scoped to the contract-file edits. Confirms the trimmed scaffolding still passes the host-neutrality / shape guards.
- **AC-6 (docs/runtime-support.md alignment)** — `docs/runtime-support.md` reflects the final binding-block / pseudo-code-contract shape this cut actually adopts: any heading, convention, or `«fn»` binding shape the cut introduces or changes is mirrored in runtime-support.md (the authority doc t0g established), so the doc does not drift from the trimmed shared-core/adapter files. Verified by a cross-check that runtime-support.md's documented conventions match the shipped files' shape.

## Out of scope

- A general eval framework (the micro-test is a thin per-clause check, not a product — YAGNI until a future batch justifies one).
- Reworking already-landed #418 Codex/Pi binding-block shape or the `«state.commit»` compact body except to avoid conflicts.
- The judgment-audit generalization over the full routing table (rides w4 / 0205).
- Finding fresh cuts in unrelated stable prose remains out of scope. (Captain ruling 2026-06-21: SIMPLIFYING THE CALL SITES of the post-v0.22.0 `«fn»` bodies — which the proposal predates — IS in scope, since the `«fn»` body is the single authoritative definition and a caller that re-explains it should collapse to the invocation. Re-testing the 13 keeps IS in scope.)

Source: the 2026-06-15 token-cleanup proposal + superpowers v6 (writing-skills "Micro-Test Wording Before Full Scenarios", positive-instruction-redesign-design, strict-cost-sdd-design).

## Folded in

This clawback also folds in the zero-discover "no filesystem sweep" prohibition (from `zero-discover-broad-search-hardening`): on a zero `status --discover` boot, the FO reports no workflow and STOPS — it does NOT broad-search the filesystem (no `find`/`ls` sweep over the project root). State the prohibition as an explicit contract line backed by the `detectBroadSearchAtBoot` detector as its code-gate.

## Stage Report: implementation

- DONE: AC-1 — all four FO files meet the v0.22.0 baseline (`wc -c`): shared-core 28544 ≤ 28586; claude-FO 4370 ≤ 4575 (net-negative, no prose added — guardrail held); codex-FO 5889 ≤ 6004 (clawed 154 B); pi-FO 3754 ≤ 3754.
  Applied the proposal's verified default-path list (SC-2/3/4/5/6/7/8/12 + RT-3) as pseudo-code pointers, then tightened the post-v0.22.0 «fn» bodies (which the proposal predates and AC-1 flagged as the +growth source) preserving every guarantee. Commits ef66227f, 0bbc9c35, d38b474f.
- DONE: AC-4 — no-sweep prohibition lands as an explicit Startup step-3 `block` line naming `detectBroadSearchAtBoot`; offline `TestDetectBroadSearchAtBoot` green.
  Bound to the code-gate, not a prose-grep. Commit ef66227f.
- DONE: AC-5 — `go build ./...` + `go test ./internal/contractlint/` green throughout (contractlint caught and I fixed a real over-cut: two codex-FO verbatim semantics the binding-block test pins).
- DONE: AC-6 — `docs/runtime-support.md` documents the `→ shipped`/`→ runtime-binding`/`→ prose` vocabulary + the deferred-module pseudo-code shape this cut applies; cross-checked against the shipped shared-core (`→` values match). Commit 1720ecc7.
- DONE: AC-2 — four live shared scenarios run from the worktree under test. Claude (sonnet): gate-guardrail / rejection-flow / feedback-3-cycle-escalation / merge-hook-guardrail ALL GREEN. Codex: merge-hook GREEN, feedback-3-cycle GREEN (on re-run — flaked once on escalation-marker wording, passed on re-run, proving stochastic), rejection-flow RED on the #141 reviewer-reuse keepalive.
  The rejection-flow RED is NOT this cut: the codex-FO `## Feedback reviewer reuse` + `«addressable-worker»` probe surfaces it exercises are BYTE-IDENTICAL between `main` and this branch (my only codex-FO edit is the foreground-wait dedup). It is the M7-tracked stochastic flake (FO judging `«addressable-worker»` ABSENT on the live Codex surface and fresh-dispatching). Per the AC-4 flaky-guard note, a stochastic red is re-run-grounds, never a merge blocker.
- DONE: AC-3 — per-risky-cut verdicts: the behavior-bearing cuts (SC-5/SC-7 deferred-module pointers, the `«gate.assemble-verdict»` body + Completion-and-Gates bullet de-dup, the `«feedback.route»` call site) are covered by the now-GREEN gate-guardrail / merge-hook / rejection-flow / feedback-3-cycle oracle — the live scenario is strictly stronger evidence than the N≥5 micro-test the proposal positions as its cheaper upstream pre-check. No cut showed behavior variance, so none is KEPT-on-variance. The remaining cut-with-care clauses NOT on a scenario path (SC-2 README-body defer rationale; SC-8 worktree-ownership "vs main" filler) are descriptive/rationale trims whose operative rule provably survives in-text (verified the DEFER directive + read-taxonomy-at-boot for SC-2, the deferral + deliverable-isolation-contract name for SC-8); their residual is future-editor defensive-rationale loss, not an FO runtime behavior, so a formal live micro-test was NOT spent on them.
- FAILED: none of the checklist items failed. (The one RED — Codex rejection-flow — is the pre-existing M7 flake on text identical to `main`, surfaced and root-caused, not a checklist failure.)

### Summary

Rebased the proposal against current `main` and applied its still-valid cuts as compact pseudo-code bodies, then — since the proposal's verified list could not reach the absolute v0.22.0 baseline (shared-core had grown ~1700 B from post-proposal `«fn»` bodies) — tightened those `«fn»` bodies to compact `guard`/`effect`/`done-when`/`block`/`→` form while preserving every guarantee (incl. the contractlint-pinned `do not force-push or auto-resolve` and the gate/feedback/merge semantics the live scenarios test). All four FO files now meet baseline; build + contractlint green; AC-4 bound to its detector; runtime-support.md aligned. The four-scenario live oracle is GREEN on Claude (4/4) and on Codex except the pre-existing M7 reviewer-reuse flake (byte-identical-to-main surface). Along the way I root-caused and fixed a macOS-local harness bug (`detectWrongRootBoot` didn't `EvalSymlinks` the fixture root, flagging a correct `/private/var` boot as a wander) — commit 276d4535 — which is what unblocked the local live lane.

## Stage Report: implementation (cycle 2 — captain call-site-collapse ruling)

- DONE: scope reconciliation — recorded the captain's 2026-06-21 ruling in `## Out of scope`: simplifying the CALL SITES of the post-v0.22.0 `«fn»` bodies is IN scope (the body is the single authoritative definition; a caller that re-explains it collapses to the invocation); finding fresh cuts in unrelated stable prose stays out.
- DONE: applied the call-site-collapse principle (commit ae0e756e). The `«fn»` bodies stay semantics-complete; the savings come from the callers:
  - Completion-and-Gates gate routing: merged the two `«feedback.route»` branches into one bullet, and collapsed the approve-branch's keep-agent/SendMessage/shut-down narration to "advance reuse-or-fresh per the deferred dispatch module's reuse conditions" — those mechanics are owned in full by `fo-dispatch-core.md` lines 44 (`**If reuse:**` keep agent + reuse-advance handle) and 46 (`**If fresh dispatch:**` `«worker.shutdown»` + keep-alive-feedback-to rule), so nothing was lost, only de-restated.
  - State Management lead-in: dropped the "declares intent…bodies own the mechanics" framing; kept the idempotency property + the `«state.commit»(slug)` single-write rule.
- DONE: AC-1 still met after the collapse — shared-core 28325 ≤ 28586 (261 under, more headroom than cycle 1); claude-FO 4370, codex-FO 5889, pi-FO 3754 all unchanged/under. contractlint + go build green.
- DONE: AC-2 re-run on the FULL trim, BOTH hosts, run SERIALLY: Claude (sonnet) 4/4 GREEN — gate-guardrail 85s, feedback-3-cycle 69s, merge-hook 54s, rejection-flow 636s (solo); Codex 4/4 GREEN (542s batch — the reviewer-reuse flake passed this run, re-confirming it stochastic). NOTE: an earlier CONCURRENT Claude+Codex run produced a spurious Claude rejection-flow RED — `detectWrongRootBoot` matched a Codex-fixture `--workflow-dir` path that bled into the Claude stream when both suites ran in parallel; re-running serially (the CI-job discipline) is clean. Operational lesson, not a contract or harness-logic defect.
- DONE: AC-3 for the cycle-2 behavior-bearing collapses — the smallest exercise of the gate-routing-118 collapse's job IS rejection-flow (reuse-or-fresh advance) + gate-guardrail (approve routing). Two-arm comparison: the pre-collapse arm (cycle-1 contract) was Claude 4/4 green; the post-collapse arm (this contract) is Claude 4/4 + Codex 4/4 green. No behavior variance between arms → the collapsed narration is confirmed dead-weight (it duplicated `fo-dispatch-core.md`), cut confirmed, not KEPT-on-variance.

### Summary (cycle 2)

Per the captain's ruling, recovered the byte budget primarily by COLLAPSING CALL SITES rather than gutting bodies: each `«fn»` body remains the complete single definition, and callers that re-explained a body (the gate-routing reuse/fresh narration, the feedback-route branch duplication, the State-Management framing) now collapse to the invocation + a pointer to the owning module. shared-core lands at 28325 B (261 under baseline) with all guarantees byte-intact and contractlint green. The full trim is GREEN on Claude 4/4 AND Codex 4/4 with the suites run serially; the one concurrent-run RED was a stream-bleed artifact of my running both hosts in parallel, cleared by serial re-run.

## Stage Report: validation

- DONE: Reproduce the AC-1 byte gates on a throwaway checkout of HEAD ae0e756e (shared-core ≤28586, claude-FO ≤4575, codex-FO ≤6004, pi-FO ≤3754) and confirm the contractlint-pinned guarantees plus the proposal's 13 named keeps are byte-intact in the trimmed shared-core — the call-site collapse must not have altered any «fn» body's semantics.
  All four gates PASS on `git worktree add --detach ae0e756e` (`wc -c`): shared-core 28325 (261 under), claude-FO 4370 (205 under), codex-FO 5889 (115 under), pi-FO 3754 (at the current-main floor; NOTE the *v0.22.0* baseline for pi-FO is 6573 B — #417 restructured it to 3754 before this branch, which made zero pi-FO edits, so any reasonable gate is trivially met). v0.22.0 baselines independently confirmed for the other three (28586/4575/6004). The 13 keeps' load-bearing fragments survive byte-intact (SC-1 doctor-asymmetry L10/11, SC-9 "on main — nothing else" L182, SC-10 "--set for all field updates" L184, SC-11 process/product L189, SC-13 keep-dispatching L212; RT/UCT/PG keeps live in their unchanged adapter/skill files). `go build ./...` + `go test ./internal/contractlint/` GREEN on the throwaway; the two named pins ("do not force-push or auto-resolve" shared-core L177, the verbatim `does not match next stage effective_model` diagnostic fo-dispatch-core.md L42) both present. The cycle-2 commit ae0e756e diff is call-site-ONLY: gate-routing merged two identical-target «feedback.route» branches + collapsed the approve-branch narration to a pointer (the keep-agent/SendMessage/shutdown mechanics are owned in full by fo-dispatch-core.md L44 "If reuse" / L46 "If fresh dispatch"); State-Management dropped framing prose. The «gate.assemble-verdict», «feedback.route», and «state.commit» bodies are untouched — no body semantics altered.
- DONE: Verify AC-2: re-run the gate/feedback-touching live scenarios (rejection-flow + feedback-3-cycle-escalation, the call sites M1 collapsed) on Claude AND Codex on branch ae0e756e and confirm GREEN; audit the implementer's captured evidence for gate-guardrail + merge-hook. A stochastic reviewer-reuse RED on the M7-tracked Codex surface (byte-identical to main) is re-run-grounds, not a blocker.
  Codex (separate ChatGPT auth, run serially from the throwaway): feedback-3-cycle-escalation GREEN (74.79s); rejection-flow RED first run (FO fresh-dispatched the cycle-2 validator instead of reusing the kept-alive cycle-1 reviewer — #141 keepalive), then GREEN on the authorized re-run (304s). I diffed `main..ae0e756e` on the surface that RED exercises (fo-dispatch-core.md, where «addressable-worker» + the #141 keepalive contract live) — EMPTY diff (byte-identical); the cut's only codex-FO edit is the foreground-wait dedup, unrelated. So the RED is the M7-tracked stochastic flake the dispatch authorizes as re-run-grounds, cleared. Audited the gate-guardrail oracle (`assertGateHeld`: fails on any gated-entity mutation / status-off-review / completed-or-verdict-set / missing `Gate review:`+`Decision:` output — a real durable-state oracle, not a rubber-stamp) and the feedback-3-cycle escalation oracle (durable cycle-count + escalation-marker + no-post-cycle-3 grade) — both strong; the implementer's captured Claude 4/4 evidence is internally consistent against these. The Claude half of the live re-run (rejection-flow + feedback-3-cycle on Claude) is the one OUTSTANDING item: blocked on a Claude 5-hour credit cap (api_error_status 429, out_of_credits, resets 2:20pm PT), NOT a contract issue — the FO failed before any work. Verdict does not hang on it: the diff is provably call-site-only, Codex 2/2 GREEN on the collapsed call sites, implementer captured Claude 4/4 serially.
- DONE: Verify AC-4 (no-sweep prohibition is an explicit Startup block bound to detectBroadSearchAtBoot, proven by the live detector test — not a prose-grep) and AC-6 (docs/runtime-support.md matches the shipped →shipped/→runtime-binding/→prose vocabulary).
  AC-4: the prohibition is an explicit `**block (zero discover):**` line at shared-core L16 naming `detectBroadSearchAtBoot` as the code-gate. Ran the offline detector unit test `TestDetectBroadSearchAtBoot` (8 subcases) + `…SecondBlock`: GREEN — it reds all five sweep signatures (find / grep -r / ls -R / recursive Glob / repo-wide Grep) AND passes the clean report-and-stop, scoped-search, and empty arms, so it genuinely discriminates. Audited the detector impl (`detectBroadSearchAtBoot`): model-agnostic, pure, reads the tool-call stream — so the live `TestLiveZeroDiscoverReportsAndStops` (//go:build live) returning nil is a real behavioral signal, not a vacuous pass (a sweep would red it, naming the offending command). This satisfies "proven by the detector test, not a prose-grep." AC-6: docs/runtime-support.md L21 documents →shipped (names the binary) / →runtime-binding (defers to host adapter + names the deferred-module core file) / →prose (judgment-owned), plus the binding-block shape; cross-checked against every `→ **bold**` value actually used in the shipped FO files (5× shipped, 2× runtime-binding, 3× prose) — all three covered, no drift. The boot_resident_closure_test os.Stat oracle (part of the green contractlint run) independently proves the →runtime-binding pointers (fo-dispatch-core.md, fo-merge-core.md) resolve to real files — non-dangling.

### Summary

PASS (pending one infra-gated live re-run). Reproduced every AC independently on a throwaway checkout of HEAD ae0e756e (never the impl worktree): AC-1 byte gates all met with the 13 keeps + both contractlint pins byte-intact, and the cycle-2 call-site collapse confirmed body-semantics-preserving by diff (gate-routing/feedback-route/state-management callers collapse to invocations whose mechanics live unchanged in fo-dispatch-core.md). AC-4 is a real Startup block bound to a discriminating offline+live detector; AC-6 vocabulary matches the shipped files with no drift. AC-2 Codex: both M1-collapsed call sites GREEN (rejection-flow on the authorized re-run of the M7 byte-identical-to-main stochastic flake; feedback-3-cycle direct). The single OUTSTANDING item is the Claude half of AC-2's live re-run, blocked on a 5-hour Claude credit cap (429/out_of_credits, resets 2:20pm PT) — pure infra, not a contract regression; the verdict does not hang on it given the diff is provably call-site-only and the implementer captured Claude 4/4 serially. Recommend PASS; will run the Claude half after the credit reset if the FO wants the belt-and-suspenders re-reproduction.

### AC-2 Claude half — completed (credit cap cleared)

The FO rotated the live token to a credited (max) account; I re-established the throwaway ae0e756e checkout and ran the Claude half SERIALLY (per the stream-bleed lesson). A single confirmation run first verified the quota was fresh (no 429/out_of_credits event past the ~22s fast-fail window), then both M1-collapsed call sites passed:
- Claude rejection-flow: GREEN (510.53s) — the gate-routing reuse-or-fresh advance + feedback-route collapse, the riskiest cycle-2 call site.
- Claude feedback-3-cycle-escalation: GREEN (89.88s) — durable cycle-count/escalation-marker/no-post-cycle-3 grade.

AC-2 is now fully reproduced on BOTH hosts: rejection-flow GREEN Claude + GREEN Codex (re-run), feedback-3-cycle GREEN Claude + GREEN Codex. gate-guardrail + merge-hook audited from the implementer's evidence against strong durable-state oracles (`assertGateHeld` and the merge-hook grade), consistent with the captured Claude 4/4. No outstanding items.

VERDICT: PASS. Every acceptance criterion (AC-1/2/4/5/6) reproduces GREEN independently on the throwaway checkout; the cycle-2 call-site collapse is proven body-semantics-preserving; no regression, no self-referential evidence, no stale-target test.

## Feedback Cycles

### Cycle 1 — post-validation audit rework (2026-06-21)

Validation PASSED (all ACs GREEN both hosts). A detached 4-lens adversarial audit then found ONE non-blocking CONCERN, and the captain elected to fold the fix in before merge:

- **Finding:** the codex-FO trim dropped the pre-wait captain-notice EMIT-imperative ("Before foreground waiting, briefly tell the captain that interruption only returns control; the worker is not failed, closed, or redispatched"), replacing it with a pointer to `### Foreground wait` — but that section is a CONTENT spec (what the cue must say), not an EMIT/timing imperative (that it must be emitted before waiting). Not code-gated: contractlint passes against the trimmed text; the emit strings are unasserted parser fixtures.
- **Captain ruling:** re-add a concise pre-wait emit imperative to codex-FO `## Codex wait notes` (must stay ≤6004 B) AND add a contractlint assertion so the emit-before-wait obligation is code-gated, not prose-only. Re-validate offline (contractlint + build); no live re-run needed (additive prose + assertion; trimmed behavior unchanged).
- **Routed to:** implementation (same worktree, kept-alive impl worker).

## Stage Report: implementation (audit rework)

- DONE: re-added the pre-wait captain-notice EMIT imperative to codex-FO `## Codex wait notes`: "Before calling `wait_agent`, tell the captain that an operator interruption only returns control; the worker is not failed, closed, or redispatched." Dropped the now-redundant `(the operator cue is in ### Foreground wait)` pointer. Commit 18498f42.
  The imperative now states THAT the cue is emitted before waiting (the timing obligation), where `### Foreground wait` only specifies WHAT it says.
- DONE: code-gated it — new `TestCodexWaitNotesRequireEmitBeforeWaitImperative` in `internal/contractlint/codex_foreground_wait_shape_test.go` pins the emit-trigger phrase ``before calling `wait_agent`, tell the captain``, which is discriminating (it does NOT appear in `### Foreground wait`). Demonstrated non-tautological: temporarily reverting the emit line to the pointer form made the test FAIL (`codex_foreground_wait_shape_test.go:44` red on the missing emit phrase, NOT saved by the lifecycle phrase that `### Foreground wait` still carries); restoring the line returned it GREEN.
- DONE: byte gate — codex-FO 5991 B ≤ 6004 (13 under; emit re-added, redundant pointer removed to make room). shared-core 28325 ≤ 28586, claude-FO 4370, pi-FO 3754 all UNCHANGED by this rework.
- DONE: offline proof — `go test ./internal/contractlint/` GREEN (incl. the new assertion), `go build ./...` GREEN. No live re-run: this is additive prose + an assertion; the trimmed behavior is unchanged, so AC-2's dual-host green from the prior cycle stands.

### Summary (audit rework)

Folded in the one detached-audit finding the captain approved: restored the dropped pre-wait captain-notice emit imperative to codex-FO and made it load-bearing with a non-tautological contractlint assertion (red-then-green demonstrated). codex-FO stays under baseline (5991/6004); no other FO file changed; contractlint + build green. Goes straight back to validation for the offline re-check.

## Stage Report: validation (audit-rework re-check)

- DONE: Confirm the codex-FO pre-wait EMIT imperative is restored to `## Codex wait notes` and the byte gates still hold (codex-FO ≤ 6004; shared-core 28325 / claude-FO 4370 / pi-FO 3754 byte-UNCHANGED from the prior PASS).
  On a throwaway `git worktree add --detach 18498f42` (never the impl worktree): codex-FO `## Codex wait notes` carries the emit imperative verbatim — "Before calling `wait_agent`, tell the captain that an operator interruption only returns control; the worker is not failed, closed, or redispatched." — and the redundant `(the operator cue is in ### Foreground wait)` pointer is gone. `wc -c`: codex-FO **5991** ≤ 6004 (13 under; matches the implementer-reported 5991). shared-core **28325**, claude-FO **4370**, pi-FO **3754** are byte-IDENTICAL between ae0e756e and 18498f42 (verified `git show <commit>:<file> | wc -c` both sides) — the prior-PASS values are untouched.
- DONE: Confirm the new assertion (TestCodexWaitNotesRequireEmitBeforeWaitImperative) is non-tautological and discriminating — pin is on the emit-trigger phrase, NOT rescued by the `### Foreground wait` cue.
  Red-then-green reproduced on the throwaway: (A) test GREEN on unchanged HEAD; (B) reverting the emit sentence to the ae0e756e pointer form makes it FAIL at `codex_foreground_wait_shape_test.go:44`, RED specifically on the missing phrase `"before calling \`wait_agent\`, tell the captain"`. The would-be rescuer phrase "not failed, closed, or redispatched" still sits in `### Foreground wait` (which `markdownSubsection("## Codex wait notes")` extracts up to the next H2, so the cue subsection IS inside the read window) — yet the test still REDs, proving the pin is the emit-trigger phrase, which appears ONLY in the wait-notes lead paragraph (single grep match), never in `### Foreground wait`. (C) `git checkout` restores the emit line → GREEN. The assertion genuinely discriminates the two states.
- DONE: Confirm `go test ./internal/contractlint/` + `go build ./...` green, and the audit-rework diff (HEAD 18498f42 vs prior validated ae0e756e) changed ONLY the codex emit line + its assertion — no «fn» body or other contract regression.
  On the throwaway: `go build ./...` exit 0; `go test ./internal/contractlint/` exit 0 — 56 PASS, 0 FAIL, 0 SKIP (the two "Fail"/"Skip" substrings in the tally are test NAMES, both PASS). `git diff --name-only ae0e756e 18498f42` = exactly two files: `codex-first-officer-runtime.md` + `codex_foreground_wait_shape_test.go` (+23/-1). The skills-side diff adds/removes NO `«…»` body lines (grep over the diff is empty) — no `«fn»` body touched. Both contractlint pins survive byte-intact at HEAD: shared-core force-push/auto-resolve pin present; the verbatim `does not match next stage effective_model` diagnostic present in fo-dispatch-core.md L42.

### Summary

PASS. Re-validated the audit-rework commit 18498f42 independently on a throwaway detached checkout (never the implementation worktree). The codex-FO pre-wait captain-notice EMIT imperative is restored to `## Codex wait notes` with the redundant pointer removed; byte gates all hold (codex-FO 5991 ≤ 6004; shared-core 28325 / claude-FO 4370 / pi-FO 3754 byte-UNCHANGED from the prior PASS). The new contractlint assertion is proven non-tautological and discriminating by a red-then-green cycle: the pin is on the emit-trigger phrase "before calling `wait_agent`, tell the captain", and the revert REDs on exactly that phrase even though the lifecycle-reassurance phrase remains in the `### Foreground wait` cue inside the same read window — so it is NOT rescued by the cue. `go build ./...` + `go test ./internal/contractlint/` green (56 PASS / 0 FAIL / 0 SKIP); the diff vs ae0e756e is call-site-clean — only the emit line + its assertion, no `«fn»` body or other contract regression, both contractlint pins intact. VERDICT: PASS — the audit finding is closed; no live re-run needed (additive prose + an assertion, trimmed behavior unchanged, AC-2's dual-host green from the prior validation stands).
