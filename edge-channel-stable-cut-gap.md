---
title: "Edge channel survives the stable-cut window (no binary/skills minor skew between a stable tag and the first prerelease)"
status: validation
source: "Captain report, 0250 Commander session 2026-07-07: edge tap installs the 0.24.0 binary while the spacedock-edge marketplace serves next-branch skills stamped 0.25.0-pre1. Verified: spacedock@next cask version 0.24.0 (updates only on tag push); origin/next .claude-plugin/plugin.json version 0.25.0-pre1; origin/next shared-core gate line 'require binary minor 0.25'. Result: every edge boot since the 0.24.0 stable cut (Jul 4) aborts at the FO version gate (binary too old, minor 0.24 < required 0.25). Broken by the release flow's own design — 'Advancing the Edge Line' bumps next to the post-release dev pre-version at the stable tag, but the edge BINARY only updates on the next tag push — not by any interim merge. Immediate remediation (separate from this task): push the first prerelease tag to realign."
started: 2026-07-07T08:22:09Z
completed:
verdict:
score: 0.5
worktree: .worktrees/spacedock-ensign-edge-channel-stable-cut-gap
issue:
id: zr2rbsjsak7xx6tetr3n37hc
mod-block: merge:pr-merge
pr: pr-merge:482
---

The release flow guarantees a broken edge channel from every stable cut until the first subsequent prerelease tag: the stable tag bumps next's manifests and contract gate line to the post-release pre-version while the spacedock@next cask keeps serving the stable-tag edge build, so the shipped version gate (same major.minor required) aborts every edge boot in the window. Direction options for ideation: (a) defer next's version bump so it rides the first prerelease tag instead of the stable tag; (b) have the stable tag's pipeline also cut a post-release pre-version edge build + cask bump in the same run, closing the window to minutes; (c) make the version gate tolerate skills exactly one minor ahead when the binary channel is edge — weakest, contract-side complexity. Acceptance sketch: value — after a stable cut, an edge-channel install boots green with zero manual steps (baseline: the 2026-07-04..07 window, every edge boot aborting); mechanism — the chosen pipeline change ships. High-stakes surface (CI and release machinery): detached adversarial audit + the release-flow dry-run treatment per docs/releasing.md.

## Root cause localization (FO forensics, 2026-07-07)

- `.github/workflows/release.yml:322-339`, job `edge-advance`, step "Reconcile the edge line past the stable release" (`if: "!contains(github.ref, '-')"`): computes `spacedock-release dev-preversion X.Y.Z` -> `X.(Y+1).0-pre1` and `stamp-version`s it into `.claude-plugin/plugin.json`, `.codex-plugin/plugin.json`, AND `skills/first-officer/references/first-officer-shared-core.md` (the `FO_PROSE` var) — rewriting the boot version-gate line to a binary minor that does not exist anywhere yet.
- `release.yml:345-353`: the marketplace calendar bump runs on both paths, so on a stable tag it actively triggers every edge installer's re-pull of the incompatible skills.
- The binary half (goreleaser edge build + `spacedock@next` cask bump) only publishes the TAG's version (0.24.0) and does not move again until the next tag push — no 0.25-line binary exists in the same run.
- Latent interaction, not a single bad line: under the retired contract-integer gate, one-minor-ahead skills booted fine (contract 3 == 3). #468 (`511dae11`) replaced it with minor-version coupling BEFORE the 0.24.0 cut; v0.24.0 (Jul 4) was the first stable tag under the new gate, converting the deliberate stamp-ahead ("never masquerade as the shipped stable") into a channel-wide boot abort. docs/releasing.md's own 0.24.0-pre1 note records the drift-cousin of this class.

## Design analysis addendum (captain question + FO analysis, 2026-07-07)

Captain question: should the stable cut always also cut an N+1.0-pre0 edge build, and what happens on a stable .1 patch release?

- **Always-cut-pre0 at the stable tag: sound, with machinery.** Content is exactly the audited stable bits re-stamped (next was just reconciled to the release commit), so nothing unaudited ships and the broken window closes to minutes. Needs an auto-tag (vX.(Y+1).0-pre0) on the stamped next commit + a recursion guard so the auto-tag does not re-trigger edge-advance. Pre-numbering is cosmetic: the boot gate requires same MINOR only (prerelease skew allowed), so a pre0 binary under pre1-stamped skills passes.
- **Maintenance patch releases expose a SECOND latent bug, independent of the original gap.** edge-advance assumes monotonic tag order; a vX.Y.1 cut from an older release line while next is on the (Y+1) dev line breaks three ways: (1) the reconcile's `merge -X theirs` FAVORS THE RELEASE, clobbering next's newer files with old patch-line content on conflict; (2) the stamp writes dev-preversion(X.Y.1) = X.(Y+1).0-pre1, REWINDING next's manifests/gate line if next has advanced past it; (3) under always-cut-pre0, the patch would attempt a colliding vX.(Y+1).0-pre0 auto-tag.
- **Fix for both: a line-ordering guard on edge-advance** — fire only when dev-preversion(tag) >= next's current manifest version; otherwise SKIP with a log line. An old-line patch then updates only the stable cask; its fix reaches edge via normal main->next flow, not the release reconcile. With the guard, always-cut-pre0 is the recommended shape for latest-line stable cuts.

## Ideation (2026-07-07)

### FLAG FOR THE IMMINENT v0.25.0 CUT (read before cutting)

This fix will almost certainly NOT ship before v0.25.0. Do NOT block the cut on it. Consequences the cut gate must plan around:

1. **v0.25.0 re-opens the window.** The cut stamps next to `0.26.0-pre1` and requires binary minor `0.26`, while the `spacedock@next` cask still serves the `0.25.0` edge build → every edge boot aborts until a `0.26`-line prerelease binary exists. Cheapest realign (staff-review-validated), manual until always-cut-pre0 lands: right after the stable cut, **dual-tag the already-greened release commit** — `git tag -a v0.26.0-pre1 <RELEASE_COMMIT> -m "<non-empty body>" && git push origin v0.26.0-pre1`. Reusing the greened SHA means the `e2e-gate` matches its existing green run (no re-green, no fresh commit to dispatch); the boot gate is minor-exact in BOTH directions and the prerelease reconcile does not rewind next's stamp, so the channel realigns cleanly. The tag MUST be annotated with a non-empty body — a lightweight tag reds the release-notes extraction step (release.yml:196-213).
2. **Nothing about v0.25.0 makes this fix harder.** The fix's tests pin version algebra, not the `0.25.0` numbers; the cut and the fix do not interact.
3. **When always-cut-pre0 later ships, expect TWO GitHub releases per stable cut** (the `vX.Y.0` stable + the auto `vX.(Y+1).0-pre0` prerelease on the same commit) — by design, not a double-cut bug.

### Problem statement

The release flow deterministically breaks the edge channel from every latest-line stable cut until the first subsequent prerelease tag (root cause + the retired-integer-gate interaction are in the addenda above). Two independent defects:

- **The window.** `edge-advance` stamps next's manifests + FO gate line PAST the release to `X.(Y+1).0-pre1` at the stable tag, but the edge BINARY (`spacedock@next` cask) only publishes the tag's own `X.Y.0` build — no `X.(Y+1)`-minor binary exists until a later prerelease tag. Skills lead the binary by one minor; the same-minor boot gate aborts.
- **Old-line patch clobber.** `edge-advance` assumes monotonic tag order. A `vX.Y.1` patch cut from an older line while next is on the `(Y+1)` dev line reconciles `-X theirs` (favoring the OLD release) into next and stamps `dev-preversion(X.Y.1)=X.(Y+1).0-pre1`, clobbering next's newer content and rewinding its manifest/gate line.

Keeping the stamp-ahead is deliberate (rejected alternative "defer the bump / option (a)": leaving next's gate at the stable minor turns a LOUD boot-abort into a SILENT incompatibility once next accrues `(Y+1)`-binary-dependent skills — minor-coupling exists to prevent exactly that). So the window must be closed by shipping a real `X.(Y+1)` edge binary, not by lowering the gate.

### Proposed approach — two mechanisms; corrections 1–2 confirmed by staff review, correction 3 retracted

**Mechanism A — line-ordering guard gating the WHOLE edge-advance job (the load-bearing fix).** A new `spacedock-release edge-advance-decision <tag> <next-plugin.json>` subcommand computes the tag's *target edge version* — `dev-preversion(tag)` for a bare stable tag, the tag's own version for a `-pre` tag (what next would inherit) — reads next's current manifest version, and prints `advance` or `skip` (exit 0 either way). A new FIRST step in the edge-advance job (`id: decision`) reads origin/next's `.claude-plugin/plugin.json` via `git show origin/next:.claude-plugin/plugin.json`, runs the subcommand, writes `advance=true|false` to `$GITHUB_OUTPUT`, and logs a `::notice::` on skip. EVERY downstream step — both reconcile steps, the calendar-bump+push step, and the always-cut-pre0 step — carries `if: <tag-shape> && steps.decision.outputs.advance == 'true'` (the calendar bump was previously unconditional; ASK 2 gates it here). On `skip` the whole job no-ops: next's tip — content, manifests, FO-prose gate line, AND the marketplace calendar key — is left byte-identical, so an old-line patch never churns an edge re-pull. This one guard covers the stable-cut window's pre0 recursion AND all three patch failure modes.

**Mechanism B — always-cut-pre0 at latest-line stable tags (closes the window).** After the stable-path reconcile+stamp+push (and gated on the same `advance` decision), a new step auto-creates `vX.(Y+1).0-pre0` **on the greened stable RELEASE_COMMIT** as an ANNOTATED tag with a defined non-empty body (`git tag -a … -m "<body>"`) — a lightweight tag would red the goreleaser job's release-notes extraction step (release.yml:196-213, which hard-errors on an empty tag body) BEFORE the edge binary builds — and pushes it via the PAT (`HOMEBREW_TAP_TOKEN`-class token that re-triggers workflows). The defined body: `"Edge prerelease auto-cut with stable vX.Y.0: publishes the X.(Y+1)-line edge binary so spacedock@next realigns with next's post-release skills. No standalone changes."` Its release run reuses the existing prerelease path end-to-end: `e2e-gate` resolves `git rev-list -1 vX.(Y+1).0-pre0` to the stable commit's SHA → matches the SAME green run that cleared the stable cut (no waiver); goreleaser builds+publishes the edge prerelease and bumps `spacedock@next` (`skip_upload: false` already publishes the edge cask on prereleases) to a binary stamped minor `X.(Y+1)` → window closes; the pre0 run's own edge-advance decision skips (`pre0 < next`'s `pre1` → `advance=false`) → no rewind, no re-tag; recursion terminates at one level (auto-tag is stable-path-only, and its `-pre0` output routes to the prerelease path where the decision is `skip`).

**Correction 1 — boundary is strict `>`, not `>=` (the addendum's `>=` is wrong).** Every legitimate forward stable cut yields `dev-preversion(tag)` strictly greater than next's manifest (a forward cut of `X.(Y+1).0` when next is `X.(Y+1).0-pre1` gives `X.(Y+2).0-pre1 > X.(Y+1).0-pre1`). Equality `dev-preversion(tag) == next` ONLY ever arises for a patch release (e.g. next `0.26.0-pre1`, tag `v0.25.1` → `dev-preversion=0.26.0-pre1`), which must SKIP. `>=` would FIRE on that equality and clobber next via `-X theirs`. Guard MUST use strict `>`.

**Correction 2 — auto-tag goes on the greened stable RELEASE_COMMIT, not the "stamped next commit" (staff-review CONFIRMED).** Deduced from the `e2e-gate` contract (not an executed spike): the addendum's "auto-tag on the stamped next commit" places the pre0 tag on an UNGREENED SHA → run 2's `e2e-gate` finds no green Runtime Live E2E run for it → goreleaser (`needs: e2e-gate`) never runs → no edge binary → window stays open. Tagging the stable RELEASE_COMMIT reuses its existing green run. The pre0 build is then the greened stable tree relabeled by ldflag version (not by any tree-manifest edit) — exactly the audited bits, nothing unaudited ships. The e2e-gate SHA-reuse is confirmed against a scratch tag in the CI dry-run spike below.

**Correction 3 — RETRACTED. `GORELEASER_CURRENT_TAG` is an OPTIONAL defensive pin, not a required fix.** My cycle-1 spike measured `git describe --tags` on the dual-tagged (`v0.25.0` + `v0.26.0-pre0`) commit — it returns `v0.25.0` both orders — and I WRONGLY inferred goreleaser would mis-stamp the edge binary. `git describe` is not goreleaser's version source. The independent staff review ran the REAL tool: goreleaser 2.16 derives its current tag from `git tag --points-at HEAD --sort -version:refname` (highest-semver tag pointing at HEAD), so it picks `v0.26.0-pre0` and stamps the edge binary minor `0.26` correctly with NO override, in both tag-creation orders → the window closes on the default. Adding `GORELEASER_CURRENT_TAG: ${{ github.ref_name }}` remains worthwhile as a belt-and-suspenders pin (deterministic, harmless on run 1 where only `v0.25.0` is present, harmless on single-tag runs) but it is NOT load-bearing. Lesson banked (this workflow's own proof policy): model-of-a-tool is never evidence about the tool — the real binary-version claim rides the CI dry-run spike below, run WITH and WITHOUT the pin, not a modeled fixture.

Guard comparison uses a new prerelease-aware `ComparePreVersion` in `internal/release` (the existing `contract.semverCompare` is dotted-int only and fails to parse `-preN`, so it cannot order `pre0 < pre1` — the exact distinction the recursion-skip needs).

### Acceptance criteria (each with its test)

- **AC-1 (VALUE, moves the recorded baseline right).** After a latest-line stable cut `vX.Y.0`, the edge channel self-heals with zero manual steps: the auto-pre0 mechanism drives the `spacedock@next` binary to minor `X.(Y+1)`, matching the `X.(Y+1)` required-minor the stable cut stamped into next's skills, so the FO version gate passes within one automated cycle. Baseline that can move the wrong way: the 2026-07-04..07 window — post-0.24.0 edge state is binary minor `0.24` vs required `0.25` (mismatch → every edge boot aborts, indefinitely, until a hand-cut prerelease). Target: the post-cut auto-pre0 run brings binary minor to `X.(Y+1)` == required `X.(Y+1)`.
  - **Test (two layers, NO tool modeling — the cycle-1 error).** (1) Version-algebra unit test in `internal/release`: for a stable cut `vX.Y.0`, `dev-preversion` stamps next's FO-prose required minor to `X.(Y+1)` AND the auto-pre0 tag's own version is `vX.(Y+1).0-pre0` (minor `X.(Y+1)`), so the required-minor and the pre0 binary's minor are equal BY CONSTRUCTION — asserted over a table of `X.Y`. (2) release.yml structure guard: the stable path creates+pushes a `vX.(Y+1).0-pre0` tag whose release run rides the existing prerelease publish path (`skip_upload: false` edge cask). The ACTUAL binary-version measurement — that goreleaser stamps `cli.Version=0.26.0-pre0` on the dual-tagged commit — is the CI dry-run spike (below), run WITH and WITHOUT `GORELEASER_CURRENT_TAG` to confirm the default already stamps correctly and the pin is harmless. No unit fixture models goreleaser's tag resolution (the retracted Correction 3's divergence twin is DELETED — it proved a fiction).

- **AC-2 (guard fires forward, skips backward, and gates the WHOLE job).** `edge-advance` advances next only when the tag's target edge version is strictly greater than next's current manifest version; an older-line tag skips EVERY downstream step — both reconcile steps, the calendar-bump+push, and the auto-pre0 step — and logs a `::notice::`. The calendar key is NOT bumped on a skip (ASK 2: previously the bump was unconditional; an old-line patch would otherwise still churn every edge installer's re-pull and push a bump commit onto next).
  - **Test:** `edge-advance-decision` table unit test — forward stable (`next 0.26.0-pre1`, `v0.26.0`→advance), old-line patch next-ahead (`0.27.0-pre1`, `v0.25.1`→skip), patch on just-released line (`0.26.0-pre1`, `v0.25.1`→skip), pre0-vs-next (`0.26.0-pre1`, `v0.26.0-pre0`→skip), major bump (`0.27.0-pre1`, `v1.0.0`→advance); plus `ComparePreVersion` ordering cases (`0.26.0-pre1 < 0.26.0`, `pre1 < pre2`, core dominates). A release.yml structure guard asserts a `decision` step emits `advance` and that BOTH reconcile steps, the calendar-bump step, AND the auto-pre0 step gate on `steps.decision.outputs.advance == 'true'`; adversarial twin removes the gate from the calendar-bump step and reds. NOTE for the implementer: this flips the existing `assertEdgeAdvanceWiring` invariant that the bump-calendar step is unconditional (`bump.ifCond == ""`) and its `ifSelectsPrerelease`/`ifSelectsStable` exact-string matchers must tolerate the `&& steps.decision.outputs.advance` conjunct — both are intentional test updates, not regressions.

- **AC-3 (three patch failure modes cannot regress next).** For `vX.Y.1` cut from an older line while next is on the `(Y+1)` line: (a) the reconcile is skipped so no `-X theirs` merge clobbers next's newer content; (b) next's manifest/gate-line version is not rewound; (c) no colliding `vX.(Y+1).0-pre0` auto-tag is attempted; (d) the marketplace calendar key is NOT bumped (ASK 2), so no edge installer re-pulls on an old-line patch.
  - **Test:** an `edge_reconcile`-style fixture puts next on `0.27`-dev with exclusive content, cuts `v0.25.1`, runs the guarded edge-advance, and asserts next's tip is byte-identical — no new commit; content, manifest, FO-prose minor, AND the marketplace calendar key all unchanged — and no pre0 tag was created. Adversarial: with the decision gate removed, the same fixture shows `-X theirs` clobbers next, the stamp rewinds the manifest, and the calendar key advances — proving the gated decision is load-bearing on all four counts.

- **AC-4 (always-cut-pre0 closes the window, terminates, ships only audited bits).** A latest-line stable cut auto-creates an ANNOTATED `vX.(Y+1).0-pre0` tag (non-empty body) on the greened RELEASE_COMMIT and pushes it via the re-triggering PAT; that run publishes an edge prerelease + bumps `spacedock@next` to a binary stamped minor `X.(Y+1)`; its own edge-advance decision skips (`pre0 < pre1` → `advance=false`) → no rewind, no re-tag; recursion ends at one level; the pre0 commit's tree == the greened stable tree.
  - **Test:** release.yml structure guard — the always-cut-pre0 step exists ONLY on the `!contains(github.ref,'-')` path and is gated on `steps.decision.outputs.advance`, tags `RELEASE_COMMIT` (asserted to be the e2e-gate-greened SHA source, not the next tip), creates an ANNOTATED tag with a non-empty body, and pushes via the PAT env; adversarial twins — (a) auto-tag on the next tip reds an "e2e-gate-reachable SHA" guard, (b) a lightweight (non-annotated / empty-body) auto-tag reds an annotated-body guard (else run 2's release-notes extraction hard-errors before the binary builds), (c) auto-tag on the prerelease path reds a recursion guard. A fixture confirms the pre0 tag routes to the prerelease path where the decision is `skip` → creates no further tag (termination), and that the pre0 commit SHA equals RELEASE_COMMIT (content == greened tree by construction). Real binary-version + notes-extraction success on the annotated tag are the CI dry-run spike, not this unit guard.

### Spike record (riskiest mechanism first — CI/release is high-stakes)

- **DONE (staff review, REAL tool): goreleaser tag resolution on a dual-tagged commit.** My cycle-1 local spike measured `git describe --tags` (returns `v0.25.0` both orders) and I WRONGLY inferred goreleaser would mis-stamp — `git describe` is not goreleaser's version source. The independent review ran the real tool: goreleaser 2.16 uses `git tag --points-at HEAD --sort -version:refname`, picks `v0.26.0-pre0` (higher semver), and stamps the edge binary minor `0.26` correctly with NO override, both tag orders. So the DEFAULT closes the window; `GORELEASER_CURRENT_TAG` is a defensive pin only. Proof-policy lesson banked (Correction 3): measure the tool, never model it.
- **TO SPIKE in implementation (named dry-run treatment).** (1) goreleaser building the edge binary at `vX.(Y+1).0-pre0` on the dual-tagged commit, run WITH and WITHOUT `GORELEASER_CURRENT_TAG` — via `goreleaser release --skip=publish,announce` (or `goreleaser build --single-target`) in a scratch clone, asserting the built binary's `--version` line reports `0.26.0-pre0` in BOTH runs (confirms the default is correct and the pin is harmless); run under the docs/releasing.md release-flow dry-run treatment. (2) the release-notes extraction step (release.yml:196-213) succeeds on the annotated auto-tag's body (no lightweight-tag hard-error). (3) `e2e-gate` SHA-reuse: `git rev-list -1 vX.(Y+1).0-pre0` == the greened stable SHA, confirmed against a scratch tag. (4) the auto-tag pushed via the re-triggering PAT actually fires a second release.yml run (vs GITHUB_TOKEN, which would NOT). A **detached adversarial audit** of the release.yml diff precedes the implementation gate, per the high-stakes-CI-surface policy.

### Test plan summary

Pure-Go unit + fixture tests in `internal/release` (edge-advance-decision table, `ComparePreVersion` ordering, AC-1 version-algebra equality table, AC-3 no-regress fixture, release.yml structure guards each with an adversarial twin) — cheap, no network, seconds. The goreleaser binary build, cask bump, annotated-tag notes-extraction, and e2e-gate SHA-reuse are the CI-level dry-run spike (minutes in a scratch clone), paid before implementation lands. No live release is cut to prove this, and NO unit fixture models goreleaser's tag resolution (the cycle-1 mistake). Estimated complexity: moderate — one new subcommand + one small compare fn + one `decision` step + one always-cut-pre0 step + `if:` decision-gates on the reconcile/calendar/pre0 steps (an optional `GORELEASER_CURRENT_TAG` env line), all backed by extensions to the existing `edge_reconcile_test.go` / `workflow_exec_guard_test.go` harness. Existing-test updates the implementer must make (intentional): `assertEdgeAdvanceWiring` no longer asserts the bump-calendar step is unconditional, and its `if:`-matchers must accept the `&& steps.decision.outputs.advance` conjunct.

### Doc diff — docs/releasing.md "Advancing the Edge Line (`next`)"

Replace the section's **Stable (`vX.Y.Z`) tag** bullet and add two paragraphs:

Before (stable bullet):
> - **Stable (`vX.Y.Z`) tag:** `next` is reconciled the same way, then stamped PAST the release to the post-release dev pre-version (`spacedock-release dev-preversion X.Y.Z` → `X.(Y+1).0-pre1`), so the edge line never masquerades as the stable version it just shipped, then the calendar key is bumped.

After (stable bullet + line-ordering guard + always-cut-pre0):
> - **Stable (`vX.Y.Z`) tag, latest line:** `next` is reconciled the same way, stamped PAST the release to `X.(Y+1).0-pre1`, the calendar key is bumped, and then an ANNOTATED `vX.(Y+1).0-pre0` tag is auto-created **on the greened release commit** and pushed (via the re-triggering tap PAT). That prerelease tag's own release run reuses the greened commit's e2e-gate pass, builds+publishes the `X.(Y+1)`-minor edge binary, and bumps the `spacedock@next` cask — so the edge binary's minor catches up to the skills' gate line within minutes instead of waiting for the next hand-cut prerelease. The auto-tag MUST be annotated with a non-empty body (the release-notes extraction step rejects a lightweight tag). Expect two GitHub releases per stable cut.
> - **Old-line / patch (`vX.Y.1`, or any tag whose target edge version is not strictly greater than `next`'s current manifest version):** the whole `edge-advance` job SKIPS (`spacedock-release edge-advance-decision` prints `skip`, logged as a `::notice::`; every downstream step is gated on the decision). `next`'s tip — content, manifests, gate line, and the marketplace calendar key — is left untouched, so no edge installer re-pulls. The patch updates only the stable cask; its fix reaches edge through the normal `main`→`next` flow, never through a `-X theirs` reconcile that would clobber `next`'s newer `(Y+1)`-line content or rewind its manifest/gate line. The auto-pre0 step is stable-latest-line-only and decision-gated, so a patch never attempts a colliding pre0 tag.
>
> The `vX.(Y+1).0-pre0` release run does not recurse: its `-pre0` tag routes to the prerelease path, whose `edge-advance-decision` skips (`pre0 < next`'s `pre1`), and the auto-pre0 step runs only on the stable path — so it neither re-tags nor rewinds `next`. goreleaser stamps the edge binary from the highest tag pointing at the commit (`git tag --points-at HEAD --sort -version:refname`), which is the `-pre0` tag, so the `X.(Y+1)` binary version is correct without an override; a `GORELEASER_CURRENT_TAG` pin may be set as belt-and-suspenders.

## Stage Report: ideation

- DONE: Design per the entity's recorded addenda — line-ordering guard as load-bearing fix + always-cut-pre0 + value AC moving the baseline right
  Body "## Ideation (2026-07-07)" AC-1 (value: post-cut edge binary minor == required minor, vs the 2026-07-04..07 abort baseline) through AC-4; guard covers the window's pre0 recursion AND all three patch modes.
- DONE: Spike-or-record the riskiest mechanism first; name the dry-run treatment + detached audit
  Local spike run: `git describe --tags` on a dual-tagged (`v0.25.0`+`v0.26.0-pre0`) commit returns `v0.25.0` both orders → forced Correction 3 (`GORELEASER_CURRENT_TAG` mandatory). Remaining goreleaser-build / e2e-gate-SHA-reuse spikes named under docs/releasing.md dry-run treatment + a detached release.yml audit.
- DONE: Each of the three patch failure modes gets a named proof + concrete doc diff for the Advancing-the-Edge-Line section
  AC-2/AC-3 name the proofs (old-line skip; no `-X theirs` clobber; no manifest/gate rewind), each with an adversarial twin; doc diff rewrites the stable bullet and adds the old-line/patch + recursion paragraphs.
- DONE: Surface anything the imminent v0.25.0 cut should do differently/avoid
  Prominent "FLAG FOR THE IMMINENT v0.25.0 CUT" callout: don't block the cut; expect the window to re-open; apply the manual `v0.26.0-pre1`-on-a-greened-commit remediation right after.

### Summary

Designed the fix from the recorded root-cause + design-analysis addenda, and the local tag-collision spike forced three corrections to the recorded design: the guard boundary is strict `>` not `>=` (equality only ever hits patch releases, which must skip); the pre0 auto-tag must sit on the greened stable RELEASE_COMMIT (the addendum's "next commit" placement is ungreened → e2e-gate blocks the pre0 build → window never closes); and goreleaser needs `GORELEASER_CURRENT_TAG` because `git describe` on the dual-tagged commit resolves to the stable tag. The value AC (AC-1) measures post-cut binary-minor == required-minor against the recorded abort baseline via a unit fixture with a divergence twin; the goreleaser binary build itself stays a named CI dry-run spike. Flagged prominently that v0.25.0 will re-open the window and needs the manual prerelease realign, since this fix won't land first.

### Feedback Cycles

- Cycle 1 (independent staff review, 2026-07-07): NEEDS MINOR REWORK — three surgical asks, no redesign. Corrections 1 (strict >) and 2 (pre0 on greened RELEASE_COMMIT; ldflag version, not tree manifest) CONFIRMED sound. Correction 3 EMPIRICALLY REFUTED: goreleaser 2.16 derives current_tag from `git tag --points-at HEAD --sort -version:refname` (higher tag wins), NOT `git describe` — the pre0 binary stamps correctly WITHOUT the override, both tag orders; the override downgrades to a defensive pin. ASK 1: fold the real-tool spike result in; fix AC-1 (no modeling goreleaser's tag resolution; delete the fiction-proving adversarial twin; real measurement rides the CI dry-run spike, run with AND without the override). ASK 2: the line-ordering skip must gate ALL downstream steps including the unconditional calendar bump (release.yml:345-353), else an old-line patch still churns every edge installer and AC-3's byte-identical-tip cannot hold. ASK 3 (critical-path, previously unstated): the pre0 auto-tag MUST be annotated with a non-empty body — the release-notes extraction step (release.yml:196-213) hard-errors on a lightweight tag, redding run 2 before the edge binary builds. Optional note adopted: the v0.25.0 cut flag should name dual-tagging the already-greened RELEASE_COMMIT as the cheapest realign path (empirically validated; boot gate is minor-exact BOTH directions, and the prerelease reconcile does not rewind next's stamp).

## Stage Report: ideation (cycle 2)

- DONE: ASK 1 — Correction 3 downgraded to a defensive pin with the real-tool result recorded; AC-1 re-scoped; fiction-proving twin deleted; spike DONE bullet's inference corrected
  Correction 3 now RETRACTED (goreleaser 2.16 = `git tag --points-at HEAD --sort -version:refname`, picks pre0, no override needed, both orders); AC-1 test is now version-algebra equality table + release.yml structure, real binary measurement moved to the CI dry-run spike run WITH and WITHOUT the pin; the divergence twin is deleted.
- DONE: ASK 2 — the line-ordering skip gates ALL downstream steps including the calendar bump
  Mechanism A now a job-level `decision` step; both reconcile steps + calendar-bump + auto-pre0 gate on `steps.decision.outputs.advance`; AC-2 adds the whole-job skip, AC-3 adds calendar-key to the byte-identical assertion (item d); flagged the intentional `assertEdgeAdvanceWiring` invariant flip for the implementer.
- DONE: ASK 3 — pre0 auto-tag specified ANNOTATED with a defined non-empty body
  Mechanism B names `git tag -a … -m "<defined body>"` and the release.yml:196-213 hard-error rationale; AC-4 structure guard asserts annotated+non-empty-body with a lightweight-tag adversarial twin; CI dry-run spike gains the notes-extraction check; v0.25.0 cut flag now names the dual-tag-the-greened-RELEASE_COMMIT cheapest path (annotated).

### Summary

Folded the three staff-review asks; no redesign. The load-bearing change is a proof-policy correction: my cycle-1 "Correction 3" modeled goreleaser via `git describe` instead of running it, and the reviewer's real-tool result (goreleaser picks the higher tag pointing at HEAD, so the default already stamps the pre0 binary correctly) retracts it to an optional pin — AC-1's adversarial twin that proved the fiction is deleted, and the real binary-version claim now rides the CI dry-run spike run with and without the pin. ASK 2 restructured Mechanism A into a job-level decision step gating every downstream edge-advance step (calendar bump included), so an old-line patch leaves next's tip byte-identical including the calendar key. ASK 3 pinned the pre0 auto-tag as annotated with a defined body to clear the release-notes extraction step. Corrections 1 (strict `>`) and 2 (pre0 on the greened RELEASE_COMMIT) held under adversarial scrutiny and are unchanged.

## Stage Report: implementation

- DONE: The gate-approved design lands — job-level decision step (strict `>` line-ordering via the new `ComparePreVersion`) gating ALL downstream steps incl. the calendar bump; always-cut-pre0 stable-path-only (ANNOTATED tag, defined non-empty body, on the greened RELEASE_COMMIT, PAT push); `GORELEASER_CURRENT_TAG` defensive pin; the intentional `assertEdgeAdvanceWiring` invariant flips applied
  `.github/workflows/release.yml` edge-advance job (new `decision` step + gated reconcile/bump/pre0 steps) + `internal/release/edge_advance_decision.go` (`ComparePreVersion`/`EdgeAdvanceDecision`/`Pre0EdgeVersion`) + `cmd/spacedock-release` `edge-advance-decision` & `edge-pre0-version`; matchers now tolerate the `&& steps.decision.outputs.advance` conjunct and the bump-unconditional assertion is removed. Commit 9638ce6f.
- DONE: The four named CI dry-run spikes run and recorded in a scratch clone (no live cut), per the docs/releasing.md dry-run treatment
  (1) goreleaser 2.16 built the edge binary on the dual-tagged (`v0.25.0`+`v0.26.0-pre0`) commit WITH and WITHOUT `GORELEASER_CURRENT_TAG`, in BOTH tag-creation orders → the built binary reports `0.26.0-pre0` in ALL FOUR combos (default is correct, pin is harmless — Correction 3's retraction confirmed on the real tool, not modeled). (2) The release.yml:196-213 notes-extraction shell on the annotated auto-tag body → non-empty; a lightweight tag yields an empty body (control reds). (3) `git rev-list -1 v0.26.0-pre0` == the greened stable SHA == RELEASE_COMMIT (e2e-gate reuses the green run). (4) PAT-vs-GITHUB_TOKEN re-trigger is GitHub server behavior — a `GITHUB_TOKEN`-authenticated push deliberately does NOT fire workflow runs, a PAT push does; NOT offline-runnable, so proven structurally by the `assertAlwaysCutPre0` PAT twin (twin d).
- DONE: All unit/fixture guards land with adversarial twins red-under-violation; docs diff applied; full offline suite green; committed on the branch
  `ComparePreVersion` ordering (incl. `pre0<pre1`, `pre2<pre10` numeric, malformed-rejects), `EdgeAdvanceDecision` table (incl. the three equality skip-cases + antisymmetry), AC-1 pre0/required-minor algebra table, AC-3 byte-identical-tip-incl-calendar fixture (+ ungated-clobber twin), release.yml structure guards (`assertEdgeAdvanceDecisionGating`: ungated-bump / ungated-reconcile / broken-emission twins; `assertAlwaysCutPre0`: greened-SHA / lightweight / recursion / PAT twins); docs/releasing.md "Advancing the Edge Line" diff applied. `go test ./...` = 15 packages ok / 0 fail; committed 9638ce6f on `spacedock-ensign/edge-channel-stable-cut-gap`.

### Summary

Implemented the cycle-2 design verbatim: a job-level line-ordering `decision` step (via the new prerelease-aware `ComparePreVersion`, since `contract.semverCompare` is dotted-int only and cannot order `pre0<pre1`) gates the WHOLE edge-advance job on strict `>`, so an old-line/patch tag leaves next's tip byte-identical (proven by the AC-3 fixture and its ungated-clobber twin); and an always-cut-pre0 step auto-cuts an annotated `vX.(Y+1).0-pre0` tag on the greened RELEASE_COMMIT via the re-triggering PAT, whose own run's decision skips (`pre0 < pre1`) so recursion ends at one level. The riskiest mechanism was spiked FIRST on the REAL goreleaser 2.16 in a scratch clone (banking the cycle-1 lesson: measure the tool, never model it) — all four version combos stamp `0.26.0-pre0`, confirming the default is correct and the pin is only belt-and-suspenders.

TWO ITEMS FOR VALIDATION / CAPTAIN: (a) Spike 4 (PAT re-trigger) is a live-GitHub-server behavior that cannot be exercised offline; it rides GitHub's documented no-recursion rule for `GITHUB_TOKEN` plus the structural PAT twin — a true end-to-end confirmation only lands on the first real stable cut. (b) DEPLOY PREREQUISITE: the auto-pre0 push needs a token with `contents: write` on `spacedock-dev/spacedock` AND PAT-class re-trigger capability. The step uses `HOMEBREW_TAP_TOKEN` (the design's named "HOMEBREW_TAP_TOKEN-class" token); confirm that secret's scope actually covers the MAIN repo (a classic `repo`-scoped PAT does; a fine-grained PAT scoped only to homebrew-tap does NOT — in which case a re-triggering PAT with main-repo write must be provisioned) before the first stable cut relies on the auto-realign.

## Stage Report: validation

- DONE: Reproduce every AC with independent evidence — decision-table equality skip-cases, ComparePreVersion ordering incl. pre0<pre1, AC-3 byte-identical-tip-incl-calendar fixture, every structure-guard twin RED under violation (re-ran, did not trust the report)
  Named tests green (`-v`): ComparePreVersion pre0<pre1 + numeric pre2<pre10 + malformed-reject; EdgeAdvanceDecision all 3 equality skip-cases; AC-3 fixture both subtests (skip→byte-identical incl. calendar / gate-removed→clobber+rewind). Re-confirmed the decision live on the REAL CLI: v0.26.0→advance, v0.25.1/v0.26.0-pre0/v0.26.1→skip, stdout exactly `advance\n` (8 bytes, no stderr leak).
- DONE: Every release.yml structure guard's adversarial twin (annotated-body, greened-SHA, recursion, calendar-gating) confirmed RED — plus an independent adversarial battery I authored
  Ran the implementer's twins (both structure-guard tests green ⇒ twins red correctly) AND my own 7-edit battery on the throwaway clone: ADV1 ungate-pre0, ADV2 rename-id, ADV3 drop-advance=false, ADV4 tag-HEAD-not-RELEASE_COMMIT, ADV6 non-PAT-token all CAUGHT. annotated-body + recursion also proven on real tools (Spikes 2/3 + CLI pre0→skip termination).
- DONE: Re-run four CI dry-run spikes on a scratch clone (no live cut) per docs/releasing.md dry-run treatment
  (1) REAL goreleaser 2.16.0 on the dual-tagged (v0.25.0+v0.26.0-pre0) commit → `spacedock 0.26.0-pre0` in ALL 4 combos (WITH/WITHOUT pin × both tag orders); control single-tag v0.25.0 → `0.25.0` (stamp tracks the tag, not a constant) — default correct, pin harmless, Correction 3 retraction confirmed on the tool. (2) Real notes-extraction shell: annotated body → non-empty GREEN; lightweight tag → exit 1 RED. (3) `git rev-list -1 v0.26.0-pre0` == greened stable SHA == HEAD (annotated tag dereferenced) → e2e-gate reuses the green run.
- DONE: Assess Spike-4 (PAT re-trigger, live-only) — judge whether the evidence level satisfies the AC or routes back
  Does NOT route back. The "PAT push fires a second run, GITHUB_TOKEN does not" property is GitHub server behavior, unreproducible offline by construction; the checkable part (right token variable) is bound by the PAT structure twin (my ADV6 + implementer twin d). Max achievable offline; end-to-end confirmation lands only on the first real cut.
- DONE: Detached adversarial audit on a throwaway checkout — explicit verdict; restate the operator prerequisite as a deploy condition
  Verdict PASSED. 5/5 realistic adversarial edits caught. Two probes (ADV5 reassign RELEASE_COMMIT after the greened line; ADV7 `|| github.actor` OR-escape) slip the substring/existence guards but require deliberately ADDING obfuscating lines — no plausible accidental refactor produces them, every realistic regression is caught; refuted nothing material. Guards are regression fences, not a boundary against a malicious editor (expected posture). Full offline suite green: 16 pkgs ok / 0 fail.

### Summary

RECOMMENDATION: PASSED (code), with one hard non-code DEPLOY CONDITION that must NOT be certified past. All four ACs carry independent, non-self-referential evidence; AC-1 is a genuine VALUE AC and the real goreleaser measurement moves the recorded baseline from binary-minor 0.24 vs required 0.25 (abort) to 0.26 == 0.26 (boots). The riskiest claim (Correction-3 retraction) was re-measured on the REAL tool, not modeled — the workflow's own proof-honesty lesson held. DEPLOY CONDITION: before the first stable cut relies on auto-realign, confirm `HOMEBREW_TAP_TOKEN` carries `contents:write` on the MAIN repo `spacedock-dev/spacedock` (a classic repo-scoped PAT does; a fine-grained tap-only PAT does NOT — the push would 403 and the window would stay open). Spike-4's PAT re-trigger is live-only and confirmed end-to-end only on that first real cut. Non-blocking bounds recorded: the AC-3 fixture mirrors the reconcile shell in Go rather than executing release.yml (mitigated — structure guards pin the wiring, the real CLI proves the decision output, Spikes 1-3 exercise the other shells); and the two immaterial guard-completeness probes above.
