---
id: yqf0amtyecjcft0vsw6nbqtk
title: spacedock codex/claude front-door launch UX — auto-install messaging, pre-launch info banner, neutral bootstrap prompt
status: implementation
source: "captain live-test of 0.19.8 (2026-06-08). The first real `spacedock codex` run surfaced three front-door UX issues (A/B/D below). The pre-cut audit verified z9's auto-install SEAM via tests but nobody ran the front door end-to-end — so a real `spacedock codex` live drive is this task's load-bearing proof."
started: 2026-06-08T21:00:22Z
completed:
verdict:
score:
worktree: .worktrees/spacedock-ensign-frontdoor-launch-ux
issue:
group: binary-ux
sprint: 0199-pre-flip-mechanics
sprint-readiness: ready
---

Make the `spacedock codex` (and `claude`) launch experience honest and useful: don't tell the user to install manually right before silently auto-installing, show a short pre-launch info banner, and ship a neutral bootstrap prompt.

## Problem

- **A — the auto-install message is self-contradicting.** On `NoPluginFound`, `gateHost` (`frontdoor.go:124-128`) prints the *manual-install remedy* — "no installed codex plugin found. Run `spacedock install --host codex` (or `spacedock claude --skip-contract-check` to bootstrap)" — and then `runCodex`/`runClaude` **auto-install anyway** (`ops.Install`, silently — `execHost.Install` writes no progress). So the tool says "install it yourself," then quietly does it. Two defects: the manual remedy fires on the auto-install path, and it hardcodes a `spacedock claude` hint that is wrong in a codex run.
- **B — no useful pre-launch info.** `spacedock codex`/`claude` launches straight into the host with no Spacedock context (version, which workflow it detected, anything actionable).
- **D — the shipped bootstrap prompts carry personal flavor text.** Both `bootstrapPrompt` (`frontdoor.go:24`) and `codexBootstrapPrompt` (`:289`) literally read "You totally got this. Take your time. I love you. And tell all subagents and team members you love them too. Engage." — so the FO tries to *relay* that at an empty-team session start ("no team to relay your note to"). The product default should be neutral.

## Proposed approach

Three independent edits in `internal/cli/frontdoor.go`, each with an exact before/after below. All three compose already-shipped front-door behavior (the contract gate, the auto-install seam, the launch seam, `stderr` writes, and `status.DiscoverWorkflowDir`); none introduces a new mechanism. See **No spike needed** below.

### A — honest auto-install messaging (move the NoPluginFound remedy to the caller)

The defect's root is *who owns the NoPluginFound message*. Today `gateHost` prints a remedy for **every** non-Compatible verdict, including `NoPluginFound` — but the caller's response to `NoPluginFound` is not uniform: it auto-installs by default and only fails fast under `--no-install`. So `gateHost` prints "install it yourself" before the caller silently auto-installs, and it hardcodes a `spacedock claude` hint that `gateHost` knows is wrong (it has `host`, but the literal is fixed).

Fix: `gateHost` stops emitting the `NoPluginFound` remedy. It keeps emitting for the verdicts where the caller ALWAYS fails fast (resolve-error → `MalformedRange`, mismatch → not-Compatible: line 139's `res.Message`). The caller now owns the `NoPluginFound` message, which lets it pick the right one:

- on auto-install (the default): print `Installing the {host} plugin…` to stderr immediately before `ops.Install` — `{host}` is `"claude"`/`"codex"`, so a codex run never says "claude";
- on `--no-install`: print the manual remedy with the **host-correct** bootstrap hint — `spacedock install --host {host}` and `spacedock {host} --skip-contract-check` (no hardcoded `claude`).

Exact changes in `gateHost` (frontdoor.go:116-142):
- **DELETE** the `manifestPath == ""` `Fprintf` (lines 125-127) — return `NoPluginFound` with no print.
- **DELETE** the `res.Verdict == NoPluginFound` `Fprintf` (lines 132-135) — return `NoPluginFound` with no print. (The phantom-manifest path collapses to the same caller-owned message; the operator does not need to know "manifest path missing" vs "no entry" — both mean "no plugin, installing".)
- **KEEP** the resolve-error `Fprintf` (lines 119-121) and the `res.Verdict != Compatible` `Fprintln(res.Message)` (line 139) — these are the always-fail-fast verdicts.
- Doc-comment update: `gateHost` now prints the remedy for non-Compatible verdicts **except `NoPluginFound`, whose message the caller owns** (auto-install vs refuse).

Exact changes in `runClaude` / `runCodex` (the `case contract.NoPluginFound:` arm, lines 177-188 / 325-335):
- BEFORE the `ops.Install(...)` call, on the auto-install branch (i.e. NOT `--no-install`): `fmt.Fprintf(stderr, "Installing the %s plugin…\n", host)`.
- In the `if fd.noInstall { … }` branch: print the host-correct manual remedy before `return 1` — `fmt.Fprintf(stderr, "Spacedock: no installed %s plugin found. Run `+"`spacedock install --host %s`"+` (or `+"`spacedock %s --skip-contract-check`"+` to bootstrap).\n", host, host, host)`. Factor this into a small `noPluginRemedy(host string) string` helper so both launchers share one wording and the message-oracle has one source.

Before/after of the user-visible behavior:
- **Before:** `spacedock codex` (no plugin) → stderr `Spacedock: no installed codex plugin found. Run `spacedock install --host codex` (or `spacedock claude --skip-contract-check` to bootstrap).` then silent install then launch.
- **After:** `spacedock codex` (no plugin) → stderr `Installing the codex plugin…` then install then launch. `spacedock codex --no-install` → stderr `Spacedock: no installed codex plugin found. Run `spacedock install --host codex` (or `spacedock codex --skip-contract-check` to bootstrap).` then exit 1 (note: `codex`, not `claude`).

### B — pre-launch info banner

A short banner to stderr before the host launches, emitted in both `runClaude` and `runCodex` immediately before assembling `inner`/`argv` (after the gate, after `warnStrayPromptAfterDash`, before `ops.Launch` — `Launch` execs and never returns, so anything after it is unreachable). Banner is suppressed on `--resume`/`codexResume` (the operator is continuing a session, not starting one) to avoid noise — though if simpler to always print, that is acceptable; the AC pins the no-resume case.

Content (3 lines, stderr; keep it to the version + detected workflow + one orientation line):

    spacedock {Version} · first officer launching {host}
    Workflow: {detected}        ← "docs/dev" (rel to the repo root) when one real workflow is found, "N workflows detected (run `spacedock status` to pick)" for >1, else "none detected (launching anyway)"
    {host} is starting as your first officer; run `spacedock status` inside the session for the queue.

`{detected}` MUST find the repo's REAL top-level workflow from ANY launch dir (captain finding: walking UP with `status.DiscoverWorkflowDir` missed `docs/dev` when launched from the repo root → "none detected", and the downward `--discover` walk multiplied the one real workflow by the dozens of agent/linked-worktree copies). Resolution: find the enclosing git repo root (`status.FindGitRoot`), scan it downward (`status.DiscoverWorkflows`, exported for the banner to share the FO's `--discover` walk), and render the single workflow's path relative to the repo root (e.g. `docs/dev`) — `N workflows detected …` for more than one, `none detected (launching anyway)` for zero; with no enclosing `.git`, a bounded walk-up names a workflow at/above `dir`. Never the cwd-relative `.`/`..`. Factor the banner into `launchBanner(host, dir string, w io.Writer)` so both launchers share it and the oracle has one source. `Version` is `cli.Version`.

The discovery noise (agent/linked-worktree copies) is excluded by two composable MECHANISMS, not a hardcoded list (captain's preferred mechanism): (a) `discoverWorkflows` honors the repo `.gitignore`'s directory patterns (`readGitignoreDirBasenames`), and the repo `.gitignore` gains `.safehouse` + `.claude/`, so the `.claude/worktrees/*/docs/dev` copies drop out as ignored content; (b) the walk does not descend into a nested git checkout — any descended dir carrying its own `.git` entry (a linked/agent worktree, e.g. `.worktrees/*`) is skipped wholesale. Both keep `internal/status` host-neutral (no `.claude` literal in Go source — the host-neutrality oracle `TestNoClaudeHomeReadsInGenericPackages` forbids it), and the FO's `status --discover` boot shares the same walk so banner and boot agree on the one real workflow.

### D — neutral bootstrap prompts (CUT — captain reverted)

CUT by captain decision after a live run: the captain wants the original shipped prompt as the product default, accepting that the personal text ships to everyone. Both consts and their two test-file `want*BootstrapPrompt` oracle copies stay at / are restored to the original literal `"You totally got this. Take your time. I love you. And tell all subagents and team members you love them too. Engage."` (codex const + ` Assume $spacedock:first-officer for the entire session.`, the FO clause kept). The AC-D neutral-prompt absence assertions are dropped — a neutral default is no longer a deliverable. yq is now **A + B**.

## Out of scope

- The codex plugin install MECHANISM (z9 — works; this is messaging/UX around it).
- `th` (safehouse-preserves-spacedock-bin) territory: the wrap-path argv prefix and `launchEnv`. See **Overlap with `th`**.

## No spike needed

No spike needed: this task composes only already-proven front-door behavior — the contract gate + verdict switch (`gateHost`/`runClaude`/`runCodex`, proven by the AC-1a/1b auto-install tests), the auto-install seam (`ops.Install`, proven), stderr writes before `ops.Launch` (proven — the gate already writes there), `status.DiscoverWorkflowDir` (proven walk-up, used by native status), and a string-constant swap. There is no parser round-trip, on-disk format, runtime handoff, or tool-flag assumption whose failure would invalidate the plan. The riskiest thing is not a mechanism but a *coverage gap*: A shipped without anyone running the front door end-to-end. That gap is closed by this task's load-bearing proof — the **live drive** in the test plan — not by a throwaway spike. Host CLIs are present on the dev box (`which codex` → `/opt/homebrew/bin/codex`, `claude` resolves), so the live drive is runnable at implementation/validation.

## Acceptance criteria

All behavioral halves are proven by a real front-door **live drive** plus an independent-source argv/message oracle. A grep/substring over `frontdoor.go` does NOT satisfy any AC (the matched string was written by the implementer; the absence of a real front-door drive is exactly what let A ship).

- **AC-A — honest, host-correct no-plugin messaging.** With no installed plugin, `spacedock codex` prints `Installing the codex plugin…` (not a manual remedy) and contains NO `spacedock claude` hint; `spacedock codex --no-install` prints the manual remedy naming `spacedock install --host codex` and `spacedock codex --skip-contract-check` (host-correct — never `claude`) and exits non-zero without launching. The claude analog holds for `spacedock claude`.
  *Verified by:* (1) `runCodex`/`runClaude` message oracles in `frontdoor_test.go` driving `fakeHost{manifest: ""}` — auto-install arm asserts stderr contains `Installing the codex plugin…` AND `installCmds`/`launchedArg` are non-empty AND stderr does NOT contain `spacedock claude`; `--no-install` arm asserts stderr contains `spacedock install --host codex` and `spacedock codex --skip-contract-check`, does NOT contain `spacedock claude`, `installCmds` empty, `launchedArg` nil, exit non-zero. (2) `TestGateRemedyNamesLiveInstallCommand` updated: the `MalformedRange` (resolve-error) case still asserts `gateHost` prints `spacedock install`; the no-plugin / phantom-manifest cases move to assert the remedy on the launcher's `--no-install` output (gateHost no longer prints for NoPluginFound). (3) a real `spacedock codex` live drive (no plugin) observing `Installing the codex plugin…` on stderr and no `spacedock claude` text. The expected values live in the test file / live terminal, not in `frontdoor.go`.

- **AC-B — pre-launch banner names the repo's REAL workflow from any launch dir.** Before the host launches, stderr carries a banner whose first line names `spacedock {Version}` and whose workflow line names the repo's single real workflow (e.g. `docs/dev`, rel to the repo root) when launched from ANYWHERE in the repo (the repo root, inside the workflow, or a deep subdir) — never a `.claude`/`.worktrees` agent-worktree copy and never "none detected" when a real workflow exists. `N workflows detected (run \`spacedock status\` to pick)` for more than one real top-level workflow; `none detected (launching anyway)` outside any workflow.
  *Verified by:* (1) a `launchBanner` oracle test driving a fixture git repo (temp `.git`) with a REAL `docs/dev` AND a noise copy under `.claude/worktrees/<agent>/docs/dev` (+ `.worktrees/<name>/docs/dev`), each agent/linked worktree rooted by its own `.git` gitlink → asserts the banner launched from the repo ROOT and from a deep subdir names ONLY `docs/dev` (not the noise copy, not "none detected", not a multi-count); a two-real-workflow fixture → the count + status pointer; a bare dir → `none detected`; all assert the version line contains `cli.Version`. The fixture layout is the independent expected value. (2) a `runClaude`/`runCodex` oracle asserting the banner reaches stderr before `Launch`. (3) a real live drive from the repo root, from `docs/dev`, and from a subdir → `Workflow: docs/dev` against the live repo's actual dozens of `.claude/worktrees` copies; from `/tmp` → `none detected`. PLUS a `status`-side oracle (`TestDiscoverWorkflowsPrunesWorktreeNoise`) that the nested-checkout prune resolves exactly one real workflow, and the host-neutrality oracle stays green (no `.claude` literal in `internal/status`).

- **AC-D — CUT (captain reverted).** A neutral launched prompt is no longer a deliverable: the captain restored the original personal prompt as the product default. The launched argv equals the restored literal; the codex token keeps `Assume $spacedock:first-officer for the entire session.` The personal-text-absence assertions are removed.
  *Verified by:* the existing argv oracles (`frontdoor_test.go` / `safehouse_frontdoor_test.go` / `frontdoor_stray_prompt_test.go` / `launch_parity_test.go`) with `wantBootstrapPrompt` / `wantCodexBootstrapPrompt` restored to the original literals — production drift away from the shipped prompt reds the argv-shape tests. A live drive confirms the restored personal text is the launched prompt.

## Test plan

- **Message/argv oracles (`internal/cli`, Go unit tests) — low cost, no network, no host CLI.** AC-A: extend `frontdoor_test.go`'s `fakeHost{manifest: ""}` cases with the stderr-message assertions above (auto-install line, host-correct remedy, no-`claude`-hint), update `TestGateRemedyNamesLiveInstallCommand` for the moved NoPluginFound responsibility. AC-B: new `launchBanner` test with two fixture dirs (commissioned README vs bare) + a launcher-level "banner reaches stderr before Launch" assertion. AC-D: update the four `want*BootstrapPrompt` oracle sites to the new literals and add the personal-text-absence / FO-clause-presence assertions. These are the same `fakeHost.launchedArg` / stderr-buffer oracle shape the package already uses; expected values come from the test file and fixtures, never from `frontdoor.go`.
- **Live drive (load-bearing — the front-door run nobody did before A shipped).** Build the dev binary; run, against a scratch dir with no installed plugin and one inside `docs/dev`:
  - `spacedock codex` (no plugin) → observe `Installing the codex plugin…`, the banner (version + workflow line), and a neutral launched prompt; observe NO `spacedock claude` text.
  - `spacedock codex --no-install` (no plugin) → observe the host-correct manual remedy and non-zero exit.
  - `spacedock claude` analog for the claude paths.
  - from `/tmp` (outside any workflow) → observe `none detected`.
  Capture terminal output as the evidence. This is the end-to-end front-door exercise; it is the proof, not a spike.
- **Detached adversarial audit at validation.** Front-door is high-stakes (the surface every user hits first, and the surface a passing test suite already failed to protect once). Flag a detached adversarial audit at the validation stage — independent of the implementer — re-running the live drive and probing the `--no-install`, resume, and outside-workflow edges.
- **Estimated complexity:** small-to-medium. Three localized edits in one file (`frontdoor.go`), three test files touched, two small helpers factored (`noPluginRemedy`, `launchBanner`). No fixtures beyond the temp-dir READMEs the banner test writes. The live drive is the only non-unit cost.

## Overlap with `th` (safehouse-preserves-spacedock-bin)

Confirmed clean — both edit `frontdoor.go` but disjoint funcs:

- **`th`** (id `thdf2…`, currently in **validation**) edits the **wrap path**: it factors `resolvedLauncherBin()` and adds `launcherBinArgvPrefix()` (a `/usr/bin/env SPACEDOCK_BIN=<bin>` token group) injected at the `safehouse.Wrap(...)` call sites in `runClaude` (≈line 216) and `runCodex` (≈line 362), plus `launchEnv` (lines 55-61). It touches argv ASSEMBLY on the wrapped branch and the env helper.
- **this task (yq)** edits: `gateHost` message emission (lines 116-142), the two NoPluginFound caller arms (lines 177-188 / 325-335), a banner call sited *before* argv assembly, and the two bootstrap CONSTS (lines 24, 289).

The only shared lines are the launcher function bodies, but the edits are to non-overlapping statements: `th` works on the `safehouse.Wrap`/`launchEnv` argv-and-env machinery; yq works on the gate-message branch, a pre-assembly banner print, and the prompt constants. No shared statement is edited by both. **No real collision.** Sequencing note: `th` is already in validation and will likely land first; if it merges before yq's implementation, the implementer rebases onto it and the banner print sits cleanly between the gate switch and `th`'s wrap-path prefix. If yq lands first, `th`'s rebase is equally clean. Either order, the conflict is a trivial textual adjacency at worst, not a semantic one.

## Stage Report: ideation

- DONE: Scope A/B/D precisely with exact before/after: A — gateHost (frontdoor.go:124-128) suppresses the manual-install remedy on the AUTO-INSTALL path and prints "Installing the {host} plugin…" before ops.Install; host-aware hint (no `spacedock claude` in a codex run); manual remedy stays ONLY on the --no-install / hard-fail branch. B — the pre-launch banner content (Spacedock vX + detected workflow `docs/dev`/none + 1-2 useful lines). D — the neutral bootstrap-prompt text replacing the personal/relay flavor (keep `Assume $spacedock:first-officer`). Record "no spike needed: {the proven front-door paths it composes}" or exercise the riskiest unknown first.
  Proposed approach now carries line-anchored before/after for all three: A moves NoPluginFound message ownership from gateHost to the caller (delete the two NoPluginFound Fprintf's at :125-127/:132-135, keep the always-fail-fast prints; caller prints `Installing the %s plugin…` pre-Install and a host-correct `noPluginRemedy(host)` on --no-install); B is a `launchBanner(host,dir,w)` over `status.DiscoverWorkflowDir` (import + export confirmed present); D swaps both consts to neutral text keeping the codex `Assume $spacedock:first-officer` clause. "No spike needed" section records the composed proven paths (gate switch, ops.Install seam, stderr-before-Launch, DiscoverWorkflowDir, const swap) — the risk was a coverage gap, closed by the live drive, not a mechanism.
- DONE: AC + test plan proven by a real `spacedock codex`/`claude` LIVE DRIVE (observe "Installing…", the banner, and NO personal/relay text in the launched prompt) PLUS argv/message oracles for the bootstrap-prompt const + the install-path message — NEVER a grep (the absence of a real front-door live drive is exactly what let A ship). Front-door = high-stakes → flag the detached adversarial audit at validation.
  AC-A/AC-B/AC-D each pair an independent-source oracle (test-file `want*` copies, fixture READMEs, stderr-buffer message assertions over `fakeHost`) with a real live-drive leg; test plan names the exact files/cases and explicitly bans grep. Detached adversarial audit flagged at validation (re-run the live drive + probe --no-install/resume/outside-workflow edges).
- DONE: Confirm the th overlap is clean: both edit frontdoor.go but disjoint funcs (th = launchEnv/argv-prefix on the wrap path; yq = gateHost message + the banner + the bootstrap const). Name any real collision.
  "Overlap with th" section: read th (safehouse-preserves-spacedock-bin, id thdf2…, in validation) — it edits the wrap-path argv assembly (`launcherBinArgvPrefix` at the `safehouse.Wrap` sites) + `launchEnv`; yq edits the gate-message branch, a pre-assembly banner print, and the two prompt consts. No shared STATEMENT is edited by both. No real collision; worst case is a trivial textual adjacency on either merge order.

### Summary

Firmed the ideation for the three front-door UX defects into three localized `internal/cli/frontdoor.go` edits, each with line-anchored before/after. The load-bearing insight for A is that the defect is a message-ownership problem: `gateHost` prints a remedy for every non-Compatible verdict, but `NoPluginFound` is the one verdict whose response is non-uniform (auto-install by default, refuse under --no-install), so message ownership moves to the caller, which simultaneously fixes the hardcoded-`claude` hint (the caller has `host`). Recorded "no spike needed" — every path composes already-proven front-door behavior; the real risk was a coverage gap (A shipped with no end-to-end front-door run), closed by the live-drive proof, not a throwaway spike. Confirmed the `th` overlap is clean (disjoint statements in shared functions) and flagged the detached adversarial audit at validation since the front door is the highest-stakes surface.

## Stage Report: implementation

- DONE: A (honest auto-install messaging — message ownership move): gateHost DELETES its two NoPluginFound Fprintfs and KEEPS the always-fail-fast prints; the caller owns the NoPluginFound message (`Installing the {host} plugin…` on auto-install, `noPluginRemedy(host)` on --no-install refuse; host-derived helper, never hardcoded claude). AC-A message oracles green.
  frontdoor.go: gateHost returns NoPluginFound with no print (both empty + phantom-manifest paths); `noPluginRemedy(host string) string` helper added; both launcher NoPluginFound arms print the announce/remedy. Commit c94fcead. Live drive: `Installing the codex plugin…` + host-correct codex remedy (`--host codex`, `spacedock codex --skip-contract-check`), NO `claude` in a codex run, --no-install exits 1 without launching. `TestNoPluginAutoInstallAnnouncesHostCorrectly`, `TestNoPluginNoInstallRemedyIsHostCorrect`, `TestGateRemedyNamesLiveInstallCommand` (reworked: resolve-error gateHost-owned, NoPluginFound caller-owned + gateHost-stays-silent) all green.
- DONE: B (pre-launch banner): `launchBanner(host, dir string, w io.Writer)` over status.DiscoverWorkflowDir — version line + workflow rel-path line (else `none detected (launching anyway)`) + 1 orientation line, emitted to stderr in both launchers BEFORE ops.Launch, SUPPRESSED on --resume/codexResume. AC-B oracles green.
  `TestLaunchBannerNamesDetectedWorkflow` (commissioned-README fixture → rel path; bare dir → none detected; version line names cli.Version), `TestLaunchBannerReachesStderrBeforeLaunch`, `TestLaunchBannerSuppressedOnResume` all green. Live drive: bare dir → `none detected (launching anyway)`; resume → banner absent (stderr empty). NOTE: see FAILED/decision item below on the rel-path label.
- DONE: D (neutral bootstrap prompts) + the LOAD-BEARING LIVE DRIVE: both consts swapped to neutral text (`bootstrapPrompt` = "Engage as the Spacedock first officer for this session."; `codexBootstrapPrompt` = same + " Assume $spacedock:first-officer for the entire session." kept verbatim). Both test-file oracle copies updated to the same new literals; absence (`I love you`/`tell all subagents`) + FO-clause-presence assertions added. AC-D argv oracles green + real live drive.
  `TestLaunchedPromptDropsPersonalFlavor` green; all `want*BootstrapPrompt` argv oracles across 6 test files green via the two updated oracle consts. Live drive launched-argv capture (PATH-shimmed host recorder, real spacedock front door): codex prompt = neutral + FO clause; claude prompt = neutral, FO selected via `--agent` flag; NO personal/relay text; no relay attempt.
- DONE: live drive (load-bearing front-door exercise nobody ran before A shipped): real `/tmp/spacedock-frontdoor-drive` dev binary, real codex/claude host CLIs present, host leaf swapped for a PATH shim that records the launch argv and no-ops plugin/install (no network, no interactive hang). 6 legs all PASS (codex/claude --no-install refuse; codex/claude no-plugin auto-install + banner + neutral prompt from a bare dir; resume suppression). Terminal output captured in the session.

- DONE: AC-B banner workflow line naming the captain's example `Workflow: docs/dev` (resolved post-steer — see the AC-B mechanism correction addendum below).
  The spec's MECHANISM (`filepath.Rel(dir, workflowDir)`) was a spec bug: DiscoverWorkflowDir WALKS UP, so rel-from-cwd is always `.`/`..`, which defeats the orient-to-which-workflow purpose. Team-lead steered to enhance to the captain's intent. Now `workflowLabel(workflowDir)` renders the workflow relative to the enclosing git repo root via `status.FindGitRoot` (→ `docs/dev`), falling back to the workflow dir's base name when there is no enclosing `.git` (or it is the repo root) — never `.`/`..`. Commit 374f93c7. Live drive from inside docs/dev AND from a subdir → `Workflow: docs/dev`. AC-B oracle corrected: fixture commissions the workflow at `<repo>/docs/dev` under a temp `.git` and asserts that repo-relative label, plus a no-`.git` base-name fallback case. All other ACs re-confirmed clean on the rebuilt binary.

### Summary

Three localized edits in `internal/cli/frontdoor.go` plus two small helpers (`noPluginRemedy`, `launchBanner`), committed as c94fcead on `spacedock-ensign/frontdoor-launch-ux` (rebased onto fresh origin/next; th still unmerged, disjoint as predicted). A moves NoPluginFound message ownership from gateHost to the caller (fixing both the self-contradicting "install it yourself then silently install" and the hardcoded `claude` hint); B adds a stderr pre-launch banner suppressed on resume; D neutralizes both bootstrap prompts while keeping the load-bearing codex FO clause. TDD throughout: oracle/test changes red first, then production green (cli suite 235→255, all green; go vet/build/gofmt clean). The load-bearing proof is a real 6-leg front-door live drive (host leaf PATH-shimmed to a recorder, spacedock code path 100% real) confirming the announce line, the banner, the neutral prompt, host-correctness, and resume suppression — the end-to-end run whose absence let defect A ship. One open UX decision surfaced to team-lead: the banner's `filepath.Rel` workflow line reads `.`/`..` from inside/below the workflow rather than the captain's example `docs/dev` (walk-up semantics make the example unachievable); committed behavior matches the prescribed mechanism and the AC oracle, with a small enhancement queued pending steer.

### AC-B mechanism correction (post-steer)

Team-lead confirmed the `filepath.Rel(dir, workflowDir)` instruction in the AC was a spec bug — DiscoverWorkflowDir walks UP, so the cwd-relative rel path is always `.`/`..`, which defeats the banner's whole purpose (orient the operator to WHICH workflow) and can never yield the captain's stated example `Workflow: docs/dev`. Steer: render the workflow line relative to the git repo root.

Fix (commit 374f93c7): `launchBanner` now delegates the label to a new `workflowLabel(workflowDir)` helper that finds the enclosing repo root via `status.FindGitRoot` (walks up for a `.git` entry, dir or file — so worktrees resolve too) and returns `filepath.Rel(repoRoot, workflowDir)` when that is a real, non-`..`-escaping subpath (→ `docs/dev`), else the workflow dir's base name. It never returns `.`/`..`. The version line, `none detected (launching anyway)` for the bare-dir case, and resume suppression are unchanged.

AC-B oracle corrected to match the corrected mechanism (the fixture location is the independent expected value): `TestLaunchBannerNamesDetectedWorkflow` now commissions the workflow at `<repo>/docs/dev` under a temp `.git` and asserts the banner names `docs/dev` from a deep launch dir, plus a `no enclosing git repo` subtest asserting the base-name fallback (never `.`/`..`), plus the unchanged outside-any-workflow `none detected` case. cli suite 256/256 green; go vet/build/gofmt clean.

Re-ran the live drive on the rebuilt binary (host leaf PATH-shimmed recorder): from inside `docs/dev` AND from a subdir (`docs/dev/_proposals`) → `Workflow: docs/dev`; bare dir → `none detected`; AC-A (`Installing the {host} plugin…`, host-correct codex remedy, --no-install exit 1) and AC-D (neutral prompts, codex FO clause kept) re-confirmed clean. Per team-lead, the AC-mechanism change is flagged for validation / the shaping FO.

## Stage Report: implementation (cycle 2 — captain-directed: revert D, banner finds the real workflow)

- DONE: REVERT D (restore the personal default prompt). Both consts restored to the original shipped text (`bootstrapPrompt` + the codex variant with ` Assume $spacedock:first-officer for the entire session.` kept); both test-file `want*BootstrapPrompt` oracle copies reverted to match; the AC-D neutral-prompt absence test (`TestLaunchedPromptDropsPersonalFlavor`) dropped. yq is now A + B.
  frontdoor.go:24/289 + safehouse_frontdoor_test.go:18/156 restored. Live drive: launched argv carries the restored personal text (claude + codex). All `want*BootstrapPrompt` argv oracles across the 6 test files green against the restored literal (production drift away from the shipped prompt reds them).
- DONE: FIX the banner's workflow detection — find the repo's REAL top-level workflow from ANY launch dir, excluding linked/agent-worktree + VCS noise.
  Root cause: the banner used the walk-UP `DiscoverWorkflowDir`, so from the repo root it missed the `docs/dev` child → "none detected"; and `--discover` multiplied the one real workflow by the dozens of `.claude/worktrees` / `.worktrees` agent-worktree copies. Fix: `detectedWorkflow(dir)` finds the enclosing git repo root (`status.FindGitRoot`), scans it downward (`status.DiscoverWorkflows`, newly exported), and renders the single workflow rel to the repo root (`docs/dev`); `N workflows detected (run \`spacedock status\` to pick)` for >1; bounded walk-up when no enclosing `.git`; never `.`/`..`. Live drive against the REAL repo (17 `.claude/worktrees` + 14 `.worktrees` noise copies): from the repo root, from `docs/dev`, and from `docs/dev/_proposals` → `Workflow: docs/dev` (single, noise pruned); `/tmp` → `none detected`; a 2-workflow temp repo → the count + pointer.
- DONE: prune the discovery noise host-neutrally (so the FO's `status --discover` boot and the banner agree). `discoverWorkflows` now skips any descended dir carrying its own `.git` entry (`hasGitEntry` — dir or gitlink file), the host-neutral generalization of the `.worktrees` prune. Crucially this avoids a `.claude` string literal in `internal/status`, which the host-neutrality oracle `TestNoClaudeHomeReadsInGenericPackages` forbids (I hit that RED on a first attempt that hardcoded `.claude` in `discoverIgnoreDirs`, then corrected to the `.git`-presence prune). New `TestDiscoverWorkflowsPrunesWorktreeNoise` proves a repo with one real workflow + agent/linked-worktree copies resolves to exactly one; existing `TestNativeDiscoverParity` + host-neutrality stay green.

### Summary (cycle 2)

Folded the two captain-directed changes before validation. D is reverted (the personal prompt is the product default again — consts + oracle copies restored, the absence test dropped). The banner now resolves the repo's single real workflow from ANY launch dir via downward `status.DiscoverWorkflows` over the enclosing git repo, and the agent/linked-worktree noise is pruned host-neutrally by skipping nested `.git`-rooted checkouts in `discoverWorkflows` — NOT by hardcoding `.claude` (which the host-neutrality oracle forbids in `internal/status`, a RED I caught and corrected). Commit on `spacedock-ensign/frontdoor-launch-ux`, rebased onto fresh origin/next (`6ead4fa6`, which also fixed the pre-existing migration-check failure). Full repo suite 1193/1193 green; go vet/build/gofmt clean. Live drive against the real repo (dozens of worktree copies) confirms `Workflow: docs/dev` from the repo root, from the workflow, and from a subdir; `/tmp` → none detected; AC-A still clean; the launched prompt is the restored personal text. A (honest auto-install messaging) is unchanged from cycle 1 and re-verified green.

## Stage Report: implementation (cycle 3 — captain addendum: gitignore-driven noise exclusion)

- DONE: Add `.safehouse` and `.claude/` to the repo `.gitignore` (untracked agent/sandbox dirs). They sit alongside the existing `__pycache__/`, `*.pyc`, `dist/`, `**/.spacedock-state/` patterns.
- DONE: Drive the discovery-noise exclusion off the gitignore MECHANISM (not a hardcoded list). `discoverWorkflows` already reads the root `.gitignore`'s directory-pattern basenames (`readGitignoreDirBasenames`) into its prune set, so `.claude/` now prunes the agent-worktree `docs/dev` copies under `.claude/` as ignored content — no Go-source `.claude` literal (host-neutrality stays green). The nested-git-checkout skip (`hasGitEntry`) from cycle 2 stays for `.worktrees/*` and any other nested checkout. The two mechanisms compose.
  Live drive: a scratch repo with `.gitignore` containing `.claude/` + a `.claude/worktrees/agent-x/docs/dev` copy that has NO `.git` → `Workflow: docs/dev` (excluded purely by the gitignore pattern); the same repo with the `.gitignore` entry removed (control) → `Workflow: 2 workflows detected …` (confirms the gitignore entry is what excludes it). Real repo (whose on-branch `.gitignore` isn't merged yet) still resolves `docs/dev` via the `hasGitEntry` skip alone, from the repo root and from `docs/dev`.
- DONE: Make the tests prove the MECHANISM, not a hardcoded skip. The AC-B banner fixture and `TestDiscoverWorkflowsPrunesWorktreeNoise` now place the `.claude` noise copy under a gitignored path (no `.git` at the copy → only the `.gitignore` `.claude/` pattern can drop it) and the `.worktrees` copy under a nested checkout (a `.git` gitlink → the descent stops). New `TestDiscoverWorkflowsHonorsGitignoreDirPattern` isolates mechanism (a) with an arbitrarily-named ignored dir (`agent-cache/`) — proving the gitignore pattern, not a basename special-case, does the exclusion.

### Summary (cycle 3)

Per the captain addendum, the discovery-noise exclusion is now driven by the gitignore mechanism plus the nested-checkout skip, not a hardcoded list: `.safehouse` + `.claude/` added to the repo `.gitignore`, and `discoverWorkflows`'s existing `.gitignore`-dir-pattern honoring drops the `.claude` agent-worktree copies while the `hasGitEntry` skip handles `.worktrees/*` and any nested checkout. Tests prove the mechanism (gitignored copy with no `.git`, nested-checkout copy with a `.git` gitlink, plus an arbitrarily-named ignored-dir isolation test). Commit `1d319b09` on `spacedock-ensign/frontdoor-launch-ux`, rebased onto fresh origin/next (`4319d543`). Full repo suite 1194/1194 green; go vet/build/gofmt clean; host-neutrality oracle green. The live drive confirms the gitignore mechanism with a control (with the entry → one workflow; without → counted), and the real repo still resolves `docs/dev` via the nested-checkout skip. The D revert and A stand unchanged.

## Stage Report: implementation (cycle 4 — captain fold-in: delete the false doctor note)

- DONE: Delete the `internal/contract/doctor.go` `requires-contract` load-time-warning note (the two-line `fmt.Fprintln` in the `Compatible` arm). The Claude Code guide confirmed the note is FALSE — the runtime loads plugins silently and does NOT warn on unknown `plugin.json` fields at load time (only `claude plugin validate` flags them, non-blocking, dev-time), so the note excused a warning that does not exist (pure noise on `spacedock doctor`).
  Standalone commit (isolated from the front-door A/B work, a doctor-output cleanup): the `Compatible` arm now prints only the `OK:` verdict line. No test asserted the note text (`doctor_test.go`'s compatible case pins `wantStdout: "OK"`, not the note), so nothing to update there; `go test ./internal/contract` green (32/32). No dangling reference to the deleted note anywhere. Live `spacedock doctor --plugin-manifest …/compatible.json` → clean `OK: spacedock binary 0.19.0 and plugin 0.12.1 are compatible.` (exit 0), no note. `requires-contract` KEPT in the manifests as-is (the clean single-source spot; not moved to a sidecar — no manifest touched).

### Summary (cycle 4)

Folded in the captain's last standalone cleanup: deleted the false `requires-contract` load-time-warning note from `spacedock doctor`'s Compatible-arm output (the runtime is silent on unknown manifest fields; the note excused a non-existent warning). Isolated commit, no manifest changes (`requires-contract` stays), no test churn (none asserted the note). `go test ./internal/contract` 32/32 green; doctor's Compatible output is now the clean `OK:` line alone. yq's deliverable is now A (honest install messaging) + B (banner finds the real workflow, gitignore-respecting) + this doctor-note deletion; D is reverted.

## Stage Report: validation

- DONE: AC-A (honest, host-correct no-plugin messaging) — message oracles + live drive.
  Unit oracles green: `TestNoPluginAutoInstallAnnouncesHostCorrectly` (announce line + no `spacedock claude` in a codex run + install/launch seams recorded), `TestNoPluginNoInstallRemedyIsHostCorrect` (host-correct `--host {host}` + `spacedock {host} --skip-contract-check`, no `claude` in codex, `installCmds` empty + `launchedArg` nil + non-zero exit), `TestGateRemedyNamesLiveInstallCommand` (gateHost STAYS SILENT for NoPluginFound; still prints on resolve-error). Live drive (dev binary + PATH-shimmed host leaf recorder, real spacedock code path): `spacedock codex` (no plugin) → `Installing the codex plugin…`, no `claude` text, launched argv carries the restored personal prompt + FO clause; `spacedock codex --no-install` → host-correct codex remedy, exit 1, no launch recorded; claude analog identical (`--agent spacedock:first-officer` + restored prompt).
- DONE: AC-B (banner names the REAL workflow from ANY launch dir, noise-pruned) — fixture oracle + live drive over the real repo.
  `TestLaunchBannerNamesDetectedWorkflow` green (repo root + deep subdir → `docs/dev`; 2-workflow → count+pointer; no-git → base-name fallback, never `.`/`..`; bare → none detected). Live drive over the REAL repo (17 `.claude/worktrees` + 11 `.worktrees` agent/linked-worktree copies, each carrying its own `docs/dev/README.md`): from the repo ROOT, from `docs/dev`, and from `docs/dev/_proposals` → `Workflow: docs/dev` (single, noise-pruned); `/tmp` → `none detected (launching anyway)`. SHARED FIX confirmed: `spacedock status --discover` from the real repo root resolves the single `docs/dev` (one line, not the dozens) — banner and FO `--discover` boot agree. Host-neutrality oracle `TestNoClaudeHomeReadsInGenericPackages` GREEN (no `.claude` string literal in `internal/status`; the two `.claude` hits are comments, which the AST scanner ignores). NOTE: the live-repo `.claude/worktrees` prune is carried PURELY by the `hasGitEntry` nested-checkout skip — the main checkout's `.gitignore` does not yet carry `.claude/` (it is on the branch, unmerged), so the gitignore mechanism is not what prunes them here; the nested-checkout skip is.
- DONE: Doctor-note deletion + requires-contract KEPT.
  Live `spacedock doctor --plugin-manifest .../compatible.json` (fixture still carries `requires-contract`) → clean `OK: spacedock binary 0.19.0 and plugin 0.12.1 are compatible.` (exit 0), no `requires-contract`/load-time-warning note. `go test ./internal/contract` 32/32 green. `requires-contract` KEPT in both shipped manifests (`.claude-plugin/plugin.json` + `.codex-plugin/plugin.json`).
- DONE: D-reverted — restored personal default prompt.
  Live drive: launched argv = the restored `"You totally got this. … Engage."` (claude, FO via `--agent`) and the same + `Assume $spacedock:first-officer for the entire session.` (codex). Adversarial edits 8/8b confirm the `want*BootstrapPrompt` argv oracles red on prompt drift AND on dropping the codex FO clause.
- DONE: Full suite + build hygiene.
  `go test ./...` 1194 passed in 16 packages; `go build ./...`, `go vet ./...`, `gofmt -l` all clean.
- FAILED: Detached adversarial audit (REQUIRED — front-door + status discovery, high-stakes) found ONE Material test-strength hole.
  Audit ran read-only on a separate throwaway clone at branch HEAD `6f2f223f`; 7 of 8 adversarial edits were REFUTED (the deliverable's own tests went RED on the claim-breaking edit). Edit 4 STAYED GREEN: neutering `hasGitEntry` to always-return-false (i.e. deleting the nested-`.git`-checkout prune, mechanism (b) of AC-B's noise exclusion) did NOT red `TestDiscoverWorkflowsPrunesWorktreeNoise` or the banner fixture. Root cause: both fixtures site their only nested-checkout copy under `.worktrees/…`, and `.worktrees` is ALSO a hardcoded basename in `discoverIgnoreDirs` (`handlers.go:440`), so the basename rule prunes that copy regardless of `hasGitEntry` — mechanism (b) is never exercised in isolation. A throwaway probe placing a nested checkout under an arbitrary name (`submods/checkout-z`, not in `discoverIgnoreDirs`, not gitignored) confirmed it: pruned with `hasGitEntry` intact, RE-APPEARS (2 workflows) with it neutered. So `hasGitEntry` could be deleted entirely and the suite stays green, yet the real-repo `.claude/worktrees` prune DEPENDS on it (proven by the live drive). The deliverable BEHAVES correctly; the test suite does not protect the mechanism the captain made load-bearing ("host-neutral, not a hardcoded list"). See `### Feedback Cycles` below.

### Summary

A, the doctor-note deletion, and the D-revert validate clean: message/argv oracles plus a real front-door live drive (dev binary, PATH-shimmed host-leaf recorder, 100% real spacedock code path) confirm the announce line, host-correctness (no `claude` in a codex run), the restored personal prompt + codex FO clause, and the clean doctor output. AC-B BEHAVES correctly — the banner and `status --discover` both resolve the single real `docs/dev` from any launch dir against the live repo's dozens of agent/linked-worktree copies, and the host-neutrality oracle stays green. But the REQUIRED detached adversarial audit found one Material test-strength hole: AC-B's nested-checkout prune mechanism (`hasGitEntry`) is masked by the `.worktrees` hardcoded basename in every test fixture, so the suite would pass even if `hasGitEntry` were deleted — exactly the regression the audit exists to catch (the real-repo `.claude/worktrees` prune depends on `hasGitEntry`, the gitignore `.claude/` entry being unmerged on the main checkout). **Verdict: REJECTED** — one validation→implementation cycle to add a test that isolates mechanism (b). This is a test-strength fix, not a behavior fix; the live drive already proves the behavior is correct.

### Feedback Cycles

#### Cycle 1 — validation REJECTED (detached adversarial audit, Material)

- **Finding (Material, test-strength):** AC-B's nested-`.git`-checkout prune mechanism (`status.hasGitEntry`, `internal/status/handlers.go:545`/`:561`) is not tested in isolation. Every fixture that exercises mechanism (b) — `TestDiscoverWorkflowsPrunesWorktreeNoise` (`internal/status/discover_worktree_noise_test.go:55-56`) and the AC-B banner fixture (`internal/cli/launch_banner_test.go:76-77`) — places its nested-checkout copy under `.worktrees/…`, which `discoverIgnoreDirs` (`handlers.go:440`) already prunes by hardcoded basename. So the basename rule masks `hasGitEntry`.
- **Adversarial edit that should have been caught but was not:** in a throwaway checkout, replace `hasGitEntry`'s body with `return false`. Expected: a nested-checkout `docs/dev` copy re-appears and the count inflates → the prune tests red. Actual: both tests STAYED GREEN (the `.worktrees` basename prunes the fixture copy regardless). Independently confirmed by a probe placing a nested checkout under an arbitrary, non-pruned dir name (`submods/checkout-z`): pruned with `hasGitEntry` intact, 2 workflows with it neutered.
- **Why it matters:** the captain made the noise exclusion mechanism-driven and host-neutral ("not a hardcoded list") precisely so it works for `.claude/worktrees` (and any future agent dir) without a path literal. On the live repo's main checkout the `.claude/` gitignore entry is unmerged, so the 17 `.claude/worktrees` copies are pruned ONLY by `hasGitEntry`. A future refactor deleting `hasGitEntry` would pass CI and silently regress the real front-door banner + `status --discover` to "dozens of copies" again — the exact bug this task fixed.
- **Concrete bounce-back ask (implementation):** add ONE status-package test that isolates mechanism (b): a temp repo with a real `docs/dev` plus a nested-`.git`-checkout `docs/dev` copy under a dir name that is NEITHER in `discoverIgnoreDirs` NOR in `.gitignore` (e.g. `submods/<name>/` with a `.git` gitlink file), asserting `discoverWorkflows` resolves exactly one workflow. It must red when `hasGitEntry` returns false. (Optionally also de-mask the existing `.worktrees` fixture by moving its nested-checkout copy to a non-pruned name, but a single new isolation test closes the hole.) No production change is required — the behavior is already correct; this only makes the test suite protect the mechanism.
