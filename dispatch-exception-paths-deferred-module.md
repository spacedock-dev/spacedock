---
title: "Exception paths (Degraded Mode, Break-Glass, dead-ensign detail) split out of claude-fo-dispatch.md into failure-triggered deferred modules"
status: validation
group: tooling
source: "fable-token-trim-scout analysis 2026-07-02: Degraded Mode (claude-fo-dispatch.md:87-117, ~780 tok), the Break-Glass manual-dispatch template (:42-52, ~520), and Context-Budget/Dead-Ensign detail (:128-149, ~650) load at first dispatch but fire only on failure (second dispatch failure, helper non-zero exit, budget-fail) — ~1.6-1.9k tok on every dispatching session's happy path. #457 just shipped the same non-user-invocable-skill pattern for adapter-less deferred modules."
id: 41cfak9bgwtpa01m1z4qkprq
started: 2026-07-02T03:02:51Z
worktree: .worktrees/spacedock-ensign-41cfak9bgw
mod-block: merge:pr-merge
pr: "#466"
---

## Problem
The Claude dispatch module front-loads its exception machinery: every session that dispatches even one worker carries the full Degraded Mode contract, the Break-Glass template, and the dead-ensign/budget-failure detail, though the happy path never fires them.

Measured at ideation (2026-07-02, `main` @ 59251f18): `skills/first-officer/references/claude-fo-dispatch.md` is 22,992 B total; the three exception spans are the Break-Glass block (lines 42-52, 2,082 B), `## Degraded Mode` (lines 87-117, 3,131 B), and `## Context Budget and Dead Ensign Handling` (lines 128-149, 2,607 B) — 7,820 B (~1.9k tok) loaded at every first dispatch, fired only on failure.

## Proposed approach

Move the three exception bodies into ONE non-user-invocable skill, `skills/fo-dispatch-recovery/SKILL.md`, loaded at its failure triggers. This is the #457 pattern followed exactly — shipped precedents: `skills/fo-status-viewer/SKILL.md` and `skills/fo-write-core/SKILL.md` (commit 59251f18, #457), and the earlier `skills/present-gate/SKILL.md`, `skills/feedback-rejection-flow/SKILL.md`, `skills/using-legacy-claude-team/SKILL.md`. Frontmatter: `user-invocable: false`; the `description` carries the when-to-load (the three failure triggers), mirroring fo-status-viewer's.

**Why one skill, not three.** Every SKILL.md description is resident in the available-skills list of EVERY session running the plugin (FO, ensigns, unrelated sessions) — three skills triple that plugin-wide overhead for paths that are rare and correlated (a helper failure can cascade into degraded mode). One load at failure time brings in ~1.9k tok of deferred context exactly when it is needed; the finer granularity of three skills buys nothing on the happy path. Multi-anchor skills are the shipped shape (fo-status-viewer carries three section anchors).

**Skill sections (bodies move verbatim, per #457's "section bodies verbatim"):**
- `## Degraded Mode` — the Triggers rationale, Effects invariants, the verbatim Captain Report Template, and the Cooperative Shutdown Sweep (including the feedback-cycle exemption).
- `## Break-Glass Manual Dispatch` — the full manual `Agent()` template block plus the "concrete Claude form of fo-dispatch-core.md's Break-Glass template" note.
- `## Context Budget Failure and Dead Ensign Handling` — the three budget-unavailable stderr conditions, the Recovery clause text, the four dead-ensign bullets, and the forward-family context-window note.

**What stays resident in claude-fo-dispatch.md** (the named reliability risk: exception time is when the FO is least reliable, so each resident trigger carries the FIRST ACTION, never a bare pointer; the pre-invocation guards stay resident):
- `## Awaiting Completion` — untouched (guards the common wait path).
- The context-budget probe invocation and the fail-safe rule — reuse-condition-0 runs on the happy path.
- The Model-to-context mapping paragraph — reuse-condition-4's comparator and the canonical model enum (values not quoted here: the `wc` entity amends the enum before this task merges; take the landed text verbatim); `fo-dispatch-core.md:93` names "the Claude enum in `claude-fo-dispatch.md`" and reuse-condition-4 runs on every happy-path reuse decision, so the enum cannot move to a failure-time skill. Post-split the enum appears once (today it is duplicated at lines 52 and 140).
- `## Terminal Worker Teardown`, `## Feedback Rejection Flow (bare mode)`, `## Event Loop`, the Spawn Call, and the Worker Back-Channel — untouched.

### Resident trigger lines: before/after diffs

**Diff 1 — Break-Glass (replaces lines 42-52, 2,082 B → 444 B measured).** Before: the full bolded intro + 8-line `Agent()` template + closing note. Note: this span directly overlaps the `jh` entity's break-glass demotion, which merges first — re-derive this diff against jh's landed text (see the merge-order dependency below). After:

> **Break-Glass Manual Dispatch (ONLY when `spacedock dispatch build` exits non-zero or is unavailable):** never hand-assemble a dispatch while the helper works. First action: report the helper failure (command, exit code, stderr) to the captain. Then `Skill(skill="spacedock:fo-dispatch-recovery")` and fill its `## Break-Glass Manual Dispatch` template; the conditional `model=` slot draws from the canonical enum in `## Context Budget` below.

**Diff 2 — Degraded Mode (replaces lines 87-117, 3,131 B → ~715 B with the pre-trip-names clause; 660 B measured without it).** Before: `## Degraded Mode` + `### Triggers` + `### Effects` + `### Captain Report Template` + `### Cooperative Shutdown Sweep`. After:

> ## Degraded Mode (trigger)
>
> Any ONE of these trips Degraded Mode — a session-wide, irreversible fallback to sequential bare dispatch: (1) any SECOND dispatch failure within the session (no time window, no counter — deliberate: the FO cannot reliably track failure timestamps); (2) the captain command `/spacedock bare`; (3) `Agent` or `SendMessage` themselves unavailable. On trip, FIRST ACTION: stop all named/background dispatch for the remainder of the session and do not route `SendMessage` to any pre-trip worker name. Then `Skill(skill="spacedock:fo-dispatch-recovery")` and follow its `## Degraded Mode` section: the bare-dispatch invariants, the verbatim captain report, and the cooperative shutdown sweep.

The "do not route `SendMessage` to any pre-trip worker name" clause is a deliberate ~55 B spend against the AC-1 margin: the no-reuse-of-pre-degrade-names prohibition otherwise lives only in the moved Effects, and a failed skill load must not leave the FO free to advance work to a presumed-zombified name.

**Diff 3 — Context Budget (replaces lines 128-149, 2,607 B → ~1,350 B measured).** Before: probe check + three stderr bullets + mapping paragraph + recovery clause + four dead-ensign bullets. After:

> ## Context Budget
>
> This is the Claude realization of reuse-condition-0's budget probe (and the feedback-rejection budget check); Codex declares none.
>
> **Context budget check:** Run `${SPACEDOCK_BIN:-spacedock} dispatch context-budget --name {ensign-name}`. Parse the JSON output. `reuse_ok: true` → reuse may proceed. `reuse_ok: false`, or ANY non-zero exit with no reading, is fail-safe — never silent-reuse: log to captain and fresh-dispatch. Before the replacement dispatch (budget-fail, zombie, or dead ensign), `Skill(skill="spacedock:fo-dispatch-recovery")` — its `## Context Budget Failure and Dead Ensign Handling` section carries the recovery clause for the prior worktree and the dead-ensign rules.
>
> **Model-to-context mapping:** [the line-140 paragraph retained verbatim as landed at merge time — reuse-condition-4's comparator, the canonical enum (do NOT re-quote its values here: `wc` amends the enum and merges first), the fallback re-stamp rule]

### Why each load point is safe at failure time

- **Degraded Mode:** its triggers are team-substrate failures (`Agent`/`SendMessage` down, repeated dispatch failure) — but `Skill()` is a harness-native local file read with no dependency on the team substrate, so the load path is disjoint from the failure that fired it. The safety-critical invariants (stop background dispatch, session-wide, irreversible, no messaging pre-trip worker names) are on the resident trigger line itself, and the host-neutral degraded effects are ALSO resident via `fo-dispatch-core.md`'s `## Dispatch Adapter` (the "no `team_name` on subsequent dispatch" effect and the blocking-dispatch clause, which claude-fo-dispatch.md:103-104 cites today): if the skill load somehow failed, the FO has already halted team dispatch and degrades to halt-and-report, never to continued wrong dispatch or to routing an advance to a presumed-zombified name.
- **Break-Glass:** the trigger is a helper-binary failure (`spacedock dispatch build` non-zero), orthogonal to skill loading. The first action (report to the captain) needs no loaded body; a failed load leaves the FO already talking to the captain with the failure surfaced.
- **Budget-fail/dead-ensign:** the trigger is a probe-binary failure or reading, orthogonal to skill loading. The fail-safe decision (fresh-dispatch, never silent-reuse) is resident (and restated host-neutrally at `fo-dispatch-core.md:34`); the skill supplies only the recovery-clause text and dead-ensign bookkeeping, so a failed load cannot cause silent-reuse.

### Registry, re-points, and doc diff

**`## Deferred load points` registry** (`skills/first-officer/references/first-officer-shared-core.md:35-42`) gains a fifth row (additive — the fold test's four tokens and greet-guard clause survive):

> - `Skill(skill="spacedock:fo-dispatch-recovery")` — dispatch failure recovery (Degraded Mode, break-glass manual dispatch, budget-fail/dead-ensign handling); named at its triggers inside the Claude dispatch module — no boot and no happy-path dispatch loads it.

This row is also what keeps `TestDeferredSkillCoresResolveAndCarryCeremony` (which hardcodes the shared core as sole namer) working unmodified once the skill joins `deferredSkillCores`.

**Re-points off the moved sections:**
- `skills/using-legacy-claude-team/SKILL.md:25` — before: "…before `claude-fo-dispatch.md`'s `## Degraded Mode` fires." → after: "…before the Degraded Mode trigger in `claude-fo-dispatch.md` fires (body in `Skill(skill="spacedock:fo-dispatch-recovery")`)."
- `skills/using-legacy-claude-team/SKILL.md:49` — before: "**Fall back to Degraded Mode** per `## Degraded Mode` in `claude-fo-dispatch.md`." → after: "**Fall back to Degraded Mode** per its trigger in `claude-fo-dispatch.md` — first action there, body via `Skill(skill="spacedock:fo-dispatch-recovery")`."
- `skills/first-officer/references/claude-first-officer-runtime.md:7` — before: "…the Awaiting-Completion idle guardrail, Degraded Mode, the Context-Budget probe, and the Event-Loop reconcile sweep + Backstop — live in `references/claude-fo-dispatch.md`…" → after: "…the Awaiting-Completion idle guardrail, the Degraded-Mode/break-glass/budget-failure trigger lines, the Context-Budget probe, and the Event-Loop reconcile sweep + Backstop — live in `references/claude-fo-dispatch.md` … (the exception bodies behind those triggers load at failure time via `Skill(skill="spacedock:fo-dispatch-recovery")`)."
- `claude-fo-dispatch.md:3` intro — "the idle and degraded-mode guardrails" → "the idle guardrail and the failure-recovery trigger lines".
- `fo-dispatch-core.md:133` ("use the adapter's Break-Glass Manual Dispatch template") — deliberately NOT edited: the host-neutral core must not name a Claude-only skill, and the pointer resolves transitively — the adapter's resident trigger line IS the adapter's break-glass entry (first action + skill load), so "the adapter's template" still reaches the template through it.
- `skills/using-legacy-claude-team/SKILL.md:50` and `:52` — checked and valid as-is: both mention Degraded Mode as a state ("cannot be entered" / "is entered"), not as a pointer at the moved section body; no re-point needed.

Blast radius checked clean at review: beyond the sites listed above, no other contract or test file references the moved section bodies.

### Merge-order dependency (recorded for the FO, which owns enforcement)

Merge order is `wc` → `jh` → this entity (41cfak9bgw). Consequences honored in this body: Diff 1 overlaps jh's break-glass demotion, so the implementation re-derives Diff 1 (and the moved break-glass body) against jh's landed text; Diff 3's bracket note does not quote the canonical enum's values because wc amends the enum; the pre-split baseline captures (equivalence protocol below) and the AC-1 measurement run AFTER jh lands — jh grows the moved span, which improves AC-1's margin.

**Doc diff (`docs/runtime-support.md:23`, user-visible on the docs site)** — two edits to the discriminator paragraph. First, the lead clause accommodates the third case — before: "A deferred module realizes as one of two shapes keyed on WHERE its host binding lives, not on whether it loads an adapter section alongside at the trigger." → after: "A deferred module realizes as a core reference file or a non-user-invocable skill, keyed on WHERE its host binding lives, not on whether it loads an adapter section alongside at the trigger." Second, append:

> A failure-triggered exception body whose trigger lives inside a host adapter's deferred reference realizes as a non-user-invocable skill too: the adapter keeps a resident trigger line that carries the first action and names the skill (`spacedock:fo-dispatch-recovery` — Degraded Mode, break-glass manual dispatch, budget-fail/dead-ensign handling), so the body loads only when its failure actually fires; the trigger line, not the boot core, is the namer.

### Test wiring

- **contractlint** (`internal/contractlint/boot_resident_closure_test.go`): add `skills/fo-dispatch-recovery/SKILL.md` to `deferredSkillCores` with anchors `## Degraded Mode`, `## Break-Glass Manual Dispatch`, `## Context Budget Failure and Dead Ensign Handling`; add `fo-dispatch-recovery` to `lazyLoadSkills`; add a walked deferred-module-bodies list containing `claude-fo-dispatch.md` so in-module `spacedock:` trigger tokens are stat-checked by the same extraction+os.Stat oracle (this retroactively closure-checks the existing `using-legacy-claude-team` token there, uncovered today). Extend `TestDeferredSkillProsePointersResolve`'s walk to that same deferred-module-bodies list — today it walks only `deferredSkillCores` files, so without the extension the trigger lines in `claude-fo-dispatch.md` would go unscanned. The extension is sound: trigger lines resolve because they carry the `Skill(skill="spacedock:fo-dispatch-recovery")` token on the same line, and the resident `## Degraded Mode (trigger)` heading is not an exact match for the watched `Degraded Mode` anchor, so it does not false-define the moved section.
- **ensigncycle greet guard** (`internal/ensigncycle/shallow_boot_measure_test.go:16`): `deferredFOSkillNames` += `"fo-dispatch-recovery"` so a pre-greet load reds; the existing RED-control machinery already proves the oracle can fail.
- **Live scenarios** (Claude-only — NOT `sharedRuntimeScenarios`, which fan out to the Codex runner; these flows are Claude-host-specific):
  - `degraded-bare` (transport decision: HEADLESS): the `/spacedock bare` trigger rides in the initial `-p` prompt — a mid-run captain turn is NOT injectable through the headless runner, so the mid-run variant would need the `ptyLiveDriver` (tmux send-keys nudge precedent, `pty_live_driver_test.go:426-482`) plus new detect-completion-then-send orchestration; that is recorded as the upgrade path, not shipped here. Headless observables: (i) `Skill` invocation with arg `spacedock:fo-dispatch-recovery` after the trigger, (ii) the verbatim captain report sentence ("Falling back to bare mode for the remainder of this session due to infrastructure failure. …"), (iii) every post-trigger `Agent()` lacks `name` and `run_in_background`. The cooperative-shutdown-sweep observable (a `shutdown_request` to a pre-trigger worker) is dropped with this transport — it needs the pty mid-run variant, since a boot-time trigger precedes any worker.
  - `break-glass-shim`: a PATH-shim `spacedock` wrapper that delegates to the real binary except `dispatch build` → stderr + exit 1 (mechanism precedent: the shallow-boot scenario's stub `gh` via `withStubPATH` — interface `claude_live_runner_test.go:82-88`, impl `:201-208`, use `:327`). Stream oracle: (i) a captain-facing helper-failure report before any `Agent()`, (ii) `Skill` arg `spacedock:fo-dispatch-recovery`, (iii) an `Agent()` call with `run_in_background=true`, a `{worker_key}-{slug}-{stage}` name, and a prompt carrying `Skill(skill="spacedock:ensign")` plus an inline `### Stage definition`.
  - **Equivalence protocol (original, not taken — kept visible as the strict path):** run both scenarios once against pre-split `main` — AFTER `jh` lands (see the merge-order dependency) — to capture baseline streams and prove the harness (shim + oracle) against the resident-body contract; check the streams in as fixtures under `internal/ensigncycle/testdata/`; offline unit oracles drive the fixtures with RED controls (a mutated fixture that keeps named/background dispatch after the bare trigger; one that hand-assembles a dispatch with no captain report). Post-split live runs must satisfy the same observables PLUS the skill-load assertion. This is the mechanism-validation-first ordering: the riskiest claim — a live FO at failure time actually follows a trigger-line skill load — is exercised end-to-end before/immediately after the contract edit, not asserted.
  - **Captain decision (2026-07-02, RE-SCOPE):** the pre-split baseline-capture requirement above is DOWNGRADED. Rationale: the new harness (the shim + the two oracles) has never run in CI against the OLD (pre-split) contract, so a pre-split baseline run exercises a harness/contract pairing that can never recur once the split lands — it would prove the harness once, at the same live-credential cost the post-split proof also needs; and in practice the implementation sandbox's one available live credential (`~/.claude/benchmark-token`) died with a 401 after a single call (see the slash-command-interception finding below), so the pre-split capture could not be completed regardless. In its place: post-split live proof via the PR's claude-live lane is the sole required live evidence for AC-2/AC-3 (no pre-split reference stream required), and the 9 offline oracle unit tests (7 RED controls) built in the implementation cycle stand as the oracle-falsifiability evidence — they prove `assertDegradedBareObservables`/`assertBreakGlassObservables` both pass a well-formed stream and fail on each of the seven ways a real run could go wrong, independent of any live capture.

### Implementation finding: `-p` prompt slash-command interception (fixed)

A live probe (implementation stage) proved that `claude -p "/spacedock bare\n..."` is intercepted by the Claude Code CLI's own slash-command dispatcher BEFORE the FO contract ever sees the text — the run returns an immediate `Unknown command: /spacedock` final message with no tool calls, never reaching the FO contract at all. This is a harness-level hazard for ANY scenario (present or future) that injects a captain-command-shaped trigger as the literal first characters of a `-p` prompt — not specific to this task. Fix applied: `degradedBarePrompt()` (`internal/ensigncycle/dispatch_recovery_fixtures_test.go`) opens with `"The captain has issued the command: /spacedock bare"` rather than a bare `"/spacedock bare"` line, so the trigger text is no longer the first four characters of the prompt. Future scenario authors driving a captain slash-command through the headless `-p` transport should apply the same prefix pattern.

### Spike determination

No pre-design spike needed — every mechanism the design rests on is shipped and proven: (1) non-user-invocable `Skill()` loading — five shipped skills, live-validated in #457's greet oracle work (commit 59251f18); (2) PATH-stub binary injection into the live FO subprocess — the shallow-boot stub `gh` (`claude_live_runner_test.go:82-88, :201-208, :327`); (3) stream skill-argument extraction — `journeymetrics` `ClaudeTurn.SkillNames` (#457); (4) the contractlint closure/anchor walkers being extended. The one new harness piece (the failing-`dispatch build` shim) is validated by its offline oracle unit tests with RED controls, per the Captain decision (2026-07-02, RE-SCOPE) above — the pre-split baseline run originally planned as the smallest end-to-end exercise is no longer required; the shim's live proof rides the POST-split claude-live run instead.

## Acceptance criteria

- **AC-1 (value-measuring, the end the entity exists for).** `claude-fo-dispatch.md`'s resident size vs `origin/main` drops by ≥1,200 tokens measured (expected ~1,300), byte proxy ≤ −5,000 B (expected ~ −5,310: moved spans 7,820 B minus measured resident replacements 444 + ~715 + ~1,350 B — thin ~310 B headroom over the floor as measured today, but the baselines are taken after `jh` lands, which grows the moved span and improves the margin; see the merge-order dependency). The baseline is independent and can move the wrong way (the file can grow), cf. `trim-dispatch-adapter-prose` AC-1. **Tested:** `git show origin/main:skills/first-officer/references/claude-fo-dispatch.md | wc -c` vs `wc -c` on the merged file (reproducible on-disk evidence, commands + output in the stage report) — measured at −5,205 B, clearing the floor. The honest plugin-wide sum (resident file deltas + the new SKILL.md size + the registry row) is reported with it, so the resident-vs-deferred trade is on the record. **Captain decision (2026-07-02, RE-SCOPE):** the token-delta half of this AC (measured via the Anthropic count-tokens API) is DESCOPED — the implementation sandbox had no reachable API credential — because the byte proxy above was designed as an independently sufficient criterion and is met with margin, so it stands alone as AC-1's passing evidence. A bytes/4 approximation (~1,300 tok) is recorded as the estimate in place of the API measurement.
- **AC-2 (behavioral equivalence, degraded mode).** Post-split, the `/spacedock bare` trigger produces the same observables the pre-split contract mandates — the verbatim captain report sentence and bare-shape `Agent()` calls (no `name`, no `run_in_background`) for every subsequent dispatch — plus the `spacedock:fo-dispatch-recovery` skill load. (The cooperative-shutdown-sweep observable is out of this AC's oracle: the shipped headless transport triggers at boot, before any worker exists; the pty mid-run variant that could exercise it is the recorded upgrade path.) **Tested:** the `degraded-bare` live scenario (claude-live lane; the merge gate, since this touches `skills/**`) run POST-split, plus its offline oracle (`assertDegradedBareObservables`) and RED controls built during implementation. **Captain decision (2026-07-02, RE-SCOPE):** the pre-split baseline capture is no longer required as the equivalence reference (see the Test wiring re-scope note) — the offline oracle's RED-control coverage stands as the falsifiability evidence, and the post-split claude-live run is the sole live proof. No prose-grep: the oracle keys on stream tool-call structure and the emitted report text, not on contract wording.
- **AC-3 (behavioral equivalence, break-glass).** Post-split, a non-zero `spacedock dispatch build` produces: captain-facing helper-failure report first, the skill load, then a break-glass-shaped `Agent()` (named background worker, `Skill(skill="spacedock:ensign")` first action, inline stage definition). **Tested:** the `break-glass-shim` live scenario run POST-split (claude-live lane), plus its offline oracle (`assertBreakGlassObservables`) and RED controls built during implementation. **Captain decision (2026-07-02, RE-SCOPE):** same re-scope as AC-2 — no pre-split baseline capture required; the offline oracle's RED-control coverage plus the post-split live run are the accepted evidence.
- **AC-4 (structural closure).** Every deferred load point named by the boot-resident bodies AND by `claude-fo-dispatch.md` resolves on disk (including `spacedock:fo-dispatch-recovery`); the skill carries its three section anchors; no surviving contract file points at a moved section in its old home (the `using-legacy-claude-team` re-points land); a pre-greet load of the new skill reds the greet guard. **Tested:** `go test ./internal/contractlint ./internal/ensigncycle` offline — `TestBootResidentDeferredLoadPointsResolve` (+ the new deferred-module-bodies walk), `TestDeferredSkillCoresResolveAndCarryCeremony`, `TestDeferredSkillProsePointersResolve`, the fold test, and the extended `deferredFOSkillNames` guard, each with its existing/added RED control.
- **AC-5 (mechanism, serves AC-1).** The `docs/runtime-support.md` discriminator covers the failure-triggered case per the doc diff above, and the `## Deferred load points` registry row ships. **Tested:** the applied diffs are on-disk state AC-4's closure and fold tests bind to (the registry row is what makes the sole-namer test pass); counts only paired with AC-1.

## Test plan

1. **Offline (no model spend, runs per-PR):** the contractlint additions and greet-guard extension in AC-4, plus fixture-driven unit oracles for the two scenarios (RED controls included). Cost: small Go test additions to existing walkers; `go build ./... && go test ./...` green.
2. **Live (claude-live lane, maintainer-approved environment; gates the merge since `skills/**` changes):** the two Claude-only scenarios in AC-2/AC-3, run POST-split. Cost: two multi-minute live runs per matrix model. **Captain decision (2026-07-02, RE-SCOPE):** the pre-split baseline capture is no longer required (see the Test wiring re-scope note) — it is not a cost this plan carries.
3. **Measurement (AC-1):** one-off git/wc byte comparison recorded in the implementation stage report (the count-tokens API half is descoped per the AC-1 captain decision; a bytes/4 estimate is recorded in its place).
4. **Order:** build the shim + scenario harness and the offline oracle/RED-control tests FIRST (mechanism validation), then the contract split, then contractlint wiring, then the AC-1 byte measurement and the post-split live runs.

## Stage Report: ideation

- DONE: The split honors the named reliability risk: each resident trigger line carries the first action (not a bare pointer), pre-invocation guards stay resident (context-budget probe invocation, Awaiting Completion), and the design states per exception body why its load point is safe at failure time.
  Diffs 1-3 each carry the first action on the resident line; `## Awaiting Completion` and the probe/fail-safe/model-enum lines stay resident (reuse-conditions 0 and 4 run on the happy path); per-body safety rationale in "Why each load point is safe at failure time".
- DONE: ACs include a measured token delta for claude-fo-dispatch.md's resident size and a behavioral-equivalence check for degraded-mode and break-glass flows post-split, each paired with how it is tested (contractlint closure, live or fixture evidence — no prose-grep as proof).
  AC-1 (≥1,200 tok / ≤ −5,000 B vs origin/main, git+count-tokens evidence); AC-2/AC-3 (pre-split-baselined live scenarios + checked-in fixture oracles with RED controls, keyed on tool-call structure, not wording); AC-4 (contractlint closure + greet guard).
- DONE: The #457 non-user-invocable-skill pattern is followed exactly (name the shipped precedent files) and the deferred-load-points registry in the shared core is updated consistently, with concrete before/after prose diffs in the task body.
  Precedents named: skills/fo-status-viewer/SKILL.md, skills/fo-write-core/SKILL.md (59251f18/#457), plus present-gate, feedback-rejection-flow, using-legacy-claude-team; registry gains an additive fifth row (before/after quoted); Diffs 1-3 plus four re-point diffs and the runtime-support.md doc diff are in the body verbatim.

### Summary

Ideation designs the split as ONE non-user-invocable skill (`fo-dispatch-recovery`, three verbatim section anchors) rather than three, because every SKILL.md description is resident in every session's skill list and the three failure paths share one trigger family. Measured the spans (7,820 B, ~1.9k tok; expected resident drop ~5.6 KB / ~1.5k tok), resolved the model-enum constraint (fo-dispatch-core.md:93 pins the enum resident for happy-path reuse-condition-4), and recorded a "no spike needed" determination on four shipped mechanisms, with the one new harness piece (the failing-`dispatch build` shim) validated first via pre-split baseline captures that double as the equivalence reference.

## Stage Report: ideation (cycle 2)

Staff review returned six material findings; all applied as body reconciliations (no architecture change).

- DONE: M1 — AC-1 arithmetic corrected to measured replacements (444 / ~715 / ~1,350 B), expected net ~ −5,310 B / ~1,300 tok, floors unchanged with thin headroom noted.
  AC-1 text and Diffs 1-3 byte figures updated; margin note points at the merge-order dependency.
- DONE: M2 — `fo-dispatch-core.md:133` added to the re-point list as a deliberate non-edit with the transitive-resolution rationale (host-neutral core must not name a Claude-only skill; the adapter's trigger line is the entry).
- DONE: M3 — the "do not route `SendMessage` to any pre-trip worker name" clause added to the Diff 2 trigger line (recorded ~55 B spend against AC-1), and the safety rationale now cites fo-dispatch-core.md's host-neutral degraded effects as the resident backstop.
- DONE: M4 — degraded-bare transport decided: HEADLESS (trigger in the initial `-p` prompt), sweep observable dropped from AC-2's oracle, pty mid-run variant (`pty_live_driver_test.go:426-482` send-keys precedent) recorded as the upgrade path; `withStubPATH` citations fixed to `:82-88, :201-208, :327`.
- DONE: M5 — merge-order dependency section added (wc → jh → this entity): Diff 1 re-derived against jh's landed text, enum values un-quoted everywhere (wc amends the enum), baselines and AC-1 measurement after jh lands.
- DONE: M6 — the vacuous scanner sentence replaced with an explicit extension of `TestDeferredSkillProsePointersResolve`'s walk to the deferred-module-bodies list, with the soundness argument (same-line Skill token; `## Degraded Mode (trigger)` does not false-define).
- DONE: Polish — using-legacy-claude-team `:50`/`:52` recorded as checked-and-valid, the runtime-support.md lead clause diff now accommodates the third shape, and the clean-blast-radius record is in the body.

### Summary

All six staff findings applied in the task body: corrected AC-1 measurement arithmetic, the fo-dispatch-core transitive-pointer rationale, the pre-trip-names trigger clause with its byte cost on the record, a headless transport decision for degraded-bare with the pty upgrade path named, the wc → jh → 41 merge-order dependency, and a non-vacuous prose-pointer-walk extension. Scope held to task-body edits only.

## Stage Report: implementation

- DONE: The skills/fo-dispatch-recovery/SKILL.md ships with user-invocable: false, the three verbatim section bodies, and the three resident trigger lines carrying their first actions exactly as the cycle-2 body records (including the pre-trip-names decision you recorded); the four re-points plus fo-dispatch-core:133 land; the registry row is additive.
  New skill at skills/fo-dispatch-recovery/SKILL.md carries `## Degraded Mode`, `## Break-Glass Manual Dispatch`, `## Context Budget Failure and Dead Ensign Handling` verbatim. claude-fo-dispatch.md now carries the three resident trigger lines (Diffs 1-3) each with FIRST ACTION inline, including the M3 "do not route SendMessage to any pre-trip worker name" clause on the Degraded Mode trigger. Re-points landed: using-legacy-claude-team/SKILL.md:25, :49; claude-first-officer-runtime.md:7; claude-fo-dispatch.md:3 intro. fo-dispatch-core.md:133 checked and deliberately left unedited per the entity's transitive-resolution rationale. Registry row appended additively to first-officer-shared-core.md `## Deferred load points`. docs/runtime-support.md doc diff applied (both discriminator-paragraph edits). Commit: see git log on this branch.

- DONE: Baselines and diffs derive from the POST-jh landed text (jh merged as #464; worktree base 8be4cc44 includes it): re-derived Diff 1 against the current (post-jh) Break-Glass Manual Dispatch block, kept the enum values un-quoted per the M5 note (the resident `## Context Budget` paragraph is the landed line-146 text verbatim, unedited), and measured AC-1 with the honest plugin-wide sum.
  AC-1 byte measurement (reproducible): `git show origin/main:skills/first-officer/references/claude-fo-dispatch.md | wc -c` = 24380; `wc -c` on the merged file = 19175; delta = -5205 B, clearing the ≤ -5000 B floor. Honest plugin-wide sum across every touched skills/ file: claude-fo-dispatch.md -5205, first-officer-shared-core.md +265 (the registry row), claude-first-officer-runtime.md +163, using-legacy-claude-team/SKILL.md +140, new fo-dispatch-recovery/SKILL.md +7685 (net new) = +3048 B net across skills/ — the deliberate resident-vs-deferred trade (the always-loaded file shrinks 5205 B; the failure-time-only skill absorbs it plus growth). docs/runtime-support.md (site docs, not plugin-resident) +494 B, reported separately.

- FAILED: The corrected AC-1 token floor (≥1,200 tok vs origin/main) is measured via the Anthropic count-tokens API and recorded.
  No ANTHROPIC_API_KEY is set in this sandbox and no reachable authenticated count-tokens credential was available (network reaches api.anthropic.com but returns 401/405 without a key). The byte-based floor (AC-1's second, independently-sufficient criterion) is met with margin (-5205 B vs the -5000 B floor); the token count was not obtained and needs a credentialed environment to close out. Escalated to team-lead separately.
  RESOLVED (captain re-scope, 2026-07-02): the count-tokens-API measurement is DESCOPED — the byte floor stands alone as AC-1's sufficient passing evidence (met at -5205 B vs the -5000 B floor). A bytes/4 approximation (~1,300 tok) is recorded as the estimate in AC-1's text in place of the API measurement. No further action needed.

- DONE: The contractlint wiring lands per the cycle-2 body: deferredSkillCores + lazyLoadSkills + the deferred-module-bodies walk with the prose-pointer extension (non-vacuous — trigger tokens stat-checked and prose-walked in claude-fo-dispatch.md), the greet guard gains fo-dispatch-recovery.
  internal/contractlint/boot_resident_closure_test.go: `skills/fo-dispatch-recovery/SKILL.md` added to `deferredSkillCores` with its three anchors; `fo-dispatch-recovery` added to `lazyLoadSkills`; new `deferredModuleBodies` var (`claude-fo-dispatch.md`) walked by both `TestBootResidentDeferredLoadPointsResolve` (extraction+os.Stat closure — retroactively closure-checks the pre-existing `using-legacy-claude-team` token there) and `TestDeferredSkillProsePointersResolve` (prose-pointer scanner). internal/ensigncycle/shallow_boot_measure_test.go: `deferredFOSkillNames` += `fo-dispatch-recovery`. `go test ./internal/contractlint ./internal/ensigncycle` and the full `go test ./...`: all green (verified twice, including after the live-tagged additions below). `go build ./...`, `go vet ./...`, `go build -tags live ./...` all clean.

- FAILED: The two live scenarios (degraded-bare HEADLESS transport, break-glass-shim) plus their checked-in fixture oracles with RED controls are built and baseline-proven before the contract split lands.
  Built and offline-verified: `internal/ensigncycle/dispatch_recovery_assert_test.go` (assertDegradedBareObservables / assertBreakGlassObservables, decoding tool_use input key-presence — not just value — off the real stream-json shape) plus `dispatch_recovery_assert_unit_test.go`, 9 unit tests all PASS including 7 RED controls (missing skill load, missing captain report, a still-named Agent() after trip, report-after-Agent, wrong Agent shape, missing stage-def-in-prompt). Built `dispatch_recovery_fixtures_test.go` (fixtures/prompts, default-tagged) and `dispatch_recovery_live_test.go` (`//go:build live` runners + PATH-shim `spacedock` wrapper for break-glass, delegating to the real binary by absolute path). `go build -tags live ./...` is clean. Live baseline-proof itself is NOT complete: this sandbox unexpectedly had a working `~/.claude/benchmark-token`; the FIRST live call (degraded-bare, original prompt) authenticated and ran, surfacing a real bug — a `-p` prompt starting with the literal string `/spacedock` is intercepted by the Claude Code CLI's own slash-command dispatcher ("Unknown command: /spacedock") before the FO contract ever sees it — which I fixed by prefixing the trigger with "The captain has issued the command:" so it is no longer the first four characters of the prompt. Every subsequent live call (3 more, both scenarios, one after the prompt fix) returned a 401 Invalid authentication credentials, consistent with a single-use or very-short-TTL benchmark token now exhausted. I stopped retrying rather than keep hammering a failing auth path. Net effect: the harness/mechanism is proven runnable and one real design bug was caught and fixed, but no scenario has a genuine baseline-proof run, and the required pre-split baseline captures (equivalence protocol) were never attempted (blocked behind the same exhausted credential). This blocks AC-2/AC-3 and must be re-run in the maintainer-approved claude-live lane with a fresh credential before merge.
  RESOLVED (captain re-scope, 2026-07-02): the pre-split baseline-capture requirement is DOWNGRADED — see the "Captain decision" note under the Test wiring equivalence protocol and AC-2/AC-3 in the task body. Rationale on record there: the harness never ran in CI against the pre-split contract anyway (so a pre-split capture proves a pairing that cannot recur), and the sandbox credential was exhausted regardless. Accepted evidence going forward: the 9 offline oracle unit tests (7 RED controls) built here, plus a POST-split live run through the PR's claude-live lane (no pre-split reference stream required). The slash-command-interception finding and its fix remain live-relevant and are called out as their own subsection in the task body for future scenario authors. No further pre-split work needed; AC-2/AC-3 close out on the post-split claude-live run alone.

### Summary

Landed the full AC-1 contract split (new `skills/fo-dispatch-recovery/SKILL.md`, Diffs 1-3 on `claude-fo-dispatch.md`, the four re-points, the additive registry row, and the `docs/runtime-support.md` doc diff) and the AC-4 contractlint/greet-guard wiring, all offline-verified green (`go build`, `go vet`, `go test ./...`, `go build -tags live ./...`). AC-1's byte floor is met with reproducible evidence (-5205 B, honest plugin-wide sum +3048 B disclosed); the token-count half of AC-1 and the full live baseline-proof for AC-2/AC-3 could not be completed in this sandbox — the token API has no reachable credential, and the one available live credential (`~/.claude/benchmark-token`) authenticated exactly once (catching and fixing a real slash-command-interception bug in the degraded-bare prompt) before returning 401 on every later call. The live scenario harness (oracles with 9 passing offline unit tests + RED controls, fixtures, and `//go:build live` runners) is built, compiles clean, and is ready to run to completion the moment a fresh live credential is available.

**Captain re-scope (2026-07-02), post-report:** both FAILED items above are RESOLVED by captain decision, recorded in the task body (AC-1, the Test wiring equivalence-protocol note, and AC-2/AC-3). AC-1 closes on the byte floor alone (count-tokens API descoped, bytes/4 estimate recorded). AC-2/AC-3 close on a POST-split claude-live run plus the 9 offline oracle unit tests (7 RED controls) as falsifiability evidence — no pre-split baseline capture required. The slash-command-interception finding (a `-p` prompt starting with `/spacedock` is eaten by the CLI's own slash-command dispatcher) is promoted to its own task-body subsection under Test wiring since it is a reusable finding for future scenario authors, not just an implementation-cycle footnote.

## Stage Report: validation

- DONE: The offline evidence is reproduced, not trusted: run the full contractlint suite (the deferred-module-bodies closure walk over claude-fo-dispatch.md, the three fo-dispatch-recovery anchors, the greet-guard extension) and the 9 oracle unit tests, then re-apply at least two of the 7 RED controls yourself (e.g. strip the skill-load assertion, keep a named Agent() after the bare trigger) and watch them fail; revert. Full go test ./... green.
  Reproduced at ba122fb9 (origin/main = worktree base 8be4cc44): the contractlint closure/anchor/prose-pointer/fold tests PASS, the 9 oracle unit tests PASS, full `go test ./...` exit 0 (15 packages ok, no FAIL lines); `go vet ./...` and `go build -tags live ./...` clean. Adversarial RED 1: stripped the `sawRecoverySkill` assertion from `assertDegradedBareObservables` → `TestAssertDegradedBareObservablesCatchesMissingSkillLoad` FAILED as required. Adversarial RED 2: added `name:"kept-worker-name"` to an Agent() call in `degradedBareGoodStream` → `TestAssertDegradedBareObservablesOffline` FAILED with the exact bare-mode diagnostic (`name-present=true`). Both reverted; `git status --porcelain` empty; full suite re-run green post-revert.

- DONE: The coverage/AC-1 audit: the three moved section bodies are verbatim vs origin/main (diff them), each resident trigger line carries its first action including the pre-trip-names clause, the four re-points + registry row + runtime-support.md doc diff are applied, and AC-1's byte delta is re-measured vs origin/main clearing the -5,000 B floor with the honest plugin-wide sum disclosed (count-tokens half descoped by captain decision — verify the descope is recorded, not silent).
  Diffed: `## Degraded Mode` (origin/main:93-124) and the moved Context-Budget spans (:140-144, :148-155) are byte-identical in the skill; the Break-Glass Agent() template block is byte-identical, with one spec-mandated divergence in its closing note — the quoted enum values replaced by a pointer to the resident `## Context Budget` section (the M5 single-occurrence rule; the enum now appears exactly once across both files, grep-verified). All three resident trigger lines carry their FIRST ACTION inline; the Degraded Mode trigger carries the "do not route `SendMessage` to any pre-trip worker name" clause verbatim. Re-points verified on disk: using-legacy-claude-team/SKILL.md:25/:49, claude-first-officer-runtime.md:7, claude-fo-dispatch.md:3 intro; registry row at first-officer-shared-core.md:43; both runtime-support.md discriminator edits (:23 lead clause, :25 appended paragraph); fo-dispatch-core.md:134 confirmed deliberately unedited (transitive resolution holds — the trigger line IS the adapter's template entry). AC-1 re-measured independently: `git show origin/main:...claude-fo-dispatch.md | wc -c` = 24380 vs merged 19175 = -5205 B, clearing the -5000 B floor. Honest plugin-wide sum reproduced per file: -5205 +265 (shared-core registry row) +163 (runtime) +140 (legacy-team) +7685 (new SKILL.md) = +3048 B across skills/; docs/runtime-support.md +494 B separate. The count-tokens descope is recorded, not silent: AC-1's captain-decision text and the implementation report's RESOLVED note both carry it, with the bytes/4 ≈ 1,300 tok estimate on record.

- DONE: AC-2/AC-3 per the captain's re-scope: recorded as pending the merge gate's claude-live run (both matrix variants) — not SKIPPED silently; verify the degraded-bare scenario prompt carries the slash-command-interception fix (trigger not at prompt start); close with PASSED/REJECTED per AC.
  Interception fix verified: `degradedBarePrompt()` opens with "The captain has issued the command: /spacedock bare" — the trigger is not the prompt's leading characters, with the live-probe rationale in the doc comment (dispatch_recovery_fixtures_test.go). Live runners are `//go:build live` and Claude-only (not `sharedRuntimeScenarios`); the break-glass shim delegates to the real binary by absolute path. Per-AC close: AC-2 PASSED on the offline evidence (oracle + RED controls reproduced above), live half PENDING the merge gate's claude-live run on both matrix variants — the recorded sole live evidence per the captain re-scope. AC-3 identical: PASSED offline, live half PENDING the same gate. Neither AC is SKIPPED; the merge gate is the recorded closer.

### Summary

Recommendation: PASSED — contingent on the merge gate's claude-live lane running the `degraded-bare` and `break-glass-shim` scenarios on both matrix variants, the captain-re-scoped sole live evidence closing AC-2/AC-3. AC-1 (byte floor -5205 B, plugin-wide sum disclosed, descope on record), AC-4 (closure/anchor/greet-guard tests green, wiring verified on disk), and AC-5 (doc diff + registry row applied) all verified with independently reproduced evidence; two RED controls were re-applied adversarially and went red, so the oracles can fail. One minor task-body inconsistency noted, no action needed: the "Skill sections" bullet lists the forward-family context-window note as moving to the skill, while the authoritative measured Diff 3 keeps the mapping paragraph (which contains that note) resident — the implementation correctly follows Diff 3, whose reuse-condition-4 happy-path rationale is load-bearing.

### Feedback Cycles

**Cycle 1 (2026-07-02, detached adversarial audit, pre-merge).** Audit `audit-exception-paths-split` on a throwaway clone: no material refutation — registry-row, skill-anchor, and greet-guard mutants all red as claimed; both un-validated RED controls bite; key-presence decode verified non-tautological against a real captured stream; verbatim-move verified byte-identical mechanically. Two offline-coverage pins routed to implementation: (T1, medium) the degraded-bare oracle's `degradedModeCaptainReportPrefix` constant is never tied offline to the skill's blockquote — a paraphrase in fo-dispatch-recovery/SKILL.md reds nothing until a live run; pin via a contractlint structural check binding the two independent values (per the read-quarantine: contractlint is the only place tests may read instruction files). (T2, low) using-legacy-claude-team/SKILL.md is outside the prose-pointer walk; append it to the walk targets so its Degraded-Mode pointer lines are closure-checked. Accepted without action, on the record: the trigger-line FIRST ACTION prose is pinned only by the live oracle's all-bare-Agent assertion (behavioral coverage, not prose); `user-invocable: false` unpinned (menu-noise materiality only).

## Stage Report: implementation (cycle 2)

- DONE: T1 — bind `degradedModeCaptainReportPrefix` to `fo-dispatch-recovery/SKILL.md`'s actual Captain Report Template text via a contractlint structural check.
  `internal/contractlint/dispatch_recovery_value_binding_test.go`: `TestDegradedModeCaptainReportPrefixBindsSkillBlockquote` extracts the Go string-literal constant from `internal/ensigncycle/dispatch_recovery_assert_test.go` via regex and asserts it appears verbatim in the skill body. RED verified live: paraphrased the skill blockquote's opening ("Falling back to bare mode..." -> "Reverting to sequential dispatch...") locally, re-ran — FAILED with the expected divergence message; reverted (`git diff` clean), re-ran — PASS. Framed explicitly in the doc comment as a value-binding check, not the banned CODE-BOUND-AS-BEHAVIOR-SUBSTITUTE (boundary_guard_test.go's package doc): it never claims the skill's prose proves AC-2's behavior — the live scenario + offline oracle do that — it only prevents the oracle's match constant and the skill's actual text from silently drifting apart.

- DONE: T2 — closure-check using-legacy-claude-team/SKILL.md's two re-pointed Degraded-Mode pointer lines without reopening the whole-file false-positive.
  First attempt (whole-file walk via `additionalProsePointerTargets` in `TestDeferredSkillProsePointersResolve`) was built, run, and REJECTED: it correctly resolved the two re-pointed lines (:25, :49) but also reds on :50 and :52 ("Degraded Mode cannot be entered" / "Degraded Mode is entered") — prose the entity's own design already reviewed as "checked and valid as-is" (Degraded Mode named as a STATE, not a pointer). The generic `referenceProsePointerDanglers` scanner has no way to distinguish the two; teaching it to would be new scanner logic, and rewording :50/:52 would be a prose edit — both outside "these two pins only," so reverted (`git diff --stat` empty on `boot_resident_closure_test.go` before commit).
  Landed instead: `TestUsingLegacyClaudeTeamDegradedModePointersNameRealAnchor` locates the two known pointer lines by a stable marker phrase present in both the OLD bare-pointer text and the CURRENT Skill-token text ("**Failure handling:**", "**Fall back to Degraded Mode**") — not by token presence, so a regression to the old bare form (which drops the token) is still caught, not silently skipped. Each marked line must carry `Skill(skill="spacedock:fo-dispatch-recovery")` and name a section that is a real anchor in `fo-dispatch-recovery/SKILL.md`. RED verified with the audit's own mutant: restored the OLD bare pointer text at :25 (`before \`claude-fo-dispatch.md\`'s \`## Degraded Mode\` fires`, no token) — FAILED with "no longer carries Skill(...) — regressed to a bare pointer with no resolving token," the "token disappears" branch the audit anticipated. Reverted (`git diff` clean), re-ran — PASS.
  **Residual observation (deviation from the audit's suggested mechanism, recorded per direction):** the generic `referenceProsePointerDanglers` scanner cannot distinguish "Degraded Mode" used as a topic/state from "Degraded Mode" used as a pointer at the moved section. This pin sidesteps that gap by scoping to the two known marker lines rather than generalizing the scanner — it closure-checks exactly the two pointers the audit cared about, but does NOT give using-legacy-claude-team/SKILL.md the same blanket closure coverage `TestDeferredSkillProsePointersResolve` gives the `deferredSkillCores`/`deferredModuleBodies` files. A future prose-pointer regression in this file OUTSIDE the two marked lines (a new bare-name mention added elsewhere) would not be caught by any offline test. Generalizing the scanner to carry that distinction (e.g., requiring a verb/preposition context around the section name, or an explicit pointer-vs-topic marker convention) is out of scope for this cycle.

- DONE: Verification and landing.
  `go build ./...`, `go vet ./...`, `go test ./...` (all 15 packages, offline), and `go build -tags live ./...` all clean after both pins landed. Committed on the ensign branch (`spacedock-ensign/dispatch-exception-paths-deferred-module`, commit `d4531bf5`, "contractlint: pin the degraded-mode captain-report constant and using-legacy-claude-team's two Degraded Mode pointers") and pushed, re-triggering PR #466 CI.

### Summary

Both feedback-cycle-1 pins landed as test-only contractlint additions. T1 shipped as specified. T2 deviated from the audit's suggested whole-file-walk mechanism after that mechanism proved to false-positive on two lines the entity's design had already reviewed as non-pointer prose; the landed fix is a narrower, marker-anchored pin that closure-checks exactly the two pointer lines the audit cared about, with the scanner's state-vs-pointer blind spot recorded as a residual observation rather than silently generalized or papered over. Both pins were verified RED against their respective mutants (a paraphrase and the audit's own bare-pointer restoration) before being reverted and committed green.

## Stage Report: validation (cycle 1 re-verification)

Re-verified the cycle-1 pins narrowly in the worktree at d4531bf5, per the scope fence. (1) Suite green as shipped: both pins (`TestDegradedModeCaptainReportPrefixBindsSkillBlockquote`, `TestUsingLegacyClaudeTeamDegradedModePointersNameRealAnchor`) PASS and full `go test ./...` exit 0 (15 packages ok, no FAIL lines). (2) Both mutants reproduced independently: paraphrasing the fo-dispatch-recovery Captain Report blockquote opening ("the remainder of" -> "the rest of") made the T1 value-binding test FAIL with the divergence diagnostic; restoring the old bare pointer at using-legacy-claude-team/SKILL.md:25 (dropping the recovery-skill token) made the T2 marker-anchored test FAIL with the "regressed to a bare pointer with no resolving token" diagnostic. Both reverted; `git status --porcelain` empty; contractlint + ensigncycle re-run green post-revert. (3) Commit d4531bf5 is test-only — one new file, `internal/contractlint/dispatch_recovery_value_binding_test.go` (147 insertions), no contract or skill files touched — and the T2 deviation (marker-anchored pin instead of the audit's whole-file walk, after the walk false-positived on the :50/:52 state-mentions) plus its residual (the generic scanner cannot distinguish state-mentions from pointers, so out-of-marker regressions in this file stay uncovered offline) are both recorded in the cycle-2 implementation report. The validation recommendation stands: PASSED, still contingent on the merge gate's claude-live lane for AC-2/AC-3.
